version: '3.8'

# This docker-compose file reproduces the reverse proxy blocking issue
# described in GitHub issue #120

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: bubblelab-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - '${POSTGRES_PORT}:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - bubblelab-network

  # BubbleLab API
  api:
    build:
      context: ..
      dockerfile: Dockerfile.api
    container_name: bubblelab-api
    expose:
      - '3001'
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - BUBBLE_ENV=${BUBBLE_ENV}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - NODEX_API_URL=${BUBBLELAB_API_URL}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - FIRE_CRAWL_API_KEY=${FIRE_CRAWL_API_KEY}
      - SLACK_TOKEN=${SLACK_TOKEN}
      - SLACK_BOT_TOKEN=${SLACK_BOT_TOKEN}
      - GOOGLE_OAUTH_CLIENT_ID=${GOOGLE_OAUTH_CLIENT_ID}
      - GOOGLE_OAUTH_CLIENT_SECRET=${GOOGLE_OAUTH_CLIENT_SECRET}
      - POSTHOG_API_KEY=${POSTHOG_API_KEY}
      - POSTHOG_HOST=${POSTHOG_HOST}
      - POSTHOG_ENABLED=${POSTHOG_ENABLED}
      - CREDENTIAL_ENCRYPTION_KEY=${CREDENTIAL_ENCRYPTION_KEY}
      - CLERK_SECRET_KEY=${CLERK_SECRET_KEY_BUBBLELAB}
      - CLERK_SECRET_KEY_BUBBLELAB=${CLERK_SECRET_KEY_BUBBLELAB}
      - CLERK_ISSUER_BUBBLELAB_DEV=${CLERK_ISSUER_BUBBLELAB_DEV}
      - CLERK_ISSUER_BUBBLELAB_PROD=${CLERK_ISSUER_BUBBLELAB_PROD}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - bubblelab-network

  # Nginx Reverse Proxy with static files built in
  # Static files are built into the nginx image, no separate service needed
  nginx:
    build:
      context: ..
      dockerfile: deployment/Dockerfile.bubble-studio
      args:
        VITE_API_URL: ${BUBBLELAB_API_URL}
        VITE_CLERK_PUBLISHABLE_KEY: ${CLERK_PUBLISHABLE_KEY}
        VITE_DISABLE_AUTH: ${DISABLE_AUTH:-true}
    container_name: bubblelab-nginx
    ports:
      - '${NGINX_PORT}:80'
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
    depends_on:
      - api
    restart: unless-stopped
    networks:
      - bubblelab-network

networks:
  bubblelab-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
