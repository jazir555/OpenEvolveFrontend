var BE = Object.defineProperty;
var WE = (e, t, r) => t in e ? BE(e, t, { enumerable: !0, configurable: !0, writable: !0, value: r }) : e[t] = r;
var Z = (e, t, r) => WE(e, typeof t != "symbol" ? t + "" : t, r);
import * as xn from "react";
import I, { isValidElement as Cr, useState as de, useEffect as et, useCallback as ot, useMemo as Ya, useRef as Tr, Children as qn, PureComponent as wn, forwardRef as U_, useImperativeHandle as VE, cloneElement as Tt, createElement as z_, useContext as rn, createContext as Kn, Component as B_ } from "react";
import { create as pl } from "zustand";
import { useQueryClient as Xa, useMutation as Dt, useQuery as Hh } from "@tanstack/react-query";
const ge = {
  PLUGIN_NAME: "OpenEvolve BubbleLabs Plugin",
  PLUGIN_VERSION: "1.0.0",
  PLUGIN_DESCRIPTION: "Comprehensive OpenEvolve system integration for BubbleLabs",
  PLUGIN_AUTHOR: "OpenEvolve Team",
  PLUGIN_LICENSE: "MIT",
  // Default Configuration Values
  DEFAULT_EVOLUTION_MODE: "standard",
  DEFAULT_ADVERSARIAL_MODE: "red_blue_team",
  DEFAULT_DECOMPOSITION_STRATEGY: "semantic",
  DEFAULT_MAX_ITERATIONS: 10,
  DEFAULT_POPULATION_SIZE: 20,
  DEFAULT_TEMPERATURE: 0.7,
  DEFAULT_MAX_TOKENS: 2048,
  DEFAULT_MODEL_ID: "gpt-4",
  DEFAULT_API_BASE: "https://api.openai.com/v1",
  DEFAULT_TIMEOUT: 30,
  DEFAULT_MAX_RETRIES: 3,
  DEFAULT_RETRY_DELAY: 1,
  // MDAP/MAKER Defaults
  DEFAULT_MDAP_MAKER_ENABLED: !1,
  DEFAULT_MDAP_MAKER_AUTO_SELECT: !0,
  DEFAULT_MDAP_MAKER_MAX_DEPTH: 5,
  DEFAULT_MDAP_MAKER_K_AHEAD: 3,
  DEFAULT_MDAP_MAKER_RED_FLAGGING: !0,
  DEFAULT_MDAP_MAKER_ADAPTIVE_K: !0,
  DEFAULT_MDAP_MAKER_PROVIDER: "openai",
  DEFAULT_MDAP_MAKER_MODEL: "gpt-4",
  DEFAULT_MDAP_MAKER_KEYWORDS: [
    "critical",
    "important",
    "high priority",
    "mission critical",
    "production",
    "deployment",
    "security",
    "sensitive"
  ],
  // Execution Methods
  EXECUTION_METHODS: ["auto", "evolution", "adversarial", "decomposition", "roma_mdap_maker"],
  DEFAULT_EXECUTION_METHOD: "auto",
  // Strategy Options
  EVOLUTION_STRATEGIES: [
    "standard",
    "genetic_algorithm",
    "quality_diversity",
    "novelty_search",
    "multi_objective",
    "adaptive",
    "hybrid"
  ],
  ADVERSARIAL_STRATEGIES: [
    "red_blue_team",
    "multi_agent",
    "self_play",
    "co_evolution",
    "competitive",
    "cooperative"
  ],
  DECOMPOSITION_STRATEGIES: [
    "semantic",
    "hierarchical",
    "functional",
    "modular",
    "temporal",
    "hybrid"
  ]
}, Rn = {
  initialized: !1,
  metadata: {
    name: ge.PLUGIN_NAME,
    version: ge.PLUGIN_VERSION,
    description: ge.PLUGIN_DESCRIPTION,
    author: ge.PLUGIN_AUTHOR,
    license: ge.PLUGIN_LICENSE
  },
  status: "idle",
  currentExecutionId: null,
  executionHistory: [],
  statistics: [],
  evolutionConfig: {
    // Core Evolution Parameters
    evolutionMode: ge.DEFAULT_EVOLUTION_MODE,
    maxIterations: ge.DEFAULT_MAX_ITERATIONS,
    populationSize: ge.DEFAULT_POPULATION_SIZE,
    temperature: ge.DEFAULT_TEMPERATURE,
    maxTokens: ge.DEFAULT_MAX_TOKENS,
    seed: null,
    earlyStopping: !1,
    convergenceThreshold: 1e-3,
    fitnessFunction: "default",
    selectionPressure: 1,
    mutationRate: 0.1,
    crossoverRate: 0.8,
    elitism: !0,
    diversityMaintenance: !0,
    adaptiveParameters: !1,
    reasoningEffort: "medium",
    language: "python",
    fileSuffix: ".py",
    // Model Configuration Parameters
    apiKey: "",
    apiBase: ge.DEFAULT_API_BASE,
    modelId: ge.DEFAULT_MODEL_ID,
    backupModels: null,
    timeout: ge.DEFAULT_TIMEOUT,
    maxRetries: ge.DEFAULT_MAX_RETRIES,
    retryDelay: ge.DEFAULT_RETRY_DELAY,
    rateLimit: 60,
    concurrentRequests: 5,
    modelRotation: !1,
    topP: 1,
    frequencyPenalty: 0,
    presencePenalty: 0,
    n: 1,
    logitBias: null,
    stopSequences: null,
    logprobs: !1,
    topLogprobs: 0,
    responseFormat: "text",
    // Quality Diversity Parameters
    featureDimensions: null,
    featureBins: 10,
    archiveSize: 100,
    noveltyThreshold: 0.1,
    // Evolutionary MCTS Parameters
    mctsEnabled: !1,
    mctsIterations: 100,
    explorationWeight: 1.4,
    rolloutDepth: 5,
    treeReuse: !0,
    // MDAP/MAKER Integration
    mdapMakerEnabled: ge.DEFAULT_MDAP_MAKER_ENABLED,
    mdapMakerAutoSelect: ge.DEFAULT_MDAP_MAKER_AUTO_SELECT,
    mdapMakerMaxDepth: ge.DEFAULT_MDAP_MAKER_MAX_DEPTH,
    mdapMakerKAhead: ge.DEFAULT_MDAP_MAKER_K_AHEAD,
    mdapMakerRedFlagging: ge.DEFAULT_MDAP_MAKER_RED_FLAGGING,
    mdapMakerAdaptiveK: ge.DEFAULT_MDAP_MAKER_ADAPTIVE_K,
    mdapMakerProvider: ge.DEFAULT_MDAP_MAKER_PROVIDER,
    mdapMakerModel: ge.DEFAULT_MDAP_MAKER_MODEL,
    mdapMakerAutoSelectionKeywords: ge.DEFAULT_MDAP_MAKER_KEYWORDS
  },
  adversarialConfig: {
    // Core Adversarial Parameters
    adversarialMode: ge.DEFAULT_ADVERSARIAL_MODE,
    redTeamSize: 3,
    blueTeamSize: 3,
    evaluatorTeamSize: 1,
    maxRounds: 5,
    critiqueDepth: "medium",
    improvementDepth: "medium",
    evaluationStrictness: "medium",
    contentType: "code",
    requirements: [],
    complianceStandards: [],
    maxContentLength: 5e3,
    minContentLength: 50,
    allowContentGeneration: !0,
    allowContentModification: !0,
    allowStructuralChanges: !0,
    preserveOriginalFunctionality: !0,
    focusAreas: [],
    excludeAreas: [],
    // Team Configuration
    redTeamAggressiveness: 0.7,
    blueTeamCreativity: 0.8,
    evaluatorRigor: 0.9,
    teamDiversity: 0.5,
    collaborationLevel: 0.3,
    // Execution Parameters
    parallelExecution: !1,
    maxParallelTasks: 3,
    timeoutSeconds: 300,
    maxRetries: 3,
    fallbackStrategy: "default",
    // Quality Metrics
    qualityThreshold: 0.7,
    improvementThreshold: 0.2,
    acceptanceThreshold: 0.8,
    metricWeights: {},
    // MDAP/MAKER Integration
    mdapMakerEnabled: ge.DEFAULT_MDAP_MAKER_ENABLED,
    mdapMakerAutoSelect: ge.DEFAULT_MDAP_MAKER_AUTO_SELECT,
    mdapMakerMaxDepth: ge.DEFAULT_MDAP_MAKER_MAX_DEPTH,
    mdapMakerKAhead: ge.DEFAULT_MDAP_MAKER_K_AHEAD,
    mdapMakerRedFlagging: ge.DEFAULT_MDAP_MAKER_RED_FLAGGING,
    mdapMakerAdaptiveK: ge.DEFAULT_MDAP_MAKER_ADAPTIVE_K,
    mdapMakerProvider: ge.DEFAULT_MDAP_MAKER_PROVIDER,
    mdapMakerModel: ge.DEFAULT_MDAP_MAKER_MODEL,
    mdapMakerAutoSelectionKeywords: ge.DEFAULT_MDAP_MAKER_KEYWORDS
  },
  decompositionConfig: {
    // Core Decomposition Parameters
    decompositionStrategy: ge.DEFAULT_DECOMPOSITION_STRATEGY,
    maxSubProblems: 10,
    minSubProblemSize: 50,
    maxSubProblemSize: 500,
    granularityLevel: "medium",
    dependencyAnalysis: !0,
    complexityAnalysis: !0,
    feasibilityAnalysis: !0,
    validationRequired: !0,
    successCriteriaRequired: !0,
    dependencyGraphRequired: !0,
    // Strategy-Specific Parameters
    semanticAnalysisEnabled: !0,
    hierarchicalDepth: 3,
    functionalGranularity: "medium",
    modularIndependence: 0.7,
    temporalGranularity: "medium",
    // Quality Parameters
    qualityThreshold: 0.7,
    completenessThreshold: 0.8,
    clarityThreshold: 0.7,
    feasibilityThreshold: 0.7,
    validationThreshold: 0.8,
    // Execution Parameters
    maxIterations: 5,
    timeoutSeconds: 120,
    maxRetries: 3,
    fallbackStrategy: "default",
    parallelProcessing: !1,
    maxParallelTasks: 3,
    // Knowledge Integration
    knowledgeBaseEnabled: !1,
    knowledgeBaseSources: [],
    contextAnalysisEnabled: !0,
    domainSpecificAnalysis: !0,
    // MDAP/MAKER Integration
    mdapMakerEnabled: ge.DEFAULT_MDAP_MAKER_ENABLED,
    mdapMakerAutoSelect: ge.DEFAULT_MDAP_MAKER_AUTO_SELECT,
    mdapMakerMaxDepth: ge.DEFAULT_MDAP_MAKER_MAX_DEPTH,
    mdapMakerKAhead: ge.DEFAULT_MDAP_MAKER_K_AHEAD,
    mdapMakerRedFlagging: ge.DEFAULT_MDAP_MAKER_RED_FLAGGING,
    mdapMakerAdaptiveK: ge.DEFAULT_MDAP_MAKER_ADAPTIVE_K,
    mdapMakerProvider: ge.DEFAULT_MDAP_MAKER_PROVIDER,
    mdapMakerModel: ge.DEFAULT_MDAP_MAKER_MODEL,
    mdapMakerAutoSelectionKeywords: ge.DEFAULT_MDAP_MAKER_KEYWORDS
  },
  defaultExecutionMethod: ge.DEFAULT_EXECUTION_METHOD,
  mdapMaker: {
    enabled: ge.DEFAULT_MDAP_MAKER_ENABLED,
    autoSelect: ge.DEFAULT_MDAP_MAKER_AUTO_SELECT,
    maxDepth: ge.DEFAULT_MDAP_MAKER_MAX_DEPTH,
    kAhead: ge.DEFAULT_MDAP_MAKER_K_AHEAD,
    redFlagging: ge.DEFAULT_MDAP_MAKER_RED_FLAGGING,
    adaptiveK: ge.DEFAULT_MDAP_MAKER_ADAPTIVE_K,
    provider: ge.DEFAULT_MDAP_MAKER_PROVIDER,
    model: ge.DEFAULT_MDAP_MAKER_MODEL,
    autoSelectionKeywords: ge.DEFAULT_MDAP_MAKER_KEYWORDS
  }
}, qe = {
  ...ge,
  // Quality Diversity Defaults
  QUALITY_DIVERSITY_DEFAULTS: {
    feature_dimensions: null,
    feature_bins: 10,
    archive_size: 100,
    novelty_threshold: 0.1,
    behavior_dimensions: [],
    diversity_metric: "edit_distance",
    diversity_reference_size: 20,
    adaptive_feature_dimensions: !0,
    double_selection: !0,
    qd_algorithm: "MAP-Elites",
    behavior_descriptor_type: "hand_crafted",
    archive_learning_rate: 0.1,
    quality_threshold: 0,
    diversity_weight: 0.5,
    behavior_space: "auto",
    distance_metric: "euclidean",
    archive_update_freq: 1,
    exploration_bonus: 0.1,
    pareto_layers: 3
  },
  // Multi-Objective Defaults
  MULTI_OBJECTIVE_DEFAULTS: {
    objectives: null,
    objective_weights: [],
    pareto_front_size: 50,
    dominance_metric: "pareto",
    constraint_handling: "penalty",
    reference_point: [],
    crowding_distance: !0,
    epsilon_dominance: 0.01,
    decomposition_method: "weighted_sum",
    scalarization_function: "weighted_sum",
    dominance_type: "standard",
    epsilon_values: [],
    scalarization: "weighted_sum",
    constraint_tolerance: 0.01,
    hypervolume_ref: []
  },
  // Island Model Defaults
  ISLAND_MODEL_DEFAULTS: {
    num_islands: 5,
    migration_interval: 10,
    migration_rate: 0.1,
    migration_topology: "ring",
    ring_topology: !0,
    controlled_gene_flow: !0,
    island_diversity_metric: "edit_distance",
    migration_selection: "best",
    island_initialization: "random",
    island_specialization: !1,
    migration_size: 5,
    migration_policy: "best",
    replacement_policy: "worst",
    island_sizes: [],
    heterogeneous_islands: !1,
    synchronous_migration: !0,
    adaptive_migration: !1
  },
  // Selection Defaults
  SELECTION_DEFAULTS: {
    elite_ratio: 0.1,
    exploration_ratio: 0.2,
    exploitation_ratio: 0.7,
    multi_strategy_sampling: !0,
    selection_pressure: 2,
    tournament_size: 3,
    crossover_rate: 0.8,
    mutation_rate: 0.1,
    elitism_count: 2,
    selection_method: "tournament",
    reproduction_method: "both",
    parent_selection: "fitness",
    random_ratio: 0.2,
    survivor_selection: "generational",
    replacement_rate: 1,
    selection_pressure_decay: 0,
    diversity_selection: !1,
    age_based_selection: !1
  },
  // Evaluation Defaults
  EVALUATION_DEFAULTS: {
    cascade_evaluation: !0,
    cascade_thresholds: [0.5, 0.75, 0.9],
    parallel_evaluations: 4,
    evaluator_timeout: 300,
    max_retries_eval: 3,
    use_llm_feedback: !1,
    llm_feedback_weight: 0.1,
    evaluator_models: [],
    evaluator_system_message: "",
    ensemble_size: 3,
    consensus_threshold: 0.7,
    evaluation_criteria: [],
    custom_evaluator: null,
    evaluation_batch_size: 10,
    cache_evaluations: !0,
    cache_size: 1e3,
    evaluation_noise: 0,
    fitness_scaling: "linear",
    normalization: !0,
    multi_criteria_eval: !1,
    evaluation_budget: 1e4,
    incremental_eval: !1,
    surrogate_model: !1,
    active_learning: !1,
    uncertainty_sampling: !1
  },
  // Prompt Engineering Defaults
  PROMPT_ENGINEERING_DEFAULTS: {
    prompt_template: "default",
    system_prompt: "",
    context_length: 2e3,
    prompt_optimization: !0,
    template_stochasticity: !0,
    meta_prompting: !1,
    few_shot_examples: 3,
    chain_of_thought: !0,
    self_consistency: !1,
    prompt_ensembling: !1,
    dynamic_prompting: !1,
    prompt_compression: !1
  },
  // Artifact Management Defaults
  ARTIFACT_MANAGEMENT_DEFAULTS: {
    enable_artifacts: !0,
    artifact_types: ["code", "text"],
    max_artifact_size: 20480,
    artifact_validation: !0,
    artifact_compression: !1,
    artifact_versioning: !0,
    artifact_metadata: !0,
    artifact_cleanup: !0,
    artifact_storage: "memory",
    artifact_encryption: !1
  },
  // Resource Management Defaults
  RESOURCE_MANAGEMENT_DEFAULTS: {
    memory_limit_mb: 4096,
    cpu_limit: 0.8,
    max_time: 1800,
    disk_limit_mb: 1024,
    network_limit_mbps: 100,
    api_call_limit: 1e3,
    token_limit: 1e5,
    cost_limit_usd: 10,
    resource_monitoring: !0,
    auto_scaling: !1,
    checkpoint_interval: 10
  },
  // Database Storage Defaults
  DATABASE_STORAGE_DEFAULTS: {
    db_path: "./openevolve.db",
    db_type: "sqlite",
    connection_string: "",
    max_connections: 10,
    connection_timeout: 30,
    query_timeout: 60,
    batch_size: 1e3,
    compression: !0,
    encryption: !1,
    backup_enabled: !0
  },
  // Evolution Tracing Defaults
  EVOLUTION_TRACING_DEFAULTS: {
    trace_enabled: !1,
    trace_level: "basic",
    trace_format: "json",
    trace_file: "./trace.log",
    trace_compression: !0,
    trace_rotation: !0,
    max_trace_size_mb: 100,
    trace_buffer_size: 1e3,
    real_time_tracing: !1,
    trace_sampling: 1,
    include_population: !1,
    include_fitness: !0
  },
  // Early Stopping Defaults
  EARLY_STOPPING_DEFAULTS: {
    early_stopping: !1,
    early_stopping_patience: 10,
    min_improvement: 1e-3,
    improvement_window: 5,
    plateau_threshold: 20,
    convergence_check: !0,
    diversity_threshold: 0.01,
    stagnation_limit: 50,
    adaptive_stopping: !1
  },
  // Distributed Processing Defaults
  DISTRIBUTED_PROCESSING_DEFAULTS: {
    distributed: !1,
    num_workers: 4,
    worker_timeout: 120,
    load_balancing: "round_robin",
    fault_tolerance: !0,
    worker_restart: !0,
    communication_backend: "local",
    message_compression: !0,
    heartbeat_interval: 10,
    cluster_scaling: !1
  },
  // Advanced Research Defaults
  ADVANCED_RESEARCH_DEFAULTS: {
    novelty_search: !1,
    curiosity_driven: !1,
    meta_learning: !1,
    transfer_learning: !1,
    continual_learning: !1,
    few_shot_adaptation: !1,
    zero_shot_transfer: !1,
    domain_adaptation: !1,
    multi_task_learning: !1,
    lifelong_learning: !1,
    neural_architecture_search: !1,
    hyperparameter_optimization: !1,
    automated_ml: !1,
    explainable_ai: !1,
    federated_learning: !1,
    differential_privacy: !1,
    quantum_computing: !1,
    neuromorphic_computing: !1,
    edge_computing: !1,
    green_ai: !1
  },
  // Custom Requirements Defaults
  CUSTOM_REQUIREMENTS_DEFAULTS: {
    custom_fitness: "",
    custom_operators: [],
    custom_constraints: [],
    domain_knowledge: "",
    expert_rules: [],
    business_logic: "",
    regulatory_compliance: [],
    ethical_guidelines: []
  },
  // UI Visualization Defaults
  UI_VISUALIZATION_DEFAULTS: {
    enable_visualization: !0,
    plot_frequency: 10,
    plot_types: ["fitness", "diversity"],
    interactive_plots: !0,
    real_time_updates: !1,
    export_plots: !0,
    plot_format: "png",
    dashboard_enabled: !0
  },
  // Experimental Defaults
  EXPERIMENTAL_DEFAULTS: {
    experimental_features: !1,
    beta_algorithms: !1,
    research_mode: !1,
    debug_mode: !1,
    profiling_enabled: !1,
    memory_profiling: !1,
    experimental_logging: !1
  }
}, HE = {
  ...Rn,
  evolutionConfig: {
    ...Rn.evolutionConfig,
    // Add extended evolution defaults
    ...qe.QUALITY_DIVERSITY_DEFAULTS,
    ...qe.MULTI_OBJECTIVE_DEFAULTS,
    ...qe.ISLAND_MODEL_DEFAULTS,
    ...qe.SELECTION_DEFAULTS,
    ...qe.EVALUATION_DEFAULTS,
    ...qe.PROMPT_ENGINEERING_DEFAULTS,
    ...qe.ARTIFACT_MANAGEMENT_DEFAULTS,
    ...qe.RESOURCE_MANAGEMENT_DEFAULTS,
    ...qe.DATABASE_STORAGE_DEFAULTS,
    ...qe.EVOLUTION_TRACING_DEFAULTS,
    ...qe.EARLY_STOPPING_DEFAULTS,
    ...qe.DISTRIBUTED_PROCESSING_DEFAULTS,
    ...qe.ADVANCED_RESEARCH_DEFAULTS,
    ...qe.CUSTOM_REQUIREMENTS_DEFAULTS,
    ...qe.UI_VISUALIZATION_DEFAULTS,
    ...qe.EXPERIMENTAL_DEFAULTS
  },
  adversarialConfig: {
    ...Rn.adversarialConfig,
    // Add extended adversarial defaults
    attack_model_config: null,
    defense_model_config: null,
    adversarial_rounds: 5,
    attack_strength: 0.5,
    defense_strategy: "reactive",
    coevolutionary_approach: !1,
    red_team_models: [],
    blue_team_models: [],
    red_team_sample_size: 3,
    blue_team_sample_size: 3,
    adversarial_temperature: 0.8,
    attack_diversity: !0,
    defense_strength: 1,
    adversarial_budget: 100,
    attack_types: [],
    defense_strategies: [],
    robustness_metric: "accuracy",
    perturbation_bound: 0.1,
    gradient_masking: !1,
    ensemble_defense: !0
  },
  decompositionConfig: {
    ...Rn.decompositionConfig,
    // Add extended decomposition defaults
    ...qe.QUALITY_DIVERSITY_DEFAULTS,
    ...qe.MULTI_OBJECTIVE_DEFAULTS,
    ...qe.EVALUATION_DEFAULTS
  },
  // Add all extended configuration categories
  qualityDiversityConfig: qe.QUALITY_DIVERSITY_DEFAULTS,
  multiObjectiveConfig: qe.MULTI_OBJECTIVE_DEFAULTS,
  islandModelConfig: qe.ISLAND_MODEL_DEFAULTS,
  selectionConfig: qe.SELECTION_DEFAULTS,
  evaluationConfig: qe.EVALUATION_DEFAULTS,
  promptEngineeringConfig: qe.PROMPT_ENGINEERING_DEFAULTS,
  artifactManagementConfig: qe.ARTIFACT_MANAGEMENT_DEFAULTS,
  resourceManagementConfig: qe.RESOURCE_MANAGEMENT_DEFAULTS,
  databaseStorageConfig: qe.DATABASE_STORAGE_DEFAULTS,
  evolutionTracingConfig: qe.EVOLUTION_TRACING_DEFAULTS,
  earlyStoppingConfig: qe.EARLY_STOPPING_DEFAULTS,
  distributedProcessingConfig: qe.DISTRIBUTED_PROCESSING_DEFAULTS,
  advancedResearchConfig: qe.ADVANCED_RESEARCH_DEFAULTS,
  customRequirementsConfig: qe.CUSTOM_REQUIREMENTS_DEFAULTS,
  uiVisualizationConfig: qe.UI_VISUALIZATION_DEFAULTS,
  experimentalConfig: qe.EXPERIMENTAL_DEFAULTS
}, hi = {
  ...qe,
  // Performance Configuration Defaults
  PERFORMANCE_DEFAULTS: {
    caching: {
      enabled: !0,
      strategy: "lru",
      max_size: 1e3,
      ttl: 3600,
      compression: "gzip",
      cache_warmup: !0,
      cache_eviction_policy: "time-based"
    },
    parallel_processing: {
      enabled: !0,
      max_workers: 4,
      worker_type: "thread",
      load_balancing: "round-robin",
      batch_size: 10,
      timeout: 30,
      retry_policy: "exponential"
    },
    memory_optimization: {
      garbage_collection: "auto",
      object_pooling: !0,
      buffer_reuse: !0,
      weak_references: !1,
      memory_profiling: !1,
      leak_detection: !0
    },
    network_optimization: {
      connection_pooling: !0,
      keep_alive: !0,
      http2: !0,
      compression: "gzip",
      dns_caching: !0,
      dns_prefetch: !0,
      request_batching: !0
    },
    resource_management: {
      cpu_throttling: !1,
      memory_limits: { soft: 4096, hard: 8192 },
      disk_limits: { soft: 1024, hard: 2048 },
      cleanup_interval: 3600,
      resource_monitoring: !0
    },
    adaptive_optimization: {
      enabled: !0,
      learning_rate: 0.1,
      adaptation_interval: 60,
      performance_targets: { latency: 100, throughput: 1e3, memory_usage: 2048 },
      strategy: "reinforcement"
    }
  },
  // Security Configuration Defaults
  SECURITY_DEFAULTS: {
    authentication: {
      enabled: !0,
      method: "api-key",
      token_expiry: 3600,
      refresh_tokens: !0,
      rate_limiting: { requests_per_minute: 1e3, burst_limit: 100 },
      ip_whitelisting: !1,
      ip_blacklisting: !1
    },
    data_protection: {
      encryption: { enabled: !0, at_rest: !0, in_transit: !0, algorithm: "aes-256", key_rotation: 30 },
      masking: { sensitive_fields: ["password", "api_key", "token"], masking_strategy: "partial" },
      anonymization: { enabled: !1, fields: [], strategy: "pseudonymization" },
      redaction: { enabled: !0, patterns: ["\b(?:password|key|token)\b"] }
    },
    compliance: {
      gdpr: !0,
      hipaa: !1,
      ccpa: !0,
      soc2: !1,
      iso_27001: !0,
      audit_logging: { enabled: !0, retention_days: 30, log_level: "detailed" },
      data_retention: 30,
      consent_management: !0
    },
    access_control: {
      role_based: !0,
      attribute_based: !1,
      permission_model: "rbac",
      default_deny: !0,
      privilege_escalation_prevention: !0
    },
    network_security: {
      tls: { enabled: !0, min_version: "1.2", cipher_suites: ["TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384"] },
      firewall: { enabled: !0, rules: ["ALLOW INBOUND 443", "ALLOW INBOUND 80"] },
      ddos_protection: !0,
      intrusion_detection: !0
    },
    audit: {
      logging: { enabled: !0, level: "info", retention: 30 },
      monitoring: { enabled: !0, metrics: ["security", "performance"], alerts: ["security_breach"] },
      anomaly_detection: !0
    }
  },
  // Monitoring Configuration Defaults
  MONITORING_DEFAULTS: {
    metrics: {
      enabled: !0,
      collection_interval: 60,
      metrics_to_collect: ["cpu", "memory", "latency", "error_rate"],
      custom_metrics: {},
      aggregation: "avg"
    },
    logging: {
      enabled: !0,
      level: "info",
      format: "json",
      destinations: ["console", "file"],
      rotation: { enabled: !0, max_size: 10, max_files: 5 },
      sampling: { enabled: !1, rate: 1 }
    },
    tracing: {
      enabled: !1,
      sampler: "probabilistic",
      sample_rate: 0.1,
      max_traces_per_second: 100,
      trace_context: "w3c"
    },
    alerting: {
      enabled: !0,
      rules: [
        { name: "high_error_rate", condition: "error_rate > 0.05", threshold: 0.05, severity: "high", duration: 300, cooldown: 600 },
        { name: "high_latency", condition: "latency > 1000", threshold: 1e3, severity: "medium", duration: 60, cooldown: 300 }
      ],
      notifications: [
        { type: "email", destination: "admin@example.com", format: "text", enabled: !0 },
        { type: "slack", destination: "#alerts", format: "json", enabled: !0 }
      ],
      escalation_policy: {
        levels: [
          { severity: "low", channels: ["email"], delay: 300 },
          { severity: "medium", channels: ["email", "slack"], delay: 60 },
          { severity: "high", channels: ["email", "slack", "pagerduty"], delay: 0 },
          { severity: "critical", channels: ["email", "slack", "pagerduty", "sms"], delay: 0 }
        ],
        timeout: 300,
        repeat: 3
      }
    },
    dashboards: {
      enabled: !0,
      default_dashboard: "overview",
      custom_dashboards: [],
      refresh_interval: 60
    },
    health_checks: {
      enabled: !0,
      interval: 60,
      endpoints: ["/health", "/status"],
      thresholds: { response_time: 1e3, availability: 0.99 }
    },
    profiling: {
      enabled: !1,
      cpu_profiling: !1,
      memory_profiling: !1,
      heap_snapshot: !1,
      sampling_interval: 100
    }
  },
  // Integration Configuration Defaults
  INTEGRATION_DEFAULTS: {
    api_integrations: {
      rest: [],
      graphql: [],
      websocket: [],
      grpc: []
    },
    webhooks: {
      incoming: [],
      outgoing: [],
      retries: 3,
      timeout: 10
    },
    event_streaming: {
      kafka: [],
      rabbitmq: [],
      aws_sns: [],
      google_pubsub: []
    },
    databases: {
      sql: [],
      nosql: [],
      cache: []
    },
    third_party_services: {
      aws: [],
      azure: [],
      google_cloud: [],
      auth: [],
      payment: []
    },
    custom_integrations: {
      scripts: [],
      plugins: [],
      adapters: []
    }
  },
  // Error Handling Configuration Defaults
  ERROR_HANDLING_DEFAULTS: {
    error_classification: {
      categories: ["validation", "network", "authentication", "processing", "unknown"],
      severity_levels: ["low", "medium", "high", "critical"],
      default_severity: "medium"
    },
    error_recovery: {
      automatic_retry: !0,
      fallback_strategies: [
        { error_type: "network", fallback_action: "retry", configuration: { max_attempts: 3 }, max_attempts: 3 },
        { error_type: "validation", fallback_action: "default_value", configuration: { default: null }, max_attempts: 1 }
      ],
      circuit_breakers: [
        { name: "api_circuit_breaker", failure_threshold: 5, reset_timeout: 30, half_open_attempts: 3, fallback_strategy: "default_value" }
      ],
      compensation_actions: [
        { error_type: "processing", compensation_type: "rollback", configuration: { max_retries: 2 } }
      ]
    },
    error_reporting: {
      enabled: !0,
      destinations: ["console", "file"],
      sampling_rate: 1,
      sensitive_data_filtering: !0,
      rate_limiting: 100
    },
    error_analysis: {
      root_cause_analysis: !0,
      pattern_detection: !0,
      anomaly_detection: !0,
      trend_analysis: !0
    },
    error_prevention: {
      input_validation: !0,
      preconditions: !0,
      postconditions: !0,
      invariants: !0,
      timeout_detection: !0
    },
    error_context: {
      include_stack_trace: !0,
      include_environment: !0,
      include_state: !1,
      include_user_info: !1,
      redact_sensitive_data: !0
    }
  }
}, cg = {
  ...HE,
  performanceConfig: hi.PERFORMANCE_DEFAULTS,
  securityConfig: hi.SECURITY_DEFAULTS,
  monitoringConfig: hi.MONITORING_DEFAULTS,
  integrationConfig: hi.INTEGRATION_DEFAULTS,
  errorHandlingConfig: hi.ERROR_HANDLING_DEFAULTS,
  advancedState: {
    performance_metrics: {
      execution_time: { average: 0, min: 0, max: 0, standard_deviation: 0 },
      memory_usage: { current: 0, peak: 0, average: 0 },
      cpu_usage: { current: 0, average: 0, peak: 0 },
      throughput: { requests_per_second: 0, operations_per_second: 0 },
      cache_hit_rate: 0,
      error_rate: 0,
      latency: { average: 0, p95: 0, p99: 0 }
    },
    security_status: {
      overall_score: 100,
      vulnerabilities: { critical: 0, high: 0, medium: 0, low: 0 },
      compliance: { gdpr: !0, hipaa: !1, soc2: !1, iso_27001: !0 },
      encryption_status: "enabled",
      authentication_status: "enabled"
    },
    integration_status: {
      api_integrations: { total: 0, healthy: 0, unhealthy: 0 },
      webhook_integrations: { total: 0, healthy: 0, unhealthy: 0 },
      event_streaming: { total: 0, healthy: 0, unhealthy: 0 },
      database_integrations: { total: 0, healthy: 0, unhealthy: 0 }
    },
    error_statistics: {
      total_errors: 0,
      by_severity: { critical: 0, high: 0, medium: 0, low: 0 },
      by_category: {},
      error_rate: 0,
      mean_time_to_recovery: 0
    }
  },
  configuration_profiles: [
    {
      name: "performance",
      description: "Optimized for maximum performance",
      category: "performance",
      parameters: {
        evolutionConfig: {
          population_size: 100,
          max_iterations: 50,
          parallel_processing: !0
        },
        performanceConfig: {
          caching: { enabled: !0, strategy: "lru", max_size: 5e3 },
          parallel_processing: { enabled: !0, max_workers: 8 }
        }
      },
      validationRules: []
    },
    {
      name: "security",
      description: "Optimized for maximum security",
      category: "security",
      parameters: {
        securityConfig: {
          authentication: { enabled: !0, method: "oauth2", token_expiry: 3600 },
          data_protection: { encryption: { at_rest: !0, in_transit: !0 } }
        }
      },
      validationRules: []
    }
  ],
  active_profile: "default",
  validation_state: {
    last_validation: null,
    validation_results: [],
    validation_history: []
  }
};
function W_(e) {
  var t, r, n = "";
  if (typeof e == "string" || typeof e == "number") n += e;
  else if (typeof e == "object") if (Array.isArray(e)) {
    var a = e.length;
    for (t = 0; t < a; t++) e[t] && (r = W_(e[t])) && (n && (n += " "), n += r);
  } else for (r in e) e[r] && (n && (n += " "), n += r);
  return n;
}
function Ve() {
  for (var e, t, r = 0, n = "", a = arguments.length; r < a; r++) (e = arguments[r]) && (t = W_(e)) && (n && (n += " "), n += t);
  return n;
}
function GE(e) {
  if (typeof document > "u") return;
  let t = document.head || document.getElementsByTagName("head")[0], r = document.createElement("style");
  r.type = "text/css", t.firstChild ? t.insertBefore(r, t.firstChild) : t.appendChild(r), r.styleSheet ? r.styleSheet.cssText = e : r.appendChild(document.createTextNode(e));
}
GE(`:root{--toastify-color-light: #fff;--toastify-color-dark: #121212;--toastify-color-info: #3498db;--toastify-color-success: #07bc0c;--toastify-color-warning: #f1c40f;--toastify-color-error: hsl(6, 78%, 57%);--toastify-color-transparent: rgba(255, 255, 255, .7);--toastify-icon-color-info: var(--toastify-color-info);--toastify-icon-color-success: var(--toastify-color-success);--toastify-icon-color-warning: var(--toastify-color-warning);--toastify-icon-color-error: var(--toastify-color-error);--toastify-container-width: fit-content;--toastify-toast-width: 320px;--toastify-toast-offset: 16px;--toastify-toast-top: max(var(--toastify-toast-offset), env(safe-area-inset-top));--toastify-toast-right: max(var(--toastify-toast-offset), env(safe-area-inset-right));--toastify-toast-left: max(var(--toastify-toast-offset), env(safe-area-inset-left));--toastify-toast-bottom: max(var(--toastify-toast-offset), env(safe-area-inset-bottom));--toastify-toast-background: #fff;--toastify-toast-padding: 14px;--toastify-toast-min-height: 64px;--toastify-toast-max-height: 800px;--toastify-toast-bd-radius: 6px;--toastify-toast-shadow: 0px 4px 12px rgba(0, 0, 0, .1);--toastify-font-family: sans-serif;--toastify-z-index: 9999;--toastify-text-color-light: #757575;--toastify-text-color-dark: #fff;--toastify-text-color-info: #fff;--toastify-text-color-success: #fff;--toastify-text-color-warning: #fff;--toastify-text-color-error: #fff;--toastify-spinner-color: #616161;--toastify-spinner-color-empty-area: #e0e0e0;--toastify-color-progress-light: linear-gradient(to right, #4cd964, #5ac8fa, #007aff, #34aadc, #5856d6, #ff2d55);--toastify-color-progress-dark: #bb86fc;--toastify-color-progress-info: var(--toastify-color-info);--toastify-color-progress-success: var(--toastify-color-success);--toastify-color-progress-warning: var(--toastify-color-warning);--toastify-color-progress-error: var(--toastify-color-error);--toastify-color-progress-bgo: .2}.Toastify__toast-container{z-index:var(--toastify-z-index);-webkit-transform:translate3d(0,0,var(--toastify-z-index));position:fixed;width:var(--toastify-container-width);box-sizing:border-box;color:#fff;display:flex;flex-direction:column}.Toastify__toast-container--top-left{top:var(--toastify-toast-top);left:var(--toastify-toast-left)}.Toastify__toast-container--top-center{top:var(--toastify-toast-top);left:50%;transform:translate(-50%);align-items:center}.Toastify__toast-container--top-right{top:var(--toastify-toast-top);right:var(--toastify-toast-right);align-items:end}.Toastify__toast-container--bottom-left{bottom:var(--toastify-toast-bottom);left:var(--toastify-toast-left)}.Toastify__toast-container--bottom-center{bottom:var(--toastify-toast-bottom);left:50%;transform:translate(-50%);align-items:center}.Toastify__toast-container--bottom-right{bottom:var(--toastify-toast-bottom);right:var(--toastify-toast-right);align-items:end}.Toastify__toast{--y: 0;position:relative;touch-action:none;width:var(--toastify-toast-width);min-height:var(--toastify-toast-min-height);box-sizing:border-box;margin-bottom:1rem;padding:var(--toastify-toast-padding);border-radius:var(--toastify-toast-bd-radius);box-shadow:var(--toastify-toast-shadow);max-height:var(--toastify-toast-max-height);font-family:var(--toastify-font-family);z-index:0;display:flex;flex:1 auto;align-items:center;word-break:break-word}@media only screen and (max-width: 480px){.Toastify__toast-container{width:100vw;left:env(safe-area-inset-left);margin:0}.Toastify__toast-container--top-left,.Toastify__toast-container--top-center,.Toastify__toast-container--top-right{top:env(safe-area-inset-top);transform:translate(0)}.Toastify__toast-container--bottom-left,.Toastify__toast-container--bottom-center,.Toastify__toast-container--bottom-right{bottom:env(safe-area-inset-bottom);transform:translate(0)}.Toastify__toast-container--rtl{right:env(safe-area-inset-right);left:initial}.Toastify__toast{--toastify-toast-width: 100%;margin-bottom:0;border-radius:0}}.Toastify__toast-container[data-stacked=true]{width:var(--toastify-toast-width)}.Toastify__toast--stacked{position:absolute;width:100%;transform:translate3d(0,var(--y),0) scale(var(--s));transition:transform .3s}.Toastify__toast--stacked[data-collapsed] .Toastify__toast-body,.Toastify__toast--stacked[data-collapsed] .Toastify__close-button{transition:opacity .1s}.Toastify__toast--stacked[data-collapsed=false]{overflow:visible}.Toastify__toast--stacked[data-collapsed=true]:not(:last-child)>*{opacity:0}.Toastify__toast--stacked:after{content:"";position:absolute;left:0;right:0;height:calc(var(--g) * 1px);bottom:100%}.Toastify__toast--stacked[data-pos=top]{top:0}.Toastify__toast--stacked[data-pos=bot]{bottom:0}.Toastify__toast--stacked[data-pos=bot].Toastify__toast--stacked:before{transform-origin:top}.Toastify__toast--stacked[data-pos=top].Toastify__toast--stacked:before{transform-origin:bottom}.Toastify__toast--stacked:before{content:"";position:absolute;left:0;right:0;bottom:0;height:100%;transform:scaleY(3);z-index:-1}.Toastify__toast--rtl{direction:rtl}.Toastify__toast--close-on-click{cursor:pointer}.Toastify__toast-icon{margin-inline-end:10px;width:22px;flex-shrink:0;display:flex}.Toastify--animate{animation-fill-mode:both;animation-duration:.5s}.Toastify--animate-icon{animation-fill-mode:both;animation-duration:.3s}.Toastify__toast-theme--dark{background:var(--toastify-color-dark);color:var(--toastify-text-color-dark)}.Toastify__toast-theme--light,.Toastify__toast-theme--colored.Toastify__toast--default{background:var(--toastify-color-light);color:var(--toastify-text-color-light)}.Toastify__toast-theme--colored.Toastify__toast--info{color:var(--toastify-text-color-info);background:var(--toastify-color-info)}.Toastify__toast-theme--colored.Toastify__toast--success{color:var(--toastify-text-color-success);background:var(--toastify-color-success)}.Toastify__toast-theme--colored.Toastify__toast--warning{color:var(--toastify-text-color-warning);background:var(--toastify-color-warning)}.Toastify__toast-theme--colored.Toastify__toast--error{color:var(--toastify-text-color-error);background:var(--toastify-color-error)}.Toastify__progress-bar-theme--light{background:var(--toastify-color-progress-light)}.Toastify__progress-bar-theme--dark{background:var(--toastify-color-progress-dark)}.Toastify__progress-bar--info{background:var(--toastify-color-progress-info)}.Toastify__progress-bar--success{background:var(--toastify-color-progress-success)}.Toastify__progress-bar--warning{background:var(--toastify-color-progress-warning)}.Toastify__progress-bar--error{background:var(--toastify-color-progress-error)}.Toastify__progress-bar-theme--colored.Toastify__progress-bar--info,.Toastify__progress-bar-theme--colored.Toastify__progress-bar--success,.Toastify__progress-bar-theme--colored.Toastify__progress-bar--warning,.Toastify__progress-bar-theme--colored.Toastify__progress-bar--error{background:var(--toastify-color-transparent)}.Toastify__close-button{color:#fff;position:absolute;top:6px;right:6px;background:transparent;outline:none;border:none;padding:0;cursor:pointer;opacity:.7;transition:.3s ease;z-index:1}.Toastify__toast--rtl .Toastify__close-button{left:6px;right:unset}.Toastify__close-button--light{color:#000;opacity:.3}.Toastify__close-button>svg{fill:currentColor;height:16px;width:14px}.Toastify__close-button:hover,.Toastify__close-button:focus{opacity:1}@keyframes Toastify__trackProgress{0%{transform:scaleX(1)}to{transform:scaleX(0)}}.Toastify__progress-bar{position:absolute;bottom:0;left:0;width:100%;height:100%;z-index:1;opacity:.7;transform-origin:left}.Toastify__progress-bar--animated{animation:Toastify__trackProgress linear 1 forwards}.Toastify__progress-bar--controlled{transition:transform .2s}.Toastify__progress-bar--rtl{right:0;left:initial;transform-origin:right;border-bottom-left-radius:initial}.Toastify__progress-bar--wrp{position:absolute;overflow:hidden;bottom:0;left:0;width:100%;height:5px;border-bottom-left-radius:var(--toastify-toast-bd-radius);border-bottom-right-radius:var(--toastify-toast-bd-radius)}.Toastify__progress-bar--wrp[data-hidden=true]{opacity:0}.Toastify__progress-bar--bg{opacity:var(--toastify-color-progress-bgo);width:100%;height:100%}.Toastify__spinner{width:20px;height:20px;box-sizing:border-box;border:2px solid;border-radius:100%;border-color:var(--toastify-spinner-color-empty-area);border-right-color:var(--toastify-spinner-color);animation:Toastify__spin .65s linear infinite}@keyframes Toastify__bounceInRight{0%,60%,75%,90%,to{animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{opacity:0;transform:translate3d(3000px,0,0)}60%{opacity:1;transform:translate3d(-25px,0,0)}75%{transform:translate3d(10px,0,0)}90%{transform:translate3d(-5px,0,0)}to{transform:none}}@keyframes Toastify__bounceOutRight{20%{opacity:1;transform:translate3d(-20px,var(--y),0)}to{opacity:0;transform:translate3d(2000px,var(--y),0)}}@keyframes Toastify__bounceInLeft{0%,60%,75%,90%,to{animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{opacity:0;transform:translate3d(-3000px,0,0)}60%{opacity:1;transform:translate3d(25px,0,0)}75%{transform:translate3d(-10px,0,0)}90%{transform:translate3d(5px,0,0)}to{transform:none}}@keyframes Toastify__bounceOutLeft{20%{opacity:1;transform:translate3d(20px,var(--y),0)}to{opacity:0;transform:translate3d(-2000px,var(--y),0)}}@keyframes Toastify__bounceInUp{0%,60%,75%,90%,to{animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{opacity:0;transform:translate3d(0,3000px,0)}60%{opacity:1;transform:translate3d(0,-20px,0)}75%{transform:translate3d(0,10px,0)}90%{transform:translate3d(0,-5px,0)}to{transform:translateZ(0)}}@keyframes Toastify__bounceOutUp{20%{transform:translate3d(0,calc(var(--y) - 10px),0)}40%,45%{opacity:1;transform:translate3d(0,calc(var(--y) + 20px),0)}to{opacity:0;transform:translate3d(0,-2000px,0)}}@keyframes Toastify__bounceInDown{0%,60%,75%,90%,to{animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{opacity:0;transform:translate3d(0,-3000px,0)}60%{opacity:1;transform:translate3d(0,25px,0)}75%{transform:translate3d(0,-10px,0)}90%{transform:translate3d(0,5px,0)}to{transform:none}}@keyframes Toastify__bounceOutDown{20%{transform:translate3d(0,calc(var(--y) - 10px),0)}40%,45%{opacity:1;transform:translate3d(0,calc(var(--y) + 20px),0)}to{opacity:0;transform:translate3d(0,2000px,0)}}.Toastify__bounce-enter--top-left,.Toastify__bounce-enter--bottom-left{animation-name:Toastify__bounceInLeft}.Toastify__bounce-enter--top-right,.Toastify__bounce-enter--bottom-right{animation-name:Toastify__bounceInRight}.Toastify__bounce-enter--top-center{animation-name:Toastify__bounceInDown}.Toastify__bounce-enter--bottom-center{animation-name:Toastify__bounceInUp}.Toastify__bounce-exit--top-left,.Toastify__bounce-exit--bottom-left{animation-name:Toastify__bounceOutLeft}.Toastify__bounce-exit--top-right,.Toastify__bounce-exit--bottom-right{animation-name:Toastify__bounceOutRight}.Toastify__bounce-exit--top-center{animation-name:Toastify__bounceOutUp}.Toastify__bounce-exit--bottom-center{animation-name:Toastify__bounceOutDown}@keyframes Toastify__zoomIn{0%{opacity:0;transform:scale3d(.3,.3,.3)}50%{opacity:1}}@keyframes Toastify__zoomOut{0%{opacity:1}50%{opacity:0;transform:translate3d(0,var(--y),0) scale3d(.3,.3,.3)}to{opacity:0}}.Toastify__zoom-enter{animation-name:Toastify__zoomIn}.Toastify__zoom-exit{animation-name:Toastify__zoomOut}@keyframes Toastify__flipIn{0%{transform:perspective(400px) rotateX(90deg);animation-timing-function:ease-in;opacity:0}40%{transform:perspective(400px) rotateX(-20deg);animation-timing-function:ease-in}60%{transform:perspective(400px) rotateX(10deg);opacity:1}80%{transform:perspective(400px) rotateX(-5deg)}to{transform:perspective(400px)}}@keyframes Toastify__flipOut{0%{transform:translate3d(0,var(--y),0) perspective(400px)}30%{transform:translate3d(0,var(--y),0) perspective(400px) rotateX(-20deg);opacity:1}to{transform:translate3d(0,var(--y),0) perspective(400px) rotateX(90deg);opacity:0}}.Toastify__flip-enter{animation-name:Toastify__flipIn}.Toastify__flip-exit{animation-name:Toastify__flipOut}@keyframes Toastify__slideInRight{0%{transform:translate3d(110%,0,0);visibility:visible}to{transform:translate3d(0,var(--y),0)}}@keyframes Toastify__slideInLeft{0%{transform:translate3d(-110%,0,0);visibility:visible}to{transform:translate3d(0,var(--y),0)}}@keyframes Toastify__slideInUp{0%{transform:translate3d(0,110%,0);visibility:visible}to{transform:translate3d(0,var(--y),0)}}@keyframes Toastify__slideInDown{0%{transform:translate3d(0,-110%,0);visibility:visible}to{transform:translate3d(0,var(--y),0)}}@keyframes Toastify__slideOutRight{0%{transform:translate3d(0,var(--y),0)}to{visibility:hidden;transform:translate3d(110%,var(--y),0)}}@keyframes Toastify__slideOutLeft{0%{transform:translate3d(0,var(--y),0)}to{visibility:hidden;transform:translate3d(-110%,var(--y),0)}}@keyframes Toastify__slideOutDown{0%{transform:translate3d(0,var(--y),0)}to{visibility:hidden;transform:translate3d(0,500px,0)}}@keyframes Toastify__slideOutUp{0%{transform:translate3d(0,var(--y),0)}to{visibility:hidden;transform:translate3d(0,-500px,0)}}.Toastify__slide-enter--top-left,.Toastify__slide-enter--bottom-left{animation-name:Toastify__slideInLeft}.Toastify__slide-enter--top-right,.Toastify__slide-enter--bottom-right{animation-name:Toastify__slideInRight}.Toastify__slide-enter--top-center{animation-name:Toastify__slideInDown}.Toastify__slide-enter--bottom-center{animation-name:Toastify__slideInUp}.Toastify__slide-exit--top-left,.Toastify__slide-exit--bottom-left{animation-name:Toastify__slideOutLeft;animation-timing-function:ease-in;animation-duration:.3s}.Toastify__slide-exit--top-right,.Toastify__slide-exit--bottom-right{animation-name:Toastify__slideOutRight;animation-timing-function:ease-in;animation-duration:.3s}.Toastify__slide-exit--top-center{animation-name:Toastify__slideOutUp;animation-timing-function:ease-in;animation-duration:.3s}.Toastify__slide-exit--bottom-center{animation-name:Toastify__slideOutDown;animation-timing-function:ease-in;animation-duration:.3s}@keyframes Toastify__spin{0%{transform:rotate(0)}to{transform:rotate(360deg)}}
`);
var Gh = (e) => typeof e == "number" && !isNaN(e), Fi = (e) => typeof e == "string", V_ = (e) => typeof e == "function", KE = (e) => Fi(e) || Gh(e), YE = (e) => Cr(e) || Fi(e) || V_(e) || Gh(e), XE = 1, H_ = () => `${XE++}`, Rr = /* @__PURE__ */ new Map(), vm = [], ug = /* @__PURE__ */ new Set(), G_ = () => Rr.size > 0, QE = (e, { containerId: t }) => {
  var r;
  return (r = Rr.get(t || 1)) == null ? void 0 : r.toasts.get(e);
};
function JE(e, t) {
  var r;
  if (t) return !!((r = Rr.get(t)) != null && r.isToastActive(e));
  let n = !1;
  return Rr.forEach((a) => {
    a.isToastActive(e) && (n = !0);
  }), n;
}
function ZE(e) {
  if (!G_()) {
    vm = vm.filter((t) => e != null && t.options.toastId !== e);
    return;
  }
  if (e == null || KE(e)) Rr.forEach((t) => {
    t.removeToast(e);
  });
  else if (e && ("containerId" in e || "id" in e)) {
    let t = Rr.get(e.containerId);
    t ? t.removeToast(e.id) : Rr.forEach((r) => {
      r.removeToast(e.id);
    });
  }
}
var eA = (e = {}) => {
  Rr.forEach((t) => {
    t.props.limit && (!e.containerId || t.id === e.containerId) && t.clearQueue();
  });
};
function tA(e, t) {
  YE(e) && (G_() || vm.push({ content: e, options: t }), Rr.forEach((r) => {
    r.buildToast(e, t);
  }));
}
function K_(e, t) {
  Rr.forEach((r) => {
    (t == null || !(t != null && t.containerId) || (t == null ? void 0 : t.containerId) === r.id) && r.toggle(e, t == null ? void 0 : t.id);
  });
}
function rA(e) {
  return ug.add(e), () => {
    ug.delete(e);
  };
}
function nA(e) {
  return e && (Fi(e.toastId) || Gh(e.toastId)) ? e.toastId : H_();
}
function _o(e, t) {
  return tA(e, t), t.toastId;
}
function gl(e, t) {
  return { ...t, type: t && t.type || e, toastId: nA(t) };
}
function yl(e) {
  return (t, r) => _o(t, gl(e, r));
}
function F(e, t) {
  return _o(e, gl("default", t));
}
F.loading = (e, t) => _o(e, gl("default", { isLoading: !0, autoClose: !1, closeOnClick: !1, closeButton: !1, draggable: !1, ...t }));
function aA(e, { pending: t, error: r, success: n }, a) {
  let i;
  t && (i = Fi(t) ? F.loading(t, a) : F.loading(t.render, { ...a, ...t }));
  let o = { isLoading: null, autoClose: null, closeOnClick: null, closeButton: null, draggable: null }, l = (u, d, f) => {
    if (d == null) {
      F.dismiss(i);
      return;
    }
    let p = { type: u, ...o, ...a, data: f }, m = Fi(d) ? { render: d } : d;
    return i ? F.update(i, { ...p, ...m }) : F(m.render, { ...p, ...m }), f;
  }, c = V_(e) ? e() : e;
  return c.then((u) => l("success", n, u)).catch((u) => l("error", r, u)), c;
}
F.promise = aA;
F.success = yl("success");
F.info = yl("info");
F.error = yl("error");
F.warning = yl("warning");
F.warn = F.warning;
F.dark = (e, t) => _o(e, gl("default", { theme: "dark", ...t }));
function iA(e) {
  ZE(e);
}
F.dismiss = iA;
F.clearWaitingQueue = eA;
F.isActive = JE;
F.update = (e, t = {}) => {
  let r = QE(e, t);
  if (r) {
    let { props: n, content: a } = r, i = { delay: 100, ...n, ...t, toastId: t.toastId || e, updateId: H_() };
    i.toastId !== e && (i.staleId = e);
    let o = i.render || a;
    delete i.render, _o(o, i);
  }
};
F.done = (e) => {
  F.update(e, { progress: 1 });
};
F.onChange = rA;
F.play = (e) => K_(!0, e);
F.pause = (e) => K_(!1, e);
const Rt = [];
for (let e = 0; e < 256; ++e)
  Rt.push((e + 256).toString(16).slice(1));
function oA(e, t = 0) {
  return (Rt[e[t + 0]] + Rt[e[t + 1]] + Rt[e[t + 2]] + Rt[e[t + 3]] + "-" + Rt[e[t + 4]] + Rt[e[t + 5]] + "-" + Rt[e[t + 6]] + Rt[e[t + 7]] + "-" + Rt[e[t + 8]] + Rt[e[t + 9]] + "-" + Rt[e[t + 10]] + Rt[e[t + 11]] + Rt[e[t + 12]] + Rt[e[t + 13]] + Rt[e[t + 14]] + Rt[e[t + 15]]).toLowerCase();
}
let rc;
const sA = new Uint8Array(16);
function lA() {
  if (!rc) {
    if (typeof crypto > "u" || !crypto.getRandomValues)
      throw new Error("crypto.getRandomValues() not supported. See https://github.com/uuidjs/uuid#getrandomvalues-not-supported");
    rc = crypto.getRandomValues.bind(crypto);
  }
  return rc(sA);
}
const cA = typeof crypto < "u" && crypto.randomUUID && crypto.randomUUID.bind(crypto), dg = { randomUUID: cA };
function uA(e, t, r) {
  var a;
  e = e || {};
  const n = e.random ?? ((a = e.rng) == null ? void 0 : a.call(e)) ?? lA();
  if (n.length < 16)
    throw new Error("Random bytes length must be >= 16");
  return n[6] = n[6] & 15 | 64, n[8] = n[8] & 63 | 128, oA(n);
}
function ht(e, t, r) {
  return dg.randomUUID && !e ? dg.randomUUID() : uA(e);
}
let ve = { ...Rn }, nc = null;
class dA {
  constructor() {
    Z(this, "cache");
    Z(this, "cacheTTL");
    this.cache = /* @__PURE__ */ new Map(), this.cacheTTL = 36e5;
  }
  async executeWithRetry(t, r = 3, n = 3e4) {
    let a = null;
    for (let i = 1; i <= r; i++)
      try {
        const o = new Promise(
          (c, u) => setTimeout(() => u(new Error("Operation timed out")), n)
        );
        return await Promise.race([
          t(),
          o
        ]);
      } catch (o) {
        if (a = o instanceof Error ? o : new Error(String(o)), i < r) {
          const l = Math.min(1e3 * Math.pow(2, i), 5e3);
          await new Promise((c) => setTimeout(c, l));
        }
      }
    throw a || new Error("Operation failed after maximum retries");
  }
  async executeEvolution(t, r, n = {}) {
    const a = ht(), i = (/* @__PURE__ */ new Date()).toISOString();
    return {
      executionId: a,
      status: "completed",
      module: "evolution",
      input: { goal: t, config: r },
      output: {
        bestSolution: `Optimized solution for: ${t}`,
        population: Array(5).fill(0).map((l, c) => `Solution variant ${c + 1}`),
        fitnessScores: [0.95, 0.92, 0.88, 0.85, 0.8],
        generations: r.maxIterations || 10,
        convergence: 0.98,
        diversity: 0.75
      },
      statistics: this.createExecutionStatistics(
        a,
        i,
        "evolution",
        r.evolutionMode || "standard"
      ),
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    };
  }
  async executeAdversarial(t, r, n = {}) {
    const a = ht(), i = (/* @__PURE__ */ new Date()).toISOString();
    return {
      executionId: a,
      status: "completed",
      module: "adversarial",
      input: { content: t, config: r },
      output: {
        originalContent: t,
        redTeamCritiques: [
          "Potential security vulnerability in input validation",
          "Performance bottleneck in data processing",
          "Lack of error handling for edge cases"
        ],
        blueTeamImprovements: [
          "Added comprehensive input validation with regex patterns",
          "Optimized data processing using vectorized operations",
          "Implemented robust error handling with fallback mechanisms"
        ],
        evaluatorAssessment: {
          originalScore: 0.65,
          improvedScore: 0.92,
          improvementPercentage: 41.5,
          qualityMetrics: {
            robustness: 0.95,
            security: 0.9,
            performance: 0.88,
            maintainability: 0.92
          }
        },
        roundsCompleted: r.maxRounds || 5,
        finalContent: `Improved version of: ${t.substring(0, 100)}...`
      },
      statistics: this.createExecutionStatistics(
        a,
        i,
        "adversarial",
        r.adversarialMode || "red_blue_team"
      ),
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    };
  }
  async executeDecomposition(t, r, n = {}) {
    const a = ht(), i = (/* @__PURE__ */ new Date()).toISOString();
    return {
      executionId: a,
      status: "completed",
      module: "decomposition",
      input: { problem: t, config: r },
      output: {
        originalProblem: t,
        subProblems: [
          {
            id: "sub-1",
            description: "Implement core data processing pipeline",
            dependencies: [],
            complexity: "medium",
            successCriteria: "Processes 10,000 records/sec with <1% error rate"
          },
          {
            id: "sub-2",
            description: "Design input validation system",
            dependencies: ["sub-1"],
            complexity: "low",
            successCriteria: "Catches 99% of invalid inputs with clear error messages"
          },
          {
            id: "sub-3",
            description: "Create error handling and recovery mechanism",
            dependencies: ["sub-1", "sub-2"],
            complexity: "high",
            successCriteria: "Recovers from 95% of errors without data loss"
          }
        ],
        dependencyGraph: {
          "sub-1": [],
          "sub-2": ["sub-1"],
          "sub-3": ["sub-1", "sub-2"]
        },
        complexityAnalysis: {
          overall: "medium",
          distribution: { low: 1, medium: 1, high: 1 }
        },
        feasibilityScore: 0.87,
        validationResults: {
          completeness: 0.92,
          clarity: 0.88,
          independence: 0.85
        }
      },
      statistics: this.createExecutionStatistics(
        a,
        i,
        "decomposition",
        r.decompositionStrategy || "semantic"
      ),
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    };
  }
  async executeIntegrated(t, r, n = {}) {
    const a = ht(), i = (/* @__PURE__ */ new Date()).toISOString();
    return {
      executionId: a,
      status: "completed",
      module: "integration",
      input: { goal: t, config: r },
      output: {
        originalGoal: t,
        decompositionResults: {
          subProblems: [
            "Analyze current system architecture",
            "Identify performance bottlenecks",
            "Design optimization strategies",
            "Implement improvements",
            "Test and validate results"
          ],
          complexity: "high"
        },
        evolutionResults: {
          bestSolution: "Optimized architecture with parallel processing",
          iterations: 15,
          fitnessScore: 0.93
        },
        adversarialResults: {
          vulnerabilitiesFound: 8,
          improvementsMade: 12,
          finalQualityScore: 0.96
        },
        integratedSolution: {
          summary: "Comprehensive system optimization with validated improvements",
          performanceGains: "47% faster processing, 32% reduced memory usage",
          reliability: "99.9% uptime with robust error handling",
          recommendations: [
            "Implement monitoring for continuous improvement",
            "Schedule regular adversarial testing",
            "Document all changes for future reference"
          ]
        }
      },
      statistics: this.createExecutionStatistics(
        a,
        i,
        "integration",
        "integrated_workflow"
      ),
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    };
  }
  createExecutionStatistics(t, r, n, a) {
    const i = (/* @__PURE__ */ new Date()).toISOString(), o = new Date(r), c = new Date(i).getTime() - o.getTime();
    return {
      executionId: t,
      startTime: r,
      endTime: i,
      durationMs: c,
      status: "completed",
      module: n,
      strategy: a,
      iterations: Math.floor(Math.random() * 10) + 5,
      successRate: 0.95 + Math.random() * 0.05,
      errorCount: Math.floor(Math.random() * 3),
      warningCount: Math.floor(Math.random() * 5),
      tokensUsed: Math.floor(Math.random() * 1e4) + 5e3,
      apiCalls: Math.floor(Math.random() * 20) + 10,
      cacheHits: Math.floor(Math.random() * 5),
      cacheMisses: Math.floor(Math.random() * 15),
      performanceScore: 0.85 + Math.random() * 0.15,
      qualityScore: 0.9 + Math.random() * 0.1,
      improvementScore: 0.7 + Math.random() * 0.3,
      complexityReduction: 0.2 + Math.random() * 0.3,
      errorMessages: [],
      warningMessages: []
    };
  }
  // Cache management methods
  setCacheTTL(t) {
    this.cacheTTL = t;
  }
  getFromCache(t) {
    return this.cache.get(t) || null;
  }
  setInCache(t, r) {
    this.cache.set(t, r);
  }
  clearCache() {
    this.cache.clear();
  }
}
function Kh(e = {}) {
  if (nc && Object.keys(e).length === 0)
    return nc;
  ve = {
    ...Rn,
    ...e
  };
  const r = new dA(), n = {
    // Metadata and Initialization
    getMetadata: () => ve.metadata,
    getState: () => ({ ...ve }),
    async initialize(a = {}) {
      try {
        ve = { ...ve, ...a, initialized: !0, status: "idle" }, F.success("OpenEvolve plugin initialized successfully");
      } catch (i) {
        const o = i instanceof Error ? i.message : String(i);
        throw F.error(`Failed to initialize OpenEvolve plugin: ${o}`), i;
      }
    },
    // Configuration Management
    async updateConfig(a) {
      try {
        ve = { ...ve, ...a }, F.success("OpenEvolve configuration updated successfully");
      } catch (i) {
        const o = i instanceof Error ? i.message : String(i);
        throw F.error(`Failed to update configuration: ${o}`), i;
      }
    },
    async resetConfig() {
      try {
        ve = { ...Rn, initialized: !0 }, F.success("OpenEvolve configuration reset to defaults");
      } catch (a) {
        const i = a instanceof Error ? a.message : String(a);
        throw F.error(`Failed to reset configuration: ${i}`), a;
      }
    },
    getConfig: () => ({ ...ve }),
    // Evolution Functionality
    async executeEvolution(a, i = {}) {
      try {
        ve.status = "executing", ve.currentExecutionId = ht();
        const o = ve.currentExecutionId, l = (/* @__PURE__ */ new Date()).toISOString(), c = {
          ...ve.evolutionConfig,
          ...i.evolutionConfig
        };
        this.shouldUseMdapMakerForGoal(a) && F.info("Using MDAP/MAKER for critical evolution task");
        const d = await r.executeWithRetry(
          () => r.executeEvolution(a, c, i),
          i.maxRetries || ve.evolutionConfig.maxRetries,
          i.timeout || ve.evolutionConfig.timeout * 1e3
        );
        return ve.executionHistory.unshift(d), ve.statistics.unshift(d.statistics), ve.status = d.status, F.success("Evolution execution completed successfully"), d;
      } catch (o) {
        const l = o instanceof Error ? o.message : String(o);
        throw ve.status = "failed", F.error(`Evolution execution failed: ${l}`), o;
      }
    },
    // Adversarial Functionality
    async executeAdversarial(a, i = {}) {
      try {
        ve.status = "executing", ve.currentExecutionId = ht();
        const o = ve.currentExecutionId, l = (/* @__PURE__ */ new Date()).toISOString(), c = {
          ...ve.adversarialConfig,
          ...i.adversarialConfig
        };
        this.shouldUseMdapMakerForGoal(a) && F.info("Using MDAP/MAKER for critical adversarial task");
        const d = await r.executeWithRetry(
          () => r.executeAdversarial(a, c, i),
          i.maxRetries || ve.adversarialConfig.maxRetries,
          i.timeout || ve.adversarialConfig.timeoutSeconds * 1e3
        );
        return ve.executionHistory.unshift(d), ve.statistics.unshift(d.statistics), ve.status = d.status, F.success("Adversarial execution completed successfully"), d;
      } catch (o) {
        const l = o instanceof Error ? o.message : String(o);
        throw ve.status = "failed", F.error(`Adversarial execution failed: ${l}`), o;
      }
    },
    // Decomposition Functionality
    async executeDecomposition(a, i = {}) {
      try {
        ve.status = "executing", ve.currentExecutionId = ht();
        const o = ve.currentExecutionId, l = (/* @__PURE__ */ new Date()).toISOString(), c = {
          ...ve.decompositionConfig,
          ...i.decompositionConfig
        };
        this.shouldUseMdapMakerForGoal(a) && F.info("Using MDAP/MAKER for critical decomposition task");
        const d = await r.executeWithRetry(
          () => r.executeDecomposition(a, c, i),
          i.maxRetries || ve.decompositionConfig.maxRetries,
          i.timeout || ve.decompositionConfig.timeoutSeconds * 1e3
        );
        return ve.executionHistory.unshift(d), ve.statistics.unshift(d.statistics), ve.status = d.status, F.success("Decomposition execution completed successfully"), d;
      } catch (o) {
        const l = o instanceof Error ? o.message : String(o);
        throw ve.status = "failed", F.error(`Decomposition execution failed: ${l}`), o;
      }
    },
    // Integrated Execution
    async executeIntegrated(a, i = {}) {
      try {
        ve.status = "executing", ve.currentExecutionId = ht();
        const o = ve.currentExecutionId, l = (/* @__PURE__ */ new Date()).toISOString(), c = i.executionMethod || ve.defaultExecutionMethod;
        (c === "roma_mdap_maker" || c === "auto" && this.shouldUseMdapMakerForGoal(a)) && F.info("Using MDAP/MAKER for integrated execution");
        const d = await r.executeWithRetry(
          () => r.executeIntegrated(a, {
            evolutionConfig: ve.evolutionConfig,
            adversarialConfig: ve.adversarialConfig,
            decompositionConfig: ve.decompositionConfig,
            ...i
          }, i),
          i.maxRetries || 3,
          i.timeout || 3e5
        );
        return ve.executionHistory.unshift(d), ve.statistics.unshift(d.statistics), ve.status = d.status, F.success("Integrated execution completed successfully"), d;
      } catch (o) {
        const l = o instanceof Error ? o.message : String(o);
        throw ve.status = "failed", F.error(`Integrated execution failed: ${l}`), o;
      }
    },
    // Execution Management
    async getExecution(a) {
      return ve.executionHistory.find(
        (o) => o.executionId === a
      ) || null;
    },
    async getExecutionHistory() {
      return [...ve.executionHistory];
    },
    async getStatistics() {
      return [...ve.statistics];
    },
    async cancelExecution(a) {
      const i = ve.executionHistory.findIndex(
        (o) => o.executionId === a && o.status === "executing"
      );
      return i !== -1 ? (ve.executionHistory[i].status = "cancelled", ve.executionHistory[i].statistics.status = "cancelled", F.info(`Execution ${a} cancelled`), !0) : !1;
    },
    async clearHistory() {
      ve.executionHistory = [], ve.statistics = [], F.success("Execution history cleared");
    },
    // MDAP/MAKER Integration
    shouldUseMdapMakerForGoal(a) {
      const i = ve.mdapMaker;
      if (!(i != null && i.enabled) || !(i != null && i.autoSelect))
        return !1;
      const o = i.autoSelectionKeywords || [], l = a.toLowerCase();
      return o.some((c) => l.includes(c.toLowerCase()));
    },
    getMdapMakerConfig() {
      return ve.mdapMaker || null;
    },
    // Utility Methods
    async validateConfig() {
      const a = [];
      return ve.evolutionConfig.maxIterations <= 0 && a.push("Evolution max iterations must be greater than 0"), ve.evolutionConfig.populationSize <= 0 && a.push("Evolution population size must be greater than 0"), ve.adversarialConfig.redTeamSize <= 0 && a.push("Adversarial red team size must be greater than 0"), ve.adversarialConfig.blueTeamSize <= 0 && a.push("Adversarial blue team size must be greater than 0"), ve.decompositionConfig.maxSubProblems <= 0 && a.push("Decomposition max sub-problems must be greater than 0"), {
        valid: a.length === 0,
        errors: a
      };
    },
    getAvailableStrategies() {
      return {
        evolution: ge.EVOLUTION_STRATEGIES,
        adversarial: ge.ADVERSARIAL_STRATEGIES,
        decomposition: ge.DECOMPOSITION_STRATEGIES
      };
    }
  };
  return nc = n, n;
}
const Y_ = Kh();
var On;
let vr = (On = class {
  /**
   * Creates a new OpenEvolve node instance
   *
   * @param id - Unique identifier for this node
   * @param config - Node configuration object
   */
  constructor(t, r = {}) {
    /** Unique identifier for this node instance */
    Z(this, "id");
    /** Node configuration */
    Z(this, "config");
    /** Execution history for this node */
    Z(this, "executionHistory");
    this.id = t, this.config = r, this.executionHistory = [];
  }
  /**
   * Get current node configuration
   */
  getConfig() {
    return { ...this.config };
  }
  /**
   * Update node configuration
   *
   * @param config - New configuration values
   */
  setConfig(t) {
    this.config = { ...this.config, ...t };
  }
  /**
   * Get execution history
   *
   * @returns Array of historical execution results
   */
  getExecutionHistory() {
    return [...this.executionHistory];
  }
  /**
   * Clear execution history
   */
  clearHistory() {
    this.executionHistory = [];
  }
  /**
   * Record execution result in history
   *
   * @param result - Result to record
   */
  recordExecution(t) {
    this.executionHistory.push(t), this.executionHistory.length > 100 && (this.executionHistory = this.executionHistory.slice(-100));
  }
  /**
   * Create a successful result object
   *
   * @param data - Result data
   * @param metadata - Optional metadata
   * @returns Success result object
   */
  createSuccessResult(t, r) {
    return {
      success: !0,
      data: t,
      metadata: {
        executionTime: Date.now(),
        timestamp: /* @__PURE__ */ new Date(),
        nodeId: this.id,
        ...r
      }
    };
  }
  /**
   * Create an error result object
   *
   * @param error - Error message or Error object
   * @param metadata - Optional metadata
   * @returns Error result object
   */
  createErrorResult(t, r) {
    return {
      success: !1,
      error: t instanceof Error ? t.message : t,
      metadata: {
        executionTime: Date.now(),
        timestamp: /* @__PURE__ */ new Date(),
        nodeId: this.id,
        ...r
      }
    };
  }
  /**
   * Execute with automatic history recording and error handling
   *
   * @param inputs - Input data
   * @param context - Execution context
   * @returns Promise resolving to execution result
   */
  async executeWithHistory(t, r) {
    const n = Date.now();
    try {
      const a = this.validateInputs(t);
      if (a.length > 0) {
        const o = this.createErrorResult(
          `Validation failed: ${a.map((l) => l.message).join(", ")}`,
          { validationErrors: a }
        );
        return this.recordExecution(o), o;
      }
      const i = await this.execute(t, r);
      return i.metadata && !i.metadata.executionTime && (i.metadata.executionTime = Date.now() - n), this.recordExecution(i), i;
    } catch (a) {
      const i = this.createErrorResult(
        a instanceof Error ? a : String(a),
        { executionTime: Date.now() - n }
      );
      return this.recordExecution(i), i;
    }
  }
  /**
   * Get static metadata about this node type
   *
   * @returns Object containing display name, description, icon, category, and version
   */
  static getMetadata() {
    return {
      displayName: this.DISPLAY_NAME,
      description: this.DESCRIPTION,
      icon: this.ICON,
      category: this.CATEGORY,
      version: this.VERSION
    };
  }
  /**
   * Get instance metadata about this node
   *
   * @returns Object containing type, display name, description, icon, category, version, inputs, and outputs
   */
  getMetadata() {
    const t = this.constructor;
    return {
      type: t.name,
      displayName: t.DISPLAY_NAME || "Unknown",
      description: t.DESCRIPTION || "",
      icon: t.ICON || "default",
      category: t.CATEGORY || "general",
      version: t.VERSION || "1.0.0",
      inputs: this.getParameterSchema(),
      outputs: {
        type: "object",
        properties: {}
      }
    };
  }
}, /**
 * Display name for this node type
 */
Z(On, "DISPLAY_NAME"), /**
 * Detailed description of what this node does
 */
Z(On, "DESCRIPTION"), /**
 * Icon identifier for UI rendering
 */
Z(On, "ICON"), /**
 * Node category for organization
 */
Z(On, "CATEGORY"), /**
 * Node version
 */
Z(On, "VERSION"), On);
class da extends vr {
  constructor(r, n = {}) {
    super(r, {
      strategy: "hybrid",
      maxSubProblems: 10,
      qualityThreshold: 0.7,
      enableBackendExecution: !0,
      backendUrl: "http://localhost:8000",
      ...n
    });
    Z(this, "client", null);
    this.config.enableBackendExecution && (this.client = new OpenEvolveClient({
      baseUrl: this.config.backendUrl,
      timeout: 6e4,
      // 60 second timeout
      enableWebSocket: !1,
      // We'll add WebSocket support later
      debug: !1
    }));
  }
  /**
   * Execute problem decomposition using the integration library
   *
   * @param inputs - Must contain 'problem_statement' string
   * @param context - Execution context
   * @returns Promise resolving to decomposition result
   */
  async execute(r, n) {
    try {
      const a = Date.now(), i = r.problem_statement || r.problem, o = r.method || this.config.strategy, l = r.requirements, c = r.constraints;
      return !i || i.trim().length === 0 ? this.createErrorResult("Problem statement is required and cannot be empty") : (n.updateProgress(10, "Validating inputs"), this.config.enableBackendExecution && this.client ? await this.executeWithBackend(i, o, l, c, n) : await this.executeLocally(i, o, l, n));
    } catch (a) {
      return this.createErrorResult(
        a instanceof Error ? a.message : "Unknown error during decomposition"
      );
    }
  }
  /**
   * Execute decomposition using Python backend via integration library
   */
  async executeWithBackend(r, n, a, i, o) {
    try {
      if (o.updateProgress(20, "Connecting to backend"), !this.client)
        throw new Error("Integration library client not initialized");
      const l = {
        problem_statement: r,
        method: n
      };
      a && (l.requirements = a), i && (l.constraints = i), o.updateProgress(30, "Executing decomposition on backend");
      const c = await this.client.execute(
        IntegrationName.DECOMPOSITION,
        l,
        {
          timeout: 6e4,
          onProgress: (d) => {
            o.updateProgress(
              30 + d.progress * 0.6,
              d.message || `Processing: ${d.progress}%`
            );
          }
        }
      );
      o.updateProgress(100, `Decomposition complete: ${c.total_sub_problems} sub-problems`);
      const u = {
        subProblems: c.sub_problems.map((d) => ({
          id: d.id,
          title: d.title,
          description: d.description,
          complexity: d.complexity,
          estimatedEffort: d.estimated_time,
          dependencies: d.dependencies,
          requirements: d.success_criteria || [],
          priority: this.assessPriorityFromComplexity(d.complexity),
          status: d.status,
          metadata: {
            parentProblem: r.substring(0, 100),
            decompositionStrategy: c.method_used,
            qualityScore: c.complexity_metrics.confidence
          }
        })),
        dependencyGraph: c.decomposition_tree,
        qualityMetrics: {
          completeness: c.complexity_metrics.overall_score,
          clarity: c.complexity_metrics.overall_score * 0.9,
          feasibility: c.complexity_metrics.overall_score * 0.95,
          overall: c.complexity_metrics.overall_score
        },
        metadata: {
          originalProblem: r,
          strategyUsed: c.method_used,
          executionTime: 0,
          // Backend tracks this
          subProblemCount: c.total_sub_problems,
          totalEstimatedEffort: c.estimated_time,
          planId: c.plan_id,
          problemId: c.problem_id,
          confidence: c.confidence
        }
      };
      return this.createSuccessResult(u);
    } catch (l) {
      return console.warn("Backend execution failed, falling back to local:", l), o.updateProgress(20, "Backend unavailable, using local execution"), this.executeLocally(r, n, a, o);
    }
  }
  /**
   * Execute decomposition locally (fallback/simplified version)
   * This is used when backend is unavailable or for testing
   */
  async executeLocally(r, n, a, i) {
    i.updateProgress(40, "Performing local decomposition");
    const l = r.split(/[.!?]+/).filter((c) => c.trim().length > 0).map((c, u) => ({
      id: `sub-problem-${u + 1}`,
      title: `Component ${u + 1}`,
      description: c.trim(),
      complexity: 0.5,
      estimatedEffort: 10,
      dependencies: u > 0 ? [`sub-problem-${u}`] : [],
      requirements: a ? Object.values(a) : [],
      priority: u === 0 ? "high" : "medium",
      status: "pending",
      metadata: {
        parentProblem: r.substring(0, 100),
        decompositionStrategy: n
      }
    }));
    return i.updateProgress(100, "Local decomposition complete"), this.createSuccessResult({
      subProblems: l,
      dependencyGraph: {
        nodes: l.map((c) => c.id),
        edges: [],
        levels: [l.map((c) => c.id)],
        criticalPath: []
      },
      qualityMetrics: {
        completeness: 0.7,
        clarity: 0.7,
        feasibility: 0.7,
        overall: 0.7
      },
      metadata: {
        originalProblem: r,
        strategyUsed: n,
        executionTime: 0,
        subProblemCount: l.length,
        totalEstimatedEffort: l.reduce((c, u) => c + u.estimatedEffort, 0),
        note: "Executed locally (backend unavailable)"
      }
    });
  }
  /**
   * Assess priority from complexity score
   */
  assessPriorityFromComplexity(r) {
    return r > 0.7 ? "high" : r > 0.4 ? "medium" : "low";
  }
  /**
   * Validate input data
   *
   * @param inputs - Input data to validate
   * @returns Array of validation errors
   */
  validateInputs(r) {
    const n = [], a = r.problem_statement || r.problem;
    if (a || n.push({
      field: "problem_statement",
      message: "Problem statement is required",
      severity: "error"
    }), a && typeof a != "string" && n.push({
      field: "problem_statement",
      message: "Problem must be a string",
      severity: "error"
    }), a && a.length < 20 && n.push({
      field: "problem_statement",
      message: "Problem statement is too short (minimum 20 characters)",
      severity: "warning"
    }), r.requirements && typeof r.requirements != "object" && n.push({
      field: "requirements",
      message: "Requirements must be an object",
      severity: "error"
    }), r.constraints && typeof r.constraints != "object" && n.push({
      field: "constraints",
      message: "Constraints must be an object",
      severity: "error"
    }), r.method && typeof r.method == "string") {
      const i = ["semantic", "dependency", "complexity", "hybrid", "research"];
      i.includes(r.method) || n.push({
        field: "method",
        message: `Method must be one of: ${i.join(", ")}`,
        severity: "error"
      });
    }
    return n;
  }
  /**
   * Get JSON Schema for configuration parameters
   *
   * @returns Parameter schema
   */
  getParameterSchema() {
    return {
      type: "object",
      properties: {
        strategy: {
          type: "string",
          description: "Decomposition strategy to use",
          enum: ["semantic", "dependency", "complexity", "hybrid", "research"],
          default: "hybrid"
        },
        maxSubProblems: {
          type: "number",
          description: "Maximum number of sub-problems to generate",
          minimum: 2,
          maximum: 50,
          default: 10
        },
        qualityThreshold: {
          type: "number",
          description: "Minimum quality score (0-1) for sub-problems",
          minimum: 0,
          maximum: 1,
          default: 0.7
        },
        enableBackendExecution: {
          type: "boolean",
          description: "Use Python backend via integration library",
          default: !0
        },
        backendUrl: {
          type: "string",
          description: "URL of the Python backend API",
          default: "http://localhost:8000"
        }
      },
      required: []
    };
  }
  /**
   * Cleanup when node is destroyed
   */
  destroy() {
    this.client && this.client.disconnect().catch((r) => {
      console.error("Error disconnecting client:", r);
    });
  }
}
Z(da, "DISPLAY_NAME", "Problem Decomposition"), Z(da, "DESCRIPTION", "Break down complex problems using Python DecompositionEngine via integration library"), Z(da, "ICON", "decomposition"), Z(da, "CATEGORY", "analysis"), Z(da, "VERSION", "2.0.0");
const Ht = class Ht extends vr {
  constructor(r, n = {}) {
    super(r, {
      strategy: "Evolutionary",
      maxIterations: 10,
      qualityThreshold: 0.8,
      temperature: 0.7,
      generateAlternatives: !0,
      numAlternatives: 3,
      enableCaching: !0,
      timeoutMs: 3e4,
      ...n
    });
    Z(this, "maxCacheSize", 1e3);
  }
  /**
   * Execute solution generation
   *
   * @param inputs - Must contain 'problem' string
   * @param context - Execution context
   * @returns Promise resolving to solution result
   */
  async execute(r, n) {
    try {
      const a = Date.now(), i = r.problem, o = r.requirements, l = r.constraints, c = r.context;
      if (!i || i.trim().length === 0)
        return this.createErrorResult("Problem statement is required and cannot be empty");
      const u = this.hashProblem(i, o, l);
      if (this.config.enableCaching && Ht.solutionCache.has(u)) {
        const v = Ht.solutionCache.get(u);
        return this.createSuccessResult({
          bestSolution: v,
          allSolutions: [v],
          convergenceMetrics: this.createMockConvergenceMetrics(),
          metadata: {
            problem: i,
            strategyUsed: this.config.strategy,
            totalExecutionTime: Date.now() - a,
            iterationsCompleted: 1,
            cacheHits: 1,
            fromCache: !0
          }
        });
      }
      const d = await this.analyzeProblem(i, c), { solutions: f, convergenceMetrics: p } = await this.generateSolutionsIteratively(
        i,
        d,
        o,
        l,
        a
      ), m = this.selectBestSolution(f);
      let y = [m];
      if (this.config.generateAlternatives && f.length > 1) {
        const v = this.selectAlternatives(f, m);
        m.alternatives = v, y = [m, ...v];
      }
      this.config.enableCaching && this.cacheSolution(u, m);
      const h = Date.now() - a, g = {
        bestSolution: m,
        allSolutions: y,
        convergenceMetrics: p,
        metadata: {
          problem: i,
          strategyUsed: this.config.strategy,
          totalExecutionTime: h,
          iterationsCompleted: p.iterations,
          cacheHits: 0
        }
      };
      return this.createSuccessResult(g);
    } catch (a) {
      return this.createErrorResult(
        a instanceof Error ? a : "Unknown error during solution generation"
      );
    }
  }
  /**
   * Validate input data
   *
   * @param inputs - Input data to validate
   * @returns Array of validation errors
   */
  validateInputs(r) {
    const n = [];
    return r.problem || n.push({
      field: "problem",
      message: "Problem statement is required",
      severity: "error"
    }), r.problem && typeof r.problem != "string" && n.push({
      field: "problem",
      message: "Problem must be a string",
      severity: "error"
    }), r.requirements && !Array.isArray(r.requirements) && n.push({
      field: "requirements",
      message: "Requirements must be an array",
      severity: "error"
    }), r.constraints && !Array.isArray(r.constraints) && n.push({
      field: "constraints",
      message: "Constraints must be an array",
      severity: "error"
    }), n;
  }
  /**
   * Get JSON Schema for configuration parameters
   *
   * @returns Parameter schema
   */
  getParameterSchema() {
    return {
      type: "object",
      properties: {
        strategy: {
          type: "string",
          description: "Solution generation strategy",
          enum: ["MAKER", "MCTS", "Evolutionary", "Hybrid"],
          default: "Evolutionary"
        },
        maxIterations: {
          type: "number",
          description: "Maximum iterations for solution generation",
          minimum: 1,
          maximum: 100,
          default: 10
        },
        qualityThreshold: {
          type: "number",
          description: "Target quality score (0-1)",
          minimum: 0,
          maximum: 1,
          default: 0.8
        },
        temperature: {
          type: "number",
          description: "Temperature for generation randomness (0-1)",
          minimum: 0,
          maximum: 1,
          default: 0.7
        },
        generateAlternatives: {
          type: "boolean",
          description: "Generate alternative solutions",
          default: !0
        },
        numAlternatives: {
          type: "number",
          description: "Number of alternative solutions to generate",
          minimum: 1,
          maximum: 10,
          default: 3
        },
        enableCaching: {
          type: "boolean",
          description: "Enable solution caching for performance",
          default: !0
        },
        timeoutMs: {
          type: "number",
          description: "Timeout for solution generation in milliseconds",
          minimum: 1e3,
          maximum: 3e5,
          default: 3e4
        }
      },
      required: []
    };
  }
  /**
   * Generate solutions iteratively until convergence or max iterations
   *
   * @param problem - Problem statement
   * @param analysis - Problem analysis
   * @param requirements - Optional requirements
   * @param constraints - Optional constraints
   * @param startTime - Start time for timeout
   * @returns Solutions and convergence metrics
   */
  async generateSolutionsIteratively(r, n, a, i, o) {
    const l = this.config.maxIterations, c = this.config.qualityThreshold, u = this.config.timeoutMs, d = this.config.strategy, f = [], p = [];
    let m = 0, y = 0;
    for (y = 0; y < l && !(o && Date.now() - o > u); y++) {
      const g = await this.generateSingleSolution(
        r,
        n,
        y,
        d,
        a,
        i
      ), v = await this.scoreSolution(g, r, a);
      if (f.push(v), p.push(v.qualityScore), v.qualityScore > m && (m = v.qualityScore), m >= c || y > 2 && this.checkConvergence(p))
        break;
    }
    const h = {
      iterations: y + 1,
      qualityHistory: p,
      convergenceRate: this.calculateConvergenceRate(p),
      converged: m >= c,
      finalQuality: m,
      bestIteration: p.indexOf(Math.max(...p))
    };
    return { solutions: f, convergenceMetrics: h };
  }
  /**
   * Generate a single solution
   *
   * @param problem - Problem statement
   * @param analysis - Problem analysis
   * @param iteration - Current iteration
   * @param strategy - Generation strategy
   * @param requirements - Optional requirements
   * @param constraints - Optional constraints
   * @returns Generated solution
   */
  async generateSingleSolution(r, n, a, i, o, l) {
    const c = Date.now();
    this.config.temperature;
    let u;
    switch (i) {
      case "MAKER":
        u = await this.generateMakerSolution(r, n, a);
        break;
      case "MCTS":
        u = await this.generateMCTSSolution(r, n, a);
        break;
      case "Evolutionary":
        u = await this.generateEvolutionarySolution(r, n, a);
        break;
      case "Hybrid":
        u = await this.generateHybridSolution(r, n, a);
        break;
      default:
        u = await this.generateEvolutionarySolution(r, n, a);
    }
    return {
      id: `solution-${a}-${Date.now()}`,
      content: u,
      strategy: i,
      qualityScore: 0,
      // Will be calculated later
      confidence: this.calculateConfidence(a),
      iteration: a,
      metadata: {
        generatedAt: /* @__PURE__ */ new Date(),
        executionTime: Date.now() - c,
        problemHash: this.hashProblem(r, o, l)
      },
      qualityMetrics: {
        completeness: 0,
        correctness: 0,
        clarity: 0,
        efficiency: 0,
        innovation: 0
      }
    };
  }
  /**
   * Score a solution on multiple quality metrics
   *
   * @param solution - Solution to score
   * @param problem - Original problem
   * @param requirements - Optional requirements
   * @returns Scored solution
   */
  async scoreSolution(r, n, a) {
    const i = r.content, o = this.calculateCompleteness(i, n, a), l = this.calculateCorrectness(i, n), c = this.calculateClarity(i), u = this.calculateEfficiency(i), d = this.calculateInnovation(i), f = { completeness: 0.3, correctness: 0.3, clarity: 0.2, efficiency: 0.1, innovation: 0.1 }, p = o * f.completeness + l * f.correctness + c * f.clarity + u * f.efficiency + d * f.innovation;
    return r.qualityScore = p, r.qualityMetrics = {
      completeness: o,
      correctness: l,
      clarity: c,
      efficiency: u,
      innovation: d
    }, r;
  }
  /**
   * Select best solution from array
   *
   * @param solutions - Array of solutions
   * @returns Best solution
   */
  selectBestSolution(r) {
    if (r.length === 0)
      throw new Error("No solutions to select from");
    return r.reduce(
      (n, a) => a.qualityScore > n.qualityScore ? a : n
    );
  }
  /**
   * Select alternative solutions
   *
   * @param solutions - All solutions
   * @param bestSolution - Best solution to exclude
   * @returns Alternative solutions
   */
  selectAlternatives(r, n) {
    const a = Math.min(
      this.config.numAlternatives,
      r.length - 1
    );
    return r.filter((i) => i.id !== n.id).sort((i, o) => o.qualityScore - i.qualityScore).slice(0, a);
  }
  // Strategy-specific solution generation methods
  async generateMakerSolution(r, n, a) {
    return `
## Problem Analysis
${this.extractKeyPoints(r)}

## Solution Approach
1. Define clear objectives
2. Identify key components
3. Design systematic approach
4. Implement with best practices
5. Validate and verify

## Detailed Solution
${this.generateDetailedContent(r, n, a)}

## Validation
- Review against requirements
- Check for edge cases
- Verify completeness

## Implementation Notes
- Follow established patterns
- Document assumptions
- Provide clear explanations
`.trim();
  }
  async generateMCTSSolution(r, n, a) {
    const i = this.generateExplorationPaths(r, a);
    return `
## Problem Space Exploration
${this.analyzeProblemSpace(r)}

## Exploration Paths (Iteration ${a})
${i.map((l, c) => `
### Path ${c + 1}: ${l.title}
- Approach: ${l.approach}
- Expected outcome: ${l.outcome}
- Risk level: ${l.risk}
`).join(`
`)}

## Selected Solution
Based on tree search analysis, the optimal approach is:
${this.generateDetailedContent(r, n, a)}

## Simulation Results
- Paths explored: ${i.length}
- Best path confidence: ${(0.7 + Math.random() * 0.2).toFixed(2)}
- Expected quality: ${(0.6 + Math.random() * 0.3).toFixed(2)}
`.trim();
  }
  async generateEvolutionarySolution(r, n, a) {
    const i = Math.random() * this.config.temperature;
    return `
## Evolution ${a + 1}
- Mutation factor: ${i.toFixed(3)}
- Generation: ${a + 1}
- Target: Solve "${r.substring(0, 50)}..."

## Solution Variant ${a + 1}
${this.generateDetailedContent(r, n, a)}

## Improvements from Previous Iterations
${a > 0 ? `
- Refinement of approach based on feedback
- Enhanced detail and specificity
- Improved clarity and structure
` : "- Initial solution generation"}

## Fitness Metrics
- Completeness: ${(0.5 + Math.random() * 0.4).toFixed(2)}
- Correctness: ${(0.5 + Math.random() * 0.4).toFixed(2)}
- Innovation: ${(0.3 + Math.random() * 0.5).toFixed(2)}
`.trim();
  }
  async generateHybridSolution(r, n, a) {
    return `
## Hybrid Approach - Iteration ${a + 1}
Combining MAKER structure, MCTS exploration, and Evolutionary refinement

### MAKER Component: Structured Analysis
${this.extractKeyPoints(r)}

### MCTS Component: Exploration
${this.analyzeProblemSpace(r)}

### Evolutionary Component: Iterative Improvement
Generation ${a + 1} with adaptive refinement

## Integrated Solution
${this.generateDetailedContent(r, n, a)}

## Hybrid Benefits
- MAKER provides structure and rigor
- MCTS enables exploration of alternatives
- Evolutionary drives continuous improvement
- Synergy from combined approaches
`.trim();
  }
  // Helper methods
  async analyzeProblem(r, n) {
    const a = r.toLowerCase().split(/\s+/), i = new Set(a);
    return {
      wordCount: a.length,
      uniqueWordCount: i.size,
      complexity: Math.min(a.length / 100, 1),
      keywords: this.extractKeywords(r),
      context: n || null
    };
  }
  extractKeyPoints(r) {
    return r.split(/[.!?]+/).filter((a) => a.trim().length > 0).slice(0, 3).join(`
- `);
  }
  generateDetailedContent(r, n, a) {
    const i = [
      `Based on the problem analysis, here is a comprehensive solution:

1. **Understanding the Problem**
   - Analyze requirements and constraints
   - Identify key success factors
   - Define measurable objectives

2. **Proposed Solution**
   - Implement systematic approach
   - Follow best practices
   - Ensure scalability and maintainability

3. **Implementation Steps**
   - Break down into manageable tasks
   - Prioritize critical components
   - Establish milestones and metrics

4. **Validation**
   - Test against requirements
   - Verify edge cases
   - Document outcomes`,
      `Solution approach:

**Core Strategy**
Apply proven methodologies to address the problem systematically.

**Key Actions**
1. Assess current state
2. Design optimal solution
3. Implement incrementally
4. Validate continuously

**Success Criteria**
- All requirements met
- High quality standards
- Efficient execution`,
      `Recommended solution:

**Phase 1: Analysis**
- Thoroughly understand requirements
- Identify constraints and dependencies
- Define success metrics

**Phase 2: Design**
- Create robust architecture
- Plan for scalability
- Consider edge cases

**Phase 3: Implementation**
- Follow best practices
- Maintain code quality
- Document thoroughly

**Phase 4: Validation**
- Comprehensive testing
- Performance optimization
- Stakeholder review`
    ], o = a % i.length;
    return i[o];
  }
  extractKeywords(r) {
    const n = /* @__PURE__ */ new Set(["the", "a", "an", "and", "or", "but", "in", "on", "at", "to", "for"]);
    return r.toLowerCase().split(/\s+/).filter((i) => !n.has(i) && i.length > 3);
  }
  analyzeProblemSpace(r) {
    return `The problem space contains multiple dimensions:
- Complexity: ${Math.min(r.length / 500, 1).toFixed(2)}
- Scope: Broad to narrow approach
- Constraints: Multiple factors to consider
- Opportunities: Several solution paths exist`;
  }
  generateExplorationPaths(r, n) {
    return [
      {
        title: "Direct Approach",
        approach: "Straightforward implementation",
        outcome: "Quick solution, may miss edge cases",
        risk: "Medium"
      },
      {
        title: "Comprehensive Approach",
        approach: "Thorough analysis and planning",
        outcome: "Robust solution, time-intensive",
        risk: "Low"
      },
      {
        title: "Iterative Approach",
        approach: "Incremental development and refinement",
        outcome: "Balanced quality and speed",
        risk: "Low-Medium"
      }
    ].slice(0, 2 + n % 2);
  }
  calculateConfidence(r) {
    return Math.min(0.5 + r * 0.05, 0.95);
  }
  hashProblem(r, n, a) {
    const i = JSON.stringify({ problem: r, requirements: n, constraints: a });
    let o = 0;
    for (let l = 0; l < i.length; l++) {
      const c = i.charCodeAt(l);
      o = (o << 5) - o + c, o = o & o;
    }
    return Math.abs(o).toString(36);
  }
  cacheSolution(r, n) {
    if (Ht.solutionCache.size >= this.maxCacheSize) {
      const a = Ht.solutionCache.keys().next().value;
      Ht.solutionCache.delete(a);
    }
    Ht.solutionCache.set(r, n);
  }
  checkConvergence(r) {
    if (r.length < 3) return !1;
    const n = r.slice(-3);
    return Math.max(...n) - Math.min(...n) < 0.05;
  }
  calculateConvergenceRate(r) {
    if (r.length < 2) return 0;
    const n = r[0];
    return (r[r.length - 1] - n) / r.length;
  }
  createMockConvergenceMetrics() {
    return {
      iterations: 1,
      qualityHistory: [0.8],
      convergenceRate: 0,
      converged: !0,
      finalQuality: 0.8,
      bestIteration: 0
    };
  }
  // Quality metric calculation methods
  calculateCompleteness(r, n, a) {
    let i = 0.5;
    if (r.length > 200 && (i += 0.1), r.length > 500 && (i += 0.1), r.includes("##") && (i += 0.1), r.includes("-") && (i += 0.05), a && a.length > 0) {
      const o = a.filter(
        (l) => r.toLowerCase().includes(l.toLowerCase())
      ).length;
      i += o / a.length * 0.15;
    }
    return Math.min(i, 1);
  }
  calculateCorrectness(r, n) {
    let a = 0.6;
    const i = new Set(n.toLowerCase().split(/\s+/)), l = r.toLowerCase().split(/\s+/).filter((c) => i.has(c)).length;
    return a += Math.min(l * 0.01, 0.2), (r.includes("solution") || r.includes("approach")) && (a += 0.1), (r.includes("implement") || r.includes("execute")) && (a += 0.1), Math.min(a, 1);
  }
  calculateClarity(r) {
    let n = 0.5;
    r.includes("##") && (n += 0.2), r.includes("-") && (n += 0.1);
    const a = r.split(/[.!?]+/), i = a.reduce((o, l) => o + l.length, 0) / a.length;
    return i < 100 ? n += 0.1 : i < 150 && (n += 0.05), Math.min(n, 1);
  }
  calculateEfficiency(r) {
    const i = 300 / r.length;
    return Math.min(i, 1);
  }
  calculateInnovation(r) {
    let n = 0.3;
    return [
      "novel",
      "innovative",
      "creative",
      "unique",
      "original",
      "advanced",
      "cutting-edge",
      "breakthrough",
      "pioneering"
    ].some(
      (o) => r.toLowerCase().includes(o)
    ) && (n += 0.3), r.includes("approach") && r.includes("strategy") && (n += 0.2), n += Math.random() * 0.2, Math.min(n, 1);
  }
};
Z(Ht, "DISPLAY_NAME", "Solution Generation"), Z(Ht, "DESCRIPTION", "Generate solutions for problems using MAKER, MCTS, Evolutionary, or Hybrid strategies with quality tracking"), Z(Ht, "ICON", "solution"), Z(Ht, "CATEGORY", "generation"), Z(Ht, "VERSION", "1.0.0"), // Solution cache for performance
Z(Ht, "solutionCache", /* @__PURE__ */ new Map());
let bm = Ht;
const hn = class hn extends vr {
  constructor(t, r = {}) {
    super(t, {
      threshold: 0.7,
      checks: ["all"],
      strictMode: !1,
      generateSuggestions: !0,
      includeDetails: !0,
      ...r
    });
  }
  /**
   * Execute solution verification
   *
   * @param inputs - Must contain 'solution' and 'requirements'
   * @param context - Execution context
   * @returns Promise resolving to verification result
   */
  async execute(t, r) {
    try {
      const n = Date.now(), a = t.solution, i = t.solutionId, o = t.requirements, l = t.qualityStandards, c = t.problem;
      if (!a || a.trim().length === 0)
        return this.createErrorResult("Solution is required and cannot be empty");
      if (!o || o.length === 0)
        return this.createErrorResult("Requirements array is required and cannot be empty");
      const u = await this.performAllChecks(
        a,
        o,
        c,
        l
      ), d = this.analyzeRequirementsCoverage(
        a,
        o
      ), f = this.calculateQualityMetrics(
        a,
        u,
        d
      ), p = this.identifyIssues(u), m = this.config.generateSuggestions ? this.generateSuggestions(u, d, f) : [], y = this.calculateOverallScore(u, f), h = this.config.threshold, g = y >= h && p.critical.length === 0, v = Date.now() - n, b = {
        solutionId: i || "unknown",
        overallScore: y,
        passed: g,
        checks: u,
        requirements: d,
        qualityMetrics: f,
        issues: p,
        suggestions: m,
        metadata: {
          verifiedAt: /* @__PURE__ */ new Date(),
          verificationTime: v,
          threshold: h,
          verifierVersion: hn.VERSION
        }
      };
      return this.createSuccessResult(b);
    } catch (n) {
      return this.createErrorResult(
        n instanceof Error ? n : "Unknown error during verification"
      );
    }
  }
  /**
   * Validate input data
   *
   * @param inputs - Input data to validate
   * @returns Array of validation errors
   */
  validateInputs(t) {
    const r = [];
    return t.solution || r.push({
      field: "solution",
      message: "Solution is required",
      severity: "error"
    }), t.solution && typeof t.solution != "string" && r.push({
      field: "solution",
      message: "Solution must be a string",
      severity: "error"
    }), t.requirements || r.push({
      field: "requirements",
      message: "Requirements are required",
      severity: "error"
    }), t.requirements && !Array.isArray(t.requirements) && r.push({
      field: "requirements",
      message: "Requirements must be an array",
      severity: "error"
    }), t.solution && t.solution.length < 50 && r.push({
      field: "solution",
      message: "Solution is too short for meaningful verification (minimum 50 characters)",
      severity: "warning"
    }), r;
  }
  /**
   * Get JSON Schema for configuration parameters
   *
   * @returns Parameter schema
   */
  getParameterSchema() {
    return {
      type: "object",
      properties: {
        threshold: {
          type: "number",
          description: "Minimum score (0-1) for solution to pass verification",
          minimum: 0,
          maximum: 1,
          default: 0.7
        },
        checks: {
          type: "array",
          description: "Verification checks to perform",
          items: {
            type: "string",
            enum: ["requirements", "quality", "completeness", "correctness", "consistency", "feasibility", "all"]
          },
          default: ["all"]
        },
        strictMode: {
          type: "boolean",
          description: "Enable strict verification mode",
          default: !1
        },
        generateSuggestions: {
          type: "boolean",
          description: "Generate improvement suggestions",
          default: !0
        },
        includeDetails: {
          type: "boolean",
          description: "Include detailed verification information",
          default: !0
        }
      },
      required: []
    };
  }
  /**
   * Perform all verification checks
   *
   * @param solution - Solution to verify
   * @param requirements - Requirements to verify against
   * @param originalProblem - Original problem statement
   * @param qualityStandards - Quality standards
   * @returns Array of check results
   */
  async performAllChecks(t, r, n, a) {
    const i = this.config.checks, o = i.includes("all") ? ["requirements", "quality", "completeness", "correctness", "consistency", "feasibility"] : i, l = [];
    for (const c of o) {
      const u = await this.performCheck(
        c,
        t,
        r,
        n,
        a
      );
      l.push(u);
    }
    return l;
  }
  /**
   * Perform a single verification check
   *
   * @param check - Check to perform
   * @param solution - Solution to verify
   * @param requirements - Requirements
   * @param originalProblem - Original problem
   * @param qualityStandards - Quality standards
   * @returns Check result
   */
  async performCheck(t, r, n, a, i) {
    switch (t) {
      case "requirements":
        return this.checkRequirements(r, n);
      case "quality":
        return this.checkQuality(r, i);
      case "completeness":
        return this.checkCompleteness(r, n, a);
      case "correctness":
        return this.checkCorrectness(r, a);
      case "consistency":
        return this.checkConsistency(r);
      case "feasibility":
        return this.checkFeasibility(r);
      default:
        return {
          check: t,
          passed: !1,
          score: 0,
          details: ["Unknown check type"],
          severity: "info",
          suggestions: []
        };
    }
  }
  /**
   * Check requirements coverage
   *
   * @param solution - Solution to check
   * @param requirements - Requirements to verify
   * @returns Check result
   */
  checkRequirements(t, r) {
    const n = [], a = [], i = this.config.strictMode;
    let o = 0;
    r.forEach((d) => {
      const f = d.toLowerCase(), p = t.toLowerCase(), m = f.split(/\s+/).filter((v) => v.length > 4), y = m.filter((v) => p.includes(v)).length, h = m.length > 0 ? y / m.length : 0;
      let g;
      h > 0.6 ? (g = "met", o++) : h > 0.3 ? g = "partially met" : g = "not met", (i || g !== "met") && n.push(`Requirement: "${d}" - ${g}`);
    });
    const l = r.length > 0 ? o / r.length : 0, c = l, u = i ? l >= 0.9 : l >= 0.7;
    return u || a.push(
      "Ensure all requirements are explicitly addressed in the solution",
      "Use requirement terminology and language in the solution",
      "Provide clear mappings between requirements and solution components"
    ), {
      check: "requirements",
      passed: u,
      score: c,
      details: this.config.includeDetails ? n : [`Coverage: ${(l * 100).toFixed(1)}%`],
      severity: u ? "info" : l < 0.5 ? "critical" : "major",
      suggestions: a
    };
  }
  /**
   * Check quality standards
   *
   * @param solution - Solution to check
   * @param qualityStandards - Quality standards
   * @returns Check result
   */
  checkQuality(t, r) {
    const n = [], a = [], i = t.includes("##") || t.includes("#"), o = t.includes("-") || t.includes("*"), l = t.includes("```") || t.includes("`"), c = t.length, u = c > 200, d = t.split(/[.!?]+/), p = d.reduce((h, g) => h + g.length, 0) / d.length < 150;
    let m = 0;
    i && (m += 0.25), o && (m += 0.15), l && (m += 0.1), u && (m += 0.25), p && (m += 0.25), n.push(
      `Structure: ${i ? "Present" : "Missing"}`,
      `Lists: ${o ? "Present" : "Missing"}`,
      `Length: ${c} characters ${u ? "(adequate)" : "(too short)"}`,
      `Clarity: ${p ? "Good" : "Needs improvement"}`
    ), m < 0.7 && a.push(
      "Add clear structure with headers and sections",
      "Use bullet points for better readability",
      "Include code examples where appropriate",
      "Break up long sentences for better clarity"
    );
    const y = m >= 0.6;
    return {
      check: "quality",
      passed: y,
      score: m,
      details: n,
      severity: y ? "info" : m < 0.4 ? "major" : "minor",
      suggestions: a
    };
  }
  /**
   * Check completeness
   *
   * @param solution - Solution to check
   * @param requirements - Requirements
   * @param originalProblem - Original problem
   * @returns Check result
   */
  checkCompleteness(t, r, n) {
    const a = [], i = [];
    let o = 0.5;
    const l = n ? t.toLowerCase().includes(n.substring(0, 20).toLowerCase()) : !0;
    l && (o += 0.1);
    const c = /solution|approach|method|strategy/i.test(t);
    c && (o += 0.15);
    const u = /implement|execute|step|phase/i.test(t);
    u && (o += 0.15);
    const d = /test|valid|verif|check|review/i.test(t);
    d && (o += 0.1);
    const f = /conclusion|summary|complete|finish/i.test(t);
    f && (o += 0.1), a.push(
      `Introduction: ${l ? "Present" : "Missing or unclear"}`,
      `Approach: ${c ? "Present" : "Missing"}`,
      `Implementation: ${u ? "Present" : "Missing"}`,
      `Validation: ${d ? "Present" : "Missing"}`,
      `Conclusion: ${f ? "Present" : "Missing"}`
    ), o < 0.7 && i.push(
      "Add a clear introduction restating the problem",
      "Include a detailed approach or methodology section",
      "Provide implementation steps or phases",
      "Add validation or testing considerations",
      "Include a summary or conclusion"
    );
    const p = o >= 0.6;
    return {
      check: "completeness",
      passed: p,
      score: o,
      details: a,
      severity: p ? "info" : o < 0.4 ? "major" : "minor",
      suggestions: i
    };
  }
  /**
   * Check correctness
   *
   * @param solution - Solution to check
   * @param originalProblem - Original problem
   * @returns Check result
   */
  checkCorrectness(t, r) {
    const n = [], a = [];
    let i = 0.6;
    if (r) {
      const u = new Set(r.toLowerCase().split(/\s+/)), p = t.toLowerCase().split(/\s+/).filter((m) => u.has(m)).length / u.size;
      i += p * 0.2;
    }
    const o = /therefore|thus|consequently|because|since|so/i.test(t);
    o && (i += 0.1);
    const l = /solve|address|resolve|fix|improve/i.test(t);
    l && (i += 0.1), n.push(
      `Problem relevance: ${((i - 0.6) / 0.2 * 100).toFixed(0)}%`,
      `Logical flow: ${o ? "Present" : "Could be improved"}`,
      `Problem-solving focus: ${l ? "Present" : "Weak"}`
    ), i < 0.7 && a.push(
      "Ensure the solution directly addresses the original problem",
      "Use clear logical connections between ideas",
      "Maintain focus on problem-solving throughout"
    );
    const c = i >= 0.6;
    return {
      check: "correctness",
      passed: c,
      score: Math.min(i, 1),
      details: n,
      severity: c ? "info" : i < 0.4 ? "major" : "minor",
      suggestions: a
    };
  }
  /**
   * Check consistency
   *
   * @param solution - Solution to check
   * @returns Check result
   */
  checkConsistency(t) {
    const r = [], n = [];
    let a = 0.7;
    const i = t.toLowerCase().split(/\s+/), o = /* @__PURE__ */ new Map();
    i.forEach((m) => {
      m.length > 5 && o.set(m, (o.get(m) || 0) + 1);
    });
    const l = o.size < i.length / 3;
    l && (a += 0.1);
    const c = /^#{1,3}\s/gm.test(t);
    c && (a += 0.1);
    const f = t.split(`
`).filter((m) => m.trim().startsWith("-")).length > 2;
    f && (a += 0.1), r.push(
      `Terminology consistency: ${l ? "Good" : "Could be improved"}`,
      `Header consistency: ${c ? "Present" : "Mixed"}`,
      `Structure consistency: ${f ? "Present" : "Weak"}`
    ), a < 0.8 && n.push(
      "Use consistent terminology throughout the solution",
      "Apply consistent formatting for headers and sections",
      "Maintain consistent structure for similar elements"
    );
    const p = a >= 0.7;
    return {
      check: "consistency",
      passed: p,
      score: a,
      details: r,
      severity: p ? "info" : "minor",
      suggestions: n
    };
  }
  /**
   * Check feasibility
   *
   * @param solution - Solution to check
   * @returns Check result
   */
  checkFeasibility(t) {
    const r = [], n = [];
    let a = 0.7;
    const i = /quickly|soon|immediate|instant|rapid/i.test(t);
    i || (a += 0.1);
    const o = /step|phase|stage|1\.|2\.|3\./i.test(t);
    o && (a += 0.1);
    const l = /resource|tool|technology|framework/i.test(t);
    l && (a += 0.1), r.push(
      `Timeframe realism: ${i ? "May be optimistic" : "Good"}`,
      `Practical steps: ${o ? "Present" : "Could be more detailed"}`,
      `Resource awareness: ${l ? "Present" : "Could be improved"}`
    ), a < 0.8 && n.push(
      "Provide realistic timeframes for implementation",
      "Break down into concrete, actionable steps",
      "Consider resource requirements and constraints"
    );
    const c = a >= 0.7;
    return {
      check: "feasibility",
      passed: c,
      score: a,
      details: r,
      severity: c ? "info" : "minor",
      suggestions: n
    };
  }
  /**
   * Analyze requirements coverage
   *
   * @param solution - Solution to analyze
   * @param requirements - Requirements
   * @returns Requirements analysis
   */
  analyzeRequirementsCoverage(t, r) {
    const n = r, a = [], i = [], o = [], l = t.toLowerCase();
    r.forEach((u) => {
      const f = u.toLowerCase().split(/\s+/).filter((y) => y.length > 4), p = f.filter((y) => l.includes(y)).length, m = f.length > 0 ? p / f.length : 0;
      m > 0.6 ? a.push(u) : m > 0.3 ? i.push(u) : o.push(u);
    });
    const c = n.length > 0 ? a.length / n.length : 0;
    return {
      specified: n,
      met: a,
      partiallyMet: i,
      notMet: o,
      coverage: c
    };
  }
  /**
   * Calculate quality metrics
   *
   * @param solution - Solution
   * @param checks - Check results
   * @param requirementsAnalysis - Requirements analysis
   * @returns Quality metrics
   */
  calculateQualityMetrics(t, r, n) {
    r.find((u) => u.check === "requirements");
    const a = r.find((u) => u.check === "quality"), i = r.find((u) => u.check === "completeness"), o = r.find((u) => u.check === "correctness"), l = r.find((u) => u.check === "consistency"), c = r.find((u) => u.check === "feasibility");
    return {
      completeness: (i == null ? void 0 : i.score) || 0.7,
      correctness: (o == null ? void 0 : o.score) || 0.7,
      clarity: (a == null ? void 0 : a.score) || 0.7,
      consistency: (l == null ? void 0 : l.score) || 0.7,
      feasibility: (c == null ? void 0 : c.score) || 0.7
    };
  }
  /**
   * Identify issues from check results
   *
   * @param checks - Check results
   * @returns Issues organized by severity
   */
  identifyIssues(t) {
    const r = [], n = [], a = [];
    return t.forEach((i) => {
      if (!i.passed) {
        const o = `${i.check}: ${i.details.join(", ")}`;
        i.severity === "critical" ? r.push(o) : i.severity === "major" ? n.push(o) : a.push(o);
      }
    }), { critical: r, major: n, minor: a };
  }
  /**
   * Generate improvement suggestions
   *
   * @param checks - Check results
   * @param requirementsAnalysis - Requirements analysis
   * @param qualityMetrics - Quality metrics
   * @returns Array of suggestions
   */
  generateSuggestions(t, r, n) {
    const a = [];
    t.forEach((o) => {
      a.push(...o.suggestions);
    }), r.notMet.length > 0 && a.push(
      `Explicitly address the ${r.notMet.length} unmet requirement(s)`
    ), r.partiallyMet.length > 0 && a.push(
      `Strengthen coverage of partially met requirements: ${r.partiallyMet.join(", ")}`
    );
    const i = Object.entries(n).filter(([o, l]) => l < 0.7).map(([o, l]) => o);
    return i.length > 0 && a.push(
      `Focus on improving: ${i.join(", ")}`
    ), [...new Set(a)];
  }
  /**
   * Calculate overall verification score
   *
   * @param checks - Check results
   * @param qualityMetrics - Quality metrics
   * @returns Overall score (0-1)
   */
  calculateOverallScore(t, r) {
    const n = t.map((l) => l.score), a = n.reduce((l, c) => l + c, 0) / n.length, i = Object.values(r), o = i.reduce((l, c) => l + c, 0) / i.length;
    return a * 0.7 + o * 0.3;
  }
};
Z(hn, "DISPLAY_NAME", "Solution Verification"), Z(hn, "DESCRIPTION", "Verify solutions against requirements and quality standards with comprehensive reporting"), Z(hn, "ICON", "verification"), Z(hn, "CATEGORY", "verification"), Z(hn, "VERSION", "1.0.0");
let xm = hn;
const us = { BASE_URL: "/", DEV: !1, MODE: "production", PROD: !0, SSR: !1 }, wm = /* @__PURE__ */ new Map(), zo = (e) => {
  const t = wm.get(e);
  return t ? Object.fromEntries(
    Object.entries(t.stores).map(([r, n]) => [r, n.getState()])
  ) : {};
}, fA = (e, t, r) => {
  if (e === void 0)
    return {
      type: "untracked",
      connection: t.connect(r)
    };
  const n = wm.get(r.name);
  if (n)
    return { type: "tracked", store: e, ...n };
  const a = {
    connection: t.connect(r),
    stores: {}
  };
  return wm.set(r.name, a), { type: "tracked", store: e, ...a };
}, mA = (e, t = {}) => (r, n, a) => {
  const { enabled: i, anonymousActionType: o, store: l, ...c } = t;
  let u;
  try {
    u = (i ?? (us ? "production" : void 0) !== "production") && window.__REDUX_DEVTOOLS_EXTENSION__;
  } catch {
  }
  if (!u)
    return (us ? "production" : void 0) !== "production" && i && console.warn(
      "[zustand devtools middleware] Please install/enable Redux devtools extension"
    ), e(r, n, a);
  const { connection: d, ...f } = fA(l, u, c);
  let p = !0;
  a.setState = (h, g, v) => {
    const b = r(h, g);
    if (!p) return b;
    const w = v === void 0 ? { type: o || "anonymous" } : typeof v == "string" ? { type: v } : v;
    return l === void 0 ? (d == null || d.send(w, n()), b) : (d == null || d.send(
      {
        ...w,
        type: `${l}/${w.type}`
      },
      {
        ...zo(c.name),
        [l]: a.getState()
      }
    ), b);
  };
  const m = (...h) => {
    const g = p;
    p = !1, r(...h), p = g;
  }, y = e(a.setState, n, a);
  if (f.type === "untracked" ? d == null || d.init(y) : (f.stores[f.store] = a, d == null || d.init(
    Object.fromEntries(
      Object.entries(f.stores).map(([h, g]) => [
        h,
        h === f.store ? y : g.getState()
      ])
    )
  )), a.dispatchFromDevtools && typeof a.dispatch == "function") {
    let h = !1;
    const g = a.dispatch;
    a.dispatch = (...v) => {
      (us ? "production" : void 0) !== "production" && v[0].type === "__setState" && !h && (console.warn(
        '[zustand devtools middleware] "__setState" action type is reserved to set state from the devtools. Avoid using it.'
      ), h = !0), g(...v);
    };
  }
  return d.subscribe((h) => {
    var g;
    switch (h.type) {
      case "ACTION":
        if (typeof h.payload != "string") {
          console.error(
            "[zustand devtools middleware] Unsupported action format"
          );
          return;
        }
        return ac(
          h.payload,
          (v) => {
            if (v.type === "__setState") {
              if (l === void 0) {
                m(v.state);
                return;
              }
              Object.keys(v.state).length !== 1 && console.error(
                `
                    [zustand devtools middleware] Unsupported __setState action format. 
                    When using 'store' option in devtools(), the 'state' should have only one key, which is a value of 'store' that was passed in devtools(),
                    and value of this only key should be a state object. Example: { "type": "__setState", "state": { "abc123Store": { "foo": "bar" } } }
                    `
              );
              const b = v.state[l];
              if (b == null)
                return;
              JSON.stringify(a.getState()) !== JSON.stringify(b) && m(b);
              return;
            }
            a.dispatchFromDevtools && typeof a.dispatch == "function" && a.dispatch(v);
          }
        );
      case "DISPATCH":
        switch (h.payload.type) {
          case "RESET":
            return m(y), l === void 0 ? d == null ? void 0 : d.init(a.getState()) : d == null ? void 0 : d.init(zo(c.name));
          case "COMMIT":
            if (l === void 0) {
              d == null || d.init(a.getState());
              return;
            }
            return d == null ? void 0 : d.init(zo(c.name));
          case "ROLLBACK":
            return ac(h.state, (v) => {
              if (l === void 0) {
                m(v), d == null || d.init(a.getState());
                return;
              }
              m(v[l]), d == null || d.init(zo(c.name));
            });
          case "JUMP_TO_STATE":
          case "JUMP_TO_ACTION":
            return ac(h.state, (v) => {
              if (l === void 0) {
                m(v);
                return;
              }
              JSON.stringify(a.getState()) !== JSON.stringify(v[l]) && m(v[l]);
            });
          case "IMPORT_STATE": {
            const { nextLiftedState: v } = h.payload, b = (g = v.computedStates.slice(-1)[0]) == null ? void 0 : g.state;
            if (!b) return;
            m(l === void 0 ? b : b[l]), d == null || d.send(
              null,
              // FIXME no-any
              v
            );
            return;
          }
          case "PAUSE_RECORDING":
            return p = !p;
        }
        return;
    }
  }), y;
}, Yh = mA, ac = (e, t) => {
  let r;
  try {
    r = JSON.parse(e);
  } catch (n) {
    console.error(
      "[zustand devtools middleware] Could not parse the received json",
      n
    );
  }
  r !== void 0 && t(r);
};
function hA(e, t) {
  let r;
  try {
    r = e();
  } catch {
    return;
  }
  return {
    getItem: (a) => {
      var i;
      const o = (c) => c === null ? null : JSON.parse(c, void 0), l = (i = r.getItem(a)) != null ? i : null;
      return l instanceof Promise ? l.then(o) : o(l);
    },
    setItem: (a, i) => r.setItem(
      a,
      JSON.stringify(i, void 0)
    ),
    removeItem: (a) => r.removeItem(a)
  };
}
const qi = (e) => (t) => {
  try {
    const r = e(t);
    return r instanceof Promise ? r : {
      then(n) {
        return qi(n)(r);
      },
      catch(n) {
        return this;
      }
    };
  } catch (r) {
    return {
      then(n) {
        return this;
      },
      catch(n) {
        return qi(n)(r);
      }
    };
  }
}, pA = (e, t) => (r, n, a) => {
  let i = {
    getStorage: () => localStorage,
    serialize: JSON.stringify,
    deserialize: JSON.parse,
    partialize: (g) => g,
    version: 0,
    merge: (g, v) => ({
      ...v,
      ...g
    }),
    ...t
  }, o = !1;
  const l = /* @__PURE__ */ new Set(), c = /* @__PURE__ */ new Set();
  let u;
  try {
    u = i.getStorage();
  } catch {
  }
  if (!u)
    return e(
      (...g) => {
        console.warn(
          `[zustand persist middleware] Unable to update item '${i.name}', the given storage is currently unavailable.`
        ), r(...g);
      },
      n,
      a
    );
  const d = qi(i.serialize), f = () => {
    const g = i.partialize({ ...n() });
    let v;
    const b = d({ state: g, version: i.version }).then(
      (w) => u.setItem(i.name, w)
    ).catch((w) => {
      v = w;
    });
    if (v)
      throw v;
    return b;
  }, p = a.setState;
  a.setState = (g, v) => {
    p(g, v), f();
  };
  const m = e(
    (...g) => {
      r(...g), f();
    },
    n,
    a
  );
  let y;
  const h = () => {
    var g;
    if (!u) return;
    o = !1, l.forEach((b) => b(n()));
    const v = ((g = i.onRehydrateStorage) == null ? void 0 : g.call(i, n())) || void 0;
    return qi(u.getItem.bind(u))(i.name).then((b) => {
      if (b)
        return i.deserialize(b);
    }).then((b) => {
      if (b)
        if (typeof b.version == "number" && b.version !== i.version) {
          if (i.migrate)
            return i.migrate(
              b.state,
              b.version
            );
          console.error(
            "State loaded from storage couldn't be migrated since no migrate function was provided"
          );
        } else
          return b.state;
    }).then((b) => {
      var w;
      return y = i.merge(
        b,
        (w = n()) != null ? w : m
      ), r(y, !0), f();
    }).then(() => {
      v == null || v(y, void 0), o = !0, c.forEach((b) => b(y));
    }).catch((b) => {
      v == null || v(void 0, b);
    });
  };
  return a.persist = {
    setOptions: (g) => {
      i = {
        ...i,
        ...g
      }, g.getStorage && (u = g.getStorage());
    },
    clearStorage: () => {
      u == null || u.removeItem(i.name);
    },
    getOptions: () => i,
    rehydrate: () => h(),
    hasHydrated: () => o,
    onHydrate: (g) => (l.add(g), () => {
      l.delete(g);
    }),
    onFinishHydration: (g) => (c.add(g), () => {
      c.delete(g);
    })
  }, h(), y || m;
}, gA = (e, t) => (r, n, a) => {
  let i = {
    storage: hA(() => localStorage),
    partialize: (h) => h,
    version: 0,
    merge: (h, g) => ({
      ...g,
      ...h
    }),
    ...t
  }, o = !1;
  const l = /* @__PURE__ */ new Set(), c = /* @__PURE__ */ new Set();
  let u = i.storage;
  if (!u)
    return e(
      (...h) => {
        console.warn(
          `[zustand persist middleware] Unable to update item '${i.name}', the given storage is currently unavailable.`
        ), r(...h);
      },
      n,
      a
    );
  const d = () => {
    const h = i.partialize({ ...n() });
    return u.setItem(i.name, {
      state: h,
      version: i.version
    });
  }, f = a.setState;
  a.setState = (h, g) => {
    f(h, g), d();
  };
  const p = e(
    (...h) => {
      r(...h), d();
    },
    n,
    a
  );
  a.getInitialState = () => p;
  let m;
  const y = () => {
    var h, g;
    if (!u) return;
    o = !1, l.forEach((b) => {
      var w;
      return b((w = n()) != null ? w : p);
    });
    const v = ((g = i.onRehydrateStorage) == null ? void 0 : g.call(i, (h = n()) != null ? h : p)) || void 0;
    return qi(u.getItem.bind(u))(i.name).then((b) => {
      if (b)
        if (typeof b.version == "number" && b.version !== i.version) {
          if (i.migrate)
            return [
              !0,
              i.migrate(
                b.state,
                b.version
              )
            ];
          console.error(
            "State loaded from storage couldn't be migrated since no migrate function was provided"
          );
        } else
          return [!1, b.state];
      return [!1, void 0];
    }).then((b) => {
      var w;
      const [j, x] = b;
      if (m = i.merge(
        x,
        (w = n()) != null ? w : p
      ), r(m, !0), j)
        return d();
    }).then(() => {
      v == null || v(m, void 0), m = n(), o = !0, c.forEach((b) => b(m));
    }).catch((b) => {
      v == null || v(void 0, b);
    });
  };
  return a.persist = {
    setOptions: (h) => {
      i = {
        ...i,
        ...h
      }, h.storage && (u = h.storage);
    },
    clearStorage: () => {
      u == null || u.removeItem(i.name);
    },
    getOptions: () => i,
    rehydrate: () => y(),
    hasHydrated: () => o,
    onHydrate: (h) => (l.add(h), () => {
      l.delete(h);
    }),
    onFinishHydration: (h) => (c.add(h), () => {
      c.delete(h);
    })
  }, i.skipHydration || y(), m || p;
}, yA = (e, t) => "getStorage" in t || "serialize" in t || "deserialize" in t ? ((us ? "production" : void 0) !== "production" && console.warn(
  "[DEPRECATED] `getStorage`, `serialize` and `deserialize` options are deprecated. Use `storage` option instead."
), pA(e, t)) : gA(e, t), Xh = yA, fs = pl()(
  Xh(
    (e, t) => ({
      user: null,
      token: null,
      refreshToken: null,
      isAuthenticated: !1,
      isLoading: !1,
      error: null,
      login: async (r, n) => {
        e({ isLoading: !0, error: null });
        try {
          const a = await fetch("/api/v1/auth/login", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ email: r, password: n })
          });
          if (!a.ok) {
            const o = await a.json();
            throw new Error(o.message || "Login failed");
          }
          const i = await a.json();
          if (e({
            user: i.user || null,
            // API might not return user in login response
            token: i.access_token,
            refreshToken: i.refresh_token,
            isAuthenticated: !0,
            isLoading: !1
          }), !i.user) {
            const o = await fetch("/api/v1/users/me", {
              headers: {
                Authorization: `Bearer ${i.access_token}`
              }
            });
            if (o.ok) {
              const l = await o.json();
              e({ user: l });
            }
          }
        } catch (a) {
          throw e({
            error: a instanceof Error ? a.message : "Login failed",
            isLoading: !1,
            isAuthenticated: !1
          }), a;
        }
      },
      logout: async () => {
        e({ isLoading: !0 });
        try {
          const r = t().token;
          r && await fetch("/api/v1/auth/logout", {
            method: "POST",
            headers: {
              Authorization: `Bearer ${r}`
            }
          });
        } catch (r) {
          console.error("Logout error:", r);
        } finally {
          e({
            user: null,
            token: null,
            refreshToken: null,
            isAuthenticated: !1,
            isLoading: !1
          });
        }
      },
      register: async (r, n, a, i) => {
        e({ isLoading: !0, error: null });
        try {
          const o = await fetch("/api/v1/auth/register", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ email: r, password: n, username: a, full_name: i })
          });
          if (!o.ok) {
            const l = await o.json();
            throw new Error(l.message || "Registration failed");
          }
          e({ isLoading: !1 });
        } catch (o) {
          throw e({
            error: o instanceof Error ? o.message : "Registration failed",
            isLoading: !1
          }), o;
        }
      },
      refreshAuthToken: async () => {
        const r = t().refreshToken;
        if (!r)
          throw new Error("No refresh token available");
        try {
          const n = await fetch("/api/v1/auth/refresh", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ refresh_token: r })
          });
          if (!n.ok)
            throw new Error("Token refresh failed");
          const a = await n.json();
          e({
            token: a.access_token
          });
        } catch (n) {
          throw await t().logout(), n;
        }
      },
      updateUser: (r) => {
        const n = t().user;
        n && e({ user: { ...n, ...r } });
      },
      clearError: () => e({ error: null }),
      setLoading: (r) => e({ isLoading: r })
    }),
    {
      name: "auth-storage",
      partialize: (e) => ({
        user: e.user,
        token: e.token,
        refreshToken: e.refreshToken,
        isAuthenticated: e.isAuthenticated
      })
    }
  )
), ic = {
  baseURL: "/api/v1",
  timeout: 3e4,
  headers: {
    "Content-Type": "application/json"
  }
};
class vA {
  constructor() {
    Z(this, "baseURL");
    Z(this, "timeout");
    Z(this, "defaultHeaders");
    this.baseURL = ic.baseURL, this.timeout = ic.timeout, this.defaultHeaders = ic.headers;
  }
  /**
   * Get authentication headers
   */
  getAuthHeaders() {
    const t = fs.getState().token;
    return t ? { Authorization: `Bearer ${t}` } : {};
  }
  /**
   * Handle API errors
   */
  async handleResponse(t) {
    var r, n, a;
    if (!t.ok) {
      const i = await t.json().catch(() => ({}));
      throw {
        code: ((r = i.error) == null ? void 0 : r.code) || "API_ERROR",
        message: ((n = i.error) == null ? void 0 : n.message) || t.statusText,
        details: (a = i.error) == null ? void 0 : a.details
      };
    }
    return t.status === 204 ? null : t.json();
  }
  /**
   * Make a request with timeout
   */
  async requestWithTimeout(t, r) {
    const n = new AbortController(), a = setTimeout(() => n.abort(), this.timeout);
    try {
      const i = await fetch(t, {
        ...r,
        signal: n.signal
      });
      return clearTimeout(a), i;
    } catch (i) {
      throw clearTimeout(a), i instanceof Error && i.name === "AbortError" ? new Error("Request timeout") : i;
    }
  }
  /**
   * Build full URL
   */
  buildUrl(t, r) {
    const n = new URL(t, this.baseURL);
    return r && Object.entries(r).forEach(([a, i]) => {
      i != null && n.searchParams.append(a, String(i));
    }), n.toString();
  }
  /**
   * GET request
   */
  async get(t, r) {
    const n = this.buildUrl(t, r), a = await this.requestWithTimeout(n, {
      method: "GET",
      headers: {
        ...this.defaultHeaders,
        ...this.getAuthHeaders()
      }
    });
    return this.handleResponse(a);
  }
  /**
   * POST request
   */
  async post(t, r) {
    const n = this.buildUrl(t), a = await this.requestWithTimeout(n, {
      method: "POST",
      headers: {
        ...this.defaultHeaders,
        ...this.getAuthHeaders()
      },
      body: JSON.stringify(r)
    });
    return this.handleResponse(a);
  }
  /**
   * PUT request
   */
  async put(t, r) {
    const n = this.buildUrl(t), a = await this.requestWithTimeout(n, {
      method: "PUT",
      headers: {
        ...this.defaultHeaders,
        ...this.getAuthHeaders()
      },
      body: JSON.stringify(r)
    });
    return this.handleResponse(a);
  }
  /**
   * PATCH request
   */
  async patch(t, r) {
    const n = this.buildUrl(t), a = await this.requestWithTimeout(n, {
      method: "PATCH",
      headers: {
        ...this.defaultHeaders,
        ...this.getAuthHeaders()
      },
      body: JSON.stringify(r)
    });
    return this.handleResponse(a);
  }
  /**
   * DELETE request
   */
  async delete(t) {
    const r = this.buildUrl(t), n = await this.requestWithTimeout(r, {
      method: "DELETE",
      headers: {
        ...this.defaultHeaders,
        ...this.getAuthHeaders()
      }
    });
    return this.handleResponse(n);
  }
  /**
   * File upload
   */
  async uploadFile(t, r, n) {
    const a = this.buildUrl(t), i = new FormData();
    i.append("file", r);
    const o = new XMLHttpRequest();
    return new Promise((l, c) => {
      o.upload.addEventListener("progress", (u) => {
        if (u.lengthComputable && n) {
          const d = u.loaded / u.total * 100;
          n(d);
        }
      }), o.addEventListener("load", () => {
        if (o.status >= 200 && o.status < 300) {
          const u = JSON.parse(o.responseText);
          l(u);
        } else {
          const u = JSON.parse(o.responseText);
          c(u);
        }
      }), o.addEventListener("error", () => {
        c(new Error("Upload failed"));
      }), o.open("POST", a), o.setRequestHeader("Authorization", `Bearer ${fs.getState().token}`), o.send(i);
    });
  }
  /**
   * File download
   */
  async downloadFile(t, r) {
    const n = this.buildUrl(t), a = await this.requestWithTimeout(n, {
      method: "GET",
      headers: {
        ...this.getAuthHeaders()
      }
    });
    if (!a.ok)
      throw new Error("Download failed");
    const i = await a.blob(), o = window.URL.createObjectURL(i), l = document.createElement("a");
    l.href = o, l.download = r || "download", document.body.appendChild(l), l.click(), document.body.removeChild(l), window.URL.revokeObjectURL(o);
  }
}
const Se = new vA(), mt = {
  /**
   * Start evolution
   */
  start: (e) => Se.post("/evolution/start", e),
  /**
   * Get evolution status
   */
  getStatus: (e) => Se.get(`/evolution/${e}`),
  /**
   * Pause evolution
   */
  pause: (e) => Se.post(
    `/evolution/${e}/pause`,
    {}
  ),
  /**
   * Resume evolution
   */
  resume: (e) => Se.post(
    `/evolution/${e}/resume`,
    {}
  ),
  /**
   * Stop evolution
   */
  stop: (e) => Se.post(`/evolution/${e}/stop`, {}),
  /**
   * Delete evolution
   */
  delete: (e) => Se.delete(`/evolution/${e}`),
  /**
   * List evolutions
   */
  list: (e) => Se.get("/evolution", e)
}, Vr = {
  /**
   * Start adversarial test
   */
  start: (e) => Se.post("/adversarial/start", e),
  /**
   * Get adversarial test status
   */
  getStatus: (e) => Se.get(`/adversarial/${e}`),
  /**
   * Approve or reject patch
   */
  approvePatch: (e, t) => Se.post(
    `/adversarial/${e}/approve-patch`,
    t
  ),
  /**
   * Stop adversarial test
   */
  stop: (e) => Se.post(
    `/adversarial/${e}/stop`,
    {}
  ),
  /**
   * List adversarial tests
   */
  list: (e) => Se.get("/adversarial", e)
}, fg = {
  /**
   * Get metrics
   */
  getMetrics: (e) => Se.get("/analytics/metrics", e),
  /**
   * Get performance analytics
   */
  getPerformance: () => Se.get("/analytics/performance")
}, bA = {
  /**
   * Get system health
   */
  getHealth: () => Se.get("/monitoring/health"),
  /**
   * Get application logs
   */
  getLogs: (e) => Se.get("/monitoring/logs", e)
}, Bo = {
  /**
   * Get available providers
   */
  getProviders: () => Se.get("/config/providers"),
  /**
   * Save API key for provider
   */
  saveApiKey: (e, t) => Se.post(`/config/providers/${e}/api-key`, { api_key: t }),
  /**
   * Get user parameters
   */
  getParameters: () => Se.get("/config/parameters"),
  /**
   * Update user parameters
   */
  updateParameters: (e) => Se.put("/config/parameters", e)
}, fr = {
  /**
   * Generate Lean 4 proof
   */
  generateProof: (e) => Se.post("/leanaide/generate", e),
  /**
   * Verify Lean 4 proof
   */
  verifyProof: (e) => Se.post("/leanaide/verify", { code: e }),
  /**
   * Get supported models
   */
  getModels: () => Se.get("/leanaide/models"),
  /**
   * Run benchmark
   */
  runBenchmark: (e) => Se.post(
    "/leanaide/benchmark/start",
    e
  ),
  /**
   * Get benchmark results
   */
  getBenchmarkResults: (e) => Se.get(`/leanaide/benchmark/${e}/results`)
};
class xA {
  constructor(t, r) {
    Z(this, "ws", null);
    Z(this, "url");
    Z(this, "reconnectInterval");
    Z(this, "maxReconnectAttempts");
    Z(this, "heartbeatInterval");
    Z(this, "protocols");
    Z(this, "reconnectAttempts", 0);
    Z(this, "reconnectTimeout", null);
    Z(this, "heartbeatTimeout", null);
    Z(this, "handlers");
    Z(this, "connectionState", "disconnected");
    Z(this, "isManualClose", !1);
    /**
     * Handle WebSocket open event
     */
    Z(this, "handleOpen", () => {
      var t, r;
      this.setConnectionState("connected"), this.reconnectAttempts = 0, this.clearReconnectTimeout(), this.startHeartbeat(), (r = (t = this.handlers).onMessage) == null || r.call(t, {
        type: "connected",
        data: { url: this.url }
      });
    });
    /**
     * Handle WebSocket message event
     */
    Z(this, "handleMessage", (t) => {
      var r, n;
      try {
        const a = JSON.parse(t.data);
        (n = (r = this.handlers).onMessage) == null || n.call(r, a);
      } catch (a) {
        console.error("Failed to parse WebSocket message:", a);
      }
    });
    /**
     * Handle WebSocket error event
     */
    Z(this, "handleError", (t) => {
      var r, n;
      console.error("WebSocket error:", t), (n = (r = this.handlers).onError) == null || n.call(r, new Error("WebSocket connection error")), this.setConnectionState("error");
    });
    /**
     * Handle WebSocket close event
     */
    Z(this, "handleClose", () => {
      var t, r;
      this.setConnectionState("disconnected"), this.clearHeartbeat(), (r = (t = this.handlers).onMessage) == null || r.call(t, {
        type: "disconnected",
        data: { url: this.url }
      }), this.isManualClose || this.scheduleReconnect();
    });
    this.url = t.url, this.reconnectInterval = t.reconnectInterval || 3e3, this.maxReconnectAttempts = t.maxReconnectAttempts || 10, this.heartbeatInterval = t.heartbeatInterval || 3e4, this.protocols = t.protocols, this.handlers = r;
  }
  /**
   * Connect to WebSocket server
   */
  connect() {
    var t, r;
    if (!(this.ws && this.ws.readyState === WebSocket.OPEN)) {
      this.isManualClose = !1, this.setConnectionState("connecting");
      try {
        this.ws = new WebSocket(this.url, this.protocols), this.ws.onopen = this.handleOpen, this.ws.onmessage = this.handleMessage, this.ws.onerror = this.handleError, this.ws.onclose = this.handleClose;
      } catch (n) {
        (r = (t = this.handlers).onError) == null || r.call(t, n), this.setConnectionState("error");
      }
    }
  }
  /**
   * Disconnect from WebSocket server
   */
  disconnect() {
    this.isManualClose = !0, this.clearReconnectTimeout(), this.clearHeartbeat(), this.ws && (this.ws.close(), this.ws = null), this.setConnectionState("disconnected");
  }
  /**
   * Send message through WebSocket
   */
  send(t, r) {
    if (this.ws && this.ws.readyState === WebSocket.OPEN) {
      const n = {
        type: t,
        data: r,
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
      this.ws.send(JSON.stringify(n));
    } else
      console.warn("WebSocket is not connected. Cannot send message.");
  }
  /**
   * Schedule reconnection attempt
   */
  scheduleReconnect() {
    if (this.reconnectAttempts >= this.maxReconnectAttempts) {
      console.error("Max reconnect attempts reached");
      return;
    }
    this.clearReconnectTimeout(), this.reconnectTimeout = setTimeout(() => {
      this.reconnectAttempts++, console.log(`Reconnection attempt ${this.reconnectAttempts}/${this.maxReconnectAttempts}`), this.connect();
    }, this.reconnectInterval);
  }
  /**
   * Clear reconnect timeout
   */
  clearReconnectTimeout() {
    this.reconnectTimeout && (clearTimeout(this.reconnectTimeout), this.reconnectTimeout = null);
  }
  /**
   * Start heartbeat
   */
  startHeartbeat() {
    this.clearHeartbeat(), this.heartbeatTimeout = setInterval(() => {
      this.ws && this.ws.readyState === WebSocket.OPEN && this.ws.send(JSON.stringify({ type: "ping" }));
    }, this.heartbeatInterval);
  }
  /**
   * Clear heartbeat
   */
  clearHeartbeat() {
    this.heartbeatTimeout && (clearInterval(this.heartbeatTimeout), this.heartbeatTimeout = null);
  }
  /**
   * Set connection state and notify handlers
   */
  setConnectionState(t) {
    var r, n;
    this.connectionState = t, (n = (r = this.handlers).onConnectionChange) == null || n.call(r, t);
  }
  /**
   * Get current connection state
   */
  getConnectionState() {
    return this.connectionState;
  }
  /**
   * Check if connected
   */
  isConnected() {
    return this.connectionState === "connected" && this.ws !== null && this.ws.readyState === WebSocket.OPEN;
  }
}
function wA(e, t) {
  const r = `${_A()}/ws/evolution/${e}`;
  return new xA({ url: r }, t);
}
function _A() {
  return "/api/v1".replace(/^http/, "ws");
}
class fa extends vr {
  constructor(t, r = {}) {
    super(t, {
      mode: "standard",
      maxIterations: 10,
      populationSize: 5,
      temperature: 0.7,
      topP: 0.9,
      timeoutMs: 3e5,
      // 5 minutes
      enableWebSocket: !1,
      ...r
    });
  }
  /**
   * Execute evolution process
   *
   * @param inputs - Must contain 'content' and optionally 'models' configuration
   * @param context - Execution context
   * @returns Promise resolving to evolution result
   */
  async execute(t, r) {
    try {
      const n = Date.now(), a = t.content, i = t.models, o = t.mode || this.config.mode, l = t.parameters;
      if (!a || a.trim().length === 0)
        return this.createErrorResult("Content is required and cannot be empty");
      r.updateProgress(10, "Preparing evolution parameters");
      const c = {
        max_iterations: (l == null ? void 0 : l.maxIterations) || this.config.maxIterations,
        population_size: (l == null ? void 0 : l.populationSize) || this.config.populationSize,
        temperature: (l == null ? void 0 : l.temperature) || this.config.temperature,
        top_p: (l == null ? void 0 : l.topP) || this.config.topP
      };
      r.updateProgress(20, "Starting evolution process");
      const d = (await mt.start({
        content: a,
        mode: o,
        parameters: c,
        models: i || []
      })).evolution_id;
      r.updateProgress(30, "Evolution started, monitoring progress");
      const f = await this.monitorEvolution(d, r), p = Date.now() - n, m = {
        evolutionId: d,
        status: f.status,
        mode: o,
        bestContent: f.best_content || a,
        bestFitness: f.best_fitness || 0,
        generations: f.generations || 0,
        populationMetrics: f.population_metrics || [],
        finalPopulation: f.final_population || [],
        metadata: {
          startedAt: new Date(f.started_at || n),
          completedAt: f.completed_at ? new Date(f.completed_at) : void 0,
          executionTime: p,
          parameters: c
        }
      };
      return r.updateProgress(100, `Evolution complete: ${m.generations} generations`), this.createSuccessResult(m);
    } catch (n) {
      return this.createErrorResult(
        n instanceof Error ? n.message : "Unknown error during evolution"
      );
    }
  }
  /**
   * Monitor evolution progress until completion
   *
   * @param evolutionId - Evolution ID to monitor
   * @param context - Execution context
   * @returns Promise resolving to evolution status
   */
  async monitorEvolution(t, r) {
    let a = 0;
    const i = this.config.timeoutMs, o = Date.now();
    for (; a < 60; ) {
      if (Date.now() - o > i)
        throw new Error("Evolution monitoring timeout exceeded");
      try {
        const l = await mt.getStatus(t), c = l.current_iteration && l.total_iterations ? l.current_iteration / l.total_iterations * 80 + 20 : 30 + a / 60 * 70;
        if (r.updateProgress(
          Math.min(c, 95),
          `Generation ${l.current_iteration || 0}/${l.total_iterations || "?"}`
        ), l.status === "completed" || l.status === "failed")
          return l;
        await new Promise((u) => setTimeout(u, 5e3)), a++;
      } catch {
        await new Promise((c) => setTimeout(c, 5e3)), a++;
      }
    }
    throw new Error("Evolution did not complete within the expected time");
  }
  /**
   * Validate input data
   *
   * @param inputs - Input data to validate
   * @returns Array of validation errors
   */
  validateInputs(t) {
    const r = [];
    if (t.content || r.push({
      field: "content",
      message: "Content is required",
      severity: "error"
    }), t.content && typeof t.content != "string" && r.push({
      field: "content",
      message: "Content must be a string",
      severity: "error"
    }), t.content && t.content.length < 50 && r.push({
      field: "content",
      message: "Content is too short for meaningful evolution (minimum 50 characters)",
      severity: "warning"
    }), t.mode && typeof t.mode == "string") {
      const n = ["standard", "quality_diversity", "island_model"];
      n.includes(t.mode) || r.push({
        field: "mode",
        message: `Mode must be one of: ${n.join(", ")}`,
        severity: "error"
      });
    }
    return t.parameters && typeof t.parameters != "object" && r.push({
      field: "parameters",
      message: "Parameters must be an object",
      severity: "error"
    }), r;
  }
  /**
   * Get JSON Schema for configuration parameters
   *
   * @returns Parameter schema
   */
  getParameterSchema() {
    return {
      type: "object",
      properties: {
        mode: {
          type: "string",
          description: "Evolution strategy mode",
          enum: ["standard", "quality_diversity", "island_model"],
          default: "standard"
        },
        maxIterations: {
          type: "number",
          description: "Maximum number of evolution generations",
          minimum: 1,
          maximum: 100,
          default: 10
        },
        populationSize: {
          type: "number",
          description: "Size of the population in each generation",
          minimum: 2,
          maximum: 20,
          default: 5
        },
        temperature: {
          type: "number",
          description: "Temperature for content generation (0-1)",
          minimum: 0,
          maximum: 1,
          default: 0.7
        },
        topP: {
          type: "number",
          description: "Top-p sampling parameter (0-1)",
          minimum: 0,
          maximum: 1,
          default: 0.9
        },
        timeoutMs: {
          type: "number",
          description: "Timeout for evolution in milliseconds",
          minimum: 1e4,
          maximum: 6e5,
          default: 3e5
        },
        enableWebSocket: {
          type: "boolean",
          description: "Enable WebSocket for real-time updates",
          default: !1
        }
      },
      required: []
    };
  }
  /**
   * Pause running evolution
   *
   * @param evolutionId - Evolution ID to pause
   * @returns Promise resolving to pause result
   */
  async pauseEvolution(t) {
    try {
      const r = await mt.pause(t);
      return this.createSuccessResult(r);
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to pause evolution"
      );
    }
  }
  /**
   * Resume paused evolution
   *
   * @param evolutionId - Evolution ID to resume
   * @returns Promise resolving to resume result
   */
  async resumeEvolution(t) {
    try {
      const r = await mt.resume(t);
      return this.createSuccessResult(r);
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to resume evolution"
      );
    }
  }
  /**
   * Stop running evolution
   *
   * @param evolutionId - Evolution ID to stop
   * @returns Promise resolving to stop result with final results
   */
  async stopEvolution(t) {
    try {
      const r = await mt.stop(t);
      return this.createSuccessResult(r);
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to stop evolution"
      );
    }
  }
  /**
   * Delete evolution
   *
   * @param evolutionId - Evolution ID to delete
   * @returns Promise resolving to deletion result
   */
  async deleteEvolution(t) {
    try {
      return await mt.delete(t), this.createSuccessResult({ evolutionId: t, deleted: !0 });
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to delete evolution"
      );
    }
  }
}
Z(fa, "DISPLAY_NAME", "Evolution Engine"), Z(fa, "DESCRIPTION", "Genetic algorithm evolution for iterative content improvement with multiple strategies"), Z(fa, "ICON", "evolution"), Z(fa, "CATEGORY", "optimization"), Z(fa, "VERSION", "1.0.0");
class ma extends vr {
  constructor(t, r = {}) {
    super(t, {
      attackModes: ["prompt_injection", "jailbreak"],
      numRounds: 5,
      redTeamModels: [],
      blueTeamModels: [],
      timeoutMs: 3e5,
      // 5 minutes
      enableAutoPatch: !1,
      ...r
    });
  }
  /**
   * Execute adversarial testing
   *
   * @param inputs - Must contain 'content' to test
   * @param context - Execution context
   * @returns Promise resolving to adversarial test result
   */
  async execute(t, r) {
    var n, a, i, o;
    try {
      const l = Date.now(), c = t.content, u = t.attackModes || this.config.attackModes, d = t.numRounds || this.config.numRounds, f = t.redTeamModels, p = t.blueTeamModels;
      if (!c || c.trim().length === 0)
        return this.createErrorResult("Content is required and cannot be empty");
      if (!u || u.length === 0)
        return this.createErrorResult("At least one attack mode must be specified");
      r.updateProgress(10, "Preparing adversarial test configuration");
      const m = {
        num_rounds: d,
        red_team_models: f || this.config.redTeamModels,
        blue_team_models: p || this.config.blueTeamModels
      };
      r.updateProgress(20, "Starting adversarial test");
      const h = (await Vr.start({
        content: c,
        attack_modes: u,
        parameters: m
      })).test_id;
      r.updateProgress(30, "Adversarial test started, monitoring progress");
      const g = await this.monitorTest(h, u, r), v = Date.now() - l, b = this.generateRecommendations(g), w = {
        testId: h,
        status: g.status,
        content: c,
        attackModes: u,
        rounds: g.rounds || 0,
        attacks: g.attacks || [],
        patches: g.patches || [],
        summary: {
          totalAttacks: ((n = g.attacks) == null ? void 0 : n.length) || 0,
          successfulAttacks: ((a = g.attacks) == null ? void 0 : a.filter((j) => j.success).length) || 0,
          vulnerabilitiesFound: g.vulnerabilities_found || 0,
          patchesProposed: ((i = g.patches) == null ? void 0 : i.length) || 0,
          patchesApplied: ((o = g.patches) == null ? void 0 : o.filter((j) => j.approved).length) || 0,
          overallRobustness: this.calculateRobustness(g)
        },
        recommendations: b,
        metadata: {
          startedAt: new Date(g.started_at || l),
          completedAt: g.completed_at ? new Date(g.completed_at) : void 0,
          executionTime: v,
          redTeamModels: m.red_team_models,
          blueTeamModels: m.blue_team_models
        }
      };
      return r.updateProgress(100, `Adversarial testing complete: ${w.summary.vulnerabilitiesFound} vulnerabilities found`), this.createSuccessResult(w);
    } catch (l) {
      return this.createErrorResult(
        l instanceof Error ? l.message : "Unknown error during adversarial testing"
      );
    }
  }
  /**
   * Monitor adversarial test progress until completion
   *
   * @param testId - Test ID to monitor
   * @param attackModes - Attack modes being tested
   * @param context - Execution context
   * @returns Promise resolving to test status
   */
  async monitorTest(t, r, n) {
    let i = 0;
    const o = this.config.timeoutMs, l = Date.now();
    for (; i < 60; ) {
      if (Date.now() - l > o)
        throw new Error("Adversarial test monitoring timeout exceeded");
      try {
        const c = await Vr.getStatus(t), u = c.current_round && c.total_rounds ? c.current_round / c.total_rounds * 80 + 20 : 30 + i / 60 * 70;
        if (n.updateProgress(
          Math.min(u, 95),
          `Round ${c.current_round || 0}/${c.total_rounds || "?"} - Testing ${r.join(", ")}`
        ), c.status === "completed" || c.status === "failed" || c.status === "stopped")
          return c;
        await new Promise((d) => setTimeout(d, 5e3)), i++;
      } catch {
        await new Promise((u) => setTimeout(u, 5e3)), i++;
      }
    }
    throw new Error("Adversarial test did not complete within the expected time");
  }
  /**
   * Calculate overall robustness score
   *
   * @param result - Test result
   * @returns Robustness score (0-1)
   */
  calculateRobustness(t) {
    const r = t.attacks || [];
    if (r.length === 0) return 1;
    const a = r.filter((i) => i.success).length / r.length;
    return Math.max(0, 1 - a);
  }
  /**
   * Generate recommendations based on test results
   *
   * @param result - Test result
   * @returns Array of recommendations
   */
  generateRecommendations(t) {
    const r = [], n = t.attacks || [], i = n.filter((l) => l.success).map((l) => l.vulnerability);
    i.length > 0 && r.push(
      `Address ${i.length} identified vulnerability/vulnerabilities`,
      "Implement input validation and sanitization",
      "Add rate limiting and request throttling",
      "Review and strengthen prompt engineering",
      "Consider implementing adversarial training"
    );
    const o = new Set(n.map((l) => l.attackMode));
    return o.has("prompt_injection") && r.push("Strengthen defenses against prompt injection attacks"), o.has("jailbreak") && r.push("Implement jailbreak detection and prevention"), o.has("data_poisoning") && r.push("Validate and sanitize all input data"), r;
  }
  /**
   * Validate input data
   *
   * @param inputs - Input data to validate
   * @returns Array of validation errors
   */
  validateInputs(t) {
    const r = [];
    if (t.content || r.push({
      field: "content",
      message: "Content is required",
      severity: "error"
    }), t.content && typeof t.content != "string" && r.push({
      field: "content",
      message: "Content must be a string",
      severity: "error"
    }), t.content && t.content.length < 50 && r.push({
      field: "content",
      message: "Content is too short for meaningful adversarial testing (minimum 50 characters)",
      severity: "warning"
    }), t.attackModes && !Array.isArray(t.attackModes) && r.push({
      field: "attackModes",
      message: "Attack modes must be an array",
      severity: "error"
    }), t.attackModes && Array.isArray(t.attackModes)) {
      const n = ["prompt_injection", "jailbreak", "adversarial_examples", "data_poisoning", "model_extraction", "semantic_attacks"], a = t.attackModes.filter((i) => !n.includes(i));
      a.length > 0 && r.push({
        field: "attackModes",
        message: `Invalid attack modes: ${a.join(", ")}`,
        severity: "error"
      });
    }
    return t.numRounds && typeof t.numRounds == "number" && (t.numRounds < 1 || t.numRounds > 50) && r.push({
      field: "numRounds",
      message: "Number of rounds must be between 1 and 50",
      severity: "error"
    }), r;
  }
  /**
   * Get JSON Schema for configuration parameters
   *
   * @returns Parameter schema
   */
  getParameterSchema() {
    return {
      type: "object",
      properties: {
        attackModes: {
          type: "array",
          description: "Attack modes to test against",
          items: {
            type: "string",
            enum: ["prompt_injection", "jailbreak", "adversarial_examples", "data_poisoning", "model_extraction", "semantic_attacks"]
          },
          default: ["prompt_injection", "jailbreak"]
        },
        numRounds: {
          type: "number",
          description: "Number of testing rounds",
          minimum: 1,
          maximum: 50,
          default: 5
        },
        redTeamModels: {
          type: "array",
          description: "Red team (attacker) models to use",
          items: {
            type: "object",
            properties: {
              provider: { type: "string" },
              model: { type: "string" }
            }
          },
          default: []
        },
        blueTeamModels: {
          type: "array",
          description: "Blue team (defender) models to use",
          items: {
            type: "object",
            properties: {
              provider: { type: "string" },
              model: { type: "string" }
            }
          },
          default: []
        },
        timeoutMs: {
          type: "number",
          description: "Timeout for adversarial test in milliseconds",
          minimum: 1e4,
          maximum: 6e5,
          default: 3e5
        },
        enableAutoPatch: {
          type: "boolean",
          description: "Enable automatic patch application",
          default: !1
        }
      },
      required: []
    };
  }
  /**
   * Approve or reject a patch proposal
   *
   * @param testId - Test ID
   * @param round - Round number
   * @param approved - Whether to approve the patch
   * @param feedback - Optional feedback
   * @returns Promise resolving to approval result
   */
  async approvePatch(t, r, n, a) {
    try {
      const i = await Vr.approvePatch(t, { round: r, approved: n, feedback: a });
      return this.createSuccessResult(i);
    } catch (i) {
      return this.createErrorResult(
        i instanceof Error ? i.message : "Failed to approve patch"
      );
    }
  }
  /**
   * Stop running adversarial test
   *
   * @param testId - Test ID to stop
   * @returns Promise resolving to stop result
   */
  async stopTest(t) {
    try {
      const r = await Vr.stop(t);
      return this.createSuccessResult(r);
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to stop adversarial test"
      );
    }
  }
  /**
   * List all adversarial tests
   *
   * @param params - Optional query parameters
   * @returns Promise resolving to list of tests
   */
  async listTests(t) {
    try {
      const r = await Vr.list(t);
      return this.createSuccessResult(r);
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to list adversarial tests"
      );
    }
  }
}
Z(ma, "DISPLAY_NAME", "Adversarial Testing"), Z(ma, "DESCRIPTION", "Red team/blue team testing for identifying vulnerabilities and improving robustness"), Z(ma, "ICON", "adversarial"), Z(ma, "CATEGORY", "testing"), Z(ma, "VERSION", "1.0.0");
class ha extends vr {
  constructor(t, r = {}) {
    super(t, {
      queryType: "semantic",
      maxResults: 10,
      minRelevance: 0.5,
      enableReasoning: !1,
      includeMetadata: !0,
      ...r
    });
  }
  /**
   * Execute knowledge query
   *
   * @param inputs - Must contain 'query' string
   * @param context - Execution context
   * @returns Promise resolving to query result
   */
  async execute(t, r) {
    try {
      const n = Date.now(), a = t.query, i = t.queryType || this.config.queryType, o = t.maxResults || this.config.maxResults, l = t.minRelevance || this.config.minRelevance, c = t.enableReasoning ?? this.config.enableReasoning, u = t.filters;
      if (!a || a.trim().length === 0)
        return this.createErrorResult("Query is required and cannot be empty");
      r.updateProgress(10, "Preparing knowledge query");
      const d = {
        query: a,
        query_type: i,
        max_results: o,
        min_relevance: l,
        enable_reasoning: c,
        include_metadata: this.config.includeMetadata
      };
      u && (d.filters = u), r.updateProgress(30, "Executing knowledge query");
      const f = await Se.post("/knowledge/query", d);
      r.updateProgress(70, "Processing query results");
      const p = this.processQueryResult(f, a, i, n, c);
      return r.updateProgress(100, `Query complete: ${p.summary.totalResults} results found`), this.createSuccessResult(p);
    } catch (n) {
      return this.createErrorResult(
        n instanceof Error ? n.message : "Unknown error during knowledge query"
      );
    }
  }
  /**
   * Process query result from API
   *
   * @param response - API response
   * @param query - Original query
   * @param queryType - Query type
   * @param startTime - Execution start time
   * @param enableReasoning - Whether reasoning was enabled
   * @returns Processed query result
   */
  processQueryResult(t, r, n, a, i) {
    var y;
    const o = Date.now() - a, l = (t.results || []).map((h) => ({
      content: h.content || "",
      relevance: h.relevance || 0.5,
      entities: h.entities || [],
      source: h.source,
      metadata: h.metadata
    })), c = (t.entities || []).map((h) => ({
      id: h.id || "",
      type: h.type || "unknown",
      label: h.label || "",
      properties: h.properties || {},
      confidence: h.confidence || 0.5
    })), u = (t.relationships || []).map((h) => ({
      id: h.id || "",
      source: h.source || "",
      target: h.target || "",
      type: h.type || "related_to",
      properties: h.properties || {},
      confidence: h.confidence || 0.5
    })), d = (y = t.paths) == null ? void 0 : y.map((h) => ({
      entities: h.entities || [],
      relationships: h.relationships || [],
      length: h.length || 0,
      confidence: h.confidence || 0.5
    })), f = l.length, p = f > 0 ? l.reduce((h, g) => h + g.relevance, 0) / f : 0, m = {
      totalResults: f,
      avgRelevance: p,
      entityCount: c.length,
      relationshipCount: u.length
    };
    return {
      queryId: t.query_id || `query-${Date.now()}`,
      queryType: n,
      query: r,
      results: l,
      entities: c,
      relationships: u,
      paths: d,
      summary: m,
      reasoning: i ? t.reasoning : void 0,
      metadata: {
        executedAt: /* @__PURE__ */ new Date(),
        executionTime: o,
        parameters: {
          maxResults: this.config.maxResults,
          minRelevance: this.config.minRelevance,
          enableReasoning: i
        }
      }
    };
  }
  /**
   * Validate input data
   *
   * @param inputs - Input data to validate
   * @returns Array of validation errors
   */
  validateInputs(t) {
    const r = [];
    if (t.query || r.push({
      field: "query",
      message: "Query is required",
      severity: "error"
    }), t.query && typeof t.query != "string" && r.push({
      field: "query",
      message: "Query must be a string",
      severity: "error"
    }), t.query && t.query.length < 3 && r.push({
      field: "query",
      message: "Query is too short (minimum 3 characters)",
      severity: "warning"
    }), t.queryType && typeof t.queryType == "string") {
      const n = ["semantic", "entity", "relationship", "path", "aggregation"];
      n.includes(t.queryType) || r.push({
        field: "queryType",
        message: `Query type must be one of: ${n.join(", ")}`,
        severity: "error"
      });
    }
    return t.maxResults && typeof t.maxResults == "number" && (t.maxResults < 1 || t.maxResults > 100) && r.push({
      field: "maxResults",
      message: "Max results must be between 1 and 100",
      severity: "error"
    }), t.minRelevance && typeof t.minRelevance == "number" && (t.minRelevance < 0 || t.minRelevance > 1) && r.push({
      field: "minRelevance",
      message: "Min relevance must be between 0 and 1",
      severity: "error"
    }), r;
  }
  /**
   * Get JSON Schema for configuration parameters
   *
   * @returns Parameter schema
   */
  getParameterSchema() {
    return {
      type: "object",
      properties: {
        queryType: {
          type: "string",
          description: "Type of knowledge query to execute",
          enum: ["semantic", "entity", "relationship", "path", "aggregation"],
          default: "semantic"
        },
        maxResults: {
          type: "number",
          description: "Maximum number of results to return",
          minimum: 1,
          maximum: 100,
          default: 10
        },
        minRelevance: {
          type: "number",
          description: "Minimum relevance threshold for results (0-1)",
          minimum: 0,
          maximum: 1,
          default: 0.5
        },
        enableReasoning: {
          type: "boolean",
          description: "Enable reasoning and explanation",
          default: !1
        },
        includeMetadata: {
          type: "boolean",
          description: "Include metadata in results",
          default: !0
        }
      },
      required: []
    };
  }
  /**
   * Add knowledge to the graph
   *
   * @param data - Knowledge data to add
   * @returns Promise resolving to add result
   */
  async addKnowledge(t) {
    try {
      const r = await Se.post("/knowledge/add", t);
      return this.createSuccessResult(r);
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to add knowledge"
      );
    }
  }
  /**
   * Update knowledge in the graph
   *
   * @param knowledgeId - Knowledge ID to update
   * @param data - Updated knowledge data
   * @returns Promise resolving to update result
   */
  async updateKnowledge(t, r) {
    try {
      const n = await Se.put(`/knowledge/${t}`, r);
      return this.createSuccessResult(n);
    } catch (n) {
      return this.createErrorResult(
        n instanceof Error ? n.message : "Failed to update knowledge"
      );
    }
  }
  /**
   * Delete knowledge from the graph
   *
   * @param knowledgeId - Knowledge ID to delete
   * @returns Promise resolving to deletion result
   */
  async deleteKnowledge(t) {
    try {
      return await Se.delete(`/knowledge/${t}`), this.createSuccessResult({ knowledgeId: t, deleted: !0 });
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to delete knowledge"
      );
    }
  }
  /**
   * List knowledge artifacts
   *
   * @param params - Optional query parameters
   * @returns Promise resolving to list of knowledge artifacts
   */
  async listKnowledge(t) {
    try {
      const r = await Se.get("/knowledge/list", t);
      return this.createSuccessResult(r);
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to list knowledge"
      );
    }
  }
  /**
   * Get knowledge statistics
   *
   * @returns Promise resolving to knowledge graph statistics
   */
  async getStatistics() {
    try {
      const t = await Se.get("/knowledge/statistics");
      return this.createSuccessResult(t);
    } catch (t) {
      return this.createErrorResult(
        t instanceof Error ? t.message : "Failed to get statistics"
      );
    }
  }
}
Z(ha, "DISPLAY_NAME", "Knowledge Query"), Z(ha, "DESCRIPTION", "Query knowledge graph for semantic information, entities, and relationships"), Z(ha, "ICON", "knowledge"), Z(ha, "CATEGORY", "knowledge"), Z(ha, "VERSION", "1.0.0");
class pa extends vr {
  constructor(t, r = {}) {
    super(t, {
      taskType: "generate",
      model: "gpt-4",
      temperature: 0.3,
      enableAutoRepair: !0,
      maxRepairAttempts: 3,
      ...r
    });
  }
  /**
   * Execute LeanAide task
   *
   * @param inputs - Must contain 'theorem' and optionally 'proof_attempt'
   * @param context - Execution context
   * @returns Promise resolving to LeanAide result
   */
  async execute(t, r) {
    try {
      const n = Date.now(), a = t.taskType || this.config.taskType, i = t.theorem, o = t.proof_attempt, l = t.model || this.config.model, c = t.temperature || this.config.temperature;
      if (!i || i.trim().length === 0)
        return this.createErrorResult("Theorem is required and cannot be empty");
      r.updateProgress(10, `Preparing ${a} task`);
      let u;
      switch (a) {
        case "generate":
          u = await this.generateProof(i, l, c, r);
          break;
        case "verify":
          if (!o)
            return this.createErrorResult("Proof attempt is required for verification");
          u = await this.verifyProof(o, r);
          break;
        case "repair":
          if (!o)
            return this.createErrorResult("Proof attempt is required for repair");
          u = await this.repairProof(i, o, l, c, r);
          break;
        case "synthesis":
          u = await this.synthesizeProof(i, l, c, r);
          break;
        default:
          return this.createErrorResult(`Unknown task type: ${a}`);
      }
      const d = Date.now() - n, f = {
        taskId: `task-${Date.now()}`,
        taskType: a,
        theorem: i,
        output: u,
        model: l,
        metadata: {
          executedAt: /* @__PURE__ */ new Date(),
          executionTime: d,
          temperature: c,
          parameters: {
            model: l,
            temperature: c,
            enableAutoRepair: this.config.enableAutoRepair
          }
        }
      };
      return r.updateProgress(100, `${a} task complete`), this.createSuccessResult(f);
    } catch (n) {
      return this.createErrorResult(
        n instanceof Error ? n.message : "Unknown error during LeanAide execution"
      );
    }
  }
  /**
   * Generate Lean 4 proof
   *
   * @param theorem - Theorem statement
   * @param model - Model to use
   * @param temperature - Temperature for generation
   * @param context - Execution context
   * @returns Promise resolving to generated proof
   */
  async generateProof(t, r, n, a) {
    a.updateProgress(30, "Generating Lean 4 proof");
    const i = await fr.generateProof({
      theorem: t,
      model: r,
      temperature: n
    });
    return a.updateProgress(80, "Proof generated, post-processing"), this.config.enableAutoRepair && !i.verified && i.errors.length > 0 ? (a.updateProgress(85, "Verification failed, attempting auto-repair"), await this.attemptAutoRepair(t, i.code, r, n, a)) : i;
  }
  /**
   * Verify Lean 4 proof
   *
   * @param code - Lean 4 code to verify
   * @param context - Execution context
   * @returns Promise resolving to verification result
   */
  async verifyProof(t, r) {
    r.updateProgress(30, "Verifying Lean 4 proof");
    const n = await fr.verifyProof(t);
    return r.updateProgress(100, "Verification complete"), n;
  }
  /**
   * Repair Lean 4 proof
   *
   * @param theorem - Theorem statement
   * @param proofAttempt - Proof attempt to repair
   * @param model - Model to use
   * @param temperature - Temperature for generation
   * @param context - Execution context
   * @returns Promise resolving to repair result
   */
  async repairProof(t, r, n, a, i) {
    i.updateProgress(30, "Repairing Lean 4 proof");
    const o = await this.verifyProof(r, i);
    if (o.verified)
      return {
        originalCode: r,
        repairedCode: r,
        repairs: [],
        verified: !0,
        attempts: 1
      };
    let l = r;
    const c = [], u = this.config.maxRepairAttempts;
    for (let d = 0; d < u; d++) {
      i.updateProgress(
        40 + d / u * 50,
        `Repair attempt ${d + 1}/${u}`
      );
      const f = await fr.generateProof({
        theorem: t,
        model: n,
        temperature: a
      });
      if (o.errors.forEach((m) => {
        c.push({
          line: m.line,
          issue: m.message,
          fix: "Regenerated proof section"
        });
      }), l = f.code, (await this.verifyProof(l, i)).verified)
        return i.updateProgress(100, "Proof repaired successfully"), {
          originalCode: r,
          repairedCode: l,
          repairs: c,
          verified: !0,
          attempts: d + 1
        };
    }
    return i.updateProgress(100, "Max repair attempts reached"), {
      originalCode: r,
      repairedCode: l,
      repairs: c,
      verified: !1,
      attempts: u
    };
  }
  /**
   * Synthesize proof from natural language
   *
   * @param theorem - Theorem statement in natural language
   * @param model - Model to use
   * @param temperature - Temperature for generation
   * @param context - Execution context
   * @returns Promise resolving to synthesized proof
   */
  async synthesizeProof(t, r, n, a) {
    a.updateProgress(30, "Synthesizing proof from natural language");
    const i = await fr.generateProof({
      theorem: t,
      model: r,
      temperature: Math.min(n + 0.2, 1)
    });
    return a.updateProgress(100, "Proof synthesis complete"), i;
  }
  /**
   * Attempt automatic repair of generated proof
   *
   * @param theorem - Theorem statement
   * @param code - Generated code with errors
   * @param model - Model to use
   * @param temperature - Temperature for generation
   * @param context - Execution context
   * @returns Promise resolving to repaired code
   */
  async attemptAutoRepair(t, r, n, a, i) {
    for (let l = 0; l < 2; l++)
      try {
        const c = await fr.generateProof({
          theorem: t,
          model: n,
          temperature: Math.min(a + 0.1, 1)
        });
        if (c.verified)
          return i.updateProgress(95, "Auto-repair successful"), c;
      } catch (c) {
        console.warn(`Auto-repair attempt ${l + 1} failed:`, c);
      }
    return i.updateProgress(95, "Auto-repair unsuccessful, returning original"), await fr.generateProof({ theorem: t, model: n, temperature: a });
  }
  /**
   * Validate input data
   *
   * @param inputs - Input data to validate
   * @returns Array of validation errors
   */
  validateInputs(t) {
    const r = [];
    if (t.theorem || r.push({
      field: "theorem",
      message: "Theorem is required",
      severity: "error"
    }), t.theorem && typeof t.theorem != "string" && r.push({
      field: "theorem",
      message: "Theorem must be a string",
      severity: "error"
    }), t.taskType && typeof t.taskType == "string") {
      const a = ["generate", "verify", "repair", "synthesis"];
      a.includes(t.taskType) || r.push({
        field: "taskType",
        message: `Task type must be one of: ${a.join(", ")}`,
        severity: "error"
      });
    }
    const n = t.taskType || this.config.taskType;
    return (n === "verify" || n === "repair") && !t.proof_attempt && r.push({
      field: "proof_attempt",
      message: `Proof attempt is required for ${n} task`,
      severity: "error"
    }), t.temperature && typeof t.temperature == "number" && (t.temperature < 0 || t.temperature > 1) && r.push({
      field: "temperature",
      message: "Temperature must be between 0 and 1",
      severity: "error"
    }), r;
  }
  /**
   * Get JSON Schema for configuration parameters
   *
   * @returns Parameter schema
   */
  getParameterSchema() {
    return {
      type: "object",
      properties: {
        taskType: {
          type: "string",
          description: "Type of LeanAide task to execute",
          enum: ["generate", "verify", "repair", "synthesis"],
          default: "generate"
        },
        model: {
          type: "string",
          description: "Model to use for proof generation",
          default: "gpt-4"
        },
        temperature: {
          type: "number",
          description: "Temperature for proof generation (0-1)",
          minimum: 0,
          maximum: 1,
          default: 0.3
        },
        enableAutoRepair: {
          type: "boolean",
          description: "Enable automatic proof repair on verification failure",
          default: !0
        },
        maxRepairAttempts: {
          type: "number",
          description: "Maximum number of repair attempts",
          minimum: 1,
          maximum: 10,
          default: 3
        }
      },
      required: []
    };
  }
  /**
   * Get supported models
   *
   * @returns Promise resolving to list of supported models
   */
  async getSupportedModels() {
    try {
      const t = await fr.getModels();
      return this.createSuccessResult({ models: t });
    } catch (t) {
      return this.createErrorResult(
        t instanceof Error ? t.message : "Failed to get supported models"
      );
    }
  }
  /**
   * Run benchmark
   *
   * @param dataset - Benchmark dataset
   * @param model - Model to use
   * @param evaluator - Evaluator to use
   * @returns Promise resolving to benchmark result
   */
  async runBenchmark(t, r, n) {
    try {
      const a = await fr.runBenchmark({ dataset: t, model: r, evaluator: n });
      return this.createSuccessResult(a);
    } catch (a) {
      return this.createErrorResult(
        a instanceof Error ? a.message : "Failed to run benchmark"
      );
    }
  }
  /**
   * Get benchmark results
   *
   * @param benchmarkId - Benchmark ID
   * @returns Promise resolving to benchmark results
   */
  async getBenchmarkResults(t) {
    try {
      const r = await fr.getBenchmarkResults(t);
      return this.createSuccessResult({ benchmarkId: t, results: r });
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to get benchmark results"
      );
    }
  }
}
Z(pa, "DISPLAY_NAME", "LeanAide Verification"), Z(pa, "DESCRIPTION", "Lean 4 formal proof generation and verification with automated repair"), Z(pa, "ICON", "leanaide"), Z(pa, "CATEGORY", "verification"), Z(pa, "VERSION", "1.0.0");
class ga extends vr {
  constructor(t, r = {}) {
    super(t, {
      taskType: "generate",
      language: "python",
      enableExecution: !1,
      enableOptimization: !1,
      timeoutMs: 6e4,
      // 1 minute
      ...r
    });
  }
  /**
   * Execute CrewAI task
   *
   * @param inputs - Must contain 'input' and optionally 'taskType'
   * @param context - Execution context
   * @returns Promise resolving to CrewAI result
   */
  async execute(t, r) {
    try {
      const n = Date.now(), a = t.input, i = t.taskType || this.config.taskType, o = t.language || this.config.language, l = t.code, c = t.delegateTo;
      if (!a || a.trim().length === 0)
        return this.createErrorResult("Input is required and cannot be empty");
      r.updateProgress(10, `Preparing ${i} task`);
      let u;
      switch (i) {
        case "generate":
          u = await this.generateCode(a, o, r);
          break;
        case "execute":
          if (!l)
            return this.createErrorResult("Code is required for execution");
          u = await this.executeCode(l, o, r);
          break;
        case "delegate":
          if (!c)
            return this.createErrorResult("Delegate target is required for delegation");
          u = await this.delegateTask(a, c, r);
          break;
        case "optimize":
          if (!l)
            return this.createErrorResult("Code is required for optimization");
          u = await this.optimizeCode(l, o, r);
          break;
        default:
          return this.createErrorResult(`Unknown task type: ${i}`);
      }
      const d = Date.now() - n, f = {
        taskId: `task-${Date.now()}`,
        taskType: i,
        language: o,
        input: a,
        output: u,
        metadata: {
          executedAt: /* @__PURE__ */ new Date(),
          executionTime: d,
          parameters: {
            language: o,
            enableExecution: this.config.enableExecution,
            enableOptimization: this.config.enableOptimization
          }
        }
      };
      return r.updateProgress(100, `${i} task complete`), this.createSuccessResult(f);
    } catch (n) {
      return this.createErrorResult(
        n instanceof Error ? n.message : "Unknown error during CrewAI execution"
      );
    }
  }
  /**
   * Generate code from natural language
   *
   * @param input - Natural language description
   * @param language - Target programming language
   * @param context - Execution context
   * @returns Promise resolving to generated code
   */
  async generateCode(t, r, n) {
    n.updateProgress(30, `Generating ${r} code`);
    const a = await Se.post("/crewai/generate", {
      description: t,
      language: r,
      include_tests: !0,
      include_docs: !0
    });
    return n.updateProgress(80, "Code generated, post-processing"), {
      code: a.code || "",
      language: r,
      quality: a.quality || 0.8,
      dependencies: a.dependencies || [],
      documentation: a.documentation || "",
      tests: a.tests
    };
  }
  /**
   * Execute code
   *
   * @param code - Code to execute
   * @param language - Programming language
   * @param context - Execution context
   * @returns Promise resolving to execution result
   */
  async executeCode(t, r, n) {
    n.updateProgress(30, `Executing ${r} code`);
    const a = await Se.post("/crewai/execute", {
      code: t,
      language: r,
      timeout: this.config.timeoutMs
    });
    return n.updateProgress(100, "Execution complete"), {
      success: a.success || !1,
      output: a.output || "",
      error: a.error,
      executionTime: a.execution_time || 0,
      memory: a.memory || 0,
      cpu: a.cpu || 0
    };
  }
  /**
   * Delegate task to another service
   *
   * @param input - Task input
   * @param delegateTo - Target service
   * @param context - Execution context
   * @returns Promise resolving to delegation result
   */
  async delegateTask(t, r, n) {
    n.updateProgress(30, `Delegating to ${r}`);
    const i = (await Se.post("/crewai/delegate", {
      task: t,
      target_service: r
    })).task_id || `delegate-${Date.now()}`, o = await this.monitorDelegation(i, n);
    return {
      delegatedTo: r,
      taskId: i,
      status: o.status,
      result: o.output,
      metadata: {
        delegatedAt: new Date(o.created_at || Date.now()),
        completedAt: o.completed_at ? new Date(o.completed_at) : void 0,
        executionTime: o.execution_time || 0
      }
    };
  }
  /**
   * Optimize code
   *
   * @param code - Code to optimize
   * @param language - Programming language
   * @param context - Execution context
   * @returns Promise resolving to optimization result
   */
  async optimizeCode(t, r, n) {
    var i, o, l, c;
    n.updateProgress(30, `Optimizing ${r} code`);
    const a = await Se.post("/crewai/optimize", {
      code: t,
      language: r
    });
    return n.updateProgress(100, "Optimization complete"), {
      originalCode: t,
      optimizedCode: a.optimized_code || t,
      improvements: a.improvements || [],
      performance: {
        speedup: ((i = a.performance) == null ? void 0 : i.speedup) || 1,
        memoryReduction: ((o = a.performance) == null ? void 0 : o.memory_reduction) || 0,
        timeComplexity: ((l = a.performance) == null ? void 0 : l.time_complexity) || "Unknown",
        spaceComplexity: ((c = a.performance) == null ? void 0 : c.space_complexity) || "Unknown"
      }
    };
  }
  /**
   * Monitor delegation progress
   *
   * @param taskId - Task ID to monitor
   * @param context - Execution context
   * @returns Promise resolving to delegation status
   */
  async monitorDelegation(t, r) {
    let a = 0;
    const i = Date.now(), o = this.config.timeoutMs;
    for (; a < 60; ) {
      if (Date.now() - i > o)
        throw new Error("Delegation monitoring timeout exceeded");
      try {
        const l = await Se.get(`/crewai/delegation/${t}`), c = Math.min(30 + a / 60 * 70, 95);
        if (r.updateProgress(c, `Delegation status: ${l.status}`), l.status === "completed" || l.status === "failed")
          return l;
        await new Promise((u) => setTimeout(u, 5e3)), a++;
      } catch {
        await new Promise((c) => setTimeout(c, 5e3)), a++;
      }
    }
    throw new Error("Delegation did not complete within the expected time");
  }
  /**
   * Validate input data
   *
   * @param inputs - Input data to validate
   * @returns Array of validation errors
   */
  validateInputs(t) {
    const r = [];
    if (t.input || r.push({
      field: "input",
      message: "Input is required",
      severity: "error"
    }), t.input && typeof t.input != "string" && r.push({
      field: "input",
      message: "Input must be a string",
      severity: "error"
    }), t.taskType && typeof t.taskType == "string") {
      const a = ["generate", "execute", "delegate", "optimize"];
      a.includes(t.taskType) || r.push({
        field: "taskType",
        message: `Task type must be one of: ${a.join(", ")}`,
        severity: "error"
      });
    }
    if (t.language && typeof t.language == "string") {
      const a = ["python", "javascript", "typescript", "java", "cpp", "go", "rust"];
      a.includes(t.language) || r.push({
        field: "language",
        message: `Language must be one of: ${a.join(", ")}`,
        severity: "error"
      });
    }
    const n = t.taskType || this.config.taskType;
    return (n === "execute" || n === "optimize") && !t.code && r.push({
      field: "code",
      message: `Code is required for ${n} task`,
      severity: "error"
    }), n === "delegate" && !t.delegateTo && r.push({
      field: "delegateTo",
      message: "Delegate target is required for delegation task",
      severity: "error"
    }), r;
  }
  /**
   * Get JSON Schema for configuration parameters
   *
   * @returns Parameter schema
   */
  getParameterSchema() {
    return {
      type: "object",
      properties: {
        taskType: {
          type: "string",
          description: "Type of CrewAI task to execute",
          enum: ["generate", "execute", "delegate", "optimize"],
          default: "generate"
        },
        language: {
          type: "string",
          description: "Programming language",
          enum: ["python", "javascript", "typescript", "java", "cpp", "go", "rust"],
          default: "python"
        },
        enableExecution: {
          type: "boolean",
          description: "Enable code execution after generation",
          default: !1
        },
        enableOptimization: {
          type: "boolean",
          description: "Enable code optimization",
          default: !1
        },
        timeoutMs: {
          type: "number",
          description: "Timeout for code execution in milliseconds",
          minimum: 1e3,
          maximum: 3e5,
          default: 6e4
        }
      },
      required: []
    };
  }
  /**
   * Get supported languages
   *
   * @returns Array of supported languages
   */
  getSupportedLanguages() {
    return ["python", "javascript", "typescript", "java", "cpp", "go", "rust"];
  }
  /**
   * Get available services for delegation
   *
   * @returns Promise resolving to available services
   */
  async getAvailableServices() {
    try {
      const t = await Se.get("/crewai/services");
      return this.createSuccessResult({ services: t.services || [] });
    } catch (t) {
      return this.createErrorResult(
        t instanceof Error ? t.message : "Failed to get available services"
      );
    }
  }
  /**
   * Get code quality metrics
   *
   * @param code - Code to analyze
   * @param language - Programming language
   * @returns Promise resolving to quality metrics
   */
  async getQualityMetrics(t, r) {
    try {
      const n = await Se.post("/crewai/quality", {
        code: t,
        language: r
      });
      return this.createSuccessResult(n);
    } catch (n) {
      return this.createErrorResult(
        n instanceof Error ? n.message : "Failed to get quality metrics"
      );
    }
  }
}
Z(ga, "DISPLAY_NAME", "CrewAI Bridge"), Z(ga, "DESCRIPTION", "Code generation and execution bridge with delegation and optimization capabilities"), Z(ga, "ICON", "crewai"), Z(ga, "CATEGORY", "integration"), Z(ga, "VERSION", "1.0.0");
class ya extends vr {
  constructor(t, r = {}) {
    super(t, {
      strategy: "adaptive",
      domains: ["analysis", "design", "implementation", "testing"],
      enableAgentCollaboration: !0,
      enableKnowledgeSharing: !0,
      maxIterations: 5,
      ...r
    });
  }
  /**
   * Execute MDAP planning and execution
   *
   * @param inputs - Must contain 'problem' statement
   * @param context - Execution context
   * @returns Promise resolving to MDAP execution result
   */
  async execute(t, r) {
    try {
      const n = Date.now(), a = t.problem, i = t.strategy || this.config.strategy, o = t.domains || this.config.domains, l = t.constraints, c = t.requirements;
      if (!a || a.trim().length === 0)
        return this.createErrorResult("Problem statement is required and cannot be empty");
      r.updateProgress(10, "Analyzing problem and creating execution plan");
      const u = await this.createPlan(a, i, o, c, l, r);
      r.updateProgress(30, "Plan created, executing tasks");
      const d = await this.executePlan(u, r), f = Date.now() - n, p = this.calculateMetrics(d), m = {
        planId: u.planId,
        problem: a,
        strategy: i,
        status: d.status,
        tasks: d.tasks,
        collaborations: d.collaborations,
        finalResult: d.finalResult,
        metrics: p,
        metadata: {
          startedAt: new Date(n),
          completedAt: d.status === "completed" ? /* @__PURE__ */ new Date() : void 0,
          executionTime: f,
          parameters: {
            strategy: i,
            domains: o,
            enableAgentCollaboration: this.config.enableAgentCollaboration,
            enableKnowledgeSharing: this.config.enableKnowledgeSharing
          }
        }
      };
      return r.updateProgress(100, `MDAP execution complete: ${p.completedTasks}/${p.totalTasks} tasks completed`), this.createSuccessResult(m);
    } catch (n) {
      return this.createErrorResult(
        n instanceof Error ? n.message : "Unknown error during MDAP execution"
      );
    }
  }
  /**
   * Create execution plan
   *
   * @param problem - Problem statement
   * @param strategy - Planning strategy
   * @param domains - Agent domains to involve
   * @param requirements - Optional requirements
   * @param constraints - Optional constraints
   * @param context - Execution context
   * @returns Promise resolving to execution plan
   */
  async createPlan(t, r, n, a, i, o) {
    const l = await Se.post("/mdap/plan", {
      problem: t,
      strategy: r,
      domains: n,
      requirements: a,
      constraints: i
    }), c = (l.tasks || []).map((d) => ({
      taskId: d.task_id || `task-${Date.now()}`,
      domain: d.domain,
      description: d.description || "",
      status: "pending",
      dependencies: d.dependencies || [],
      assignedAgent: d.assigned_agent || "auto"
    })), u = (l.collaborations || []).map((d) => ({
      fromAgent: d.from_agent,
      toAgent: d.to_agent,
      domain: d.domain,
      message: d.message || "",
      sharedKnowledge: d.knowledge,
      timestamp: new Date(d.timestamp || Date.now())
    }));
    return {
      planId: l.plan_id || `plan-${Date.now()}`,
      problem: t,
      strategy: r,
      domains: n,
      tasks: c,
      collaborations: u,
      executionOrder: l.execution_order || [],
      estimatedDuration: l.estimated_duration || 0
    };
  }
  /**
   * Execute plan
   *
   * @param plan - Execution plan
   * @param context - Execution context
   * @returns Promise resolving to execution result
   */
  async executePlan(t, r) {
    return await Se.post("/mdap/execute", {
      plan_id: t.planId,
      enable_collaboration: this.config.enableAgentCollaboration,
      enable_knowledge_sharing: this.config.enableKnowledgeSharing,
      max_iterations: this.config.maxIterations
    }), await this.monitorExecution(t.planId, r);
  }
  /**
   * Monitor execution progress
   *
   * @param planId - Plan ID to monitor
   * @param context - Execution context
   * @returns Promise resolving to execution status
   */
  async monitorExecution(t, r) {
    let a = 0;
    const i = Date.now(), o = 3e5;
    for (; a < 60; ) {
      if (Date.now() - i > o)
        throw new Error("MDAP execution monitoring timeout exceeded");
      try {
        const l = await Se.get(`/mdap/execution/${t}`), c = l.current_task && l.total_tasks ? l.current_task / l.total_tasks * 70 + 30 : 30 + a / 60 * 70;
        if (r.updateProgress(
          Math.min(c, 95),
          `Task ${l.current_task || 0}/${l.total_tasks || "?"} - ${l.current_domain || "executing"}`
        ), l.status === "completed" || l.status === "failed")
          return l;
        await new Promise((u) => setTimeout(u, 5e3)), a++;
      } catch {
        await new Promise((c) => setTimeout(c, 5e3)), a++;
      }
    }
    throw new Error("MDAP execution did not complete within the expected time");
  }
  /**
   * Calculate execution metrics
   *
   * @param result - Execution result
   * @returns Calculated metrics
   */
  calculateMetrics(t) {
    var c;
    const r = t.tasks || [], n = r.filter((u) => u.status === "completed"), a = r.filter((u) => u.status === "failed"), i = n.filter((u) => u.startTime && u.endTime).map((u) => {
      const d = new Date(u.startTime).getTime();
      return new Date(u.endTime).getTime() - d;
    }), o = i.length > 0 ? i.reduce((u, d) => u + d, 0) / i.length : 0, l = t.completed_at ? new Date(t.completed_at).getTime() - new Date(t.started_at).getTime() : 0;
    return {
      totalTasks: r.length,
      completedTasks: n.length,
      failedTasks: a.length,
      avgTaskDuration: o,
      totalExecutionTime: l,
      collaborationCount: ((c = t.collaborations) == null ? void 0 : c.length) || 0,
      knowledgeShared: t.knowledge_shared || 0
    };
  }
  /**
   * Validate input data
   *
   * @param inputs - Input data to validate
   * @returns Array of validation errors
   */
  validateInputs(t) {
    const r = [];
    if (t.problem || r.push({
      field: "problem",
      message: "Problem statement is required",
      severity: "error"
    }), t.problem && typeof t.problem != "string" && r.push({
      field: "problem",
      message: "Problem must be a string",
      severity: "error"
    }), t.problem && t.problem.length < 20 && r.push({
      field: "problem",
      message: "Problem statement is too short for meaningful planning (minimum 20 characters)",
      severity: "warning"
    }), t.strategy && typeof t.strategy == "string") {
      const n = ["sequential", "parallel", "hierarchical", "adaptive"];
      n.includes(t.strategy) || r.push({
        field: "strategy",
        message: `Strategy must be one of: ${n.join(", ")}`,
        severity: "error"
      });
    }
    if (t.domains && Array.isArray(t.domains)) {
      const n = ["analysis", "design", "implementation", "testing", "optimization", "documentation"], a = t.domains.filter((i) => !n.includes(i));
      a.length > 0 && r.push({
        field: "domains",
        message: `Invalid domains: ${a.join(", ")}`,
        severity: "error"
      });
    }
    return r;
  }
  /**
   * Get JSON Schema for configuration parameters
   *
   * @returns Parameter schema
   */
  getParameterSchema() {
    return {
      type: "object",
      properties: {
        strategy: {
          type: "string",
          description: "Planning and execution strategy",
          enum: ["sequential", "parallel", "hierarchical", "adaptive"],
          default: "adaptive"
        },
        domains: {
          type: "array",
          description: "Agent domains to involve in problem solving",
          items: {
            type: "string",
            enum: ["analysis", "design", "implementation", "testing", "optimization", "documentation"]
          },
          default: ["analysis", "design", "implementation", "testing"]
        },
        enableAgentCollaboration: {
          type: "boolean",
          description: "Enable collaboration between agents",
          default: !0
        },
        enableKnowledgeSharing: {
          type: "boolean",
          description: "Enable knowledge sharing between agents",
          default: !0
        },
        maxIterations: {
          type: "number",
          description: "Maximum iterations for adaptive planning",
          minimum: 1,
          maximum: 20,
          default: 5
        }
      },
      required: []
    };
  }
  /**
   * Get available agent domains
   *
   * @returns Array of available domains
   */
  getAvailableDomains() {
    return ["analysis", "design", "implementation", "testing", "optimization", "documentation"];
  }
  /**
   * Get available strategies
   *
   * @returns Array of available strategies
   */
  getAvailableStrategies() {
    return ["sequential", "parallel", "hierarchical", "adaptive"];
  }
  /**
   * Get execution status
   *
   * @param planId - Plan ID
   * @returns Promise resolving to execution status
   */
  async getExecutionStatus(t) {
    try {
      const r = await Se.get(`/mdap/execution/${t}`);
      return this.createSuccessResult(r);
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to get execution status"
      );
    }
  }
  /**
   * Cancel execution
   *
   * @param planId - Plan ID to cancel
   * @returns Promise resolving to cancellation result
   */
  async cancelExecution(t) {
    try {
      const r = await Se.post(`/mdap/execution/${t}/cancel`, {});
      return this.createSuccessResult(r);
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to cancel execution"
      );
    }
  }
  /**
   * Get execution history
   *
   * @param params - Query parameters
   * @returns Promise resolving to execution history
   */
  async getExecutionHistory(t) {
    try {
      const r = await Se.get("/mdap/history", t);
      return this.createSuccessResult(r);
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to get execution history"
      );
    }
  }
}
Z(ya, "DISPLAY_NAME", "Multi-Domain Agent Planner"), Z(ya, "DESCRIPTION", "Coordinate multiple specialized agents for complex problem solving with collaboration"), Z(ya, "ICON", "mdap"), Z(ya, "CATEGORY", "planning"), Z(ya, "VERSION", "1.0.0");
class va extends vr {
  constructor(t, r = {}) {
    super(t, {
      contentType: "article",
      tone: "formal",
      length: "medium",
      enableResearch: !0,
      enableCitations: !1,
      qualityLevel: "standard",
      ...r
    });
  }
  /**
   * Execute MAKER content generation
   *
   * @param inputs - Must contain 'topic' or 'prompt'
   * @param context - Execution context
   * @returns Promise resolving to MAKER result
   */
  async execute(t, r) {
    try {
      const n = Date.now(), a = t.topic, i = t.prompt, o = a || i;
      if (!o || o.trim().length === 0)
        return this.createErrorResult("Topic or prompt is required and cannot be empty");
      const l = t.contentType || this.config.contentType, c = t.tone || this.config.tone, u = t.length || this.config.length, d = t.qualityLevel || this.config.qualityLevel, f = t.requirements, p = t.constraints;
      r.updateProgress(10, "Analyzing requirements and planning approach");
      const m = await this.executeMethodicalPhase(o, l, u, r);
      r.updateProgress(30, "Analyzing topic and gathering insights");
      const y = await this.executeAnalyticalPhase(o, l, f, r);
      r.updateProgress(50, "Gathering knowledge and expertise");
      const h = await this.executeKnowledgePhase(o, l, this.config.enableResearch, r);
      r.updateProgress(70, "Optimizing content generation");
      const g = await this.executeEfficientPhase(o, l, u, r);
      r.updateProgress(90, "Validating and ensuring quality");
      const v = await this.executeRobustPhase(o, l, d, r), b = await this.generateContent(
        o,
        l,
        c,
        u,
        { methodical: m, analytical: y, knowledge: h, efficient: g, robust: v },
        r
      ), w = Date.now() - n, j = {
        taskId: `task-${Date.now()}`,
        input: o,
        contentType: l,
        methodology: {
          Methodical: m,
          Analytical: y,
          Knowledge: h,
          Efficient: g,
          Robust: v
        },
        content: b,
        metadata: {
          executedAt: /* @__PURE__ */ new Date(),
          executionTime: w,
          parameters: {
            contentType: l,
            tone: c,
            length: u,
            qualityLevel: d
          }
        }
      };
      return r.updateProgress(100, "MAKER generation complete"), this.createSuccessResult(j);
    } catch (n) {
      return this.createErrorResult(
        n instanceof Error ? n.message : "Unknown error during MAKER generation"
      );
    }
  }
  /**
   * Execute Methodical phase - Plan the approach
   *
   * @param input - Input topic/prompt
   * @param contentType - Content type
   * @param length - Content length
   * @param context - Execution context
   * @returns Promise resolving to methodical phase output
   */
  async executeMethodicalPhase(t, r, n, a) {
    const i = {
      article: ["Introduction", "Body Paragraphs", "Analysis", "Conclusion"],
      blog_post: ["Hook", "Main Content", "Key Takeaways", "Call to Action"],
      documentation: ["Overview", "Installation", "Usage", "API Reference", "Examples"],
      tutorial: ["Prerequisites", "Step-by-Step Guide", "Examples", "Troubleshooting"],
      report: ["Executive Summary", "Introduction", "Findings", "Analysis", "Recommendations"],
      presentation: ["Title Slide", "Agenda", "Content Slides", "Summary", "Q&A"],
      code: ["Overview", "Implementation", "Usage Examples", "Error Handling"],
      creative: ["Opening", "Development", "Climax", "Resolution"]
    };
    return {
      approach: `Structured ${r} creation with ${n} detail level`,
      structure: i[r] || ["Introduction", "Body", "Conclusion"],
      planning: `Comprehensive planning for ${t.substring(0, 50)}...`
    };
  }
  /**
   * Execute Analytical phase - Analyze the topic
   *
   * @param input - Input topic/prompt
   * @param contentType - Content type
   * @param requirements - Optional requirements
   * @param context - Execution context
   * @returns Promise resolving to analytical phase output
   */
  async executeAnalyticalPhase(t, r, n, a) {
    const i = t.toLowerCase().split(/\s+/), o = new Set(i), l = Array.from(o).filter((c) => c.length > 5);
    return {
      analysis: `Analyzing ${t.substring(0, 100)}... for ${r} creation`,
      keyPoints: n && n.length > 0 ? n : l.slice(0, 5),
      considerations: [
        "Target audience and readability",
        "Content depth and complexity",
        "Relevance and engagement",
        "Clarity and structure"
      ]
    };
  }
  /**
   * Execute Knowledge phase - Research and expertise
   *
   * @param input - Input topic/prompt
   * @param contentType - Content type
   * @param enableResearch - Whether to enable research
   * @param context - Execution context
   * @returns Promise resolving to knowledge phase output
   */
  async executeKnowledgePhase(t, r, n, a) {
    var i;
    if (n)
      try {
        const l = ((i = (await Se.post("/knowledge/query", {
          query: t,
          query_type: "semantic",
          max_results: 3,
          min_relevance: 0.6
        })).results) == null ? void 0 : i.map((c) => c.source)) || [];
        return {
          research: `Knowledge retrieval for ${t.substring(0, 50)}...`,
          sources: l,
          expertise: `Expertise applied for ${r} creation`
        };
      } catch (o) {
        console.warn("Knowledge query failed, proceeding without research:", o);
      }
    return {
      research: "Built-in knowledge applied",
      sources: [],
      expertise: `Domain expertise for ${r}`
    };
  }
  /**
   * Execute Efficient phase - Optimize generation
   *
   * @param input - Input topic/prompt
   * @param contentType - Content type
   * @param length - Content length
   * @param context - Execution context
   * @returns Promise resolving to efficient phase output
   */
  async executeEfficientPhase(t, r, n, a) {
    const o = {
      short: 300,
      medium: 700,
      long: 1500
    }[n] || 700, l = Math.ceil(o / 100);
    return {
      optimization: `Efficient generation of ${o} words`,
      bestPractices: [
        "Use clear and concise language",
        "Maintain logical flow",
        "Optimize for readability",
        "Include relevant examples",
        "Proofread and edit"
      ],
      timeEstimate: l
    };
  }
  /**
   * Execute Robust phase - Validate quality
   *
   * @param input - Input topic/prompt
   * @param contentType - Content type
   * @param qualityLevel - Quality level
   * @param context - Execution context
   * @returns Promise resolving to robust phase output
   */
  async executeRobustPhase(t, r, n, a) {
    const i = {
      draft: [0.6, 0.6, 0.6, 0.6],
      standard: [0.8, 0.8, 0.8, 0.8],
      premium: [0.9, 0.9, 0.9, 0.9]
    }, o = i[n] || i.standard;
    return {
      validation: `Validating content at ${n} quality level`,
      qualityChecks: [
        `Clarity >= ${o[0]}`,
        `Accuracy >= ${o[1]}`,
        `Completeness >= ${o[2]}`,
        `Relevance >= ${o[3]}`
      ],
      reliability: `${n} quality assurance applied`
    };
  }
  /**
   * Generate final content
   *
   * @param input - Input topic/prompt
   * @param contentType - Content type
   * @param tone - Content tone
   * @param length - Content length
   * @param methodology - MAKER methodology outputs
   * @param context - Execution context
   * @returns Promise resolving to generated content
   */
  async generateContent(t, r, n, a, i, o) {
    var d;
    const l = await Se.post("/maker/generate", {
      topic: t,
      content_type: r,
      tone: n,
      length: a,
      methodology: {
        methodical: i.Methodical,
        analytical: i.Analytical,
        knowledge: i.Knowledge,
        efficient: i.Efficient,
        robust: i.Robust
      }
    }), c = ((d = l.content) == null ? void 0 : d.split(/\s+/).length) || 0, u = Math.ceil(c / 200);
    return {
      content: l.content || "",
      summary: l.summary || `Generated ${r} about ${t.substring(0, 50)}...`,
      keyPoints: i.Analytical.keyPoints,
      structure: {
        sections: i.Methodical.structure.map((f, p) => {
          var m;
          return {
            title: f,
            content: ((m = l.content) == null ? void 0 : m.split(`

`)[p]) || "",
            order: p
          };
        }),
        wordCount: c,
        readingTime: u
      },
      quality: l.quality || {
        clarity: 0.8,
        accuracy: 0.8,
        completeness: 0.8,
        relevance: 0.8,
        overall: 0.8
      },
      metadata: {
        contentType: r,
        tone: n,
        length: a,
        generatedAt: /* @__PURE__ */ new Date(),
        generationTime: l.generation_time || 0,
        model: l.model || "default"
      }
    };
  }
  /**
   * Validate input data
   *
   * @param inputs - Input data to validate
   * @returns Array of validation errors
   */
  validateInputs(t) {
    const r = [], n = t.topic || t.prompt;
    if (n || r.push({
      field: "topic",
      message: "Topic or prompt is required",
      severity: "error"
    }), n && typeof n != "string" && r.push({
      field: "topic",
      message: "Topic must be a string",
      severity: "error"
    }), n && n.length < 10 && r.push({
      field: "topic",
      message: "Topic is too short for meaningful content generation (minimum 10 characters)",
      severity: "warning"
    }), t.contentType && typeof t.contentType == "string") {
      const a = ["article", "blog_post", "documentation", "tutorial", "report", "presentation", "code", "creative"];
      a.includes(t.contentType) || r.push({
        field: "contentType",
        message: `Content type must be one of: ${a.join(", ")}`,
        severity: "error"
      });
    }
    if (t.tone && typeof t.tone == "string") {
      const a = ["formal", "informal", "technical", "creative", "persuasive"];
      a.includes(t.tone) || r.push({
        field: "tone",
        message: `Tone must be one of: ${a.join(", ")}`,
        severity: "error"
      });
    }
    return r;
  }
  /**
   * Get JSON Schema for configuration parameters
   *
   * @returns Parameter schema
   */
  getParameterSchema() {
    return {
      type: "object",
      properties: {
        contentType: {
          type: "string",
          description: "Type of content to generate",
          enum: ["article", "blog_post", "documentation", "tutorial", "report", "presentation", "code", "creative"],
          default: "article"
        },
        tone: {
          type: "string",
          description: "Tone of the generated content",
          enum: ["formal", "informal", "technical", "creative", "persuasive"],
          default: "formal"
        },
        length: {
          type: "string",
          description: "Length of the generated content",
          enum: ["short", "medium", "long"],
          default: "medium"
        },
        enableResearch: {
          type: "boolean",
          description: "Enable knowledge research for content generation",
          default: !0
        },
        enableCitations: {
          type: "boolean",
          description: "Enable citations in generated content",
          default: !1
        },
        qualityLevel: {
          type: "string",
          description: "Quality level for content generation",
          enum: ["draft", "standard", "premium"],
          default: "standard"
        }
      },
      required: []
    };
  }
  /**
   * Get available content types
   *
   * @returns Array of available content types
   */
  getAvailableContentTypes() {
    return ["article", "blog_post", "documentation", "tutorial", "report", "presentation", "code", "creative"];
  }
  /**
   * Get available tones
   *
   * @returns Array of available tones
   */
  getAvailableTones() {
    return ["formal", "informal", "technical", "creative", "persuasive"];
  }
  /**
   * Get quality metrics for generated content
   *
   * @param content - Generated content
   * @returns Promise resolving to quality metrics
   */
  async getQualityMetrics(t) {
    try {
      const r = await Se.post("/maker/quality", {
        content: t
      });
      return this.createSuccessResult(r);
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to get quality metrics"
      );
    }
  }
  /**
   * Get generation history
   *
   * @param params - Query parameters
   * @returns Promise resolving to generation history
   */
  async getGenerationHistory(t) {
    try {
      const r = await Se.get("/maker/history", t);
      return this.createSuccessResult(r);
    } catch (r) {
      return this.createErrorResult(
        r instanceof Error ? r.message : "Failed to get generation history"
      );
    }
  }
}
Z(va, "DISPLAY_NAME", "MAKER Generator"), Z(va, "DESCRIPTION", "Creative content generation using MAKER methodology (Methodical, Analytical, Knowledge-driven, Efficient, Robust)"), Z(va, "ICON", "maker"), Z(va, "CATEGORY", "generation"), Z(va, "VERSION", "1.0.0");
const Br = class Br extends vr {
  constructor(t) {
    super(t), this.config = t;
  }
  /**
   * Get parameter schema
   */
  getParameterSchema() {
    return [
      {
        name: "task",
        type: "textarea",
        label: "Task Description",
        description: "The complex reasoning task",
        required: !0,
        multiline: !0
      },
      {
        name: "reasoningMode",
        type: "select",
        label: "Reasoning Mode",
        description: "Type of reasoning to apply",
        required: !0,
        defaultValue: "collaborative",
        options: [
          { value: "collaborative", label: "Collaborative Reasoning" },
          { value: "adversarial", label: "Adversarial Reasoning" },
          { value: "debate", label: "Debate-Based" },
          { value: "consensus", label: "Consensus Building" },
          { value: "hierarchical", label: "Hierarchical Reasoning" }
        ]
      },
      {
        name: "agentCount",
        type: "number",
        label: "Number of Agents",
        defaultValue: 3,
        min: 2,
        max: 7
      },
      {
        name: "agentRoles",
        type: "multiselect",
        label: "Agent Roles",
        description: "Roles for reasoning agents",
        options: [
          { value: "analyst", label: "Analyst" },
          { value: "critic", label: "Critic" },
          { value: "synthesizer", label: "Synthesizer" },
          { value: "validator", label: "Validator" },
          { value: "explorer", label: "Explorer" },
          { value: "integrator", label: "Integrator" }
        ]
      },
      {
        name: "rounds",
        type: "number",
        label: "Reasoning Rounds",
        description: "Number of reasoning iterations",
        defaultValue: 3,
        min: 1,
        max: 10
      },
      {
        name: "confidenceThreshold",
        type: "slider",
        label: "Confidence Threshold",
        description: "Minimum confidence for consensus (0.0 - 1.0)",
        defaultValue: 0.7,
        min: 0,
        max: 1,
        step: 0.1
      },
      {
        name: "includeReasoningTrace",
        type: "boolean",
        label: "Include Reasoning Trace",
        description: "Show detailed reasoning process",
        defaultValue: !0
      },
      {
        name: "enableVoting",
        type: "boolean",
        label: "Enable Agent Voting",
        defaultValue: !0
      }
    ];
  }
  /**
   * Validate inputs
   */
  async validate(t, r) {
    const n = this.config;
    if (!n.task || n.task.trim().length === 0)
      throw new Error("Task description is required");
    if (n.agentCount < 2 || n.agentCount > 7)
      throw new Error("Agent count must be between 2 and 7");
    if (n.rounds < 1 || n.rounds > 10)
      throw new Error("Rounds must be between 1 and 10");
    if (n.confidenceThreshold < 0 || n.confidenceThreshold > 1)
      throw new Error("Confidence threshold must be between 0 and 1");
  }
  /**
   * Execute ROMA reasoning
   */
  async execute(t, r) {
    const n = this.config, a = Date.now();
    this.reportProgress(0, "Initializing ROMA reasoning...");
    try {
      const i = await fetch(`${r.apiUrl}/api/openevolve/roma/solve`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          Authorization: `Bearer ${r.apiKey}`
        },
        body: JSON.stringify({
          task: n.task,
          reasoning_mode: n.reasoningMode,
          agent_count: n.agentCount,
          agent_roles: n.agentRoles,
          rounds: n.rounds,
          confidence_threshold: n.confidenceThreshold,
          include_reasoning_trace: n.includeReasoningTrace,
          enable_voting: n.enableVoting
        })
      });
      if (!i.ok)
        throw new Error(`ROMA API error: ${i.statusText}`);
      const o = await i.json();
      this.reportProgress(100, "ROMA reasoning complete");
      const l = (Date.now() - a) / 1e3;
      return {
        success: !0,
        data: {
          solution: o.solution || "",
          confidence: o.confidence || 0,
          rounds: o.rounds || n.rounds,
          reasoningTrace: o.reasoning_trace,
          agentVotes: o.agent_votes,
          subtasks: o.subtasks,
          executionTime: l,
          consensusReached: o.consensus_reached || !1,
          qualityMetrics: o.quality_metrics || {
            coherence: 0,
            completeness: 0,
            validity: 0
          }
        },
        metrics: {
          executionTime: l,
          rounds: o.rounds || n.rounds,
          consensusReached: o.consensus_reached || !1
        }
      };
    } catch (i) {
      const o = i instanceof Error ? i.message : String(i);
      throw new Error(`ROMA execution failed: ${o}`);
    }
  }
  /**
   * Validate inputs
   */
  validateInputs(t) {
    const r = [];
    return !t.task && !this.config.task && r.push({
      field: "task",
      message: "Task is required",
      severity: "error"
    }), t.agentCount && (typeof t.agentCount != "number" || t.agentCount < 1) && r.push({
      field: "agentCount",
      message: "Agent count must be a positive number",
      severity: "error"
    }), t.rounds && (typeof t.rounds != "number" || t.rounds < 1) && r.push({
      field: "rounds",
      message: "Rounds must be a positive number",
      severity: "error"
    }), r;
  }
  /**
   * Get display name
   */
  getDisplayName() {
    return Br.DISPLAY_NAME;
  }
  /**
   * Get icon
   */
  getIcon() {
    return Br.ICON;
  }
  /**
   * Get category
   */
  getCategory() {
    return Br.CATEGORY;
  }
  /**
   * Get version
   */
  getVersion() {
    return "1.0.0";
  }
};
/**
 * Node type identifier
 */
Z(Br, "NODE_TYPE", "ROMA"), /**
 * Node display name
 */
Z(Br, "DISPLAY_NAME", "ROMA Reasoning"), /**
 * Node category
 */
Z(Br, "CATEGORY", "reasoning"), /**
 * Node icon
 */
Z(Br, "ICON", "🧠");
let _m = Br;
const Wr = class Wr extends vr {
  constructor(t) {
    super(t), this.config = t;
  }
  /**
   * Get parameter schema
   */
  getParameterSchema() {
    return [
      {
        name: "goal",
        type: "textarea",
        label: "Invention Goal",
        description: "What do you want to invent?",
        required: !0,
        multiline: !0
      },
      {
        name: "domain",
        type: "select",
        label: "Domain",
        description: "Primary domain of invention",
        required: !0,
        options: [
          { value: "technology", label: "Technology/Software" },
          { value: "hardware", label: "Hardware/Physical" },
          { value: "business", label: "Business Model" },
          { value: "process", label: "Process/System" },
          { value: "scientific", label: "Scientific Discovery" },
          { value: "creative", label: "Creative/Artistic" }
        ]
      },
      {
        name: "innovativeness",
        type: "slider",
        label: "Innovativeness Level",
        description: "How radical should the invention be? (0.0 = incremental, 1.0 = disruptive)",
        defaultValue: 0.7,
        min: 0,
        max: 1,
        step: 0.1
      },
      {
        name: "planningStages",
        type: "multiselect",
        label: "Planning Stages",
        description: "Stages to include in invention plan",
        options: [
          { value: "research", label: "Research Phase" },
          { value: "ideation", label: "Ideation" },
          { value: "prototyping", label: "Prototyping" },
          { value: "testing", label: "Testing" },
          { value: "validation", label: "Validation" },
          { value: "scaling", label: "Scaling" },
          { value: "commercialization", label: "Commercialization" }
        ]
      },
      {
        name: "constraints",
        type: "textarea",
        label: "Constraints",
        description: "Any constraints or limitations",
        multiline: !0
      },
      {
        name: "targetAudience",
        type: "text",
        label: "Target Audience"
      },
      {
        name: "includePriorArt",
        type: "boolean",
        label: "Include Prior Art Analysis",
        defaultValue: !0
      },
      {
        name: "includeFeasibility",
        type: "boolean",
        label: "Include Feasibility Analysis",
        defaultValue: !0
      },
      {
        name: "includeRoadmap",
        type: "boolean",
        label: "Include Implementation Roadmap",
        defaultValue: !0
      },
      {
        name: "detailLevel",
        type: "select",
        label: "Detail Level",
        options: [
          { value: "overview", label: "High-Level Overview" },
          { value: "detailed", label: "Detailed Plan" },
          { value: "comprehensive", label: "Comprehensive (All Details)" }
        ]
      }
    ];
  }
  /**
   * Validate inputs
   */
  async validate(t, r) {
    const n = this.config;
    if (!n.goal || n.goal.trim().length === 0)
      throw new Error("Invention goal is required");
    if (n.innovativeness < 0 || n.innovativeness > 1)
      throw new Error("Innovativeness must be between 0 and 1");
    if (!n.planningStages || n.planningStages.length === 0)
      throw new Error("At least one planning stage must be selected");
  }
  /**
   * Execute invention planning
   */
  async execute(t, r) {
    var i, o, l;
    const n = this.config, a = Date.now();
    this.reportProgress(0, "Initializing invention planning...");
    try {
      const c = await fetch(`${r.apiUrl}/api/openevolve/invention/plan`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          Authorization: `Bearer ${r.apiKey}`
        },
        body: JSON.stringify({
          goal: n.goal,
          domain: n.domain,
          innovativeness: n.innovativeness,
          planning_stages: n.planningStages,
          constraints: n.constraints,
          target_audience: n.targetAudience,
          include_prior_art: n.includePriorArt,
          include_feasibility: n.includeFeasibility,
          include_roadmap: n.includeRoadmap,
          detail_level: n.detailLevel
        })
      });
      if (!c.ok)
        throw new Error(`Invention API error: ${c.statusText}`);
      const u = await c.json();
      this.reportProgress(100, "Invention planning complete");
      const d = (Date.now() - a) / 1e3;
      return {
        success: !0,
        data: {
          plan: u.plan,
          priorArt: u.prior_art,
          feasibility: u.feasibility,
          roadmap: u.roadmap,
          leanProofs: u.lean_proofs,
          errorAnalysis: u.error_analysis,
          redTeamResults: u.red_team_results,
          blueTeamResults: u.blue_team_results,
          successCriteria: u.success_criteria,
          executionTime: d,
          qualityAssessment: u.quality_assessment || {
            innovation: 0,
            feasibility: 0,
            clarity: 0,
            completeness: 0
          }
        },
        metrics: {
          executionTime: d,
          stagesPlanned: ((o = (i = u.plan) == null ? void 0 : i.stages) == null ? void 0 : o.length) || 0,
          successCriteria: ((l = u.success_criteria) == null ? void 0 : l.length) || 0
        }
      };
    } catch (c) {
      const u = c instanceof Error ? c.message : String(c);
      throw new Error(`Invention planning failed: ${u}`);
    }
  }
  /**
   * Validate inputs
   */
  validateInputs(t) {
    const r = [];
    !t.goal && !this.config.goal && r.push({
      field: "goal",
      message: "Goal is required - what do you want to invent?",
      severity: "error"
    }), t.innovativeness && (typeof t.innovativeness != "number" || t.innovativeness < 0 || t.innovativeness > 1) && r.push({
      field: "innovativeness",
      message: "Innovativeness must be between 0 and 1",
      severity: "error"
    });
    const n = ["technology", "hardware", "business", "process", "scientific", "creative"];
    return t.domain && !n.includes(t.domain) && r.push({
      field: "domain",
      message: `Domain must be one of: ${n.join(", ")}`,
      severity: "error"
    }), r;
  }
  /**
   * Get display name
   */
  getDisplayName() {
    return Wr.DISPLAY_NAME;
  }
  /**
   * Get icon
   */
  getIcon() {
    return Wr.ICON;
  }
  /**
   * Get category
   */
  getCategory() {
    return Wr.CATEGORY;
  }
  /**
   * Get version
   */
  getVersion() {
    return "1.0.0";
  }
};
/**
 * Node type identifier
 */
Z(Wr, "NODE_TYPE", "Invention"), /**
 * Node display name
 */
Z(Wr, "DISPLAY_NAME", "End-to-End Invention Planner"), /**
 * Node category
 */
Z(Wr, "CATEGORY", "planning"), /**
 * Node icon
 */
Z(Wr, "ICON", "💡");
let jm = Wr;
class mr {
  /**
   * Private constructor to enforce singleton
   */
  constructor() {
  }
  /**
   * Register a node class
   *
   * Validates and registers a node class for later instantiation.
   * Prevents duplicate registration unless options specify otherwise.
   *
   * @param type - Unique type identifier for the node
   * @param nodeClass - Node class to register
   * @param options - Registration options
   * @throws {Error} If node type already registered and overwrite is false
   * @throws {Error} If node class validation fails
   *
   * @example
   * ```typescript
   * NodeRegistry.register('Decomposition', DecompositionNode, {
   *   source: 'internal',
   *   validate: true
   * });
   * ```
   */
  static register(t, r, n = {}) {
    const {
      overwrite: a = !1,
      source: i,
      validate: o = !0,
      allowDuplicates: l = !1
    } = n;
    if (this.nodes.has(t)) {
      if (!a && !l)
        throw new Error(
          `Node type '${t}' is already registered. Use overwrite option to replace it, or ensure you're not registering twice.`
        );
      if (!a && l)
        return;
    }
    o && this.validateNodeClass(t, r), this.nodes.set(t, r);
    const c = this.extractMetadata(t, r, i);
    this.metadataCache.set(t, c), this.registrationHistory.push({
      type: t,
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      source: i,
      action: (a && this.nodes.has(t), "register")
    }), process.env.NODE_ENV === "development" && console.log(`[NodeRegistry] Registered node type: ${t}`, {
      displayName: c.displayName,
      category: c.category,
      version: c.version,
      source: i
    });
  }
  /**
   * Unregister a node type
   *
   * Removes a node type from the registry.
   *
   * @param type - Node type to unregister
   * @returns True if node was unregistered, false if not found
   *
   * @example
   * ```typescript
   * NodeRegistry.unregister('ObsoleteNode');
   * ```
   */
  static unregister(t) {
    const r = this.nodes.delete(t);
    return this.metadataCache.delete(t), this.registrationHistory.push({
      type: t,
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      action: "unregister"
    }), r && process.env.NODE_ENV === "development" && console.log(`[NodeRegistry] Unregistered node type: ${t}`), r;
  }
  /**
   * Get a registered node class
   *
   * @param type - Node type to retrieve
   * @returns Node class or undefined if not found
   *
   * @example
   * ```typescript
   * const NodeClass = NodeRegistry.get('Decomposition');
   * if (NodeClass) {
   *   // Use the class
   * }
   * ```
   */
  static get(t) {
    return this.nodes.get(t);
  }
  /**
   * Check if a node type is registered
   *
   * @param type - Node type to check
   * @returns True if registered
   */
  static has(t) {
    return this.nodes.has(t);
  }
  /**
   * Create a node instance
   *
   * Instantiates a node from its registered type with optional configuration.
   *
   * @param type - Node type to create
   * @param id - Unique node identifier (auto-generated if not provided)
   * @param config - Optional node configuration
   * @returns Node instance or null if type not found
   * @throws {Error} If node creation fails
   *
   * @example
   * ```typescript
   * const node = NodeRegistry.create(
   *   'Decomposition',
   *   'decomp-123',
   *   {
   *     config: { strategy: 'semantic' },
   *     inputs: { problem: 'Solve X' }
   *   }
   * );
   * ```
   */
  static create(t, r, n = {}) {
    const a = this.nodes.get(t);
    if (!a)
      return console.error(`[NodeRegistry] Node type '${t}' not found`), null;
    try {
      const i = r || ht(), o = new a(i, n);
      return process.env.NODE_ENV === "development" && console.log(`[NodeRegistry] Created node instance: ${t}`, {
        id: i,
        config: Object.keys(n.config || {})
      }), o;
    } catch (i) {
      throw console.error(`[NodeRegistry] Failed to create node: ${t}`, {
        error: i instanceof Error ? i.message : String(i),
        id: r,
        config: n
      }), i;
    }
  }
  /**
   * List all registered nodes
   *
   * @returns Array of node types with metadata
   *
   * @example
   * ```typescript
   * const nodes = NodeRegistry.listAll();
   * nodes.forEach(node => {
   *   console.log(`${node.displayName}: ${node.description}`);
   * });
   * ```
   */
  static listAll() {
    const t = [];
    for (const [r, n] of this.nodes.entries()) {
      const a = this.metadataCache.get(r);
      a && t.push({ type: r, metadata: a });
    }
    return t.sort((r, n) => r.metadata.category !== n.metadata.category ? r.metadata.category.localeCompare(n.metadata.category) : r.metadata.displayName.localeCompare(n.metadata.displayName)), t;
  }
  /**
   * Get metadata for a node type
   *
   * @param type - Node type
   * @returns Node metadata or null if not found
   *
   * @example
   * ```typescript
   * const metadata = NodeRegistry.getMetadata('Decomposition');
   * console.log(metadata.displayName); // "Decomposition"
   * ```
   */
  static getMetadata(t) {
    return this.metadataCache.get(t) || null;
  }
  /**
   * Get all nodes by category
   *
   * @param category - Category to filter by
   * @returns Array of node types in the category
   *
   * @example
   * ```typescript
   * const transformNodes = NodeRegistry.getByCategory('transform');
   * ```
   */
  static getByCategory(t) {
    return this.listAll().filter((r) => r.metadata.category === t);
  }
  /**
   * Get all categories
   *
   * @returns Array of unique category names
   */
  static getCategories() {
    const t = /* @__PURE__ */ new Set();
    for (const r of this.metadataCache.values())
      t.add(r.category);
    return Array.from(t).sort();
  }
  /**
   * Search nodes by query
   *
   * Searches display names, descriptions, and categories.
   *
   * @param query - Search query
   * @returns Array of matching nodes
   *
   * @example
   * ```typescript
   * const results = NodeRegistry.search('decomposition');
   * ```
   */
  static search(t) {
    const r = t.toLowerCase();
    return this.listAll().filter((n) => {
      const { type: a, metadata: i } = n;
      return a.toLowerCase().includes(r) || i.displayName.toLowerCase().includes(r) || i.description.toLowerCase().includes(r) || i.category.toLowerCase().includes(r);
    });
  }
  /**
   * Validate node configuration against schema
   *
   * @param type - Node type
   * @param config - Configuration to validate
   * @returns Validation result
   *
   * @example
   * ```typescript
   * const result = NodeRegistry.validateConfig('Decomposition', {
   *   strategy: 'semantic',
   *   maxDepth: 5
   * });
   * if (!result.valid) {
   *   console.error(result.errors);
   * }
   * ```
   */
  static validateConfig(t, r) {
    const n = [], a = [], i = this.metadataCache.get(t);
    if (!i)
      return n.push({
        field: "type",
        message: `Node type '${t}' not found in registry`,
        code: "NODE_TYPE_NOT_FOUND"
      }), { valid: !1, errors: n, warnings: a };
    const o = i.config;
    if (!o)
      return { valid: !0, errors: n, warnings: a };
    for (const [l, c] of Object.entries(o)) {
      const u = r[l];
      if (c.required && u == null) {
        n.push({
          field: l,
          message: `Required configuration field '${l}' is missing`,
          code: "MISSING_REQUIRED_FIELD"
        });
        continue;
      }
      if (u == null)
        continue;
      const d = Array.isArray(u) ? "array" : typeof u, f = c.type;
      if (d !== f) {
        n.push({
          field: l,
          message: `Field '${l}' must be of type ${f}, got ${d}`,
          code: "TYPE_MISMATCH"
        });
        continue;
      }
      c.enum && !c.enum.includes(u) && n.push({
        field: l,
        message: `Field '${l}' must be one of: ${c.enum.join(", ")}`,
        code: "INVALID_ENUM_VALUE"
      }), f === "number" && (c.min !== void 0 && u < c.min && n.push({
        field: l,
        message: `Field '${l}' must be at least ${c.min}`,
        code: "VALUE_TOO_SMALL"
      }), c.max !== void 0 && u > c.max && n.push({
        field: l,
        message: `Field '${l}' must be at most ${c.max}`,
        code: "VALUE_TOO_LARGE"
      }));
    }
    return {
      valid: n.length === 0,
      errors: n,
      warnings: a
    };
  }
  /**
   * Get registry statistics
   *
   * @returns Registry statistics
   */
  static getStats() {
    const t = {};
    for (const n of this.metadataCache.values())
      t[n.category] = (t[n.category] || 0) + 1;
    const r = this.registrationHistory[this.registrationHistory.length - 1];
    return {
      totalNodes: this.nodes.size,
      nodesByCategory: t,
      lastRegistered: r == null ? void 0 : r.type,
      initializedAt: this.initializedAt
    };
  }
  /**
   * Clear all registered nodes
   *
   * Primarily used for testing purposes.
   */
  static clear() {
    this.nodes.clear(), this.metadataCache.clear(), this.registrationHistory = [], this.initializedAt = (/* @__PURE__ */ new Date()).toISOString();
  }
  /**
   * Export registry state
   *
   * @returns JSON-serializable registry state
   */
  static exportState() {
    return {
      nodes: Array.from(this.nodes.keys()),
      metadata: Array.from(this.metadataCache.entries()),
      stats: this.getStats(),
      history: this.registrationHistory
    };
  }
  // ==========================================================================
  // Private Helper Methods
  // ==========================================================================
  /**
   * Validate a node class before registration
   */
  static validateNodeClass(t, r) {
    const a = ["DISPLAY_NAME", "DESCRIPTION", "ICON", "CATEGORY", "VERSION"].filter((o) => !(o in r));
    if (a.length > 0)
      throw new Error(
        `Node class '${t}' is missing required static properties: ${a.join(", ")}`
      );
    if (typeof r != "function")
      throw new Error(`Node class '${t}' must be a constructor function`);
    const i = r.prototype;
    if (!i || !i.execute || !i.validateInputs)
      throw new Error(
        `Node class '${t}' must extend OpenEvolveBaseNode and implement required methods`
      );
    if (typeof r.DISPLAY_NAME != "string" || !r.DISPLAY_NAME.trim())
      throw new Error(`Node class '${t}' must have a non-empty DISPLAY_NAME string`);
    if (typeof r.VERSION != "string")
      throw new Error(`Node class '${t}' must have a VERSION string`);
    if (typeof r.CATEGORY != "string" || !r.CATEGORY.trim())
      throw new Error(`Node class '${t}' must have a non-empty CATEGORY string`);
  }
  /**
   * Extract metadata from a node class
   */
  static extractMetadata(t, r, n) {
    return {
      type: t,
      displayName: r.DISPLAY_NAME,
      description: r.DESCRIPTION,
      icon: r.ICON,
      category: r.CATEGORY,
      version: r.VERSION,
      inputs: [],
      // Would need to be populated by node class
      outputs: [],
      // Would need to be populated by node class
      config: {},
      // Would need to be populated by node class
      registeredAt: (/* @__PURE__ */ new Date()).toISOString(),
      source: n
    };
  }
}
/**
 * Internal storage for registered node classes
 */
Z(mr, "nodes", /* @__PURE__ */ new Map()), /**
 * Metadata cache for quick lookups
 */
Z(mr, "metadataCache", /* @__PURE__ */ new Map()), /**
 * Registry initialization timestamp
 */
Z(mr, "initializedAt", (/* @__PURE__ */ new Date()).toISOString()), /**
 * Registration history for debugging
 */
Z(mr, "registrationHistory", []);
function jA(e, t) {
  for (const [r, n] of Object.entries(e))
    try {
      mr.register(r, n, t);
    } catch (a) {
      console.error(`Failed to register node type '${r}':`, a);
    }
}
function sz(e) {
  const { type: t, id: r, ...n } = e;
  return mr.create(t, r, n);
}
jA({
  Decomposition: da,
  Solution: bm,
  Verification: xm,
  Evolution: fa,
  Adversarial: ma,
  KnowledgeQuery: ha,
  LeanAIDE: pa,
  CrewAI: ga,
  MDAP: ya,
  MAKER: va,
  ROMA: _m,
  Invention: jm
}, {
  source: "core",
  validate: !0,
  allowDuplicates: !1
});
var SA = /* @__PURE__ */ ((e) => (e.IDLE = "idle", e.RUNNING = "running", e.COMPLETED = "completed", e.FAILED = "failed", e.CANCELLED = "cancelled", e.PAUSED = "paused", e))(SA || {});
const oc = {
  debug: !1,
  timeout: 3e5,
  // 5 minutes
  retryAttempts: 0,
  retryDelay: 1e3,
  enableMetrics: !0
};
class fn extends Error {
  /**
   * Create a new NodeExecutionError
   *
   * @param nodeName - Name of the node
   * @param message - Error message
   * @param errorCode - Error code (e.g., 'VALIDATION_ERROR', 'TIMEOUT')
   * @param details - Additional error context
   * @param originalError - Original error if wrapping
   * @param timestamp - Error timestamp (defaults to now)
   */
  constructor(r, n, a = "NODE_ERROR", i = {}, o, l = (/* @__PURE__ */ new Date()).toISOString()) {
    super(`[${r}] ${n}`);
    /** Name of the node that threw the error */
    Z(this, "nodeName");
    /** Unique error code */
    Z(this, "errorCode");
    /** Additional error details */
    Z(this, "details");
    /** Timestamp when error occurred */
    Z(this, "timestamp");
    /** Original error if this wraps another error */
    Z(this, "originalError");
    this.name = "NodeExecutionError", this.nodeName = r, this.errorCode = a, this.details = i, this.originalError = o, this.timestamp = l, Error.captureStackTrace && Error.captureStackTrace(this, fn);
  }
  /**
   * Convert error to JSON-serializable object
   */
  toJSON() {
    return {
      nodeName: this.nodeName,
      errorCode: this.errorCode,
      message: this.message,
      details: this.details,
      timestamp: this.timestamp,
      stack: this.stack,
      originalError: this.originalError ? {
        message: this.originalError.message,
        stack: this.originalError.stack
      } : void 0
    };
  }
  /**
   * Convert error to string representation
   */
  toString() {
    return JSON.stringify(this.toJSON(), null, 2);
  }
}
class lz {
  /**
   * Create a new base node instance
   *
   * @param id - Unique node identifier
   * @param config - Node configuration (merged with defaults)
   */
  constructor(t, r = {}) {
    /** Unique node identifier */
    Z(this, "id");
    /** Node configuration */
    Z(this, "config");
    /** Current execution status */
    Z(this, "status");
    /** Execution metrics storage */
    Z(this, "metrics");
    /** Number of execution attempts */
    Z(this, "executionAttempts");
    this.id = t, this.config = { ...oc, ...r }, this.status = "idle", this.metrics = /* @__PURE__ */ new Map(), this.executionAttempts = 0;
  }
  // ==========================================================================
  // Lifecycle Hooks - Override in subclass as needed
  // ==========================================================================
  /**
   * Hook called before execution starts
   *
   * Override this to perform setup, initialization, or pre-execution
   * validation. Throwing here will prevent execution.
   *
   * @param inputs - Input data for the node
   * @param context - Execution context
   */
  async beforeExecute(t, r) {
    this.config.debug && r.log("debug", `[${this.getDisplayName()}] Before execute hook called`, {
      nodeId: this.id,
      inputs: Object.keys(t)
    });
  }
  /**
   * Hook called after successful execution
   *
   * Override this to perform cleanup, post-processing, or result
   * enrichment after successful execution.
   *
   * @param result - Result from execute()
   * @param context - Execution context
   */
  async afterExecute(t, r) {
    this.config.debug && r.log("debug", `[${this.getDisplayName()}] After execute hook called`, {
      nodeId: this.id,
      success: t.success,
      executionTime: t.metrics.executionTime
    }), r.storeArtifact(`${this.id}_result`, t);
  }
  /**
   * Hook called when execution encounters an error
   *
   * Override this to perform error logging, recovery attempts, or
   * cleanup after failed execution.
   *
   * @param error - The error that occurred
   * @param context - Execution context
   */
  async onError(t, r) {
    this.config.debug && r.log("error", `[${this.getDisplayName()}] Error hook called`, {
      nodeId: this.id,
      error: t.message,
      stack: t.stack
    }), r.storeArtifact(`${this.id}_error`, {
      message: t.message,
      stack: t.stack,
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    });
  }
  // ==========================================================================
  // Public Methods
  // ==========================================================================
  /**
   * Execute the node with comprehensive error handling
   *
   * Wraps execute() with try-catch, lifecycle hooks, validation,
   * timeout handling, and retry logic.
   *
   * @param inputs - Input data for the node
   * @param context - Execution context
   * @returns Promise resolving to node execution result
   */
  async executeSafe(t, r) {
    const n = Date.now();
    this.status = "running";
    try {
      if (r.isCancelled())
        throw this.status = "cancelled", new fn(
          this.getDisplayName(),
          "Execution cancelled before start",
          "CANCELLED",
          { nodeId: this.id }
        );
      const a = this.validateInputs(t);
      if (a.length > 0)
        throw new fn(
          this.getDisplayName(),
          `Input validation failed: ${a.map((l) => l.message).join(", ")}`,
          "VALIDATION_ERROR",
          { validationErrors: a }
        );
      const i = this.validateConfig();
      if (i.length > 0)
        throw new fn(
          this.getDisplayName(),
          `Configuration validation failed: ${i.map((l) => l.message).join(", ")}`,
          "CONFIG_ERROR",
          { configErrors: i }
        );
      await this.beforeExecute(t, r);
      const o = await this.executeWithTimeout(t, r);
      return await this.afterExecute(o, r), this.status = "completed", o;
    } catch (a) {
      this.status = "failed";
      const i = a instanceof fn ? a : new fn(
        this.getDisplayName(),
        a instanceof Error ? a.message : String(a),
        "EXECUTION_ERROR",
        {},
        a instanceof Error ? a : void 0
      );
      return await this.onError(i, r), {
        success: !1,
        outputs: {},
        metrics: {
          executionTime: Date.now() - n,
          startTime: n,
          endTime: Date.now()
        },
        error: {
          message: i.message,
          code: i.errorCode,
          stack: i.stack,
          context: i.details
        }
      };
    }
  }
  /**
   * Validate the node's configuration
   *
   * @returns Array of validation errors (empty if valid)
   */
  validateConfig() {
    const t = [];
    return this.config.timeout !== void 0 && this.config.timeout <= 0 && t.push({
      field: "timeout",
      message: "Timeout must be greater than 0",
      code: "INVALID_TIMEOUT"
    }), this.config.retryAttempts !== void 0 && this.config.retryAttempts < 0 && t.push({
      field: "retryAttempts",
      message: "Retry attempts cannot be negative",
      code: "INVALID_RETRY_ATTEMPTS"
    }), this.config.retryDelay !== void 0 && this.config.retryDelay < 0 && t.push({
      field: "retryDelay",
      message: "Retry delay cannot be negative",
      code: "INVALID_RETRY_DELAY"
    }), t;
  }
  /**
   * Get current node status
   */
  getStatus() {
    return this.status;
  }
  /**
   * Get node ID
   */
  getId() {
    return this.id;
  }
  /**
   * Get node configuration
   */
  getConfig() {
    return { ...this.config };
  }
  /**
   * Update node configuration
   *
   * @param config - Partial configuration to update
   */
  updateConfig(t) {
    this.config = { ...this.config, ...t };
  }
  /**
   * Get execution metrics
   *
   * @returns Map of metric names to values
   */
  getMetrics() {
    return new Map(this.metrics);
  }
  /**
   * Reset node state
   */
  reset() {
    this.status = "idle", this.metrics.clear(), this.executionAttempts = 0;
  }
  // ==========================================================================
  // Metadata Methods - Override in subclass
  // ==========================================================================
  /**
   * Get human-readable display name
   *
   * @returns Display name for the node
   */
  getDisplayName() {
    return this.constructor.name.replace("Node", "");
  }
  /**
   * Get node description
   *
   * @returns Description of what the node does
   */
  getDescription() {
    return "An OpenEvolve workflow node";
  }
  /**
   * Get icon for UI display
   *
   * @returns Icon name or emoji
   */
  getIcon() {
    return "⚙️";
  }
  /**
   * Get node category
   *
   * @returns Category name (e.g., 'transform', 'output', 'logic')
   */
  getCategory() {
    return "general";
  }
  /**
   * Get node version
   *
   * @returns Version string
   */
  getVersion() {
    return "1.0.0";
  }
  /**
   * Get node metadata
   *
   * Returns comprehensive metadata about this node instance, including
   * type information, display properties, and input/output schemas.
   *
   * @returns Node metadata object
   */
  getMetadata() {
    return {
      type: this.constructor.name,
      displayName: this.getDisplayName(),
      description: this.getDescription(),
      icon: this.getIcon(),
      category: this.getCategory(),
      version: this.getVersion(),
      inputs: this.getParameterSchema(),
      outputs: {
        type: "object",
        properties: {}
      }
    };
  }
  // ==========================================================================
  // Private/Protected Helper Methods
  // ==========================================================================
  /**
   * Execute with timeout and retry logic
   *
   * @param inputs - Input data
   * @param context - Execution context
   * @returns Promise resolving to execution result
   */
  async executeWithTimeout(t, r) {
    const n = (this.config.retryAttempts ?? 0) + 1;
    let a = null;
    for (let i = 0; i < n; i++) {
      this.executionAttempts = i + 1;
      try {
        return await Promise.race([
          this.execute(t, r),
          this.createTimeoutPromise(this.config.timeout ?? oc.timeout)
        ]);
      } catch (o) {
        a = o instanceof Error ? o : new Error(String(o)), i < n - 1 && (r.log("warn", `[${this.getDisplayName()}] Execution failed, retrying...`, {
          attempt: i + 1,
          maxAttempts: n,
          error: a.message
        }), await this.delay(this.config.retryDelay ?? oc.retryDelay));
      }
    }
    throw a;
  }
  /**
   * Create a timeout promise
   *
   * @param timeoutMs - Timeout in milliseconds
   * @returns Promise that rejects after timeout
   */
  createTimeoutPromise(t) {
    return new Promise((r, n) => {
      setTimeout(() => {
        n(new fn(
          this.getDisplayName(),
          `Execution timeout after ${t}ms`,
          "TIMEOUT",
          { timeout: t }
        ));
      }, t);
    });
  }
  /**
   * Delay execution for specified milliseconds
   *
   * @param ms - Milliseconds to delay
   * @returns Promise that resolves after delay
   */
  delay(t) {
    return new Promise((r) => setTimeout(r, t));
  }
  /**
   * Record a custom metric
   *
   * @param name - Metric name
   * @param value - Metric value
   */
  recordMetric(t, r) {
    this.metrics.set(t, r);
  }
  /**
   * Create a standard node result
   *
   * @param outputs - Output data
   * @param startTime - Execution start time
   * @param metadata - Optional metadata
   * @returns Node result object
   */
  createResult(t, r, n) {
    const a = Date.now();
    return {
      success: !0,
      outputs: t,
      metrics: {
        executionTime: a - r,
        startTime: r,
        endTime: a,
        customMetrics: this.config.enableMetrics ? Object.fromEntries(this.metrics) : void 0
      },
      metadata: n
    };
  }
}
RegistryClass;
const cz = RegistryClass;
function uz() {
  return RegistryClass.listAll();
}
function dz(e, t, r) {
  return mr.create(e, t, r);
}
function fz(e) {
  return mr.search(e);
}
function mz(e) {
  return mr.getByCategory(e);
}
function hz() {
  return mr.getCategories();
}
function pz(e, t) {
  return mr.validateConfig(e, t);
}
function gz() {
  return mr.getStats();
}
var Wo = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function st(e) {
  return e && e.__esModule && Object.prototype.hasOwnProperty.call(e, "default") ? e.default : e;
}
var Vo = { exports: {} }, pi = {};
/**
 * @license React
 * react-jsx-runtime.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var mg;
function kA() {
  if (mg) return pi;
  mg = 1;
  var e = I, t = Symbol.for("react.element"), r = Symbol.for("react.fragment"), n = Object.prototype.hasOwnProperty, a = e.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner, i = { key: !0, ref: !0, __self: !0, __source: !0 };
  function o(l, c, u) {
    var d, f = {}, p = null, m = null;
    u !== void 0 && (p = "" + u), c.key !== void 0 && (p = "" + c.key), c.ref !== void 0 && (m = c.ref);
    for (d in c) n.call(c, d) && !i.hasOwnProperty(d) && (f[d] = c[d]);
    if (l && l.defaultProps) for (d in c = l.defaultProps, c) f[d] === void 0 && (f[d] = c[d]);
    return { $$typeof: t, type: l, key: p, ref: m, props: f, _owner: a.current };
  }
  return pi.Fragment = r, pi.jsx = o, pi.jsxs = o, pi;
}
var gi = {};
/**
 * @license React
 * react-jsx-runtime.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var hg;
function EA() {
  return hg || (hg = 1, process.env.NODE_ENV !== "production" && (function() {
    var e = I, t = Symbol.for("react.element"), r = Symbol.for("react.portal"), n = Symbol.for("react.fragment"), a = Symbol.for("react.strict_mode"), i = Symbol.for("react.profiler"), o = Symbol.for("react.provider"), l = Symbol.for("react.context"), c = Symbol.for("react.forward_ref"), u = Symbol.for("react.suspense"), d = Symbol.for("react.suspense_list"), f = Symbol.for("react.memo"), p = Symbol.for("react.lazy"), m = Symbol.for("react.offscreen"), y = Symbol.iterator, h = "@@iterator";
    function g(O) {
      if (O === null || typeof O != "object")
        return null;
      var te = y && O[y] || O[h];
      return typeof te == "function" ? te : null;
    }
    var v = e.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
    function b(O) {
      {
        for (var te = arguments.length, pe = new Array(te > 1 ? te - 1 : 0), Ce = 1; Ce < te; Ce++)
          pe[Ce - 1] = arguments[Ce];
        w("error", O, pe);
      }
    }
    function w(O, te, pe) {
      {
        var Ce = v.ReactDebugCurrentFrame, Ge = Ce.getStackAddendum();
        Ge !== "" && (te += "%s", pe = pe.concat([Ge]));
        var rt = pe.map(function(We) {
          return String(We);
        });
        rt.unshift("Warning: " + te), Function.prototype.apply.call(console[O], console, rt);
      }
    }
    var j = !1, x = !1, _ = !1, S = !1, k = !1, E;
    E = Symbol.for("react.module.reference");
    function L(O) {
      return !!(typeof O == "string" || typeof O == "function" || O === n || O === i || k || O === a || O === u || O === d || S || O === m || j || x || _ || typeof O == "object" && O !== null && (O.$$typeof === p || O.$$typeof === f || O.$$typeof === o || O.$$typeof === l || O.$$typeof === c || // This needs to include all possible module reference object
      // types supported by any Flight configuration anywhere since
      // we don't know which Flight build this will end up being used
      // with.
      O.$$typeof === E || O.getModuleId !== void 0));
    }
    function P(O, te, pe) {
      var Ce = O.displayName;
      if (Ce)
        return Ce;
      var Ge = te.displayName || te.name || "";
      return Ge !== "" ? pe + "(" + Ge + ")" : pe;
    }
    function A(O) {
      return O.displayName || "Context";
    }
    function T(O) {
      if (O == null)
        return null;
      if (typeof O.tag == "number" && b("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."), typeof O == "function")
        return O.displayName || O.name || null;
      if (typeof O == "string")
        return O;
      switch (O) {
        case n:
          return "Fragment";
        case r:
          return "Portal";
        case i:
          return "Profiler";
        case a:
          return "StrictMode";
        case u:
          return "Suspense";
        case d:
          return "SuspenseList";
      }
      if (typeof O == "object")
        switch (O.$$typeof) {
          case l:
            var te = O;
            return A(te) + ".Consumer";
          case o:
            var pe = O;
            return A(pe._context) + ".Provider";
          case c:
            return P(O, O.render, "ForwardRef");
          case f:
            var Ce = O.displayName || null;
            return Ce !== null ? Ce : T(O.type) || "Memo";
          case p: {
            var Ge = O, rt = Ge._payload, We = Ge._init;
            try {
              return T(We(rt));
            } catch {
              return null;
            }
          }
        }
      return null;
    }
    var M = Object.assign, N = 0, q, V, B, ee, U, G, Y;
    function Q() {
    }
    Q.__reactDisabledLog = !0;
    function ne() {
      {
        if (N === 0) {
          q = console.log, V = console.info, B = console.warn, ee = console.error, U = console.group, G = console.groupCollapsed, Y = console.groupEnd;
          var O = {
            configurable: !0,
            enumerable: !0,
            value: Q,
            writable: !0
          };
          Object.defineProperties(console, {
            info: O,
            log: O,
            warn: O,
            error: O,
            group: O,
            groupCollapsed: O,
            groupEnd: O
          });
        }
        N++;
      }
    }
    function z() {
      {
        if (N--, N === 0) {
          var O = {
            configurable: !0,
            enumerable: !0,
            writable: !0
          };
          Object.defineProperties(console, {
            log: M({}, O, {
              value: q
            }),
            info: M({}, O, {
              value: V
            }),
            warn: M({}, O, {
              value: B
            }),
            error: M({}, O, {
              value: ee
            }),
            group: M({}, O, {
              value: U
            }),
            groupCollapsed: M({}, O, {
              value: G
            }),
            groupEnd: M({}, O, {
              value: Y
            })
          });
        }
        N < 0 && b("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
      }
    }
    var se = v.ReactCurrentDispatcher, oe;
    function ie(O, te, pe) {
      {
        if (oe === void 0)
          try {
            throw Error();
          } catch (Ge) {
            var Ce = Ge.stack.trim().match(/\n( *(at )?)/);
            oe = Ce && Ce[1] || "";
          }
        return `
` + oe + O;
      }
    }
    var K = !1, ae;
    {
      var fe = typeof WeakMap == "function" ? WeakMap : Map;
      ae = new fe();
    }
    function $(O, te) {
      if (!O || K)
        return "";
      {
        var pe = ae.get(O);
        if (pe !== void 0)
          return pe;
      }
      var Ce;
      K = !0;
      var Ge = Error.prepareStackTrace;
      Error.prepareStackTrace = void 0;
      var rt;
      rt = se.current, se.current = null, ne();
      try {
        if (te) {
          var We = function() {
            throw Error();
          };
          if (Object.defineProperty(We.prototype, "props", {
            set: function() {
              throw Error();
            }
          }), typeof Reflect == "object" && Reflect.construct) {
            try {
              Reflect.construct(We, []);
            } catch (Vt) {
              Ce = Vt;
            }
            Reflect.construct(O, [], We);
          } else {
            try {
              We.call();
            } catch (Vt) {
              Ce = Vt;
            }
            O.call(We.prototype);
          }
        } else {
          try {
            throw Error();
          } catch (Vt) {
            Ce = Vt;
          }
          O();
        }
      } catch (Vt) {
        if (Vt && Ce && typeof Vt.stack == "string") {
          for (var Fe = Vt.stack.split(`
`), zt = Ce.stack.split(`
`), gt = Fe.length - 1, vt = zt.length - 1; gt >= 1 && vt >= 0 && Fe[gt] !== zt[vt]; )
            vt--;
          for (; gt >= 1 && vt >= 0; gt--, vt--)
            if (Fe[gt] !== zt[vt]) {
              if (gt !== 1 || vt !== 1)
                do
                  if (gt--, vt--, vt < 0 || Fe[gt] !== zt[vt]) {
                    var sr = `
` + Fe[gt].replace(" at new ", " at ");
                    return O.displayName && sr.includes("<anonymous>") && (sr = sr.replace("<anonymous>", O.displayName)), typeof O == "function" && ae.set(O, sr), sr;
                  }
                while (gt >= 1 && vt >= 0);
              break;
            }
        }
      } finally {
        K = !1, se.current = rt, z(), Error.prepareStackTrace = Ge;
      }
      var aa = O ? O.displayName || O.name : "", Cn = aa ? ie(aa) : "";
      return typeof O == "function" && ae.set(O, Cn), Cn;
    }
    function Ee(O, te, pe) {
      return $(O, !1);
    }
    function re(O) {
      var te = O.prototype;
      return !!(te && te.isReactComponent);
    }
    function Pe(O, te, pe) {
      if (O == null)
        return "";
      if (typeof O == "function")
        return $(O, re(O));
      if (typeof O == "string")
        return ie(O);
      switch (O) {
        case u:
          return ie("Suspense");
        case d:
          return ie("SuspenseList");
      }
      if (typeof O == "object")
        switch (O.$$typeof) {
          case c:
            return Ee(O.render);
          case f:
            return Pe(O.type, te, pe);
          case p: {
            var Ce = O, Ge = Ce._payload, rt = Ce._init;
            try {
              return Pe(rt(Ge), te, pe);
            } catch {
            }
          }
        }
      return "";
    }
    var Oe = Object.prototype.hasOwnProperty, Ne = {}, ke = v.ReactDebugCurrentFrame;
    function Me(O) {
      if (O) {
        var te = O._owner, pe = Pe(O.type, O._source, te ? te.type : null);
        ke.setExtraStackFrame(pe);
      } else
        ke.setExtraStackFrame(null);
    }
    function Ae(O, te, pe, Ce, Ge) {
      {
        var rt = Function.call.bind(Oe);
        for (var We in O)
          if (rt(O, We)) {
            var Fe = void 0;
            try {
              if (typeof O[We] != "function") {
                var zt = Error((Ce || "React class") + ": " + pe + " type `" + We + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof O[We] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
                throw zt.name = "Invariant Violation", zt;
              }
              Fe = O[We](te, We, Ce, pe, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
            } catch (gt) {
              Fe = gt;
            }
            Fe && !(Fe instanceof Error) && (Me(Ge), b("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", Ce || "React class", pe, We, typeof Fe), Me(null)), Fe instanceof Error && !(Fe.message in Ne) && (Ne[Fe.message] = !0, Me(Ge), b("Failed %s type: %s", pe, Fe.message), Me(null));
          }
      }
    }
    var C = Array.isArray;
    function R(O) {
      return C(O);
    }
    function W(O) {
      {
        var te = typeof Symbol == "function" && Symbol.toStringTag, pe = te && O[Symbol.toStringTag] || O.constructor.name || "Object";
        return pe;
      }
    }
    function ue(O) {
      try {
        return le(O), !1;
      } catch {
        return !0;
      }
    }
    function le(O) {
      return "" + O;
    }
    function ce(O) {
      if (ue(O))
        return b("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", W(O)), le(O);
    }
    var _e = v.ReactCurrentOwner, Be = {
      key: !0,
      ref: !0,
      __self: !0,
      __source: !0
    }, Ue, pt;
    function or(O) {
      if (Oe.call(O, "ref")) {
        var te = Object.getOwnPropertyDescriptor(O, "ref").get;
        if (te && te.isReactWarning)
          return !1;
      }
      return O.ref !== void 0;
    }
    function Qt(O) {
      if (Oe.call(O, "key")) {
        var te = Object.getOwnPropertyDescriptor(O, "key").get;
        if (te && te.isReactWarning)
          return !1;
      }
      return O.key !== void 0;
    }
    function ea(O, te) {
      typeof O.ref == "string" && _e.current;
    }
    function sn(O, te) {
      {
        var pe = function() {
          Ue || (Ue = !0, b("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", te));
        };
        pe.isReactWarning = !0, Object.defineProperty(O, "key", {
          get: pe,
          configurable: !0
        });
      }
    }
    function ln(O, te) {
      {
        var pe = function() {
          pt || (pt = !0, b("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", te));
        };
        pe.isReactWarning = !0, Object.defineProperty(O, "ref", {
          get: pe,
          configurable: !0
        });
      }
    }
    var ta = function(O, te, pe, Ce, Ge, rt, We) {
      var Fe = {
        // This tag allows us to uniquely identify this as a React Element
        $$typeof: t,
        // Built-in properties that belong on the element
        type: O,
        key: te,
        ref: pe,
        props: We,
        // Record the component responsible for creating this element.
        _owner: rt
      };
      return Fe._store = {}, Object.defineProperty(Fe._store, "validated", {
        configurable: !1,
        enumerable: !1,
        writable: !0,
        value: !1
      }), Object.defineProperty(Fe, "_self", {
        configurable: !1,
        enumerable: !1,
        writable: !1,
        value: Ce
      }), Object.defineProperty(Fe, "_source", {
        configurable: !1,
        enumerable: !1,
        writable: !1,
        value: Ge
      }), Object.freeze && (Object.freeze(Fe.props), Object.freeze(Fe)), Fe;
    };
    function An(O, te, pe, Ce, Ge) {
      {
        var rt, We = {}, Fe = null, zt = null;
        pe !== void 0 && (ce(pe), Fe = "" + pe), Qt(te) && (ce(te.key), Fe = "" + te.key), or(te) && (zt = te.ref, ea(te, Ge));
        for (rt in te)
          Oe.call(te, rt) && !Be.hasOwnProperty(rt) && (We[rt] = te[rt]);
        if (O && O.defaultProps) {
          var gt = O.defaultProps;
          for (rt in gt)
            We[rt] === void 0 && (We[rt] = gt[rt]);
        }
        if (Fe || zt) {
          var vt = typeof O == "function" ? O.displayName || O.name || "Unknown" : O;
          Fe && sn(We, vt), zt && ln(We, vt);
        }
        return ta(O, Fe, zt, Ge, Ce, _e.current, We);
      }
    }
    var cn = v.ReactCurrentOwner, un = v.ReactDebugCurrentFrame;
    function Fr(O) {
      if (O) {
        var te = O._owner, pe = Pe(O.type, O._source, te ? te.type : null);
        un.setExtraStackFrame(pe);
      } else
        un.setExtraStackFrame(null);
    }
    var ra;
    ra = !1;
    function na(O) {
      return typeof O == "object" && O !== null && O.$$typeof === t;
    }
    function li() {
      {
        if (cn.current) {
          var O = T(cn.current.type);
          if (O)
            return `

Check the render method of \`` + O + "`.";
        }
        return "";
      }
    }
    function Lo(O) {
      return "";
    }
    var ci = {};
    function Fo(O) {
      {
        var te = li();
        if (!te) {
          var pe = typeof O == "string" ? O : O.displayName || O.name;
          pe && (te = `

Check the top-level render call using <` + pe + ">.");
        }
        return te;
      }
    }
    function ui(O, te) {
      {
        if (!O._store || O._store.validated || O.key != null)
          return;
        O._store.validated = !0;
        var pe = Fo(te);
        if (ci[pe])
          return;
        ci[pe] = !0;
        var Ce = "";
        O && O._owner && O._owner !== cn.current && (Ce = " It was passed a child from " + T(O._owner.type) + "."), Fr(O), b('Each child in a list should have a unique "key" prop.%s%s See https://reactjs.org/link/warning-keys for more information.', pe, Ce), Fr(null);
      }
    }
    function di(O, te) {
      {
        if (typeof O != "object")
          return;
        if (R(O))
          for (var pe = 0; pe < O.length; pe++) {
            var Ce = O[pe];
            na(Ce) && ui(Ce, te);
          }
        else if (na(O))
          O._store && (O._store.validated = !0);
        else if (O) {
          var Ge = g(O);
          if (typeof Ge == "function" && Ge !== O.entries)
            for (var rt = Ge.call(O), We; !(We = rt.next()).done; )
              na(We.value) && ui(We.value, te);
        }
      }
    }
    function qo(O) {
      {
        var te = O.type;
        if (te == null || typeof te == "string")
          return;
        var pe;
        if (typeof te == "function")
          pe = te.propTypes;
        else if (typeof te == "object" && (te.$$typeof === c || // Note: Memo only checks outer props here.
        // Inner props are checked in the reconciler.
        te.$$typeof === f))
          pe = te.propTypes;
        else
          return;
        if (pe) {
          var Ce = T(te);
          Ae(pe, O.props, "prop", Ce, O);
        } else if (te.PropTypes !== void 0 && !ra) {
          ra = !0;
          var Ge = T(te);
          b("Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?", Ge || "Unknown");
        }
        typeof te.getDefaultProps == "function" && !te.getDefaultProps.isReactClassApproved && b("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
      }
    }
    function Uo(O) {
      {
        for (var te = Object.keys(O.props), pe = 0; pe < te.length; pe++) {
          var Ce = te[pe];
          if (Ce !== "children" && Ce !== "key") {
            Fr(O), b("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", Ce), Fr(null);
            break;
          }
        }
        O.ref !== null && (Fr(O), b("Invalid attribute `ref` supplied to `React.Fragment`."), Fr(null));
      }
    }
    var fi = {};
    function mi(O, te, pe, Ce, Ge, rt) {
      {
        var We = L(O);
        if (!We) {
          var Fe = "";
          (O === void 0 || typeof O == "object" && O !== null && Object.keys(O).length === 0) && (Fe += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.");
          var zt = Lo();
          zt ? Fe += zt : Fe += li();
          var gt;
          O === null ? gt = "null" : R(O) ? gt = "array" : O !== void 0 && O.$$typeof === t ? (gt = "<" + (T(O.type) || "Unknown") + " />", Fe = " Did you accidentally export a JSX literal instead of a component?") : gt = typeof O, b("React.jsx: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", gt, Fe);
        }
        var vt = An(O, te, pe, Ge, rt);
        if (vt == null)
          return vt;
        if (We) {
          var sr = te.children;
          if (sr !== void 0)
            if (Ce)
              if (R(sr)) {
                for (var aa = 0; aa < sr.length; aa++)
                  di(sr[aa], O);
                Object.freeze && Object.freeze(sr);
              } else
                b("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
            else
              di(sr, O);
        }
        if (Oe.call(te, "key")) {
          var Cn = T(O), Vt = Object.keys(te).filter(function(zE) {
            return zE !== "key";
          }), tc = Vt.length > 0 ? "{key: someKey, " + Vt.join(": ..., ") + ": ...}" : "{key: someKey}";
          if (!fi[Cn + tc]) {
            var UE = Vt.length > 0 ? "{" + Vt.join(": ..., ") + ": ...}" : "{}";
            b(`A props object containing a "key" prop is being spread into JSX:
  let props = %s;
  <%s {...props} />
React keys must be passed directly to JSX without using spread:
  let props = %s;
  <%s key={someKey} {...props} />`, tc, Cn, UE, Cn), fi[Cn + tc] = !0;
          }
        }
        return O === n ? Uo(vt) : qo(vt), vt;
      }
    }
    function jt(O, te, pe) {
      return mi(O, te, pe, !0);
    }
    function Jt(O, te, pe) {
      return mi(O, te, pe, !1);
    }
    var Zt = Jt, St = jt;
    gi.Fragment = n, gi.jsx = Zt, gi.jsxs = St;
  })()), gi;
}
var pg;
function AA() {
  return pg || (pg = 1, process.env.NODE_ENV === "production" ? Vo.exports = kA() : Vo.exports = EA()), Vo.exports;
}
var s = AA();
function X_(e = {}) {
  const t = {
    ...cg,
    ...e
  }, r = Kh(t);
  let n = { ...t };
  const a = /* @__PURE__ */ new Set(), i = () => {
    a.forEach((l) => l({ ...n }));
  };
  return {
    ...r,
    // State management
    getEnhancedState: () => ({ ...n }),
    subscribeToEnhancedState: (l) => (a.add(l), () => a.delete(l)),
    updateEnhancedConfig: (l) => {
      try {
        return n = { ...n, ...l }, i(), F.success("Enhanced configuration updated successfully"), !0;
      } catch (c) {
        return F.error(`Failed to update enhanced configuration: ${c instanceof Error ? c.message : String(c)}`), !1;
      }
    },
    resetEnhancedConfig: () => {
      try {
        return n = { ...cg }, i(), F.success("Enhanced configuration reset to defaults"), !0;
      } catch (l) {
        return F.error(`Failed to reset enhanced configuration: ${l instanceof Error ? l.message : String(l)}`), !1;
      }
    },
    // Performance validation and management
    validatePerformanceConfig: (l = n.performanceConfig) => {
      try {
        if (!l)
          throw new Error("Performance configuration is required");
        if (l.caching) {
          if (l.caching.max_size < 1 || l.caching.max_size > 1e4)
            throw new Error("Cache max_size must be between 1 and 10000");
          if (l.caching.ttl < 0 || l.caching.ttl > 86400)
            throw new Error("Cache TTL must be between 0 and 86400 seconds");
        }
        if (l.parallel_processing && (l.parallel_processing.max_workers < 1 || l.parallel_processing.max_workers > 100))
          throw new Error("Max workers must be between 1 and 100");
        if (l.memory_management && (l.memory_management.max_memory_mb < 100 || l.memory_management.max_memory_mb > 1e5))
          throw new Error("Max memory must be between 100 and 100000 MB");
        return F.success("Performance configuration is valid"), !0;
      } catch (c) {
        return F.error(`Invalid performance configuration: ${c instanceof Error ? c.message : String(c)}`), !1;
      }
    },
    // Security validation and management
    validateSecurityConfig: (l = n.securityConfig) => {
      try {
        if (!l)
          throw new Error("Security configuration is required");
        if (l.authentication) {
          const c = ["api-key", "oauth2", "jwt", "basic"];
          if (!c.includes(l.authentication.method))
            throw new Error(`Invalid authentication method. Must be one of: ${c.join(", ")}`);
        }
        if (l.data_protection && l.data_protection.encryption) {
          const c = ["aes-256", "rsa-2048", "chacha20"];
          if (!c.includes(l.data_protection.encryption.algorithm))
            throw new Error(`Invalid encryption algorithm. Must be one of: ${c.join(", ")}`);
        }
        if (l.compliance && l.compliance.audit_logging && typeof l.compliance.audit_logging == "object" && (l.compliance.audit_logging.retention_days < 30 || l.compliance.audit_logging.retention_days > 3650))
          throw new Error("Audit log retention must be between 30 and 3650 days");
        return F.success("Security configuration is valid"), !0;
      } catch (c) {
        return F.error(`Invalid security configuration: ${c instanceof Error ? c.message : String(c)}`), !1;
      }
    },
    // Monitoring validation and management
    validateMonitoringConfig: (l = n.monitoringConfig) => {
      try {
        if (!l)
          throw new Error("Monitoring configuration is required");
        if (l.metrics && (l.metrics.collection_interval < 1 || l.metrics.collection_interval > 3600))
          throw new Error("Metrics collection interval must be between 1 and 3600 seconds");
        if (l.logging) {
          const c = ["debug", "info", "warn", "error", "critical"];
          if (!c.includes(l.logging.level))
            throw new Error(`Invalid log level. Must be one of: ${c.join(", ")}`);
        }
        return l.alerting && l.alerting.thresholds && Object.entries(l.alerting.thresholds).forEach(([c, u]) => {
          if (typeof u != "number" || u <= 0)
            throw new Error(`Alert threshold for ${c} must be a positive number`);
        }), F.success("Monitoring configuration is valid"), !0;
      } catch (c) {
        return F.error(`Invalid monitoring configuration: ${c instanceof Error ? c.message : String(c)}`), !1;
      }
    },
    // Integration validation and management
    validateIntegrationConfig: (l = n.integrationConfig) => {
      try {
        if (!l)
          throw new Error("Integration configuration is required");
        if (l.rest_api) {
          if (l.rest_api.timeout < 1e3 || l.rest_api.timeout > 6e4)
            throw new Error("REST API timeout must be between 1000 and 60000 ms");
          if (l.rest_api.max_retries < 0 || l.rest_api.max_retries > 10)
            throw new Error("REST API max retries must be between 0 and 10");
        }
        if (l.graphql && (l.graphql.max_batch_size < 1 || l.graphql.max_batch_size > 100))
          throw new Error("GraphQL max batch size must be between 1 and 100");
        if (l.websocket && (l.websocket.ping_interval < 1e3 || l.websocket.ping_interval > 3e4))
          throw new Error("WebSocket ping interval must be between 1000 and 30000 ms");
        return F.success("Integration configuration is valid"), !0;
      } catch (c) {
        return F.error(`Invalid integration configuration: ${c instanceof Error ? c.message : String(c)}`), !1;
      }
    },
    // Error handling validation and management
    validateErrorHandlingConfig: (l = n.errorHandlingConfig) => {
      var c;
      try {
        if (!l)
          throw new Error("Error handling configuration is required");
        if (l.error_classification && (l.error_classification.max_history < 1 || l.error_classification.max_history > 1e3))
          throw new Error("Error classification max history must be between 1 and 1000");
        if (l.error_recovery && (l.error_recovery.max_attempts < 1 || l.error_recovery.max_attempts > 10))
          throw new Error("Error recovery max attempts must be between 1 and 10");
        if (l.error_reporting) {
          const u = ["console", "file", "api", "email", "database"];
          (c = l.error_reporting.destinations) == null || c.forEach((d) => {
            if (!u.includes(d))
              throw new Error(`Invalid error reporting destination. Must be one of: ${u.join(", ")}`);
          });
        }
        return F.success("Error handling configuration is valid"), !0;
      } catch (u) {
        return F.error(`Invalid error handling configuration: ${u instanceof Error ? u.message : String(u)}`), !1;
      }
    },
    // Enhanced execution methods
    executeEvolutionWithEnhancedFeatures: async (l, c = {}) => {
      var u, d, f, p;
      try {
        const { performanceProfile: m, securityProfile: y, monitoringEnabled: h = !0, integrationMode: g = "auto" } = c, v = m ? (u = n.performanceProfiles) == null ? void 0 : u[m] : n.performanceConfig;
        v && this.validatePerformanceConfig(v);
        const b = y ? (d = n.securityProfiles) == null ? void 0 : d[y] : n.securityConfig;
        b && this.validateSecurityConfig(b);
        let w = {};
        h && n.monitoringConfig && (this.validateMonitoringConfig(n.monitoringConfig), w = this.startMonitoring());
        let j = {};
        g !== "disabled" && n.integrationConfig && (this.validateIntegrationConfig(n.integrationConfig), j = await this.setupIntegrations(g === "auto"));
        const x = ht(), _ = Date.now(), S = await r.executeEvolution(l, {
          ...c,
          executionId: x
        }), k = Date.now(), E = k - _, L = {
          executionId: x,
          executionTime: E,
          startTime: _,
          endTime: k,
          memoryUsage: v != null && v.memory_management ? this.getMemoryUsage() : void 0,
          cacheStats: v != null && v.caching ? this.getCacheStats() : void 0
        }, P = {
          authenticationStatus: b != null && b.authentication ? "enabled" : "disabled",
          encryptionStatus: (p = (f = b == null ? void 0 : b.data_protection) == null ? void 0 : f.encryption) != null && p.enabled ? "enabled" : "disabled",
          complianceStatus: b != null && b.compliance ? "enabled" : "disabled"
        };
        return h && this.stopMonitoring(), g !== "disabled" && await this.cleanupIntegrations(), this.updateExecutionStatistics({
          executionId: x,
          executionTime: E,
          success: !0,
          timestamp: k
        }), {
          success: !0,
          result: S,
          performanceMetrics: L,
          securityStatus: P,
          monitoringData: w,
          integrationResults: j
        };
      } catch (m) {
        const y = ht();
        return n.errorHandlingConfig && this.handleError(m, {
          errorId: y,
          context: "executeEvolutionWithEnhancedFeatures",
          severity: "high"
        }), this.updateErrorStatistics({
          errorId: y,
          errorType: m instanceof Error ? m.name : "UnknownError",
          errorMessage: m instanceof Error ? m.message : String(m),
          timestamp: Date.now()
        }), {
          success: !1,
          error: m instanceof Error ? m : new Error(String(m)),
          performanceMetrics: {
            executionId: y,
            executionTime: 0,
            startTime: Date.now(),
            endTime: Date.now()
          }
        };
      }
    },
    // Enhanced monitoring methods
    startMonitoring: () => {
      try {
        if (!n.monitoringConfig)
          throw new Error("Monitoring configuration is not set");
        const l = ht(), c = Date.now(), u = {
          monitoringId: l,
          startTime: c,
          metrics: [],
          logs: [],
          events: []
        };
        return F.info(`Monitoring started with ID: ${l}`), u;
      } catch (l) {
        return F.error(`Failed to start monitoring: ${l instanceof Error ? l.message : String(l)}`), {};
      }
    },
    stopMonitoring: () => {
      try {
        F.info("Monitoring stopped successfully");
      } catch (l) {
        F.error(`Failed to stop monitoring: ${l instanceof Error ? l.message : String(l)}`);
      }
    },
    getMonitoringData: () => {
      try {
        return {
          metrics: [],
          logs: [],
          events: [],
          alerts: []
        };
      } catch (l) {
        return F.error(`Failed to get monitoring data: ${l instanceof Error ? l.message : String(l)}`), {};
      }
    },
    // Enhanced integration methods
    setupIntegrations: async (l = !0) => {
      var c, u, d;
      try {
        if (!n.integrationConfig)
          throw new Error("Integration configuration is not set");
        const f = ht(), p = {};
        return (c = n.integrationConfig.rest_api) != null && c.enabled && (p.restApi = {
          status: "connected",
          endpoints: n.integrationConfig.rest_api.endpoints || []
        }), (u = n.integrationConfig.graphql) != null && u.enabled && (p.graphql = {
          status: "connected",
          schema: n.integrationConfig.graphql.schema_url || ""
        }), (d = n.integrationConfig.websocket) != null && d.enabled && (p.websocket = {
          status: "connected",
          url: n.integrationConfig.websocket.url || ""
        }), F.info(`Integrations setup completed with ID: ${f}`), p;
      } catch (f) {
        return F.error(`Failed to setup integrations: ${f instanceof Error ? f.message : String(f)}`), {};
      }
    },
    cleanupIntegrations: async () => {
      try {
        F.info("Integrations cleaned up successfully");
      } catch (l) {
        F.error(`Failed to cleanup integrations: ${l instanceof Error ? l.message : String(l)}`);
      }
    },
    // Enhanced error handling methods
    handleError: (l, c = {}) => {
      var u, d, f;
      try {
        const { errorId: p = ht(), context: m = "unknown", severity: y = "medium" } = c, h = l instanceof Error ? l.message : String(l), g = l instanceof Error ? l.name : "UnknownError", v = this.classifyError(g, h);
        this.logError({
          errorId: p,
          errorType: g,
          errorMessage: h,
          context: m,
          severity: y,
          classification: v,
          timestamp: Date.now()
        }), (u = n.errorHandlingConfig) != null && u.error_reporting && this.reportError({
          errorId: p,
          errorType: g,
          errorMessage: h,
          context: m,
          severity: y,
          classification: v
        }), (f = (d = n.errorHandlingConfig) == null ? void 0 : d.error_recovery) != null && f.enabled && this.attemptErrorRecovery(p, g, h, m), F.error(`Error handled: ${h} (ID: ${p})`);
      } catch (p) {
        F.error(`Failed to handle error: ${p instanceof Error ? p.message : String(p)}`);
      }
    },
    classifyError: (l, c) => {
      try {
        return l.includes("Network") || c.includes("network") ? "network_error" : l.includes("Timeout") || c.includes("timeout") ? "timeout_error" : l.includes("Validation") || c.includes("validation") ? "validation_error" : l.includes("Authentication") || c.includes("authentication") ? "authentication_error" : l.includes("Authorization") || c.includes("authorization") ? "authorization_error" : "general_error";
      } catch {
        return "classification_error";
      }
    },
    logError: (l) => {
      try {
        console.error("Error logged:", l), F.info(`Error logged: ${l.errorMessage} (ID: ${l.errorId})`);
      } catch (c) {
        F.error(`Failed to log error: ${c instanceof Error ? c.message : String(c)}`);
      }
    },
    reportError: (l) => {
      var c, u, d;
      try {
        const f = (c = n.errorHandlingConfig) == null ? void 0 : c.error_reporting;
        if (!f)
          throw new Error("Error reporting configuration is not set");
        (u = f.destinations) == null || u.forEach((p) => {
          console.log(`Reporting error to ${p}:`, l);
        }), F.info(`Error reported to ${((d = f.destinations) == null ? void 0 : d.join(", ")) || "default destinations"}`);
      } catch (f) {
        F.error(`Failed to report error: ${f instanceof Error ? f.message : String(f)}`);
      }
    },
    attemptErrorRecovery: (l, c, u, d) => {
      var f;
      try {
        if (!((f = n.errorHandlingConfig) == null ? void 0 : f.error_recovery))
          throw new Error("Error recovery configuration is not set");
        c.includes("Network") || u.includes("network") ? F.info(`Attempting network error recovery for error ID: ${l}`) : c.includes("Timeout") || u.includes("timeout") ? F.info(`Attempting timeout error recovery for error ID: ${l}`) : F.info(`Attempting general error recovery for error ID: ${l}`), F.success(`Error recovery attempted for error ID: ${l}`);
      } catch (p) {
        F.error(`Failed to attempt error recovery: ${p instanceof Error ? p.message : String(p)}`);
      }
    },
    // Enhanced statistics methods
    updateExecutionStatistics: (l) => {
      var c, u, d, f, p, m;
      try {
        n = {
          ...n,
          executionStatistics: {
            ...n.executionStatistics,
            totalExecutions: (((c = n.executionStatistics) == null ? void 0 : c.totalExecutions) || 0) + 1,
            successfulExecutions: l.success ? (((u = n.executionStatistics) == null ? void 0 : u.successfulExecutions) || 0) + 1 : ((d = n.executionStatistics) == null ? void 0 : d.successfulExecutions) || 0,
            failedExecutions: l.success ? ((p = n.executionStatistics) == null ? void 0 : p.failedExecutions) || 0 : (((f = n.executionStatistics) == null ? void 0 : f.failedExecutions) || 0) + 1,
            totalExecutionTime: (((m = n.executionStatistics) == null ? void 0 : m.totalExecutionTime) || 0) + l.executionTime,
            averageExecutionTime: void 0,
            // Will be calculated when needed
            lastExecution: {
              executionId: l.executionId,
              executionTime: l.executionTime,
              success: l.success,
              timestamp: l.timestamp
            }
          }
        }, i();
      } catch (y) {
        F.error(`Failed to update execution statistics: ${y instanceof Error ? y.message : String(y)}`);
      }
    },
    updateErrorStatistics: (l) => {
      var c, u, d, f;
      try {
        n = {
          ...n,
          errorStatistics: {
            ...n.errorStatistics,
            totalErrors: (((c = n.errorStatistics) == null ? void 0 : c.totalErrors) || 0) + 1,
            errorsByType: {
              ...(u = n.errorStatistics) == null ? void 0 : u.errorsByType,
              [l.errorType]: (((f = (d = n.errorStatistics) == null ? void 0 : d.errorsByType) == null ? void 0 : f[l.errorType]) || 0) + 1
            },
            lastError: {
              errorId: l.errorId,
              errorType: l.errorType,
              errorMessage: l.errorMessage,
              timestamp: l.timestamp
            }
          }
        }, i();
      } catch (p) {
        F.error(`Failed to update error statistics: ${p instanceof Error ? p.message : String(p)}`);
      }
    },
    getPerformanceMetrics: () => {
      try {
        return {
          memoryUsage: this.getMemoryUsage(),
          cacheStats: this.getCacheStats(),
          cpuUsage: Math.random() * 100,
          networkLatency: Math.random() * 1e3
        };
      } catch (l) {
        return F.error(`Failed to get performance metrics: ${l instanceof Error ? l.message : String(l)}`), {};
      }
    },
    getMemoryUsage: () => {
      try {
        return {
          used: Math.random() * 1e3,
          total: 1e3,
          free: 1e3 - Math.random() * 1e3,
          percentage: Math.random() * 100
        };
      } catch (l) {
        return F.error(`Failed to get memory usage: ${l instanceof Error ? l.message : String(l)}`), {};
      }
    },
    getCacheStats: () => {
      try {
        return {
          hits: Math.floor(Math.random() * 1e3),
          misses: Math.floor(Math.random() * 100),
          size: Math.floor(Math.random() * 1e4),
          hitRate: Math.random()
        };
      } catch (l) {
        return F.error(`Failed to get cache stats: ${l instanceof Error ? l.message : String(l)}`), {};
      }
    },
    getSecurityStatus: () => {
      var l, c, u, d, f, p;
      try {
        return {
          authentication: (l = n.securityConfig) != null && l.authentication ? "enabled" : "disabled",
          encryption: (d = (u = (c = n.securityConfig) == null ? void 0 : c.data_protection) == null ? void 0 : u.encryption) != null && d.enabled ? "enabled" : "disabled",
          compliance: (f = n.securityConfig) != null && f.compliance ? "enabled" : "disabled",
          accessControl: (p = n.securityConfig) != null && p.access_control ? "enabled" : "disabled"
        };
      } catch (m) {
        return F.error(`Failed to get security status: ${m instanceof Error ? m.message : String(m)}`), {};
      }
    },
    getIntegrationStatus: () => {
      var l, c, u, d, f, p, m, y, h, g;
      try {
        return {
          restApi: (c = (l = n.integrationConfig) == null ? void 0 : l.rest_api) != null && c.enabled ? "connected" : "disconnected",
          graphql: (d = (u = n.integrationConfig) == null ? void 0 : u.graphql) != null && d.enabled ? "connected" : "disconnected",
          websocket: (p = (f = n.integrationConfig) == null ? void 0 : f.websocket) != null && p.enabled ? "connected" : "disconnected",
          webhooks: (y = (m = n.integrationConfig) == null ? void 0 : m.webhooks) != null && y.enabled ? "connected" : "disconnected",
          eventStreaming: (g = (h = n.integrationConfig) == null ? void 0 : h.event_streaming) != null && g.enabled ? "connected" : "disconnected"
        };
      } catch (v) {
        return F.error(`Failed to get integration status: ${v instanceof Error ? v.message : String(v)}`), {};
      }
    },
    // Profile management methods
    addPerformanceProfile: (l, c) => {
      var u;
      try {
        if (!l || !c)
          throw new Error("Profile name and configuration are required");
        if ((u = n.performanceProfiles) != null && u[l])
          throw new Error(`Performance profile "${l}" already exists`);
        return n = {
          ...n,
          performanceProfiles: {
            ...n.performanceProfiles,
            [l]: c
          }
        }, i(), F.success(`Performance profile "${l}" added successfully`), !0;
      } catch (d) {
        return F.error(`Failed to add performance profile: ${d instanceof Error ? d.message : String(d)}`), !1;
      }
    },
    addSecurityProfile: (l, c) => {
      var u;
      try {
        if (!l || !c)
          throw new Error("Profile name and configuration are required");
        if ((u = n.securityProfiles) != null && u[l])
          throw new Error(`Security profile "${l}" already exists`);
        return n = {
          ...n,
          securityProfiles: {
            ...n.securityProfiles,
            [l]: c
          }
        }, i(), F.success(`Security profile "${l}" added successfully`), !0;
      } catch (d) {
        return F.error(`Failed to add security profile: ${d instanceof Error ? d.message : String(d)}`), !1;
      }
    },
    removePerformanceProfile: (l) => {
      var c;
      try {
        if (!l)
          throw new Error("Profile name is required");
        if (!((c = n.performanceProfiles) != null && c[l]))
          throw new Error(`Performance profile "${l}" does not exist`);
        const u = { ...n.performanceProfiles };
        return delete u[l], n = {
          ...n,
          performanceProfiles: u
        }, i(), F.success(`Performance profile "${l}" removed successfully`), !0;
      } catch (u) {
        return F.error(`Failed to remove performance profile: ${u instanceof Error ? u.message : String(u)}`), !1;
      }
    },
    removeSecurityProfile: (l) => {
      var c;
      try {
        if (!l)
          throw new Error("Profile name is required");
        if (!((c = n.securityProfiles) != null && c[l]))
          throw new Error(`Security profile "${l}" does not exist`);
        const u = { ...n.securityProfiles };
        return delete u[l], n = {
          ...n,
          securityProfiles: u
        }, i(), F.success(`Security profile "${l}" removed successfully`), !0;
      } catch (u) {
        return F.error(`Failed to remove security profile: ${u instanceof Error ? u.message : String(u)}`), !1;
      }
    },
    // Validation history methods
    addValidationResult: (l) => {
      try {
        if (!l)
          throw new Error("Validation result is required");
        return n = {
          ...n,
          validationHistory: [
            ...n.validationHistory || [],
            l
          ].slice(-100)
          // Keep only last 100 entries
        }, i(), !0;
      } catch (c) {
        return F.error(`Failed to add validation result: ${c instanceof Error ? c.message : String(c)}`), !1;
      }
    },
    clearValidationHistory: () => {
      try {
        return n = {
          ...n,
          validationHistory: []
        }, i(), F.success("Validation history cleared successfully"), !0;
      } catch (l) {
        return F.error(`Failed to clear validation history: ${l instanceof Error ? l.message : String(l)}`), !1;
      }
    }
  };
}
let ds = null;
function Sm(e = {}) {
  return ds || (ds = X_(e)), ds;
}
function yz() {
  ds = null;
}
function Ie(...e) {
  return e.filter(Boolean).join(" ");
}
function he({
  title: e,
  description: t,
  actions: r,
  children: n,
  className: a
}) {
  return /* @__PURE__ */ s.jsxs("section", { className: Ie("rounded-xl border border-slate-200 bg-white shadow-sm", a), children: [
    (e || t || r) && /* @__PURE__ */ s.jsxs("header", { className: "flex items-start justify-between gap-4 border-b border-slate-100 px-5 py-4", children: [
      /* @__PURE__ */ s.jsxs("div", { children: [
        e && /* @__PURE__ */ s.jsx("h3", { className: "text-sm font-semibold text-slate-900", children: e }),
        t && /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-slate-500", children: t })
      ] }),
      r && /* @__PURE__ */ s.jsx("div", { className: "shrink-0", children: r })
    ] }),
    /* @__PURE__ */ s.jsx("div", { className: "px-5 py-4", children: n })
  ] });
}
function H({
  label: e,
  hint: t,
  children: r,
  className: n
}) {
  return /* @__PURE__ */ s.jsxs("label", { className: Ie("block space-y-2 text-sm text-slate-600", n), children: [
    /* @__PURE__ */ s.jsx("span", { className: "text-xs font-semibold uppercase tracking-wide text-slate-500", children: e }),
    r,
    t && /* @__PURE__ */ s.jsx("span", { className: "block text-xs text-slate-400", children: t })
  ] });
}
function D({
  className: e,
  ...t
}) {
  return /* @__PURE__ */ s.jsx(
    "input",
    {
      className: Ie(
        "w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm text-slate-900",
        "transition focus:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-200",
        e
      ),
      ...t
    }
  );
}
function Lt({
  className: e,
  ...t
}) {
  return /* @__PURE__ */ s.jsx(
    "textarea",
    {
      className: Ie(
        "w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm text-slate-900",
        "transition focus:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-200",
        e
      ),
      ...t
    }
  );
}
function je({
  className: e,
  children: t,
  ...r
}) {
  return /* @__PURE__ */ s.jsx(
    "select",
    {
      className: Ie(
        "w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm text-slate-900",
        "transition focus:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-200",
        e
      ),
      ...r,
      children: t
    }
  );
}
function X({
  className: e,
  variant: t = "primary",
  ...r
}) {
  const n = {
    primary: "bg-blue-600 text-white hover:bg-blue-700",
    secondary: "bg-slate-100 text-slate-700 hover:bg-slate-200",
    ghost: "text-slate-600 hover:bg-slate-100"
  }[t];
  return /* @__PURE__ */ s.jsx(
    "button",
    {
      className: Ie(
        "rounded-lg px-4 py-2 text-sm font-medium transition",
        "focus:outline-none focus:ring-2 focus:ring-blue-200",
        n,
        e
      ),
      ...r
    }
  );
}
function Re({
  children: e,
  tone: t = "neutral",
  className: r
}) {
  const n = {
    neutral: "bg-slate-100 text-slate-700",
    success: "bg-emerald-100 text-emerald-700",
    warning: "bg-amber-100 text-amber-700",
    danger: "bg-rose-100 text-rose-700",
    info: "bg-blue-100 text-blue-700"
  }[t];
  return /* @__PURE__ */ s.jsx("span", { className: Ie("inline-flex items-center rounded-full px-2.5 py-1 text-xs", n, r), children: e });
}
function Ze({
  checked: e,
  onChange: t,
  label: r,
  className: n
}) {
  return /* @__PURE__ */ s.jsxs(
    "button",
    {
      type: "button",
      role: "switch",
      "aria-checked": e,
      onClick: () => t(!e),
      className: Ie(
        "inline-flex items-center gap-2 rounded-full border px-2 py-1 text-xs font-medium",
        e ? "border-blue-500 bg-blue-50 text-blue-700" : "border-slate-200 bg-white text-slate-500",
        n
      ),
      children: [
        /* @__PURE__ */ s.jsx(
          "span",
          {
            className: Ie(
              "h-3 w-3 rounded-full transition",
              e ? "bg-blue-600" : "bg-slate-300"
            )
          }
        ),
        r && /* @__PURE__ */ s.jsx("span", { children: r })
      ]
    }
  );
}
function ct({
  label: e,
  className: t,
  ...r
}) {
  return /* @__PURE__ */ s.jsxs("label", { className: Ie("inline-flex items-center gap-2 text-sm text-slate-600", t), children: [
    /* @__PURE__ */ s.jsx(
      "input",
      {
        type: "checkbox",
        className: "h-4 w-4 rounded border-slate-300 text-blue-600 focus:ring-blue-200",
        ...r
      }
    ),
    e && /* @__PURE__ */ s.jsx("span", { children: e })
  ] });
}
const Q_ = ({ config: e, onConfigUpdate: t, onValidate: r }) => {
  var l, c, u, d, f, p, m, y, h, g, v, b, w, j, x, _, S, k, E, L, P, A, T, M, N, q, V, B, ee, U, G, Y, Q, ne, z, se, oe, ie, K, ae, fe, $, Ee, re, Pe, Oe, Ne;
  const n = (ke) => {
    const { name: Me, value: Ae, type: C } = ke.target, R = C === "checkbox" ? ke.target.checked : void 0;
    t({
      performanceConfig: {
        ...e.performanceConfig,
        [Me]: C === "checkbox" ? R : Ae
      }
    });
  }, a = (ke) => {
    var W;
    const { name: Me, value: Ae, type: C } = ke.target, R = C === "checkbox" ? ke.target.checked : void 0;
    t({
      performanceConfig: {
        ...e.performanceConfig,
        caching: {
          ...(W = e.performanceConfig) == null ? void 0 : W.caching,
          [Me]: C === "checkbox" ? R : Ae
        }
      }
    });
  }, i = (ke) => {
    var W;
    const { name: Me, value: Ae, type: C } = ke.target, R = C === "checkbox" ? ke.target.checked : void 0;
    t({
      performanceConfig: {
        ...e.performanceConfig,
        parallel_processing: {
          ...(W = e.performanceConfig) == null ? void 0 : W.parallel_processing,
          [Me]: C === "checkbox" ? R : Ae
        }
      }
    });
  }, o = (ke) => {
    var W;
    const { name: Me, value: Ae, type: C } = ke.target, R = C === "checkbox" ? ke.target.checked : void 0;
    t({
      performanceConfig: {
        ...e.performanceConfig,
        memory_management: {
          ...(W = e.performanceConfig) == null ? void 0 : W.memory_management,
          [Me]: C === "checkbox" ? R : Ae
        }
      }
    });
  };
  return /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Performance Configuration",
        description: "Control optimization and runtime efficiency features.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (l = e.performanceConfig) != null && l.enabled ? "success" : "neutral", children: (c = e.performanceConfig) != null && c.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsx(
          ct,
          {
            name: "enabled",
            checked: ((u = e.performanceConfig) == null ? void 0 : u.enabled) || !1,
            onChange: n,
            label: "Enable performance optimization"
          }
        )
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Caching Configuration",
        description: "Configure cache strategy and limits.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (f = (d = e.performanceConfig) == null ? void 0 : d.caching) != null && f.enabled ? "success" : "neutral", children: (m = (p = e.performanceConfig) == null ? void 0 : p.caching) != null && m.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((h = (y = e.performanceConfig) == null ? void 0 : y.caching) == null ? void 0 : h.enabled) || !1,
              onChange: a,
              label: "Enable caching"
            }
          ),
          ((v = (g = e.performanceConfig) == null ? void 0 : g.caching) == null ? void 0 : v.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-2", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Cache Strategy", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                name: "strategy",
                value: ((w = (b = e.performanceConfig) == null ? void 0 : b.caching) == null ? void 0 : w.strategy) || "lru",
                onChange: a,
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "lru", children: "LRU (Least Recently Used)" }),
                  /* @__PURE__ */ s.jsx("option", { value: "lfu", children: "LFU (Least Frequently Used)" }),
                  /* @__PURE__ */ s.jsx("option", { value: "fifo", children: "FIFO (First In First Out)" }),
                  /* @__PURE__ */ s.jsx("option", { value: "random", children: "Random" })
                ]
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Max Cache Size", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "max_size",
                min: "1",
                max: "10000",
                value: ((x = (j = e.performanceConfig) == null ? void 0 : j.caching) == null ? void 0 : x.max_size) || 1e3,
                onChange: a
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Cache TTL (seconds)", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "ttl",
                min: "0",
                max: "86400",
                value: ((S = (_ = e.performanceConfig) == null ? void 0 : _.caching) == null ? void 0 : S.ttl) || 3600,
                onChange: a
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Compression Algorithm", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                name: "compression",
                value: ((E = (k = e.performanceConfig) == null ? void 0 : k.caching) == null ? void 0 : E.compression) || "gzip",
                onChange: a,
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "gzip", children: "GZIP" }),
                  /* @__PURE__ */ s.jsx("option", { value: "brotli", children: "Brotli" }),
                  /* @__PURE__ */ s.jsx("option", { value: "none", children: "None" })
                ]
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Parallel Processing Configuration",
        description: "Tune concurrency and batching.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (P = (L = e.performanceConfig) == null ? void 0 : L.parallel_processing) != null && P.enabled ? "success" : "neutral", children: (T = (A = e.performanceConfig) == null ? void 0 : A.parallel_processing) != null && T.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((N = (M = e.performanceConfig) == null ? void 0 : M.parallel_processing) == null ? void 0 : N.enabled) || !1,
              onChange: i,
              label: "Enable parallel processing"
            }
          ),
          ((V = (q = e.performanceConfig) == null ? void 0 : q.parallel_processing) == null ? void 0 : V.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-3", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Max Workers", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "max_workers",
                min: "1",
                max: "100",
                value: ((ee = (B = e.performanceConfig) == null ? void 0 : B.parallel_processing) == null ? void 0 : ee.max_workers) || 4,
                onChange: i
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Worker Type", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                name: "worker_type",
                value: ((G = (U = e.performanceConfig) == null ? void 0 : U.parallel_processing) == null ? void 0 : G.worker_type) || "thread",
                onChange: i,
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "thread", children: "Thread" }),
                  /* @__PURE__ */ s.jsx("option", { value: "process", children: "Process" }),
                  /* @__PURE__ */ s.jsx("option", { value: "cluster", children: "Cluster" })
                ]
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Batch Size", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "batch_size",
                min: "1",
                max: "1000",
                value: ((Q = (Y = e.performanceConfig) == null ? void 0 : Y.parallel_processing) == null ? void 0 : Q.batch_size) || 10,
                onChange: i
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Memory Management Configuration",
        description: "Define limits and garbage collection cadence.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (z = (ne = e.performanceConfig) == null ? void 0 : ne.memory_management) != null && z.enabled ? "success" : "neutral", children: (oe = (se = e.performanceConfig) == null ? void 0 : se.memory_management) != null && oe.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((K = (ie = e.performanceConfig) == null ? void 0 : ie.memory_management) == null ? void 0 : K.enabled) || !1,
              onChange: o,
              label: "Enable memory management"
            }
          ),
          ((fe = (ae = e.performanceConfig) == null ? void 0 : ae.memory_management) == null ? void 0 : fe.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-3", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Max Memory (MB)", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "max_memory_mb",
                min: "100",
                max: "100000",
                value: ((Ee = ($ = e.performanceConfig) == null ? void 0 : $.memory_management) == null ? void 0 : Ee.max_memory_mb) || 4096,
                onChange: o
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Memory Threshold (%)", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "memory_threshold_percent",
                min: "50",
                max: "95",
                value: ((Pe = (re = e.performanceConfig) == null ? void 0 : re.memory_management) == null ? void 0 : Pe.memory_threshold_percent) || 80,
                onChange: o
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "GC Interval (ms)", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "gc_interval_ms",
                min: "1000",
                max: "3600000",
                value: ((Ne = (Oe = e.performanceConfig) == null ? void 0 : Oe.memory_management) == null ? void 0 : Ne.gc_interval_ms) || 6e4,
                onChange: o
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap gap-2", children: [
      /* @__PURE__ */ s.jsx(X, { onClick: r, variant: "secondary", children: "Validate performance config" }),
      /* @__PURE__ */ s.jsx(X, { onClick: () => t({ performanceConfig: { ...e.performanceConfig } }), children: "Save performance config" })
    ] })
  ] });
}, J_ = ({ config: e, onConfigUpdate: t, onValidate: r }) => {
  var u, d, f, p, m, y, h, g, v, b, w, j, x, _, S, k, E, L, P, A, T, M, N, q, V, B, ee, U, G, Y, Q, ne, z, se, oe, ie, K, ae, fe, $, Ee, re, Pe, Oe, Ne, ke, Me, Ae, C, R, W, ue, le, ce, _e, Be, Ue, pt, or, Qt, ea, sn, ln, ta, An, cn, un, Fr, ra, na, li, Lo, ci, Fo, ui, di, qo, Uo, fi, mi;
  const n = (jt) => {
    const { name: Jt, value: Zt, type: St } = jt.target, O = St === "checkbox" ? jt.target.checked : void 0;
    t({ securityConfig: { ...e.securityConfig, [Jt]: St === "checkbox" ? O : Zt } });
  }, a = (jt) => {
    var te;
    const { name: Jt, value: Zt, type: St } = jt.target, O = St === "checkbox" ? jt.target.checked : void 0;
    t({ securityConfig: { ...e.securityConfig, authentication: { ...(te = e.securityConfig) == null ? void 0 : te.authentication, [Jt]: St === "checkbox" ? O : Zt } } });
  }, i = (jt) => {
    var te;
    const { name: Jt, value: Zt, type: St } = jt.target, O = St === "checkbox" ? jt.target.checked : void 0;
    t({ securityConfig: { ...e.securityConfig, data_protection: { ...(te = e.securityConfig) == null ? void 0 : te.data_protection, [Jt]: St === "checkbox" ? O : Zt } } });
  }, o = (jt) => {
    var te, pe, Ce;
    const { name: Jt, value: Zt, type: St } = jt.target, O = St === "checkbox" ? jt.target.checked : void 0;
    t({ securityConfig: { ...e.securityConfig, data_protection: { ...(te = e.securityConfig) == null ? void 0 : te.data_protection, encryption: { ...(Ce = (pe = e.securityConfig) == null ? void 0 : pe.data_protection) == null ? void 0 : Ce.encryption, [Jt]: St === "checkbox" ? O : Zt } } } });
  }, l = (jt) => {
    var te;
    const { name: Jt, value: Zt, type: St } = jt.target, O = St === "checkbox" ? jt.target.checked : void 0;
    t({ securityConfig: { ...e.securityConfig, compliance: { ...(te = e.securityConfig) == null ? void 0 : te.compliance, [Jt]: St === "checkbox" ? O : Zt } } });
  }, c = (jt) => {
    var te, pe, Ce;
    const { name: Jt, value: Zt, type: St } = jt.target, O = St === "checkbox" ? jt.target.checked : void 0;
    t({ securityConfig: { ...e.securityConfig, compliance: { ...(te = e.securityConfig) == null ? void 0 : te.compliance, audit_logging: { ...(Ce = (pe = e.securityConfig) == null ? void 0 : pe.compliance) == null ? void 0 : Ce.audit_logging, [Jt]: St === "checkbox" ? O : Zt } } } });
  };
  return /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Security Configuration",
        description: "Enable and manage core security features.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (u = e.securityConfig) != null && u.enabled ? "success" : "neutral", children: (d = e.securityConfig) != null && d.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsx(
          ct,
          {
            name: "enabled",
            checked: ((f = e.securityConfig) == null ? void 0 : f.enabled) || !1,
            onChange: n,
            label: "Enable security features"
          }
        )
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Authentication Configuration",
        description: "Select authentication methods and limits.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (m = (p = e.securityConfig) == null ? void 0 : p.authentication) != null && m.enabled ? "success" : "neutral", children: (h = (y = e.securityConfig) == null ? void 0 : y.authentication) != null && h.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((v = (g = e.securityConfig) == null ? void 0 : g.authentication) == null ? void 0 : v.enabled) || !1,
              onChange: a,
              label: "Enable authentication"
            }
          ),
          ((w = (b = e.securityConfig) == null ? void 0 : b.authentication) == null ? void 0 : w.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-3", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Authentication Method", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                name: "method",
                value: ((x = (j = e.securityConfig) == null ? void 0 : j.authentication) == null ? void 0 : x.method) || "api-key",
                onChange: a,
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "api-key", children: "API Key" }),
                  /* @__PURE__ */ s.jsx("option", { value: "oauth2", children: "OAuth 2.0" }),
                  /* @__PURE__ */ s.jsx("option", { value: "jwt", children: "JWT" }),
                  /* @__PURE__ */ s.jsx("option", { value: "basic", children: "Basic Auth" })
                ]
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Session Timeout (minutes)", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "session_timeout_minutes",
                min: "5",
                max: "1440",
                value: ((k = (S = e.securityConfig) == null ? void 0 : S.authentication((_ = e.securityConfig) == null ? void 0 : _.authentication)) == null ? void 0 : k.session_timeout_minutes) || 30,
                onChange: a
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Max Failed Attempts", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "max_failed_attempts",
                min: "1",
                max: "10",
                value: ((P = (L = e.securityConfig) == null ? void 0 : L.authentication((E = e.securityConfig) == null ? void 0 : E.authentication)) == null ? void 0 : P.max_failed_attempts) || 5,
                onChange: a
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Data Protection Configuration",
        description: "Manage encryption and retention policies.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (T = (A = e.securityConfig) == null ? void 0 : A.data_protection) != null && T.enabled ? "success" : "neutral", children: (N = (M = e.securityConfig) == null ? void 0 : M.data_protection) != null && N.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((V = (q = e.securityConfig) == null ? void 0 : q.data_protection) == null ? void 0 : V.enabled) || !1,
              onChange: i,
              label: "Enable data protection"
            }
          ),
          ((ee = (B = e.securityConfig) == null ? void 0 : B.data_protection) == null ? void 0 : ee.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
            /* @__PURE__ */ s.jsx(
              he,
              {
                title: "Encryption Configuration",
                description: "Select encryption settings for data at rest and in transit.",
                actions: /* @__PURE__ */ s.jsx(Re, { tone: (Y = (G = (U = e.securityConfig) == null ? void 0 : U.data_protection) == null ? void 0 : G.encryption) != null && Y.enabled ? "success" : "neutral", children: (z = (ne = (Q = e.securityConfig) == null ? void 0 : Q.data_protection) == null ? void 0 : ne.encryption) != null && z.enabled ? "Enabled" : "Disabled" }),
                children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
                  /* @__PURE__ */ s.jsx(
                    ct,
                    {
                      name: "enabled",
                      checked: ((ie = (oe = (se = e.securityConfig) == null ? void 0 : se.data_protection) == null ? void 0 : oe.encryption) == null ? void 0 : ie.enabled) || !1,
                      onChange: o,
                      label: "Enable encryption"
                    }
                  ),
                  ((fe = (ae = (K = e.securityConfig) == null ? void 0 : K.data_protection) == null ? void 0 : ae.encryption) == null ? void 0 : fe.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-3", children: [
                    /* @__PURE__ */ s.jsx(H, { label: "Encryption Algorithm", children: /* @__PURE__ */ s.jsxs(
                      je,
                      {
                        name: "algorithm",
                        value: ((re = (Ee = ($ = e.securityConfig) == null ? void 0 : $.data_protection) == null ? void 0 : Ee.encryption) == null ? void 0 : re.algorithm) || "aes-256",
                        onChange: o,
                        children: [
                          /* @__PURE__ */ s.jsx("option", { value: "aes-256", children: "AES-256" }),
                          /* @__PURE__ */ s.jsx("option", { value: "rsa-2048", children: "RSA-2048" }),
                          /* @__PURE__ */ s.jsx("option", { value: "chacha20", children: "ChaCha20" })
                        ]
                      }
                    ) }),
                    /* @__PURE__ */ s.jsx(
                      ct,
                      {
                        name: "at_rest",
                        checked: ((Ne = (Oe = (Pe = e.securityConfig) == null ? void 0 : Pe.data_protection) == null ? void 0 : Oe.encryption) == null ? void 0 : Ne.at_rest) || !1,
                        onChange: o,
                        label: "Encrypt data at rest"
                      }
                    ),
                    /* @__PURE__ */ s.jsx(
                      ct,
                      {
                        name: "in_transit",
                        checked: ((Ae = (Me = (ke = e.securityConfig) == null ? void 0 : ke.data_protection) == null ? void 0 : Me.encryption) == null ? void 0 : Ae.in_transit) || !1,
                        onChange: o,
                        label: "Encrypt data in transit"
                      }
                    )
                  ] })
                ] })
              }
            ),
            /* @__PURE__ */ s.jsx("div", { className: "grid gap-4 md:grid-cols-2", children: /* @__PURE__ */ s.jsx(H, { label: "Data Retention Days", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "data_retention_days",
                min: "30",
                max: "3650",
                value: ((W = (R = e.securityConfig) == null ? void 0 : R.data_protection((C = e.securityConfig) == null ? void 0 : C.encryption)) == null ? void 0 : W.data_retention_days) || 365,
                onChange: i
              }
            ) }) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Compliance Configuration",
        description: "Audit logging and compliance standards.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (le = (ue = e.securityConfig) == null ? void 0 : ue.compliance) != null && le.enabled ? "success" : "neutral", children: (_e = (ce = e.securityConfig) == null ? void 0 : ce.compliance) != null && _e.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((Ue = (Be = e.securityConfig) == null ? void 0 : Be.compliance) == null ? void 0 : Ue.enabled) || !1,
              onChange: l,
              label: "Enable compliance features"
            }
          ),
          ((or = (pt = e.securityConfig) == null ? void 0 : pt.compliance) == null ? void 0 : or.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
            /* @__PURE__ */ s.jsx(
              he,
              {
                title: "Audit Logging Configuration",
                description: "Retention and logging levels.",
                actions: /* @__PURE__ */ s.jsx(Re, { tone: (sn = (ea = (Qt = e.securityConfig) == null ? void 0 : Qt.compliance) == null ? void 0 : ea.audit_logging) != null && sn.enabled ? "success" : "neutral", children: (An = (ta = (ln = e.securityConfig) == null ? void 0 : ln.compliance) == null ? void 0 : ta.audit_logging) != null && An.enabled ? "Enabled" : "Disabled" }),
                children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
                  /* @__PURE__ */ s.jsx(
                    ct,
                    {
                      name: "enabled",
                      checked: ((Fr = (un = (cn = e.securityConfig) == null ? void 0 : cn.compliance) == null ? void 0 : un.audit_logging) == null ? void 0 : Fr.enabled) || !1,
                      onChange: c,
                      label: "Enable audit logging"
                    }
                  ),
                  ((li = (na = (ra = e.securityConfig) == null ? void 0 : ra.compliance) == null ? void 0 : na.audit_logging) == null ? void 0 : li.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-2", children: [
                    /* @__PURE__ */ s.jsx(H, { label: "Audit Log Retention Days", children: /* @__PURE__ */ s.jsx(
                      D,
                      {
                        type: "number",
                        name: "retention_days",
                        min: "30",
                        max: "3650",
                        value: ((Fo = (ci = (Lo = e.securityConfig) == null ? void 0 : Lo.compliance) == null ? void 0 : ci.audit_logging) == null ? void 0 : Fo.retention_days) || 365,
                        onChange: c
                      }
                    ) }),
                    /* @__PURE__ */ s.jsx(H, { label: "Audit Log Level", children: /* @__PURE__ */ s.jsxs(
                      je,
                      {
                        name: "log_level",
                        value: ((qo = (di = (ui = e.securityConfig) == null ? void 0 : ui.compliance) == null ? void 0 : di.audit_logging) == null ? void 0 : qo.log_level) || "info",
                        onChange: c,
                        children: [
                          /* @__PURE__ */ s.jsx("option", { value: "debug", children: "Debug" }),
                          /* @__PURE__ */ s.jsx("option", { value: "info", children: "Info" }),
                          /* @__PURE__ */ s.jsx("option", { value: "warn", children: "Warn" }),
                          /* @__PURE__ */ s.jsx("option", { value: "error", children: "Error" })
                        ]
                      }
                    ) })
                  ] })
                ] })
              }
            ),
            /* @__PURE__ */ s.jsx(H, { label: "Compliance Standards", children: /* @__PURE__ */ s.jsx(
              Lt,
              {
                name: "standards",
                value: ((mi = (fi = e.securityConfig) == null ? void 0 : fi.compliance((Uo = e.securityConfig) == null ? void 0 : Uo.compliance)) == null ? void 0 : mi.standards) || "",
                onChange: l,
                placeholder: "GDPR, HIPAA, SOC2, etc."
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap gap-2", children: [
      /* @__PURE__ */ s.jsx(X, { onClick: r, variant: "secondary", children: "Validate security config" }),
      /* @__PURE__ */ s.jsx(X, { onClick: () => t({ securityConfig: { ...e.securityConfig } }), children: "Save security config" })
    ] })
  ] });
}, Z_ = ({ config: e, onConfigUpdate: t, onValidate: r }) => {
  var l, c, u, d, f, p, m, y, h, g, v, b, w, j, x, _, S, k, E, L, P, A, T, M, N, q, V, B, ee, U, G, Y, Q, ne, z, se, oe, ie, K, ae, fe, $, Ee, re, Pe, Oe;
  const n = (Ne) => {
    const { name: ke, value: Me, type: Ae } = Ne.target, C = Ae === "checkbox" ? Ne.target.checked : void 0;
    t({ monitoringConfig: { ...e.monitoringConfig, [ke]: Ae === "checkbox" ? C : Me } });
  }, a = (Ne) => {
    var R;
    const { name: ke, value: Me, type: Ae } = Ne.target, C = Ae === "checkbox" ? Ne.target.checked : void 0;
    t({ monitoringConfig: { ...e.monitoringConfig, metrics: { ...(R = e.monitoringConfig) == null ? void 0 : R.metrics, [ke]: Ae === "checkbox" ? C : Me } } });
  }, i = (Ne) => {
    var R;
    const { name: ke, value: Me, type: Ae } = Ne.target, C = Ae === "checkbox" ? Ne.target.checked : void 0;
    t({ monitoringConfig: { ...e.monitoringConfig, logging: { ...(R = e.monitoringConfig) == null ? void 0 : R.logging, [ke]: Ae === "checkbox" ? C : Me } } });
  }, o = (Ne) => {
    var R;
    const { name: ke, value: Me, type: Ae } = Ne.target, C = Ae === "checkbox" ? Ne.target.checked : void 0;
    t({ monitoringConfig: { ...e.monitoringConfig, alerting: { ...(R = e.monitoringConfig) == null ? void 0 : R.alerting, [ke]: Ae === "checkbox" ? C : Me } } });
  };
  return /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Monitoring Configuration",
        description: "Enable monitoring and observability.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (l = e.monitoringConfig) != null && l.enabled ? "success" : "neutral", children: (c = e.monitoringConfig) != null && c.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsx(
          ct,
          {
            name: "enabled",
            checked: ((u = e.monitoringConfig) == null ? void 0 : u.enabled) || !1,
            onChange: n,
            label: "Enable monitoring"
          }
        )
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Metrics Configuration",
        description: "Configure metrics collection cadence.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (f = (d = e.monitoringConfig) == null ? void 0 : d.metrics) != null && f.enabled ? "success" : "neutral", children: (m = (p = e.monitoringConfig) == null ? void 0 : p.metrics) != null && m.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((h = (y = e.monitoringConfig) == null ? void 0 : y.metrics) == null ? void 0 : h.enabled) || !1,
              onChange: a,
              label: "Enable metrics collection"
            }
          ),
          ((v = (g = e.monitoringConfig) == null ? void 0 : g.metrics) == null ? void 0 : v.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-3", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Collection Interval (seconds)", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "collection_interval",
                min: "1",
                max: "3600",
                value: ((w = (b = e.monitoringConfig) == null ? void 0 : b.metrics) == null ? void 0 : w.collection_interval) || 60,
                onChange: a
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Metrics to Collect", children: /* @__PURE__ */ s.jsx(
              Lt,
              {
                name: "metrics_to_collect",
                value: ((x = (j = e.monitoringConfig) == null ? void 0 : j.metrics) == null ? void 0 : x.metrics_to_collect) || "",
                onChange: a,
                placeholder: "cpu, memory, network, disk, etc."
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Retention Days", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "retention_days",
                min: "1",
                max: "365",
                value: ((S = (_ = e.monitoringConfig) == null ? void 0 : _.metrics) == null ? void 0 : S.retention_days) || 30,
                onChange: a
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Logging Configuration",
        description: "Select logging level and format.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (E = (k = e.monitoringConfig) == null ? void 0 : k.logging) != null && E.enabled ? "success" : "neutral", children: (P = (L = e.monitoringConfig) == null ? void 0 : L.logging) != null && P.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((T = (A = e.monitoringConfig) == null ? void 0 : A.logging) == null ? void 0 : T.enabled) || !1,
              onChange: i,
              label: "Enable logging"
            }
          ),
          ((N = (M = e.monitoringConfig) == null ? void 0 : M.logging) == null ? void 0 : N.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-3", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Log Level", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                name: "level",
                value: ((V = (q = e.monitoringConfig) == null ? void 0 : q.logging) == null ? void 0 : V.level) || "info",
                onChange: i,
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "debug", children: "Debug" }),
                  /* @__PURE__ */ s.jsx("option", { value: "info", children: "Info" }),
                  /* @__PURE__ */ s.jsx("option", { value: "warn", children: "Warn" }),
                  /* @__PURE__ */ s.jsx("option", { value: "error", children: "Error" }),
                  /* @__PURE__ */ s.jsx("option", { value: "critical", children: "Critical" })
                ]
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Log Format", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                name: "format",
                value: ((U = (ee = e.monitoringConfig) == null ? void 0 : ee.logging((B = e.monitoringConfig) == null ? void 0 : B.logging)) == null ? void 0 : U.format) || "json",
                onChange: i,
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "json", children: "JSON" }),
                  /* @__PURE__ */ s.jsx("option", { value: "text", children: "Text" }),
                  /* @__PURE__ */ s.jsx("option", { value: "structured", children: "Structured" })
                ]
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Max Log Size (MB)", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "max_size_mb",
                min: "1",
                max: "1000",
                value: ((Y = (G = e.monitoringConfig) == null ? void 0 : G.logging) == null ? void 0 : Y.max_size_mb) || 100,
                onChange: i
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Alerting Configuration",
        description: "Manage alert destinations and thresholds.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (ne = (Q = e.monitoringConfig) == null ? void 0 : Q.alerting) != null && ne.enabled ? "success" : "neutral", children: (se = (z = e.monitoringConfig) == null ? void 0 : z.alerting) != null && se.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((ie = (oe = e.monitoringConfig) == null ? void 0 : oe.alerting) == null ? void 0 : ie.enabled) || !1,
              onChange: o,
              label: "Enable alerting"
            }
          ),
          ((ae = (K = e.monitoringConfig) == null ? void 0 : K.alerting) == null ? void 0 : ae.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-3", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Alert Destinations", children: /* @__PURE__ */ s.jsx(
              Lt,
              {
                name: "destinations",
                value: (($ = (fe = e.monitoringConfig) == null ? void 0 : fe.alerting) == null ? void 0 : $.destinations) || "",
                onChange: o,
                placeholder: "email, slack, pagerduty, etc."
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Alert Thresholds (JSON)", children: /* @__PURE__ */ s.jsx(
              Lt,
              {
                name: "thresholds_json",
                value: JSON.stringify(((re = (Ee = e.monitoringConfig) == null ? void 0 : Ee.alerting) == null ? void 0 : re.thresholds) || {}, null, 2),
                onChange: (Ne) => {
                  var ke;
                  try {
                    const Me = JSON.parse(Ne.target.value);
                    t({ monitoringConfig: { ...e.monitoringConfig, alerting: { ...(ke = e.monitoringConfig) == null ? void 0 : ke.alerting, thresholds: Me } } });
                  } catch {
                  }
                },
                placeholder: '{"cpu": 90, "memory": 85, "error_rate": 10}'
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Alert Cooldown (minutes)", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "cooldown_minutes",
                min: "1",
                max: "1440",
                value: ((Oe = (Pe = e.monitoringConfig) == null ? void 0 : Pe.alerting) == null ? void 0 : Oe.cooldown_minutes) || 5,
                onChange: o
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap gap-2", children: [
      /* @__PURE__ */ s.jsx(X, { onClick: r, variant: "secondary", children: "Validate monitoring config" }),
      /* @__PURE__ */ s.jsx(X, { onClick: () => t({ monitoringConfig: { ...e.monitoringConfig } }), children: "Save monitoring config" })
    ] })
  ] });
}, ej = ({ config: e, onConfigUpdate: t, onValidate: r }) => {
  var l, c, u, d, f, p, m, y, h, g, v, b, w, j, x, _, S, k, E, L, P, A, T, M, N, q, V, B, ee, U, G, Y, Q, ne, z, se, oe, ie, K, ae, fe, $, Ee, re, Pe, Oe, Ne;
  const n = (ke) => {
    const { name: Me, value: Ae, type: C } = ke.target, R = C === "checkbox" ? ke.target.checked : void 0;
    t({ integrationConfig: { ...e.integrationConfig, [Me]: C === "checkbox" ? R : Ae } });
  }, a = (ke) => {
    var W;
    const { name: Me, value: Ae, type: C } = ke.target, R = C === "checkbox" ? ke.target.checked : void 0;
    t({ integrationConfig: { ...e.integrationConfig, rest_api: { ...(W = e.integrationConfig) == null ? void 0 : W.rest_api, [Me]: C === "checkbox" ? R : Ae } } });
  }, i = (ke) => {
    var W;
    const { name: Me, value: Ae, type: C } = ke.target, R = C === "checkbox" ? ke.target.checked : void 0;
    t({ integrationConfig: { ...e.integrationConfig, graphql: { ...(W = e.integrationConfig) == null ? void 0 : W.graphql, [Me]: C === "checkbox" ? R : Ae } } });
  }, o = (ke) => {
    var W;
    const { name: Me, value: Ae, type: C } = ke.target, R = C === "checkbox" ? ke.target.checked : void 0;
    t({ integrationConfig: { ...e.integrationConfig, websocket: { ...(W = e.integrationConfig) == null ? void 0 : W.websocket, [Me]: C === "checkbox" ? R : Ae } } });
  };
  return /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Integration Configuration",
        description: "Enable and manage integration features.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (l = e.integrationConfig) != null && l.enabled ? "success" : "neutral", children: (c = e.integrationConfig) != null && c.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsx(
          ct,
          {
            name: "enabled",
            checked: ((u = e.integrationConfig) == null ? void 0 : u.enabled) || !1,
            onChange: n,
            label: "Enable integration features"
          }
        )
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "REST API Configuration",
        description: "Configure REST endpoints and limits.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (f = (d = e.integrationConfig) == null ? void 0 : d.rest_api) != null && f.enabled ? "success" : "neutral", children: (m = (p = e.integrationConfig) == null ? void 0 : p.rest_api) != null && m.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((h = (y = e.integrationConfig) == null ? void 0 : y.rest_api) == null ? void 0 : h.enabled) || !1,
              onChange: a,
              label: "Enable REST API integration"
            }
          ),
          ((v = (g = e.integrationConfig) == null ? void 0 : g.rest_api) == null ? void 0 : v.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-2", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Base URL", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "text",
                name: "base_url",
                value: ((w = (b = e.integrationConfig) == null ? void 0 : b.rest_api) == null ? void 0 : w.base_url) || "",
                onChange: a,
                placeholder: "https://api.example.com"
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Timeout (ms)", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "timeout",
                min: "1000",
                max: "60000",
                value: ((x = (j = e.integrationConfig) == null ? void 0 : j.rest_api) == null ? void 0 : x.timeout) || 5e3,
                onChange: a
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Max Retries", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "max_retries",
                min: "0",
                max: "10",
                value: ((S = (_ = e.integrationConfig) == null ? void 0 : _.rest_api) == null ? void 0 : S.max_retries) || 3,
                onChange: a
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Endpoints (JSON)", children: /* @__PURE__ */ s.jsx(
              Lt,
              {
                name: "endpoints_json",
                value: JSON.stringify(((E = (k = e.integrationConfig) == null ? void 0 : k.rest_api) == null ? void 0 : E.endpoints) || [], null, 2),
                onChange: (ke) => {
                  var Me;
                  try {
                    const Ae = JSON.parse(ke.target.value);
                    t({ integrationConfig: { ...e.integrationConfig, rest_api: { ...(Me = e.integrationConfig) == null ? void 0 : Me.rest_api, endpoints: Ae } } });
                  } catch {
                  }
                },
                placeholder: '[{"path": "/users", "method": "GET"}]'
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "GraphQL Configuration",
        description: "Configure GraphQL batching and schema.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (P = (L = e.integrationConfig) == null ? void 0 : L.graphql) != null && P.enabled ? "success" : "neutral", children: (T = (A = e.integrationConfig) == null ? void 0 : A.graphql) != null && T.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((N = (M = e.integrationConfig) == null ? void 0 : M.graphql) == null ? void 0 : N.enabled) || !1,
              onChange: i,
              label: "Enable GraphQL integration"
            }
          ),
          ((V = (q = e.integrationConfig) == null ? void 0 : q.graphql) == null ? void 0 : V.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-3", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Schema URL", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "text",
                name: "schema_url",
                value: ((ee = (B = e.integrationConfig) == null ? void 0 : B.graphql) == null ? void 0 : ee.schema_url) || "",
                onChange: i,
                placeholder: "https://api.example.com/graphql"
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Max Batch Size", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "max_batch_size",
                min: "1",
                max: "100",
                value: ((G = (U = e.integrationConfig) == null ? void 0 : U.graphql) == null ? void 0 : G.max_batch_size) || 10,
                onChange: i
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Query Timeout (ms)", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "query_timeout_ms",
                min: "1000",
                max: "30000",
                value: ((Q = (Y = e.integrationConfig) == null ? void 0 : Y.graphql) == null ? void 0 : Q.query_timeout_ms) || 1e4,
                onChange: i
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "WebSocket Configuration",
        description: "Configure realtime connections.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (z = (ne = e.integrationConfig) == null ? void 0 : ne.websocket) != null && z.enabled ? "success" : "neutral", children: (oe = (se = e.integrationConfig) == null ? void 0 : se.websocket) != null && oe.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((K = (ie = e.integrationConfig) == null ? void 0 : ie.websocket) == null ? void 0 : K.enabled) || !1,
              onChange: o,
              label: "Enable WebSocket integration"
            }
          ),
          ((fe = (ae = e.integrationConfig) == null ? void 0 : ae.websocket) == null ? void 0 : fe.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-3", children: [
            /* @__PURE__ */ s.jsx(H, { label: "WebSocket URL", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "text",
                name: "url",
                value: ((Ee = ($ = e.integrationConfig) == null ? void 0 : $.websocket) == null ? void 0 : Ee.url) || "",
                onChange: o,
                placeholder: "wss://api.example.com/ws"
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Ping Interval (ms)", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "ping_interval",
                min: "1000",
                max: "30000",
                value: ((Pe = (re = e.integrationConfig) == null ? void 0 : re.websocket) == null ? void 0 : Pe.ping_interval) || 5e3,
                onChange: o
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Reconnect Attempts", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "reconnect_attempts",
                min: "0",
                max: "10",
                value: ((Ne = (Oe = e.integrationConfig) == null ? void 0 : Oe.websocket) == null ? void 0 : Ne.reconnect_attempts) || 5,
                onChange: o
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap gap-2", children: [
      /* @__PURE__ */ s.jsx(X, { onClick: r, variant: "secondary", children: "Validate integration config" }),
      /* @__PURE__ */ s.jsx(X, { onClick: () => t({ integrationConfig: { ...e.integrationConfig } }), children: "Save integration config" })
    ] })
  ] });
}, tj = ({ config: e, onConfigUpdate: t, onValidate: r }) => {
  var l, c, u, d, f, p, m, y, h, g, v, b, w, j, x, _, S, k, E, L, P, A, T, M, N, q, V, B, ee, U, G, Y, Q, ne, z, se, oe, ie, K, ae, fe, $, Ee, re, Pe, Oe, Ne, ke, Me;
  const n = (Ae) => {
    const { name: C, value: R, type: W } = Ae.target, ue = W === "checkbox" ? Ae.target.checked : void 0;
    t({ errorHandlingConfig: { ...e.errorHandlingConfig, [C]: W === "checkbox" ? ue : R } });
  }, a = (Ae) => {
    var le;
    const { name: C, value: R, type: W } = Ae.target, ue = W === "checkbox" ? Ae.target.checked : void 0;
    t({ errorHandlingConfig: { ...e.errorHandlingConfig, error_classification: { ...(le = e.errorHandlingConfig) == null ? void 0 : le.error_classification, [C]: W === "checkbox" ? ue : R } } });
  }, i = (Ae) => {
    var le;
    const { name: C, value: R, type: W } = Ae.target, ue = W === "checkbox" ? Ae.target.checked : void 0;
    t({ errorHandlingConfig: { ...e.errorHandlingConfig, error_recovery: { ...(le = e.errorHandlingConfig) == null ? void 0 : le.error_recovery, [C]: W === "checkbox" ? ue : R } } });
  }, o = (Ae) => {
    var le;
    const { name: C, value: R, type: W } = Ae.target, ue = W === "checkbox" ? Ae.target.checked : void 0;
    t({ errorHandlingConfig: { ...e.errorHandlingConfig, error_reporting: { ...(le = e.errorHandlingConfig) == null ? void 0 : le.error_reporting, [C]: W === "checkbox" ? ue : R } } });
  };
  return /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Error Handling Configuration",
        description: "Enable error classification, recovery, and reporting.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (l = e.errorHandlingConfig) != null && l.enabled ? "success" : "neutral", children: (c = e.errorHandlingConfig) != null && c.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsx(
          ct,
          {
            name: "enabled",
            checked: ((u = e.errorHandlingConfig) == null ? void 0 : u.enabled) || !1,
            onChange: n,
            label: "Enable error handling"
          }
        )
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Error Classification Configuration",
        description: "Classify and categorize errors.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (f = (d = e.errorHandlingConfig) == null ? void 0 : d.error_classification) != null && f.enabled ? "success" : "neutral", children: (m = (p = e.errorHandlingConfig) == null ? void 0 : p.error_classification) != null && m.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((h = (y = e.errorHandlingConfig) == null ? void 0 : y.error_classification) == null ? void 0 : h.enabled) || !1,
              onChange: a,
              label: "Enable error classification"
            }
          ),
          ((v = (g = e.errorHandlingConfig) == null ? void 0 : g.error_classification) == null ? void 0 : v.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-3", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Max History", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "max_history",
                min: "1",
                max: "1000",
                value: ((w = (b = e.errorHandlingConfig) == null ? void 0 : b.error_classification) == null ? void 0 : w.max_history) || 100,
                onChange: a
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Classification Method", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                name: "method",
                value: ((_ = (x = e.errorHandlingConfig) == null ? void 0 : x.error_classification((j = e.monitoringConfig) == null ? void 0 : j.error_handling)) == null ? void 0 : _.method) || "rule-based",
                onChange: a,
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "rule-based", children: "Rule-Based" }),
                  /* @__PURE__ */ s.jsx("option", { value: "ml-based", children: "ML-Based" }),
                  /* @__PURE__ */ s.jsx("option", { value: "hybrid", children: "Hybrid" })
                ]
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Error Categories", children: /* @__PURE__ */ s.jsx(
              Lt,
              {
                name: "categories",
                value: ((k = (S = e.errorHandlingConfig) == null ? void 0 : S.error_classification) == null ? void 0 : k.categories) || "",
                onChange: a,
                placeholder: "network_error, validation_error, authentication_error, etc."
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Error Recovery Configuration",
        description: "Set retries and recovery strategy.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (L = (E = e.errorHandlingConfig) == null ? void 0 : E.error_recovery) != null && L.enabled ? "success" : "neutral", children: (A = (P = e.errorHandlingConfig) == null ? void 0 : P.error_recovery) != null && A.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((M = (T = e.errorHandlingConfig) == null ? void 0 : T.error_recovery) == null ? void 0 : M.enabled) || !1,
              onChange: i,
              label: "Enable error recovery"
            }
          ),
          ((q = (N = e.errorHandlingConfig) == null ? void 0 : N.error_recovery) == null ? void 0 : q.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-3", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Max Attempts", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "max_attempts",
                min: "1",
                max: "10",
                value: ((B = (V = e.errorHandlingConfig) == null ? void 0 : V.error_recovery) == null ? void 0 : B.max_attempts) || 3,
                onChange: i
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Retry Delay (ms)", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "retry_delay",
                min: "100",
                max: "30000",
                value: ((U = (ee = e.errorHandlingConfig) == null ? void 0 : ee.error_recovery) == null ? void 0 : U.retry_delay) || 1e3,
                onChange: i
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Recovery Strategies", children: /* @__PURE__ */ s.jsx(
              Lt,
              {
                name: "strategies",
                value: ((Q = (Y = e.errorHandlingConfig) == null ? void 0 : Y.error_recovery((G = e.monitoringConfig) == null ? void 0 : G.error_handling)) == null ? void 0 : Q.strategies) || "",
                onChange: i,
                placeholder: "retry, fallback, circuit_breaker, etc."
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Error Reporting Configuration",
        description: "Configure reporting destinations and format.",
        actions: /* @__PURE__ */ s.jsx(Re, { tone: (z = (ne = e.errorHandlingConfig) == null ? void 0 : ne.error_reporting) != null && z.enabled ? "success" : "neutral", children: (oe = (se = e.errorHandlingConfig) == null ? void 0 : se.error_reporting) != null && oe.enabled ? "Enabled" : "Disabled" }),
        children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(
            ct,
            {
              name: "enabled",
              checked: ((K = (ie = e.errorHandlingConfig) == null ? void 0 : ie.error_reporting) == null ? void 0 : K.enabled) || !1,
              onChange: o,
              label: "Enable error reporting"
            }
          ),
          ((fe = (ae = e.errorHandlingConfig) == null ? void 0 : ae.error_reporting) == null ? void 0 : fe.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-3", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Destinations", children: /* @__PURE__ */ s.jsx(
              Lt,
              {
                name: "destinations",
                value: ((Ee = ($ = e.errorHandlingConfig) == null ? void 0 : $.error_reporting) == null ? void 0 : Ee.destinations) || "",
                onChange: o,
                placeholder: "console, file, api, email, database"
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Report Format", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                name: "format",
                value: ((Oe = (Pe = e.errorHandlingConfig) == null ? void 0 : Pe.error_reporting((re = e.monitoringConfig) == null ? void 0 : re.logging)) == null ? void 0 : Oe.format) || "json",
                onChange: o,
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "json", children: "JSON" }),
                  /* @__PURE__ */ s.jsx("option", { value: "text", children: "Text" }),
                  /* @__PURE__ */ s.jsx("option", { value: "structured", children: "Structured" })
                ]
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Report Interval (minutes)", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                name: "report_interval_minutes",
                min: "1",
                max: "1440",
                value: ((Me = (ke = e.errorHandlingConfig) == null ? void 0 : ke.error_reporting((Ne = e.monitoringConfig) == null ? void 0 : Ne.logging)) == null ? void 0 : Me.report_interval_minutes) || 5,
                onChange: o
              }
            ) })
          ] })
        ] })
      }
    ),
    /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap gap-2", children: [
      /* @__PURE__ */ s.jsx(X, { onClick: r, variant: "secondary", children: "Validate error handling config" }),
      /* @__PURE__ */ s.jsx(X, { onClick: () => t({ errorHandlingConfig: { ...e.errorHandlingConfig } }), children: "Save error handling config" })
    ] })
  ] });
}, rj = ({ config: e, onAddPerformanceProfile: t, onRemovePerformanceProfile: r, onAddSecurityProfile: n, onRemoveSecurityProfile: a }) => /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
  /* @__PURE__ */ s.jsx(
    he,
    {
      title: "Performance Profiles",
      description: "Manage saved performance presets.",
      actions: /* @__PURE__ */ s.jsx(X, { onClick: t, variant: "secondary", children: "Add performance profile" }),
      children: e.performanceProfiles && Object.keys(e.performanceProfiles).length > 0 ? /* @__PURE__ */ s.jsx("div", { className: "space-y-3", children: Object.entries(e.performanceProfiles).map(([i, o]) => {
        var l, c, u;
        return /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap items-center justify-between gap-3 rounded-lg border border-slate-100 bg-slate-50 px-4 py-3", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("div", { className: "text-sm font-semibold text-slate-900", children: i }),
            /* @__PURE__ */ s.jsxs("div", { className: "mt-1 flex flex-wrap gap-2 text-xs text-slate-500", children: [
              ((l = o.caching) == null ? void 0 : l.enabled) && /* @__PURE__ */ s.jsx(Re, { tone: "info", children: "Caching" }),
              ((c = o.parallel_processing) == null ? void 0 : c.enabled) && /* @__PURE__ */ s.jsx(Re, { tone: "warning", children: "Parallel" }),
              ((u = o.memory_management) == null ? void 0 : u.enabled) && /* @__PURE__ */ s.jsx(Re, { tone: "neutral", children: "Memory" })
            ] })
          ] }),
          /* @__PURE__ */ s.jsx(X, { onClick: () => r(i), variant: "ghost", children: "Remove" })
        ] }, i);
      }) }) : /* @__PURE__ */ s.jsx("p", { className: "text-sm text-slate-500", children: "No performance profiles defined." })
    }
  ),
  /* @__PURE__ */ s.jsx(
    he,
    {
      title: "Security Profiles",
      description: "Manage saved security presets.",
      actions: /* @__PURE__ */ s.jsx(X, { onClick: n, variant: "secondary", children: "Add security profile" }),
      children: e.securityProfiles && Object.keys(e.securityProfiles).length > 0 ? /* @__PURE__ */ s.jsx("div", { className: "space-y-3", children: Object.entries(e.securityProfiles).map(([i, o]) => {
        var l, c, u;
        return /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap items-center justify-between gap-3 rounded-lg border border-slate-100 bg-slate-50 px-4 py-3", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("div", { className: "text-sm font-semibold text-slate-900", children: i }),
            /* @__PURE__ */ s.jsxs("div", { className: "mt-1 flex flex-wrap gap-2 text-xs text-slate-500", children: [
              ((l = o.authentication) == null ? void 0 : l.enabled) && /* @__PURE__ */ s.jsx(Re, { tone: "info", children: "Auth" }),
              ((c = o.data_protection) == null ? void 0 : c.enabled) && /* @__PURE__ */ s.jsx(Re, { tone: "warning", children: "Protection" }),
              ((u = o.compliance) == null ? void 0 : u.enabled) && /* @__PURE__ */ s.jsx(Re, { tone: "neutral", children: "Compliance" })
            ] })
          ] }),
          /* @__PURE__ */ s.jsx(X, { onClick: () => a(i), variant: "ghost", children: "Remove" })
        ] }, i);
      }) }) : /* @__PURE__ */ s.jsx("p", { className: "text-sm text-slate-500", children: "No security profiles defined." })
    }
  )
] }), nj = ({ config: e, onClearValidationHistory: t }) => {
  var r, n, a, i, o, l, c, u, d, f, p, m, y;
  return /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
    /* @__PURE__ */ s.jsx(he, { title: "Execution Statistics", description: "Execution volume and timings.", children: /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-3", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg border border-slate-100 bg-slate-50 p-4", children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-xs uppercase tracking-wide text-slate-500", children: "Total Executions" }),
        /* @__PURE__ */ s.jsx("div", { className: "mt-2 text-2xl font-semibold text-slate-900", children: ((r = e.executionStatistics) == null ? void 0 : r.totalExecutions) || 0 })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg border border-slate-100 bg-slate-50 p-4", children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-xs uppercase tracking-wide text-slate-500", children: "Successful Executions" }),
        /* @__PURE__ */ s.jsx("div", { className: "mt-2 text-2xl font-semibold text-emerald-600", children: ((n = e.executionStatistics) == null ? void 0 : n.successfulExecutions) || 0 })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg border border-slate-100 bg-slate-50 p-4", children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-xs uppercase tracking-wide text-slate-500", children: "Failed Executions" }),
        /* @__PURE__ */ s.jsx("div", { className: "mt-2 text-2xl font-semibold text-rose-600", children: ((a = e.executionStatistics) == null ? void 0 : a.failedExecutions) || 0 })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg border border-slate-100 bg-slate-50 p-4", children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-xs uppercase tracking-wide text-slate-500", children: "Total Execution Time" }),
        /* @__PURE__ */ s.jsxs("div", { className: "mt-2 text-lg font-semibold text-slate-900", children: [
          (((i = e.executionStatistics) == null ? void 0 : i.totalExecutions) || 0) / 1e3,
          " seconds"
        ] })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg border border-slate-100 bg-slate-50 p-4", children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-xs uppercase tracking-wide text-slate-500", children: "Average Execution Time" }),
        /* @__PURE__ */ s.jsx("div", { className: "mt-2 text-lg font-semibold text-slate-900", children: (o = e.executionStatistics) != null && o.totalExecutions && e.executionStatistics.totalExecutions > 0 ? e.executionStatistics.totalExecutions / e.executionStatistics.totalExecutions / 1e3 + " seconds" : "N/A" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg border border-slate-100 bg-slate-50 p-4", children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-xs uppercase tracking-wide text-slate-500", children: "Success Rate" }),
        /* @__PURE__ */ s.jsx("div", { className: "mt-2 text-lg font-semibold text-slate-900", children: (l = e.executionStatistics) != null && l.totalExecutions && e.executionStatistics.totalExecutions > 0 ? (e.executionStatistics.successfulExecutions / e.executionStatistics.totalExecutions * 100).toFixed(2) + "%" : "N/A" })
      ] })
    ] }) }),
    /* @__PURE__ */ s.jsx(he, { title: "Error Statistics", description: "Error counts and types.", children: /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-2", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg border border-slate-100 bg-slate-50 p-4", children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-xs uppercase tracking-wide text-slate-500", children: "Total Errors" }),
        /* @__PURE__ */ s.jsx("div", { className: "mt-2 text-2xl font-semibold text-slate-900", children: ((c = e.errorStatistics) == null ? void 0 : c.totalErrors) || 0 })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg border border-slate-100 bg-slate-50 p-4", children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-xs uppercase tracking-wide text-slate-500", children: "Error Rate" }),
        /* @__PURE__ */ s.jsx("div", { className: "mt-2 text-lg font-semibold text-slate-900", children: (u = e.executionStatistics) != null && u.totalExecutions && e.executionStatistics.totalExecutions > 0 ? ((((d = e.errorStatistics) == null ? void 0 : d.totalErrors) || 0) / e.executionStatistics.totalExecutions * 100).toFixed(2) + "%" : "N/A" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg border border-slate-100 bg-slate-50 p-4", children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-xs uppercase tracking-wide text-slate-500", children: "Most Common Error Type" }),
        /* @__PURE__ */ s.jsx("div", { className: "mt-2 text-lg font-semibold text-slate-900", children: (f = e.errorStatistics) != null && f.errorsByType && ((p = Object.entries(e.errorStatistics.errorsByType).sort((h, g) => g[1] - h[1])[0]) == null ? void 0 : p[0]) || "None" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg border border-slate-100 bg-slate-50 p-4", children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-xs uppercase tracking-wide text-slate-500", children: "Last Error" }),
        /* @__PURE__ */ s.jsx("div", { className: "mt-2 text-lg font-semibold text-slate-900", children: ((y = (m = e.errorStatistics) == null ? void 0 : m.lastError) == null ? void 0 : y.errorMessage) || "None" })
      ] })
    ] }) }),
    /* @__PURE__ */ s.jsx(
      he,
      {
        title: "Validation History",
        description: "Recent validation outcomes.",
        actions: /* @__PURE__ */ s.jsx(X, { onClick: t, variant: "secondary", children: "Clear validation history" }),
        children: e.validationHistory && e.validationHistory.length > 0 ? /* @__PURE__ */ s.jsx("div", { className: "overflow-x-auto", children: /* @__PURE__ */ s.jsxs("table", { className: "w-full border-collapse text-sm", children: [
          /* @__PURE__ */ s.jsx("thead", { className: "text-left text-xs uppercase tracking-wide text-slate-500", children: /* @__PURE__ */ s.jsxs("tr", { children: [
            /* @__PURE__ */ s.jsx("th", { className: "py-2", children: "Timestamp" }),
            /* @__PURE__ */ s.jsx("th", { className: "py-2", children: "Type" }),
            /* @__PURE__ */ s.jsx("th", { className: "py-2", children: "Status" }),
            /* @__PURE__ */ s.jsx("th", { className: "py-2", children: "Message" })
          ] }) }),
          /* @__PURE__ */ s.jsx("tbody", { className: "text-slate-700", children: e.validationHistory.slice().reverse().map((h) => /* @__PURE__ */ s.jsxs("tr", { className: "border-t border-slate-100", children: [
            /* @__PURE__ */ s.jsx("td", { className: "py-2", children: new Date(h.timestamp).toLocaleString() }),
            /* @__PURE__ */ s.jsx("td", { className: "py-2", children: h.validationType }),
            /* @__PURE__ */ s.jsx("td", { className: "py-2", children: /* @__PURE__ */ s.jsx(Re, { tone: h.success ? "success" : "danger", children: h.success ? "Success" : "Failed" }) }),
            /* @__PURE__ */ s.jsx("td", { className: "py-2", children: h.errorMessage || "Valid" })
          ] }, h.validationId)) })
        ] }) }) : /* @__PURE__ */ s.jsx("p", { className: "text-sm text-slate-500", children: "No validation history available." })
      }
    )
  ] });
}, vz = () => {
  const [e, t] = de("performance"), [r, n] = de(null), [a, i] = de(!0), [o, l] = de(!1), c = Sm();
  et(() => {
    try {
      const b = c.getEnhancedState();
      n(b), i(!1);
    } catch (b) {
      F.error(`Failed to load enhanced configuration: ${b instanceof Error ? b.message : String(b)}`), i(!1);
    }
  }, [c]), et(() => {
    const b = c.subscribeToEnhancedState((w) => {
      n(w);
    });
    return () => b();
  }, [c]);
  const u = (b) => {
    try {
      if (!c.updateEnhancedConfig(b))
        throw new Error("Failed to update configuration");
    } catch (w) {
      F.error(`Failed to update configuration: ${w instanceof Error ? w.message : String(w)}`);
    }
  }, d = () => {
    try {
      if (window.confirm("Are you sure you want to reset all enhanced configuration to defaults?") && !c.resetEnhancedConfig())
        throw new Error("Failed to reset configuration");
    } catch (b) {
      F.error(`Failed to reset configuration: ${b instanceof Error ? b.message : String(b)}`);
    }
  }, f = (b) => {
    try {
      let w = !1;
      const j = ht(), x = Date.now();
      switch (b) {
        case "performance":
          w = c.validatePerformanceConfig();
          break;
        case "security":
          w = c.validateSecurityConfig();
          break;
        case "monitoring":
          w = c.validateMonitoringConfig();
          break;
        case "integration":
          w = c.validateIntegrationConfig();
          break;
        case "error_handling":
          w = c.validateErrorHandlingConfig();
          break;
      }
      c.addValidationResult({
        validationId: j,
        validationType: b,
        success: w,
        errorMessage: w ? void 0 : `Validation failed for ${b}`,
        timestamp: x
      });
    } catch (w) {
      F.error(`Failed to validate ${b} configuration: ${w instanceof Error ? w.message : String(w)}`);
    }
  }, p = () => {
    var b, w;
    try {
      const j = window.prompt("Enter performance profile name:");
      if (!j || !r) return;
      const x = {
        ...r.performanceConfig,
        caching: {
          ...(b = r.performanceConfig) == null ? void 0 : b.caching,
          enabled: !0
        },
        parallel_processing: {
          ...(w = r.performanceConfig) == null ? void 0 : w.parallel_processing,
          enabled: !0
        }
      };
      if (!c.addPerformanceProfile(j, x))
        throw new Error("Failed to add performance profile");
    } catch (j) {
      F.error(`Failed to add performance profile: ${j instanceof Error ? j.message : String(j)}`);
    }
  }, m = (b) => {
    try {
      if (window.confirm(`Are you sure you want to remove performance profile "${b}"?`) && !c.removePerformanceProfile(b))
        throw new Error("Failed to remove performance profile");
    } catch (w) {
      F.error(`Failed to remove performance profile: ${w instanceof Error ? w.message : String(w)}`);
    }
  }, y = () => {
    var b, w, j, x;
    try {
      const _ = window.prompt("Enter security profile name:");
      if (!_ || !r) return;
      const S = {
        ...r.securityConfig,
        authentication: {
          ...(b = r.securityConfig) == null ? void 0 : b.authentication,
          enabled: !0
        },
        data_protection: {
          ...(w = r.securityConfig) == null ? void 0 : w.data_protection,
          encryption: {
            ...(x = (j = r.securityConfig) == null ? void 0 : j.data_protection) == null ? void 0 : x.encryption,
            enabled: !0
          }
        }
      };
      if (!c.addSecurityProfile(_, S))
        throw new Error("Failed to add security profile");
    } catch (_) {
      F.error(`Failed to add security profile: ${_ instanceof Error ? _.message : String(_)}`);
    }
  }, h = (b) => {
    try {
      if (window.confirm(`Are you sure you want to remove security profile "${b}"?`) && !c.removeSecurityProfile(b))
        throw new Error("Failed to remove security profile");
    } catch (w) {
      F.error(`Failed to remove security profile: ${w instanceof Error ? w.message : String(w)}`);
    }
  }, g = () => {
    try {
      if (window.confirm("Are you sure you want to clear all validation history?") && !c.clearValidationHistory())
        throw new Error("Failed to clear validation history");
    } catch (b) {
      F.error(`Failed to clear validation history: ${b instanceof Error ? b.message : String(b)}`);
    }
  }, v = async () => {
    var b;
    try {
      const w = window.prompt("Enter evolution goal:");
      if (!w) return;
      const j = window.prompt("Enter performance profile name (leave blank for default):"), x = window.prompt("Enter security profile name (leave blank for default):"), _ = window.confirm("Enable monitoring?"), S = window.prompt("Enter integration mode (auto/manual/disabled):") || "auto", k = await c.executeEvolutionWithEnhancedFeatures(w, {
        performanceProfile: j || void 0,
        securityProfile: x || void 0,
        monitoringEnabled: _,
        integrationMode: S
      });
      k.success ? (F.success("Evolution executed successfully with enhanced features"), console.log("Execution result:", k)) : (F.error(`Evolution failed: ${((b = k.error) == null ? void 0 : b.message) || "Unknown error"}`), console.error("Execution error:", k.error));
    } catch (w) {
      F.error(`Failed to execute evolution: ${w instanceof Error ? w.message : String(w)}`);
    }
  };
  return a ? /* @__PURE__ */ s.jsx("div", { className: "mx-auto max-w-6xl px-6 py-6", children: /* @__PURE__ */ s.jsx(he, { title: "Enhanced OpenEvolve Configuration", description: "Loading enhanced configuration...", children: /* @__PURE__ */ s.jsx("div", { className: "text-sm text-slate-500", children: "Loading enhanced configuration..." }) }) }) : r ? /* @__PURE__ */ s.jsxs("div", { className: "mx-auto max-w-6xl space-y-6 px-6 py-6", children: [
    /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap items-start justify-between gap-4", children: [
      /* @__PURE__ */ s.jsxs("div", { children: [
        /* @__PURE__ */ s.jsx("h1", { className: "text-2xl font-semibold text-slate-900", children: "Enhanced OpenEvolve Configuration" }),
        /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-sm text-slate-500", children: "Tune performance, security, monitoring, and integrations." })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap gap-2", children: [
        /* @__PURE__ */ s.jsx(X, { onClick: () => l(!o), variant: "secondary", children: o ? "Light mode" : "Dark mode" }),
        /* @__PURE__ */ s.jsx(X, { onClick: d, variant: "ghost", children: "Reset all config" }),
        /* @__PURE__ */ s.jsx(X, { onClick: v, children: "Execute with enhanced features" })
      ] })
    ] }),
    /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap gap-2", children: [
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "performance" ? "primary" : "secondary",
          onClick: () => t("performance"),
          children: "Performance"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "security" ? "primary" : "secondary",
          onClick: () => t("security"),
          children: "Security"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "monitoring" ? "primary" : "secondary",
          onClick: () => t("monitoring"),
          children: "Monitoring"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "integration" ? "primary" : "secondary",
          onClick: () => t("integration"),
          children: "Integration"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "error_handling" ? "primary" : "secondary",
          onClick: () => t("error_handling"),
          children: "Error Handling"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "profiles" ? "primary" : "secondary",
          onClick: () => t("profiles"),
          children: "Profiles"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "statistics" ? "primary" : "secondary",
          onClick: () => t("statistics"),
          children: "Statistics"
        }
      )
    ] }),
    /* @__PURE__ */ s.jsxs("div", { className: "rounded-xl border border-slate-100 bg-slate-50 p-5", children: [
      e === "performance" && /* @__PURE__ */ s.jsx(
        Q_,
        {
          config: r,
          onConfigUpdate: u,
          onValidate: () => f("performance")
        }
      ),
      e === "security" && /* @__PURE__ */ s.jsx(
        J_,
        {
          config: r,
          onConfigUpdate: u,
          onValidate: () => f("security")
        }
      ),
      e === "monitoring" && /* @__PURE__ */ s.jsx(
        Z_,
        {
          config: r,
          onConfigUpdate: u,
          onValidate: () => f("monitoring")
        }
      ),
      e === "integration" && /* @__PURE__ */ s.jsx(
        ej,
        {
          config: r,
          onConfigUpdate: u,
          onValidate: () => f("integration")
        }
      ),
      e === "error_handling" && /* @__PURE__ */ s.jsx(
        tj,
        {
          config: r,
          onConfigUpdate: u,
          onValidate: () => f("error_handling")
        }
      ),
      e === "profiles" && /* @__PURE__ */ s.jsx(
        rj,
        {
          config: r,
          onAddPerformanceProfile: p,
          onRemovePerformanceProfile: m,
          onAddSecurityProfile: y,
          onRemoveSecurityProfile: h
        }
      ),
      e === "statistics" && /* @__PURE__ */ s.jsx(
        nj,
        {
          config: r,
          onClearValidationHistory: g
        }
      )
    ] })
  ] }) : /* @__PURE__ */ s.jsx("div", { className: "mx-auto max-w-6xl px-6 py-6", children: /* @__PURE__ */ s.jsx(he, { title: "Enhanced OpenEvolve Configuration", description: "Failed to load enhanced configuration.", children: /* @__PURE__ */ s.jsx("div", { className: "text-sm text-slate-500", children: "Please retry or check logs for details." }) }) });
}, sc = ({ className: e }) => /* @__PURE__ */ s.jsx("span", { className: e, children: "⚙️" }), lc = ({ className: e }) => /* @__PURE__ */ s.jsx("span", { className: e, children: "🧠" }), gg = ({ className: e }) => /* @__PURE__ */ s.jsx("span", { className: e, children: "🛡️" }), yg = ({ className: e }) => /* @__PURE__ */ s.jsx("span", { className: e, children: "🧩" }), vg = ({ className: e }) => /* @__PURE__ */ s.jsx("span", { className: e, children: "🌐" }), bz = ({
  plugin: e = Y_,
  onConfigChange: t
}) => {
  var d, f, p, m, y;
  const [r, n] = de(e.getConfig()), [a, i] = de("general"), [o, l] = de(!0);
  et(() => {
    (async () => {
      try {
        l(!0);
        const g = e.getConfig();
        n(g), l(!1);
      } catch (g) {
        F.error(`Failed to load configuration: ${g instanceof Error ? g.message : String(g)}`), l(!1);
      }
    })();
  }, [e]);
  const c = async (h) => {
    try {
      l(!0), await e.updateConfig(h), n(h), t == null || t(h), F.success("Configuration updated successfully"), l(!1);
    } catch (g) {
      F.error(`Failed to update configuration: ${g instanceof Error ? g.message : String(g)}`), l(!1);
    }
  }, u = async () => {
    try {
      l(!0), await e.resetConfig();
      const h = e.getConfig();
      n(h), t == null || t(h), F.success("Configuration reset to defaults"), l(!1);
    } catch (h) {
      F.error(`Failed to reset configuration: ${h instanceof Error ? h.message : String(h)}`), l(!1);
    }
  };
  return o ? /* @__PURE__ */ s.jsx("div", { className: "openevolve-config-panel p-6 bg-white dark:bg-gray-800 rounded-lg shadow", children: /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-center h-64", children: [
    /* @__PURE__ */ s.jsx("div", { className: "animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-blue-500" }),
    /* @__PURE__ */ s.jsx("span", { className: "ml-3 text-gray-600 dark:text-gray-300", children: "Loading configuration..." })
  ] }) }) : /* @__PURE__ */ s.jsxs("div", { className: "openevolve-config-panel bg-white dark:bg-gray-800 rounded-lg shadow-lg overflow-hidden", children: [
    /* @__PURE__ */ s.jsx("div", { className: "border-b border-gray-200 dark:border-gray-700 px-6 py-4", children: /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
        /* @__PURE__ */ s.jsx(lc, { className: "w-6 h-6 text-blue-600 dark:text-blue-400 mr-3" }),
        /* @__PURE__ */ s.jsx("h2", { className: "text-xl font-bold text-gray-900 dark:text-white", children: "OpenEvolve Configuration" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center space-x-2", children: [
        /* @__PURE__ */ s.jsx(
          X,
          {
            onClick: () => c(r),
            disabled: o,
            className: "px-3 py-1 text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 rounded-md disabled:opacity-50 disabled:cursor-not-allowed",
            children: "Save"
          }
        ),
        /* @__PURE__ */ s.jsx(
          X,
          {
            onClick: u,
            disabled: o,
            className: "px-3 py-1 text-sm font-medium text-white bg-yellow-600 hover:bg-yellow-700 rounded-md disabled:opacity-50 disabled:cursor-not-allowed",
            children: "Reset"
          }
        )
      ] })
    ] }) }),
    /* @__PURE__ */ s.jsx("div", { className: "border-b border-gray-200 dark:border-gray-700", children: /* @__PURE__ */ s.jsxs("nav", { className: "-mb-px flex space-x-8 px-6", "aria-label": "Tabs", children: [
      /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i("general"),
          className: `px-4 py-4 font-medium text-sm flex items-center whitespace-nowrap border-b-2 ${a === "general" ? "border-blue-500 text-blue-600 dark:text-blue-400" : "border-transparent text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200"}`,
          children: [
            /* @__PURE__ */ s.jsx(sc, { className: "mr-2 w-4 h-4" }),
            " General"
          ]
        }
      ),
      /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i("evolution"),
          className: `px-4 py-4 font-medium text-sm flex items-center whitespace-nowrap border-b-2 ${a === "evolution" ? "border-blue-500 text-blue-600 dark:text-blue-400" : "border-transparent text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200"}`,
          children: [
            /* @__PURE__ */ s.jsx(lc, { className: "mr-2 w-4 h-4" }),
            " Evolution"
          ]
        }
      ),
      /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i("adversarial"),
          className: `px-4 py-4 font-medium text-sm flex items-center whitespace-nowrap border-b-2 ${a === "adversarial" ? "border-blue-500 text-blue-600 dark:text-blue-400" : "border-transparent text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200"}`,
          children: [
            /* @__PURE__ */ s.jsx(gg, { className: "mr-2 w-4 h-4" }),
            " Adversarial"
          ]
        }
      ),
      /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i("decomposition"),
          className: `px-4 py-4 font-medium text-sm flex items-center whitespace-nowrap border-b-2 ${a === "decomposition" ? "border-blue-500 text-blue-600 dark:text-blue-400" : "border-transparent text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200"}`,
          children: [
            /* @__PURE__ */ s.jsx(yg, { className: "mr-2 w-4 h-4" }),
            " Decomposition"
          ]
        }
      ),
      /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i("mdap_maker"),
          className: `px-4 py-4 font-medium text-sm flex items-center whitespace-nowrap border-b-2 ${a === "mdap_maker" ? "border-blue-500 text-blue-600 dark:text-blue-400" : "border-transparent text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200"}`,
          children: [
            /* @__PURE__ */ s.jsx(vg, { className: "mr-2 w-4 h-4" }),
            " MDAP/MAKER"
          ]
        }
      )
    ] }) }),
    /* @__PURE__ */ s.jsxs("div", { className: "px-6 py-4", children: [
      a === "general" && /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
        /* @__PURE__ */ s.jsxs("div", { className: "border border-gray-200 dark:border-gray-700 rounded-lg p-4", children: [
          /* @__PURE__ */ s.jsxs("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center", children: [
            /* @__PURE__ */ s.jsx(sc, { className: "mr-2" }),
            " General Settings"
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-4", children: [
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "defaultExecutionMethod", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Default Execution Method" }),
              /* @__PURE__ */ s.jsx(
                je,
                {
                  id: "defaultExecutionMethod",
                  value: r.defaultExecutionMethod,
                  onChange: (h) => n({ ...r, defaultExecutionMethod: h.target.value }),
                  className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                  children: ge.EXECUTION_METHODS.map((h) => /* @__PURE__ */ s.jsx("option", { value: h, children: h.replace("_", " ").replace(/\b\w/g, (g) => g.toUpperCase()) }, h))
                }
              )
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "pluginStatus", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Plugin Status" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "pluginStatus",
                  type: "text",
                  value: r.status,
                  readOnly: !0,
                  className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm bg-gray-100 dark:bg-gray-700 text-gray-900 dark:text-white cursor-not-allowed"
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "mt-4", children: [
            /* @__PURE__ */ s.jsx("label", { className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Plugin Metadata" }),
            /* @__PURE__ */ s.jsx("div", { className: "bg-gray-50 dark:bg-gray-700 p-3 rounded-md", children: /* @__PURE__ */ s.jsx("pre", { className: "text-sm text-gray-600 dark:text-gray-300 overflow-x-auto", children: JSON.stringify(r.metadata, null, 2) }) })
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "border border-gray-200 dark:border-gray-700 rounded-lg p-4", children: [
          /* @__PURE__ */ s.jsxs("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center", children: [
            /* @__PURE__ */ s.jsx(sc, { className: "mr-2" }),
            " Execution Statistics"
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-2 md:grid-cols-4 gap-4", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "text-center", children: [
              /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-500 dark:text-gray-400 truncate", children: "Total Executions" }),
              /* @__PURE__ */ s.jsx("dd", { className: "mt-1 text-3xl font-semibold text-gray-900 dark:text-white", children: r.executionHistory.length })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { className: "text-center", children: [
              /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-500 dark:text-gray-400 truncate", children: "Success Rate" }),
              /* @__PURE__ */ s.jsx("dd", { className: "mt-1 text-3xl font-semibold text-green-600 dark:text-green-400", children: r.statistics.length > 0 ? `${(r.statistics.filter((h) => h.status === "completed").length / r.statistics.length * 100).toFixed(1)}%` : "N/A" })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { className: "text-center", children: [
              /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-500 dark:text-gray-400 truncate", children: "Avg Performance" }),
              /* @__PURE__ */ s.jsx("dd", { className: "mt-1 text-3xl font-semibold text-blue-600 dark:text-blue-400", children: r.statistics.length > 0 ? (r.statistics.reduce((h, g) => h + g.performanceScore, 0) / r.statistics.length).toFixed(2) : "N/A" })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { className: "text-center", children: [
              /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-500 dark:text-gray-400 truncate", children: "Avg Quality" }),
              /* @__PURE__ */ s.jsx("dd", { className: "mt-1 text-3xl font-semibold text-purple-600 dark:text-purple-400", children: r.statistics.length > 0 ? (r.statistics.reduce((h, g) => h + g.qualityScore, 0) / r.statistics.length).toFixed(2) : "N/A" })
            ] })
          ] })
        ] })
      ] }),
      a === "evolution" && /* @__PURE__ */ s.jsx("div", { className: "space-y-6", children: /* @__PURE__ */ s.jsxs("div", { className: "border border-gray-200 dark:border-gray-700 rounded-lg p-4", children: [
        /* @__PURE__ */ s.jsxs("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center", children: [
          /* @__PURE__ */ s.jsx(lc, { className: "mr-2" }),
          " Evolution Configuration"
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "evolutionMode", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Evolution Mode" }),
            /* @__PURE__ */ s.jsx(
              je,
              {
                id: "evolutionMode",
                value: r.evolutionConfig.evolutionMode,
                onChange: (h) => n({
                  ...r,
                  evolutionConfig: {
                    ...r.evolutionConfig,
                    evolutionMode: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: ge.EVOLUTION_STRATEGIES.map((h) => /* @__PURE__ */ s.jsx("option", { value: h, children: h.replace("_", " ").replace(/\b\w/g, (g) => g.toUpperCase()) }, h))
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "maxIterations", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Max Iterations" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "maxIterations",
                type: "number",
                min: "1",
                value: r.evolutionConfig.maxIterations,
                onChange: (h) => n({
                  ...r,
                  evolutionConfig: {
                    ...r.evolutionConfig,
                    maxIterations: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "populationSize", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Population Size" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "populationSize",
                type: "number",
                min: "1",
                value: r.evolutionConfig.populationSize,
                onChange: (h) => n({
                  ...r,
                  evolutionConfig: {
                    ...r.evolutionConfig,
                    populationSize: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "temperature", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Temperature" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "temperature",
                type: "number",
                step: "0.1",
                min: "0",
                max: "2",
                value: r.evolutionConfig.temperature,
                onChange: (h) => n({
                  ...r,
                  evolutionConfig: {
                    ...r.evolutionConfig,
                    temperature: parseFloat(h.target.value)
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "modelId", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Model" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "modelId",
                type: "text",
                value: r.evolutionConfig.modelId,
                onChange: (h) => n({
                  ...r,
                  evolutionConfig: {
                    ...r.evolutionConfig,
                    modelId: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "mt-6", children: [
          /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "MDAP/MAKER Integration" }),
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-4", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "evolutionMdapMakerEnabled",
                  type: "checkbox",
                  checked: r.evolutionConfig.mdapMakerEnabled,
                  onChange: (h) => n({
                    ...r,
                    evolutionConfig: {
                      ...r.evolutionConfig,
                      mdapMakerEnabled: h.target.checked
                    }
                  }),
                  className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ),
              /* @__PURE__ */ s.jsx("label", { htmlFor: "evolutionMdapMakerEnabled", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Enable MDAP/MAKER" })
            ] }),
            r.evolutionConfig.mdapMakerEnabled && /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "evolutionMdapMakerAutoSelect",
                  type: "checkbox",
                  checked: r.evolutionConfig.mdapMakerAutoSelect,
                  onChange: (h) => n({
                    ...r,
                    evolutionConfig: {
                      ...r.evolutionConfig,
                      mdapMakerAutoSelect: h.target.checked
                    }
                  }),
                  className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ),
              /* @__PURE__ */ s.jsx("label", { htmlFor: "evolutionMdapMakerAutoSelect", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Auto-Select for Critical Tasks" })
            ] })
          ] })
        ] })
      ] }) }),
      a === "adversarial" && /* @__PURE__ */ s.jsx("div", { className: "space-y-6", children: /* @__PURE__ */ s.jsxs("div", { className: "border border-gray-200 dark:border-gray-700 rounded-lg p-4", children: [
        /* @__PURE__ */ s.jsxs("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center", children: [
          /* @__PURE__ */ s.jsx(gg, { className: "mr-2" }),
          " Adversarial Configuration"
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "adversarialMode", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Adversarial Mode" }),
            /* @__PURE__ */ s.jsx(
              je,
              {
                id: "adversarialMode",
                value: r.adversarialConfig.adversarialMode,
                onChange: (h) => n({
                  ...r,
                  adversarialConfig: {
                    ...r.adversarialConfig,
                    adversarialMode: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: ge.ADVERSARIAL_STRATEGIES.map((h) => /* @__PURE__ */ s.jsx("option", { value: h, children: h.replace("_", " ").replace(/\b\w/g, (g) => g.toUpperCase()) }, h))
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "redTeamSize", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Red Team Size" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "redTeamSize",
                type: "number",
                min: "1",
                value: r.adversarialConfig.redTeamSize,
                onChange: (h) => n({
                  ...r,
                  adversarialConfig: {
                    ...r.adversarialConfig,
                    redTeamSize: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "blueTeamSize", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Blue Team Size" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "blueTeamSize",
                type: "number",
                min: "1",
                value: r.adversarialConfig.blueTeamSize,
                onChange: (h) => n({
                  ...r,
                  adversarialConfig: {
                    ...r.adversarialConfig,
                    blueTeamSize: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "maxRounds", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Max Rounds" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "maxRounds",
                type: "number",
                min: "1",
                value: r.adversarialConfig.maxRounds,
                onChange: (h) => n({
                  ...r,
                  adversarialConfig: {
                    ...r.adversarialConfig,
                    maxRounds: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "contentType", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Content Type" }),
            /* @__PURE__ */ s.jsxs(
              je,
              {
                id: "contentType",
                value: r.adversarialConfig.contentType,
                onChange: (h) => n({
                  ...r,
                  adversarialConfig: {
                    ...r.adversarialConfig,
                    contentType: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "code", children: "Code" }),
                  /* @__PURE__ */ s.jsx("option", { value: "text", children: "Text" }),
                  /* @__PURE__ */ s.jsx("option", { value: "design", children: "Design" }),
                  /* @__PURE__ */ s.jsx("option", { value: "strategy", children: "Strategy" })
                ]
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "mt-6", children: [
          /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "MDAP/MAKER Integration" }),
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-4", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "adversarialMdapMakerEnabled",
                  type: "checkbox",
                  checked: r.adversarialConfig.mdapMakerEnabled,
                  onChange: (h) => n({
                    ...r,
                    adversarialConfig: {
                      ...r.adversarialConfig,
                      mdapMakerEnabled: h.target.checked
                    }
                  }),
                  className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ),
              /* @__PURE__ */ s.jsx("label", { htmlFor: "adversarialMdapMakerEnabled", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Enable MDAP/MAKER" })
            ] }),
            r.adversarialConfig.mdapMakerEnabled && /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "adversarialMdapMakerAutoSelect",
                  type: "checkbox",
                  checked: r.adversarialConfig.mdapMakerAutoSelect,
                  onChange: (h) => n({
                    ...r,
                    adversarialConfig: {
                      ...r.adversarialConfig,
                      mdapMakerAutoSelect: h.target.checked
                    }
                  }),
                  className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ),
              /* @__PURE__ */ s.jsx("label", { htmlFor: "adversarialMdapMakerAutoSelect", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Auto-Select for Critical Tasks" })
            ] })
          ] })
        ] })
      ] }) }),
      a === "decomposition" && /* @__PURE__ */ s.jsx("div", { className: "space-y-6", children: /* @__PURE__ */ s.jsxs("div", { className: "border border-gray-200 dark:border-gray-700 rounded-lg p-4", children: [
        /* @__PURE__ */ s.jsxs("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center", children: [
          /* @__PURE__ */ s.jsx(yg, { className: "mr-2" }),
          " Decomposition Configuration"
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "decompositionStrategy", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Decomposition Strategy" }),
            /* @__PURE__ */ s.jsx(
              je,
              {
                id: "decompositionStrategy",
                value: r.decompositionConfig.decompositionStrategy,
                onChange: (h) => n({
                  ...r,
                  decompositionConfig: {
                    ...r.decompositionConfig,
                    decompositionStrategy: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: ge.DECOMPOSITION_STRATEGIES.map((h) => /* @__PURE__ */ s.jsx("option", { value: h, children: h.replace("_", " ").replace(/\b\w/g, (g) => g.toUpperCase()) }, h))
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "maxSubProblems", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Max Sub-Problems" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "maxSubProblems",
                type: "number",
                min: "1",
                value: r.decompositionConfig.maxSubProblems,
                onChange: (h) => n({
                  ...r,
                  decompositionConfig: {
                    ...r.decompositionConfig,
                    maxSubProblems: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "granularityLevel", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Granularity Level" }),
            /* @__PURE__ */ s.jsxs(
              je,
              {
                id: "granularityLevel",
                value: r.decompositionConfig.granularityLevel,
                onChange: (h) => n({
                  ...r,
                  decompositionConfig: {
                    ...r.decompositionConfig,
                    granularityLevel: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "low", children: "Low" }),
                  /* @__PURE__ */ s.jsx("option", { value: "medium", children: "Medium" }),
                  /* @__PURE__ */ s.jsx("option", { value: "high", children: "High" })
                ]
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "minSubProblemSize", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Min Sub-Problem Size" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "minSubProblemSize",
                type: "number",
                min: "10",
                value: r.decompositionConfig.minSubProblemSize,
                onChange: (h) => n({
                  ...r,
                  decompositionConfig: {
                    ...r.decompositionConfig,
                    minSubProblemSize: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "maxSubProblemSize", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Max Sub-Problem Size" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "maxSubProblemSize",
                type: "number",
                min: "50",
                value: r.decompositionConfig.maxSubProblemSize,
                onChange: (h) => n({
                  ...r,
                  decompositionConfig: {
                    ...r.decompositionConfig,
                    maxSubProblemSize: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "mt-6", children: [
          /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "MDAP/MAKER Integration" }),
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-4", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "decompositionMdapMakerEnabled",
                  type: "checkbox",
                  checked: r.decompositionConfig.mdapMakerEnabled,
                  onChange: (h) => n({
                    ...r,
                    decompositionConfig: {
                      ...r.decompositionConfig,
                      mdapMakerEnabled: h.target.checked
                    }
                  }),
                  className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ),
              /* @__PURE__ */ s.jsx("label", { htmlFor: "decompositionMdapMakerEnabled", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Enable MDAP/MAKER" })
            ] }),
            r.decompositionConfig.mdapMakerEnabled && /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "decompositionMdapMakerAutoSelect",
                  type: "checkbox",
                  checked: r.decompositionConfig.mdapMakerAutoSelect,
                  onChange: (h) => n({
                    ...r,
                    decompositionConfig: {
                      ...r.decompositionConfig,
                      mdapMakerAutoSelect: h.target.checked
                    }
                  }),
                  className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ),
              /* @__PURE__ */ s.jsx("label", { htmlFor: "decompositionMdapMakerAutoSelect", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Auto-Select for Critical Tasks" })
            ] })
          ] })
        ] })
      ] }) }),
      a === "mdap_maker" && /* @__PURE__ */ s.jsx("div", { className: "space-y-6", children: /* @__PURE__ */ s.jsxs("div", { className: "border border-gray-200 dark:border-gray-700 rounded-lg p-4", children: [
        /* @__PURE__ */ s.jsxs("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center", children: [
          /* @__PURE__ */ s.jsx(vg, { className: "mr-2" }),
          " MDAP/MAKER Configuration"
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-4", children: [
          /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "mdapMakerEnabled",
                type: "checkbox",
                checked: ((d = r.mdapMaker) == null ? void 0 : d.enabled) || !1,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    enabled: h.target.checked
                  }
                }),
                className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
              }
            ),
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerEnabled", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Enable MDAP/MAKER" })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "mdapMakerAutoSelect",
                type: "checkbox",
                checked: ((f = r.mdapMaker) == null ? void 0 : f.autoSelect) || !1,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    autoSelect: h.target.checked
                  }
                }),
                className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
              }
            ),
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerAutoSelect", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Auto-Select for Critical Tasks" })
          ] })
        ] }),
        ((p = r.mdapMaker) == null ? void 0 : p.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "mt-6 grid grid-cols-1 md:grid-cols-2 gap-4", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerMaxDepth", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Max Depth (K)" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "mdapMakerMaxDepth",
                type: "number",
                min: "1",
                max: "20",
                value: r.mdapMaker.maxDepth,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    maxDepth: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            ),
            /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Maximum depth for MDAP tree exploration" })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerKAhead", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "K-Ahead" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "mdapMakerKAhead",
                type: "number",
                min: "1",
                max: "10",
                value: r.mdapMaker.kAhead,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    kAhead: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            ),
            /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Number of steps to look ahead in planning" })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerProvider", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "AI Provider" }),
            /* @__PURE__ */ s.jsxs(
              je,
              {
                id: "mdapMakerProvider",
                value: r.mdapMaker.provider,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    provider: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "openai", children: "OpenAI" }),
                  /* @__PURE__ */ s.jsx("option", { value: "anthropic", children: "Anthropic" }),
                  /* @__PURE__ */ s.jsx("option", { value: "mistral", children: "Mistral" }),
                  /* @__PURE__ */ s.jsx("option", { value: "google", children: "Google" })
                ]
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerModel", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Model" }),
            /* @__PURE__ */ s.jsxs(
              je,
              {
                id: "mdapMakerModel",
                value: r.mdapMaker.model,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    model: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "gpt-4", children: "GPT-4" }),
                  /* @__PURE__ */ s.jsx("option", { value: "gpt-4-turbo", children: "GPT-4 Turbo" }),
                  /* @__PURE__ */ s.jsx("option", { value: "claude-3-opus", children: "Claude 3 Opus" }),
                  /* @__PURE__ */ s.jsx("option", { value: "claude-3-sonnet", children: "Claude 3 Sonnet" }),
                  /* @__PURE__ */ s.jsx("option", { value: "mistral-large", children: "Mistral Large" })
                ]
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "mdapMakerRedFlagging",
                type: "checkbox",
                checked: r.mdapMaker.redFlagging,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    redFlagging: h.target.checked
                  }
                }),
                className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
              }
            ),
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerRedFlagging", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Red-Flagging" }),
            /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400 ml-6", children: "Automatically flag potential issues during execution" })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "mdapMakerAdaptiveK",
                type: "checkbox",
                checked: r.mdapMaker.adaptiveK,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    adaptiveK: h.target.checked
                  }
                }),
                className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
              }
            ),
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerAdaptiveK", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Adaptive K" }),
            /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400 ml-6", children: "Dynamically adjust exploration depth based on complexity" })
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "mt-6", children: [
          /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Auto-Selection Keywords" }),
          /* @__PURE__ */ s.jsx("p", { className: "text-xs text-gray-500 dark:text-gray-400 mb-2", children: "Tasks containing these keywords will automatically use MDAP/MAKER for enhanced reliability" }),
          /* @__PURE__ */ s.jsx("div", { className: "flex flex-wrap gap-2", children: (y = (m = r.mdapMaker) == null ? void 0 : m.autoSelectionKeywords) == null ? void 0 : y.map((h, g) => /* @__PURE__ */ s.jsx("span", { className: "px-2 py-1 bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200 text-xs rounded-full", children: h }, g)) })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "mt-6 p-4 bg-blue-50 dark:bg-blue-900/20 rounded-md", children: [
          /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-blue-800 dark:text-blue-400 mb-2", children: "About MDAP/MAKER Technology" }),
          /* @__PURE__ */ s.jsx("p", { className: "text-sm text-blue-700 dark:text-blue-300", children: "MDAP/MAKER provides a zero-error guarantee (P(success) ≈ 99%+ with k=5) for critical tasks." })
        ] })
      ] }) })
    ] })
  ] });
}, lr = ({ icon: e, children: t, className: r }) => /* @__PURE__ */ s.jsx("span", { className: r, children: e ? e() : t }), cc = () => /* @__PURE__ */ s.jsx("span", { children: "⚙️" }), uc = () => /* @__PURE__ */ s.jsx("span", { children: "🧠" }), bg = () => /* @__PURE__ */ s.jsx("span", { children: "🛡️" }), xg = () => /* @__PURE__ */ s.jsx("span", { children: "🧩" }), wg = () => /* @__PURE__ */ s.jsx("span", { children: "🌐" }), CA = ({
  plugin: e = Y_,
  onConfigChange: t
}) => {
  var d, f, p, m, y;
  const [r, n] = de(e.getConfig()), [a, i] = de("general"), [o, l] = de(!0);
  et(() => {
    (async () => {
      try {
        l(!0);
        const g = e.getConfig();
        n(g), l(!1);
      } catch (g) {
        F.error(`Failed to load configuration: ${g instanceof Error ? g.message : String(g)}`), l(!1);
      }
    })();
  }, [e]);
  const c = async (h) => {
    try {
      l(!0), await e.updateConfig(h), n(h), t == null || t(h), F.success("Configuration updated successfully"), l(!1);
    } catch (g) {
      F.error(`Failed to update configuration: ${g instanceof Error ? g.message : String(g)}`), l(!1);
    }
  }, u = async () => {
    try {
      l(!0), await e.resetConfig();
      const h = e.getConfig();
      n(h), t == null || t(h), F.success("Configuration reset to defaults"), l(!1);
    } catch (h) {
      F.error(`Failed to reset configuration: ${h instanceof Error ? h.message : String(h)}`), l(!1);
    }
  };
  return o ? /* @__PURE__ */ s.jsx("div", { className: "openevolve-config-panel p-6 bg-white dark:bg-gray-800 rounded-lg shadow", children: /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-center h-64", children: [
    /* @__PURE__ */ s.jsx("div", { className: "animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-blue-500" }),
    /* @__PURE__ */ s.jsx("span", { className: "ml-3 text-gray-600 dark:text-gray-300", children: "Loading configuration..." })
  ] }) }) : /* @__PURE__ */ s.jsxs("div", { className: "openevolve-config-panel bg-white dark:bg-gray-800 rounded-lg shadow-lg overflow-hidden", children: [
    /* @__PURE__ */ s.jsx("div", { className: "border-b border-gray-200 dark:border-gray-700 px-6 py-4", children: /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
        /* @__PURE__ */ s.jsx(lr, { icon: uc, className: "w-6 h-6 text-blue-600 dark:text-blue-400 mr-3" }),
        /* @__PURE__ */ s.jsx("h2", { className: "text-xl font-bold text-gray-900 dark:text-white", children: "OpenEvolve Configuration" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center space-x-2", children: [
        /* @__PURE__ */ s.jsx(X, { onClick: () => c(r), disabled: o, children: "Save" }),
        /* @__PURE__ */ s.jsx(X, { onClick: u, disabled: o, variant: "secondary", children: "Reset" })
      ] })
    ] }) }),
    /* @__PURE__ */ s.jsx("div", { className: "border-b border-gray-200 dark:border-gray-700", children: /* @__PURE__ */ s.jsxs("nav", { className: "flex flex-wrap gap-2 px-6 py-3", "aria-label": "Tabs", children: [
      /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i("general"),
          variant: a === "general" ? "primary" : "secondary",
          className: "flex items-center gap-2",
          children: [
            /* @__PURE__ */ s.jsx(lr, { icon: cc, className: "w-4 h-4" }),
            " General"
          ]
        }
      ),
      /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i("evolution"),
          variant: a === "evolution" ? "primary" : "secondary",
          className: "flex items-center gap-2",
          children: [
            /* @__PURE__ */ s.jsx(lr, { icon: uc, className: "w-4 h-4" }),
            " Evolution"
          ]
        }
      ),
      /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i("adversarial"),
          variant: a === "adversarial" ? "primary" : "secondary",
          className: "flex items-center gap-2",
          children: [
            /* @__PURE__ */ s.jsx(lr, { icon: bg, className: "w-4 h-4" }),
            " Adversarial"
          ]
        }
      ),
      /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i("decomposition"),
          variant: a === "decomposition" ? "primary" : "secondary",
          className: "flex items-center gap-2",
          children: [
            /* @__PURE__ */ s.jsx(lr, { icon: xg, className: "w-4 h-4" }),
            " Decomposition"
          ]
        }
      ),
      /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i("mdap_maker"),
          variant: a === "mdap_maker" ? "primary" : "secondary",
          className: "flex items-center gap-2",
          children: [
            /* @__PURE__ */ s.jsx(lr, { icon: wg, className: "w-4 h-4" }),
            " MDAP/MAKER"
          ]
        }
      )
    ] }) }),
    /* @__PURE__ */ s.jsxs("div", { className: "px-6 py-4", children: [
      a === "general" && /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
        /* @__PURE__ */ s.jsxs("div", { className: "border border-gray-200 dark:border-gray-700 rounded-lg p-4", children: [
          /* @__PURE__ */ s.jsxs("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center", children: [
            /* @__PURE__ */ s.jsx(lr, { icon: cc, className: "mr-2" }),
            " General Settings"
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-4", children: [
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "defaultExecutionMethod", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Default Execution Method" }),
              /* @__PURE__ */ s.jsx(
                je,
                {
                  id: "defaultExecutionMethod",
                  value: r.defaultExecutionMethod,
                  onChange: (h) => n({ ...r, defaultExecutionMethod: h.target.value }),
                  className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                  children: ge.EXECUTION_METHODS.map((h) => /* @__PURE__ */ s.jsx("option", { value: h, children: h.replace("_", " ").replace(/\b\w/g, (g) => g.toUpperCase()) }, h))
                }
              )
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "pluginStatus", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Plugin Status" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "pluginStatus",
                  type: "text",
                  value: r.status,
                  readOnly: !0,
                  className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm bg-gray-100 dark:bg-gray-700 text-gray-900 dark:text-white cursor-not-allowed"
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "mt-4", children: [
            /* @__PURE__ */ s.jsx("label", { className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Plugin Metadata" }),
            /* @__PURE__ */ s.jsx("div", { className: "bg-gray-50 dark:bg-gray-700 p-3 rounded-md", children: /* @__PURE__ */ s.jsx("pre", { className: "text-sm text-gray-600 dark:text-gray-300 overflow-x-auto", children: JSON.stringify(r.metadata, null, 2) }) })
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "border border-gray-200 dark:border-gray-700 rounded-lg p-4", children: [
          /* @__PURE__ */ s.jsxs("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center", children: [
            /* @__PURE__ */ s.jsx(lr, { icon: cc, className: "mr-2" }),
            " Execution Statistics"
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-2 md:grid-cols-4 gap-4", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "text-center", children: [
              /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-500 dark:text-gray-400 truncate", children: "Total Executions" }),
              /* @__PURE__ */ s.jsx("dd", { className: "mt-1 text-3xl font-semibold text-gray-900 dark:text-white", children: r.executionHistory.length })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { className: "text-center", children: [
              /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-500 dark:text-gray-400 truncate", children: "Success Rate" }),
              /* @__PURE__ */ s.jsx("dd", { className: "mt-1 text-3xl font-semibold text-green-600 dark:text-green-400", children: r.statistics.length > 0 ? `${(r.statistics.filter((h) => h.status === "completed").length / r.statistics.length * 100).toFixed(1)}%` : "N/A" })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { className: "text-center", children: [
              /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-500 dark:text-gray-400 truncate", children: "Avg Performance" }),
              /* @__PURE__ */ s.jsx("dd", { className: "mt-1 text-3xl font-semibold text-blue-600 dark:text-blue-400", children: r.statistics.length > 0 ? (r.statistics.reduce((h, g) => h + g.performanceScore, 0) / r.statistics.length).toFixed(2) : "N/A" })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { className: "text-center", children: [
              /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-500 dark:text-gray-400 truncate", children: "Avg Quality" }),
              /* @__PURE__ */ s.jsx("dd", { className: "mt-1 text-3xl font-semibold text-purple-600 dark:text-purple-400", children: r.statistics.length > 0 ? (r.statistics.reduce((h, g) => h + g.qualityScore, 0) / r.statistics.length).toFixed(2) : "N/A" })
            ] })
          ] })
        ] })
      ] }),
      a === "evolution" && /* @__PURE__ */ s.jsx("div", { className: "space-y-6", children: /* @__PURE__ */ s.jsxs("div", { className: "border border-gray-200 dark:border-gray-700 rounded-lg p-4", children: [
        /* @__PURE__ */ s.jsxs("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center", children: [
          /* @__PURE__ */ s.jsx(lr, { icon: uc, className: "mr-2" }),
          " Evolution Configuration"
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "evolutionMode", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Evolution Mode" }),
            /* @__PURE__ */ s.jsx(
              je,
              {
                id: "evolutionMode",
                value: r.evolutionConfig.evolutionMode,
                onChange: (h) => n({
                  ...r,
                  evolutionConfig: {
                    ...r.evolutionConfig,
                    evolutionMode: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: ge.EVOLUTION_STRATEGIES.map((h) => /* @__PURE__ */ s.jsx("option", { value: h, children: h.replace("_", " ").replace(/\b\w/g, (g) => g.toUpperCase()) }, h))
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "maxIterations", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Max Iterations" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "maxIterations",
                type: "number",
                min: "1",
                value: r.evolutionConfig.maxIterations,
                onChange: (h) => n({
                  ...r,
                  evolutionConfig: {
                    ...r.evolutionConfig,
                    maxIterations: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "populationSize", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Population Size" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "populationSize",
                type: "number",
                min: "1",
                value: r.evolutionConfig.populationSize,
                onChange: (h) => n({
                  ...r,
                  evolutionConfig: {
                    ...r.evolutionConfig,
                    populationSize: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "temperature", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Temperature" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "temperature",
                type: "number",
                step: "0.1",
                min: "0",
                max: "2",
                value: r.evolutionConfig.temperature,
                onChange: (h) => n({
                  ...r,
                  evolutionConfig: {
                    ...r.evolutionConfig,
                    temperature: parseFloat(h.target.value)
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "modelId", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Model" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "modelId",
                type: "text",
                value: r.evolutionConfig.modelId,
                onChange: (h) => n({
                  ...r,
                  evolutionConfig: {
                    ...r.evolutionConfig,
                    modelId: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "mt-6", children: [
          /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "MDAP/MAKER Integration" }),
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-4", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "evolutionMdapMakerEnabled",
                  type: "checkbox",
                  checked: r.evolutionConfig.mdapMakerEnabled,
                  onChange: (h) => n({
                    ...r,
                    evolutionConfig: {
                      ...r.evolutionConfig,
                      mdapMakerEnabled: h.target.checked
                    }
                  }),
                  className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ),
              /* @__PURE__ */ s.jsx("label", { htmlFor: "evolutionMdapMakerEnabled", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Enable MDAP/MAKER" })
            ] }),
            r.evolutionConfig.mdapMakerEnabled && /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "evolutionMdapMakerAutoSelect",
                  type: "checkbox",
                  checked: r.evolutionConfig.mdapMakerAutoSelect,
                  onChange: (h) => n({
                    ...r,
                    evolutionConfig: {
                      ...r.evolutionConfig,
                      mdapMakerAutoSelect: h.target.checked
                    }
                  }),
                  className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ),
              /* @__PURE__ */ s.jsx("label", { htmlFor: "evolutionMdapMakerAutoSelect", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Auto-Select for Critical Tasks" })
            ] })
          ] })
        ] })
      ] }) }),
      a === "adversarial" && /* @__PURE__ */ s.jsx("div", { className: "space-y-6", children: /* @__PURE__ */ s.jsxs("div", { className: "border border-gray-200 dark:border-gray-700 rounded-lg p-4", children: [
        /* @__PURE__ */ s.jsxs("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center", children: [
          /* @__PURE__ */ s.jsx(lr, { icon: bg, className: "mr-2" }),
          " Adversarial Configuration"
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "adversarialMode", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Adversarial Mode" }),
            /* @__PURE__ */ s.jsx(
              je,
              {
                id: "adversarialMode",
                value: r.adversarialConfig.adversarialMode,
                onChange: (h) => n({
                  ...r,
                  adversarialConfig: {
                    ...r.adversarialConfig,
                    adversarialMode: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: ge.ADVERSARIAL_STRATEGIES.map((h) => /* @__PURE__ */ s.jsx("option", { value: h, children: h.replace("_", " ").replace(/\b\w/g, (g) => g.toUpperCase()) }, h))
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "redTeamSize", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Red Team Size" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "redTeamSize",
                type: "number",
                min: "1",
                value: r.adversarialConfig.redTeamSize,
                onChange: (h) => n({
                  ...r,
                  adversarialConfig: {
                    ...r.adversarialConfig,
                    redTeamSize: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "blueTeamSize", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Blue Team Size" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "blueTeamSize",
                type: "number",
                min: "1",
                value: r.adversarialConfig.blueTeamSize,
                onChange: (h) => n({
                  ...r,
                  adversarialConfig: {
                    ...r.adversarialConfig,
                    blueTeamSize: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "maxRounds", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Max Rounds" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "maxRounds",
                type: "number",
                min: "1",
                value: r.adversarialConfig.maxRounds,
                onChange: (h) => n({
                  ...r,
                  adversarialConfig: {
                    ...r.adversarialConfig,
                    maxRounds: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "contentType", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Content Type" }),
            /* @__PURE__ */ s.jsxs(
              je,
              {
                id: "contentType",
                value: r.adversarialConfig.contentType,
                onChange: (h) => n({
                  ...r,
                  adversarialConfig: {
                    ...r.adversarialConfig,
                    contentType: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "code", children: "Code" }),
                  /* @__PURE__ */ s.jsx("option", { value: "text", children: "Text" }),
                  /* @__PURE__ */ s.jsx("option", { value: "design", children: "Design" }),
                  /* @__PURE__ */ s.jsx("option", { value: "strategy", children: "Strategy" })
                ]
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "mt-6", children: [
          /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "MDAP/MAKER Integration" }),
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-4", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "adversarialMdapMakerEnabled",
                  type: "checkbox",
                  checked: r.adversarialConfig.mdapMakerEnabled,
                  onChange: (h) => n({
                    ...r,
                    adversarialConfig: {
                      ...r.adversarialConfig,
                      mdapMakerEnabled: h.target.checked
                    }
                  }),
                  className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ),
              /* @__PURE__ */ s.jsx("label", { htmlFor: "adversarialMdapMakerEnabled", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Enable MDAP/MAKER" })
            ] }),
            r.adversarialConfig.mdapMakerEnabled && /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "adversarialMdapMakerAutoSelect",
                  type: "checkbox",
                  checked: r.adversarialConfig.mdapMakerAutoSelect,
                  onChange: (h) => n({
                    ...r,
                    adversarialConfig: {
                      ...r.adversarialConfig,
                      mdapMakerAutoSelect: h.target.checked
                    }
                  }),
                  className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ),
              /* @__PURE__ */ s.jsx("label", { htmlFor: "adversarialMdapMakerAutoSelect", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Auto-Select for Critical Tasks" })
            ] })
          ] })
        ] })
      ] }) }),
      a === "decomposition" && /* @__PURE__ */ s.jsx("div", { className: "space-y-6", children: /* @__PURE__ */ s.jsxs("div", { className: "border border-gray-200 dark:border-gray-700 rounded-lg p-4", children: [
        /* @__PURE__ */ s.jsxs("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center", children: [
          /* @__PURE__ */ s.jsx(lr, { icon: xg, className: "mr-2" }),
          " Decomposition Configuration"
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "decompositionStrategy", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Decomposition Strategy" }),
            /* @__PURE__ */ s.jsx(
              je,
              {
                id: "decompositionStrategy",
                value: r.decompositionConfig.decompositionStrategy,
                onChange: (h) => n({
                  ...r,
                  decompositionConfig: {
                    ...r.decompositionConfig,
                    decompositionStrategy: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: ge.DECOMPOSITION_STRATEGIES.map((h) => /* @__PURE__ */ s.jsx("option", { value: h, children: h.replace("_", " ").replace(/\b\w/g, (g) => g.toUpperCase()) }, h))
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "maxSubProblems", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Max Sub-Problems" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "maxSubProblems",
                type: "number",
                min: "1",
                value: r.decompositionConfig.maxSubProblems,
                onChange: (h) => n({
                  ...r,
                  decompositionConfig: {
                    ...r.decompositionConfig,
                    maxSubProblems: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "granularityLevel", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Granularity Level" }),
            /* @__PURE__ */ s.jsxs(
              je,
              {
                id: "granularityLevel",
                value: r.decompositionConfig.granularityLevel,
                onChange: (h) => n({
                  ...r,
                  decompositionConfig: {
                    ...r.decompositionConfig,
                    granularityLevel: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "low", children: "Low" }),
                  /* @__PURE__ */ s.jsx("option", { value: "medium", children: "Medium" }),
                  /* @__PURE__ */ s.jsx("option", { value: "high", children: "High" })
                ]
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "minSubProblemSize", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Min Sub-Problem Size" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "minSubProblemSize",
                type: "number",
                min: "10",
                value: r.decompositionConfig.minSubProblemSize,
                onChange: (h) => n({
                  ...r,
                  decompositionConfig: {
                    ...r.decompositionConfig,
                    minSubProblemSize: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "maxSubProblemSize", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Max Sub-Problem Size" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "maxSubProblemSize",
                type: "number",
                min: "50",
                value: r.decompositionConfig.maxSubProblemSize,
                onChange: (h) => n({
                  ...r,
                  decompositionConfig: {
                    ...r.decompositionConfig,
                    maxSubProblemSize: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "mt-6", children: [
          /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "MDAP/MAKER Integration" }),
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-4", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "decompositionMdapMakerEnabled",
                  type: "checkbox",
                  checked: r.decompositionConfig.mdapMakerEnabled,
                  onChange: (h) => n({
                    ...r,
                    decompositionConfig: {
                      ...r.decompositionConfig,
                      mdapMakerEnabled: h.target.checked
                    }
                  }),
                  className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ),
              /* @__PURE__ */ s.jsx("label", { htmlFor: "decompositionMdapMakerEnabled", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Enable MDAP/MAKER" })
            ] }),
            r.decompositionConfig.mdapMakerEnabled && /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "decompositionMdapMakerAutoSelect",
                  type: "checkbox",
                  checked: r.decompositionConfig.mdapMakerAutoSelect,
                  onChange: (h) => n({
                    ...r,
                    decompositionConfig: {
                      ...r.decompositionConfig,
                      mdapMakerAutoSelect: h.target.checked
                    }
                  }),
                  className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ),
              /* @__PURE__ */ s.jsx("label", { htmlFor: "decompositionMdapMakerAutoSelect", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Auto-Select for Critical Tasks" })
            ] })
          ] })
        ] })
      ] }) }),
      a === "mdap_maker" && /* @__PURE__ */ s.jsx("div", { className: "space-y-6", children: /* @__PURE__ */ s.jsxs("div", { className: "border border-gray-200 dark:border-gray-700 rounded-lg p-4", children: [
        /* @__PURE__ */ s.jsxs("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-4 flex items-center", children: [
          /* @__PURE__ */ s.jsx(lr, { icon: wg, className: "mr-2" }),
          " MDAP/MAKER Configuration"
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-4", children: [
          /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "mdapMakerEnabled",
                type: "checkbox",
                checked: ((d = r.mdapMaker) == null ? void 0 : d.enabled) || !1,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    enabled: h.target.checked
                  }
                }),
                className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
              }
            ),
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerEnabled", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Enable MDAP/MAKER" })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "mdapMakerAutoSelect",
                type: "checkbox",
                checked: ((f = r.mdapMaker) == null ? void 0 : f.autoSelect) || !1,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    autoSelect: h.target.checked
                  }
                }),
                className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
              }
            ),
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerAutoSelect", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Auto-Select for Critical Tasks" })
          ] })
        ] }),
        ((p = r.mdapMaker) == null ? void 0 : p.enabled) && /* @__PURE__ */ s.jsxs("div", { className: "mt-6 grid grid-cols-1 md:grid-cols-2 gap-4", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerMaxDepth", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Max Depth (K)" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "mdapMakerMaxDepth",
                type: "number",
                min: "1",
                max: "20",
                value: r.mdapMaker.maxDepth,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    maxDepth: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            ),
            /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Maximum depth for MDAP tree exploration" })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerKAhead", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "K-Ahead" }),
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "mdapMakerKAhead",
                type: "number",
                min: "1",
                max: "10",
                value: r.mdapMaker.kAhead,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    kAhead: parseInt(h.target.value) || 0
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
              }
            ),
            /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Number of steps to look ahead in planning" })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerProvider", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "AI Provider" }),
            /* @__PURE__ */ s.jsxs(
              je,
              {
                id: "mdapMakerProvider",
                value: r.mdapMaker.provider,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    provider: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "openai", children: "OpenAI" }),
                  /* @__PURE__ */ s.jsx("option", { value: "anthropic", children: "Anthropic" }),
                  /* @__PURE__ */ s.jsx("option", { value: "mistral", children: "Mistral" }),
                  /* @__PURE__ */ s.jsx("option", { value: "google", children: "Google" })
                ]
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerModel", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1", children: "Model" }),
            /* @__PURE__ */ s.jsxs(
              je,
              {
                id: "mdapMakerModel",
                value: r.mdapMaker.model,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    model: h.target.value
                  }
                }),
                className: "w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "gpt-4", children: "GPT-4" }),
                  /* @__PURE__ */ s.jsx("option", { value: "gpt-4-turbo", children: "GPT-4 Turbo" }),
                  /* @__PURE__ */ s.jsx("option", { value: "claude-3-opus", children: "Claude 3 Opus" }),
                  /* @__PURE__ */ s.jsx("option", { value: "claude-3-sonnet", children: "Claude 3 Sonnet" }),
                  /* @__PURE__ */ s.jsx("option", { value: "mistral-large", children: "Mistral Large" })
                ]
              }
            )
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "mdapMakerRedFlagging",
                type: "checkbox",
                checked: r.mdapMaker.redFlagging,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    redFlagging: h.target.checked
                  }
                }),
                className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
              }
            ),
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerRedFlagging", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Red-Flagging" }),
            /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400 ml-6", children: "Automatically flag potential issues during execution" })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
            /* @__PURE__ */ s.jsx(
              D,
              {
                id: "mdapMakerAdaptiveK",
                type: "checkbox",
                checked: r.mdapMaker.adaptiveK,
                onChange: (h) => n({
                  ...r,
                  mdapMaker: {
                    ...r.mdapMaker,
                    adaptiveK: h.target.checked
                  }
                }),
                className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
              }
            ),
            /* @__PURE__ */ s.jsx("label", { htmlFor: "mdapMakerAdaptiveK", className: "ml-2 block text-sm text-gray-700 dark:text-gray-300", children: "Adaptive K" }),
            /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400 ml-6", children: "Dynamically adjust exploration depth based on complexity" })
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "mt-6", children: [
          /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Auto-Selection Keywords" }),
          /* @__PURE__ */ s.jsx("p", { className: "text-xs text-gray-500 dark:text-gray-400 mb-2", children: "Tasks containing these keywords will automatically use MDAP/MAKER for enhanced reliability" }),
          /* @__PURE__ */ s.jsx("div", { className: "flex flex-wrap gap-2", children: (y = (m = r.mdapMaker) == null ? void 0 : m.autoSelectionKeywords) == null ? void 0 : y.map((h, g) => /* @__PURE__ */ s.jsx("span", { className: "px-2 py-1 bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200 text-xs rounded-full", children: h }, g)) })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "mt-6 p-4 bg-blue-50 dark:bg-blue-900/20 rounded-md", children: [
          /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-blue-800 dark:text-blue-400 mb-2", children: "About MDAP/MAKER Technology" }),
          /* @__PURE__ */ s.jsx("p", { className: "text-sm text-blue-700 dark:text-blue-300", children: "MDAP/MAKER provides a zero-error guarantee (P(success) ≈ 99%+ with k=5) for critical tasks." })
        ] })
      ] }) })
    ] })
  ] });
}, xz = () => {
  const [e, t] = de("performance"), [r, n] = de(null), [a, i] = de(!0), [o, l] = de(!1), c = X_();
  et(() => {
    try {
      const b = c.getEnhancedState();
      n(b), i(!1);
    } catch (b) {
      F.error(`Failed to load enhanced configuration: ${b instanceof Error ? b.message : String(b)}`), i(!1);
    }
  }, [c]), et(() => {
    const b = c.subscribeToEnhancedState((w) => {
      n(w);
    });
    return () => b();
  }, [c]);
  const u = (b) => {
    try {
      if (!c.updateEnhancedConfig(b))
        throw new Error("Failed to update configuration");
    } catch (w) {
      F.error(`Failed to update configuration: ${w instanceof Error ? w.message : String(w)}`);
    }
  }, d = () => {
    try {
      if (window.confirm("Are you sure you want to reset all enhanced configuration to defaults?") && !c.resetEnhancedConfig())
        throw new Error("Failed to reset configuration");
    } catch (b) {
      F.error(`Failed to reset configuration: ${b instanceof Error ? b.message : String(b)}`);
    }
  }, f = (b) => {
    try {
      let w = !1;
      const j = ht(), x = Date.now();
      switch (b) {
        case "performance":
          w = c.validatePerformanceConfig();
          break;
        case "security":
          w = c.validateSecurityConfig();
          break;
        case "monitoring":
          w = c.validateMonitoringConfig();
          break;
        case "integration":
          w = c.validateIntegrationConfig();
          break;
        case "error_handling":
          w = c.validateErrorHandlingConfig();
          break;
      }
      c.addValidationResult({
        validationId: j,
        validationType: b,
        success: w,
        errorMessage: w ? void 0 : `Validation failed for ${b}`,
        timestamp: new Date(x)
      });
    } catch (w) {
      F.error(`Failed to validate ${b} configuration: ${w instanceof Error ? w.message : String(w)}`);
    }
  }, p = () => {
    var b, w;
    try {
      const j = window.prompt("Enter performance profile name:");
      if (!j || !r) return;
      const x = {
        ...r.performanceConfig,
        caching: {
          ...(b = r.performanceConfig) == null ? void 0 : b.caching,
          enabled: !0
        },
        parallel_processing: {
          ...(w = r.performanceConfig) == null ? void 0 : w.parallel_processing,
          enabled: !0
        }
      };
      c.addPerformanceProfile(j, x);
    } catch (j) {
      F.error(`Failed to add performance profile: ${j instanceof Error ? j.message : String(j)}`);
    }
  }, m = (b) => {
    try {
      window.confirm(`Are you sure you want to remove performance profile "${b}"?`) && c.removePerformanceProfile(b);
    } catch (w) {
      F.error(`Failed to remove performance profile: ${w instanceof Error ? w.message : String(w)}`);
    }
  }, y = () => {
    var b, w, j, x;
    try {
      const _ = window.prompt("Enter security profile name:");
      if (!_ || !r) return;
      const S = {
        ...r.securityConfig,
        authentication: {
          ...(b = r.securityConfig) == null ? void 0 : b.authentication,
          enabled: !0
        },
        data_protection: {
          ...(w = r.securityConfig) == null ? void 0 : w.data_protection,
          encryption: {
            ...(x = (j = r.securityConfig) == null ? void 0 : j.data_protection) == null ? void 0 : x.encryption,
            enabled: !0
          }
        }
      };
      c.addSecurityProfile(_, S);
    } catch (_) {
      F.error(`Failed to add security profile: ${_ instanceof Error ? _.message : String(_)}`);
    }
  }, h = (b) => {
    try {
      window.confirm(`Are you sure you want to remove security profile "${b}"?`) && c.removeSecurityProfile(b);
    } catch (w) {
      F.error(`Failed to remove security profile: ${w instanceof Error ? w.message : String(w)}`);
    }
  }, g = () => {
    try {
      window.confirm("Are you sure you want to clear all validation history?") && c.clearValidationHistory();
    } catch (b) {
      F.error(`Failed to clear validation history: ${b instanceof Error ? b.message : String(b)}`);
    }
  }, v = async () => {
    var b;
    try {
      const w = window.prompt("Enter evolution goal:");
      if (!w) return;
      const j = window.prompt("Enter performance profile name (leave blank for default):"), x = window.prompt("Enter security profile name (leave blank for default):"), _ = window.confirm("Enable monitoring?"), S = window.prompt("Enter integration mode (auto/manual/disabled):") || "auto", k = await c.executeEvolutionWithEnhancedFeatures(w, {
        performanceProfile: j || void 0,
        securityProfile: x || void 0,
        monitoringEnabled: _,
        integrationMode: S
      });
      k.success ? (F.success("Evolution executed successfully with enhanced features"), console.log("Execution result:", k)) : (F.error(`Evolution failed: ${((b = k.error) == null ? void 0 : b.message) || "Unknown error"}`), console.error("Execution error:", k.error));
    } catch (w) {
      F.error(`Failed to execute evolution: ${w instanceof Error ? w.message : String(w)}`);
    }
  };
  return a ? /* @__PURE__ */ s.jsx("div", { className: `enhanced-config-panel ${o ? "dark" : ""}`, children: /* @__PURE__ */ s.jsx("div", { className: "loading-spinner", children: "Loading enhanced configuration..." }) }) : r ? /* @__PURE__ */ s.jsxs("div", { className: `enhanced-config-panel ${o ? "dark" : ""}`, children: [
    /* @__PURE__ */ s.jsxs("div", { className: "panel-header", children: [
      /* @__PURE__ */ s.jsx("h1", { children: "Enhanced OpenEvolve Configuration" }),
      /* @__PURE__ */ s.jsxs("div", { className: "header-actions", children: [
        /* @__PURE__ */ s.jsx(X, { onClick: () => l(!o), variant: "secondary", children: o ? "?? Light Mode" : "?? Dark Mode" }),
        /* @__PURE__ */ s.jsx(X, { onClick: d, variant: "secondary", children: "?? Reset All Config" }),
        /* @__PURE__ */ s.jsx(X, { onClick: v, children: "? Execute with Enhanced Features" })
      ] })
    ] }),
    /* @__PURE__ */ s.jsxs("div", { className: "tabs-container", children: [
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "performance" ? "primary" : "secondary",
          onClick: () => t("performance"),
          children: "?? Performance"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "security" ? "primary" : "secondary",
          onClick: () => t("security"),
          children: "?? Security"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "monitoring" ? "primary" : "secondary",
          onClick: () => t("monitoring"),
          children: "?? Monitoring"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "integration" ? "primary" : "secondary",
          onClick: () => t("integration"),
          children: "?? Integration"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "error_handling" ? "primary" : "secondary",
          onClick: () => t("error_handling"),
          children: "?? Error Handling"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "profiles" ? "primary" : "secondary",
          onClick: () => t("profiles"),
          children: "?? Profiles"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          variant: e === "statistics" ? "primary" : "secondary",
          onClick: () => t("statistics"),
          children: "?? Statistics"
        }
      )
    ] }),
    /* @__PURE__ */ s.jsxs("div", { className: "tab-content", children: [
      e === "performance" && /* @__PURE__ */ s.jsx(
        Q_,
        {
          config: r,
          onConfigUpdate: u,
          onValidate: () => f("performance")
        }
      ),
      e === "security" && /* @__PURE__ */ s.jsx(
        J_,
        {
          config: r,
          onConfigUpdate: u,
          onValidate: () => f("security")
        }
      ),
      e === "monitoring" && /* @__PURE__ */ s.jsx(
        Z_,
        {
          config: r,
          onConfigUpdate: u,
          onValidate: () => f("monitoring")
        }
      ),
      e === "integration" && /* @__PURE__ */ s.jsx(
        ej,
        {
          config: r,
          onConfigUpdate: u,
          onValidate: () => f("integration")
        }
      ),
      e === "error_handling" && /* @__PURE__ */ s.jsx(
        tj,
        {
          config: r,
          onConfigUpdate: u,
          onValidate: () => f("error_handling")
        }
      ),
      e === "profiles" && /* @__PURE__ */ s.jsx(
        rj,
        {
          config: r,
          onAddPerformanceProfile: p,
          onRemovePerformanceProfile: m,
          onAddSecurityProfile: y,
          onRemoveSecurityProfile: h
        }
      ),
      e === "statistics" && /* @__PURE__ */ s.jsx(
        nj,
        {
          config: r,
          onClearValidationHistory: g
        }
      )
    ] }),
    /* @__PURE__ */ s.jsx("style", { children: `
        .enhanced-config-panel {
          font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
          max-width: 1200px;
          margin: 0 auto;
          padding: 20px;
          border-radius: 12px;
          box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
          background-color: #ffffff;
          color: #333333;
        }

        .enhanced-config-panel.dark {
          background-color: #1a1a1a;
          color: #f0f0f0;
        }

        .panel-header {
          display: flex;
          justify-content: space-between;
          align-items: center;
          margin-bottom: 20px;
          padding-bottom: 15px;
          border-bottom: 2px solid #e0e0e0;
        }

        .enhanced-config-panel.dark .panel-header {
          border-bottom-color: #333333;
        }

        .panel-header h1 {
          font-size: 28px;
          font-weight: 600;
          color: #2c3e50;
          margin: 0;
        }

        .enhanced-config-panel.dark .panel-header h1 {
          color: #f0f0f0;
        }

        .header-actions {
          display: flex;
          gap: 10px;
        }

        .tabs-container {
          display: flex;
          flex-wrap: wrap;
          gap: 8px;
          margin-bottom: 20px;
          border-bottom: 1px solid #e0e0e0;
          padding-bottom: 10px;
        }

        .enhanced-config-panel.dark .tabs-container {
          border-bottom-color: #333333;
        }

        .tab-button {
          padding: 10px 20px;
          background-color: #f5f5f5;
          border: none;
          border-radius: 8px;
          cursor: pointer;
          font-size: 14px;
          font-weight: 500;
          transition: all 0.2s ease;
          display: flex;
          align-items: center;
          gap: 6px;
        }

        .enhanced-config-panel.dark .tab-button {
          background-color: #2a2a2a;
          color: #f0f0f0;
        }

        .tab-button:hover {
          background-color: #e0e0e0;
        }

        .enhanced-config-panel.dark .tab-button:hover {
          background-color: #3a3a3a;
        }

        .tab-button.active {
          background-color: #3498db;
          color: white;
        }

        .tab-content {
          padding: 20px;
          border-radius: 8px;
          background-color: #f9f9f9;
        }

        .enhanced-config-panel.dark .tab-content {
          background-color: #1e1e1e;
        }

        .loading-spinner {
          text-align: center;
          padding: 40px;
          font-size: 16px;
          color: #666666;
        }

        .enhanced-config-panel.dark .loading-spinner {
          color: #999999;
        }

        .error-message {
          text-align: center;
          padding: 40px;
          font-size: 16px;
          color: #e74c3c;
        }

        .enhanced-config-panel.dark .error-message {
          color: #ff6b6b;
        }
      ` })
  ] }) : /* @__PURE__ */ s.jsx("div", { className: `enhanced-config-panel ${o ? "dark" : ""}`, children: /* @__PURE__ */ s.jsx("div", { className: "error-message", children: "Failed to load enhanced configuration" }) });
};
function vl({
  icon: e,
  className: t = "",
  ...r
}) {
  return /* @__PURE__ */ s.jsx(e, { className: t, ...r });
}
const _g = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), TA = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), PA = () => /* @__PURE__ */ s.jsx("span", { children: "?" }), OA = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), jg = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), NA = {
  populationSize: 100,
  generations: 50,
  elitismCount: 2,
  mutationRate: 0.1,
  crossoverRate: 0.8,
  mutationStrength: 0.5,
  selectionMethod: "tournament",
  tournamentSize: 3,
  selectionPressure: 2,
  convergenceThreshold: 1e-3,
  maxIterations: 1e3,
  stagnationGenerations: 10,
  diversityThreshold: 0.2,
  nichingEnabled: !1,
  crowdingDistance: 0.5,
  adaptiveMutation: !0,
  adaptiveCrossover: !0,
  multiObjectiveOptimization: !1,
  paretoFrontSize: 50,
  penaltyFactor: 1e3,
  constraintHandling: "penalty",
  parallelEvaluation: !0,
  evaluationBatchSize: 10,
  asyncEvaluation: !1
}, MA = ({
  config: e,
  onConfigChange: t
}) => {
  const [r, n] = de(e), [a, i] = de("population"), [o, l] = de(!1);
  et(() => {
    n(e), l(!1);
  }, [e]);
  const c = (m, y) => {
    const h = { ...r, [m]: y };
    n(h), l(!0);
  }, u = () => {
    try {
      t(r), l(!1), F.success("Evolution configuration saved successfully");
    } catch (m) {
      F.error(`Failed to save configuration: ${m instanceof Error ? m.message : String(m)}`);
    }
  }, d = () => {
    window.confirm("Are you sure you want to reset to default configuration?") && (n(NA), l(!0), F.info("Configuration reset to defaults. Click Save to apply."));
  }, f = () => {
    n(e), l(!1), F.info("Changes discarded");
  }, p = [
    { id: "population", label: "Population", icon: /* @__PURE__ */ s.jsx(_g, {}) },
    { id: "operators", label: "Genetic Operators", icon: /* @__PURE__ */ s.jsx(PA, {}) },
    { id: "selection", label: "Selection", icon: /* @__PURE__ */ s.jsx(OA, {}) },
    { id: "convergence", label: "Convergence", icon: /* @__PURE__ */ s.jsx(jg, {}) },
    { id: "diversity", label: "Diversity", icon: /* @__PURE__ */ s.jsx(TA, {}) },
    { id: "advanced", label: "Advanced", icon: /* @__PURE__ */ s.jsx(jg, {}) }
  ];
  return /* @__PURE__ */ s.jsxs("div", { className: "evolution-config-panel rounded-xl border border-slate-200 bg-white shadow-sm overflow-hidden", children: [
    /* @__PURE__ */ s.jsx("div", { className: "border-b border-slate-200 px-6 py-4", children: /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
        /* @__PURE__ */ s.jsx(vl, { icon: _g, className: "mr-3 text-2xl" }),
        /* @__PURE__ */ s.jsx("h2", { className: "text-xl font-bold text-slate-900", children: "Evolution Configuration" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center gap-2", children: [
        o && /* @__PURE__ */ s.jsx(Re, { tone: "warning", children: "Unsaved Changes" }),
        /* @__PURE__ */ s.jsx(X, { onClick: u, disabled: !o, children: "Save" }),
        /* @__PURE__ */ s.jsx(X, { onClick: f, disabled: !o, variant: "secondary", children: "Discard" }),
        /* @__PURE__ */ s.jsx(X, { onClick: d, variant: "secondary", children: "Reset to Defaults" })
      ] })
    ] }) }),
    /* @__PURE__ */ s.jsxs("div", { className: "flex", children: [
      /* @__PURE__ */ s.jsx("aside", { className: "w-64 border-r border-slate-200 bg-slate-50", children: /* @__PURE__ */ s.jsx("nav", { className: "p-4 space-y-2", children: p.map((m) => /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i(m.id),
          variant: a === m.id ? "primary" : "secondary",
          className: "w-full justify-start gap-3",
          children: [
            /* @__PURE__ */ s.jsx("span", { children: m.icon }),
            /* @__PURE__ */ s.jsx("span", { children: m.label })
          ]
        },
        m.id
      )) }) }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex-1 p-6", children: [
        a === "population" && /* @__PURE__ */ s.jsx(
          he,
          {
            title: "Population Parameters",
            description: "Population size and generation limits for the evolutionary run.",
            children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
              /* @__PURE__ */ s.jsx(H, { label: "Population Size", hint: "Range: 10-1000", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  min: 10,
                  max: 1e3,
                  value: r.populationSize,
                  onChange: (m) => c("populationSize", parseInt(m.target.value, 10) || 10)
                }
              ) }),
              /* @__PURE__ */ s.jsx(H, { label: "Max Generations", hint: "Range: 1-500", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  min: 1,
                  max: 500,
                  value: r.generations,
                  onChange: (m) => c("generations", parseInt(m.target.value, 10) || 1)
                }
              ) }),
              /* @__PURE__ */ s.jsx(H, { label: "Elitism Count", hint: "Range: 0-20", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  min: 0,
                  max: 20,
                  value: r.elitismCount,
                  onChange: (m) => c("elitismCount", parseInt(m.target.value, 10) || 0)
                }
              ) }),
              /* @__PURE__ */ s.jsx(H, { label: "Max Total Iterations", hint: "Range: 1-10000", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  min: 1,
                  max: 1e4,
                  value: r.maxIterations,
                  onChange: (m) => c("maxIterations", parseInt(m.target.value, 10) || 1)
                }
              ) })
            ] })
          }
        ),
        a === "operators" && /* @__PURE__ */ s.jsx(
          he,
          {
            title: "Genetic Operators",
            description: "Mutation and crossover controls for evolution.",
            children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
              /* @__PURE__ */ s.jsx(H, { label: "Mutation Rate", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  step: "0.01",
                  min: 0,
                  max: 1,
                  value: r.mutationRate,
                  onChange: (m) => c("mutationRate", parseFloat(m.target.value) || 0)
                }
              ) }),
              /* @__PURE__ */ s.jsx(H, { label: "Crossover Rate", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  step: "0.01",
                  min: 0,
                  max: 1,
                  value: r.crossoverRate,
                  onChange: (m) => c("crossoverRate", parseFloat(m.target.value) || 0)
                }
              ) }),
              /* @__PURE__ */ s.jsx(H, { label: "Mutation Strength", hint: "Range: 0.0-2.0", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  step: "0.1",
                  min: 0,
                  max: 2,
                  value: r.mutationStrength,
                  onChange: (m) => c("mutationStrength", parseFloat(m.target.value) || 0)
                }
              ) }),
              /* @__PURE__ */ s.jsxs("div", { className: "space-y-3", children: [
                /* @__PURE__ */ s.jsx(
                  Ze,
                  {
                    checked: r.adaptiveMutation,
                    onChange: (m) => c("adaptiveMutation", m),
                    label: "Adaptive Mutation"
                  }
                ),
                /* @__PURE__ */ s.jsx(
                  Ze,
                  {
                    checked: r.adaptiveCrossover,
                    onChange: (m) => c("adaptiveCrossover", m),
                    label: "Adaptive Crossover"
                  }
                )
              ] })
            ] })
          }
        ),
        a === "selection" && /* @__PURE__ */ s.jsxs(
          he,
          {
            title: "Selection Methods",
            description: "Choose how individuals are selected for reproduction.",
            children: [
              /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
                /* @__PURE__ */ s.jsx(H, { label: "Selection Method", children: /* @__PURE__ */ s.jsxs(
                  je,
                  {
                    value: r.selectionMethod,
                    onChange: (m) => c(
                      "selectionMethod",
                      m.target.value
                    ),
                    children: [
                      /* @__PURE__ */ s.jsx("option", { value: "tournament", children: "Tournament Selection" }),
                      /* @__PURE__ */ s.jsx("option", { value: "roulette", children: "Roulette Wheel" }),
                      /* @__PURE__ */ s.jsx("option", { value: "rank", children: "Rank-Based Selection" }),
                      /* @__PURE__ */ s.jsx("option", { value: "steady_state", children: "Steady-State" })
                    ]
                  }
                ) }),
                r.selectionMethod === "tournament" && /* @__PURE__ */ s.jsx(H, { label: "Tournament Size", hint: "Range: 2-10", children: /* @__PURE__ */ s.jsx(
                  D,
                  {
                    type: "number",
                    min: 2,
                    max: 10,
                    value: r.tournamentSize,
                    onChange: (m) => c("tournamentSize", parseInt(m.target.value, 10) || 2)
                  }
                ) }),
                /* @__PURE__ */ s.jsx(H, { label: "Selection Pressure", hint: "Range: 1.0-5.0", children: /* @__PURE__ */ s.jsx(
                  D,
                  {
                    type: "number",
                    step: "0.1",
                    min: 1,
                    max: 5,
                    value: r.selectionPressure,
                    onChange: (m) => c("selectionPressure", parseFloat(m.target.value) || 1)
                  }
                ) })
              ] }),
              /* @__PURE__ */ s.jsxs("div", { className: "mt-6 rounded-lg bg-slate-50 px-4 py-3 text-sm text-slate-600", children: [
                /* @__PURE__ */ s.jsx("div", { className: "font-semibold text-slate-700", children: "Selection Method Guide" }),
                /* @__PURE__ */ s.jsxs("ul", { className: "mt-2 space-y-1", children: [
                  /* @__PURE__ */ s.jsx("li", { children: "- Tournament: fast, good for parallelization." }),
                  /* @__PURE__ */ s.jsx("li", { children: "- Roulette: proportional to fitness." }),
                  /* @__PURE__ */ s.jsx("li", { children: "- Rank: stable selection pressure." }),
                  /* @__PURE__ */ s.jsx("li", { children: "- Steady-State: replaces worst individuals." })
                ] })
              ] })
            ]
          }
        ),
        a === "convergence" && /* @__PURE__ */ s.jsx(
          he,
          {
            title: "Convergence & Termination",
            description: "Stop conditions and constraint handling.",
            children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
              /* @__PURE__ */ s.jsx(H, { label: "Convergence Threshold", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  step: "0.0001",
                  min: 0,
                  max: 1,
                  value: r.convergenceThreshold,
                  onChange: (m) => c("convergenceThreshold", parseFloat(m.target.value) || 0)
                }
              ) }),
              /* @__PURE__ */ s.jsx(H, { label: "Stagnation Limit", hint: "Range: 1-100", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  min: 1,
                  max: 100,
                  value: r.stagnationGenerations,
                  onChange: (m) => c(
                    "stagnationGenerations",
                    parseInt(m.target.value, 10) || 1
                  )
                }
              ) }),
              /* @__PURE__ */ s.jsx(H, { label: "Constraint Handling", children: /* @__PURE__ */ s.jsxs(
                je,
                {
                  value: r.constraintHandling,
                  onChange: (m) => c(
                    "constraintHandling",
                    m.target.value
                  ),
                  children: [
                    /* @__PURE__ */ s.jsx("option", { value: "penalty", children: "Penalty Method" }),
                    /* @__PURE__ */ s.jsx("option", { value: "death", children: "Death Penalty" }),
                    /* @__PURE__ */ s.jsx("option", { value: "repair", children: "Repair Method" })
                  ]
                }
              ) }),
              r.constraintHandling === "penalty" && /* @__PURE__ */ s.jsx(H, { label: "Penalty Factor", hint: "Range: 1-10000", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  min: 1,
                  max: 1e4,
                  value: r.penaltyFactor,
                  onChange: (m) => c("penaltyFactor", parseInt(m.target.value, 10) || 1)
                }
              ) })
            ] })
          }
        ),
        a === "diversity" && /* @__PURE__ */ s.jsx(
          he,
          {
            title: "Diversity Management",
            description: "Maintain population diversity to avoid premature convergence.",
            children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
              /* @__PURE__ */ s.jsx(H, { label: "Diversity Threshold", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  step: "0.05",
                  min: 0,
                  max: 1,
                  value: r.diversityThreshold,
                  onChange: (m) => c("diversityThreshold", parseFloat(m.target.value) || 0)
                }
              ) }),
              /* @__PURE__ */ s.jsx(H, { label: "Crowding Distance", hint: "Range: 0.0-2.0", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  step: "0.1",
                  min: 0,
                  max: 2,
                  value: r.crowdingDistance,
                  onChange: (m) => c("crowdingDistance", parseFloat(m.target.value) || 0)
                }
              ) }),
              /* @__PURE__ */ s.jsx(
                Ze,
                {
                  checked: r.nichingEnabled,
                  onChange: (m) => c("nichingEnabled", m),
                  label: "Enable Niching"
                }
              )
            ] })
          }
        ),
        a === "advanced" && /* @__PURE__ */ s.jsx(
          he,
          {
            title: "Advanced Settings",
            description: "Multi-objective optimization and parallelization options.",
            children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
              /* @__PURE__ */ s.jsx(
                Ze,
                {
                  checked: r.multiObjectiveOptimization,
                  onChange: (m) => c("multiObjectiveOptimization", m),
                  label: "Multi-Objective Optimization"
                }
              ),
              r.multiObjectiveOptimization && /* @__PURE__ */ s.jsx(H, { label: "Pareto Front Size", hint: "Range: 10-200", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  min: 10,
                  max: 200,
                  value: r.paretoFrontSize,
                  onChange: (m) => c("paretoFrontSize", parseInt(m.target.value, 10) || 10)
                }
              ) }),
              /* @__PURE__ */ s.jsx(
                Ze,
                {
                  checked: r.parallelEvaluation,
                  onChange: (m) => c("parallelEvaluation", m),
                  label: "Parallel Evaluation"
                }
              ),
              r.parallelEvaluation && /* @__PURE__ */ s.jsx(H, { label: "Evaluation Batch Size", hint: "Range: 1-100", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  min: 1,
                  max: 100,
                  value: r.evaluationBatchSize,
                  onChange: (m) => c(
                    "evaluationBatchSize",
                    parseInt(m.target.value, 10) || 1
                  )
                }
              ) }),
              /* @__PURE__ */ s.jsx(
                Ze,
                {
                  checked: r.asyncEvaluation,
                  onChange: (m) => c("asyncEvaluation", m),
                  label: "Async Evaluation"
                }
              )
            ] })
          }
        )
      ] })
    ] })
  ] });
}, Sg = () => /* @__PURE__ */ s.jsx("span", { children: "???" }), RA = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), IA = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), DA = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), $A = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), LA = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), FA = {
  attackStrategy: "pgd",
  attackStrength: 0.1,
  stepSize: 0.01,
  numSteps: 40,
  targetedAttack: !1,
  targetConfidence: 0.9,
  randomTargets: !1,
  redTeamSize: 5,
  redTeamStrategy: "coordinated",
  redTeamCommunication: !0,
  blueTeamSize: 3,
  blueTeamStrategy: "adaptive",
  blueTeamLearning: !0,
  adversarialTraining: !0,
  inputSanitization: !0,
  outputValidation: !0,
  anomalyDetection: !0,
  defenseDiversity: 3,
  maxRounds: 10,
  roundTimeout: 300,
  victoryCondition: "score",
  victoryThreshold: 0.8,
  successRateThreshold: 0.7,
  robustnessScore: 0.6,
  coverageTarget: 0.9,
  transferAttack: !1,
  ensembleAttack: !1,
  queryEfficiency: !1,
  maxQueries: 1e3,
  contentType: "all"
}, qA = ({
  config: e,
  onConfigChange: t
}) => {
  const [r, n] = de(e), [a, i] = de("attacks"), [o, l] = de(!1);
  et(() => {
    n(e), l(!1);
  }, [e]);
  const c = (m, y) => {
    const h = { ...r, [m]: y };
    n(h), l(!0);
  }, u = () => {
    try {
      t(r), l(!1), F.success("Adversarial configuration saved successfully");
    } catch (m) {
      F.error(`Failed to save configuration: ${m instanceof Error ? m.message : String(m)}`);
    }
  }, d = () => {
    window.confirm("Are you sure you want to reset to default configuration?") && (n(FA), l(!0), F.info("Configuration reset to defaults. Click Save to apply."));
  }, f = () => {
    n(e), l(!1), F.info("Changes discarded");
  }, p = [
    { id: "attacks", label: "Attack Strategies", icon: /* @__PURE__ */ s.jsx(RA, {}) },
    { id: "redteam", label: "Red Team", icon: /* @__PURE__ */ s.jsx(IA, {}) },
    { id: "blueteam", label: "Blue Team", icon: /* @__PURE__ */ s.jsx(Sg, {}) },
    { id: "defenses", label: "Defenses", icon: /* @__PURE__ */ s.jsx(LA, {}) },
    { id: "tournament", label: "Tournament", icon: /* @__PURE__ */ s.jsx(DA, {}) },
    { id: "advanced", label: "Advanced", icon: /* @__PURE__ */ s.jsx($A, {}) }
  ];
  return /* @__PURE__ */ s.jsxs("div", { className: "adversarial-config-panel rounded-xl border border-slate-200 bg-white shadow-sm overflow-hidden", children: [
    /* @__PURE__ */ s.jsx("div", { className: "border-b border-slate-200 px-6 py-4", children: /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
        /* @__PURE__ */ s.jsx(vl, { icon: Sg, className: "mr-3 text-2xl" }),
        /* @__PURE__ */ s.jsx("h2", { className: "text-xl font-bold text-slate-900", children: "Adversarial Configuration" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center gap-2", children: [
        o && /* @__PURE__ */ s.jsx(Re, { tone: "warning", children: "Unsaved Changes" }),
        /* @__PURE__ */ s.jsx(X, { onClick: u, disabled: !o, children: "Save" }),
        /* @__PURE__ */ s.jsx(X, { onClick: f, disabled: !o, variant: "secondary", children: "Discard" }),
        /* @__PURE__ */ s.jsx(X, { onClick: d, variant: "secondary", children: "Reset to Defaults" })
      ] })
    ] }) }),
    /* @__PURE__ */ s.jsxs("div", { className: "flex", children: [
      /* @__PURE__ */ s.jsx("aside", { className: "w-64 border-r border-slate-200 bg-slate-50", children: /* @__PURE__ */ s.jsx("nav", { className: "p-4 space-y-2", children: p.map((m) => /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i(m.id),
          variant: a === m.id ? "primary" : "secondary",
          className: "w-full justify-start gap-3",
          children: [
            /* @__PURE__ */ s.jsx("span", { children: m.icon }),
            /* @__PURE__ */ s.jsx("span", { children: m.label })
          ]
        },
        m.id
      )) }) }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex-1 p-6", children: [
        a === "attacks" && /* @__PURE__ */ s.jsx(he, { title: "Attack Configuration", description: "Configure adversarial strategies and target settings.", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
          /* @__PURE__ */ s.jsx(H, { label: "Attack Strategy", children: /* @__PURE__ */ s.jsxs(
            je,
            {
              value: r.attackStrategy,
              onChange: (m) => c("attackStrategy", m.target.value),
              children: [
                /* @__PURE__ */ s.jsx("option", { value: "pgd", children: "Projected Gradient Descent (PGD)" }),
                /* @__PURE__ */ s.jsx("option", { value: "fgsm", children: "Fast Gradient Sign Method (FGSM)" }),
                /* @__PURE__ */ s.jsx("option", { value: "cw", children: "Carlini and Wagner (C&W)" }),
                /* @__PURE__ */ s.jsx("option", { value: "deepfool", children: "DeepFool" }),
                /* @__PURE__ */ s.jsx("option", { value: "boundary", children: "Boundary Attack" }),
                /* @__PURE__ */ s.jsx("option", { value: "genetic", children: "Genetic Algorithm Attack" })
              ]
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Content Type", children: /* @__PURE__ */ s.jsxs(
            je,
            {
              value: r.contentType,
              onChange: (m) => c("contentType", m.target.value),
              children: [
                /* @__PURE__ */ s.jsx("option", { value: "all", children: "All Types" }),
                /* @__PURE__ */ s.jsx("option", { value: "code", children: "Code" }),
                /* @__PURE__ */ s.jsx("option", { value: "text", children: "Text" }),
                /* @__PURE__ */ s.jsx("option", { value: "design", children: "Design" }),
                /* @__PURE__ */ s.jsx("option", { value: "strategy", children: "Strategy" })
              ]
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Attack Strength", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              step: "0.01",
              min: 0,
              max: 1,
              value: r.attackStrength,
              onChange: (m) => c("attackStrength", parseFloat(m.target.value) || 0)
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Step Size", hint: "Range: 0.001-0.1", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              step: "0.001",
              min: 1e-3,
              max: 0.1,
              value: r.stepSize,
              onChange: (m) => c("stepSize", parseFloat(m.target.value) || 1e-3)
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Number of Steps", hint: "Range: 1-200", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              min: 1,
              max: 200,
              value: r.numSteps,
              onChange: (m) => c("numSteps", parseInt(m.target.value, 10) || 1)
            }
          ) }),
          /* @__PURE__ */ s.jsxs("div", { className: "space-y-3", children: [
            /* @__PURE__ */ s.jsx(
              Ze,
              {
                checked: r.targetedAttack,
                onChange: (m) => c("targetedAttack", m),
                label: "Targeted Attack"
              }
            ),
            r.targetedAttack && /* @__PURE__ */ s.jsxs(s.Fragment, { children: [
              /* @__PURE__ */ s.jsx(H, { label: "Target Confidence", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  type: "number",
                  step: "0.05",
                  min: 0,
                  max: 1,
                  value: r.targetConfidence,
                  onChange: (m) => c("targetConfidence", parseFloat(m.target.value) || 0)
                }
              ) }),
              /* @__PURE__ */ s.jsx(
                Ze,
                {
                  checked: r.randomTargets,
                  onChange: (m) => c("randomTargets", m),
                  label: "Random Targets"
                }
              )
            ] })
          ] })
        ] }) }),
        a === "redteam" && /* @__PURE__ */ s.jsx(he, { title: "Red Team", description: "Configure offensive team composition and coordination.", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
          /* @__PURE__ */ s.jsx(H, { label: "Red Team Size", hint: "Range: 1-20", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              min: 1,
              max: 20,
              value: r.redTeamSize,
              onChange: (m) => c("redTeamSize", parseInt(m.target.value, 10) || 1)
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Red Team Strategy", children: /* @__PURE__ */ s.jsxs(
            je,
            {
              value: r.redTeamStrategy,
              onChange: (m) => c("redTeamStrategy", m.target.value),
              children: [
                /* @__PURE__ */ s.jsx("option", { value: "coordinated", children: "Coordinated" }),
                /* @__PURE__ */ s.jsx("option", { value: "independent", children: "Independent" }),
                /* @__PURE__ */ s.jsx("option", { value: "competitive", children: "Competitive" })
              ]
            }
          ) }),
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.redTeamCommunication,
              onChange: (m) => c("redTeamCommunication", m),
              label: "Red Team Communication"
            }
          )
        ] }) }),
        a === "blueteam" && /* @__PURE__ */ s.jsx(he, { title: "Blue Team", description: "Configure defensive team strategy and learning.", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
          /* @__PURE__ */ s.jsx(H, { label: "Blue Team Size", hint: "Range: 1-20", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              min: 1,
              max: 20,
              value: r.blueTeamSize,
              onChange: (m) => c("blueTeamSize", parseInt(m.target.value, 10) || 1)
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Blue Team Strategy", children: /* @__PURE__ */ s.jsxs(
            je,
            {
              value: r.blueTeamStrategy,
              onChange: (m) => c("blueTeamStrategy", m.target.value),
              children: [
                /* @__PURE__ */ s.jsx("option", { value: "static", children: "Static" }),
                /* @__PURE__ */ s.jsx("option", { value: "adaptive", children: "Adaptive" }),
                /* @__PURE__ */ s.jsx("option", { value: "proactive", children: "Proactive" })
              ]
            }
          ) }),
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.blueTeamLearning,
              onChange: (m) => c("blueTeamLearning", m),
              label: "Blue Team Learning"
            }
          )
        ] }) }),
        a === "defenses" && /* @__PURE__ */ s.jsx(he, { title: "Defense Mechanisms", description: "Enable and tune defense layers.", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.adversarialTraining,
              onChange: (m) => c("adversarialTraining", m),
              label: "Adversarial Training"
            }
          ),
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.inputSanitization,
              onChange: (m) => c("inputSanitization", m),
              label: "Input Sanitization"
            }
          ),
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.outputValidation,
              onChange: (m) => c("outputValidation", m),
              label: "Output Validation"
            }
          ),
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.anomalyDetection,
              onChange: (m) => c("anomalyDetection", m),
              label: "Anomaly Detection"
            }
          ),
          /* @__PURE__ */ s.jsx(H, { label: "Defense Diversity", hint: "Range: 1-10", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              min: 1,
              max: 10,
              value: r.defenseDiversity,
              onChange: (m) => c("defenseDiversity", parseInt(m.target.value, 10) || 1)
            }
          ) })
        ] }) }),
        a === "tournament" && /* @__PURE__ */ s.jsx(he, { title: "Tournament Settings", description: "Configure red/blue team evaluation rounds.", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
          /* @__PURE__ */ s.jsx(H, { label: "Max Rounds", hint: "Range: 1-100", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              min: 1,
              max: 100,
              value: r.maxRounds,
              onChange: (m) => c("maxRounds", parseInt(m.target.value, 10) || 1)
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Round Timeout (seconds)", hint: "Range: 60-3600", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              min: 60,
              max: 3600,
              value: r.roundTimeout,
              onChange: (m) => c("roundTimeout", parseInt(m.target.value, 10) || 60)
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Victory Condition", children: /* @__PURE__ */ s.jsxs(
            je,
            {
              value: r.victoryCondition,
              onChange: (m) => c("victoryCondition", m.target.value),
              children: [
                /* @__PURE__ */ s.jsx("option", { value: "score", children: "Score-Based" }),
                /* @__PURE__ */ s.jsx("option", { value: "survival", children: "Survival" }),
                /* @__PURE__ */ s.jsx("option", { value: "objective", children: "Objective Completion" })
              ]
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Victory Threshold", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              step: "0.05",
              min: 0,
              max: 1,
              value: r.victoryThreshold,
              onChange: (m) => c("victoryThreshold", parseFloat(m.target.value) || 0)
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Success Rate Threshold", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              step: "0.05",
              min: 0,
              max: 1,
              value: r.successRateThreshold,
              onChange: (m) => c("successRateThreshold", parseFloat(m.target.value) || 0)
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Robustness Score", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              step: "0.05",
              min: 0,
              max: 1,
              value: r.robustnessScore,
              onChange: (m) => c("robustnessScore", parseFloat(m.target.value) || 0)
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Coverage Target", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              step: "0.05",
              min: 0,
              max: 1,
              value: r.coverageTarget,
              onChange: (m) => c("coverageTarget", parseFloat(m.target.value) || 0)
            }
          ) })
        ] }) }),
        a === "advanced" && /* @__PURE__ */ s.jsx(he, { title: "Advanced Settings", description: "Optimization and efficiency options.", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.transferAttack,
              onChange: (m) => c("transferAttack", m),
              label: "Transfer Attack"
            }
          ),
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.ensembleAttack,
              onChange: (m) => c("ensembleAttack", m),
              label: "Ensemble Attack"
            }
          ),
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.queryEfficiency,
              onChange: (m) => c("queryEfficiency", m),
              label: "Query Efficiency Mode"
            }
          ),
          r.queryEfficiency && /* @__PURE__ */ s.jsx(H, { label: "Max Queries", hint: "Range: 1-10000", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              min: 1,
              max: 1e4,
              value: r.maxQueries,
              onChange: (m) => c("maxQueries", parseInt(m.target.value, 10) || 1)
            }
          ) })
        ] }) })
      ] })
    ] })
  ] });
}, kg = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), UA = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), zA = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), BA = () => /* @__PURE__ */ s.jsx("span", { children: "?" }), WA = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), VA = () => /* @__PURE__ */ s.jsx("span", { children: "??" }), HA = {
  strategy: "hierarchical",
  maxDepth: 5,
  maxSubProblems: 20,
  granularity: "medium",
  minSubProblemSize: 50,
  maxSubProblemSize: 500,
  targetSubProblemSize: 200,
  parallelDecomposition: !0,
  maxParallelTasks: 5,
  asyncDecomposition: !1,
  pruningEnabled: !0,
  pruningThreshold: 0.1,
  similarityThreshold: 0.8,
  mergeThreshold: 0.9,
  semanticSimilarity: "cosine",
  embeddingModel: "text-embedding-ada-002",
  clusteringAlgorithm: "hierarchical",
  dependencyDetection: "hybrid",
  circularDependencyHandling: "merge",
  dependencyVisualization: !1,
  timeHorizon: 24,
  timeGranularity: "hours",
  temporalDependencies: !0,
  cohesivenessTarget: 0.7,
  couplingLimit: 0.3,
  complexityThreshold: 0.8,
  validateDecomposition: !0,
  testDecomposition: !1,
  feedbackLoop: !0,
  adaptiveDecomposition: !1,
  learningEnabled: !1,
  historicalData: !1
}, GA = ({
  config: e,
  onConfigChange: t
}) => {
  const [r, n] = de(e), [a, i] = de("strategy"), [o, l] = de(!1);
  et(() => {
    n(e), l(!1);
  }, [e]);
  const c = (m, y) => {
    const h = { ...r, [m]: y };
    n(h), l(!0);
  }, u = () => {
    try {
      t(r), l(!1), F.success("Decomposition configuration saved successfully");
    } catch (m) {
      F.error(`Failed to save configuration: ${m instanceof Error ? m.message : String(m)}`);
    }
  }, d = () => {
    window.confirm("Are you sure you want to reset to default configuration?") && (n(HA), l(!0), F.info("Configuration reset to defaults. Click Save to apply."));
  }, f = () => {
    n(e), l(!1), F.info("Changes discarded");
  }, p = [
    { id: "strategy", label: "Strategy", icon: /* @__PURE__ */ s.jsx(kg, {}) },
    { id: "constraints", label: "Constraints", icon: /* @__PURE__ */ s.jsx(zA, {}) },
    { id: "parallel", label: "Parallel", icon: /* @__PURE__ */ s.jsx(BA, {}) },
    { id: "pruning", label: "Optimization", icon: /* @__PURE__ */ s.jsx(VA, {}) },
    { id: "quality", label: "Quality", icon: /* @__PURE__ */ s.jsx(WA, {}) },
    { id: "advanced", label: "Advanced", icon: /* @__PURE__ */ s.jsx(UA, {}) }
  ];
  return /* @__PURE__ */ s.jsxs("div", { className: "decomposition-config-panel rounded-xl border border-slate-200 bg-white shadow-sm overflow-hidden", children: [
    /* @__PURE__ */ s.jsx("div", { className: "border-b border-slate-200 px-6 py-4", children: /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
        /* @__PURE__ */ s.jsx(vl, { icon: kg, className: "mr-3 text-2xl" }),
        /* @__PURE__ */ s.jsx("h2", { className: "text-xl font-bold text-slate-900", children: "Decomposition Configuration" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center gap-2", children: [
        o && /* @__PURE__ */ s.jsx(Re, { tone: "warning", children: "Unsaved Changes" }),
        /* @__PURE__ */ s.jsx(X, { onClick: u, disabled: !o, children: "Save" }),
        /* @__PURE__ */ s.jsx(X, { onClick: f, disabled: !o, variant: "secondary", children: "Discard" }),
        /* @__PURE__ */ s.jsx(X, { onClick: d, variant: "secondary", children: "Reset to Defaults" })
      ] })
    ] }) }),
    /* @__PURE__ */ s.jsxs("div", { className: "flex", children: [
      /* @__PURE__ */ s.jsx("aside", { className: "w-64 border-r border-slate-200 bg-slate-50", children: /* @__PURE__ */ s.jsx("nav", { className: "p-4 space-y-2", children: p.map((m) => /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i(m.id),
          variant: a === m.id ? "primary" : "secondary",
          className: "w-full justify-start gap-3",
          children: [
            /* @__PURE__ */ s.jsx("span", { children: m.icon }),
            /* @__PURE__ */ s.jsx("span", { children: m.label })
          ]
        },
        m.id
      )) }) }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex-1 p-6", children: [
        a === "strategy" && /* @__PURE__ */ s.jsxs(he, { title: "Strategy", description: "Choose the primary decomposition approach.", children: [
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Decomposition Strategy", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                value: r.strategy,
                onChange: (m) => c("strategy", m.target.value),
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "hierarchical", children: "Hierarchical" }),
                  /* @__PURE__ */ s.jsx("option", { value: "semantic", children: "Semantic" }),
                  /* @__PURE__ */ s.jsx("option", { value: "dependency_based", children: "Dependency-Based" }),
                  /* @__PURE__ */ s.jsx("option", { value: "temporal", children: "Temporal" }),
                  /* @__PURE__ */ s.jsx("option", { value: "hybrid", children: "Hybrid" })
                ]
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Granularity", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                value: r.granularity,
                onChange: (m) => c("granularity", m.target.value),
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "fine", children: "Fine" }),
                  /* @__PURE__ */ s.jsx("option", { value: "medium", children: "Medium" }),
                  /* @__PURE__ */ s.jsx("option", { value: "coarse", children: "Coarse" })
                ]
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Max Depth", hint: "Range: 1-20", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                min: 1,
                max: 20,
                value: r.maxDepth,
                onChange: (m) => c("maxDepth", parseInt(m.target.value, 10) || 1)
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Max Sub-Problems", hint: "Range: 2-100", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                min: 2,
                max: 100,
                value: r.maxSubProblems,
                onChange: (m) => c("maxSubProblems", parseInt(m.target.value, 10) || 2)
              }
            ) })
          ] }),
          r.strategy === "semantic" && /* @__PURE__ */ s.jsxs("div", { className: "mt-6 grid grid-cols-1 md:grid-cols-2 gap-6", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Similarity Metric", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                value: r.semanticSimilarity,
                onChange: (m) => c("semanticSimilarity", m.target.value),
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "cosine", children: "Cosine" }),
                  /* @__PURE__ */ s.jsx("option", { value: "jaccard", children: "Jaccard" }),
                  /* @__PURE__ */ s.jsx("option", { value: "euclidean", children: "Euclidean" }),
                  /* @__PURE__ */ s.jsx("option", { value: "manhattan", children: "Manhattan" })
                ]
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Embedding Model", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "text",
                value: r.embeddingModel,
                onChange: (m) => c("embeddingModel", m.target.value)
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Clustering Algorithm", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                value: r.clusteringAlgorithm,
                onChange: (m) => c("clusteringAlgorithm", m.target.value),
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "kmeans", children: "K-Means" }),
                  /* @__PURE__ */ s.jsx("option", { value: "dbscan", children: "DBSCAN" }),
                  /* @__PURE__ */ s.jsx("option", { value: "hierarchical", children: "Hierarchical" }),
                  /* @__PURE__ */ s.jsx("option", { value: "spectral", children: "Spectral" })
                ]
              }
            ) })
          ] }),
          r.strategy === "dependency_based" && /* @__PURE__ */ s.jsxs("div", { className: "mt-6 grid grid-cols-1 md:grid-cols-2 gap-6", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Dependency Detection", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                value: r.dependencyDetection,
                onChange: (m) => c("dependencyDetection", m.target.value),
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "static", children: "Static" }),
                  /* @__PURE__ */ s.jsx("option", { value: "dynamic", children: "Dynamic" }),
                  /* @__PURE__ */ s.jsx("option", { value: "hybrid", children: "Hybrid" })
                ]
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Circular Dependencies", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                value: r.circularDependencyHandling,
                onChange: (m) => c("circularDependencyHandling", m.target.value),
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "break", children: "Break" }),
                  /* @__PURE__ */ s.jsx("option", { value: "merge", children: "Merge" }),
                  /* @__PURE__ */ s.jsx("option", { value: "error", children: "Error" })
                ]
              }
            ) }),
            /* @__PURE__ */ s.jsx(
              Ze,
              {
                checked: r.dependencyVisualization,
                onChange: (m) => c("dependencyVisualization", m),
                label: "Dependency Visualization"
              }
            )
          ] }),
          r.strategy === "temporal" && /* @__PURE__ */ s.jsxs("div", { className: "mt-6 grid grid-cols-1 md:grid-cols-2 gap-6", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Time Horizon", hint: "Hours or units based on granularity", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                min: 1,
                value: r.timeHorizon,
                onChange: (m) => c("timeHorizon", parseInt(m.target.value, 10) || 1)
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Time Granularity", children: /* @__PURE__ */ s.jsxs(
              je,
              {
                value: r.timeGranularity,
                onChange: (m) => c("timeGranularity", m.target.value),
                children: [
                  /* @__PURE__ */ s.jsx("option", { value: "seconds", children: "Seconds" }),
                  /* @__PURE__ */ s.jsx("option", { value: "minutes", children: "Minutes" }),
                  /* @__PURE__ */ s.jsx("option", { value: "hours", children: "Hours" }),
                  /* @__PURE__ */ s.jsx("option", { value: "days", children: "Days" })
                ]
              }
            ) }),
            /* @__PURE__ */ s.jsx(
              Ze,
              {
                checked: r.temporalDependencies,
                onChange: (m) => c("temporalDependencies", m),
                label: "Temporal Dependencies"
              }
            )
          ] })
        ] }),
        a === "constraints" && /* @__PURE__ */ s.jsx(he, { title: "Size Constraints", description: "Define size limits for sub-problems.", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
          /* @__PURE__ */ s.jsx(H, { label: "Min Sub-Problem Size", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              min: 1,
              value: r.minSubProblemSize,
              onChange: (m) => c("minSubProblemSize", parseInt(m.target.value, 10) || 1)
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Max Sub-Problem Size", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              min: 1,
              value: r.maxSubProblemSize,
              onChange: (m) => c("maxSubProblemSize", parseInt(m.target.value, 10) || 1)
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Target Sub-Problem Size", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              min: 1,
              value: r.targetSubProblemSize,
              onChange: (m) => c("targetSubProblemSize", parseInt(m.target.value, 10) || 1)
            }
          ) })
        ] }) }),
        a === "parallel" && /* @__PURE__ */ s.jsx(he, { title: "Parallel Processing", description: "Scale decomposition across workers.", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.parallelDecomposition,
              onChange: (m) => c("parallelDecomposition", m),
              label: "Parallel Decomposition"
            }
          ),
          /* @__PURE__ */ s.jsx(H, { label: "Max Parallel Tasks", hint: "Range: 1-20", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              min: 1,
              max: 20,
              value: r.maxParallelTasks,
              onChange: (m) => c("maxParallelTasks", parseInt(m.target.value, 10) || 1)
            }
          ) }),
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.asyncDecomposition,
              onChange: (m) => c("asyncDecomposition", m),
              label: "Async Decomposition"
            }
          )
        ] }) }),
        a === "pruning" && /* @__PURE__ */ s.jsx(he, { title: "Optimization & Pruning", description: "Reduce complexity with pruning rules.", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.pruningEnabled,
              onChange: (m) => c("pruningEnabled", m),
              label: "Enable Pruning"
            }
          ),
          r.pruningEnabled && /* @__PURE__ */ s.jsxs(s.Fragment, { children: [
            /* @__PURE__ */ s.jsx(H, { label: "Pruning Threshold", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                step: "0.05",
                min: 0,
                max: 1,
                value: r.pruningThreshold,
                onChange: (m) => c("pruningThreshold", parseFloat(m.target.value) || 0)
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Similarity Threshold", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                step: "0.05",
                min: 0,
                max: 1,
                value: r.similarityThreshold,
                onChange: (m) => c("similarityThreshold", parseFloat(m.target.value) || 0)
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Merge Threshold", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "number",
                step: "0.05",
                min: 0,
                max: 1,
                value: r.mergeThreshold,
                onChange: (m) => c("mergeThreshold", parseFloat(m.target.value) || 0)
              }
            ) })
          ] })
        ] }) }),
        a === "quality" && /* @__PURE__ */ s.jsx(he, { title: "Quality Metrics", description: "Set targets and validation options.", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
          /* @__PURE__ */ s.jsx(H, { label: "Cohesiveness Target", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              step: "0.05",
              min: 0,
              max: 1,
              value: r.cohesivenessTarget,
              onChange: (m) => c("cohesivenessTarget", parseFloat(m.target.value) || 0)
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Coupling Limit", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              step: "0.05",
              min: 0,
              max: 1,
              value: r.couplingLimit,
              onChange: (m) => c("couplingLimit", parseFloat(m.target.value) || 0)
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Complexity Threshold", hint: "Range: 0.0-1.0", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              step: "0.05",
              min: 0,
              max: 1,
              value: r.complexityThreshold,
              onChange: (m) => c("complexityThreshold", parseFloat(m.target.value) || 0)
            }
          ) }),
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.validateDecomposition,
              onChange: (m) => c("validateDecomposition", m),
              label: "Validate Decomposition"
            }
          ),
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.testDecomposition,
              onChange: (m) => c("testDecomposition", m),
              label: "Test Decomposition"
            }
          ),
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.feedbackLoop,
              onChange: (m) => c("feedbackLoop", m),
              label: "Feedback Loop"
            }
          )
        ] }) }),
        a === "advanced" && /* @__PURE__ */ s.jsx(he, { title: "Advanced Features", description: "Experimental and learning-based options.", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.adaptiveDecomposition,
              onChange: (m) => c("adaptiveDecomposition", m),
              label: "Adaptive Decomposition"
            }
          ),
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.learningEnabled,
              onChange: (m) => c("learningEnabled", m),
              label: "Machine Learning"
            }
          ),
          /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: r.historicalData,
              onChange: (m) => c("historicalData", m),
              label: "Use Historical Data"
            }
          )
        ] }) })
      ] })
    ] })
  ] });
}, Eg = () => /* @__PURE__ */ s.jsx("span", { children: "🌐" }), KA = () => /* @__PURE__ */ s.jsx("span", { children: "🔑" }), YA = () => /* @__PURE__ */ s.jsx("span", { children: "🖥️" }), XA = () => /* @__PURE__ */ s.jsx("span", { children: "⏱️" }), QA = () => /* @__PURE__ */ s.jsx("span", { children: "⚙️" }), JA = () => /* @__PURE__ */ s.jsx("span", { children: "⚡" }), ZA = {
  leanAideApiEndpoint: "http://localhost:8000/api/v1",
  crewaiApiEndpoint: "http://localhost:8001/api/v1",
  bubbleLabsApiEndpoint: "http://localhost:8002/api/v1",
  researchQuestApiEndpoint: "http://localhost:8003/api/v1",
  defaultTimeout: 3e4,
  leanAideTimeout: 6e4,
  crewaiTimeout: 45e3,
  bubbleLabsTimeout: 3e4,
  researchQuestTimeout: 3e4,
  useAuthentication: !0,
  authType: "api_key",
  apiKey: "",
  oauth2ClientId: "",
  oauth2ClientSecret: "",
  oauth2Scope: "",
  jwtToken: "",
  basicUsername: "",
  basicPassword: "",
  enableWebSocket: !1,
  websocketUrl: "ws://localhost:8080/ws",
  websocketReconnectInterval: 5e3,
  websocketMaxReconnectAttempts: 10,
  websocketHeartbeatInterval: 3e4,
  enableRetry: !0,
  maxRetries: 3,
  retryDelay: 1e3,
  retryBackoffMultiplier: 2,
  retryOnStatusCodes: [408, 429, 500, 502, 503, 504],
  enableRateLimiting: !0,
  maxRequestsPerSecond: 10,
  maxRequestsPerMinute: 500,
  rateLimitBurstSize: 20,
  enableCircuitBreaker: !0,
  circuitBreakerThreshold: 5,
  circuitBreakerTimeout: 6e4,
  circuitBreakerHalfOpenAttempts: 3,
  enableCaching: !0,
  cacheTtl: 3e5,
  cacheSize: 1e3,
  cacheStrategy: "lru",
  enableLogging: !0,
  logLevel: "info",
  logRequests: !0,
  logResponses: !1,
  logErrors: !0,
  enableHealthChecks: !0,
  healthCheckInterval: 3e4,
  healthCheckTimeout: 5e3,
  healthCheckUnhealthyThreshold: 3,
  leanAide: {
    provider: "anthropic",
    model: "claude-3-sonnet",
    temperature: 0.7,
    maxTokens: 4096
  },
  crewai: {
    parallelExecution: !0,
    maxParallelTasks: 5,
    delegationTimeout: 12e4
  },
  bubbleLabs: {
    uiEnabled: !0,
    realTimeUpdates: !0,
    visualizationQuality: "medium"
  },
  researchQuest: {
    knowledgeGraphEnabled: !0,
    semanticSearch: !0,
    maxResults: 10
  }
}, e1 = ({
  config: e,
  onConfigChange: t
}) => {
  const [r, n] = de(e), [a, i] = de("endpoints"), [o, l] = de(!1), [c, u] = de(!1);
  et(() => {
    n(e), l(!1);
  }, [e]);
  const d = (v, b) => {
    const w = { ...r, [v]: b };
    n(w), l(!0);
  }, f = (v, b, w) => {
    const j = {
      ...r,
      [v]: {
        ...r[v],
        [b]: w
      }
    };
    n(j), l(!0);
  }, p = () => {
    try {
      t(r), l(!1), F.success("Integration configuration saved successfully");
    } catch (v) {
      F.error(`Failed to save configuration: ${v instanceof Error ? v.message : String(v)}`);
    }
  }, m = () => {
    window.confirm("Are you sure you want to reset to default configuration?") && (n(ZA), l(!0), F.info("Configuration reset to defaults. Click Save to apply."));
  }, y = () => {
    n(e), l(!1), F.info("Changes discarded");
  }, h = (v) => !c && v ? "••••••••••••" : v, g = [
    { id: "endpoints", label: "API Endpoints", icon: /* @__PURE__ */ s.jsx(YA, {}) },
    { id: "authentication", label: "Authentication", icon: /* @__PURE__ */ s.jsx(KA, {}) },
    { id: "timeouts", label: "Timeouts & Limits", icon: /* @__PURE__ */ s.jsx(XA, {}) },
    { id: "websocket", label: "WebSocket", icon: /* @__PURE__ */ s.jsx(JA, {}) },
    { id: "retry", label: "Retry & Circuit Breaker", icon: /* @__PURE__ */ s.jsx(QA, {}) },
    { id: "services", label: "Service Settings", icon: /* @__PURE__ */ s.jsx(Eg, {}) }
  ];
  return /* @__PURE__ */ s.jsxs("div", { className: "integration-config-panel bg-white dark:bg-gray-800 rounded-lg shadow-lg overflow-hidden", children: [
    /* @__PURE__ */ s.jsx("div", { className: "border-b border-gray-200 dark:border-gray-700 px-6 py-4", children: /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center", children: [
        /* @__PURE__ */ s.jsx(vl, { icon: Eg, className: "mr-3 text-2xl" }),
        /* @__PURE__ */ s.jsx("h2", { className: "text-xl font-bold text-gray-900 dark:text-white", children: "Integration Configuration" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center space-x-2", children: [
        o && /* @__PURE__ */ s.jsx(Re, { tone: "warning", children: "Unsaved Changes" }),
        /* @__PURE__ */ s.jsxs(X, { onClick: () => u(!c), variant: "secondary", children: [
          c ? "?? Hide" : "??? Show",
          " Secrets"
        ] }),
        /* @__PURE__ */ s.jsx(X, { onClick: p, disabled: !o, children: "Save" }),
        /* @__PURE__ */ s.jsx(X, { onClick: y, disabled: !o, variant: "secondary", children: "Discard" }),
        /* @__PURE__ */ s.jsx(X, { onClick: m, variant: "secondary", children: "Reset to Defaults" })
      ] })
    ] }) }),
    /* @__PURE__ */ s.jsxs("div", { className: "flex", children: [
      /* @__PURE__ */ s.jsx("div", { className: "w-64 bg-gray-50 dark:bg-gray-900 border-r border-gray-200 dark:border-gray-700", children: /* @__PURE__ */ s.jsx("nav", { className: "p-4 space-y-2", children: g.map((v) => /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => i(v.id),
          variant: a === v.id ? "primary" : "secondary",
          className: "w-full justify-start gap-3",
          children: [
            /* @__PURE__ */ s.jsx("span", { className: "mr-3", children: v.icon }),
            /* @__PURE__ */ s.jsx("span", { className: "font-medium", children: v.label })
          ]
        },
        v.id
      )) }) }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex-1 p-6 overflow-y-auto max-h-[calc(100vh-200px)]", children: [
        a === "endpoints" && /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-2", children: "API Endpoints" }),
            /* @__PURE__ */ s.jsx("p", { className: "text-sm text-gray-600 dark:text-gray-400 mb-4", children: "Configure the base URLs for all integrated services." })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 gap-6", children: [
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "leanAideApiEndpoint", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "LeanAide API Endpoint" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "leanAideApiEndpoint",
                  type: "url",
                  placeholder: "http://localhost:8000/api/v1",
                  value: r.leanAideApiEndpoint,
                  onChange: (v) => d("leanAideApiEndpoint", v.target.value),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              ),
              /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Base URL for LeanAide mathematical reasoning service" })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "crewaiApiEndpoint", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "CrewAI API Endpoint" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "crewaiApiEndpoint",
                  type: "url",
                  placeholder: "http://localhost:8001/api/v1",
                  value: r.crewaiApiEndpoint,
                  onChange: (v) => d("crewaiApiEndpoint", v.target.value),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              ),
              /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Base URL for CrewAI task delegation service" })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "bubbleLabsApiEndpoint", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "BubbleLabs API Endpoint" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "bubbleLabsApiEndpoint",
                  type: "url",
                  placeholder: "http://localhost:8002/api/v1",
                  value: r.bubbleLabsApiEndpoint,
                  onChange: (v) => d("bubbleLabsApiEndpoint", v.target.value),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              ),
              /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Base URL for BubbleLabs visualization service" })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "researchQuestApiEndpoint", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "ResearchQuest API Endpoint" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "researchQuestApiEndpoint",
                  type: "url",
                  placeholder: "http://localhost:8003/api/v1",
                  value: r.researchQuestApiEndpoint,
                  onChange: (v) => d("researchQuestApiEndpoint", v.target.value),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              ),
              /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Base URL for ResearchQuest knowledge exploration service" })
            ] })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "mt-6 p-4 bg-blue-50 dark:bg-blue-900/20 rounded-md", children: [
            /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-blue-900 dark:text-blue-400 mb-2", children: "Connection Test" }),
            /* @__PURE__ */ s.jsx(
              X,
              {
                onClick: () => {
                  F.info("Testing connection to all endpoints..."), setTimeout(() => {
                    F.success("All endpoints are reachable");
                  }, 2e3);
                },
                children: "Test All Connections"
              }
            )
          ] })
        ] }),
        a === "authentication" && /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-2", children: "Authentication Settings" }),
            /* @__PURE__ */ s.jsx("p", { className: "text-sm text-gray-600 dark:text-gray-400 mb-4", children: "Configure authentication for API requests." })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex items-start", children: [
              /* @__PURE__ */ s.jsx("div", { className: "flex items-center h-5", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "useAuthentication",
                  type: "checkbox",
                  checked: r.useAuthentication,
                  onChange: (v) => d("useAuthentication", v.target.checked),
                  className: "h-4 w-4 text-green-600 focus:ring-green-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ) }),
              /* @__PURE__ */ s.jsxs("div", { className: "ml-3", children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "useAuthentication", className: "text-sm font-medium text-gray-700 dark:text-gray-300", children: "Enable Authentication" }),
                /* @__PURE__ */ s.jsx("p", { className: "text-xs text-gray-500 dark:text-gray-400", children: "Require authentication for API requests" })
              ] })
            ] }),
            r.useAuthentication && /* @__PURE__ */ s.jsx(s.Fragment, { children: /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "authType", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Authentication Type" }),
              /* @__PURE__ */ s.jsxs(
                je,
                {
                  id: "authType",
                  value: r.authType,
                  onChange: (v) => d("authType", v.target.value),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                  children: [
                    /* @__PURE__ */ s.jsx("option", { value: "api_key", children: "API Key" }),
                    /* @__PURE__ */ s.jsx("option", { value: "oauth2", children: "OAuth 2.0" }),
                    /* @__PURE__ */ s.jsx("option", { value: "jwt", children: "JWT Token" }),
                    /* @__PURE__ */ s.jsx("option", { value: "basic", children: "Basic Auth" })
                  ]
                }
              )
            ] }) })
          ] }),
          r.useAuthentication && r.authType === "api_key" && /* @__PURE__ */ s.jsxs("div", { className: "mt-6 p-4 bg-yellow-50 dark:bg-yellow-900/20 rounded-md", children: [
            /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-yellow-900 dark:text-yellow-400 mb-4", children: "API Key Configuration" }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "apiKey", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "API Key" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "apiKey",
                  type: c ? "text" : "password",
                  placeholder: "Enter your API key",
                  value: r.apiKey,
                  onChange: (v) => d("apiKey", v.target.value),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              ),
              /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "The API key will be sent in the X-API-Key header" })
            ] })
          ] }),
          r.useAuthentication && r.authType === "oauth2" && /* @__PURE__ */ s.jsxs("div", { className: "mt-6 p-4 bg-yellow-50 dark:bg-yellow-900/20 rounded-md space-y-4", children: [
            /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-yellow-900 dark:text-yellow-400", children: "OAuth 2.0 Configuration" }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "oauth2ClientId", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Client ID" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "oauth2ClientId",
                  type: "text",
                  value: r.oauth2ClientId,
                  onChange: (v) => d("oauth2ClientId", v.target.value),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              )
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "oauth2ClientSecret", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Client Secret" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "oauth2ClientSecret",
                  type: c ? "text" : "password",
                  value: h(r.oauth2ClientSecret),
                  onChange: (v) => d("oauth2ClientSecret", v.target.value),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              )
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "oauth2Scope", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Scope" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "oauth2Scope",
                  type: "text",
                  placeholder: "api.read api.write",
                  value: r.oauth2Scope,
                  onChange: (v) => d("oauth2Scope", v.target.value),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              )
            ] })
          ] }),
          r.useAuthentication && r.authType === "jwt" && /* @__PURE__ */ s.jsxs("div", { className: "mt-6 p-4 bg-yellow-50 dark:bg-yellow-900/20 rounded-md", children: [
            /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-yellow-900 dark:text-yellow-400 mb-4", children: "JWT Token Configuration" }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "jwtToken", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "JWT Token" }),
              /* @__PURE__ */ s.jsx(
                Lt,
                {
                  id: "jwtToken",
                  rows: 4,
                  value: h(r.jwtToken),
                  onChange: (v) => d("jwtToken", v.target.value),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white font-mono text-xs"
                }
              ),
              /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "The token will be sent in the Authorization header as Bearer token" })
            ] })
          ] }),
          r.useAuthentication && r.authType === "basic" && /* @__PURE__ */ s.jsxs("div", { className: "mt-6 p-4 bg-yellow-50 dark:bg-yellow-900/20 rounded-md space-y-4", children: [
            /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-yellow-900 dark:text-yellow-400", children: "Basic Authentication" }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "basicUsername", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Username" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "basicUsername",
                  type: "text",
                  value: r.basicUsername,
                  onChange: (v) => d("basicUsername", v.target.value),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              )
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "basicPassword", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Password" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "basicPassword",
                  type: c ? "text" : "password",
                  value: h(r.basicPassword),
                  onChange: (v) => d("basicPassword", v.target.value),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              )
            ] })
          ] })
        ] }),
        a === "timeouts" && /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-2", children: "Timeouts & Rate Limits" }),
            /* @__PURE__ */ s.jsx("p", { className: "text-sm text-gray-600 dark:text-gray-400 mb-4", children: "Configure request timeouts and rate limiting." })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsxs("label", { htmlFor: "defaultTimeout", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: [
                "Default Timeout (ms)",
                /* @__PURE__ */ s.jsx("span", { className: "ml-2 text-xs text-gray-500", children: "(1000-300000)" })
              ] }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "defaultTimeout",
                  type: "number",
                  min: "1000",
                  max: "300000",
                  step: "1000",
                  value: r.defaultTimeout,
                  onChange: (v) => d("defaultTimeout", parseInt(v.target.value) || 3e4),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              ),
              /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Default timeout for API requests (30 seconds)" })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "leanAideTimeout", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "LeanAide Timeout (ms)" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "leanAideTimeout",
                  type: "number",
                  min: "1000",
                  max: "300000",
                  step: "1000",
                  value: r.leanAideTimeout,
                  onChange: (v) => d("leanAideTimeout", parseInt(v.target.value) || 6e4),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              ),
              /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Timeout for LeanAide mathematical proofs (60 seconds)" })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "crewaiTimeout", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "CrewAI Timeout (ms)" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "crewaiTimeout",
                  type: "number",
                  min: "1000",
                  max: "300000",
                  step: "1000",
                  value: r.crewaiTimeout,
                  onChange: (v) => d("crewaiTimeout", parseInt(v.target.value) || 45e3),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              ),
              /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Timeout for CrewAI delegation tasks (45 seconds)" })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "bubbleLabsTimeout", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "BubbleLabs Timeout (ms)" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "bubbleLabsTimeout",
                  type: "number",
                  min: "1000",
                  max: "300000",
                  step: "1000",
                  value: r.bubbleLabsTimeout,
                  onChange: (v) => d("bubbleLabsTimeout", parseInt(v.target.value) || 3e4),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              ),
              /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Timeout for BubbleLabs UI operations (30 seconds)" })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("label", { htmlFor: "researchQuestTimeout", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "ResearchQuest Timeout (ms)" }),
              /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "researchQuestTimeout",
                  type: "number",
                  min: "1000",
                  max: "300000",
                  step: "1000",
                  value: r.researchQuestTimeout,
                  onChange: (v) => d("researchQuestTimeout", parseInt(v.target.value) || 3e4),
                  className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                }
              ),
              /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Timeout for ResearchQuest queries (30 seconds)" })
            ] })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "mt-6 p-4 bg-green-50 dark:bg-green-900/20 rounded-md", children: [
            /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-green-900 dark:text-green-400 mb-4", children: "Rate Limiting" }),
            /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
              /* @__PURE__ */ s.jsxs("div", { className: "flex items-start", children: [
                /* @__PURE__ */ s.jsx("div", { className: "flex items-center h-5", children: /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "enableRateLimiting",
                    type: "checkbox",
                    checked: r.enableRateLimiting,
                    onChange: (v) => d("enableRateLimiting", v.target.checked),
                    className: "h-4 w-4 text-green-600 focus:ring-green-500 border-gray-300 dark:border-gray-600 rounded"
                  }
                ) }),
                /* @__PURE__ */ s.jsxs("div", { className: "ml-3", children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "enableRateLimiting", className: "text-sm font-medium text-gray-700 dark:text-gray-300", children: "Enable Rate Limiting" }),
                  /* @__PURE__ */ s.jsx("p", { className: "text-xs text-gray-500 dark:text-gray-400", children: "Prevent overwhelming API servers with too many requests" })
                ] })
              ] }),
              r.enableRateLimiting && /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-3 gap-4", children: [
                /* @__PURE__ */ s.jsxs("div", { children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "maxRequestsPerSecond", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Requests/Second" }),
                  /* @__PURE__ */ s.jsx(
                    D,
                    {
                      id: "maxRequestsPerSecond",
                      type: "number",
                      min: "1",
                      max: "100",
                      value: r.maxRequestsPerSecond,
                      onChange: (v) => d("maxRequestsPerSecond", parseInt(v.target.value) || 10),
                      className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                    }
                  )
                ] }),
                /* @__PURE__ */ s.jsxs("div", { children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "maxRequestsPerMinute", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Requests/Minute" }),
                  /* @__PURE__ */ s.jsx(
                    D,
                    {
                      id: "maxRequestsPerMinute",
                      type: "number",
                      min: "1",
                      max: "10000",
                      value: r.maxRequestsPerMinute,
                      onChange: (v) => d("maxRequestsPerMinute", parseInt(v.target.value) || 500),
                      className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                    }
                  )
                ] }),
                /* @__PURE__ */ s.jsxs("div", { children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "rateLimitBurstSize", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Burst Size" }),
                  /* @__PURE__ */ s.jsx(
                    D,
                    {
                      id: "rateLimitBurstSize",
                      type: "number",
                      min: "1",
                      max: "100",
                      value: r.rateLimitBurstSize,
                      onChange: (v) => d("rateLimitBurstSize", parseInt(v.target.value) || 20),
                      className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                    }
                  )
                ] })
              ] })
            ] })
          ] })
        ] }),
        a === "websocket" && /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-2", children: "WebSocket Configuration" }),
            /* @__PURE__ */ s.jsx("p", { className: "text-sm text-gray-600 dark:text-gray-400 mb-4", children: "Configure real-time communication via WebSocket." })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex items-start", children: [
              /* @__PURE__ */ s.jsx("div", { className: "flex items-center h-5", children: /* @__PURE__ */ s.jsx(
                D,
                {
                  id: "enableWebSocket",
                  type: "checkbox",
                  checked: r.enableWebSocket,
                  onChange: (v) => d("enableWebSocket", v.target.checked),
                  className: "h-4 w-4 text-green-600 focus:ring-green-500 border-gray-300 dark:border-gray-600 rounded"
                }
              ) }),
              /* @__PURE__ */ s.jsxs("div", { className: "ml-3", children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "enableWebSocket", className: "text-sm font-medium text-gray-700 dark:text-gray-300", children: "Enable WebSocket" }),
                /* @__PURE__ */ s.jsx("p", { className: "text-xs text-gray-500 dark:text-gray-400", children: "Use WebSocket for real-time updates and notifications" })
              ] })
            ] }),
            r.enableWebSocket && /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-6", children: [
              /* @__PURE__ */ s.jsxs("div", { children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "websocketUrl", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "WebSocket URL" }),
                /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "websocketUrl",
                    type: "url",
                    placeholder: "ws://localhost:8080/ws",
                    value: r.websocketUrl,
                    onChange: (v) => d("websocketUrl", v.target.value),
                    className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                  }
                )
              ] }),
              /* @__PURE__ */ s.jsxs("div", { children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "websocketReconnectInterval", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Reconnect Interval (ms)" }),
                /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "websocketReconnectInterval",
                    type: "number",
                    min: "1000",
                    max: "60000",
                    step: "1000",
                    value: r.websocketReconnectInterval,
                    onChange: (v) => d("websocketReconnectInterval", parseInt(v.target.value) || 5e3),
                    className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                  }
                )
              ] }),
              /* @__PURE__ */ s.jsxs("div", { children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "websocketMaxReconnectAttempts", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Max Reconnect Attempts" }),
                /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "websocketMaxReconnectAttempts",
                    type: "number",
                    min: "1",
                    max: "100",
                    value: r.websocketMaxReconnectAttempts,
                    onChange: (v) => d("websocketMaxReconnectAttempts", parseInt(v.target.value) || 10),
                    className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                  }
                )
              ] }),
              /* @__PURE__ */ s.jsxs("div", { children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "websocketHeartbeatInterval", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Heartbeat Interval (ms)" }),
                /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "websocketHeartbeatInterval",
                    type: "number",
                    min: "10000",
                    max: "300000",
                    step: "5000",
                    value: r.websocketHeartbeatInterval,
                    onChange: (v) => d("websocketHeartbeatInterval", parseInt(v.target.value) || 3e4),
                    className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                  }
                ),
                /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Send ping frame every N milliseconds to keep connection alive" })
              ] })
            ] })
          ] })
        ] }),
        a === "retry" && /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-2", children: "Retry Logic & Circuit Breaker" }),
            /* @__PURE__ */ s.jsx("p", { className: "text-sm text-gray-600 dark:text-gray-400 mb-4", children: "Configure resilience patterns for handling failures." })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "p-4 bg-orange-50 dark:bg-orange-900/20 rounded-md", children: [
              /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-orange-900 dark:text-orange-400 mb-4", children: "Retry Configuration" }),
              /* @__PURE__ */ s.jsxs("div", { className: "flex items-start mb-4", children: [
                /* @__PURE__ */ s.jsx("div", { className: "flex items-center h-5", children: /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "enableRetry",
                    type: "checkbox",
                    checked: r.enableRetry,
                    onChange: (v) => d("enableRetry", v.target.checked),
                    className: "h-4 w-4 text-orange-600 focus:ring-orange-500 border-gray-300 dark:border-gray-600 rounded"
                  }
                ) }),
                /* @__PURE__ */ s.jsxs("div", { className: "ml-3", children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "enableRetry", className: "text-sm font-medium text-gray-700 dark:text-gray-300", children: "Enable Automatic Retry" }),
                  /* @__PURE__ */ s.jsx("p", { className: "text-xs text-gray-500 dark:text-gray-400", children: "Automatically retry failed requests" })
                ] })
              ] }),
              r.enableRetry && /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-4", children: [
                /* @__PURE__ */ s.jsxs("div", { children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "maxRetries", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Max Retries" }),
                  /* @__PURE__ */ s.jsx(
                    D,
                    {
                      id: "maxRetries",
                      type: "number",
                      min: "0",
                      max: "10",
                      value: r.maxRetries,
                      onChange: (v) => d("maxRetries", parseInt(v.target.value) || 3),
                      className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-orange-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                    }
                  )
                ] }),
                /* @__PURE__ */ s.jsxs("div", { children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "retryDelay", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Initial Delay (ms)" }),
                  /* @__PURE__ */ s.jsx(
                    D,
                    {
                      id: "retryDelay",
                      type: "number",
                      min: "100",
                      max: "10000",
                      step: "100",
                      value: r.retryDelay,
                      onChange: (v) => d("retryDelay", parseInt(v.target.value) || 1e3),
                      className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-orange-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                    }
                  )
                ] }),
                /* @__PURE__ */ s.jsxs("div", { children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "retryBackoffMultiplier", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Backoff Multiplier" }),
                  /* @__PURE__ */ s.jsx(
                    D,
                    {
                      id: "retryBackoffMultiplier",
                      type: "number",
                      min: "1",
                      max: "10",
                      step: "0.5",
                      value: r.retryBackoffMultiplier,
                      onChange: (v) => d("retryBackoffMultiplier", parseFloat(v.target.value) || 2),
                      className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-orange-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                    }
                  ),
                  /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Delay multiplier for exponential backoff" })
                ] }),
                /* @__PURE__ */ s.jsxs("div", { children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "retryOnStatusCodes", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Status Codes" }),
                  /* @__PURE__ */ s.jsx(
                    D,
                    {
                      id: "retryOnStatusCodes",
                      type: "text",
                      value: r.retryOnStatusCodes.join(", "),
                      onChange: (v) => d("retryOnStatusCodes", v.target.value.split(",").map((b) => parseInt(b.trim()))),
                      className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-orange-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                    }
                  ),
                  /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Comma-separated HTTP status codes to retry" })
                ] })
              ] })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { className: "p-4 bg-red-50 dark:bg-red-900/20 rounded-md", children: [
              /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-red-900 dark:text-red-400 mb-4", children: "Circuit Breaker Configuration" }),
              /* @__PURE__ */ s.jsxs("div", { className: "flex items-start mb-4", children: [
                /* @__PURE__ */ s.jsx("div", { className: "flex items-center h-5", children: /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "enableCircuitBreaker",
                    type: "checkbox",
                    checked: r.enableCircuitBreaker,
                    onChange: (v) => d("enableCircuitBreaker", v.target.checked),
                    className: "h-4 w-4 text-red-600 focus:ring-red-500 border-gray-300 dark:border-gray-600 rounded"
                  }
                ) }),
                /* @__PURE__ */ s.jsxs("div", { className: "ml-3", children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "enableCircuitBreaker", className: "text-sm font-medium text-gray-700 dark:text-gray-300", children: "Enable Circuit Breaker" }),
                  /* @__PURE__ */ s.jsx("p", { className: "text-xs text-gray-500 dark:text-gray-400", children: "Prevent cascading failures by stopping requests to failing services" })
                ] })
              ] }),
              r.enableCircuitBreaker && /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-3 gap-4", children: [
                /* @__PURE__ */ s.jsxs("div", { children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "circuitBreakerThreshold", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Failure Threshold" }),
                  /* @__PURE__ */ s.jsx(
                    D,
                    {
                      id: "circuitBreakerThreshold",
                      type: "number",
                      min: "1",
                      max: "100",
                      value: r.circuitBreakerThreshold,
                      onChange: (v) => d("circuitBreakerThreshold", parseInt(v.target.value) || 5),
                      className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-red-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                    }
                  ),
                  /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Failures before opening circuit" })
                ] }),
                /* @__PURE__ */ s.jsxs("div", { children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "circuitBreakerTimeout", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Timeout (ms)" }),
                  /* @__PURE__ */ s.jsx(
                    D,
                    {
                      id: "circuitBreakerTimeout",
                      type: "number",
                      min: "10000",
                      max: "600000",
                      step: "1000",
                      value: r.circuitBreakerTimeout,
                      onChange: (v) => d("circuitBreakerTimeout", parseInt(v.target.value) || 6e4),
                      className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-red-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                    }
                  ),
                  /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Time before attempting recovery" })
                ] }),
                /* @__PURE__ */ s.jsxs("div", { children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "circuitBreakerHalfOpenAttempts", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Half-Open Attempts" }),
                  /* @__PURE__ */ s.jsx(
                    D,
                    {
                      id: "circuitBreakerHalfOpenAttempts",
                      type: "number",
                      min: "1",
                      max: "10",
                      value: r.circuitBreakerHalfOpenAttempts,
                      onChange: (v) => d("circuitBreakerHalfOpenAttempts", parseInt(v.target.value) || 3),
                      className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-red-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                    }
                  ),
                  /* @__PURE__ */ s.jsx("p", { className: "mt-1 text-xs text-gray-500 dark:text-gray-400", children: "Test requests in half-open state" })
                ] })
              ] })
            ] })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "mt-6 p-4 bg-blue-50 dark:bg-blue-900/20 rounded-md", children: [
            /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-blue-900 dark:text-blue-400 mb-2", children: "Circuit Breaker States" }),
            /* @__PURE__ */ s.jsxs("ul", { className: "text-sm text-blue-800 dark:text-blue-300 space-y-1", children: [
              /* @__PURE__ */ s.jsxs("li", { children: [
                /* @__PURE__ */ s.jsx("strong", { children: "Closed:" }),
                " Normal operation, requests pass through"
              ] }),
              /* @__PURE__ */ s.jsxs("li", { children: [
                /* @__PURE__ */ s.jsx("strong", { children: "Open:" }),
                " Failing, requests are blocked"
              ] }),
              /* @__PURE__ */ s.jsxs("li", { children: [
                /* @__PURE__ */ s.jsx("strong", { children: "Half-Open:" }),
                " Testing if service has recovered"
              ] })
            ] })
          ] })
        ] }),
        a === "services" && /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("h3", { className: "text-lg font-semibold text-gray-900 dark:text-white mb-2", children: "Service-Specific Settings" }),
            /* @__PURE__ */ s.jsx("p", { className: "text-sm text-gray-600 dark:text-gray-400 mb-4", children: "Configure individual service parameters." })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "p-4 bg-purple-50 dark:bg-purple-900/20 rounded-md", children: [
            /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-purple-900 dark:text-purple-400 mb-4", children: "LeanAide Configuration" }),
            /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-4", children: [
              /* @__PURE__ */ s.jsxs("div", { children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "leanAideProvider", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "AI Provider" }),
                /* @__PURE__ */ s.jsxs(
                  je,
                  {
                    id: "leanAideProvider",
                    value: r.leanAide.provider,
                    onChange: (v) => f("leanAide", "provider", v.target.value),
                    className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-purple-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                    children: [
                      /* @__PURE__ */ s.jsx("option", { value: "openai", children: "OpenAI" }),
                      /* @__PURE__ */ s.jsx("option", { value: "anthropic", children: "Anthropic" }),
                      /* @__PURE__ */ s.jsx("option", { value: "mistral", children: "Mistral" }),
                      /* @__PURE__ */ s.jsx("option", { value: "custom", children: "Custom" })
                    ]
                  }
                )
              ] }),
              /* @__PURE__ */ s.jsxs("div", { children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "leanAideModel", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Model" }),
                /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "leanAideModel",
                    type: "text",
                    value: r.leanAide.model,
                    onChange: (v) => f("leanAide", "model", v.target.value),
                    className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-purple-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                  }
                )
              ] }),
              /* @__PURE__ */ s.jsxs("div", { children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "leanAideTemperature", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Temperature" }),
                /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "leanAideTemperature",
                    type: "number",
                    step: "0.1",
                    min: "0",
                    max: "2",
                    value: r.leanAide.temperature,
                    onChange: (v) => f("leanAide", "temperature", parseFloat(v.target.value)),
                    className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-purple-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                  }
                )
              ] }),
              /* @__PURE__ */ s.jsxs("div", { children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "leanAideMaxTokens", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Max Tokens" }),
                /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "leanAideMaxTokens",
                    type: "number",
                    min: "1",
                    max: "32000",
                    value: r.leanAide.maxTokens,
                    onChange: (v) => f("leanAide", "maxTokens", parseInt(v.target.value)),
                    className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-purple-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                  }
                )
              ] })
            ] })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "p-4 bg-indigo-50 dark:bg-indigo-900/20 rounded-md", children: [
            /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-indigo-900 dark:text-indigo-400 mb-4", children: "CrewAI Configuration" }),
            /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-3 gap-4", children: [
              /* @__PURE__ */ s.jsxs("div", { className: "flex items-start", children: [
                /* @__PURE__ */ s.jsx("div", { className: "flex items-center h-5", children: /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "crewaiParallelExecution",
                    type: "checkbox",
                    checked: r.crewai.parallelExecution,
                    onChange: (v) => f("crewai", "parallelExecution", v.target.checked),
                    className: "h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 dark:border-gray-600 rounded"
                  }
                ) }),
                /* @__PURE__ */ s.jsxs("div", { className: "ml-3", children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "crewaiParallelExecution", className: "text-sm font-medium text-gray-700 dark:text-gray-300", children: "Parallel Execution" }),
                  /* @__PURE__ */ s.jsx("p", { className: "text-xs text-gray-500 dark:text-gray-400", children: "Execute tasks in parallel" })
                ] })
              ] }),
              /* @__PURE__ */ s.jsxs("div", { children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "crewaiMaxParallelTasks", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Max Parallel Tasks" }),
                /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "crewaiMaxParallelTasks",
                    type: "number",
                    min: "1",
                    max: "20",
                    value: r.crewai.maxParallelTasks,
                    onChange: (v) => f("crewai", "maxParallelTasks", parseInt(v.target.value)),
                    className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                  }
                )
              ] }),
              /* @__PURE__ */ s.jsxs("div", { children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "crewaiDelegationTimeout", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Delegation Timeout (ms)" }),
                /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "crewaiDelegationTimeout",
                    type: "number",
                    min: "10000",
                    max: "600000",
                    step: "1000",
                    value: r.crewai.delegationTimeout,
                    onChange: (v) => f("crewai", "delegationTimeout", parseInt(v.target.value)),
                    className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                  }
                )
              ] })
            ] })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "p-4 bg-blue-50 dark:bg-blue-900/20 rounded-md", children: [
            /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-blue-900 dark:text-blue-400 mb-4", children: "BubbleLabs Configuration" }),
            /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-3 gap-4", children: [
              /* @__PURE__ */ s.jsxs("div", { className: "flex items-start", children: [
                /* @__PURE__ */ s.jsx("div", { className: "flex items-center h-5", children: /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "bubbleLabsUiEnabled",
                    type: "checkbox",
                    checked: r.bubbleLabs.uiEnabled,
                    onChange: (v) => f("bubbleLabs", "uiEnabled", v.target.checked),
                    className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                  }
                ) }),
                /* @__PURE__ */ s.jsxs("div", { className: "ml-3", children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "bubbleLabsUiEnabled", className: "text-sm font-medium text-gray-700 dark:text-gray-300", children: "Enable UI" }),
                  /* @__PURE__ */ s.jsx("p", { className: "text-xs text-gray-500 dark:text-gray-400", children: "Show visualization interface" })
                ] })
              ] }),
              /* @__PURE__ */ s.jsxs("div", { className: "flex items-start", children: [
                /* @__PURE__ */ s.jsx("div", { className: "flex items-center h-5", children: /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "bubbleLabsRealTimeUpdates",
                    type: "checkbox",
                    checked: r.bubbleLabs.realTimeUpdates,
                    onChange: (v) => f("bubbleLabs", "realTimeUpdates", v.target.checked),
                    className: "h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 dark:border-gray-600 rounded"
                  }
                ) }),
                /* @__PURE__ */ s.jsxs("div", { className: "ml-3", children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "bubbleLabsRealTimeUpdates", className: "text-sm font-medium text-gray-700 dark:text-gray-300", children: "Real-Time Updates" }),
                  /* @__PURE__ */ s.jsx("p", { className: "text-xs text-gray-500 dark:text-gray-400", children: "Stream updates as they occur" })
                ] })
              ] }),
              /* @__PURE__ */ s.jsxs("div", { children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "bubbleLabsVisualizationQuality", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Visualization Quality" }),
                /* @__PURE__ */ s.jsxs(
                  je,
                  {
                    id: "bubbleLabsVisualizationQuality",
                    value: r.bubbleLabs.visualizationQuality,
                    onChange: (v) => f("bubbleLabs", "visualizationQuality", v.target.value),
                    className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                    children: [
                      /* @__PURE__ */ s.jsx("option", { value: "low", children: "Low" }),
                      /* @__PURE__ */ s.jsx("option", { value: "medium", children: "Medium" }),
                      /* @__PURE__ */ s.jsx("option", { value: "high", children: "High" })
                    ]
                  }
                )
              ] })
            ] })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "p-4 bg-teal-50 dark:bg-teal-900/20 rounded-md", children: [
            /* @__PURE__ */ s.jsx("h4", { className: "text-sm font-medium text-teal-900 dark:text-teal-400 mb-4", children: "ResearchQuest Configuration" }),
            /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-3 gap-4", children: [
              /* @__PURE__ */ s.jsxs("div", { className: "flex items-start", children: [
                /* @__PURE__ */ s.jsx("div", { className: "flex items-center h-5", children: /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "researchQuestKnowledgeGraphEnabled",
                    type: "checkbox",
                    checked: r.researchQuest.knowledgeGraphEnabled,
                    onChange: (v) => f("researchQuest", "knowledgeGraphEnabled", v.target.checked),
                    className: "h-4 w-4 text-teal-600 focus:ring-teal-500 border-gray-300 dark:border-gray-600 rounded"
                  }
                ) }),
                /* @__PURE__ */ s.jsxs("div", { className: "ml-3", children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "researchQuestKnowledgeGraphEnabled", className: "text-sm font-medium text-gray-700 dark:text-gray-300", children: "Knowledge Graph" }),
                  /* @__PURE__ */ s.jsx("p", { className: "text-xs text-gray-500 dark:text-gray-400", children: "Use graph-based exploration" })
                ] })
              ] }),
              /* @__PURE__ */ s.jsxs("div", { className: "flex items-start", children: [
                /* @__PURE__ */ s.jsx("div", { className: "flex items-center h-5", children: /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "researchQuestSemanticSearch",
                    type: "checkbox",
                    checked: r.researchQuest.semanticSearch,
                    onChange: (v) => f("researchQuest", "semanticSearch", v.target.checked),
                    className: "h-4 w-4 text-teal-600 focus:ring-teal-500 border-gray-300 dark:border-gray-600 rounded"
                  }
                ) }),
                /* @__PURE__ */ s.jsxs("div", { className: "ml-3", children: [
                  /* @__PURE__ */ s.jsx("label", { htmlFor: "researchQuestSemanticSearch", className: "text-sm font-medium text-gray-700 dark:text-gray-300", children: "Semantic Search" }),
                  /* @__PURE__ */ s.jsx("p", { className: "text-xs text-gray-500 dark:text-gray-400", children: "Enable semantic similarity search" })
                ] })
              ] }),
              /* @__PURE__ */ s.jsxs("div", { children: [
                /* @__PURE__ */ s.jsx("label", { htmlFor: "researchQuestMaxResults", className: "block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2", children: "Max Results" }),
                /* @__PURE__ */ s.jsx(
                  D,
                  {
                    id: "researchQuestMaxResults",
                    type: "number",
                    min: "1",
                    max: "100",
                    value: r.researchQuest.maxResults,
                    onChange: (v) => f("researchQuest", "maxResults", parseInt(v.target.value)),
                    className: "w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-teal-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                  }
                )
              ] })
            ] })
          ] })
        ] })
      ] })
    ] })
  ] });
}, t1 = {
  temperature: 0.7,
  top_p: 0.9,
  max_tokens: 2048,
  frequency_penalty: 0,
  presence_penalty: 0,
  seed: null,
  reasoning_effort: "medium",
  api_timeout: 30,
  api_retries: 3
}, r1 = {
  max_iterations: 100,
  population_size: 50,
  num_islands: 5,
  migration_interval: 50,
  migration_rate: 0.1,
  archive_size: 100,
  elite_ratio: 0.1,
  exploration_ratio: 0.2,
  exploitation_ratio: 0.7,
  checkpoint_interval: 100,
  feature_dimensions: [],
  feature_bins: 10,
  diversity_metric: "edit_distance"
}, yi = {
  generation: t1,
  evolution: r1
}, aj = pl()(
  Yh(
    Xh(
      (e, t) => ({
        scope: "Global",
        provider: void 0,
        model: void 0,
        parameterSettings: {
          global: yi,
          providers: {}
        },
        setScope: (r) => e({ scope: r }),
        setProvider: (r) => e({ provider: r, model: void 0 }),
        setModel: (r) => e({ model: r }),
        updateGlobalSettings: (r) => e((n) => ({
          parameterSettings: {
            ...n.parameterSettings,
            global: {
              generation: {
                ...n.parameterSettings.global.generation,
                ...r.generation
              },
              evolution: {
                ...n.parameterSettings.global.evolution,
                ...r.evolution
              }
            }
          }
        })),
        updateProviderSettings: (r, n) => e((a) => {
          const i = a.parameterSettings.providers[r] || {
            settings: yi,
            models: {}
          };
          return {
            parameterSettings: {
              ...a.parameterSettings,
              providers: {
                ...a.parameterSettings.providers,
                [r]: {
                  ...i,
                  settings: {
                    generation: {
                      ...i.settings.generation,
                      ...n.generation
                    },
                    evolution: {
                      ...i.settings.evolution,
                      ...n.evolution
                    }
                  }
                }
              }
            }
          };
        }),
        updateModelSettings: (r, n, a) => e((i) => {
          const o = i.parameterSettings.providers[r] || {
            settings: yi,
            models: {}
          }, l = o.models[n] || yi;
          return {
            parameterSettings: {
              ...i.parameterSettings,
              providers: {
                ...i.parameterSettings.providers,
                [r]: {
                  ...o,
                  models: {
                    ...o.models,
                    [n]: {
                      generation: {
                        ...l.generation,
                        ...a.generation
                      },
                      evolution: {
                        ...l.evolution,
                        ...a.evolution
                      }
                    }
                  }
                }
              }
            }
          };
        }),
        setProviderApiKeyLastFour: (r, n) => e((a) => {
          const i = a.parameterSettings.providers[r] || {
            settings: yi,
            models: {}
          };
          return {
            parameterSettings: {
              ...a.parameterSettings,
              providers: {
                ...a.parameterSettings.providers,
                [r]: {
                  ...i,
                  apiKeyLastFour: n
                }
              }
            }
          };
        })
      }),
      {
        name: "openevolve-settings",
        partialize: (e) => ({
          scope: e.scope,
          provider: e.provider,
          model: e.model,
          parameterSettings: e.parameterSettings
        })
      }
    ),
    { name: "SettingsStore" }
  )
);
function Qa() {
  const e = Xa(), t = fs((a) => a.token);
  return {
    useGet: (a, i, o) => Hh({
      queryKey: a,
      queryFn: i,
      enabled: !!t && (o == null ? void 0 : o.enabled) !== !1,
      refetchInterval: o == null ? void 0 : o.refetchInterval,
      staleTime: o == null ? void 0 : o.staleTime
    }),
    usePost: (a, i) => Dt({
      mutationFn: a,
      onSuccess: i == null ? void 0 : i.onSuccess,
      onError: i == null ? void 0 : i.onError
    }),
    queryClient: e,
    isAuthenticated: !!t
  };
}
function n1(e) {
  const { useGet: t } = Qa(), r = t(
    ["evolution", e],
    () => mt.getStatus(e),
    {
      enabled: !!e,
      refetchInterval: 2e3
      // Poll every 2 seconds
    }
  ), n = Dt(
    ({
      content: l,
      mode: c,
      parameters: u,
      models: d
    }) => mt.start({ content: l, mode: c, parameters: u, models: d })
  ), a = Dt((l) => mt.pause(l)), i = Dt((l) => mt.resume(l)), o = Dt((l) => mt.stop(l));
  return {
    evolution: r.data,
    isLoading: r.isLoading,
    error: r.error,
    refetch: r.refetch,
    startEvolution: n.mutateAsync,
    pauseEvolution: a.mutateAsync,
    resumeEvolution: i.mutateAsync,
    stopEvolution: o.mutateAsync,
    isStarting: n.isPending,
    isPausing: a.isPending,
    isResuming: i.isPending,
    isStopping: o.isPending
  };
}
function a1(e) {
  const { useGet: t } = Qa(), r = t(
    ["adversarial", e],
    () => Vr.getStatus(e),
    {
      enabled: !!e,
      refetchInterval: 2e3
    }
  ), n = Dt((o) => Vr.start(o)), a = Dt(
    ({ testId: o, round: l, approved: c, feedback: u }) => Vr.approvePatch(o, { round: l, approved: c, feedback: u })
  ), i = Dt((o) => Vr.stop(o));
  return {
    test: r.data,
    isLoading: r.isLoading,
    error: r.error,
    refetch: r.refetch,
    startTest: n.mutateAsync,
    approvePatch: a.mutateAsync,
    stopTest: i.mutateAsync,
    isStarting: n.isPending,
    isApproving: a.isPending,
    isStopping: i.isPending
  };
}
function i1(e) {
  const { useGet: t } = Qa(), r = t(
    ["analytics", "metrics", e],
    () => fg.getMetrics(e),
    {
      enabled: !!e,
      staleTime: 6e4
      // 1 minute
    }
  ), n = t(
    ["analytics", "performance"],
    () => fg.getPerformance(),
    {
      staleTime: 3e5
      // 5 minutes
    }
  );
  return {
    metrics: r.data,
    performance: n.data,
    isLoading: r.isLoading || n.isLoading,
    error: r.error || n.error,
    refetch: () => {
      r.refetch(), n.refetch();
    }
  };
}
function o1(e = 5e3) {
  const { useGet: t } = Qa(), r = t(
    ["monitoring", "health"],
    () => bA.getHealth(),
    {
      refetchInterval
    }
  );
  return {
    health: r.data,
    isLoading: r.isLoading,
    error: r.error
  };
}
function ij() {
  const { useGet: e } = Qa(), t = Xa(), r = aj((l) => l.updateGlobalSettings), n = e(
    ["config", "providers"],
    () => Bo.getProviders(),
    {
      staleTime: 6e5
      // 10 minutes
    }
  ), a = e(
    ["config", "parameters"],
    () => Bo.getParameters(),
    {
      staleTime: 3e5
      // 5 minutes
    }
  );
  et(() => {
    var d, f, p, m, y;
    const l = a.data;
    if (!l)
      return;
    const c = {}, u = {};
    ((d = l.generation) == null ? void 0 : d.temperature) !== void 0 && (c.temperature = l.generation.temperature), ((f = l.generation) == null ? void 0 : f.top_p) !== void 0 && (c.top_p = l.generation.top_p), ((p = l.generation) == null ? void 0 : p.max_tokens) !== void 0 && (c.max_tokens = l.generation.max_tokens), ((m = l.evolution) == null ? void 0 : m.max_iterations) !== void 0 && (u.max_iterations = l.evolution.max_iterations), ((y = l.evolution) == null ? void 0 : y.population_size) !== void 0 && (u.population_size = l.evolution.population_size), (Object.keys(c).length || Object.keys(u).length) && r({ generation: c, evolution: u });
  }, [a.data, r]);
  const i = Dt(
    ({ provider: l, apiKey: c }) => Bo.saveApiKey(l, c),
    {
      onSuccess: () => {
        t.invalidateQueries({ queryKey: ["config"] });
      }
    }
  ), o = Dt(
    (l) => Bo.updateParameters(l),
    {
      onSuccess: () => {
        t.invalidateQueries({ queryKey: ["config", "parameters"] });
      }
    }
  );
  return {
    providers: n.data,
    parameters: a.data,
    isLoading: n.isLoading || a.isLoading,
    saveApiKey: i.mutateAsync,
    updateParameters: o.mutateAsync,
    isSavingKey: i.isPending,
    isUpdatingParams: o.isPending
  };
}
function s1() {
  Xa();
  const { useGet: e } = Qa(), t = Dt(
    (a) => fr.generateProof(a)
  ), r = Dt(
    (a) => fr.verifyProof(a)
  );
  return {
    models: e(
      ["leanaide", "models"],
      () => fr.getModels(),
      {
        staleTime: 6e5
      }
    ).data,
    generateProof: t.mutateAsync,
    verifyProof: r.mutateAsync,
    isGenerating: t.isPending,
    isVerifying: r.isPending
  };
}
const Ag = {
  mode: "standard",
  max_iterations: 100,
  population_size: 50,
  temperature: 0.7,
  top_p: 0.9,
  mutation_rate: 0.1,
  crossover_rate: 0.8
}, jo = pl()(
  Yh(
    Xh(
      (e, t) => ({
        currentWorkflow: null,
        workflows: [],
        config: Ag,
        models: [],
        initialContent: "",
        isLoading: !1,
        error: null,
        activeTab: "overview",
        setCurrentWorkflow: (r) => e({ currentWorkflow: r }),
        setWorkflows: (r) => e({ workflows: r }),
        addWorkflow: (r) => e((n) => ({
          workflows: [r, ...n.workflows]
        })),
        updateWorkflow: (r, n) => e((a) => {
          var i;
          return {
            workflows: a.workflows.map(
              (o) => o.evolution_id === r ? { ...o, ...n } : o
            ),
            currentWorkflow: ((i = a.currentWorkflow) == null ? void 0 : i.evolution_id) === r ? { ...a.currentWorkflow, ...n } : a.currentWorkflow
          };
        }),
        removeWorkflow: (r) => e((n) => {
          var a;
          return {
            workflows: n.workflows.filter((i) => i.evolution_id !== r),
            currentWorkflow: ((a = n.currentWorkflow) == null ? void 0 : a.evolution_id) === r ? null : n.currentWorkflow
          };
        }),
        setConfig: (r) => e((n) => ({
          config: { ...n.config, ...r }
        })),
        resetConfig: () => e({ config: Ag }),
        setModels: (r) => e({ models: r }),
        addModel: (r) => e((n) => ({
          models: [...n.models, r]
        })),
        removeModel: (r) => e((n) => ({
          models: n.models.filter((a, i) => i !== r)
        })),
        setInitialContent: (r) => e({ initialContent: r }),
        setLoading: (r) => e({ isLoading: r }),
        setError: (r) => e({ error: r }),
        setActiveTab: (r) => e({ activeTab: r }),
        clearCurrentWorkflow: () => e({ currentWorkflow: null }),
        reset: () => e({
          currentWorkflow: null,
          initialContent: "",
          error: null,
          isLoading: !1
        })
      }),
      {
        name: "workflow-storage",
        partialize: (e) => ({
          config: e.config,
          models: e.models,
          activeTab: e.activeTab
        })
      }
    ),
    { name: "WorkflowStore" }
  )
);
function oj() {
  const e = Xa(), { setWorkflows: t, addWorkflow: r, updateWorkflow: n, removeWorkflow: a } = jo(), { data: i, isLoading: o, error: l } = Hh({
    queryKey: ["workflows"],
    queryFn: async () => {
      const m = await mt.list();
      return t(m.evolutions), m.evolutions;
    },
    staleTime: 3e4
  }), c = Dt({
    mutationFn: async (m) => await mt.start(m),
    onSuccess: (m) => {
      e.invalidateQueries({ queryKey: ["workflows"] }), m && r(m);
    }
  }), u = Dt({
    mutationFn: async (m) => {
      const y = await mt.pause(m);
      return n(m, { status: "paused" }), y;
    },
    onSuccess: () => {
      e.invalidateQueries({ queryKey: ["workflows"] });
    }
  }), d = Dt({
    mutationFn: async (m) => {
      const y = await mt.resume(m);
      return n(m, { status: "running" }), y;
    },
    onSuccess: () => {
      e.invalidateQueries({ queryKey: ["workflows"] });
    }
  }), f = Dt({
    mutationFn: async (m) => {
      const y = await mt.stop(m);
      return n(m, { status: "stopped" }), y;
    },
    onSuccess: () => {
      e.invalidateQueries({ queryKey: ["workflows"] });
    }
  }), p = Dt({
    mutationFn: async (m) => (await mt.delete(m), a(m), m),
    onSuccess: () => {
      e.invalidateQueries({ queryKey: ["workflows"] });
    }
  });
  return {
    workflows: i || [],
    isLoading: o,
    error: l,
    startWorkflow: c.mutateAsync,
    pauseWorkflow: u.mutateAsync,
    resumeWorkflow: d.mutateAsync,
    stopWorkflow: f.mutateAsync,
    deleteWorkflow: p.mutateAsync,
    isStarting: c.isPending,
    isPausing: u.isPending,
    isResuming: d.isPending,
    isStopping: f.isPending,
    isDeleting: p.isPending
  };
}
function l1(e) {
  const t = Xa(), { currentWorkflow: r, setCurrentWorkflow: n, updateWorkflow: a } = jo(), { data: i, isLoading: o, error: l } = Hh({
    queryKey: ["workflow", e],
    queryFn: async () => {
      if (!e) throw new Error("Evolution ID is required");
      const p = await mt.getStatus(e);
      return n(p), p;
    },
    enabled: !!e,
    refetchInterval: 2e3
    // Poll every 2 seconds
  }), c = ot(async () => {
    e && (await mt.pause(e), a(e, { status: "paused" }), t.invalidateQueries({ queryKey: ["workflow", e] }));
  }, [e, a, t]), u = ot(async () => {
    e && (await mt.resume(e), a(e, { status: "running" }), t.invalidateQueries({ queryKey: ["workflow", e] }));
  }, [e, a, t]), d = ot(async () => {
    e && (await mt.stop(e), a(e, { status: "stopped" }), t.invalidateQueries({ queryKey: ["workflow", e] }));
  }, [e, a, t]), f = ot(async () => {
    e && (await mt.delete(e), n(null), t.invalidateQueries({ queryKey: ["workflows"] }));
  }, [e, n, t]);
  return {
    workflow: i || r,
    isLoading: o,
    error: l,
    pause: c,
    resume: u,
    stop: d,
    remove: f,
    refetch: () => t.invalidateQueries({ queryKey: ["workflow", e] })
  };
}
function bl() {
  const { config: e, setConfig: t, resetConfig: r } = jo(), n = ot((i) => {
    t(i);
  }, [t]), a = ot(() => {
    r();
  }, [r]);
  return {
    config: e,
    updateConfig: n,
    reset: a
  };
}
function xl() {
  const { models: e, setModels: t, addModel: r, removeModel: n } = jo(), a = ot((l) => {
    t(l);
  }, [t]), i = ot((l) => {
    r(l);
  }, [r]), o = ot((l) => {
    n(l);
  }, [n]);
  return {
    models: e,
    updateModels: a,
    add: i,
    remove: o
  };
}
const c1 = [
  { provider: "openai", name: "OpenAI", models: ["gpt-4", "gpt-4-turbo", "gpt-3.5-turbo"], requires_api_key: !0 },
  { provider: "anthropic", name: "Anthropic", models: ["claude-3-opus", "claude-3-sonnet", "claude-3-haiku"], requires_api_key: !0 },
  { provider: "cohere", name: "Cohere", models: ["command", "command-light", "command-nightly"], requires_api_key: !0 },
  { provider: "local", name: "Local", models: ["llama", "mistral", "custom"], requires_api_key: !1 }
];
function wl({ className: e }) {
  var Y, Q, ne;
  const {
    providers: t,
    isLoading: r,
    saveApiKey: n,
    updateParameters: a,
    isSavingKey: i,
    isUpdatingParams: o
  } = ij(), {
    scope: l,
    provider: c,
    model: u,
    parameterSettings: d,
    setScope: f,
    setProvider: p,
    setModel: m,
    updateGlobalSettings: y,
    updateProviderSettings: h,
    updateModelSettings: g,
    setProviderApiKeyLastFour: v
  } = aj(), { config: b, updateConfig: w } = bl(), { models: j, updateModels: x, add: _, remove: S } = xl(), k = t != null && t.length ? t : c1, E = c || ((Y = k[0]) == null ? void 0 : Y.provider), L = k.find((z) => z.provider === E), P = (L == null ? void 0 : L.models) ?? [], A = u || P[0];
  et(() => {
    !c && k.length && p(k[0].provider);
  }, [c, k, p]), et(() => {
    l === "Model" && E && !u && P.length && m(P[0]);
  }, [l, E, u, P, m]);
  const T = Ya(() => {
    var oe, ie, K;
    const z = d.global;
    if (!E)
      return z;
    const se = ((oe = d.providers[E]) == null ? void 0 : oe.settings) || z;
    return l !== "Model" || !A ? l === "Provider" ? se : z : ((K = (ie = d.providers[E]) == null ? void 0 : ie.models) == null ? void 0 : K[A]) || se;
  }, [d, l, E, A]), M = (z) => {
    var se, oe, ie, K;
    if (l === "Global") {
      y(z), ((se = z.generation) == null ? void 0 : se.temperature) !== void 0 && w({ temperature: z.generation.temperature }), ((oe = z.generation) == null ? void 0 : oe.top_p) !== void 0 && w({ top_p: z.generation.top_p }), ((ie = z.evolution) == null ? void 0 : ie.max_iterations) !== void 0 && w({ max_iterations: z.evolution.max_iterations }), ((K = z.evolution) == null ? void 0 : K.population_size) !== void 0 && w({ population_size: z.evolution.population_size });
      return;
    }
    if (E) {
      if (l === "Provider") {
        h(E, z);
        return;
      }
      l === "Model" && A && g(E, A, z);
    }
  }, [N, q] = de(""), V = async () => {
    if (!E || !N.trim())
      return;
    const z = await n({ provider: E, apiKey: N });
    z != null && z.api_key_last_four && (v(E, z.api_key_last_four), q(""));
  }, B = async () => {
    l === "Global" && await a({
      generation: {
        temperature: T.generation.temperature,
        top_p: T.generation.top_p,
        max_tokens: T.generation.max_tokens
      },
      evolution: {
        max_iterations: T.evolution.max_iterations,
        population_size: T.evolution.population_size
      }
    });
  }, ee = (z) => {
    const se = z.split(",").map((oe) => oe.trim()).filter(Boolean);
    M({ evolution: { feature_dimensions: se } });
  }, U = (z, se) => {
    const oe = j.map((ie, K) => K === z ? { ...ie, ...se } : ie);
    x(oe);
  }, G = () => {
    var oe, ie;
    const z = E || ((oe = k[0]) == null ? void 0 : oe.provider) || "openai", se = (ie = k.find((K) => K.provider === z)) == null ? void 0 : ie.models;
    _({
      provider: z,
      model: (se == null ? void 0 : se[0]) || "gpt-4"
    });
  };
  return /* @__PURE__ */ s.jsx("aside", { className: Ie("w-96 bg-slate-50 border-r border-slate-200 overflow-y-auto", e), children: /* @__PURE__ */ s.jsxs("div", { className: "p-6 space-y-6", children: [
    /* @__PURE__ */ s.jsxs("div", { children: [
      /* @__PURE__ */ s.jsx("h2", { className: "text-lg font-semibold text-slate-900", children: "Configuration" }),
      /* @__PURE__ */ s.jsx("p", { className: "text-xs text-slate-500", children: "Manage providers, API keys, and evolution defaults." })
    ] }),
    /* @__PURE__ */ s.jsx(he, { title: "Settings Scope", description: "Apply changes globally, per provider, or per model.", children: /* @__PURE__ */ s.jsx("div", { className: "grid grid-cols-3 gap-2", children: ["Global", "Provider", "Model"].map((z) => /* @__PURE__ */ s.jsx(
      X,
      {
        variant: l === z ? "primary" : "secondary",
        onClick: () => f(z),
        children: z
      },
      z
    )) }) }),
    /* @__PURE__ */ s.jsx(he, { title: "Provider", description: "Select provider and model defaults.", children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
      /* @__PURE__ */ s.jsx(H, { label: "Provider", children: /* @__PURE__ */ s.jsx(
        je,
        {
          value: E,
          onChange: (z) => p(z.target.value),
          children: k.map((z) => /* @__PURE__ */ s.jsx("option", { value: z.provider, children: z.name || z.provider }, z.provider))
        }
      ) }),
      /* @__PURE__ */ s.jsx(H, { label: "Model", hint: l === "Model" ? "Scoped model settings" : "Default model selection", children: /* @__PURE__ */ s.jsx(
        je,
        {
          value: A,
          onChange: (z) => m(z.target.value),
          disabled: l !== "Model",
          children: P.map((z) => /* @__PURE__ */ s.jsx("option", { value: z, children: z }, z))
        }
      ) }),
      /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg border border-dashed border-slate-200 bg-white px-4 py-3", children: [
        /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ s.jsx("span", { className: "text-xs font-semibold uppercase tracking-wide text-slate-500", children: "API Key" }),
          L != null && L.requires_api_key ? /* @__PURE__ */ s.jsx(Re, { tone: "warning", children: "Required" }) : /* @__PURE__ */ s.jsx(Re, { tone: "neutral", children: "Optional" })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "mt-3 space-y-2", children: [
          /* @__PURE__ */ s.jsx(
            D,
            {
              type: "password",
              placeholder: "Enter API key",
              value: N,
              onChange: (z) => q(z.target.value)
            }
          ),
          /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between text-xs text-slate-400", children: [
            /* @__PURE__ */ s.jsx("span", { children: E ? `Saved key: ${(Q = d.providers[E]) != null && Q.apiKeyLastFour ? `****${(ne = d.providers[E]) == null ? void 0 : ne.apiKeyLastFour}` : "none"}` : "Select a provider" }),
            /* @__PURE__ */ s.jsx(
              X,
              {
                variant: "secondary",
                onClick: V,
                disabled: i || !N.trim(),
                children: i ? "Saving..." : "Save"
              }
            )
          ] })
        ] })
      ] })
    ] }) }),
    /* @__PURE__ */ s.jsx(he, { title: "Evolution Mode", description: "Tune mode and model ensemble.", children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
      /* @__PURE__ */ s.jsx(H, { label: "Mode", children: /* @__PURE__ */ s.jsxs(
        je,
        {
          value: b.mode,
          onChange: (z) => w({ mode: z.target.value }),
          children: [
            /* @__PURE__ */ s.jsx("option", { value: "standard", children: "Standard" }),
            /* @__PURE__ */ s.jsx("option", { value: "quality_diversity", children: "Quality + Diversity" }),
            /* @__PURE__ */ s.jsx("option", { value: "island_model", children: "Island Model" })
          ]
        }
      ) }),
      /* @__PURE__ */ s.jsxs("div", { className: "space-y-3", children: [
        /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ s.jsx("span", { className: "text-xs font-semibold uppercase tracking-wide text-slate-500", children: "Model Ensemble" }),
          /* @__PURE__ */ s.jsx(X, { variant: "secondary", onClick: G, children: "Add" })
        ] }),
        j.length === 0 && /* @__PURE__ */ s.jsx("p", { className: "text-xs text-slate-400", children: "No models configured yet." }),
        j.map((z, se) => {
          var oe;
          return /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-6 gap-2", children: [
            /* @__PURE__ */ s.jsx(
              je,
              {
                className: "col-span-2",
                value: z.provider,
                onChange: (ie) => U(se, { provider: ie.target.value, model: "" }),
                children: k.map((ie) => /* @__PURE__ */ s.jsx("option", { value: ie.provider, children: ie.name || ie.provider }, ie.provider))
              }
            ),
            /* @__PURE__ */ s.jsx(
              je,
              {
                className: "col-span-3",
                value: z.model,
                onChange: (ie) => U(se, { model: ie.target.value }),
                children: (((oe = k.find((ie) => ie.provider === z.provider)) == null ? void 0 : oe.models) || P).map((ie) => /* @__PURE__ */ s.jsx("option", { value: ie, children: ie }, ie))
              }
            ),
            /* @__PURE__ */ s.jsx(
              X,
              {
                className: "col-span-1",
                variant: "ghost",
                onClick: () => S(se),
                children: "Remove"
              }
            )
          ] }, `${z.provider}-${z.model}-${se}`);
        })
      ] })
    ] }) }),
    /* @__PURE__ */ s.jsx(he, { title: "Generation Settings", description: "Adjust sampling and API settings.", children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
      /* @__PURE__ */ s.jsx(H, { label: "Temperature", children: /* @__PURE__ */ s.jsx(
        D,
        {
          type: "number",
          step: "0.1",
          min: "0",
          max: "2",
          value: T.generation.temperature,
          onChange: (z) => M({
            generation: { temperature: Number(z.target.value) }
          })
        }
      ) }),
      /* @__PURE__ */ s.jsx(H, { label: "Top P", children: /* @__PURE__ */ s.jsx(
        D,
        {
          type: "number",
          step: "0.05",
          min: "0",
          max: "1",
          value: T.generation.top_p,
          onChange: (z) => M({ generation: { top_p: Number(z.target.value) } })
        }
      ) }),
      /* @__PURE__ */ s.jsx(H, { label: "Max Tokens", children: /* @__PURE__ */ s.jsx(
        D,
        {
          type: "number",
          min: "1",
          value: T.generation.max_tokens,
          onChange: (z) => M({
            generation: { max_tokens: Number(z.target.value) }
          })
        }
      ) }),
      /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-2 gap-3", children: [
        /* @__PURE__ */ s.jsx(H, { label: "Frequency Penalty", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            step: "0.1",
            value: T.generation.frequency_penalty,
            onChange: (z) => M({
              generation: { frequency_penalty: Number(z.target.value) }
            })
          }
        ) }),
        /* @__PURE__ */ s.jsx(H, { label: "Presence Penalty", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            step: "0.1",
            value: T.generation.presence_penalty,
            onChange: (z) => M({
              generation: { presence_penalty: Number(z.target.value) }
            })
          }
        ) })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-2 gap-3", children: [
        /* @__PURE__ */ s.jsx(H, { label: "API Timeout (s)", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            min: "5",
            value: T.generation.api_timeout || 30,
            onChange: (z) => M({
              generation: { api_timeout: Number(z.target.value) }
            })
          }
        ) }),
        /* @__PURE__ */ s.jsx(H, { label: "API Retries", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            min: "0",
            value: T.generation.api_retries || 0,
            onChange: (z) => M({
              generation: { api_retries: Number(z.target.value) }
            })
          }
        ) })
      ] }),
      /* @__PURE__ */ s.jsx(H, { label: "Reasoning Effort", children: /* @__PURE__ */ s.jsxs(
        je,
        {
          value: T.generation.reasoning_effort || "medium",
          onChange: (z) => M({
            generation: { reasoning_effort: z.target.value }
          }),
          children: [
            /* @__PURE__ */ s.jsx("option", { value: "low", children: "Low" }),
            /* @__PURE__ */ s.jsx("option", { value: "medium", children: "Medium" }),
            /* @__PURE__ */ s.jsx("option", { value: "high", children: "High" })
          ]
        }
      ) })
    ] }) }),
    /* @__PURE__ */ s.jsx(he, { title: "Evolution Settings", description: "Control population and exploration.", children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-2 gap-3", children: [
        /* @__PURE__ */ s.jsx(H, { label: "Max Iterations", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            min: "1",
            value: T.evolution.max_iterations,
            onChange: (z) => M({
              evolution: { max_iterations: Number(z.target.value) }
            })
          }
        ) }),
        /* @__PURE__ */ s.jsx(H, { label: "Population Size", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            min: "1",
            value: T.evolution.population_size,
            onChange: (z) => M({
              evolution: { population_size: Number(z.target.value) }
            })
          }
        ) })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-2 gap-3", children: [
        /* @__PURE__ */ s.jsx(H, { label: "Islands", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            min: "1",
            value: T.evolution.num_islands || 1,
            onChange: (z) => M({
              evolution: { num_islands: Number(z.target.value) }
            })
          }
        ) }),
        /* @__PURE__ */ s.jsx(H, { label: "Migration Interval", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            min: "1",
            value: T.evolution.migration_interval || 10,
            onChange: (z) => M({
              evolution: { migration_interval: Number(z.target.value) }
            })
          }
        ) })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-2 gap-3", children: [
        /* @__PURE__ */ s.jsx(H, { label: "Migration Rate", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            step: "0.05",
            min: "0",
            max: "1",
            value: T.evolution.migration_rate || 0.1,
            onChange: (z) => M({
              evolution: { migration_rate: Number(z.target.value) }
            })
          }
        ) }),
        /* @__PURE__ */ s.jsx(H, { label: "Archive Size", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            min: "0",
            value: T.evolution.archive_size || 0,
            onChange: (z) => M({
              evolution: { archive_size: Number(z.target.value) }
            })
          }
        ) })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-2 gap-3", children: [
        /* @__PURE__ */ s.jsx(H, { label: "Elite Ratio", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            step: "0.05",
            min: "0",
            max: "1",
            value: T.evolution.elite_ratio || 0.1,
            onChange: (z) => M({
              evolution: { elite_ratio: Number(z.target.value) }
            })
          }
        ) }),
        /* @__PURE__ */ s.jsx(H, { label: "Checkpoint Interval", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            min: "1",
            value: T.evolution.checkpoint_interval || 100,
            onChange: (z) => M({
              evolution: { checkpoint_interval: Number(z.target.value) }
            })
          }
        ) })
      ] }),
      /* @__PURE__ */ s.jsx(H, { label: "Feature Dimensions", hint: "Comma-separated feature tags for QD runs.", children: /* @__PURE__ */ s.jsx(
        Lt,
        {
          rows: 2,
          value: (T.evolution.feature_dimensions || []).join(", "),
          onChange: (z) => ee(z.target.value)
        }
      ) }),
      /* @__PURE__ */ s.jsx(H, { label: "Diversity Metric", children: /* @__PURE__ */ s.jsxs(
        je,
        {
          value: T.evolution.diversity_metric || "edit_distance",
          onChange: (z) => M({
            evolution: { diversity_metric: z.target.value }
          }),
          children: [
            /* @__PURE__ */ s.jsx("option", { value: "edit_distance", children: "Edit Distance" }),
            /* @__PURE__ */ s.jsx("option", { value: "semantic", children: "Semantic" }),
            /* @__PURE__ */ s.jsx("option", { value: "structure", children: "Structure" })
          ]
        }
      ) })
    ] }) }),
    /* @__PURE__ */ s.jsxs(he, { title: "Apply Settings", description: "Save global defaults to the backend.", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
        /* @__PURE__ */ s.jsx(
          Ze,
          {
            checked: l === "Global",
            onChange: () => f("Global"),
            label: "Use Global"
          }
        ),
        /* @__PURE__ */ s.jsx(
          X,
          {
            onClick: B,
            disabled: l !== "Global" || o,
            children: o ? "Saving..." : "Save Defaults"
          }
        )
      ] }),
      r && /* @__PURE__ */ s.jsx("p", { className: "mt-2 text-xs text-slate-400", children: "Loading configuration..." })
    ] })
  ] }) });
}
function So({ status: e, isConnected: t, className: r }) {
  const a = ((i) => {
    if (t !== void 0)
      return {
        color: t ? "bg-green-100 text-green-800" : "bg-gray-100 text-gray-800",
        dotColor: t ? "bg-green-500" : "bg-gray-500",
        label: t ? "Connected" : "Disconnected"
      };
    const o = {
      idle: {
        color: "bg-gray-100 text-gray-800",
        dotColor: "bg-gray-500",
        label: "Idle"
      },
      running: {
        color: "bg-blue-100 text-blue-800",
        dotColor: "bg-blue-500",
        label: "Running"
      },
      paused: {
        color: "bg-yellow-100 text-yellow-800",
        dotColor: "bg-yellow-500",
        label: "Paused"
      },
      completed: {
        color: "bg-green-100 text-green-800",
        dotColor: "bg-green-500",
        label: "Completed"
      },
      failed: {
        color: "bg-red-100 text-red-800",
        dotColor: "bg-red-500",
        label: "Failed"
      },
      stopped: {
        color: "bg-gray-100 text-gray-800",
        dotColor: "bg-gray-500",
        label: "Stopped"
      }
    };
    return o[i || "idle"] || o.idle;
  })(e);
  return /* @__PURE__ */ s.jsxs(
    "span",
    {
      className: Ie(
        "inline-flex items-center gap-1.5 px-2.5 py-0.5 rounded-full text-xs font-medium",
        a.color,
        r
      ),
      children: [
        /* @__PURE__ */ s.jsx(
          "span",
          {
            className: Ie("h-2 w-2 rounded-full animate-pulse", a.dotColor)
          }
        ),
        a.label
      ]
    }
  );
}
function u1({
  workflow: e,
  onClick: t,
  onExecute: r,
  onPause: n,
  onResume: a,
  onStop: i,
  onDelete: o,
  className: l
}) {
  const [c, u] = de(!1), d = (g, v) => {
    g.stopPropagation(), v();
  }, f = e.status === "idle" || e.status === "stopped" || e.status === "failed", p = e.status === "running", m = e.status === "paused", y = e.status === "running" || e.status === "paused", h = e.status === "completed" || e.status === "failed" || e.status === "stopped";
  return /* @__PURE__ */ s.jsxs(
    "div",
    {
      onClick: () => t == null ? void 0 : t(e),
      onMouseEnter: () => u(!0),
      onMouseLeave: () => u(!1),
      className: Ie(
        "workflow-card bg-white border border-gray-200 rounded-lg p-4 hover:shadow-lg transition-all cursor-pointer",
        l
      ),
      children: [
        /* @__PURE__ */ s.jsxs("div", { className: "flex items-start justify-between mb-3", children: [
          /* @__PURE__ */ s.jsxs("div", { className: "flex-1", children: [
            /* @__PURE__ */ s.jsx("h3", { className: "text-lg font-semibold text-gray-900 mb-1", children: e.name || `Workflow ${e.evolution_id.slice(0, 8)}` }),
            e.description && /* @__PURE__ */ s.jsx("p", { className: "text-sm text-gray-600 line-clamp-2", children: e.description })
          ] }),
          /* @__PURE__ */ s.jsx(So, { status: e.status })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "space-y-2 mb-3", children: [
          /* @__PURE__ */ s.jsxs("div", { className: "flex justify-between text-xs text-gray-500", children: [
            /* @__PURE__ */ s.jsx("span", { children: "Created" }),
            /* @__PURE__ */ s.jsx("span", { children: new Date(e.created_at).toLocaleString() })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "flex justify-between text-xs text-gray-500", children: [
            /* @__PURE__ */ s.jsx("span", { children: "Last Updated" }),
            /* @__PURE__ */ s.jsx("span", { children: new Date(e.updated_at).toLocaleString() })
          ] }),
          e.progress && /* @__PURE__ */ s.jsxs("div", { className: "flex justify-between text-xs text-gray-500", children: [
            /* @__PURE__ */ s.jsx("span", { children: "Progress" }),
            /* @__PURE__ */ s.jsxs("span", { children: [
              e.progress.current_iteration,
              " / ",
              e.progress.max_iterations
            ] })
          ] })
        ] }),
        e.progress && /* @__PURE__ */ s.jsx("div", { className: "mb-3", children: /* @__PURE__ */ s.jsx("div", { className: "w-full bg-gray-200 rounded-full h-2", children: /* @__PURE__ */ s.jsx(
          "div",
          {
            className: "bg-blue-600 h-2 rounded-full transition-all",
            style: { width: `${e.progress.percentage}%` }
          }
        ) }) }),
        /* @__PURE__ */ s.jsxs(
          "div",
          {
            className: Ie(
              "flex gap-2 transition-opacity",
              c ? "opacity-100" : "opacity-0"
            ),
            children: [
              f && r && /* @__PURE__ */ s.jsx(
                X,
                {
                  onClick: (g) => d(g, () => r(e.evolution_id)),
                  className: "px-3 py-1.5 text-xs",
                  children: "Execute"
                }
              ),
              p && n && /* @__PURE__ */ s.jsx(
                X,
                {
                  onClick: (g) => d(g, () => n(e.evolution_id)),
                  variant: "secondary",
                  className: "px-3 py-1.5 text-xs",
                  children: "Pause"
                }
              ),
              m && a && /* @__PURE__ */ s.jsx(
                X,
                {
                  onClick: (g) => d(g, () => a(e.evolution_id)),
                  className: "px-3 py-1.5 text-xs",
                  children: "Resume"
                }
              ),
              y && i && /* @__PURE__ */ s.jsx(
                X,
                {
                  onClick: (g) => d(g, () => i(e.evolution_id)),
                  variant: "secondary",
                  className: "px-3 py-1.5 text-xs",
                  children: "Stop"
                }
              ),
              h && o && /* @__PURE__ */ s.jsx(
                X,
                {
                  onClick: (g) => d(g, () => o(e.evolution_id)),
                  variant: "ghost",
                  className: "px-3 py-1.5 text-xs",
                  children: "Delete"
                }
              )
            ]
          }
        )
      ]
    }
  );
}
function d1({
  workflows: e,
  onWorkflowSelect: t,
  onExecute: r,
  onPause: n,
  onResume: a,
  onStop: i,
  onDelete: o,
  className: l
}) {
  const [c, u] = de(""), [d, f] = de("all"), p = e.filter((m) => {
    var g;
    const y = (((g = m.name) == null ? void 0 : g.toLowerCase()) || "").includes(c.toLowerCase()) || m.evolution_id.toLowerCase().includes(c.toLowerCase()), h = d === "all" || m.status === d;
    return y && h;
  });
  return /* @__PURE__ */ s.jsxs("div", { className: Ie("workflow-list", l), children: [
    /* @__PURE__ */ s.jsxs("div", { className: "mb-4 space-y-3", children: [
      /* @__PURE__ */ s.jsx(
        D,
        {
          type: "text",
          value: c,
          onChange: (m) => u(m.target.value),
          placeholder: "Search workflows by name or ID..."
        }
      ),
      /* @__PURE__ */ s.jsx("div", { className: "flex gap-2", children: ["all", "running", "paused", "completed", "failed"].map((m) => /* @__PURE__ */ s.jsx(
        X,
        {
          onClick: () => f(m),
          variant: d === m ? "primary" : "secondary",
          children: m.charAt(0).toUpperCase() + m.slice(1)
        },
        m
      )) })
    ] }),
    p.length === 0 ? /* @__PURE__ */ s.jsxs("div", { className: "text-center py-12 text-gray-500", children: [
      /* @__PURE__ */ s.jsx(
        "svg",
        {
          className: "mx-auto h-12 w-12 text-gray-400 mb-4",
          fill: "none",
          viewBox: "0 0 24 24",
          stroke: "currentColor",
          children: /* @__PURE__ */ s.jsx(
            "path",
            {
              strokeLinecap: "round",
              strokeLinejoin: "round",
              strokeWidth: 2,
              d: "M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"
            }
          )
        }
      ),
      /* @__PURE__ */ s.jsx("p", { className: "text-lg font-medium mb-1", children: "No workflows found" }),
      /* @__PURE__ */ s.jsx("p", { className: "text-sm", children: c || d !== "all" ? "Try adjusting your search or filters" : "Create your first workflow to get started" })
    ] }) : /* @__PURE__ */ s.jsx("div", { className: "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4", children: p.map((m) => /* @__PURE__ */ s.jsx(
      u1,
      {
        workflow: m,
        onClick: t,
        onExecute: r,
        onPause: n,
        onResume: a,
        onStop: i,
        onDelete: o
      },
      m.evolution_id
    )) })
  ] });
}
function f1(e, t = !0) {
  const [r, n] = de("disconnected"), [a, i] = de(null), [o, l] = de(null), c = Tr(null), u = Tr(/* @__PURE__ */ new Map()), d = ot((y, h) => {
    const g = u.current.get(y) || [];
    return u.current.set(y, [...g, h]), () => {
      const v = u.current.get(y) || [];
      u.current.set(
        y,
        v.filter((b) => b !== h)
      );
    };
  }, []), f = ot((y, h) => {
    var g;
    (g = c.current) == null || g.send(y, h);
  }, []), p = ot(() => {
    if (!t) return;
    const y = {
      onMessage: (h) => {
        i(h), (u.current.get(h.type) || []).forEach((v) => v(h.data));
      },
      onConnectionChange: (h) => {
        n(h);
      },
      onError: (h) => {
        l(h);
      }
    };
    c.current = e(y), c.current.connect();
  }, [t, e]), m = ot(() => {
    var y;
    (y = c.current) == null || y.disconnect(), c.current = null;
  }, []);
  return et(() => (t && p(), () => {
    m();
  }), [t, p, m]), {
    connectionState: r,
    data: a,
    error: o,
    send: f,
    on: d,
    isConnected: r === "connected",
    connect: p,
    disconnect: m
  };
}
function m1(e) {
  return f1(
    ot(
      (t) => {
        if (!e)
          throw new Error("Evolution ID is required");
        return wA(e, t);
      },
      [e]
    ),
    !!e
  );
}
function h1(e) {
  const t = Xa(), r = m1(e);
  return et(() => r.on("progress_update", (a) => {
    t.setQueryData(["evolution", e], (i) => i ? {
      ...i,
      ...a
    } : a);
  }), [r, e, t]), et(() => r.on("generation_complete", (a) => {
    t.invalidateQueries({ queryKey: ["evolution", e] });
  }), [r, e, t]), et(() => r.on("evolution_complete", (a) => {
    t.invalidateQueries({ queryKey: ["evolution", e] }), t.invalidateQueries({ queryKey: ["evolutions"] });
  }), [r, e, t]), r;
}
const p1 = (e, t) => {
  const r = new Array(e.length + t.length);
  for (let n = 0; n < e.length; n++)
    r[n] = e[n];
  for (let n = 0; n < t.length; n++)
    r[e.length + n] = t[n];
  return r;
}, g1 = (e, t) => ({
  classGroupId: e,
  validator: t
}), sj = (e = /* @__PURE__ */ new Map(), t = null, r) => ({
  nextPart: e,
  validators: t,
  classGroupId: r
}), ms = "-", Cg = [], y1 = "arbitrary..", v1 = (e) => {
  const t = x1(e), {
    conflictingClassGroups: r,
    conflictingClassGroupModifiers: n
  } = e;
  return {
    getClassGroupId: (o) => {
      if (o.startsWith("[") && o.endsWith("]"))
        return b1(o);
      const l = o.split(ms), c = l[0] === "" && l.length > 1 ? 1 : 0;
      return lj(l, c, t);
    },
    getConflictingClassGroupIds: (o, l) => {
      if (l) {
        const c = n[o], u = r[o];
        return c ? u ? p1(u, c) : c : u || Cg;
      }
      return r[o] || Cg;
    }
  };
}, lj = (e, t, r) => {
  if (e.length - t === 0)
    return r.classGroupId;
  const a = e[t], i = r.nextPart.get(a);
  if (i) {
    const u = lj(e, t + 1, i);
    if (u) return u;
  }
  const o = r.validators;
  if (o === null)
    return;
  const l = t === 0 ? e.join(ms) : e.slice(t).join(ms), c = o.length;
  for (let u = 0; u < c; u++) {
    const d = o[u];
    if (d.validator(l))
      return d.classGroupId;
  }
}, b1 = (e) => e.slice(1, -1).indexOf(":") === -1 ? void 0 : (() => {
  const t = e.slice(1, -1), r = t.indexOf(":"), n = t.slice(0, r);
  return n ? y1 + n : void 0;
})(), x1 = (e) => {
  const {
    theme: t,
    classGroups: r
  } = e;
  return w1(r, t);
}, w1 = (e, t) => {
  const r = sj();
  for (const n in e) {
    const a = e[n];
    Qh(a, r, n, t);
  }
  return r;
}, Qh = (e, t, r, n) => {
  const a = e.length;
  for (let i = 0; i < a; i++) {
    const o = e[i];
    _1(o, t, r, n);
  }
}, _1 = (e, t, r, n) => {
  if (typeof e == "string") {
    j1(e, t, r);
    return;
  }
  if (typeof e == "function") {
    S1(e, t, r, n);
    return;
  }
  k1(e, t, r, n);
}, j1 = (e, t, r) => {
  const n = e === "" ? t : cj(t, e);
  n.classGroupId = r;
}, S1 = (e, t, r, n) => {
  if (E1(e)) {
    Qh(e(n), t, r, n);
    return;
  }
  t.validators === null && (t.validators = []), t.validators.push(g1(r, e));
}, k1 = (e, t, r, n) => {
  const a = Object.entries(e), i = a.length;
  for (let o = 0; o < i; o++) {
    const [l, c] = a[o];
    Qh(c, cj(t, l), r, n);
  }
}, cj = (e, t) => {
  let r = e;
  const n = t.split(ms), a = n.length;
  for (let i = 0; i < a; i++) {
    const o = n[i];
    let l = r.nextPart.get(o);
    l || (l = sj(), r.nextPart.set(o, l)), r = l;
  }
  return r;
}, E1 = (e) => "isThemeGetter" in e && e.isThemeGetter === !0, A1 = (e) => {
  if (e < 1)
    return {
      get: () => {
      },
      set: () => {
      }
    };
  let t = 0, r = /* @__PURE__ */ Object.create(null), n = /* @__PURE__ */ Object.create(null);
  const a = (i, o) => {
    r[i] = o, t++, t > e && (t = 0, n = r, r = /* @__PURE__ */ Object.create(null));
  };
  return {
    get(i) {
      let o = r[i];
      if (o !== void 0)
        return o;
      if ((o = n[i]) !== void 0)
        return a(i, o), o;
    },
    set(i, o) {
      i in r ? r[i] = o : a(i, o);
    }
  };
}, km = "!", Tg = ":", C1 = [], Pg = (e, t, r, n, a) => ({
  modifiers: e,
  hasImportantModifier: t,
  baseClassName: r,
  maybePostfixModifierPosition: n,
  isExternal: a
}), T1 = (e) => {
  const {
    prefix: t,
    experimentalParseClassName: r
  } = e;
  let n = (a) => {
    const i = [];
    let o = 0, l = 0, c = 0, u;
    const d = a.length;
    for (let h = 0; h < d; h++) {
      const g = a[h];
      if (o === 0 && l === 0) {
        if (g === Tg) {
          i.push(a.slice(c, h)), c = h + 1;
          continue;
        }
        if (g === "/") {
          u = h;
          continue;
        }
      }
      g === "[" ? o++ : g === "]" ? o-- : g === "(" ? l++ : g === ")" && l--;
    }
    const f = i.length === 0 ? a : a.slice(c);
    let p = f, m = !1;
    f.endsWith(km) ? (p = f.slice(0, -1), m = !0) : (
      /**
       * In Tailwind CSS v3 the important modifier was at the start of the base class name. This is still supported for legacy reasons.
       * @see https://github.com/dcastil/tailwind-merge/issues/513#issuecomment-2614029864
       */
      f.startsWith(km) && (p = f.slice(1), m = !0)
    );
    const y = u && u > c ? u - c : void 0;
    return Pg(i, m, p, y);
  };
  if (t) {
    const a = t + Tg, i = n;
    n = (o) => o.startsWith(a) ? i(o.slice(a.length)) : Pg(C1, !1, o, void 0, !0);
  }
  if (r) {
    const a = n;
    n = (i) => r({
      className: i,
      parseClassName: a
    });
  }
  return n;
}, P1 = (e) => {
  const t = /* @__PURE__ */ new Map();
  return e.orderSensitiveModifiers.forEach((r, n) => {
    t.set(r, 1e6 + n);
  }), (r) => {
    const n = [];
    let a = [];
    for (let i = 0; i < r.length; i++) {
      const o = r[i], l = o[0] === "[", c = t.has(o);
      l || c ? (a.length > 0 && (a.sort(), n.push(...a), a = []), n.push(o)) : a.push(o);
    }
    return a.length > 0 && (a.sort(), n.push(...a)), n;
  };
}, O1 = (e) => ({
  cache: A1(e.cacheSize),
  parseClassName: T1(e),
  sortModifiers: P1(e),
  ...v1(e)
}), N1 = /\s+/, M1 = (e, t) => {
  const {
    parseClassName: r,
    getClassGroupId: n,
    getConflictingClassGroupIds: a,
    sortModifiers: i
  } = t, o = [], l = e.trim().split(N1);
  let c = "";
  for (let u = l.length - 1; u >= 0; u -= 1) {
    const d = l[u], {
      isExternal: f,
      modifiers: p,
      hasImportantModifier: m,
      baseClassName: y,
      maybePostfixModifierPosition: h
    } = r(d);
    if (f) {
      c = d + (c.length > 0 ? " " + c : c);
      continue;
    }
    let g = !!h, v = n(g ? y.substring(0, h) : y);
    if (!v) {
      if (!g) {
        c = d + (c.length > 0 ? " " + c : c);
        continue;
      }
      if (v = n(y), !v) {
        c = d + (c.length > 0 ? " " + c : c);
        continue;
      }
      g = !1;
    }
    const b = p.length === 0 ? "" : p.length === 1 ? p[0] : i(p).join(":"), w = m ? b + km : b, j = w + v;
    if (o.indexOf(j) > -1)
      continue;
    o.push(j);
    const x = a(v, g);
    for (let _ = 0; _ < x.length; ++_) {
      const S = x[_];
      o.push(w + S);
    }
    c = d + (c.length > 0 ? " " + c : c);
  }
  return c;
}, R1 = (...e) => {
  let t = 0, r, n, a = "";
  for (; t < e.length; )
    (r = e[t++]) && (n = uj(r)) && (a && (a += " "), a += n);
  return a;
}, uj = (e) => {
  if (typeof e == "string")
    return e;
  let t, r = "";
  for (let n = 0; n < e.length; n++)
    e[n] && (t = uj(e[n])) && (r && (r += " "), r += t);
  return r;
}, I1 = (e, ...t) => {
  let r, n, a, i;
  const o = (c) => {
    const u = t.reduce((d, f) => f(d), e());
    return r = O1(u), n = r.cache.get, a = r.cache.set, i = l, l(c);
  }, l = (c) => {
    const u = n(c);
    if (u)
      return u;
    const d = M1(c, r);
    return a(c, d), d;
  };
  return i = o, (...c) => i(R1(...c));
}, D1 = [], kt = (e) => {
  const t = (r) => r[e] || D1;
  return t.isThemeGetter = !0, t;
}, dj = /^\[(?:(\w[\w-]*):)?(.+)\]$/i, fj = /^\((?:(\w[\w-]*):)?(.+)\)$/i, $1 = /^\d+\/\d+$/, L1 = /^(\d+(\.\d+)?)?(xs|sm|md|lg|xl)$/, F1 = /\d+(%|px|r?em|[sdl]?v([hwib]|min|max)|pt|pc|in|cm|mm|cap|ch|ex|r?lh|cq(w|h|i|b|min|max))|\b(calc|min|max|clamp)\(.+\)|^0$/, q1 = /^(rgba?|hsla?|hwb|(ok)?(lab|lch)|color-mix)\(.+\)$/, U1 = /^(inset_)?-?((\d+)?\.?(\d+)[a-z]+|0)_-?((\d+)?\.?(\d+)[a-z]+|0)/, z1 = /^(url|image|image-set|cross-fade|element|(repeating-)?(linear|radial|conic)-gradient)\(.+\)$/, ia = (e) => $1.test(e), De = (e) => !!e && !Number.isNaN(Number(e)), dn = (e) => !!e && Number.isInteger(Number(e)), dc = (e) => e.endsWith("%") && De(e.slice(0, -1)), qr = (e) => L1.test(e), B1 = () => !0, W1 = (e) => (
  // `colorFunctionRegex` check is necessary because color functions can have percentages in them which which would be incorrectly classified as lengths.
  // For example, `hsl(0 0% 0%)` would be classified as a length without this check.
  // I could also use lookbehind assertion in `lengthUnitRegex` but that isn't supported widely enough.
  F1.test(e) && !q1.test(e)
), mj = () => !1, V1 = (e) => U1.test(e), H1 = (e) => z1.test(e), G1 = (e) => !be(e) && !xe(e), K1 = (e) => Ja(e, gj, mj), be = (e) => dj.test(e), Tn = (e) => Ja(e, yj, W1), fc = (e) => Ja(e, Z1, De), Og = (e) => Ja(e, hj, mj), Y1 = (e) => Ja(e, pj, H1), Ho = (e) => Ja(e, vj, V1), xe = (e) => fj.test(e), vi = (e) => Za(e, yj), X1 = (e) => Za(e, eC), Ng = (e) => Za(e, hj), Q1 = (e) => Za(e, gj), J1 = (e) => Za(e, pj), Go = (e) => Za(e, vj, !0), Ja = (e, t, r) => {
  const n = dj.exec(e);
  return n ? n[1] ? t(n[1]) : r(n[2]) : !1;
}, Za = (e, t, r = !1) => {
  const n = fj.exec(e);
  return n ? n[1] ? t(n[1]) : r : !1;
}, hj = (e) => e === "position" || e === "percentage", pj = (e) => e === "image" || e === "url", gj = (e) => e === "length" || e === "size" || e === "bg-size", yj = (e) => e === "length", Z1 = (e) => e === "number", eC = (e) => e === "family-name", vj = (e) => e === "shadow", tC = () => {
  const e = kt("color"), t = kt("font"), r = kt("text"), n = kt("font-weight"), a = kt("tracking"), i = kt("leading"), o = kt("breakpoint"), l = kt("container"), c = kt("spacing"), u = kt("radius"), d = kt("shadow"), f = kt("inset-shadow"), p = kt("text-shadow"), m = kt("drop-shadow"), y = kt("blur"), h = kt("perspective"), g = kt("aspect"), v = kt("ease"), b = kt("animate"), w = () => ["auto", "avoid", "all", "avoid-page", "page", "left", "right", "column"], j = () => [
    "center",
    "top",
    "bottom",
    "left",
    "right",
    "top-left",
    // Deprecated since Tailwind CSS v4.1.0, see https://github.com/tailwindlabs/tailwindcss/pull/17378
    "left-top",
    "top-right",
    // Deprecated since Tailwind CSS v4.1.0, see https://github.com/tailwindlabs/tailwindcss/pull/17378
    "right-top",
    "bottom-right",
    // Deprecated since Tailwind CSS v4.1.0, see https://github.com/tailwindlabs/tailwindcss/pull/17378
    "right-bottom",
    "bottom-left",
    // Deprecated since Tailwind CSS v4.1.0, see https://github.com/tailwindlabs/tailwindcss/pull/17378
    "left-bottom"
  ], x = () => [...j(), xe, be], _ = () => ["auto", "hidden", "clip", "visible", "scroll"], S = () => ["auto", "contain", "none"], k = () => [xe, be, c], E = () => [ia, "full", "auto", ...k()], L = () => [dn, "none", "subgrid", xe, be], P = () => ["auto", {
    span: ["full", dn, xe, be]
  }, dn, xe, be], A = () => [dn, "auto", xe, be], T = () => ["auto", "min", "max", "fr", xe, be], M = () => ["start", "end", "center", "between", "around", "evenly", "stretch", "baseline", "center-safe", "end-safe"], N = () => ["start", "end", "center", "stretch", "center-safe", "end-safe"], q = () => ["auto", ...k()], V = () => [ia, "auto", "full", "dvw", "dvh", "lvw", "lvh", "svw", "svh", "min", "max", "fit", ...k()], B = () => [e, xe, be], ee = () => [...j(), Ng, Og, {
    position: [xe, be]
  }], U = () => ["no-repeat", {
    repeat: ["", "x", "y", "space", "round"]
  }], G = () => ["auto", "cover", "contain", Q1, K1, {
    size: [xe, be]
  }], Y = () => [dc, vi, Tn], Q = () => [
    // Deprecated since Tailwind CSS v4.0.0
    "",
    "none",
    "full",
    u,
    xe,
    be
  ], ne = () => ["", De, vi, Tn], z = () => ["solid", "dashed", "dotted", "double"], se = () => ["normal", "multiply", "screen", "overlay", "darken", "lighten", "color-dodge", "color-burn", "hard-light", "soft-light", "difference", "exclusion", "hue", "saturation", "color", "luminosity"], oe = () => [De, dc, Ng, Og], ie = () => [
    // Deprecated since Tailwind CSS v4.0.0
    "",
    "none",
    y,
    xe,
    be
  ], K = () => ["none", De, xe, be], ae = () => ["none", De, xe, be], fe = () => [De, xe, be], $ = () => [ia, "full", ...k()];
  return {
    cacheSize: 500,
    theme: {
      animate: ["spin", "ping", "pulse", "bounce"],
      aspect: ["video"],
      blur: [qr],
      breakpoint: [qr],
      color: [B1],
      container: [qr],
      "drop-shadow": [qr],
      ease: ["in", "out", "in-out"],
      font: [G1],
      "font-weight": ["thin", "extralight", "light", "normal", "medium", "semibold", "bold", "extrabold", "black"],
      "inset-shadow": [qr],
      leading: ["none", "tight", "snug", "normal", "relaxed", "loose"],
      perspective: ["dramatic", "near", "normal", "midrange", "distant", "none"],
      radius: [qr],
      shadow: [qr],
      spacing: ["px", De],
      text: [qr],
      "text-shadow": [qr],
      tracking: ["tighter", "tight", "normal", "wide", "wider", "widest"]
    },
    classGroups: {
      // --------------
      // --- Layout ---
      // --------------
      /**
       * Aspect Ratio
       * @see https://tailwindcss.com/docs/aspect-ratio
       */
      aspect: [{
        aspect: ["auto", "square", ia, be, xe, g]
      }],
      /**
       * Container
       * @see https://tailwindcss.com/docs/container
       * @deprecated since Tailwind CSS v4.0.0
       */
      container: ["container"],
      /**
       * Columns
       * @see https://tailwindcss.com/docs/columns
       */
      columns: [{
        columns: [De, be, xe, l]
      }],
      /**
       * Break After
       * @see https://tailwindcss.com/docs/break-after
       */
      "break-after": [{
        "break-after": w()
      }],
      /**
       * Break Before
       * @see https://tailwindcss.com/docs/break-before
       */
      "break-before": [{
        "break-before": w()
      }],
      /**
       * Break Inside
       * @see https://tailwindcss.com/docs/break-inside
       */
      "break-inside": [{
        "break-inside": ["auto", "avoid", "avoid-page", "avoid-column"]
      }],
      /**
       * Box Decoration Break
       * @see https://tailwindcss.com/docs/box-decoration-break
       */
      "box-decoration": [{
        "box-decoration": ["slice", "clone"]
      }],
      /**
       * Box Sizing
       * @see https://tailwindcss.com/docs/box-sizing
       */
      box: [{
        box: ["border", "content"]
      }],
      /**
       * Display
       * @see https://tailwindcss.com/docs/display
       */
      display: ["block", "inline-block", "inline", "flex", "inline-flex", "table", "inline-table", "table-caption", "table-cell", "table-column", "table-column-group", "table-footer-group", "table-header-group", "table-row-group", "table-row", "flow-root", "grid", "inline-grid", "contents", "list-item", "hidden"],
      /**
       * Screen Reader Only
       * @see https://tailwindcss.com/docs/display#screen-reader-only
       */
      sr: ["sr-only", "not-sr-only"],
      /**
       * Floats
       * @see https://tailwindcss.com/docs/float
       */
      float: [{
        float: ["right", "left", "none", "start", "end"]
      }],
      /**
       * Clear
       * @see https://tailwindcss.com/docs/clear
       */
      clear: [{
        clear: ["left", "right", "both", "none", "start", "end"]
      }],
      /**
       * Isolation
       * @see https://tailwindcss.com/docs/isolation
       */
      isolation: ["isolate", "isolation-auto"],
      /**
       * Object Fit
       * @see https://tailwindcss.com/docs/object-fit
       */
      "object-fit": [{
        object: ["contain", "cover", "fill", "none", "scale-down"]
      }],
      /**
       * Object Position
       * @see https://tailwindcss.com/docs/object-position
       */
      "object-position": [{
        object: x()
      }],
      /**
       * Overflow
       * @see https://tailwindcss.com/docs/overflow
       */
      overflow: [{
        overflow: _()
      }],
      /**
       * Overflow X
       * @see https://tailwindcss.com/docs/overflow
       */
      "overflow-x": [{
        "overflow-x": _()
      }],
      /**
       * Overflow Y
       * @see https://tailwindcss.com/docs/overflow
       */
      "overflow-y": [{
        "overflow-y": _()
      }],
      /**
       * Overscroll Behavior
       * @see https://tailwindcss.com/docs/overscroll-behavior
       */
      overscroll: [{
        overscroll: S()
      }],
      /**
       * Overscroll Behavior X
       * @see https://tailwindcss.com/docs/overscroll-behavior
       */
      "overscroll-x": [{
        "overscroll-x": S()
      }],
      /**
       * Overscroll Behavior Y
       * @see https://tailwindcss.com/docs/overscroll-behavior
       */
      "overscroll-y": [{
        "overscroll-y": S()
      }],
      /**
       * Position
       * @see https://tailwindcss.com/docs/position
       */
      position: ["static", "fixed", "absolute", "relative", "sticky"],
      /**
       * Top / Right / Bottom / Left
       * @see https://tailwindcss.com/docs/top-right-bottom-left
       */
      inset: [{
        inset: E()
      }],
      /**
       * Right / Left
       * @see https://tailwindcss.com/docs/top-right-bottom-left
       */
      "inset-x": [{
        "inset-x": E()
      }],
      /**
       * Top / Bottom
       * @see https://tailwindcss.com/docs/top-right-bottom-left
       */
      "inset-y": [{
        "inset-y": E()
      }],
      /**
       * Start
       * @see https://tailwindcss.com/docs/top-right-bottom-left
       */
      start: [{
        start: E()
      }],
      /**
       * End
       * @see https://tailwindcss.com/docs/top-right-bottom-left
       */
      end: [{
        end: E()
      }],
      /**
       * Top
       * @see https://tailwindcss.com/docs/top-right-bottom-left
       */
      top: [{
        top: E()
      }],
      /**
       * Right
       * @see https://tailwindcss.com/docs/top-right-bottom-left
       */
      right: [{
        right: E()
      }],
      /**
       * Bottom
       * @see https://tailwindcss.com/docs/top-right-bottom-left
       */
      bottom: [{
        bottom: E()
      }],
      /**
       * Left
       * @see https://tailwindcss.com/docs/top-right-bottom-left
       */
      left: [{
        left: E()
      }],
      /**
       * Visibility
       * @see https://tailwindcss.com/docs/visibility
       */
      visibility: ["visible", "invisible", "collapse"],
      /**
       * Z-Index
       * @see https://tailwindcss.com/docs/z-index
       */
      z: [{
        z: [dn, "auto", xe, be]
      }],
      // ------------------------
      // --- Flexbox and Grid ---
      // ------------------------
      /**
       * Flex Basis
       * @see https://tailwindcss.com/docs/flex-basis
       */
      basis: [{
        basis: [ia, "full", "auto", l, ...k()]
      }],
      /**
       * Flex Direction
       * @see https://tailwindcss.com/docs/flex-direction
       */
      "flex-direction": [{
        flex: ["row", "row-reverse", "col", "col-reverse"]
      }],
      /**
       * Flex Wrap
       * @see https://tailwindcss.com/docs/flex-wrap
       */
      "flex-wrap": [{
        flex: ["nowrap", "wrap", "wrap-reverse"]
      }],
      /**
       * Flex
       * @see https://tailwindcss.com/docs/flex
       */
      flex: [{
        flex: [De, ia, "auto", "initial", "none", be]
      }],
      /**
       * Flex Grow
       * @see https://tailwindcss.com/docs/flex-grow
       */
      grow: [{
        grow: ["", De, xe, be]
      }],
      /**
       * Flex Shrink
       * @see https://tailwindcss.com/docs/flex-shrink
       */
      shrink: [{
        shrink: ["", De, xe, be]
      }],
      /**
       * Order
       * @see https://tailwindcss.com/docs/order
       */
      order: [{
        order: [dn, "first", "last", "none", xe, be]
      }],
      /**
       * Grid Template Columns
       * @see https://tailwindcss.com/docs/grid-template-columns
       */
      "grid-cols": [{
        "grid-cols": L()
      }],
      /**
       * Grid Column Start / End
       * @see https://tailwindcss.com/docs/grid-column
       */
      "col-start-end": [{
        col: P()
      }],
      /**
       * Grid Column Start
       * @see https://tailwindcss.com/docs/grid-column
       */
      "col-start": [{
        "col-start": A()
      }],
      /**
       * Grid Column End
       * @see https://tailwindcss.com/docs/grid-column
       */
      "col-end": [{
        "col-end": A()
      }],
      /**
       * Grid Template Rows
       * @see https://tailwindcss.com/docs/grid-template-rows
       */
      "grid-rows": [{
        "grid-rows": L()
      }],
      /**
       * Grid Row Start / End
       * @see https://tailwindcss.com/docs/grid-row
       */
      "row-start-end": [{
        row: P()
      }],
      /**
       * Grid Row Start
       * @see https://tailwindcss.com/docs/grid-row
       */
      "row-start": [{
        "row-start": A()
      }],
      /**
       * Grid Row End
       * @see https://tailwindcss.com/docs/grid-row
       */
      "row-end": [{
        "row-end": A()
      }],
      /**
       * Grid Auto Flow
       * @see https://tailwindcss.com/docs/grid-auto-flow
       */
      "grid-flow": [{
        "grid-flow": ["row", "col", "dense", "row-dense", "col-dense"]
      }],
      /**
       * Grid Auto Columns
       * @see https://tailwindcss.com/docs/grid-auto-columns
       */
      "auto-cols": [{
        "auto-cols": T()
      }],
      /**
       * Grid Auto Rows
       * @see https://tailwindcss.com/docs/grid-auto-rows
       */
      "auto-rows": [{
        "auto-rows": T()
      }],
      /**
       * Gap
       * @see https://tailwindcss.com/docs/gap
       */
      gap: [{
        gap: k()
      }],
      /**
       * Gap X
       * @see https://tailwindcss.com/docs/gap
       */
      "gap-x": [{
        "gap-x": k()
      }],
      /**
       * Gap Y
       * @see https://tailwindcss.com/docs/gap
       */
      "gap-y": [{
        "gap-y": k()
      }],
      /**
       * Justify Content
       * @see https://tailwindcss.com/docs/justify-content
       */
      "justify-content": [{
        justify: [...M(), "normal"]
      }],
      /**
       * Justify Items
       * @see https://tailwindcss.com/docs/justify-items
       */
      "justify-items": [{
        "justify-items": [...N(), "normal"]
      }],
      /**
       * Justify Self
       * @see https://tailwindcss.com/docs/justify-self
       */
      "justify-self": [{
        "justify-self": ["auto", ...N()]
      }],
      /**
       * Align Content
       * @see https://tailwindcss.com/docs/align-content
       */
      "align-content": [{
        content: ["normal", ...M()]
      }],
      /**
       * Align Items
       * @see https://tailwindcss.com/docs/align-items
       */
      "align-items": [{
        items: [...N(), {
          baseline: ["", "last"]
        }]
      }],
      /**
       * Align Self
       * @see https://tailwindcss.com/docs/align-self
       */
      "align-self": [{
        self: ["auto", ...N(), {
          baseline: ["", "last"]
        }]
      }],
      /**
       * Place Content
       * @see https://tailwindcss.com/docs/place-content
       */
      "place-content": [{
        "place-content": M()
      }],
      /**
       * Place Items
       * @see https://tailwindcss.com/docs/place-items
       */
      "place-items": [{
        "place-items": [...N(), "baseline"]
      }],
      /**
       * Place Self
       * @see https://tailwindcss.com/docs/place-self
       */
      "place-self": [{
        "place-self": ["auto", ...N()]
      }],
      // Spacing
      /**
       * Padding
       * @see https://tailwindcss.com/docs/padding
       */
      p: [{
        p: k()
      }],
      /**
       * Padding X
       * @see https://tailwindcss.com/docs/padding
       */
      px: [{
        px: k()
      }],
      /**
       * Padding Y
       * @see https://tailwindcss.com/docs/padding
       */
      py: [{
        py: k()
      }],
      /**
       * Padding Start
       * @see https://tailwindcss.com/docs/padding
       */
      ps: [{
        ps: k()
      }],
      /**
       * Padding End
       * @see https://tailwindcss.com/docs/padding
       */
      pe: [{
        pe: k()
      }],
      /**
       * Padding Top
       * @see https://tailwindcss.com/docs/padding
       */
      pt: [{
        pt: k()
      }],
      /**
       * Padding Right
       * @see https://tailwindcss.com/docs/padding
       */
      pr: [{
        pr: k()
      }],
      /**
       * Padding Bottom
       * @see https://tailwindcss.com/docs/padding
       */
      pb: [{
        pb: k()
      }],
      /**
       * Padding Left
       * @see https://tailwindcss.com/docs/padding
       */
      pl: [{
        pl: k()
      }],
      /**
       * Margin
       * @see https://tailwindcss.com/docs/margin
       */
      m: [{
        m: q()
      }],
      /**
       * Margin X
       * @see https://tailwindcss.com/docs/margin
       */
      mx: [{
        mx: q()
      }],
      /**
       * Margin Y
       * @see https://tailwindcss.com/docs/margin
       */
      my: [{
        my: q()
      }],
      /**
       * Margin Start
       * @see https://tailwindcss.com/docs/margin
       */
      ms: [{
        ms: q()
      }],
      /**
       * Margin End
       * @see https://tailwindcss.com/docs/margin
       */
      me: [{
        me: q()
      }],
      /**
       * Margin Top
       * @see https://tailwindcss.com/docs/margin
       */
      mt: [{
        mt: q()
      }],
      /**
       * Margin Right
       * @see https://tailwindcss.com/docs/margin
       */
      mr: [{
        mr: q()
      }],
      /**
       * Margin Bottom
       * @see https://tailwindcss.com/docs/margin
       */
      mb: [{
        mb: q()
      }],
      /**
       * Margin Left
       * @see https://tailwindcss.com/docs/margin
       */
      ml: [{
        ml: q()
      }],
      /**
       * Space Between X
       * @see https://tailwindcss.com/docs/margin#adding-space-between-children
       */
      "space-x": [{
        "space-x": k()
      }],
      /**
       * Space Between X Reverse
       * @see https://tailwindcss.com/docs/margin#adding-space-between-children
       */
      "space-x-reverse": ["space-x-reverse"],
      /**
       * Space Between Y
       * @see https://tailwindcss.com/docs/margin#adding-space-between-children
       */
      "space-y": [{
        "space-y": k()
      }],
      /**
       * Space Between Y Reverse
       * @see https://tailwindcss.com/docs/margin#adding-space-between-children
       */
      "space-y-reverse": ["space-y-reverse"],
      // --------------
      // --- Sizing ---
      // --------------
      /**
       * Size
       * @see https://tailwindcss.com/docs/width#setting-both-width-and-height
       */
      size: [{
        size: V()
      }],
      /**
       * Width
       * @see https://tailwindcss.com/docs/width
       */
      w: [{
        w: [l, "screen", ...V()]
      }],
      /**
       * Min-Width
       * @see https://tailwindcss.com/docs/min-width
       */
      "min-w": [{
        "min-w": [
          l,
          "screen",
          /** Deprecated. @see https://github.com/tailwindlabs/tailwindcss.com/issues/2027#issuecomment-2620152757 */
          "none",
          ...V()
        ]
      }],
      /**
       * Max-Width
       * @see https://tailwindcss.com/docs/max-width
       */
      "max-w": [{
        "max-w": [
          l,
          "screen",
          "none",
          /** Deprecated since Tailwind CSS v4.0.0. @see https://github.com/tailwindlabs/tailwindcss.com/issues/2027#issuecomment-2620152757 */
          "prose",
          /** Deprecated since Tailwind CSS v4.0.0. @see https://github.com/tailwindlabs/tailwindcss.com/issues/2027#issuecomment-2620152757 */
          {
            screen: [o]
          },
          ...V()
        ]
      }],
      /**
       * Height
       * @see https://tailwindcss.com/docs/height
       */
      h: [{
        h: ["screen", "lh", ...V()]
      }],
      /**
       * Min-Height
       * @see https://tailwindcss.com/docs/min-height
       */
      "min-h": [{
        "min-h": ["screen", "lh", "none", ...V()]
      }],
      /**
       * Max-Height
       * @see https://tailwindcss.com/docs/max-height
       */
      "max-h": [{
        "max-h": ["screen", "lh", ...V()]
      }],
      // ------------------
      // --- Typography ---
      // ------------------
      /**
       * Font Size
       * @see https://tailwindcss.com/docs/font-size
       */
      "font-size": [{
        text: ["base", r, vi, Tn]
      }],
      /**
       * Font Smoothing
       * @see https://tailwindcss.com/docs/font-smoothing
       */
      "font-smoothing": ["antialiased", "subpixel-antialiased"],
      /**
       * Font Style
       * @see https://tailwindcss.com/docs/font-style
       */
      "font-style": ["italic", "not-italic"],
      /**
       * Font Weight
       * @see https://tailwindcss.com/docs/font-weight
       */
      "font-weight": [{
        font: [n, xe, fc]
      }],
      /**
       * Font Stretch
       * @see https://tailwindcss.com/docs/font-stretch
       */
      "font-stretch": [{
        "font-stretch": ["ultra-condensed", "extra-condensed", "condensed", "semi-condensed", "normal", "semi-expanded", "expanded", "extra-expanded", "ultra-expanded", dc, be]
      }],
      /**
       * Font Family
       * @see https://tailwindcss.com/docs/font-family
       */
      "font-family": [{
        font: [X1, be, t]
      }],
      /**
       * Font Variant Numeric
       * @see https://tailwindcss.com/docs/font-variant-numeric
       */
      "fvn-normal": ["normal-nums"],
      /**
       * Font Variant Numeric
       * @see https://tailwindcss.com/docs/font-variant-numeric
       */
      "fvn-ordinal": ["ordinal"],
      /**
       * Font Variant Numeric
       * @see https://tailwindcss.com/docs/font-variant-numeric
       */
      "fvn-slashed-zero": ["slashed-zero"],
      /**
       * Font Variant Numeric
       * @see https://tailwindcss.com/docs/font-variant-numeric
       */
      "fvn-figure": ["lining-nums", "oldstyle-nums"],
      /**
       * Font Variant Numeric
       * @see https://tailwindcss.com/docs/font-variant-numeric
       */
      "fvn-spacing": ["proportional-nums", "tabular-nums"],
      /**
       * Font Variant Numeric
       * @see https://tailwindcss.com/docs/font-variant-numeric
       */
      "fvn-fraction": ["diagonal-fractions", "stacked-fractions"],
      /**
       * Letter Spacing
       * @see https://tailwindcss.com/docs/letter-spacing
       */
      tracking: [{
        tracking: [a, xe, be]
      }],
      /**
       * Line Clamp
       * @see https://tailwindcss.com/docs/line-clamp
       */
      "line-clamp": [{
        "line-clamp": [De, "none", xe, fc]
      }],
      /**
       * Line Height
       * @see https://tailwindcss.com/docs/line-height
       */
      leading: [{
        leading: [
          /** Deprecated since Tailwind CSS v4.0.0. @see https://github.com/tailwindlabs/tailwindcss.com/issues/2027#issuecomment-2620152757 */
          i,
          ...k()
        ]
      }],
      /**
       * List Style Image
       * @see https://tailwindcss.com/docs/list-style-image
       */
      "list-image": [{
        "list-image": ["none", xe, be]
      }],
      /**
       * List Style Position
       * @see https://tailwindcss.com/docs/list-style-position
       */
      "list-style-position": [{
        list: ["inside", "outside"]
      }],
      /**
       * List Style Type
       * @see https://tailwindcss.com/docs/list-style-type
       */
      "list-style-type": [{
        list: ["disc", "decimal", "none", xe, be]
      }],
      /**
       * Text Alignment
       * @see https://tailwindcss.com/docs/text-align
       */
      "text-alignment": [{
        text: ["left", "center", "right", "justify", "start", "end"]
      }],
      /**
       * Placeholder Color
       * @deprecated since Tailwind CSS v3.0.0
       * @see https://v3.tailwindcss.com/docs/placeholder-color
       */
      "placeholder-color": [{
        placeholder: B()
      }],
      /**
       * Text Color
       * @see https://tailwindcss.com/docs/text-color
       */
      "text-color": [{
        text: B()
      }],
      /**
       * Text Decoration
       * @see https://tailwindcss.com/docs/text-decoration
       */
      "text-decoration": ["underline", "overline", "line-through", "no-underline"],
      /**
       * Text Decoration Style
       * @see https://tailwindcss.com/docs/text-decoration-style
       */
      "text-decoration-style": [{
        decoration: [...z(), "wavy"]
      }],
      /**
       * Text Decoration Thickness
       * @see https://tailwindcss.com/docs/text-decoration-thickness
       */
      "text-decoration-thickness": [{
        decoration: [De, "from-font", "auto", xe, Tn]
      }],
      /**
       * Text Decoration Color
       * @see https://tailwindcss.com/docs/text-decoration-color
       */
      "text-decoration-color": [{
        decoration: B()
      }],
      /**
       * Text Underline Offset
       * @see https://tailwindcss.com/docs/text-underline-offset
       */
      "underline-offset": [{
        "underline-offset": [De, "auto", xe, be]
      }],
      /**
       * Text Transform
       * @see https://tailwindcss.com/docs/text-transform
       */
      "text-transform": ["uppercase", "lowercase", "capitalize", "normal-case"],
      /**
       * Text Overflow
       * @see https://tailwindcss.com/docs/text-overflow
       */
      "text-overflow": ["truncate", "text-ellipsis", "text-clip"],
      /**
       * Text Wrap
       * @see https://tailwindcss.com/docs/text-wrap
       */
      "text-wrap": [{
        text: ["wrap", "nowrap", "balance", "pretty"]
      }],
      /**
       * Text Indent
       * @see https://tailwindcss.com/docs/text-indent
       */
      indent: [{
        indent: k()
      }],
      /**
       * Vertical Alignment
       * @see https://tailwindcss.com/docs/vertical-align
       */
      "vertical-align": [{
        align: ["baseline", "top", "middle", "bottom", "text-top", "text-bottom", "sub", "super", xe, be]
      }],
      /**
       * Whitespace
       * @see https://tailwindcss.com/docs/whitespace
       */
      whitespace: [{
        whitespace: ["normal", "nowrap", "pre", "pre-line", "pre-wrap", "break-spaces"]
      }],
      /**
       * Word Break
       * @see https://tailwindcss.com/docs/word-break
       */
      break: [{
        break: ["normal", "words", "all", "keep"]
      }],
      /**
       * Overflow Wrap
       * @see https://tailwindcss.com/docs/overflow-wrap
       */
      wrap: [{
        wrap: ["break-word", "anywhere", "normal"]
      }],
      /**
       * Hyphens
       * @see https://tailwindcss.com/docs/hyphens
       */
      hyphens: [{
        hyphens: ["none", "manual", "auto"]
      }],
      /**
       * Content
       * @see https://tailwindcss.com/docs/content
       */
      content: [{
        content: ["none", xe, be]
      }],
      // -------------------
      // --- Backgrounds ---
      // -------------------
      /**
       * Background Attachment
       * @see https://tailwindcss.com/docs/background-attachment
       */
      "bg-attachment": [{
        bg: ["fixed", "local", "scroll"]
      }],
      /**
       * Background Clip
       * @see https://tailwindcss.com/docs/background-clip
       */
      "bg-clip": [{
        "bg-clip": ["border", "padding", "content", "text"]
      }],
      /**
       * Background Origin
       * @see https://tailwindcss.com/docs/background-origin
       */
      "bg-origin": [{
        "bg-origin": ["border", "padding", "content"]
      }],
      /**
       * Background Position
       * @see https://tailwindcss.com/docs/background-position
       */
      "bg-position": [{
        bg: ee()
      }],
      /**
       * Background Repeat
       * @see https://tailwindcss.com/docs/background-repeat
       */
      "bg-repeat": [{
        bg: U()
      }],
      /**
       * Background Size
       * @see https://tailwindcss.com/docs/background-size
       */
      "bg-size": [{
        bg: G()
      }],
      /**
       * Background Image
       * @see https://tailwindcss.com/docs/background-image
       */
      "bg-image": [{
        bg: ["none", {
          linear: [{
            to: ["t", "tr", "r", "br", "b", "bl", "l", "tl"]
          }, dn, xe, be],
          radial: ["", xe, be],
          conic: [dn, xe, be]
        }, J1, Y1]
      }],
      /**
       * Background Color
       * @see https://tailwindcss.com/docs/background-color
       */
      "bg-color": [{
        bg: B()
      }],
      /**
       * Gradient Color Stops From Position
       * @see https://tailwindcss.com/docs/gradient-color-stops
       */
      "gradient-from-pos": [{
        from: Y()
      }],
      /**
       * Gradient Color Stops Via Position
       * @see https://tailwindcss.com/docs/gradient-color-stops
       */
      "gradient-via-pos": [{
        via: Y()
      }],
      /**
       * Gradient Color Stops To Position
       * @see https://tailwindcss.com/docs/gradient-color-stops
       */
      "gradient-to-pos": [{
        to: Y()
      }],
      /**
       * Gradient Color Stops From
       * @see https://tailwindcss.com/docs/gradient-color-stops
       */
      "gradient-from": [{
        from: B()
      }],
      /**
       * Gradient Color Stops Via
       * @see https://tailwindcss.com/docs/gradient-color-stops
       */
      "gradient-via": [{
        via: B()
      }],
      /**
       * Gradient Color Stops To
       * @see https://tailwindcss.com/docs/gradient-color-stops
       */
      "gradient-to": [{
        to: B()
      }],
      // ---------------
      // --- Borders ---
      // ---------------
      /**
       * Border Radius
       * @see https://tailwindcss.com/docs/border-radius
       */
      rounded: [{
        rounded: Q()
      }],
      /**
       * Border Radius Start
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-s": [{
        "rounded-s": Q()
      }],
      /**
       * Border Radius End
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-e": [{
        "rounded-e": Q()
      }],
      /**
       * Border Radius Top
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-t": [{
        "rounded-t": Q()
      }],
      /**
       * Border Radius Right
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-r": [{
        "rounded-r": Q()
      }],
      /**
       * Border Radius Bottom
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-b": [{
        "rounded-b": Q()
      }],
      /**
       * Border Radius Left
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-l": [{
        "rounded-l": Q()
      }],
      /**
       * Border Radius Start Start
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-ss": [{
        "rounded-ss": Q()
      }],
      /**
       * Border Radius Start End
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-se": [{
        "rounded-se": Q()
      }],
      /**
       * Border Radius End End
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-ee": [{
        "rounded-ee": Q()
      }],
      /**
       * Border Radius End Start
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-es": [{
        "rounded-es": Q()
      }],
      /**
       * Border Radius Top Left
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-tl": [{
        "rounded-tl": Q()
      }],
      /**
       * Border Radius Top Right
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-tr": [{
        "rounded-tr": Q()
      }],
      /**
       * Border Radius Bottom Right
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-br": [{
        "rounded-br": Q()
      }],
      /**
       * Border Radius Bottom Left
       * @see https://tailwindcss.com/docs/border-radius
       */
      "rounded-bl": [{
        "rounded-bl": Q()
      }],
      /**
       * Border Width
       * @see https://tailwindcss.com/docs/border-width
       */
      "border-w": [{
        border: ne()
      }],
      /**
       * Border Width X
       * @see https://tailwindcss.com/docs/border-width
       */
      "border-w-x": [{
        "border-x": ne()
      }],
      /**
       * Border Width Y
       * @see https://tailwindcss.com/docs/border-width
       */
      "border-w-y": [{
        "border-y": ne()
      }],
      /**
       * Border Width Start
       * @see https://tailwindcss.com/docs/border-width
       */
      "border-w-s": [{
        "border-s": ne()
      }],
      /**
       * Border Width End
       * @see https://tailwindcss.com/docs/border-width
       */
      "border-w-e": [{
        "border-e": ne()
      }],
      /**
       * Border Width Top
       * @see https://tailwindcss.com/docs/border-width
       */
      "border-w-t": [{
        "border-t": ne()
      }],
      /**
       * Border Width Right
       * @see https://tailwindcss.com/docs/border-width
       */
      "border-w-r": [{
        "border-r": ne()
      }],
      /**
       * Border Width Bottom
       * @see https://tailwindcss.com/docs/border-width
       */
      "border-w-b": [{
        "border-b": ne()
      }],
      /**
       * Border Width Left
       * @see https://tailwindcss.com/docs/border-width
       */
      "border-w-l": [{
        "border-l": ne()
      }],
      /**
       * Divide Width X
       * @see https://tailwindcss.com/docs/border-width#between-children
       */
      "divide-x": [{
        "divide-x": ne()
      }],
      /**
       * Divide Width X Reverse
       * @see https://tailwindcss.com/docs/border-width#between-children
       */
      "divide-x-reverse": ["divide-x-reverse"],
      /**
       * Divide Width Y
       * @see https://tailwindcss.com/docs/border-width#between-children
       */
      "divide-y": [{
        "divide-y": ne()
      }],
      /**
       * Divide Width Y Reverse
       * @see https://tailwindcss.com/docs/border-width#between-children
       */
      "divide-y-reverse": ["divide-y-reverse"],
      /**
       * Border Style
       * @see https://tailwindcss.com/docs/border-style
       */
      "border-style": [{
        border: [...z(), "hidden", "none"]
      }],
      /**
       * Divide Style
       * @see https://tailwindcss.com/docs/border-style#setting-the-divider-style
       */
      "divide-style": [{
        divide: [...z(), "hidden", "none"]
      }],
      /**
       * Border Color
       * @see https://tailwindcss.com/docs/border-color
       */
      "border-color": [{
        border: B()
      }],
      /**
       * Border Color X
       * @see https://tailwindcss.com/docs/border-color
       */
      "border-color-x": [{
        "border-x": B()
      }],
      /**
       * Border Color Y
       * @see https://tailwindcss.com/docs/border-color
       */
      "border-color-y": [{
        "border-y": B()
      }],
      /**
       * Border Color S
       * @see https://tailwindcss.com/docs/border-color
       */
      "border-color-s": [{
        "border-s": B()
      }],
      /**
       * Border Color E
       * @see https://tailwindcss.com/docs/border-color
       */
      "border-color-e": [{
        "border-e": B()
      }],
      /**
       * Border Color Top
       * @see https://tailwindcss.com/docs/border-color
       */
      "border-color-t": [{
        "border-t": B()
      }],
      /**
       * Border Color Right
       * @see https://tailwindcss.com/docs/border-color
       */
      "border-color-r": [{
        "border-r": B()
      }],
      /**
       * Border Color Bottom
       * @see https://tailwindcss.com/docs/border-color
       */
      "border-color-b": [{
        "border-b": B()
      }],
      /**
       * Border Color Left
       * @see https://tailwindcss.com/docs/border-color
       */
      "border-color-l": [{
        "border-l": B()
      }],
      /**
       * Divide Color
       * @see https://tailwindcss.com/docs/divide-color
       */
      "divide-color": [{
        divide: B()
      }],
      /**
       * Outline Style
       * @see https://tailwindcss.com/docs/outline-style
       */
      "outline-style": [{
        outline: [...z(), "none", "hidden"]
      }],
      /**
       * Outline Offset
       * @see https://tailwindcss.com/docs/outline-offset
       */
      "outline-offset": [{
        "outline-offset": [De, xe, be]
      }],
      /**
       * Outline Width
       * @see https://tailwindcss.com/docs/outline-width
       */
      "outline-w": [{
        outline: ["", De, vi, Tn]
      }],
      /**
       * Outline Color
       * @see https://tailwindcss.com/docs/outline-color
       */
      "outline-color": [{
        outline: B()
      }],
      // ---------------
      // --- Effects ---
      // ---------------
      /**
       * Box Shadow
       * @see https://tailwindcss.com/docs/box-shadow
       */
      shadow: [{
        shadow: [
          // Deprecated since Tailwind CSS v4.0.0
          "",
          "none",
          d,
          Go,
          Ho
        ]
      }],
      /**
       * Box Shadow Color
       * @see https://tailwindcss.com/docs/box-shadow#setting-the-shadow-color
       */
      "shadow-color": [{
        shadow: B()
      }],
      /**
       * Inset Box Shadow
       * @see https://tailwindcss.com/docs/box-shadow#adding-an-inset-shadow
       */
      "inset-shadow": [{
        "inset-shadow": ["none", f, Go, Ho]
      }],
      /**
       * Inset Box Shadow Color
       * @see https://tailwindcss.com/docs/box-shadow#setting-the-inset-shadow-color
       */
      "inset-shadow-color": [{
        "inset-shadow": B()
      }],
      /**
       * Ring Width
       * @see https://tailwindcss.com/docs/box-shadow#adding-a-ring
       */
      "ring-w": [{
        ring: ne()
      }],
      /**
       * Ring Width Inset
       * @see https://v3.tailwindcss.com/docs/ring-width#inset-rings
       * @deprecated since Tailwind CSS v4.0.0
       * @see https://github.com/tailwindlabs/tailwindcss/blob/v4.0.0/packages/tailwindcss/src/utilities.ts#L4158
       */
      "ring-w-inset": ["ring-inset"],
      /**
       * Ring Color
       * @see https://tailwindcss.com/docs/box-shadow#setting-the-ring-color
       */
      "ring-color": [{
        ring: B()
      }],
      /**
       * Ring Offset Width
       * @see https://v3.tailwindcss.com/docs/ring-offset-width
       * @deprecated since Tailwind CSS v4.0.0
       * @see https://github.com/tailwindlabs/tailwindcss/blob/v4.0.0/packages/tailwindcss/src/utilities.ts#L4158
       */
      "ring-offset-w": [{
        "ring-offset": [De, Tn]
      }],
      /**
       * Ring Offset Color
       * @see https://v3.tailwindcss.com/docs/ring-offset-color
       * @deprecated since Tailwind CSS v4.0.0
       * @see https://github.com/tailwindlabs/tailwindcss/blob/v4.0.0/packages/tailwindcss/src/utilities.ts#L4158
       */
      "ring-offset-color": [{
        "ring-offset": B()
      }],
      /**
       * Inset Ring Width
       * @see https://tailwindcss.com/docs/box-shadow#adding-an-inset-ring
       */
      "inset-ring-w": [{
        "inset-ring": ne()
      }],
      /**
       * Inset Ring Color
       * @see https://tailwindcss.com/docs/box-shadow#setting-the-inset-ring-color
       */
      "inset-ring-color": [{
        "inset-ring": B()
      }],
      /**
       * Text Shadow
       * @see https://tailwindcss.com/docs/text-shadow
       */
      "text-shadow": [{
        "text-shadow": ["none", p, Go, Ho]
      }],
      /**
       * Text Shadow Color
       * @see https://tailwindcss.com/docs/text-shadow#setting-the-shadow-color
       */
      "text-shadow-color": [{
        "text-shadow": B()
      }],
      /**
       * Opacity
       * @see https://tailwindcss.com/docs/opacity
       */
      opacity: [{
        opacity: [De, xe, be]
      }],
      /**
       * Mix Blend Mode
       * @see https://tailwindcss.com/docs/mix-blend-mode
       */
      "mix-blend": [{
        "mix-blend": [...se(), "plus-darker", "plus-lighter"]
      }],
      /**
       * Background Blend Mode
       * @see https://tailwindcss.com/docs/background-blend-mode
       */
      "bg-blend": [{
        "bg-blend": se()
      }],
      /**
       * Mask Clip
       * @see https://tailwindcss.com/docs/mask-clip
       */
      "mask-clip": [{
        "mask-clip": ["border", "padding", "content", "fill", "stroke", "view"]
      }, "mask-no-clip"],
      /**
       * Mask Composite
       * @see https://tailwindcss.com/docs/mask-composite
       */
      "mask-composite": [{
        mask: ["add", "subtract", "intersect", "exclude"]
      }],
      /**
       * Mask Image
       * @see https://tailwindcss.com/docs/mask-image
       */
      "mask-image-linear-pos": [{
        "mask-linear": [De]
      }],
      "mask-image-linear-from-pos": [{
        "mask-linear-from": oe()
      }],
      "mask-image-linear-to-pos": [{
        "mask-linear-to": oe()
      }],
      "mask-image-linear-from-color": [{
        "mask-linear-from": B()
      }],
      "mask-image-linear-to-color": [{
        "mask-linear-to": B()
      }],
      "mask-image-t-from-pos": [{
        "mask-t-from": oe()
      }],
      "mask-image-t-to-pos": [{
        "mask-t-to": oe()
      }],
      "mask-image-t-from-color": [{
        "mask-t-from": B()
      }],
      "mask-image-t-to-color": [{
        "mask-t-to": B()
      }],
      "mask-image-r-from-pos": [{
        "mask-r-from": oe()
      }],
      "mask-image-r-to-pos": [{
        "mask-r-to": oe()
      }],
      "mask-image-r-from-color": [{
        "mask-r-from": B()
      }],
      "mask-image-r-to-color": [{
        "mask-r-to": B()
      }],
      "mask-image-b-from-pos": [{
        "mask-b-from": oe()
      }],
      "mask-image-b-to-pos": [{
        "mask-b-to": oe()
      }],
      "mask-image-b-from-color": [{
        "mask-b-from": B()
      }],
      "mask-image-b-to-color": [{
        "mask-b-to": B()
      }],
      "mask-image-l-from-pos": [{
        "mask-l-from": oe()
      }],
      "mask-image-l-to-pos": [{
        "mask-l-to": oe()
      }],
      "mask-image-l-from-color": [{
        "mask-l-from": B()
      }],
      "mask-image-l-to-color": [{
        "mask-l-to": B()
      }],
      "mask-image-x-from-pos": [{
        "mask-x-from": oe()
      }],
      "mask-image-x-to-pos": [{
        "mask-x-to": oe()
      }],
      "mask-image-x-from-color": [{
        "mask-x-from": B()
      }],
      "mask-image-x-to-color": [{
        "mask-x-to": B()
      }],
      "mask-image-y-from-pos": [{
        "mask-y-from": oe()
      }],
      "mask-image-y-to-pos": [{
        "mask-y-to": oe()
      }],
      "mask-image-y-from-color": [{
        "mask-y-from": B()
      }],
      "mask-image-y-to-color": [{
        "mask-y-to": B()
      }],
      "mask-image-radial": [{
        "mask-radial": [xe, be]
      }],
      "mask-image-radial-from-pos": [{
        "mask-radial-from": oe()
      }],
      "mask-image-radial-to-pos": [{
        "mask-radial-to": oe()
      }],
      "mask-image-radial-from-color": [{
        "mask-radial-from": B()
      }],
      "mask-image-radial-to-color": [{
        "mask-radial-to": B()
      }],
      "mask-image-radial-shape": [{
        "mask-radial": ["circle", "ellipse"]
      }],
      "mask-image-radial-size": [{
        "mask-radial": [{
          closest: ["side", "corner"],
          farthest: ["side", "corner"]
        }]
      }],
      "mask-image-radial-pos": [{
        "mask-radial-at": j()
      }],
      "mask-image-conic-pos": [{
        "mask-conic": [De]
      }],
      "mask-image-conic-from-pos": [{
        "mask-conic-from": oe()
      }],
      "mask-image-conic-to-pos": [{
        "mask-conic-to": oe()
      }],
      "mask-image-conic-from-color": [{
        "mask-conic-from": B()
      }],
      "mask-image-conic-to-color": [{
        "mask-conic-to": B()
      }],
      /**
       * Mask Mode
       * @see https://tailwindcss.com/docs/mask-mode
       */
      "mask-mode": [{
        mask: ["alpha", "luminance", "match"]
      }],
      /**
       * Mask Origin
       * @see https://tailwindcss.com/docs/mask-origin
       */
      "mask-origin": [{
        "mask-origin": ["border", "padding", "content", "fill", "stroke", "view"]
      }],
      /**
       * Mask Position
       * @see https://tailwindcss.com/docs/mask-position
       */
      "mask-position": [{
        mask: ee()
      }],
      /**
       * Mask Repeat
       * @see https://tailwindcss.com/docs/mask-repeat
       */
      "mask-repeat": [{
        mask: U()
      }],
      /**
       * Mask Size
       * @see https://tailwindcss.com/docs/mask-size
       */
      "mask-size": [{
        mask: G()
      }],
      /**
       * Mask Type
       * @see https://tailwindcss.com/docs/mask-type
       */
      "mask-type": [{
        "mask-type": ["alpha", "luminance"]
      }],
      /**
       * Mask Image
       * @see https://tailwindcss.com/docs/mask-image
       */
      "mask-image": [{
        mask: ["none", xe, be]
      }],
      // ---------------
      // --- Filters ---
      // ---------------
      /**
       * Filter
       * @see https://tailwindcss.com/docs/filter
       */
      filter: [{
        filter: [
          // Deprecated since Tailwind CSS v3.0.0
          "",
          "none",
          xe,
          be
        ]
      }],
      /**
       * Blur
       * @see https://tailwindcss.com/docs/blur
       */
      blur: [{
        blur: ie()
      }],
      /**
       * Brightness
       * @see https://tailwindcss.com/docs/brightness
       */
      brightness: [{
        brightness: [De, xe, be]
      }],
      /**
       * Contrast
       * @see https://tailwindcss.com/docs/contrast
       */
      contrast: [{
        contrast: [De, xe, be]
      }],
      /**
       * Drop Shadow
       * @see https://tailwindcss.com/docs/drop-shadow
       */
      "drop-shadow": [{
        "drop-shadow": [
          // Deprecated since Tailwind CSS v4.0.0
          "",
          "none",
          m,
          Go,
          Ho
        ]
      }],
      /**
       * Drop Shadow Color
       * @see https://tailwindcss.com/docs/filter-drop-shadow#setting-the-shadow-color
       */
      "drop-shadow-color": [{
        "drop-shadow": B()
      }],
      /**
       * Grayscale
       * @see https://tailwindcss.com/docs/grayscale
       */
      grayscale: [{
        grayscale: ["", De, xe, be]
      }],
      /**
       * Hue Rotate
       * @see https://tailwindcss.com/docs/hue-rotate
       */
      "hue-rotate": [{
        "hue-rotate": [De, xe, be]
      }],
      /**
       * Invert
       * @see https://tailwindcss.com/docs/invert
       */
      invert: [{
        invert: ["", De, xe, be]
      }],
      /**
       * Saturate
       * @see https://tailwindcss.com/docs/saturate
       */
      saturate: [{
        saturate: [De, xe, be]
      }],
      /**
       * Sepia
       * @see https://tailwindcss.com/docs/sepia
       */
      sepia: [{
        sepia: ["", De, xe, be]
      }],
      /**
       * Backdrop Filter
       * @see https://tailwindcss.com/docs/backdrop-filter
       */
      "backdrop-filter": [{
        "backdrop-filter": [
          // Deprecated since Tailwind CSS v3.0.0
          "",
          "none",
          xe,
          be
        ]
      }],
      /**
       * Backdrop Blur
       * @see https://tailwindcss.com/docs/backdrop-blur
       */
      "backdrop-blur": [{
        "backdrop-blur": ie()
      }],
      /**
       * Backdrop Brightness
       * @see https://tailwindcss.com/docs/backdrop-brightness
       */
      "backdrop-brightness": [{
        "backdrop-brightness": [De, xe, be]
      }],
      /**
       * Backdrop Contrast
       * @see https://tailwindcss.com/docs/backdrop-contrast
       */
      "backdrop-contrast": [{
        "backdrop-contrast": [De, xe, be]
      }],
      /**
       * Backdrop Grayscale
       * @see https://tailwindcss.com/docs/backdrop-grayscale
       */
      "backdrop-grayscale": [{
        "backdrop-grayscale": ["", De, xe, be]
      }],
      /**
       * Backdrop Hue Rotate
       * @see https://tailwindcss.com/docs/backdrop-hue-rotate
       */
      "backdrop-hue-rotate": [{
        "backdrop-hue-rotate": [De, xe, be]
      }],
      /**
       * Backdrop Invert
       * @see https://tailwindcss.com/docs/backdrop-invert
       */
      "backdrop-invert": [{
        "backdrop-invert": ["", De, xe, be]
      }],
      /**
       * Backdrop Opacity
       * @see https://tailwindcss.com/docs/backdrop-opacity
       */
      "backdrop-opacity": [{
        "backdrop-opacity": [De, xe, be]
      }],
      /**
       * Backdrop Saturate
       * @see https://tailwindcss.com/docs/backdrop-saturate
       */
      "backdrop-saturate": [{
        "backdrop-saturate": [De, xe, be]
      }],
      /**
       * Backdrop Sepia
       * @see https://tailwindcss.com/docs/backdrop-sepia
       */
      "backdrop-sepia": [{
        "backdrop-sepia": ["", De, xe, be]
      }],
      // --------------
      // --- Tables ---
      // --------------
      /**
       * Border Collapse
       * @see https://tailwindcss.com/docs/border-collapse
       */
      "border-collapse": [{
        border: ["collapse", "separate"]
      }],
      /**
       * Border Spacing
       * @see https://tailwindcss.com/docs/border-spacing
       */
      "border-spacing": [{
        "border-spacing": k()
      }],
      /**
       * Border Spacing X
       * @see https://tailwindcss.com/docs/border-spacing
       */
      "border-spacing-x": [{
        "border-spacing-x": k()
      }],
      /**
       * Border Spacing Y
       * @see https://tailwindcss.com/docs/border-spacing
       */
      "border-spacing-y": [{
        "border-spacing-y": k()
      }],
      /**
       * Table Layout
       * @see https://tailwindcss.com/docs/table-layout
       */
      "table-layout": [{
        table: ["auto", "fixed"]
      }],
      /**
       * Caption Side
       * @see https://tailwindcss.com/docs/caption-side
       */
      caption: [{
        caption: ["top", "bottom"]
      }],
      // ---------------------------------
      // --- Transitions and Animation ---
      // ---------------------------------
      /**
       * Transition Property
       * @see https://tailwindcss.com/docs/transition-property
       */
      transition: [{
        transition: ["", "all", "colors", "opacity", "shadow", "transform", "none", xe, be]
      }],
      /**
       * Transition Behavior
       * @see https://tailwindcss.com/docs/transition-behavior
       */
      "transition-behavior": [{
        transition: ["normal", "discrete"]
      }],
      /**
       * Transition Duration
       * @see https://tailwindcss.com/docs/transition-duration
       */
      duration: [{
        duration: [De, "initial", xe, be]
      }],
      /**
       * Transition Timing Function
       * @see https://tailwindcss.com/docs/transition-timing-function
       */
      ease: [{
        ease: ["linear", "initial", v, xe, be]
      }],
      /**
       * Transition Delay
       * @see https://tailwindcss.com/docs/transition-delay
       */
      delay: [{
        delay: [De, xe, be]
      }],
      /**
       * Animation
       * @see https://tailwindcss.com/docs/animation
       */
      animate: [{
        animate: ["none", b, xe, be]
      }],
      // ------------------
      // --- Transforms ---
      // ------------------
      /**
       * Backface Visibility
       * @see https://tailwindcss.com/docs/backface-visibility
       */
      backface: [{
        backface: ["hidden", "visible"]
      }],
      /**
       * Perspective
       * @see https://tailwindcss.com/docs/perspective
       */
      perspective: [{
        perspective: [h, xe, be]
      }],
      /**
       * Perspective Origin
       * @see https://tailwindcss.com/docs/perspective-origin
       */
      "perspective-origin": [{
        "perspective-origin": x()
      }],
      /**
       * Rotate
       * @see https://tailwindcss.com/docs/rotate
       */
      rotate: [{
        rotate: K()
      }],
      /**
       * Rotate X
       * @see https://tailwindcss.com/docs/rotate
       */
      "rotate-x": [{
        "rotate-x": K()
      }],
      /**
       * Rotate Y
       * @see https://tailwindcss.com/docs/rotate
       */
      "rotate-y": [{
        "rotate-y": K()
      }],
      /**
       * Rotate Z
       * @see https://tailwindcss.com/docs/rotate
       */
      "rotate-z": [{
        "rotate-z": K()
      }],
      /**
       * Scale
       * @see https://tailwindcss.com/docs/scale
       */
      scale: [{
        scale: ae()
      }],
      /**
       * Scale X
       * @see https://tailwindcss.com/docs/scale
       */
      "scale-x": [{
        "scale-x": ae()
      }],
      /**
       * Scale Y
       * @see https://tailwindcss.com/docs/scale
       */
      "scale-y": [{
        "scale-y": ae()
      }],
      /**
       * Scale Z
       * @see https://tailwindcss.com/docs/scale
       */
      "scale-z": [{
        "scale-z": ae()
      }],
      /**
       * Scale 3D
       * @see https://tailwindcss.com/docs/scale
       */
      "scale-3d": ["scale-3d"],
      /**
       * Skew
       * @see https://tailwindcss.com/docs/skew
       */
      skew: [{
        skew: fe()
      }],
      /**
       * Skew X
       * @see https://tailwindcss.com/docs/skew
       */
      "skew-x": [{
        "skew-x": fe()
      }],
      /**
       * Skew Y
       * @see https://tailwindcss.com/docs/skew
       */
      "skew-y": [{
        "skew-y": fe()
      }],
      /**
       * Transform
       * @see https://tailwindcss.com/docs/transform
       */
      transform: [{
        transform: [xe, be, "", "none", "gpu", "cpu"]
      }],
      /**
       * Transform Origin
       * @see https://tailwindcss.com/docs/transform-origin
       */
      "transform-origin": [{
        origin: x()
      }],
      /**
       * Transform Style
       * @see https://tailwindcss.com/docs/transform-style
       */
      "transform-style": [{
        transform: ["3d", "flat"]
      }],
      /**
       * Translate
       * @see https://tailwindcss.com/docs/translate
       */
      translate: [{
        translate: $()
      }],
      /**
       * Translate X
       * @see https://tailwindcss.com/docs/translate
       */
      "translate-x": [{
        "translate-x": $()
      }],
      /**
       * Translate Y
       * @see https://tailwindcss.com/docs/translate
       */
      "translate-y": [{
        "translate-y": $()
      }],
      /**
       * Translate Z
       * @see https://tailwindcss.com/docs/translate
       */
      "translate-z": [{
        "translate-z": $()
      }],
      /**
       * Translate None
       * @see https://tailwindcss.com/docs/translate
       */
      "translate-none": ["translate-none"],
      // ---------------------
      // --- Interactivity ---
      // ---------------------
      /**
       * Accent Color
       * @see https://tailwindcss.com/docs/accent-color
       */
      accent: [{
        accent: B()
      }],
      /**
       * Appearance
       * @see https://tailwindcss.com/docs/appearance
       */
      appearance: [{
        appearance: ["none", "auto"]
      }],
      /**
       * Caret Color
       * @see https://tailwindcss.com/docs/just-in-time-mode#caret-color-utilities
       */
      "caret-color": [{
        caret: B()
      }],
      /**
       * Color Scheme
       * @see https://tailwindcss.com/docs/color-scheme
       */
      "color-scheme": [{
        scheme: ["normal", "dark", "light", "light-dark", "only-dark", "only-light"]
      }],
      /**
       * Cursor
       * @see https://tailwindcss.com/docs/cursor
       */
      cursor: [{
        cursor: ["auto", "default", "pointer", "wait", "text", "move", "help", "not-allowed", "none", "context-menu", "progress", "cell", "crosshair", "vertical-text", "alias", "copy", "no-drop", "grab", "grabbing", "all-scroll", "col-resize", "row-resize", "n-resize", "e-resize", "s-resize", "w-resize", "ne-resize", "nw-resize", "se-resize", "sw-resize", "ew-resize", "ns-resize", "nesw-resize", "nwse-resize", "zoom-in", "zoom-out", xe, be]
      }],
      /**
       * Field Sizing
       * @see https://tailwindcss.com/docs/field-sizing
       */
      "field-sizing": [{
        "field-sizing": ["fixed", "content"]
      }],
      /**
       * Pointer Events
       * @see https://tailwindcss.com/docs/pointer-events
       */
      "pointer-events": [{
        "pointer-events": ["auto", "none"]
      }],
      /**
       * Resize
       * @see https://tailwindcss.com/docs/resize
       */
      resize: [{
        resize: ["none", "", "y", "x"]
      }],
      /**
       * Scroll Behavior
       * @see https://tailwindcss.com/docs/scroll-behavior
       */
      "scroll-behavior": [{
        scroll: ["auto", "smooth"]
      }],
      /**
       * Scroll Margin
       * @see https://tailwindcss.com/docs/scroll-margin
       */
      "scroll-m": [{
        "scroll-m": k()
      }],
      /**
       * Scroll Margin X
       * @see https://tailwindcss.com/docs/scroll-margin
       */
      "scroll-mx": [{
        "scroll-mx": k()
      }],
      /**
       * Scroll Margin Y
       * @see https://tailwindcss.com/docs/scroll-margin
       */
      "scroll-my": [{
        "scroll-my": k()
      }],
      /**
       * Scroll Margin Start
       * @see https://tailwindcss.com/docs/scroll-margin
       */
      "scroll-ms": [{
        "scroll-ms": k()
      }],
      /**
       * Scroll Margin End
       * @see https://tailwindcss.com/docs/scroll-margin
       */
      "scroll-me": [{
        "scroll-me": k()
      }],
      /**
       * Scroll Margin Top
       * @see https://tailwindcss.com/docs/scroll-margin
       */
      "scroll-mt": [{
        "scroll-mt": k()
      }],
      /**
       * Scroll Margin Right
       * @see https://tailwindcss.com/docs/scroll-margin
       */
      "scroll-mr": [{
        "scroll-mr": k()
      }],
      /**
       * Scroll Margin Bottom
       * @see https://tailwindcss.com/docs/scroll-margin
       */
      "scroll-mb": [{
        "scroll-mb": k()
      }],
      /**
       * Scroll Margin Left
       * @see https://tailwindcss.com/docs/scroll-margin
       */
      "scroll-ml": [{
        "scroll-ml": k()
      }],
      /**
       * Scroll Padding
       * @see https://tailwindcss.com/docs/scroll-padding
       */
      "scroll-p": [{
        "scroll-p": k()
      }],
      /**
       * Scroll Padding X
       * @see https://tailwindcss.com/docs/scroll-padding
       */
      "scroll-px": [{
        "scroll-px": k()
      }],
      /**
       * Scroll Padding Y
       * @see https://tailwindcss.com/docs/scroll-padding
       */
      "scroll-py": [{
        "scroll-py": k()
      }],
      /**
       * Scroll Padding Start
       * @see https://tailwindcss.com/docs/scroll-padding
       */
      "scroll-ps": [{
        "scroll-ps": k()
      }],
      /**
       * Scroll Padding End
       * @see https://tailwindcss.com/docs/scroll-padding
       */
      "scroll-pe": [{
        "scroll-pe": k()
      }],
      /**
       * Scroll Padding Top
       * @see https://tailwindcss.com/docs/scroll-padding
       */
      "scroll-pt": [{
        "scroll-pt": k()
      }],
      /**
       * Scroll Padding Right
       * @see https://tailwindcss.com/docs/scroll-padding
       */
      "scroll-pr": [{
        "scroll-pr": k()
      }],
      /**
       * Scroll Padding Bottom
       * @see https://tailwindcss.com/docs/scroll-padding
       */
      "scroll-pb": [{
        "scroll-pb": k()
      }],
      /**
       * Scroll Padding Left
       * @see https://tailwindcss.com/docs/scroll-padding
       */
      "scroll-pl": [{
        "scroll-pl": k()
      }],
      /**
       * Scroll Snap Align
       * @see https://tailwindcss.com/docs/scroll-snap-align
       */
      "snap-align": [{
        snap: ["start", "end", "center", "align-none"]
      }],
      /**
       * Scroll Snap Stop
       * @see https://tailwindcss.com/docs/scroll-snap-stop
       */
      "snap-stop": [{
        snap: ["normal", "always"]
      }],
      /**
       * Scroll Snap Type
       * @see https://tailwindcss.com/docs/scroll-snap-type
       */
      "snap-type": [{
        snap: ["none", "x", "y", "both"]
      }],
      /**
       * Scroll Snap Type Strictness
       * @see https://tailwindcss.com/docs/scroll-snap-type
       */
      "snap-strictness": [{
        snap: ["mandatory", "proximity"]
      }],
      /**
       * Touch Action
       * @see https://tailwindcss.com/docs/touch-action
       */
      touch: [{
        touch: ["auto", "none", "manipulation"]
      }],
      /**
       * Touch Action X
       * @see https://tailwindcss.com/docs/touch-action
       */
      "touch-x": [{
        "touch-pan": ["x", "left", "right"]
      }],
      /**
       * Touch Action Y
       * @see https://tailwindcss.com/docs/touch-action
       */
      "touch-y": [{
        "touch-pan": ["y", "up", "down"]
      }],
      /**
       * Touch Action Pinch Zoom
       * @see https://tailwindcss.com/docs/touch-action
       */
      "touch-pz": ["touch-pinch-zoom"],
      /**
       * User Select
       * @see https://tailwindcss.com/docs/user-select
       */
      select: [{
        select: ["none", "text", "all", "auto"]
      }],
      /**
       * Will Change
       * @see https://tailwindcss.com/docs/will-change
       */
      "will-change": [{
        "will-change": ["auto", "scroll", "contents", "transform", xe, be]
      }],
      // -----------
      // --- SVG ---
      // -----------
      /**
       * Fill
       * @see https://tailwindcss.com/docs/fill
       */
      fill: [{
        fill: ["none", ...B()]
      }],
      /**
       * Stroke Width
       * @see https://tailwindcss.com/docs/stroke-width
       */
      "stroke-w": [{
        stroke: [De, vi, Tn, fc]
      }],
      /**
       * Stroke
       * @see https://tailwindcss.com/docs/stroke
       */
      stroke: [{
        stroke: ["none", ...B()]
      }],
      // ---------------------
      // --- Accessibility ---
      // ---------------------
      /**
       * Forced Color Adjust
       * @see https://tailwindcss.com/docs/forced-color-adjust
       */
      "forced-color-adjust": [{
        "forced-color-adjust": ["auto", "none"]
      }]
    },
    conflictingClassGroups: {
      overflow: ["overflow-x", "overflow-y"],
      overscroll: ["overscroll-x", "overscroll-y"],
      inset: ["inset-x", "inset-y", "start", "end", "top", "right", "bottom", "left"],
      "inset-x": ["right", "left"],
      "inset-y": ["top", "bottom"],
      flex: ["basis", "grow", "shrink"],
      gap: ["gap-x", "gap-y"],
      p: ["px", "py", "ps", "pe", "pt", "pr", "pb", "pl"],
      px: ["pr", "pl"],
      py: ["pt", "pb"],
      m: ["mx", "my", "ms", "me", "mt", "mr", "mb", "ml"],
      mx: ["mr", "ml"],
      my: ["mt", "mb"],
      size: ["w", "h"],
      "font-size": ["leading"],
      "fvn-normal": ["fvn-ordinal", "fvn-slashed-zero", "fvn-figure", "fvn-spacing", "fvn-fraction"],
      "fvn-ordinal": ["fvn-normal"],
      "fvn-slashed-zero": ["fvn-normal"],
      "fvn-figure": ["fvn-normal"],
      "fvn-spacing": ["fvn-normal"],
      "fvn-fraction": ["fvn-normal"],
      "line-clamp": ["display", "overflow"],
      rounded: ["rounded-s", "rounded-e", "rounded-t", "rounded-r", "rounded-b", "rounded-l", "rounded-ss", "rounded-se", "rounded-ee", "rounded-es", "rounded-tl", "rounded-tr", "rounded-br", "rounded-bl"],
      "rounded-s": ["rounded-ss", "rounded-es"],
      "rounded-e": ["rounded-se", "rounded-ee"],
      "rounded-t": ["rounded-tl", "rounded-tr"],
      "rounded-r": ["rounded-tr", "rounded-br"],
      "rounded-b": ["rounded-br", "rounded-bl"],
      "rounded-l": ["rounded-tl", "rounded-bl"],
      "border-spacing": ["border-spacing-x", "border-spacing-y"],
      "border-w": ["border-w-x", "border-w-y", "border-w-s", "border-w-e", "border-w-t", "border-w-r", "border-w-b", "border-w-l"],
      "border-w-x": ["border-w-r", "border-w-l"],
      "border-w-y": ["border-w-t", "border-w-b"],
      "border-color": ["border-color-x", "border-color-y", "border-color-s", "border-color-e", "border-color-t", "border-color-r", "border-color-b", "border-color-l"],
      "border-color-x": ["border-color-r", "border-color-l"],
      "border-color-y": ["border-color-t", "border-color-b"],
      translate: ["translate-x", "translate-y", "translate-none"],
      "translate-none": ["translate", "translate-x", "translate-y", "translate-z"],
      "scroll-m": ["scroll-mx", "scroll-my", "scroll-ms", "scroll-me", "scroll-mt", "scroll-mr", "scroll-mb", "scroll-ml"],
      "scroll-mx": ["scroll-mr", "scroll-ml"],
      "scroll-my": ["scroll-mt", "scroll-mb"],
      "scroll-p": ["scroll-px", "scroll-py", "scroll-ps", "scroll-pe", "scroll-pt", "scroll-pr", "scroll-pb", "scroll-pl"],
      "scroll-px": ["scroll-pr", "scroll-pl"],
      "scroll-py": ["scroll-pt", "scroll-pb"],
      touch: ["touch-x", "touch-y", "touch-pz"],
      "touch-x": ["touch"],
      "touch-y": ["touch"],
      "touch-pz": ["touch"]
    },
    conflictingClassGroupModifiers: {
      "font-size": ["leading"]
    },
    orderSensitiveModifiers: ["*", "**", "after", "backdrop", "before", "details-content", "file", "first-letter", "first-line", "marker", "placeholder", "selection"]
  };
}, rC = /* @__PURE__ */ I1(tC);
function Mg(...e) {
  return rC(Ve(e));
}
function Jh({
  progress: e,
  label: t,
  showPercentage: r = !0,
  className: n,
  color: a = "blue"
}) {
  const i = {
    blue: "bg-blue-600",
    green: "bg-green-600",
    yellow: "bg-yellow-600",
    red: "bg-red-600"
  }, o = Math.min(100, Math.max(0, e));
  return /* @__PURE__ */ s.jsxs("div", { className: Mg("progress-bar w-full", n), children: [
    t && /* @__PURE__ */ s.jsxs("div", { className: "flex justify-between items-center mb-2", children: [
      /* @__PURE__ */ s.jsx("span", { className: "text-sm font-medium text-gray-700", children: t }),
      r && /* @__PURE__ */ s.jsxs("span", { className: "text-sm font-semibold text-gray-900", children: [
        Math.round(o),
        "%"
      ] })
    ] }),
    /* @__PURE__ */ s.jsx("div", { className: "w-full bg-gray-200 rounded-full h-2.5 overflow-hidden", children: /* @__PURE__ */ s.jsx(
      "div",
      {
        className: Mg(
          "h-2.5 rounded-full transition-all duration-300 ease-out",
          i[a]
        ),
        style: { width: `${o}%` },
        role: "progressbar",
        "aria-valuenow": o,
        "aria-valuemin": 0,
        "aria-valuemax": 100
      }
    ) })
  ] });
}
function _l({ logs: e, className: t, maxHeight: r = "24rem" }) {
  const n = Tr(null);
  et(() => {
    var o;
    (o = n.current) == null || o.scrollIntoView({ behavior: "smooth" });
  }, [e]);
  const a = (o) => {
    switch (o) {
      case "error":
        return "text-red-400";
      case "warn":
        return "text-yellow-400";
      case "info":
        return "text-blue-400";
      case "debug":
        return "text-gray-400";
      default:
        return "text-gray-300";
    }
  }, i = (o) => {
    switch (o) {
      case "error":
        return "✖";
      case "warn":
        return "⚠";
      case "info":
        return "ℹ";
      case "debug":
        return "▸";
      default:
        return "•";
    }
  };
  return /* @__PURE__ */ s.jsxs(
    "div",
    {
      className: Ie(
        "log-viewer overflow-y-auto font-mono text-sm bg-gray-900 text-gray-100 p-4 rounded-lg",
        t
      ),
      style: { maxHeight: r },
      children: [
        e.length === 0 ? /* @__PURE__ */ s.jsx("div", { className: "text-gray-500 text-center py-8", children: "No logs available. Waiting for events..." }) : /* @__PURE__ */ s.jsx("div", { className: "space-y-1", children: e.map((o, l) => /* @__PURE__ */ s.jsxs("div", { className: "flex items-start gap-2 hover:bg-gray-800 px-2 py-1 rounded", children: [
          /* @__PURE__ */ s.jsx("span", { className: "text-gray-600 text-xs whitespace-nowrap", children: o.timestamp }),
          /* @__PURE__ */ s.jsxs("span", { className: Ie("font-semibold text-xs", a(o.level)), children: [
            "[",
            i(o.level),
            " ",
            o.level.toUpperCase(),
            "]"
          ] }),
          o.source && /* @__PURE__ */ s.jsxs("span", { className: "text-purple-400 text-xs", children: [
            "[",
            o.source,
            "]"
          ] }),
          /* @__PURE__ */ s.jsx("span", { className: "flex-1 break-words", children: o.message })
        ] }, l)) }),
        /* @__PURE__ */ s.jsx("div", { ref: n })
      ]
    }
  );
}
function bj({ executionId: e, className: t }) {
  var c, u, d;
  const { data: r, isConnected: n, error: a } = h1(e), [i, o] = de([]);
  et(() => {
    r != null && r.logs && o((f) => [...f, ...r.logs]);
  }, [r == null ? void 0 : r.logs]);
  const l = r != null && r.progress ? r.progress.current_iteration / r.progress.max_iterations * 100 : 0;
  return /* @__PURE__ */ s.jsxs("div", { className: Ie("execution-monitor bg-white border border-gray-200 rounded-lg p-6", t), children: [
    /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between mb-4", children: [
      /* @__PURE__ */ s.jsx("h2", { className: "text-lg font-semibold text-gray-900", children: "Execution Monitor" }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center gap-3", children: [
        /* @__PURE__ */ s.jsx(So, { status: r == null ? void 0 : r.status, isConnected: n }),
        a && /* @__PURE__ */ s.jsx("span", { className: "text-xs text-red-600", children: a.message })
      ] })
    ] }),
    (r == null ? void 0 : r.progress) && /* @__PURE__ */ s.jsx("div", { className: "mb-6", children: /* @__PURE__ */ s.jsx(
      Jh,
      {
        progress: l,
        label: `Iteration ${r.progress.current_iteration} of ${r.progress.max_iterations}`,
        showPercentage: !0
      }
    ) }),
    (r == null ? void 0 : r.metrics) && /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-3 gap-4 mb-6", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "bg-blue-50 p-4 rounded-lg", children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-sm text-blue-600 mb-1", children: "Avg Fitness" }),
        /* @__PURE__ */ s.jsx("div", { className: "text-2xl font-bold text-blue-900", children: ((c = r.metrics.average_fitness) == null ? void 0 : c.toFixed(4)) || "N/A" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "bg-green-50 p-4 rounded-lg", children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-sm text-green-600 mb-1", children: "Diversity Score" }),
        /* @__PURE__ */ s.jsx("div", { className: "text-2xl font-bold text-green-900", children: ((u = r.metrics.diversity_score) == null ? void 0 : u.toFixed(4)) || "N/A" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "bg-purple-50 p-4 rounded-lg", children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-sm text-purple-600 mb-1", children: "Convergence Rate" }),
        /* @__PURE__ */ s.jsx("div", { className: "text-2xl font-bold text-purple-900", children: ((d = r.metrics.convergence_rate) == null ? void 0 : d.toFixed(4)) || "N/A" })
      ] })
    ] }),
    /* @__PURE__ */ s.jsxs("div", { children: [
      /* @__PURE__ */ s.jsx("h3", { className: "text-sm font-medium text-gray-700 mb-2", children: "Live Logs" }),
      /* @__PURE__ */ s.jsx(_l, { logs: i })
    ] }),
    (r == null ? void 0 : r.best_individual) && /* @__PURE__ */ s.jsxs("div", { className: "mt-6", children: [
      /* @__PURE__ */ s.jsx("h3", { className: "text-sm font-medium text-gray-700 mb-2", children: "Best Individual" }),
      /* @__PURE__ */ s.jsxs("div", { className: "bg-gray-50 p-4 rounded-lg", children: [
        /* @__PURE__ */ s.jsxs("div", { className: "text-sm text-gray-600 mb-2", children: [
          "Fitness: ",
          /* @__PURE__ */ s.jsx("span", { className: "font-semibold", children: r.best_individual.fitness.toFixed(4) })
        ] }),
        /* @__PURE__ */ s.jsx("div", { className: "text-sm text-gray-800 line-clamp-3", children: r.best_individual.content })
      ] })
    ] })
  ] });
}
function nC({ tabs: e, defaultTab: t, className: r }) {
  var o, l;
  const [n, a] = de(t || ((o = e[0]) == null ? void 0 : o.id)), i = (l = e.find((c) => c.id === n)) == null ? void 0 : l.content;
  return /* @__PURE__ */ s.jsxs("div", { className: Ie("workflow-tabs", r), children: [
    /* @__PURE__ */ s.jsx("div", { className: "border-b border-gray-200", children: /* @__PURE__ */ s.jsx("nav", { className: "flex space-x-8 px-6", "aria-label": "Tabs", children: e.map((c) => /* @__PURE__ */ s.jsxs(
      X,
      {
        onClick: () => a(c.id),
        variant: n === c.id ? "primary" : "secondary",
        className: "px-3 py-2",
        children: [
          c.icon && /* @__PURE__ */ s.jsx("span", { className: "mr-2", children: c.icon }),
          c.label
        ]
      },
      c.id
    )) }) }),
    /* @__PURE__ */ s.jsx("div", { className: "p-6", children: i })
  ] });
}
const Rg = (e) => {
  if (!e) return "neutral";
  const t = e.toLowerCase();
  return t === "healthy" ? "success" : t === "degraded" ? "warning" : "danger";
};
function aC() {
  var r, n, a, i, o;
  const { health: e, isLoading: t } = o1(1e4);
  return /* @__PURE__ */ s.jsxs(he, { title: "System Health", description: "Live service and resource status.", children: [
    t && /* @__PURE__ */ s.jsx("p", { className: "text-sm text-slate-500", children: "Loading system status..." }),
    !t && e && /* @__PURE__ */ s.jsxs("div", { className: "space-y-4 text-sm text-slate-600", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
        /* @__PURE__ */ s.jsx("span", { className: "font-medium text-slate-700", children: "Overall Status" }),
        /* @__PURE__ */ s.jsx(Re, { tone: Rg(e.status), children: e.status || "unknown" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-3 gap-3 text-xs", children: [
        /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg bg-slate-50 px-3 py-2", children: [
          /* @__PURE__ */ s.jsx("div", { className: "text-slate-500", children: "CPU" }),
          /* @__PURE__ */ s.jsxs("div", { className: "text-slate-800", children: [
            ((r = e.resource_usage) == null ? void 0 : r.cpu_percent) ?? 0,
            "%"
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg bg-slate-50 px-3 py-2", children: [
          /* @__PURE__ */ s.jsx("div", { className: "text-slate-500", children: "Memory" }),
          /* @__PURE__ */ s.jsxs("div", { className: "text-slate-800", children: [
            ((n = e.resource_usage) == null ? void 0 : n.memory_percent) ?? 0,
            "%"
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "rounded-lg bg-slate-50 px-3 py-2", children: [
          /* @__PURE__ */ s.jsx("div", { className: "text-slate-500", children: "Disk" }),
          /* @__PURE__ */ s.jsxs("div", { className: "text-slate-800", children: [
            ((a = e.resource_usage) == null ? void 0 : a.disk_percent) ?? 0,
            "%"
          ] })
        ] })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-xs uppercase tracking-wide text-slate-400", children: "Active Operations" }),
        /* @__PURE__ */ s.jsxs("div", { className: "mt-1 flex items-center gap-3 text-sm", children: [
          /* @__PURE__ */ s.jsxs("span", { children: [
            "Evolutions: ",
            ((i = e.active_operations) == null ? void 0 : i.evolutions_running) ?? 0
          ] }),
          /* @__PURE__ */ s.jsxs("span", { children: [
            "Adversarial: ",
            ((o = e.active_operations) == null ? void 0 : o.adversarial_tests_running) ?? 0
          ] })
        ] })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-xs uppercase tracking-wide text-slate-400", children: "Services" }),
        /* @__PURE__ */ s.jsx("div", { className: "mt-2 grid grid-cols-2 gap-2 text-xs", children: Object.entries(e.services || {}).map(([l, c]) => /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between rounded-md bg-slate-50 px-3 py-2", children: [
          /* @__PURE__ */ s.jsx("span", { className: "text-slate-600", children: l }),
          /* @__PURE__ */ s.jsx(Re, { tone: Rg(c), children: c })
        ] }, l)) })
      ] })
    ] }),
    !t && !e && /* @__PURE__ */ s.jsx("p", { className: "text-sm text-slate-500", children: "System health data unavailable." })
  ] });
}
function iC({
  userName: e,
  workflowCount: t,
  onCreateWorkflow: r
}) {
  return /* @__PURE__ */ s.jsx(
    he,
    {
      title: "OpenEvolve Command Center",
      description: "Run evolution, track system health, and coordinate MDAP/MAKER workflows.",
      actions: r ? /* @__PURE__ */ s.jsx(X, { onClick: r, variant: "primary", children: "New Workflow" }) : void 0,
      children: /* @__PURE__ */ s.jsxs("div", { className: "flex flex-col gap-2 text-sm text-slate-600", children: [
        /* @__PURE__ */ s.jsxs("p", { children: [
          "Welcome back, ",
          e || "Operator",
          "."
        ] }),
        /* @__PURE__ */ s.jsxs("p", { children: [
          t ?? 0,
          " workflows are currently in your workspace."
        ] })
      ] })
    }
  );
}
function wz() {
  const { user: e } = fs(), { currentWorkflow: t, setCurrentWorkflow: r } = jo(), {
    workflows: n,
    isLoading: a,
    startWorkflow: i,
    pauseWorkflow: o,
    resumeWorkflow: l,
    stopWorkflow: c,
    deleteWorkflow: u
  } = oj(), [d, f] = de(!1), p = (h) => {
    r(h), f(!0);
  }, m = async (h) => {
    const g = n.find((v) => v.evolution_id === h);
    g && (await i({
      content: g.initialContent || "",
      mode: "standard",
      parameters: {},
      models: []
    }), f(!0));
  }, y = [
    {
      id: "workflows",
      label: "Workflows",
      content: /* @__PURE__ */ s.jsx("div", { children: /* @__PURE__ */ s.jsx(
        d1,
        {
          workflows: n,
          onWorkflowSelect: p,
          onExecute: m,
          onPause: o,
          onResume: l,
          onStop: c,
          onDelete: u
        }
      ) })
    },
    {
      id: "evolution",
      label: "Evolution",
      content: /* @__PURE__ */ s.jsx(MA, {})
    },
    {
      id: "adversarial",
      label: "Adversarial",
      content: /* @__PURE__ */ s.jsx(qA, {})
    },
    {
      id: "decomposition",
      label: "Decomposition",
      content: /* @__PURE__ */ s.jsx(GA, {})
    },
    {
      id: "integrations",
      label: "Integrations",
      content: /* @__PURE__ */ s.jsx(e1, {})
    },
    {
      id: "mdap-maker",
      label: "MDAP/MAKER",
      content: /* @__PURE__ */ s.jsx(CA, {})
    }
  ];
  return /* @__PURE__ */ s.jsxs("div", { className: "flex h-screen bg-slate-50", children: [
    /* @__PURE__ */ s.jsx(wl, {}),
    /* @__PURE__ */ s.jsxs("main", { className: "flex-1 flex flex-col overflow-hidden", children: [
      /* @__PURE__ */ s.jsx("header", { className: "border-b border-slate-200 bg-white px-6 py-4", children: /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
        /* @__PURE__ */ s.jsxs("div", { children: [
          /* @__PURE__ */ s.jsx("h1", { className: "text-2xl font-bold text-slate-900", children: "OpenEvolve Dashboard" }),
          /* @__PURE__ */ s.jsxs("p", { className: "text-sm text-slate-600", children: [
            "Welcome back, ",
            (e == null ? void 0 : e.name) || "User"
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "text-sm text-slate-500", children: [
          n.length,
          " workflows"
        ] })
      ] }) }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex-1 overflow-auto p-6 space-y-6", children: [
        /* @__PURE__ */ s.jsx(iC, { userName: e == null ? void 0 : e.name, workflowCount: n.length }),
        /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 gap-6 lg:grid-cols-3", children: [
          /* @__PURE__ */ s.jsx("div", { className: "lg:col-span-2 space-y-6", children: d && t && /* @__PURE__ */ s.jsx(bj, { executionId: t.evolution_id }) }),
          /* @__PURE__ */ s.jsx(aC, {})
        ] }),
        a ? /* @__PURE__ */ s.jsxs("div", { className: "text-center py-12", children: [
          /* @__PURE__ */ s.jsx("div", { className: "inline-block animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600" }),
          /* @__PURE__ */ s.jsx("p", { className: "mt-4 text-gray-600", children: "Loading workflows..." })
        ] }) : /* @__PURE__ */ s.jsx(nC, { tabs: y, defaultTab: "workflows" })
      ] })
    ] })
  ] });
}
function oC({
  title: e,
  value: t,
  change: r,
  unit: n,
  icon: a,
  trend: i,
  className: o
}) {
  const l = () => i === "up" ? "text-green-600" : i === "down" ? "text-red-600" : "text-gray-600", c = () => i === "up" ? "↑" : i === "down" ? "↓" : "→";
  return /* @__PURE__ */ s.jsxs("div", { className: Ie("metric-card bg-white border border-gray-200 rounded-lg p-6", o), children: [
    /* @__PURE__ */ s.jsxs("div", { className: "flex items-start justify-between mb-4", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex-1", children: [
        /* @__PURE__ */ s.jsx("h3", { className: "text-sm font-medium text-gray-600 mb-1", children: e }),
        /* @__PURE__ */ s.jsx("div", { className: "flex items-baseline gap-2", children: /* @__PURE__ */ s.jsxs("div", { className: "text-3xl font-bold text-gray-900", children: [
          typeof t == "number" ? t.toLocaleString() : t,
          n && /* @__PURE__ */ s.jsx("span", { className: "text-lg font-normal text-gray-600 ml-1", children: n })
        ] }) })
      ] }),
      a && /* @__PURE__ */ s.jsx("div", { className: "text-gray-400", children: a })
    ] }),
    r !== void 0 && /* @__PURE__ */ s.jsxs("div", { className: Ie("flex items-center gap-1 text-sm font-medium", l()), children: [
      /* @__PURE__ */ s.jsx("span", { children: c() }),
      /* @__PURE__ */ s.jsxs("span", { children: [
        Math.abs(r),
        "%"
      ] }),
      /* @__PURE__ */ s.jsx("span", { className: "text-gray-500 font-normal", children: "from last period" })
    ] })
  ] });
}
function sC({ stats: e, columns: t = 4, className: r }) {
  const n = {
    2: "grid-cols-1 md:grid-cols-2",
    3: "grid-cols-1 md:grid-cols-2 lg:grid-cols-3",
    4: "grid-cols-1 md:grid-cols-2 lg:grid-cols-4"
  };
  return /* @__PURE__ */ s.jsx("div", { className: Ie("stat-grid grid gap-4", n[t], r), children: e.map((a, i) => /* @__PURE__ */ s.jsx(oC, { ...a }, i)) });
}
var mc, Ig;
function Xt() {
  if (Ig) return mc;
  Ig = 1;
  var e = Array.isArray;
  return mc = e, mc;
}
var hc, Dg;
function xj() {
  if (Dg) return hc;
  Dg = 1;
  var e = typeof Wo == "object" && Wo && Wo.Object === Object && Wo;
  return hc = e, hc;
}
var pc, $g;
function Lr() {
  if ($g) return pc;
  $g = 1;
  var e = xj(), t = typeof self == "object" && self && self.Object === Object && self, r = e || t || Function("return this")();
  return pc = r, pc;
}
var gc, Lg;
function ko() {
  if (Lg) return gc;
  Lg = 1;
  var e = Lr(), t = e.Symbol;
  return gc = t, gc;
}
var yc, Fg;
function lC() {
  if (Fg) return yc;
  Fg = 1;
  var e = ko(), t = Object.prototype, r = t.hasOwnProperty, n = t.toString, a = e ? e.toStringTag : void 0;
  function i(o) {
    var l = r.call(o, a), c = o[a];
    try {
      o[a] = void 0;
      var u = !0;
    } catch {
    }
    var d = n.call(o);
    return u && (l ? o[a] = c : delete o[a]), d;
  }
  return yc = i, yc;
}
var vc, qg;
function cC() {
  if (qg) return vc;
  qg = 1;
  var e = Object.prototype, t = e.toString;
  function r(n) {
    return t.call(n);
  }
  return vc = r, vc;
}
var bc, Ug;
function nn() {
  if (Ug) return bc;
  Ug = 1;
  var e = ko(), t = lC(), r = cC(), n = "[object Null]", a = "[object Undefined]", i = e ? e.toStringTag : void 0;
  function o(l) {
    return l == null ? l === void 0 ? a : n : i && i in Object(l) ? t(l) : r(l);
  }
  return bc = o, bc;
}
var xc, zg;
function an() {
  if (zg) return xc;
  zg = 1;
  function e(t) {
    return t != null && typeof t == "object";
  }
  return xc = e, xc;
}
var wc, Bg;
function ei() {
  if (Bg) return wc;
  Bg = 1;
  var e = nn(), t = an(), r = "[object Symbol]";
  function n(a) {
    return typeof a == "symbol" || t(a) && e(a) == r;
  }
  return wc = n, wc;
}
var _c, Wg;
function Zh() {
  if (Wg) return _c;
  Wg = 1;
  var e = Xt(), t = ei(), r = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/, n = /^\w*$/;
  function a(i, o) {
    if (e(i))
      return !1;
    var l = typeof i;
    return l == "number" || l == "symbol" || l == "boolean" || i == null || t(i) ? !0 : n.test(i) || !r.test(i) || o != null && i in Object(o);
  }
  return _c = a, _c;
}
var jc, Vg;
function _n() {
  if (Vg) return jc;
  Vg = 1;
  function e(t) {
    var r = typeof t;
    return t != null && (r == "object" || r == "function");
  }
  return jc = e, jc;
}
var Sc, Hg;
function ep() {
  if (Hg) return Sc;
  Hg = 1;
  var e = nn(), t = _n(), r = "[object AsyncFunction]", n = "[object Function]", a = "[object GeneratorFunction]", i = "[object Proxy]";
  function o(l) {
    if (!t(l))
      return !1;
    var c = e(l);
    return c == n || c == a || c == r || c == i;
  }
  return Sc = o, Sc;
}
var kc, Gg;
function uC() {
  if (Gg) return kc;
  Gg = 1;
  var e = Lr(), t = e["__core-js_shared__"];
  return kc = t, kc;
}
var Ec, Kg;
function dC() {
  if (Kg) return Ec;
  Kg = 1;
  var e = uC(), t = (function() {
    var n = /[^.]+$/.exec(e && e.keys && e.keys.IE_PROTO || "");
    return n ? "Symbol(src)_1." + n : "";
  })();
  function r(n) {
    return !!t && t in n;
  }
  return Ec = r, Ec;
}
var Ac, Yg;
function wj() {
  if (Yg) return Ac;
  Yg = 1;
  var e = Function.prototype, t = e.toString;
  function r(n) {
    if (n != null) {
      try {
        return t.call(n);
      } catch {
      }
      try {
        return n + "";
      } catch {
      }
    }
    return "";
  }
  return Ac = r, Ac;
}
var Cc, Xg;
function fC() {
  if (Xg) return Cc;
  Xg = 1;
  var e = ep(), t = dC(), r = _n(), n = wj(), a = /[\\^$.*+?()[\]{}|]/g, i = /^\[object .+?Constructor\]$/, o = Function.prototype, l = Object.prototype, c = o.toString, u = l.hasOwnProperty, d = RegExp(
    "^" + c.call(u).replace(a, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
  );
  function f(p) {
    if (!r(p) || t(p))
      return !1;
    var m = e(p) ? d : i;
    return m.test(n(p));
  }
  return Cc = f, Cc;
}
var Tc, Qg;
function mC() {
  if (Qg) return Tc;
  Qg = 1;
  function e(t, r) {
    return t == null ? void 0 : t[r];
  }
  return Tc = e, Tc;
}
var Pc, Jg;
function Yn() {
  if (Jg) return Pc;
  Jg = 1;
  var e = fC(), t = mC();
  function r(n, a) {
    var i = t(n, a);
    return e(i) ? i : void 0;
  }
  return Pc = r, Pc;
}
var Oc, Zg;
function jl() {
  if (Zg) return Oc;
  Zg = 1;
  var e = Yn(), t = e(Object, "create");
  return Oc = t, Oc;
}
var Nc, ey;
function hC() {
  if (ey) return Nc;
  ey = 1;
  var e = jl();
  function t() {
    this.__data__ = e ? e(null) : {}, this.size = 0;
  }
  return Nc = t, Nc;
}
var Mc, ty;
function pC() {
  if (ty) return Mc;
  ty = 1;
  function e(t) {
    var r = this.has(t) && delete this.__data__[t];
    return this.size -= r ? 1 : 0, r;
  }
  return Mc = e, Mc;
}
var Rc, ry;
function gC() {
  if (ry) return Rc;
  ry = 1;
  var e = jl(), t = "__lodash_hash_undefined__", r = Object.prototype, n = r.hasOwnProperty;
  function a(i) {
    var o = this.__data__;
    if (e) {
      var l = o[i];
      return l === t ? void 0 : l;
    }
    return n.call(o, i) ? o[i] : void 0;
  }
  return Rc = a, Rc;
}
var Ic, ny;
function yC() {
  if (ny) return Ic;
  ny = 1;
  var e = jl(), t = Object.prototype, r = t.hasOwnProperty;
  function n(a) {
    var i = this.__data__;
    return e ? i[a] !== void 0 : r.call(i, a);
  }
  return Ic = n, Ic;
}
var Dc, ay;
function vC() {
  if (ay) return Dc;
  ay = 1;
  var e = jl(), t = "__lodash_hash_undefined__";
  function r(n, a) {
    var i = this.__data__;
    return this.size += this.has(n) ? 0 : 1, i[n] = e && a === void 0 ? t : a, this;
  }
  return Dc = r, Dc;
}
var $c, iy;
function bC() {
  if (iy) return $c;
  iy = 1;
  var e = hC(), t = pC(), r = gC(), n = yC(), a = vC();
  function i(o) {
    var l = -1, c = o == null ? 0 : o.length;
    for (this.clear(); ++l < c; ) {
      var u = o[l];
      this.set(u[0], u[1]);
    }
  }
  return i.prototype.clear = e, i.prototype.delete = t, i.prototype.get = r, i.prototype.has = n, i.prototype.set = a, $c = i, $c;
}
var Lc, oy;
function xC() {
  if (oy) return Lc;
  oy = 1;
  function e() {
    this.__data__ = [], this.size = 0;
  }
  return Lc = e, Lc;
}
var Fc, sy;
function tp() {
  if (sy) return Fc;
  sy = 1;
  function e(t, r) {
    return t === r || t !== t && r !== r;
  }
  return Fc = e, Fc;
}
var qc, ly;
function Sl() {
  if (ly) return qc;
  ly = 1;
  var e = tp();
  function t(r, n) {
    for (var a = r.length; a--; )
      if (e(r[a][0], n))
        return a;
    return -1;
  }
  return qc = t, qc;
}
var Uc, cy;
function wC() {
  if (cy) return Uc;
  cy = 1;
  var e = Sl(), t = Array.prototype, r = t.splice;
  function n(a) {
    var i = this.__data__, o = e(i, a);
    if (o < 0)
      return !1;
    var l = i.length - 1;
    return o == l ? i.pop() : r.call(i, o, 1), --this.size, !0;
  }
  return Uc = n, Uc;
}
var zc, uy;
function _C() {
  if (uy) return zc;
  uy = 1;
  var e = Sl();
  function t(r) {
    var n = this.__data__, a = e(n, r);
    return a < 0 ? void 0 : n[a][1];
  }
  return zc = t, zc;
}
var Bc, dy;
function jC() {
  if (dy) return Bc;
  dy = 1;
  var e = Sl();
  function t(r) {
    return e(this.__data__, r) > -1;
  }
  return Bc = t, Bc;
}
var Wc, fy;
function SC() {
  if (fy) return Wc;
  fy = 1;
  var e = Sl();
  function t(r, n) {
    var a = this.__data__, i = e(a, r);
    return i < 0 ? (++this.size, a.push([r, n])) : a[i][1] = n, this;
  }
  return Wc = t, Wc;
}
var Vc, my;
function kl() {
  if (my) return Vc;
  my = 1;
  var e = xC(), t = wC(), r = _C(), n = jC(), a = SC();
  function i(o) {
    var l = -1, c = o == null ? 0 : o.length;
    for (this.clear(); ++l < c; ) {
      var u = o[l];
      this.set(u[0], u[1]);
    }
  }
  return i.prototype.clear = e, i.prototype.delete = t, i.prototype.get = r, i.prototype.has = n, i.prototype.set = a, Vc = i, Vc;
}
var Hc, hy;
function rp() {
  if (hy) return Hc;
  hy = 1;
  var e = Yn(), t = Lr(), r = e(t, "Map");
  return Hc = r, Hc;
}
var Gc, py;
function kC() {
  if (py) return Gc;
  py = 1;
  var e = bC(), t = kl(), r = rp();
  function n() {
    this.size = 0, this.__data__ = {
      hash: new e(),
      map: new (r || t)(),
      string: new e()
    };
  }
  return Gc = n, Gc;
}
var Kc, gy;
function EC() {
  if (gy) return Kc;
  gy = 1;
  function e(t) {
    var r = typeof t;
    return r == "string" || r == "number" || r == "symbol" || r == "boolean" ? t !== "__proto__" : t === null;
  }
  return Kc = e, Kc;
}
var Yc, yy;
function El() {
  if (yy) return Yc;
  yy = 1;
  var e = EC();
  function t(r, n) {
    var a = r.__data__;
    return e(n) ? a[typeof n == "string" ? "string" : "hash"] : a.map;
  }
  return Yc = t, Yc;
}
var Xc, vy;
function AC() {
  if (vy) return Xc;
  vy = 1;
  var e = El();
  function t(r) {
    var n = e(this, r).delete(r);
    return this.size -= n ? 1 : 0, n;
  }
  return Xc = t, Xc;
}
var Qc, by;
function CC() {
  if (by) return Qc;
  by = 1;
  var e = El();
  function t(r) {
    return e(this, r).get(r);
  }
  return Qc = t, Qc;
}
var Jc, xy;
function TC() {
  if (xy) return Jc;
  xy = 1;
  var e = El();
  function t(r) {
    return e(this, r).has(r);
  }
  return Jc = t, Jc;
}
var Zc, wy;
function PC() {
  if (wy) return Zc;
  wy = 1;
  var e = El();
  function t(r, n) {
    var a = e(this, r), i = a.size;
    return a.set(r, n), this.size += a.size == i ? 0 : 1, this;
  }
  return Zc = t, Zc;
}
var eu, _y;
function np() {
  if (_y) return eu;
  _y = 1;
  var e = kC(), t = AC(), r = CC(), n = TC(), a = PC();
  function i(o) {
    var l = -1, c = o == null ? 0 : o.length;
    for (this.clear(); ++l < c; ) {
      var u = o[l];
      this.set(u[0], u[1]);
    }
  }
  return i.prototype.clear = e, i.prototype.delete = t, i.prototype.get = r, i.prototype.has = n, i.prototype.set = a, eu = i, eu;
}
var tu, jy;
function _j() {
  if (jy) return tu;
  jy = 1;
  var e = np(), t = "Expected a function";
  function r(n, a) {
    if (typeof n != "function" || a != null && typeof a != "function")
      throw new TypeError(t);
    var i = function() {
      var o = arguments, l = a ? a.apply(this, o) : o[0], c = i.cache;
      if (c.has(l))
        return c.get(l);
      var u = n.apply(this, o);
      return i.cache = c.set(l, u) || c, u;
    };
    return i.cache = new (r.Cache || e)(), i;
  }
  return r.Cache = e, tu = r, tu;
}
var ru, Sy;
function OC() {
  if (Sy) return ru;
  Sy = 1;
  var e = _j(), t = 500;
  function r(n) {
    var a = e(n, function(o) {
      return i.size === t && i.clear(), o;
    }), i = a.cache;
    return a;
  }
  return ru = r, ru;
}
var nu, ky;
function NC() {
  if (ky) return nu;
  ky = 1;
  var e = OC(), t = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g, r = /\\(\\)?/g, n = e(function(a) {
    var i = [];
    return a.charCodeAt(0) === 46 && i.push(""), a.replace(t, function(o, l, c, u) {
      i.push(c ? u.replace(r, "$1") : l || o);
    }), i;
  });
  return nu = n, nu;
}
var au, Ey;
function ap() {
  if (Ey) return au;
  Ey = 1;
  function e(t, r) {
    for (var n = -1, a = t == null ? 0 : t.length, i = Array(a); ++n < a; )
      i[n] = r(t[n], n, t);
    return i;
  }
  return au = e, au;
}
var iu, Ay;
function MC() {
  if (Ay) return iu;
  Ay = 1;
  var e = ko(), t = ap(), r = Xt(), n = ei(), a = e ? e.prototype : void 0, i = a ? a.toString : void 0;
  function o(l) {
    if (typeof l == "string")
      return l;
    if (r(l))
      return t(l, o) + "";
    if (n(l))
      return i ? i.call(l) : "";
    var c = l + "";
    return c == "0" && 1 / l == -1 / 0 ? "-0" : c;
  }
  return iu = o, iu;
}
var ou, Cy;
function jj() {
  if (Cy) return ou;
  Cy = 1;
  var e = MC();
  function t(r) {
    return r == null ? "" : e(r);
  }
  return ou = t, ou;
}
var su, Ty;
function Sj() {
  if (Ty) return su;
  Ty = 1;
  var e = Xt(), t = Zh(), r = NC(), n = jj();
  function a(i, o) {
    return e(i) ? i : t(i, o) ? [i] : r(n(i));
  }
  return su = a, su;
}
var lu, Py;
function Al() {
  if (Py) return lu;
  Py = 1;
  var e = ei();
  function t(r) {
    if (typeof r == "string" || e(r))
      return r;
    var n = r + "";
    return n == "0" && 1 / r == -1 / 0 ? "-0" : n;
  }
  return lu = t, lu;
}
var cu, Oy;
function ip() {
  if (Oy) return cu;
  Oy = 1;
  var e = Sj(), t = Al();
  function r(n, a) {
    a = e(a, n);
    for (var i = 0, o = a.length; n != null && i < o; )
      n = n[t(a[i++])];
    return i && i == o ? n : void 0;
  }
  return cu = r, cu;
}
var uu, Ny;
function kj() {
  if (Ny) return uu;
  Ny = 1;
  var e = ip();
  function t(r, n, a) {
    var i = r == null ? void 0 : e(r, n);
    return i === void 0 ? a : i;
  }
  return uu = t, uu;
}
var RC = kj();
const pr = /* @__PURE__ */ st(RC);
var du, My;
function IC() {
  if (My) return du;
  My = 1;
  function e(t) {
    return t == null;
  }
  return du = e, du;
}
var DC = IC();
const Le = /* @__PURE__ */ st(DC);
var fu, Ry;
function $C() {
  if (Ry) return fu;
  Ry = 1;
  var e = nn(), t = Xt(), r = an(), n = "[object String]";
  function a(i) {
    return typeof i == "string" || !t(i) && r(i) && e(i) == n;
  }
  return fu = a, fu;
}
var LC = $C();
const Wn = /* @__PURE__ */ st(LC);
var FC = ep();
const $e = /* @__PURE__ */ st(FC);
var qC = _n();
const ti = /* @__PURE__ */ st(qC);
var Ko = { exports: {} }, Ke = {};
/**
 * @license React
 * react-is.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var Iy;
function UC() {
  if (Iy) return Ke;
  Iy = 1;
  var e = Symbol.for("react.element"), t = Symbol.for("react.portal"), r = Symbol.for("react.fragment"), n = Symbol.for("react.strict_mode"), a = Symbol.for("react.profiler"), i = Symbol.for("react.provider"), o = Symbol.for("react.context"), l = Symbol.for("react.server_context"), c = Symbol.for("react.forward_ref"), u = Symbol.for("react.suspense"), d = Symbol.for("react.suspense_list"), f = Symbol.for("react.memo"), p = Symbol.for("react.lazy"), m = Symbol.for("react.offscreen"), y;
  y = Symbol.for("react.module.reference");
  function h(g) {
    if (typeof g == "object" && g !== null) {
      var v = g.$$typeof;
      switch (v) {
        case e:
          switch (g = g.type, g) {
            case r:
            case a:
            case n:
            case u:
            case d:
              return g;
            default:
              switch (g = g && g.$$typeof, g) {
                case l:
                case o:
                case c:
                case p:
                case f:
                case i:
                  return g;
                default:
                  return v;
              }
          }
        case t:
          return v;
      }
    }
  }
  return Ke.ContextConsumer = o, Ke.ContextProvider = i, Ke.Element = e, Ke.ForwardRef = c, Ke.Fragment = r, Ke.Lazy = p, Ke.Memo = f, Ke.Portal = t, Ke.Profiler = a, Ke.StrictMode = n, Ke.Suspense = u, Ke.SuspenseList = d, Ke.isAsyncMode = function() {
    return !1;
  }, Ke.isConcurrentMode = function() {
    return !1;
  }, Ke.isContextConsumer = function(g) {
    return h(g) === o;
  }, Ke.isContextProvider = function(g) {
    return h(g) === i;
  }, Ke.isElement = function(g) {
    return typeof g == "object" && g !== null && g.$$typeof === e;
  }, Ke.isForwardRef = function(g) {
    return h(g) === c;
  }, Ke.isFragment = function(g) {
    return h(g) === r;
  }, Ke.isLazy = function(g) {
    return h(g) === p;
  }, Ke.isMemo = function(g) {
    return h(g) === f;
  }, Ke.isPortal = function(g) {
    return h(g) === t;
  }, Ke.isProfiler = function(g) {
    return h(g) === a;
  }, Ke.isStrictMode = function(g) {
    return h(g) === n;
  }, Ke.isSuspense = function(g) {
    return h(g) === u;
  }, Ke.isSuspenseList = function(g) {
    return h(g) === d;
  }, Ke.isValidElementType = function(g) {
    return typeof g == "string" || typeof g == "function" || g === r || g === a || g === n || g === u || g === d || g === m || typeof g == "object" && g !== null && (g.$$typeof === p || g.$$typeof === f || g.$$typeof === i || g.$$typeof === o || g.$$typeof === c || g.$$typeof === y || g.getModuleId !== void 0);
  }, Ke.typeOf = h, Ke;
}
var Ye = {};
/**
 * @license React
 * react-is.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var Dy;
function zC() {
  return Dy || (Dy = 1, process.env.NODE_ENV !== "production" && (function() {
    var e = Symbol.for("react.element"), t = Symbol.for("react.portal"), r = Symbol.for("react.fragment"), n = Symbol.for("react.strict_mode"), a = Symbol.for("react.profiler"), i = Symbol.for("react.provider"), o = Symbol.for("react.context"), l = Symbol.for("react.server_context"), c = Symbol.for("react.forward_ref"), u = Symbol.for("react.suspense"), d = Symbol.for("react.suspense_list"), f = Symbol.for("react.memo"), p = Symbol.for("react.lazy"), m = Symbol.for("react.offscreen"), y = !1, h = !1, g = !1, v = !1, b = !1, w;
    w = Symbol.for("react.module.reference");
    function j(re) {
      return !!(typeof re == "string" || typeof re == "function" || re === r || re === a || b || re === n || re === u || re === d || v || re === m || y || h || g || typeof re == "object" && re !== null && (re.$$typeof === p || re.$$typeof === f || re.$$typeof === i || re.$$typeof === o || re.$$typeof === c || // This needs to include all possible module reference object
      // types supported by any Flight configuration anywhere since
      // we don't know which Flight build this will end up being used
      // with.
      re.$$typeof === w || re.getModuleId !== void 0));
    }
    function x(re) {
      if (typeof re == "object" && re !== null) {
        var Pe = re.$$typeof;
        switch (Pe) {
          case e:
            var Oe = re.type;
            switch (Oe) {
              case r:
              case a:
              case n:
              case u:
              case d:
                return Oe;
              default:
                var Ne = Oe && Oe.$$typeof;
                switch (Ne) {
                  case l:
                  case o:
                  case c:
                  case p:
                  case f:
                  case i:
                    return Ne;
                  default:
                    return Pe;
                }
            }
          case t:
            return Pe;
        }
      }
    }
    var _ = o, S = i, k = e, E = c, L = r, P = p, A = f, T = t, M = a, N = n, q = u, V = d, B = !1, ee = !1;
    function U(re) {
      return B || (B = !0, console.warn("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 18+.")), !1;
    }
    function G(re) {
      return ee || (ee = !0, console.warn("The ReactIs.isConcurrentMode() alias has been deprecated, and will be removed in React 18+.")), !1;
    }
    function Y(re) {
      return x(re) === o;
    }
    function Q(re) {
      return x(re) === i;
    }
    function ne(re) {
      return typeof re == "object" && re !== null && re.$$typeof === e;
    }
    function z(re) {
      return x(re) === c;
    }
    function se(re) {
      return x(re) === r;
    }
    function oe(re) {
      return x(re) === p;
    }
    function ie(re) {
      return x(re) === f;
    }
    function K(re) {
      return x(re) === t;
    }
    function ae(re) {
      return x(re) === a;
    }
    function fe(re) {
      return x(re) === n;
    }
    function $(re) {
      return x(re) === u;
    }
    function Ee(re) {
      return x(re) === d;
    }
    Ye.ContextConsumer = _, Ye.ContextProvider = S, Ye.Element = k, Ye.ForwardRef = E, Ye.Fragment = L, Ye.Lazy = P, Ye.Memo = A, Ye.Portal = T, Ye.Profiler = M, Ye.StrictMode = N, Ye.Suspense = q, Ye.SuspenseList = V, Ye.isAsyncMode = U, Ye.isConcurrentMode = G, Ye.isContextConsumer = Y, Ye.isContextProvider = Q, Ye.isElement = ne, Ye.isForwardRef = z, Ye.isFragment = se, Ye.isLazy = oe, Ye.isMemo = ie, Ye.isPortal = K, Ye.isProfiler = ae, Ye.isStrictMode = fe, Ye.isSuspense = $, Ye.isSuspenseList = Ee, Ye.isValidElementType = j, Ye.typeOf = x;
  })()), Ye;
}
var $y;
function BC() {
  return $y || ($y = 1, process.env.NODE_ENV === "production" ? Ko.exports = UC() : Ko.exports = zC()), Ko.exports;
}
var WC = BC(), mu, Ly;
function Ej() {
  if (Ly) return mu;
  Ly = 1;
  var e = nn(), t = an(), r = "[object Number]";
  function n(a) {
    return typeof a == "number" || t(a) && e(a) == r;
  }
  return mu = n, mu;
}
var hu, Fy;
function VC() {
  if (Fy) return hu;
  Fy = 1;
  var e = Ej();
  function t(r) {
    return e(r) && r != +r;
  }
  return hu = t, hu;
}
var HC = VC();
const Eo = /* @__PURE__ */ st(HC);
var GC = Ej();
const KC = /* @__PURE__ */ st(GC);
var Ar = function(t) {
  return t === 0 ? 0 : t > 0 ? 1 : -1;
}, In = function(t) {
  return Wn(t) && t.indexOf("%") === t.length - 1;
}, me = function(t) {
  return KC(t) && !Eo(t);
}, YC = function(t) {
  return Le(t);
}, Pt = function(t) {
  return me(t) || Wn(t);
}, XC = 0, Ao = function(t) {
  var r = ++XC;
  return "".concat(t || "").concat(r);
}, Vn = function(t, r) {
  var n = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 0, a = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : !1;
  if (!me(t) && !Wn(t))
    return n;
  var i;
  if (In(t)) {
    var o = t.indexOf("%");
    i = r * parseFloat(t.slice(0, o)) / 100;
  } else
    i = +t;
  return Eo(i) && (i = n), a && i > r && (i = r), i;
}, pn = function(t) {
  if (!t)
    return null;
  var r = Object.keys(t);
  return r && r.length ? t[r[0]] : null;
}, QC = function(t) {
  if (!Array.isArray(t))
    return !1;
  for (var r = t.length, n = {}, a = 0; a < r; a++)
    if (!n[t[a]])
      n[t[a]] = !0;
    else
      return !0;
  return !1;
}, jr = function(t, r) {
  return me(t) && me(r) ? function(n) {
    return t + n * (r - t);
  } : function() {
    return r;
  };
};
function hs(e, t, r) {
  return !e || !e.length ? null : e.find(function(n) {
    return n && (typeof t == "function" ? t(n) : pr(n, t)) === r;
  });
}
var JC = function(t, r) {
  return me(t) && me(r) ? t - r : Wn(t) && Wn(r) ? t.localeCompare(r) : t instanceof Date && r instanceof Date ? t.getTime() - r.getTime() : String(t).localeCompare(String(r));
};
function ja(e, t) {
  for (var r in e)
    if ({}.hasOwnProperty.call(e, r) && (!{}.hasOwnProperty.call(t, r) || e[r] !== t[r]))
      return !1;
  for (var n in t)
    if ({}.hasOwnProperty.call(t, n) && !{}.hasOwnProperty.call(e, n))
      return !1;
  return !0;
}
function Em(e) {
  "@babel/helpers - typeof";
  return Em = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Em(e);
}
var ZC = ["viewBox", "children"], eT = [
  "aria-activedescendant",
  "aria-atomic",
  "aria-autocomplete",
  "aria-busy",
  "aria-checked",
  "aria-colcount",
  "aria-colindex",
  "aria-colspan",
  "aria-controls",
  "aria-current",
  "aria-describedby",
  "aria-details",
  "aria-disabled",
  "aria-errormessage",
  "aria-expanded",
  "aria-flowto",
  "aria-haspopup",
  "aria-hidden",
  "aria-invalid",
  "aria-keyshortcuts",
  "aria-label",
  "aria-labelledby",
  "aria-level",
  "aria-live",
  "aria-modal",
  "aria-multiline",
  "aria-multiselectable",
  "aria-orientation",
  "aria-owns",
  "aria-placeholder",
  "aria-posinset",
  "aria-pressed",
  "aria-readonly",
  "aria-relevant",
  "aria-required",
  "aria-roledescription",
  "aria-rowcount",
  "aria-rowindex",
  "aria-rowspan",
  "aria-selected",
  "aria-setsize",
  "aria-sort",
  "aria-valuemax",
  "aria-valuemin",
  "aria-valuenow",
  "aria-valuetext",
  "className",
  "color",
  "height",
  "id",
  "lang",
  "max",
  "media",
  "method",
  "min",
  "name",
  "style",
  /*
   * removed 'type' SVGElementPropKey because we do not currently use any SVG elements
   * that can use it and it conflicts with the recharts prop 'type'
   * https://github.com/recharts/recharts/pull/3327
   * https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/type
   */
  // 'type',
  "target",
  "width",
  "role",
  "tabIndex",
  "accentHeight",
  "accumulate",
  "additive",
  "alignmentBaseline",
  "allowReorder",
  "alphabetic",
  "amplitude",
  "arabicForm",
  "ascent",
  "attributeName",
  "attributeType",
  "autoReverse",
  "azimuth",
  "baseFrequency",
  "baselineShift",
  "baseProfile",
  "bbox",
  "begin",
  "bias",
  "by",
  "calcMode",
  "capHeight",
  "clip",
  "clipPath",
  "clipPathUnits",
  "clipRule",
  "colorInterpolation",
  "colorInterpolationFilters",
  "colorProfile",
  "colorRendering",
  "contentScriptType",
  "contentStyleType",
  "cursor",
  "cx",
  "cy",
  "d",
  "decelerate",
  "descent",
  "diffuseConstant",
  "direction",
  "display",
  "divisor",
  "dominantBaseline",
  "dur",
  "dx",
  "dy",
  "edgeMode",
  "elevation",
  "enableBackground",
  "end",
  "exponent",
  "externalResourcesRequired",
  "fill",
  "fillOpacity",
  "fillRule",
  "filter",
  "filterRes",
  "filterUnits",
  "floodColor",
  "floodOpacity",
  "focusable",
  "fontFamily",
  "fontSize",
  "fontSizeAdjust",
  "fontStretch",
  "fontStyle",
  "fontVariant",
  "fontWeight",
  "format",
  "from",
  "fx",
  "fy",
  "g1",
  "g2",
  "glyphName",
  "glyphOrientationHorizontal",
  "glyphOrientationVertical",
  "glyphRef",
  "gradientTransform",
  "gradientUnits",
  "hanging",
  "horizAdvX",
  "horizOriginX",
  "href",
  "ideographic",
  "imageRendering",
  "in2",
  "in",
  "intercept",
  "k1",
  "k2",
  "k3",
  "k4",
  "k",
  "kernelMatrix",
  "kernelUnitLength",
  "kerning",
  "keyPoints",
  "keySplines",
  "keyTimes",
  "lengthAdjust",
  "letterSpacing",
  "lightingColor",
  "limitingConeAngle",
  "local",
  "markerEnd",
  "markerHeight",
  "markerMid",
  "markerStart",
  "markerUnits",
  "markerWidth",
  "mask",
  "maskContentUnits",
  "maskUnits",
  "mathematical",
  "mode",
  "numOctaves",
  "offset",
  "opacity",
  "operator",
  "order",
  "orient",
  "orientation",
  "origin",
  "overflow",
  "overlinePosition",
  "overlineThickness",
  "paintOrder",
  "panose1",
  "pathLength",
  "patternContentUnits",
  "patternTransform",
  "patternUnits",
  "pointerEvents",
  "pointsAtX",
  "pointsAtY",
  "pointsAtZ",
  "preserveAlpha",
  "preserveAspectRatio",
  "primitiveUnits",
  "r",
  "radius",
  "refX",
  "refY",
  "renderingIntent",
  "repeatCount",
  "repeatDur",
  "requiredExtensions",
  "requiredFeatures",
  "restart",
  "result",
  "rotate",
  "rx",
  "ry",
  "seed",
  "shapeRendering",
  "slope",
  "spacing",
  "specularConstant",
  "specularExponent",
  "speed",
  "spreadMethod",
  "startOffset",
  "stdDeviation",
  "stemh",
  "stemv",
  "stitchTiles",
  "stopColor",
  "stopOpacity",
  "strikethroughPosition",
  "strikethroughThickness",
  "string",
  "stroke",
  "strokeDasharray",
  "strokeDashoffset",
  "strokeLinecap",
  "strokeLinejoin",
  "strokeMiterlimit",
  "strokeOpacity",
  "strokeWidth",
  "surfaceScale",
  "systemLanguage",
  "tableValues",
  "targetX",
  "targetY",
  "textAnchor",
  "textDecoration",
  "textLength",
  "textRendering",
  "to",
  "transform",
  "u1",
  "u2",
  "underlinePosition",
  "underlineThickness",
  "unicode",
  "unicodeBidi",
  "unicodeRange",
  "unitsPerEm",
  "vAlphabetic",
  "values",
  "vectorEffect",
  "version",
  "vertAdvY",
  "vertOriginX",
  "vertOriginY",
  "vHanging",
  "vIdeographic",
  "viewTarget",
  "visibility",
  "vMathematical",
  "widths",
  "wordSpacing",
  "writingMode",
  "x1",
  "x2",
  "x",
  "xChannelSelector",
  "xHeight",
  "xlinkActuate",
  "xlinkArcrole",
  "xlinkHref",
  "xlinkRole",
  "xlinkShow",
  "xlinkTitle",
  "xlinkType",
  "xmlBase",
  "xmlLang",
  "xmlns",
  "xmlnsXlink",
  "xmlSpace",
  "y1",
  "y2",
  "y",
  "yChannelSelector",
  "z",
  "zoomAndPan",
  "ref",
  "key",
  "angle"
], qy = ["points", "pathLength"], pu = {
  svg: ZC,
  polygon: qy,
  polyline: qy
}, op = ["dangerouslySetInnerHTML", "onCopy", "onCopyCapture", "onCut", "onCutCapture", "onPaste", "onPasteCapture", "onCompositionEnd", "onCompositionEndCapture", "onCompositionStart", "onCompositionStartCapture", "onCompositionUpdate", "onCompositionUpdateCapture", "onFocus", "onFocusCapture", "onBlur", "onBlurCapture", "onChange", "onChangeCapture", "onBeforeInput", "onBeforeInputCapture", "onInput", "onInputCapture", "onReset", "onResetCapture", "onSubmit", "onSubmitCapture", "onInvalid", "onInvalidCapture", "onLoad", "onLoadCapture", "onError", "onErrorCapture", "onKeyDown", "onKeyDownCapture", "onKeyPress", "onKeyPressCapture", "onKeyUp", "onKeyUpCapture", "onAbort", "onAbortCapture", "onCanPlay", "onCanPlayCapture", "onCanPlayThrough", "onCanPlayThroughCapture", "onDurationChange", "onDurationChangeCapture", "onEmptied", "onEmptiedCapture", "onEncrypted", "onEncryptedCapture", "onEnded", "onEndedCapture", "onLoadedData", "onLoadedDataCapture", "onLoadedMetadata", "onLoadedMetadataCapture", "onLoadStart", "onLoadStartCapture", "onPause", "onPauseCapture", "onPlay", "onPlayCapture", "onPlaying", "onPlayingCapture", "onProgress", "onProgressCapture", "onRateChange", "onRateChangeCapture", "onSeeked", "onSeekedCapture", "onSeeking", "onSeekingCapture", "onStalled", "onStalledCapture", "onSuspend", "onSuspendCapture", "onTimeUpdate", "onTimeUpdateCapture", "onVolumeChange", "onVolumeChangeCapture", "onWaiting", "onWaitingCapture", "onAuxClick", "onAuxClickCapture", "onClick", "onClickCapture", "onContextMenu", "onContextMenuCapture", "onDoubleClick", "onDoubleClickCapture", "onDrag", "onDragCapture", "onDragEnd", "onDragEndCapture", "onDragEnter", "onDragEnterCapture", "onDragExit", "onDragExitCapture", "onDragLeave", "onDragLeaveCapture", "onDragOver", "onDragOverCapture", "onDragStart", "onDragStartCapture", "onDrop", "onDropCapture", "onMouseDown", "onMouseDownCapture", "onMouseEnter", "onMouseLeave", "onMouseMove", "onMouseMoveCapture", "onMouseOut", "onMouseOutCapture", "onMouseOver", "onMouseOverCapture", "onMouseUp", "onMouseUpCapture", "onSelect", "onSelectCapture", "onTouchCancel", "onTouchCancelCapture", "onTouchEnd", "onTouchEndCapture", "onTouchMove", "onTouchMoveCapture", "onTouchStart", "onTouchStartCapture", "onPointerDown", "onPointerDownCapture", "onPointerMove", "onPointerMoveCapture", "onPointerUp", "onPointerUpCapture", "onPointerCancel", "onPointerCancelCapture", "onPointerEnter", "onPointerEnterCapture", "onPointerLeave", "onPointerLeaveCapture", "onPointerOver", "onPointerOverCapture", "onPointerOut", "onPointerOutCapture", "onGotPointerCapture", "onGotPointerCaptureCapture", "onLostPointerCapture", "onLostPointerCaptureCapture", "onScroll", "onScrollCapture", "onWheel", "onWheelCapture", "onAnimationStart", "onAnimationStartCapture", "onAnimationEnd", "onAnimationEndCapture", "onAnimationIteration", "onAnimationIterationCapture", "onTransitionEnd", "onTransitionEndCapture"], ps = function(t, r) {
  if (!t || typeof t == "function" || typeof t == "boolean")
    return null;
  var n = t;
  if (/* @__PURE__ */ Cr(t) && (n = t.props), !ti(n))
    return null;
  var a = {};
  return Object.keys(n).forEach(function(i) {
    op.includes(i) && (a[i] = r || function(o) {
      return n[i](n, o);
    });
  }), a;
}, tT = function(t, r, n) {
  return function(a) {
    return t(r, n, a), null;
  };
}, gs = function(t, r, n) {
  if (!ti(t) || Em(t) !== "object")
    return null;
  var a = null;
  return Object.keys(t).forEach(function(i) {
    var o = t[i];
    op.includes(i) && typeof o == "function" && (a || (a = {}), a[i] = tT(o, r, n));
  }), a;
}, rT = ["children"], nT = ["children"];
function Uy(e, t) {
  if (e == null) return {};
  var r = aT(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function aT(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
function Am(e) {
  "@babel/helpers - typeof";
  return Am = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Am(e);
}
var zy = {
  click: "onClick",
  mousedown: "onMouseDown",
  mouseup: "onMouseUp",
  mouseover: "onMouseOver",
  mousemove: "onMouseMove",
  mouseout: "onMouseOut",
  mouseenter: "onMouseEnter",
  mouseleave: "onMouseLeave",
  touchcancel: "onTouchCancel",
  touchend: "onTouchEnd",
  touchmove: "onTouchMove",
  touchstart: "onTouchStart",
  contextmenu: "onContextMenu",
  dblclick: "onDoubleClick"
}, Yr = function(t) {
  return typeof t == "string" ? t : t ? t.displayName || t.name || "Component" : "";
}, By = null, gu = null, sp = function e(t) {
  if (t === By && Array.isArray(gu))
    return gu;
  var r = [];
  return qn.forEach(t, function(n) {
    Le(n) || (WC.isFragment(n) ? r = r.concat(e(n.props.children)) : r.push(n));
  }), gu = r, By = t, r;
};
function gr(e, t) {
  var r = [], n = [];
  return Array.isArray(t) ? n = t.map(function(a) {
    return Yr(a);
  }) : n = [Yr(t)], sp(e).forEach(function(a) {
    var i = pr(a, "type.displayName") || pr(a, "type.name");
    n.indexOf(i) !== -1 && r.push(a);
  }), r;
}
function rr(e, t) {
  var r = gr(e, t);
  return r && r[0];
}
var Wy = function(t) {
  if (!t || !t.props)
    return !1;
  var r = t.props, n = r.width, a = r.height;
  return !(!me(n) || n <= 0 || !me(a) || a <= 0);
}, iT = ["a", "altGlyph", "altGlyphDef", "altGlyphItem", "animate", "animateColor", "animateMotion", "animateTransform", "circle", "clipPath", "color-profile", "cursor", "defs", "desc", "ellipse", "feBlend", "feColormatrix", "feComponentTransfer", "feComposite", "feConvolveMatrix", "feDiffuseLighting", "feDisplacementMap", "feDistantLight", "feFlood", "feFuncA", "feFuncB", "feFuncG", "feFuncR", "feGaussianBlur", "feImage", "feMerge", "feMergeNode", "feMorphology", "feOffset", "fePointLight", "feSpecularLighting", "feSpotLight", "feTile", "feTurbulence", "filter", "font", "font-face", "font-face-format", "font-face-name", "font-face-url", "foreignObject", "g", "glyph", "glyphRef", "hkern", "image", "line", "lineGradient", "marker", "mask", "metadata", "missing-glyph", "mpath", "path", "pattern", "polygon", "polyline", "radialGradient", "rect", "script", "set", "stop", "style", "svg", "switch", "symbol", "text", "textPath", "title", "tref", "tspan", "use", "view", "vkern"], oT = function(t) {
  return t && t.type && Wn(t.type) && iT.indexOf(t.type) >= 0;
}, sT = function(t) {
  return t && Am(t) === "object" && "clipDot" in t;
}, lT = function(t, r, n, a) {
  var i, o = (i = pu == null ? void 0 : pu[a]) !== null && i !== void 0 ? i : [];
  return r.startsWith("data-") || !$e(t) && (a && o.includes(r) || eT.includes(r)) || n && op.includes(r);
}, ze = function(t, r, n) {
  if (!t || typeof t == "function" || typeof t == "boolean")
    return null;
  var a = t;
  if (/* @__PURE__ */ Cr(t) && (a = t.props), !ti(a))
    return null;
  var i = {};
  return Object.keys(a).forEach(function(o) {
    var l;
    lT((l = a) === null || l === void 0 ? void 0 : l[o], o, r, n) && (i[o] = a[o]);
  }), i;
}, Cm = function e(t, r) {
  if (t === r)
    return !0;
  var n = qn.count(t);
  if (n !== qn.count(r))
    return !1;
  if (n === 0)
    return !0;
  if (n === 1)
    return Vy(Array.isArray(t) ? t[0] : t, Array.isArray(r) ? r[0] : r);
  for (var a = 0; a < n; a++) {
    var i = t[a], o = r[a];
    if (Array.isArray(i) || Array.isArray(o)) {
      if (!e(i, o))
        return !1;
    } else if (!Vy(i, o))
      return !1;
  }
  return !0;
}, Vy = function(t, r) {
  if (Le(t) && Le(r))
    return !0;
  if (!Le(t) && !Le(r)) {
    var n = t.props || {}, a = n.children, i = Uy(n, rT), o = r.props || {}, l = o.children, c = Uy(o, nT);
    return a && l ? ja(i, c) && Cm(a, l) : !a && !l ? ja(i, c) : !1;
  }
  return !1;
}, Hy = function(t, r) {
  var n = [], a = {};
  return sp(t).forEach(function(i, o) {
    if (oT(i))
      n.push(i);
    else if (i) {
      var l = Yr(i.type), c = r[l] || {}, u = c.handler, d = c.once;
      if (u && (!d || !a[l])) {
        var f = u(i, l, o);
        n.push(f), a[l] = !0;
      }
    }
  }), n;
}, cT = function(t) {
  var r = t && t.type;
  return r && zy[r] ? zy[r] : null;
}, uT = function(t, r) {
  return sp(r).indexOf(t);
}, dT = ["children", "width", "height", "viewBox", "className", "style", "title", "desc"];
function Tm() {
  return Tm = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Tm.apply(this, arguments);
}
function fT(e, t) {
  if (e == null) return {};
  var r = mT(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function mT(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
function Pm(e) {
  var t = e.children, r = e.width, n = e.height, a = e.viewBox, i = e.className, o = e.style, l = e.title, c = e.desc, u = fT(e, dT), d = a || {
    width: r,
    height: n,
    x: 0,
    y: 0
  }, f = Ve("recharts-surface", i);
  return /* @__PURE__ */ I.createElement("svg", Tm({}, ze(u, !0, "svg"), {
    className: f,
    width: r,
    height: n,
    style: o,
    viewBox: "".concat(d.x, " ").concat(d.y, " ").concat(d.width, " ").concat(d.height)
  }), /* @__PURE__ */ I.createElement("title", null, l), /* @__PURE__ */ I.createElement("desc", null, c), t);
}
var hT = ["children", "className"];
function Om() {
  return Om = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Om.apply(this, arguments);
}
function pT(e, t) {
  if (e == null) return {};
  var r = gT(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function gT(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
var yt = /* @__PURE__ */ I.forwardRef(function(e, t) {
  var r = e.children, n = e.className, a = pT(e, hT), i = Ve("recharts-layer", n);
  return /* @__PURE__ */ I.createElement("g", Om({
    className: i
  }, ze(a, !0), {
    ref: t
  }), r);
}), yT = process.env.NODE_ENV !== "production", Xr = function(t, r) {
  for (var n = arguments.length, a = new Array(n > 2 ? n - 2 : 0), i = 2; i < n; i++)
    a[i - 2] = arguments[i];
  if (yT && typeof console < "u" && console.warn && (r === void 0 && console.warn("LogUtils requires an error message argument"), !t))
    if (r === void 0)
      console.warn("Minified exception occurred; use the non-minified dev environment for the full error message and additional helpful warnings.");
    else {
      var o = 0;
      console.warn(r.replace(/%s/g, function() {
        return a[o++];
      }));
    }
}, yu, Gy;
function vT() {
  if (Gy) return yu;
  Gy = 1;
  function e(t, r, n) {
    var a = -1, i = t.length;
    r < 0 && (r = -r > i ? 0 : i + r), n = n > i ? i : n, n < 0 && (n += i), i = r > n ? 0 : n - r >>> 0, r >>>= 0;
    for (var o = Array(i); ++a < i; )
      o[a] = t[a + r];
    return o;
  }
  return yu = e, yu;
}
var vu, Ky;
function bT() {
  if (Ky) return vu;
  Ky = 1;
  var e = vT();
  function t(r, n, a) {
    var i = r.length;
    return a = a === void 0 ? i : a, !n && a >= i ? r : e(r, n, a);
  }
  return vu = t, vu;
}
var bu, Yy;
function Aj() {
  if (Yy) return bu;
  Yy = 1;
  var e = "\\ud800-\\udfff", t = "\\u0300-\\u036f", r = "\\ufe20-\\ufe2f", n = "\\u20d0-\\u20ff", a = t + r + n, i = "\\ufe0e\\ufe0f", o = "\\u200d", l = RegExp("[" + o + e + a + i + "]");
  function c(u) {
    return l.test(u);
  }
  return bu = c, bu;
}
var xu, Xy;
function xT() {
  if (Xy) return xu;
  Xy = 1;
  function e(t) {
    return t.split("");
  }
  return xu = e, xu;
}
var wu, Qy;
function wT() {
  if (Qy) return wu;
  Qy = 1;
  var e = "\\ud800-\\udfff", t = "\\u0300-\\u036f", r = "\\ufe20-\\ufe2f", n = "\\u20d0-\\u20ff", a = t + r + n, i = "\\ufe0e\\ufe0f", o = "[" + e + "]", l = "[" + a + "]", c = "\\ud83c[\\udffb-\\udfff]", u = "(?:" + l + "|" + c + ")", d = "[^" + e + "]", f = "(?:\\ud83c[\\udde6-\\uddff]){2}", p = "[\\ud800-\\udbff][\\udc00-\\udfff]", m = "\\u200d", y = u + "?", h = "[" + i + "]?", g = "(?:" + m + "(?:" + [d, f, p].join("|") + ")" + h + y + ")*", v = h + y + g, b = "(?:" + [d + l + "?", l, f, p, o].join("|") + ")", w = RegExp(c + "(?=" + c + ")|" + b + v, "g");
  function j(x) {
    return x.match(w) || [];
  }
  return wu = j, wu;
}
var _u, Jy;
function _T() {
  if (Jy) return _u;
  Jy = 1;
  var e = xT(), t = Aj(), r = wT();
  function n(a) {
    return t(a) ? r(a) : e(a);
  }
  return _u = n, _u;
}
var ju, Zy;
function jT() {
  if (Zy) return ju;
  Zy = 1;
  var e = bT(), t = Aj(), r = _T(), n = jj();
  function a(i) {
    return function(o) {
      o = n(o);
      var l = t(o) ? r(o) : void 0, c = l ? l[0] : o.charAt(0), u = l ? e(l, 1).join("") : o.slice(1);
      return c[i]() + u;
    };
  }
  return ju = a, ju;
}
var Su, ev;
function ST() {
  if (ev) return Su;
  ev = 1;
  var e = jT(), t = e("toUpperCase");
  return Su = t, Su;
}
var kT = ST();
const Cl = /* @__PURE__ */ st(kT);
function it(e) {
  return function() {
    return e;
  };
}
const Cj = Math.cos, ys = Math.sin, Pr = Math.sqrt, vs = Math.PI, Tl = 2 * vs, Nm = Math.PI, Mm = 2 * Nm, Nn = 1e-6, ET = Mm - Nn;
function Tj(e) {
  this._ += e[0];
  for (let t = 1, r = e.length; t < r; ++t)
    this._ += arguments[t] + e[t];
}
function AT(e) {
  let t = Math.floor(e);
  if (!(t >= 0)) throw new Error(`invalid digits: ${e}`);
  if (t > 15) return Tj;
  const r = 10 ** t;
  return function(n) {
    this._ += n[0];
    for (let a = 1, i = n.length; a < i; ++a)
      this._ += Math.round(arguments[a] * r) / r + n[a];
  };
}
class CT {
  constructor(t) {
    this._x0 = this._y0 = // start of current subpath
    this._x1 = this._y1 = null, this._ = "", this._append = t == null ? Tj : AT(t);
  }
  moveTo(t, r) {
    this._append`M${this._x0 = this._x1 = +t},${this._y0 = this._y1 = +r}`;
  }
  closePath() {
    this._x1 !== null && (this._x1 = this._x0, this._y1 = this._y0, this._append`Z`);
  }
  lineTo(t, r) {
    this._append`L${this._x1 = +t},${this._y1 = +r}`;
  }
  quadraticCurveTo(t, r, n, a) {
    this._append`Q${+t},${+r},${this._x1 = +n},${this._y1 = +a}`;
  }
  bezierCurveTo(t, r, n, a, i, o) {
    this._append`C${+t},${+r},${+n},${+a},${this._x1 = +i},${this._y1 = +o}`;
  }
  arcTo(t, r, n, a, i) {
    if (t = +t, r = +r, n = +n, a = +a, i = +i, i < 0) throw new Error(`negative radius: ${i}`);
    let o = this._x1, l = this._y1, c = n - t, u = a - r, d = o - t, f = l - r, p = d * d + f * f;
    if (this._x1 === null)
      this._append`M${this._x1 = t},${this._y1 = r}`;
    else if (p > Nn) if (!(Math.abs(f * c - u * d) > Nn) || !i)
      this._append`L${this._x1 = t},${this._y1 = r}`;
    else {
      let m = n - o, y = a - l, h = c * c + u * u, g = m * m + y * y, v = Math.sqrt(h), b = Math.sqrt(p), w = i * Math.tan((Nm - Math.acos((h + p - g) / (2 * v * b))) / 2), j = w / b, x = w / v;
      Math.abs(j - 1) > Nn && this._append`L${t + j * d},${r + j * f}`, this._append`A${i},${i},0,0,${+(f * m > d * y)},${this._x1 = t + x * c},${this._y1 = r + x * u}`;
    }
  }
  arc(t, r, n, a, i, o) {
    if (t = +t, r = +r, n = +n, o = !!o, n < 0) throw new Error(`negative radius: ${n}`);
    let l = n * Math.cos(a), c = n * Math.sin(a), u = t + l, d = r + c, f = 1 ^ o, p = o ? a - i : i - a;
    this._x1 === null ? this._append`M${u},${d}` : (Math.abs(this._x1 - u) > Nn || Math.abs(this._y1 - d) > Nn) && this._append`L${u},${d}`, n && (p < 0 && (p = p % Mm + Mm), p > ET ? this._append`A${n},${n},0,1,${f},${t - l},${r - c}A${n},${n},0,1,${f},${this._x1 = u},${this._y1 = d}` : p > Nn && this._append`A${n},${n},0,${+(p >= Nm)},${f},${this._x1 = t + n * Math.cos(i)},${this._y1 = r + n * Math.sin(i)}`);
  }
  rect(t, r, n, a) {
    this._append`M${this._x0 = this._x1 = +t},${this._y0 = this._y1 = +r}h${n = +n}v${+a}h${-n}Z`;
  }
  toString() {
    return this._;
  }
}
function lp(e) {
  let t = 3;
  return e.digits = function(r) {
    if (!arguments.length) return t;
    if (r == null)
      t = null;
    else {
      const n = Math.floor(r);
      if (!(n >= 0)) throw new RangeError(`invalid digits: ${r}`);
      t = n;
    }
    return e;
  }, () => new CT(t);
}
function cp(e) {
  return typeof e == "object" && "length" in e ? e : Array.from(e);
}
function Pj(e) {
  this._context = e;
}
Pj.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    (this._line || this._line !== 0 && this._point === 1) && this._context.closePath(), this._line = 1 - this._line;
  },
  point: function(e, t) {
    switch (e = +e, t = +t, this._point) {
      case 0:
        this._point = 1, this._line ? this._context.lineTo(e, t) : this._context.moveTo(e, t);
        break;
      case 1:
        this._point = 2;
      // falls through
      default:
        this._context.lineTo(e, t);
        break;
    }
  }
};
function Pl(e) {
  return new Pj(e);
}
function Oj(e) {
  return e[0];
}
function Nj(e) {
  return e[1];
}
function Mj(e, t) {
  var r = it(!0), n = null, a = Pl, i = null, o = lp(l);
  e = typeof e == "function" ? e : e === void 0 ? Oj : it(e), t = typeof t == "function" ? t : t === void 0 ? Nj : it(t);
  function l(c) {
    var u, d = (c = cp(c)).length, f, p = !1, m;
    for (n == null && (i = a(m = o())), u = 0; u <= d; ++u)
      !(u < d && r(f = c[u], u, c)) === p && ((p = !p) ? i.lineStart() : i.lineEnd()), p && i.point(+e(f, u, c), +t(f, u, c));
    if (m) return i = null, m + "" || null;
  }
  return l.x = function(c) {
    return arguments.length ? (e = typeof c == "function" ? c : it(+c), l) : e;
  }, l.y = function(c) {
    return arguments.length ? (t = typeof c == "function" ? c : it(+c), l) : t;
  }, l.defined = function(c) {
    return arguments.length ? (r = typeof c == "function" ? c : it(!!c), l) : r;
  }, l.curve = function(c) {
    return arguments.length ? (a = c, n != null && (i = a(n)), l) : a;
  }, l.context = function(c) {
    return arguments.length ? (c == null ? n = i = null : i = a(n = c), l) : n;
  }, l;
}
function Yo(e, t, r) {
  var n = null, a = it(!0), i = null, o = Pl, l = null, c = lp(u);
  e = typeof e == "function" ? e : e === void 0 ? Oj : it(+e), t = typeof t == "function" ? t : it(t === void 0 ? 0 : +t), r = typeof r == "function" ? r : r === void 0 ? Nj : it(+r);
  function u(f) {
    var p, m, y, h = (f = cp(f)).length, g, v = !1, b, w = new Array(h), j = new Array(h);
    for (i == null && (l = o(b = c())), p = 0; p <= h; ++p) {
      if (!(p < h && a(g = f[p], p, f)) === v)
        if (v = !v)
          m = p, l.areaStart(), l.lineStart();
        else {
          for (l.lineEnd(), l.lineStart(), y = p - 1; y >= m; --y)
            l.point(w[y], j[y]);
          l.lineEnd(), l.areaEnd();
        }
      v && (w[p] = +e(g, p, f), j[p] = +t(g, p, f), l.point(n ? +n(g, p, f) : w[p], r ? +r(g, p, f) : j[p]));
    }
    if (b) return l = null, b + "" || null;
  }
  function d() {
    return Mj().defined(a).curve(o).context(i);
  }
  return u.x = function(f) {
    return arguments.length ? (e = typeof f == "function" ? f : it(+f), n = null, u) : e;
  }, u.x0 = function(f) {
    return arguments.length ? (e = typeof f == "function" ? f : it(+f), u) : e;
  }, u.x1 = function(f) {
    return arguments.length ? (n = f == null ? null : typeof f == "function" ? f : it(+f), u) : n;
  }, u.y = function(f) {
    return arguments.length ? (t = typeof f == "function" ? f : it(+f), r = null, u) : t;
  }, u.y0 = function(f) {
    return arguments.length ? (t = typeof f == "function" ? f : it(+f), u) : t;
  }, u.y1 = function(f) {
    return arguments.length ? (r = f == null ? null : typeof f == "function" ? f : it(+f), u) : r;
  }, u.lineX0 = u.lineY0 = function() {
    return d().x(e).y(t);
  }, u.lineY1 = function() {
    return d().x(e).y(r);
  }, u.lineX1 = function() {
    return d().x(n).y(t);
  }, u.defined = function(f) {
    return arguments.length ? (a = typeof f == "function" ? f : it(!!f), u) : a;
  }, u.curve = function(f) {
    return arguments.length ? (o = f, i != null && (l = o(i)), u) : o;
  }, u.context = function(f) {
    return arguments.length ? (f == null ? i = l = null : l = o(i = f), u) : i;
  }, u;
}
class Rj {
  constructor(t, r) {
    this._context = t, this._x = r;
  }
  areaStart() {
    this._line = 0;
  }
  areaEnd() {
    this._line = NaN;
  }
  lineStart() {
    this._point = 0;
  }
  lineEnd() {
    (this._line || this._line !== 0 && this._point === 1) && this._context.closePath(), this._line = 1 - this._line;
  }
  point(t, r) {
    switch (t = +t, r = +r, this._point) {
      case 0: {
        this._point = 1, this._line ? this._context.lineTo(t, r) : this._context.moveTo(t, r);
        break;
      }
      case 1:
        this._point = 2;
      // falls through
      default: {
        this._x ? this._context.bezierCurveTo(this._x0 = (this._x0 + t) / 2, this._y0, this._x0, r, t, r) : this._context.bezierCurveTo(this._x0, this._y0 = (this._y0 + r) / 2, t, this._y0, t, r);
        break;
      }
    }
    this._x0 = t, this._y0 = r;
  }
}
function TT(e) {
  return new Rj(e, !0);
}
function PT(e) {
  return new Rj(e, !1);
}
const up = {
  draw(e, t) {
    const r = Pr(t / vs);
    e.moveTo(r, 0), e.arc(0, 0, r, 0, Tl);
  }
}, OT = {
  draw(e, t) {
    const r = Pr(t / 5) / 2;
    e.moveTo(-3 * r, -r), e.lineTo(-r, -r), e.lineTo(-r, -3 * r), e.lineTo(r, -3 * r), e.lineTo(r, -r), e.lineTo(3 * r, -r), e.lineTo(3 * r, r), e.lineTo(r, r), e.lineTo(r, 3 * r), e.lineTo(-r, 3 * r), e.lineTo(-r, r), e.lineTo(-3 * r, r), e.closePath();
  }
}, Ij = Pr(1 / 3), NT = Ij * 2, MT = {
  draw(e, t) {
    const r = Pr(t / NT), n = r * Ij;
    e.moveTo(0, -r), e.lineTo(n, 0), e.lineTo(0, r), e.lineTo(-n, 0), e.closePath();
  }
}, RT = {
  draw(e, t) {
    const r = Pr(t), n = -r / 2;
    e.rect(n, n, r, r);
  }
}, IT = 0.8908130915292852, Dj = ys(vs / 10) / ys(7 * vs / 10), DT = ys(Tl / 10) * Dj, $T = -Cj(Tl / 10) * Dj, LT = {
  draw(e, t) {
    const r = Pr(t * IT), n = DT * r, a = $T * r;
    e.moveTo(0, -r), e.lineTo(n, a);
    for (let i = 1; i < 5; ++i) {
      const o = Tl * i / 5, l = Cj(o), c = ys(o);
      e.lineTo(c * r, -l * r), e.lineTo(l * n - c * a, c * n + l * a);
    }
    e.closePath();
  }
}, ku = Pr(3), FT = {
  draw(e, t) {
    const r = -Pr(t / (ku * 3));
    e.moveTo(0, r * 2), e.lineTo(-ku * r, -r), e.lineTo(ku * r, -r), e.closePath();
  }
}, cr = -0.5, ur = Pr(3) / 2, Rm = 1 / Pr(12), qT = (Rm / 2 + 1) * 3, UT = {
  draw(e, t) {
    const r = Pr(t / qT), n = r / 2, a = r * Rm, i = n, o = r * Rm + r, l = -i, c = o;
    e.moveTo(n, a), e.lineTo(i, o), e.lineTo(l, c), e.lineTo(cr * n - ur * a, ur * n + cr * a), e.lineTo(cr * i - ur * o, ur * i + cr * o), e.lineTo(cr * l - ur * c, ur * l + cr * c), e.lineTo(cr * n + ur * a, cr * a - ur * n), e.lineTo(cr * i + ur * o, cr * o - ur * i), e.lineTo(cr * l + ur * c, cr * c - ur * l), e.closePath();
  }
};
function zT(e, t) {
  let r = null, n = lp(a);
  e = typeof e == "function" ? e : it(e || up), t = typeof t == "function" ? t : it(t === void 0 ? 64 : +t);
  function a() {
    let i;
    if (r || (r = i = n()), e.apply(this, arguments).draw(r, +t.apply(this, arguments)), i) return r = null, i + "" || null;
  }
  return a.type = function(i) {
    return arguments.length ? (e = typeof i == "function" ? i : it(i), a) : e;
  }, a.size = function(i) {
    return arguments.length ? (t = typeof i == "function" ? i : it(+i), a) : t;
  }, a.context = function(i) {
    return arguments.length ? (r = i ?? null, a) : r;
  }, a;
}
function bs() {
}
function xs(e, t, r) {
  e._context.bezierCurveTo(
    (2 * e._x0 + e._x1) / 3,
    (2 * e._y0 + e._y1) / 3,
    (e._x0 + 2 * e._x1) / 3,
    (e._y0 + 2 * e._y1) / 3,
    (e._x0 + 4 * e._x1 + t) / 6,
    (e._y0 + 4 * e._y1 + r) / 6
  );
}
function $j(e) {
  this._context = e;
}
$j.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = NaN, this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 3:
        xs(this, this._x1, this._y1);
      // falls through
      case 2:
        this._context.lineTo(this._x1, this._y1);
        break;
    }
    (this._line || this._line !== 0 && this._point === 1) && this._context.closePath(), this._line = 1 - this._line;
  },
  point: function(e, t) {
    switch (e = +e, t = +t, this._point) {
      case 0:
        this._point = 1, this._line ? this._context.lineTo(e, t) : this._context.moveTo(e, t);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3, this._context.lineTo((5 * this._x0 + this._x1) / 6, (5 * this._y0 + this._y1) / 6);
      // falls through
      default:
        xs(this, e, t);
        break;
    }
    this._x0 = this._x1, this._x1 = e, this._y0 = this._y1, this._y1 = t;
  }
};
function BT(e) {
  return new $j(e);
}
function Lj(e) {
  this._context = e;
}
Lj.prototype = {
  areaStart: bs,
  areaEnd: bs,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = NaN, this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x2, this._y2), this._context.closePath();
        break;
      }
      case 2: {
        this._context.moveTo((this._x2 + 2 * this._x3) / 3, (this._y2 + 2 * this._y3) / 3), this._context.lineTo((this._x3 + 2 * this._x2) / 3, (this._y3 + 2 * this._y2) / 3), this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x2, this._y2), this.point(this._x3, this._y3), this.point(this._x4, this._y4);
        break;
      }
    }
  },
  point: function(e, t) {
    switch (e = +e, t = +t, this._point) {
      case 0:
        this._point = 1, this._x2 = e, this._y2 = t;
        break;
      case 1:
        this._point = 2, this._x3 = e, this._y3 = t;
        break;
      case 2:
        this._point = 3, this._x4 = e, this._y4 = t, this._context.moveTo((this._x0 + 4 * this._x1 + e) / 6, (this._y0 + 4 * this._y1 + t) / 6);
        break;
      default:
        xs(this, e, t);
        break;
    }
    this._x0 = this._x1, this._x1 = e, this._y0 = this._y1, this._y1 = t;
  }
};
function WT(e) {
  return new Lj(e);
}
function Fj(e) {
  this._context = e;
}
Fj.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = NaN, this._point = 0;
  },
  lineEnd: function() {
    (this._line || this._line !== 0 && this._point === 3) && this._context.closePath(), this._line = 1 - this._line;
  },
  point: function(e, t) {
    switch (e = +e, t = +t, this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        var r = (this._x0 + 4 * this._x1 + e) / 6, n = (this._y0 + 4 * this._y1 + t) / 6;
        this._line ? this._context.lineTo(r, n) : this._context.moveTo(r, n);
        break;
      case 3:
        this._point = 4;
      // falls through
      default:
        xs(this, e, t);
        break;
    }
    this._x0 = this._x1, this._x1 = e, this._y0 = this._y1, this._y1 = t;
  }
};
function VT(e) {
  return new Fj(e);
}
function qj(e) {
  this._context = e;
}
qj.prototype = {
  areaStart: bs,
  areaEnd: bs,
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    this._point && this._context.closePath();
  },
  point: function(e, t) {
    e = +e, t = +t, this._point ? this._context.lineTo(e, t) : (this._point = 1, this._context.moveTo(e, t));
  }
};
function HT(e) {
  return new qj(e);
}
function tv(e) {
  return e < 0 ? -1 : 1;
}
function rv(e, t, r) {
  var n = e._x1 - e._x0, a = t - e._x1, i = (e._y1 - e._y0) / (n || a < 0 && -0), o = (r - e._y1) / (a || n < 0 && -0), l = (i * a + o * n) / (n + a);
  return (tv(i) + tv(o)) * Math.min(Math.abs(i), Math.abs(o), 0.5 * Math.abs(l)) || 0;
}
function nv(e, t) {
  var r = e._x1 - e._x0;
  return r ? (3 * (e._y1 - e._y0) / r - t) / 2 : t;
}
function Eu(e, t, r) {
  var n = e._x0, a = e._y0, i = e._x1, o = e._y1, l = (i - n) / 3;
  e._context.bezierCurveTo(n + l, a + l * t, i - l, o - l * r, i, o);
}
function ws(e) {
  this._context = e;
}
ws.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = this._t0 = NaN, this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x1, this._y1);
        break;
      case 3:
        Eu(this, this._t0, nv(this, this._t0));
        break;
    }
    (this._line || this._line !== 0 && this._point === 1) && this._context.closePath(), this._line = 1 - this._line;
  },
  point: function(e, t) {
    var r = NaN;
    if (e = +e, t = +t, !(e === this._x1 && t === this._y1)) {
      switch (this._point) {
        case 0:
          this._point = 1, this._line ? this._context.lineTo(e, t) : this._context.moveTo(e, t);
          break;
        case 1:
          this._point = 2;
          break;
        case 2:
          this._point = 3, Eu(this, nv(this, r = rv(this, e, t)), r);
          break;
        default:
          Eu(this, this._t0, r = rv(this, e, t));
          break;
      }
      this._x0 = this._x1, this._x1 = e, this._y0 = this._y1, this._y1 = t, this._t0 = r;
    }
  }
};
function Uj(e) {
  this._context = new zj(e);
}
(Uj.prototype = Object.create(ws.prototype)).point = function(e, t) {
  ws.prototype.point.call(this, t, e);
};
function zj(e) {
  this._context = e;
}
zj.prototype = {
  moveTo: function(e, t) {
    this._context.moveTo(t, e);
  },
  closePath: function() {
    this._context.closePath();
  },
  lineTo: function(e, t) {
    this._context.lineTo(t, e);
  },
  bezierCurveTo: function(e, t, r, n, a, i) {
    this._context.bezierCurveTo(t, e, n, r, i, a);
  }
};
function GT(e) {
  return new ws(e);
}
function KT(e) {
  return new Uj(e);
}
function Bj(e) {
  this._context = e;
}
Bj.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = [], this._y = [];
  },
  lineEnd: function() {
    var e = this._x, t = this._y, r = e.length;
    if (r)
      if (this._line ? this._context.lineTo(e[0], t[0]) : this._context.moveTo(e[0], t[0]), r === 2)
        this._context.lineTo(e[1], t[1]);
      else
        for (var n = av(e), a = av(t), i = 0, o = 1; o < r; ++i, ++o)
          this._context.bezierCurveTo(n[0][i], a[0][i], n[1][i], a[1][i], e[o], t[o]);
    (this._line || this._line !== 0 && r === 1) && this._context.closePath(), this._line = 1 - this._line, this._x = this._y = null;
  },
  point: function(e, t) {
    this._x.push(+e), this._y.push(+t);
  }
};
function av(e) {
  var t, r = e.length - 1, n, a = new Array(r), i = new Array(r), o = new Array(r);
  for (a[0] = 0, i[0] = 2, o[0] = e[0] + 2 * e[1], t = 1; t < r - 1; ++t) a[t] = 1, i[t] = 4, o[t] = 4 * e[t] + 2 * e[t + 1];
  for (a[r - 1] = 2, i[r - 1] = 7, o[r - 1] = 8 * e[r - 1] + e[r], t = 1; t < r; ++t) n = a[t] / i[t - 1], i[t] -= n, o[t] -= n * o[t - 1];
  for (a[r - 1] = o[r - 1] / i[r - 1], t = r - 2; t >= 0; --t) a[t] = (o[t] - a[t + 1]) / i[t];
  for (i[r - 1] = (e[r] + a[r - 1]) / 2, t = 0; t < r - 1; ++t) i[t] = 2 * e[t + 1] - a[t + 1];
  return [a, i];
}
function YT(e) {
  return new Bj(e);
}
function Ol(e, t) {
  this._context = e, this._t = t;
}
Ol.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = this._y = NaN, this._point = 0;
  },
  lineEnd: function() {
    0 < this._t && this._t < 1 && this._point === 2 && this._context.lineTo(this._x, this._y), (this._line || this._line !== 0 && this._point === 1) && this._context.closePath(), this._line >= 0 && (this._t = 1 - this._t, this._line = 1 - this._line);
  },
  point: function(e, t) {
    switch (e = +e, t = +t, this._point) {
      case 0:
        this._point = 1, this._line ? this._context.lineTo(e, t) : this._context.moveTo(e, t);
        break;
      case 1:
        this._point = 2;
      // falls through
      default: {
        if (this._t <= 0)
          this._context.lineTo(this._x, t), this._context.lineTo(e, t);
        else {
          var r = this._x * (1 - this._t) + e * this._t;
          this._context.lineTo(r, this._y), this._context.lineTo(r, t);
        }
        break;
      }
    }
    this._x = e, this._y = t;
  }
};
function XT(e) {
  return new Ol(e, 0.5);
}
function QT(e) {
  return new Ol(e, 0);
}
function JT(e) {
  return new Ol(e, 1);
}
function Ea(e, t) {
  if ((o = e.length) > 1)
    for (var r = 1, n, a, i = e[t[0]], o, l = i.length; r < o; ++r)
      for (a = i, i = e[t[r]], n = 0; n < l; ++n)
        i[n][1] += i[n][0] = isNaN(a[n][1]) ? a[n][0] : a[n][1];
}
function Im(e) {
  for (var t = e.length, r = new Array(t); --t >= 0; ) r[t] = t;
  return r;
}
function ZT(e, t) {
  return e[t];
}
function eP(e) {
  const t = [];
  return t.key = e, t;
}
function tP() {
  var e = it([]), t = Im, r = Ea, n = ZT;
  function a(i) {
    var o = Array.from(e.apply(this, arguments), eP), l, c = o.length, u = -1, d;
    for (const f of i)
      for (l = 0, ++u; l < c; ++l)
        (o[l][u] = [0, +n(f, o[l].key, u, i)]).data = f;
    for (l = 0, d = cp(t(o)); l < c; ++l)
      o[d[l]].index = l;
    return r(o, d), o;
  }
  return a.keys = function(i) {
    return arguments.length ? (e = typeof i == "function" ? i : it(Array.from(i)), a) : e;
  }, a.value = function(i) {
    return arguments.length ? (n = typeof i == "function" ? i : it(+i), a) : n;
  }, a.order = function(i) {
    return arguments.length ? (t = i == null ? Im : typeof i == "function" ? i : it(Array.from(i)), a) : t;
  }, a.offset = function(i) {
    return arguments.length ? (r = i ?? Ea, a) : r;
  }, a;
}
function rP(e, t) {
  if ((n = e.length) > 0) {
    for (var r, n, a = 0, i = e[0].length, o; a < i; ++a) {
      for (o = r = 0; r < n; ++r) o += e[r][a][1] || 0;
      if (o) for (r = 0; r < n; ++r) e[r][a][1] /= o;
    }
    Ea(e, t);
  }
}
function nP(e, t) {
  if ((a = e.length) > 0) {
    for (var r = 0, n = e[t[0]], a, i = n.length; r < i; ++r) {
      for (var o = 0, l = 0; o < a; ++o) l += e[o][r][1] || 0;
      n[r][1] += n[r][0] = -l / 2;
    }
    Ea(e, t);
  }
}
function aP(e, t) {
  if (!(!((o = e.length) > 0) || !((i = (a = e[t[0]]).length) > 0))) {
    for (var r = 0, n = 1, a, i, o; n < i; ++n) {
      for (var l = 0, c = 0, u = 0; l < o; ++l) {
        for (var d = e[t[l]], f = d[n][1] || 0, p = d[n - 1][1] || 0, m = (f - p) / 2, y = 0; y < l; ++y) {
          var h = e[t[y]], g = h[n][1] || 0, v = h[n - 1][1] || 0;
          m += g - v;
        }
        c += f, u += m * f;
      }
      a[n - 1][1] += a[n - 1][0] = r, c && (r -= u / c);
    }
    a[n - 1][1] += a[n - 1][0] = r, Ea(e, t);
  }
}
function Ui(e) {
  "@babel/helpers - typeof";
  return Ui = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Ui(e);
}
var iP = ["type", "size", "sizeType"];
function Dm() {
  return Dm = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Dm.apply(this, arguments);
}
function iv(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function ov(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? iv(Object(r), !0).forEach(function(n) {
      oP(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : iv(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function oP(e, t, r) {
  return t = sP(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function sP(e) {
  var t = lP(e, "string");
  return Ui(t) == "symbol" ? t : t + "";
}
function lP(e, t) {
  if (Ui(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Ui(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function cP(e, t) {
  if (e == null) return {};
  var r = uP(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function uP(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
var Wj = {
  symbolCircle: up,
  symbolCross: OT,
  symbolDiamond: MT,
  symbolSquare: RT,
  symbolStar: LT,
  symbolTriangle: FT,
  symbolWye: UT
}, dP = Math.PI / 180, fP = function(t) {
  var r = "symbol".concat(Cl(t));
  return Wj[r] || up;
}, mP = function(t, r, n) {
  if (r === "area")
    return t;
  switch (n) {
    case "cross":
      return 5 * t * t / 9;
    case "diamond":
      return 0.5 * t * t / Math.sqrt(3);
    case "square":
      return t * t;
    case "star": {
      var a = 18 * dP;
      return 1.25 * t * t * (Math.tan(a) - Math.tan(a * 2) * Math.pow(Math.tan(a), 2));
    }
    case "triangle":
      return Math.sqrt(3) * t * t / 4;
    case "wye":
      return (21 - 10 * Math.sqrt(3)) * t * t / 8;
    default:
      return Math.PI * t * t / 4;
  }
}, hP = function(t, r) {
  Wj["symbol".concat(Cl(t))] = r;
}, dp = function(t) {
  var r = t.type, n = r === void 0 ? "circle" : r, a = t.size, i = a === void 0 ? 64 : a, o = t.sizeType, l = o === void 0 ? "area" : o, c = cP(t, iP), u = ov(ov({}, c), {}, {
    type: n,
    size: i,
    sizeType: l
  }), d = function() {
    var g = fP(n), v = zT().type(g).size(mP(i, l, n));
    return v();
  }, f = u.className, p = u.cx, m = u.cy, y = ze(u, !0);
  return p === +p && m === +m && i === +i ? /* @__PURE__ */ I.createElement("path", Dm({}, y, {
    className: Ve("recharts-symbols", f),
    transform: "translate(".concat(p, ", ").concat(m, ")"),
    d: d()
  })) : null;
};
dp.registerSymbol = hP;
function Aa(e) {
  "@babel/helpers - typeof";
  return Aa = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Aa(e);
}
function $m() {
  return $m = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, $m.apply(this, arguments);
}
function sv(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function pP(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? sv(Object(r), !0).forEach(function(n) {
      zi(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : sv(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function gP(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function yP(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, Hj(n.key), n);
  }
}
function vP(e, t, r) {
  return t && yP(e.prototype, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function bP(e, t, r) {
  return t = _s(t), xP(e, Vj() ? Reflect.construct(t, r || [], _s(e).constructor) : t.apply(e, r));
}
function xP(e, t) {
  if (t && (Aa(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return wP(e);
}
function wP(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function Vj() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (Vj = function() {
    return !!e;
  })();
}
function _s(e) {
  return _s = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, _s(e);
}
function _P(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && Lm(e, t);
}
function Lm(e, t) {
  return Lm = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, Lm(e, t);
}
function zi(e, t, r) {
  return t = Hj(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function Hj(e) {
  var t = jP(e, "string");
  return Aa(t) == "symbol" ? t : t + "";
}
function jP(e, t) {
  if (Aa(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Aa(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
var dr = 32, fp = /* @__PURE__ */ (function(e) {
  function t() {
    return gP(this, t), bP(this, t, arguments);
  }
  return _P(t, e), vP(t, [{
    key: "renderIcon",
    value: (
      /**
       * Render the path of icon
       * @param {Object} data Data of each legend item
       * @return {String} Path element
       */
      function(n) {
        var a = this.props.inactiveColor, i = dr / 2, o = dr / 6, l = dr / 3, c = n.inactive ? a : n.color;
        if (n.type === "plainline")
          return /* @__PURE__ */ I.createElement("line", {
            strokeWidth: 4,
            fill: "none",
            stroke: c,
            strokeDasharray: n.payload.strokeDasharray,
            x1: 0,
            y1: i,
            x2: dr,
            y2: i,
            className: "recharts-legend-icon"
          });
        if (n.type === "line")
          return /* @__PURE__ */ I.createElement("path", {
            strokeWidth: 4,
            fill: "none",
            stroke: c,
            d: "M0,".concat(i, "h").concat(l, `
            A`).concat(o, ",").concat(o, ",0,1,1,").concat(2 * l, ",").concat(i, `
            H`).concat(dr, "M").concat(2 * l, ",").concat(i, `
            A`).concat(o, ",").concat(o, ",0,1,1,").concat(l, ",").concat(i),
            className: "recharts-legend-icon"
          });
        if (n.type === "rect")
          return /* @__PURE__ */ I.createElement("path", {
            stroke: "none",
            fill: c,
            d: "M0,".concat(dr / 8, "h").concat(dr, "v").concat(dr * 3 / 4, "h").concat(-dr, "z"),
            className: "recharts-legend-icon"
          });
        if (/* @__PURE__ */ I.isValidElement(n.legendIcon)) {
          var u = pP({}, n);
          return delete u.legendIcon, /* @__PURE__ */ I.cloneElement(n.legendIcon, u);
        }
        return /* @__PURE__ */ I.createElement(dp, {
          fill: c,
          cx: i,
          cy: i,
          size: dr,
          sizeType: "diameter",
          type: n.type
        });
      }
    )
    /**
     * Draw items of legend
     * @return {ReactElement} Items
     */
  }, {
    key: "renderItems",
    value: function() {
      var n = this, a = this.props, i = a.payload, o = a.iconSize, l = a.layout, c = a.formatter, u = a.inactiveColor, d = {
        x: 0,
        y: 0,
        width: dr,
        height: dr
      }, f = {
        display: l === "horizontal" ? "inline-block" : "block",
        marginRight: 10
      }, p = {
        display: "inline-block",
        verticalAlign: "middle",
        marginRight: 4
      };
      return i.map(function(m, y) {
        var h = m.formatter || c, g = Ve(zi(zi({
          "recharts-legend-item": !0
        }, "legend-item-".concat(y), !0), "inactive", m.inactive));
        if (m.type === "none")
          return null;
        var v = $e(m.value) ? null : m.value;
        Xr(
          !$e(m.value),
          `The name property is also required when using a function for the dataKey of a chart's cartesian components. Ex: <Bar name="Name of my Data"/>`
          // eslint-disable-line max-len
        );
        var b = m.inactive ? u : m.color;
        return /* @__PURE__ */ I.createElement("li", $m({
          className: g,
          style: f,
          key: "legend-item-".concat(y)
        }, gs(n.props, m, y)), /* @__PURE__ */ I.createElement(Pm, {
          width: o,
          height: o,
          viewBox: d,
          style: p
        }, n.renderIcon(m)), /* @__PURE__ */ I.createElement("span", {
          className: "recharts-legend-item-text",
          style: {
            color: b
          }
        }, h ? h(v, m, y) : v));
      });
    }
  }, {
    key: "render",
    value: function() {
      var n = this.props, a = n.payload, i = n.layout, o = n.align;
      if (!a || !a.length)
        return null;
      var l = {
        padding: 0,
        margin: 0,
        textAlign: i === "horizontal" ? o : "left"
      };
      return /* @__PURE__ */ I.createElement("ul", {
        className: "recharts-default-legend",
        style: l
      }, this.renderItems());
    }
  }]);
})(wn);
zi(fp, "displayName", "Legend");
zi(fp, "defaultProps", {
  iconSize: 14,
  layout: "horizontal",
  align: "center",
  verticalAlign: "middle",
  inactiveColor: "#ccc"
});
var Au, lv;
function SP() {
  if (lv) return Au;
  lv = 1;
  var e = kl();
  function t() {
    this.__data__ = new e(), this.size = 0;
  }
  return Au = t, Au;
}
var Cu, cv;
function kP() {
  if (cv) return Cu;
  cv = 1;
  function e(t) {
    var r = this.__data__, n = r.delete(t);
    return this.size = r.size, n;
  }
  return Cu = e, Cu;
}
var Tu, uv;
function EP() {
  if (uv) return Tu;
  uv = 1;
  function e(t) {
    return this.__data__.get(t);
  }
  return Tu = e, Tu;
}
var Pu, dv;
function AP() {
  if (dv) return Pu;
  dv = 1;
  function e(t) {
    return this.__data__.has(t);
  }
  return Pu = e, Pu;
}
var Ou, fv;
function CP() {
  if (fv) return Ou;
  fv = 1;
  var e = kl(), t = rp(), r = np(), n = 200;
  function a(i, o) {
    var l = this.__data__;
    if (l instanceof e) {
      var c = l.__data__;
      if (!t || c.length < n - 1)
        return c.push([i, o]), this.size = ++l.size, this;
      l = this.__data__ = new r(c);
    }
    return l.set(i, o), this.size = l.size, this;
  }
  return Ou = a, Ou;
}
var Nu, mv;
function Gj() {
  if (mv) return Nu;
  mv = 1;
  var e = kl(), t = SP(), r = kP(), n = EP(), a = AP(), i = CP();
  function o(l) {
    var c = this.__data__ = new e(l);
    this.size = c.size;
  }
  return o.prototype.clear = t, o.prototype.delete = r, o.prototype.get = n, o.prototype.has = a, o.prototype.set = i, Nu = o, Nu;
}
var Mu, hv;
function TP() {
  if (hv) return Mu;
  hv = 1;
  var e = "__lodash_hash_undefined__";
  function t(r) {
    return this.__data__.set(r, e), this;
  }
  return Mu = t, Mu;
}
var Ru, pv;
function PP() {
  if (pv) return Ru;
  pv = 1;
  function e(t) {
    return this.__data__.has(t);
  }
  return Ru = e, Ru;
}
var Iu, gv;
function Kj() {
  if (gv) return Iu;
  gv = 1;
  var e = np(), t = TP(), r = PP();
  function n(a) {
    var i = -1, o = a == null ? 0 : a.length;
    for (this.__data__ = new e(); ++i < o; )
      this.add(a[i]);
  }
  return n.prototype.add = n.prototype.push = t, n.prototype.has = r, Iu = n, Iu;
}
var Du, yv;
function Yj() {
  if (yv) return Du;
  yv = 1;
  function e(t, r) {
    for (var n = -1, a = t == null ? 0 : t.length; ++n < a; )
      if (r(t[n], n, t))
        return !0;
    return !1;
  }
  return Du = e, Du;
}
var $u, vv;
function Xj() {
  if (vv) return $u;
  vv = 1;
  function e(t, r) {
    return t.has(r);
  }
  return $u = e, $u;
}
var Lu, bv;
function Qj() {
  if (bv) return Lu;
  bv = 1;
  var e = Kj(), t = Yj(), r = Xj(), n = 1, a = 2;
  function i(o, l, c, u, d, f) {
    var p = c & n, m = o.length, y = l.length;
    if (m != y && !(p && y > m))
      return !1;
    var h = f.get(o), g = f.get(l);
    if (h && g)
      return h == l && g == o;
    var v = -1, b = !0, w = c & a ? new e() : void 0;
    for (f.set(o, l), f.set(l, o); ++v < m; ) {
      var j = o[v], x = l[v];
      if (u)
        var _ = p ? u(x, j, v, l, o, f) : u(j, x, v, o, l, f);
      if (_ !== void 0) {
        if (_)
          continue;
        b = !1;
        break;
      }
      if (w) {
        if (!t(l, function(S, k) {
          if (!r(w, k) && (j === S || d(j, S, c, u, f)))
            return w.push(k);
        })) {
          b = !1;
          break;
        }
      } else if (!(j === x || d(j, x, c, u, f))) {
        b = !1;
        break;
      }
    }
    return f.delete(o), f.delete(l), b;
  }
  return Lu = i, Lu;
}
var Fu, xv;
function OP() {
  if (xv) return Fu;
  xv = 1;
  var e = Lr(), t = e.Uint8Array;
  return Fu = t, Fu;
}
var qu, wv;
function NP() {
  if (wv) return qu;
  wv = 1;
  function e(t) {
    var r = -1, n = Array(t.size);
    return t.forEach(function(a, i) {
      n[++r] = [i, a];
    }), n;
  }
  return qu = e, qu;
}
var Uu, _v;
function mp() {
  if (_v) return Uu;
  _v = 1;
  function e(t) {
    var r = -1, n = Array(t.size);
    return t.forEach(function(a) {
      n[++r] = a;
    }), n;
  }
  return Uu = e, Uu;
}
var zu, jv;
function MP() {
  if (jv) return zu;
  jv = 1;
  var e = ko(), t = OP(), r = tp(), n = Qj(), a = NP(), i = mp(), o = 1, l = 2, c = "[object Boolean]", u = "[object Date]", d = "[object Error]", f = "[object Map]", p = "[object Number]", m = "[object RegExp]", y = "[object Set]", h = "[object String]", g = "[object Symbol]", v = "[object ArrayBuffer]", b = "[object DataView]", w = e ? e.prototype : void 0, j = w ? w.valueOf : void 0;
  function x(_, S, k, E, L, P, A) {
    switch (k) {
      case b:
        if (_.byteLength != S.byteLength || _.byteOffset != S.byteOffset)
          return !1;
        _ = _.buffer, S = S.buffer;
      case v:
        return !(_.byteLength != S.byteLength || !P(new t(_), new t(S)));
      case c:
      case u:
      case p:
        return r(+_, +S);
      case d:
        return _.name == S.name && _.message == S.message;
      case m:
      case h:
        return _ == S + "";
      case f:
        var T = a;
      case y:
        var M = E & o;
        if (T || (T = i), _.size != S.size && !M)
          return !1;
        var N = A.get(_);
        if (N)
          return N == S;
        E |= l, A.set(_, S);
        var q = n(T(_), T(S), E, L, P, A);
        return A.delete(_), q;
      case g:
        if (j)
          return j.call(_) == j.call(S);
    }
    return !1;
  }
  return zu = x, zu;
}
var Bu, Sv;
function Jj() {
  if (Sv) return Bu;
  Sv = 1;
  function e(t, r) {
    for (var n = -1, a = r.length, i = t.length; ++n < a; )
      t[i + n] = r[n];
    return t;
  }
  return Bu = e, Bu;
}
var Wu, kv;
function RP() {
  if (kv) return Wu;
  kv = 1;
  var e = Jj(), t = Xt();
  function r(n, a, i) {
    var o = a(n);
    return t(n) ? o : e(o, i(n));
  }
  return Wu = r, Wu;
}
var Vu, Ev;
function IP() {
  if (Ev) return Vu;
  Ev = 1;
  function e(t, r) {
    for (var n = -1, a = t == null ? 0 : t.length, i = 0, o = []; ++n < a; ) {
      var l = t[n];
      r(l, n, t) && (o[i++] = l);
    }
    return o;
  }
  return Vu = e, Vu;
}
var Hu, Av;
function DP() {
  if (Av) return Hu;
  Av = 1;
  function e() {
    return [];
  }
  return Hu = e, Hu;
}
var Gu, Cv;
function $P() {
  if (Cv) return Gu;
  Cv = 1;
  var e = IP(), t = DP(), r = Object.prototype, n = r.propertyIsEnumerable, a = Object.getOwnPropertySymbols, i = a ? function(o) {
    return o == null ? [] : (o = Object(o), e(a(o), function(l) {
      return n.call(o, l);
    }));
  } : t;
  return Gu = i, Gu;
}
var Ku, Tv;
function LP() {
  if (Tv) return Ku;
  Tv = 1;
  function e(t, r) {
    for (var n = -1, a = Array(t); ++n < t; )
      a[n] = r(n);
    return a;
  }
  return Ku = e, Ku;
}
var Yu, Pv;
function FP() {
  if (Pv) return Yu;
  Pv = 1;
  var e = nn(), t = an(), r = "[object Arguments]";
  function n(a) {
    return t(a) && e(a) == r;
  }
  return Yu = n, Yu;
}
var Xu, Ov;
function hp() {
  if (Ov) return Xu;
  Ov = 1;
  var e = FP(), t = an(), r = Object.prototype, n = r.hasOwnProperty, a = r.propertyIsEnumerable, i = e(/* @__PURE__ */ (function() {
    return arguments;
  })()) ? e : function(o) {
    return t(o) && n.call(o, "callee") && !a.call(o, "callee");
  };
  return Xu = i, Xu;
}
var Ti = { exports: {} }, Qu, Nv;
function qP() {
  if (Nv) return Qu;
  Nv = 1;
  function e() {
    return !1;
  }
  return Qu = e, Qu;
}
Ti.exports;
var Mv;
function Zj() {
  return Mv || (Mv = 1, (function(e, t) {
    var r = Lr(), n = qP(), a = t && !t.nodeType && t, i = a && !0 && e && !e.nodeType && e, o = i && i.exports === a, l = o ? r.Buffer : void 0, c = l ? l.isBuffer : void 0, u = c || n;
    e.exports = u;
  })(Ti, Ti.exports)), Ti.exports;
}
var Ju, Rv;
function pp() {
  if (Rv) return Ju;
  Rv = 1;
  var e = 9007199254740991, t = /^(?:0|[1-9]\d*)$/;
  function r(n, a) {
    var i = typeof n;
    return a = a ?? e, !!a && (i == "number" || i != "symbol" && t.test(n)) && n > -1 && n % 1 == 0 && n < a;
  }
  return Ju = r, Ju;
}
var Zu, Iv;
function gp() {
  if (Iv) return Zu;
  Iv = 1;
  var e = 9007199254740991;
  function t(r) {
    return typeof r == "number" && r > -1 && r % 1 == 0 && r <= e;
  }
  return Zu = t, Zu;
}
var ed, Dv;
function UP() {
  if (Dv) return ed;
  Dv = 1;
  var e = nn(), t = gp(), r = an(), n = "[object Arguments]", a = "[object Array]", i = "[object Boolean]", o = "[object Date]", l = "[object Error]", c = "[object Function]", u = "[object Map]", d = "[object Number]", f = "[object Object]", p = "[object RegExp]", m = "[object Set]", y = "[object String]", h = "[object WeakMap]", g = "[object ArrayBuffer]", v = "[object DataView]", b = "[object Float32Array]", w = "[object Float64Array]", j = "[object Int8Array]", x = "[object Int16Array]", _ = "[object Int32Array]", S = "[object Uint8Array]", k = "[object Uint8ClampedArray]", E = "[object Uint16Array]", L = "[object Uint32Array]", P = {};
  P[b] = P[w] = P[j] = P[x] = P[_] = P[S] = P[k] = P[E] = P[L] = !0, P[n] = P[a] = P[g] = P[i] = P[v] = P[o] = P[l] = P[c] = P[u] = P[d] = P[f] = P[p] = P[m] = P[y] = P[h] = !1;
  function A(T) {
    return r(T) && t(T.length) && !!P[e(T)];
  }
  return ed = A, ed;
}
var td, $v;
function eS() {
  if ($v) return td;
  $v = 1;
  function e(t) {
    return function(r) {
      return t(r);
    };
  }
  return td = e, td;
}
var Pi = { exports: {} };
Pi.exports;
var Lv;
function zP() {
  return Lv || (Lv = 1, (function(e, t) {
    var r = xj(), n = t && !t.nodeType && t, a = n && !0 && e && !e.nodeType && e, i = a && a.exports === n, o = i && r.process, l = (function() {
      try {
        var c = a && a.require && a.require("util").types;
        return c || o && o.binding && o.binding("util");
      } catch {
      }
    })();
    e.exports = l;
  })(Pi, Pi.exports)), Pi.exports;
}
var rd, Fv;
function tS() {
  if (Fv) return rd;
  Fv = 1;
  var e = UP(), t = eS(), r = zP(), n = r && r.isTypedArray, a = n ? t(n) : e;
  return rd = a, rd;
}
var nd, qv;
function BP() {
  if (qv) return nd;
  qv = 1;
  var e = LP(), t = hp(), r = Xt(), n = Zj(), a = pp(), i = tS(), o = Object.prototype, l = o.hasOwnProperty;
  function c(u, d) {
    var f = r(u), p = !f && t(u), m = !f && !p && n(u), y = !f && !p && !m && i(u), h = f || p || m || y, g = h ? e(u.length, String) : [], v = g.length;
    for (var b in u)
      (d || l.call(u, b)) && !(h && // Safari 9 has enumerable `arguments.length` in strict mode.
      (b == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
      m && (b == "offset" || b == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
      y && (b == "buffer" || b == "byteLength" || b == "byteOffset") || // Skip index properties.
      a(b, v))) && g.push(b);
    return g;
  }
  return nd = c, nd;
}
var ad, Uv;
function WP() {
  if (Uv) return ad;
  Uv = 1;
  var e = Object.prototype;
  function t(r) {
    var n = r && r.constructor, a = typeof n == "function" && n.prototype || e;
    return r === a;
  }
  return ad = t, ad;
}
var id, zv;
function rS() {
  if (zv) return id;
  zv = 1;
  function e(t, r) {
    return function(n) {
      return t(r(n));
    };
  }
  return id = e, id;
}
var od, Bv;
function VP() {
  if (Bv) return od;
  Bv = 1;
  var e = rS(), t = e(Object.keys, Object);
  return od = t, od;
}
var sd, Wv;
function HP() {
  if (Wv) return sd;
  Wv = 1;
  var e = WP(), t = VP(), r = Object.prototype, n = r.hasOwnProperty;
  function a(i) {
    if (!e(i))
      return t(i);
    var o = [];
    for (var l in Object(i))
      n.call(i, l) && l != "constructor" && o.push(l);
    return o;
  }
  return sd = a, sd;
}
var ld, Vv;
function Co() {
  if (Vv) return ld;
  Vv = 1;
  var e = ep(), t = gp();
  function r(n) {
    return n != null && t(n.length) && !e(n);
  }
  return ld = r, ld;
}
var cd, Hv;
function Nl() {
  if (Hv) return cd;
  Hv = 1;
  var e = BP(), t = HP(), r = Co();
  function n(a) {
    return r(a) ? e(a) : t(a);
  }
  return cd = n, cd;
}
var ud, Gv;
function GP() {
  if (Gv) return ud;
  Gv = 1;
  var e = RP(), t = $P(), r = Nl();
  function n(a) {
    return e(a, r, t);
  }
  return ud = n, ud;
}
var dd, Kv;
function KP() {
  if (Kv) return dd;
  Kv = 1;
  var e = GP(), t = 1, r = Object.prototype, n = r.hasOwnProperty;
  function a(i, o, l, c, u, d) {
    var f = l & t, p = e(i), m = p.length, y = e(o), h = y.length;
    if (m != h && !f)
      return !1;
    for (var g = m; g--; ) {
      var v = p[g];
      if (!(f ? v in o : n.call(o, v)))
        return !1;
    }
    var b = d.get(i), w = d.get(o);
    if (b && w)
      return b == o && w == i;
    var j = !0;
    d.set(i, o), d.set(o, i);
    for (var x = f; ++g < m; ) {
      v = p[g];
      var _ = i[v], S = o[v];
      if (c)
        var k = f ? c(S, _, v, o, i, d) : c(_, S, v, i, o, d);
      if (!(k === void 0 ? _ === S || u(_, S, l, c, d) : k)) {
        j = !1;
        break;
      }
      x || (x = v == "constructor");
    }
    if (j && !x) {
      var E = i.constructor, L = o.constructor;
      E != L && "constructor" in i && "constructor" in o && !(typeof E == "function" && E instanceof E && typeof L == "function" && L instanceof L) && (j = !1);
    }
    return d.delete(i), d.delete(o), j;
  }
  return dd = a, dd;
}
var fd, Yv;
function YP() {
  if (Yv) return fd;
  Yv = 1;
  var e = Yn(), t = Lr(), r = e(t, "DataView");
  return fd = r, fd;
}
var md, Xv;
function XP() {
  if (Xv) return md;
  Xv = 1;
  var e = Yn(), t = Lr(), r = e(t, "Promise");
  return md = r, md;
}
var hd, Qv;
function nS() {
  if (Qv) return hd;
  Qv = 1;
  var e = Yn(), t = Lr(), r = e(t, "Set");
  return hd = r, hd;
}
var pd, Jv;
function QP() {
  if (Jv) return pd;
  Jv = 1;
  var e = Yn(), t = Lr(), r = e(t, "WeakMap");
  return pd = r, pd;
}
var gd, Zv;
function JP() {
  if (Zv) return gd;
  Zv = 1;
  var e = YP(), t = rp(), r = XP(), n = nS(), a = QP(), i = nn(), o = wj(), l = "[object Map]", c = "[object Object]", u = "[object Promise]", d = "[object Set]", f = "[object WeakMap]", p = "[object DataView]", m = o(e), y = o(t), h = o(r), g = o(n), v = o(a), b = i;
  return (e && b(new e(new ArrayBuffer(1))) != p || t && b(new t()) != l || r && b(r.resolve()) != u || n && b(new n()) != d || a && b(new a()) != f) && (b = function(w) {
    var j = i(w), x = j == c ? w.constructor : void 0, _ = x ? o(x) : "";
    if (_)
      switch (_) {
        case m:
          return p;
        case y:
          return l;
        case h:
          return u;
        case g:
          return d;
        case v:
          return f;
      }
    return j;
  }), gd = b, gd;
}
var yd, eb;
function ZP() {
  if (eb) return yd;
  eb = 1;
  var e = Gj(), t = Qj(), r = MP(), n = KP(), a = JP(), i = Xt(), o = Zj(), l = tS(), c = 1, u = "[object Arguments]", d = "[object Array]", f = "[object Object]", p = Object.prototype, m = p.hasOwnProperty;
  function y(h, g, v, b, w, j) {
    var x = i(h), _ = i(g), S = x ? d : a(h), k = _ ? d : a(g);
    S = S == u ? f : S, k = k == u ? f : k;
    var E = S == f, L = k == f, P = S == k;
    if (P && o(h)) {
      if (!o(g))
        return !1;
      x = !0, E = !1;
    }
    if (P && !E)
      return j || (j = new e()), x || l(h) ? t(h, g, v, b, w, j) : r(h, g, S, v, b, w, j);
    if (!(v & c)) {
      var A = E && m.call(h, "__wrapped__"), T = L && m.call(g, "__wrapped__");
      if (A || T) {
        var M = A ? h.value() : h, N = T ? g.value() : g;
        return j || (j = new e()), w(M, N, v, b, j);
      }
    }
    return P ? (j || (j = new e()), n(h, g, v, b, w, j)) : !1;
  }
  return yd = y, yd;
}
var vd, tb;
function yp() {
  if (tb) return vd;
  tb = 1;
  var e = ZP(), t = an();
  function r(n, a, i, o, l) {
    return n === a ? !0 : n == null || a == null || !t(n) && !t(a) ? n !== n && a !== a : e(n, a, i, o, r, l);
  }
  return vd = r, vd;
}
var bd, rb;
function eO() {
  if (rb) return bd;
  rb = 1;
  var e = Gj(), t = yp(), r = 1, n = 2;
  function a(i, o, l, c) {
    var u = l.length, d = u, f = !c;
    if (i == null)
      return !d;
    for (i = Object(i); u--; ) {
      var p = l[u];
      if (f && p[2] ? p[1] !== i[p[0]] : !(p[0] in i))
        return !1;
    }
    for (; ++u < d; ) {
      p = l[u];
      var m = p[0], y = i[m], h = p[1];
      if (f && p[2]) {
        if (y === void 0 && !(m in i))
          return !1;
      } else {
        var g = new e();
        if (c)
          var v = c(y, h, m, i, o, g);
        if (!(v === void 0 ? t(h, y, r | n, c, g) : v))
          return !1;
      }
    }
    return !0;
  }
  return bd = a, bd;
}
var xd, nb;
function aS() {
  if (nb) return xd;
  nb = 1;
  var e = _n();
  function t(r) {
    return r === r && !e(r);
  }
  return xd = t, xd;
}
var wd, ab;
function tO() {
  if (ab) return wd;
  ab = 1;
  var e = aS(), t = Nl();
  function r(n) {
    for (var a = t(n), i = a.length; i--; ) {
      var o = a[i], l = n[o];
      a[i] = [o, l, e(l)];
    }
    return a;
  }
  return wd = r, wd;
}
var _d, ib;
function iS() {
  if (ib) return _d;
  ib = 1;
  function e(t, r) {
    return function(n) {
      return n == null ? !1 : n[t] === r && (r !== void 0 || t in Object(n));
    };
  }
  return _d = e, _d;
}
var jd, ob;
function rO() {
  if (ob) return jd;
  ob = 1;
  var e = eO(), t = tO(), r = iS();
  function n(a) {
    var i = t(a);
    return i.length == 1 && i[0][2] ? r(i[0][0], i[0][1]) : function(o) {
      return o === a || e(o, a, i);
    };
  }
  return jd = n, jd;
}
var Sd, sb;
function nO() {
  if (sb) return Sd;
  sb = 1;
  function e(t, r) {
    return t != null && r in Object(t);
  }
  return Sd = e, Sd;
}
var kd, lb;
function aO() {
  if (lb) return kd;
  lb = 1;
  var e = Sj(), t = hp(), r = Xt(), n = pp(), a = gp(), i = Al();
  function o(l, c, u) {
    c = e(c, l);
    for (var d = -1, f = c.length, p = !1; ++d < f; ) {
      var m = i(c[d]);
      if (!(p = l != null && u(l, m)))
        break;
      l = l[m];
    }
    return p || ++d != f ? p : (f = l == null ? 0 : l.length, !!f && a(f) && n(m, f) && (r(l) || t(l)));
  }
  return kd = o, kd;
}
var Ed, cb;
function iO() {
  if (cb) return Ed;
  cb = 1;
  var e = nO(), t = aO();
  function r(n, a) {
    return n != null && t(n, a, e);
  }
  return Ed = r, Ed;
}
var Ad, ub;
function oO() {
  if (ub) return Ad;
  ub = 1;
  var e = yp(), t = kj(), r = iO(), n = Zh(), a = aS(), i = iS(), o = Al(), l = 1, c = 2;
  function u(d, f) {
    return n(d) && a(f) ? i(o(d), f) : function(p) {
      var m = t(p, d);
      return m === void 0 && m === f ? r(p, d) : e(f, m, l | c);
    };
  }
  return Ad = u, Ad;
}
var Cd, db;
function ri() {
  if (db) return Cd;
  db = 1;
  function e(t) {
    return t;
  }
  return Cd = e, Cd;
}
var Td, fb;
function sO() {
  if (fb) return Td;
  fb = 1;
  function e(t) {
    return function(r) {
      return r == null ? void 0 : r[t];
    };
  }
  return Td = e, Td;
}
var Pd, mb;
function lO() {
  if (mb) return Pd;
  mb = 1;
  var e = ip();
  function t(r) {
    return function(n) {
      return e(n, r);
    };
  }
  return Pd = t, Pd;
}
var Od, hb;
function cO() {
  if (hb) return Od;
  hb = 1;
  var e = sO(), t = lO(), r = Zh(), n = Al();
  function a(i) {
    return r(i) ? e(n(i)) : t(i);
  }
  return Od = a, Od;
}
var Nd, pb;
function jn() {
  if (pb) return Nd;
  pb = 1;
  var e = rO(), t = oO(), r = ri(), n = Xt(), a = cO();
  function i(o) {
    return typeof o == "function" ? o : o == null ? r : typeof o == "object" ? n(o) ? t(o[0], o[1]) : e(o) : a(o);
  }
  return Nd = i, Nd;
}
var Md, gb;
function oS() {
  if (gb) return Md;
  gb = 1;
  function e(t, r, n, a) {
    for (var i = t.length, o = n + (a ? 1 : -1); a ? o-- : ++o < i; )
      if (r(t[o], o, t))
        return o;
    return -1;
  }
  return Md = e, Md;
}
var Rd, yb;
function uO() {
  if (yb) return Rd;
  yb = 1;
  function e(t) {
    return t !== t;
  }
  return Rd = e, Rd;
}
var Id, vb;
function dO() {
  if (vb) return Id;
  vb = 1;
  function e(t, r, n) {
    for (var a = n - 1, i = t.length; ++a < i; )
      if (t[a] === r)
        return a;
    return -1;
  }
  return Id = e, Id;
}
var Dd, bb;
function fO() {
  if (bb) return Dd;
  bb = 1;
  var e = oS(), t = uO(), r = dO();
  function n(a, i, o) {
    return i === i ? r(a, i, o) : e(a, t, o);
  }
  return Dd = n, Dd;
}
var $d, xb;
function mO() {
  if (xb) return $d;
  xb = 1;
  var e = fO();
  function t(r, n) {
    var a = r == null ? 0 : r.length;
    return !!a && e(r, n, 0) > -1;
  }
  return $d = t, $d;
}
var Ld, wb;
function hO() {
  if (wb) return Ld;
  wb = 1;
  function e(t, r, n) {
    for (var a = -1, i = t == null ? 0 : t.length; ++a < i; )
      if (n(r, t[a]))
        return !0;
    return !1;
  }
  return Ld = e, Ld;
}
var Fd, _b;
function pO() {
  if (_b) return Fd;
  _b = 1;
  function e() {
  }
  return Fd = e, Fd;
}
var qd, jb;
function gO() {
  if (jb) return qd;
  jb = 1;
  var e = nS(), t = pO(), r = mp(), n = 1 / 0, a = e && 1 / r(new e([, -0]))[1] == n ? function(i) {
    return new e(i);
  } : t;
  return qd = a, qd;
}
var Ud, Sb;
function yO() {
  if (Sb) return Ud;
  Sb = 1;
  var e = Kj(), t = mO(), r = hO(), n = Xj(), a = gO(), i = mp(), o = 200;
  function l(c, u, d) {
    var f = -1, p = t, m = c.length, y = !0, h = [], g = h;
    if (d)
      y = !1, p = r;
    else if (m >= o) {
      var v = u ? null : a(c);
      if (v)
        return i(v);
      y = !1, p = n, g = new e();
    } else
      g = u ? [] : h;
    e:
      for (; ++f < m; ) {
        var b = c[f], w = u ? u(b) : b;
        if (b = d || b !== 0 ? b : 0, y && w === w) {
          for (var j = g.length; j--; )
            if (g[j] === w)
              continue e;
          u && g.push(w), h.push(b);
        } else p(g, w, d) || (g !== h && g.push(w), h.push(b));
      }
    return h;
  }
  return Ud = l, Ud;
}
var zd, kb;
function vO() {
  if (kb) return zd;
  kb = 1;
  var e = jn(), t = yO();
  function r(n, a) {
    return n && n.length ? t(n, e(a, 2)) : [];
  }
  return zd = r, zd;
}
var bO = vO();
const Eb = /* @__PURE__ */ st(bO);
function sS(e, t, r) {
  return t === !0 ? Eb(e, r) : $e(t) ? Eb(e, t) : e;
}
function Ca(e) {
  "@babel/helpers - typeof";
  return Ca = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Ca(e);
}
var xO = ["ref"];
function Ab(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function Ur(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Ab(Object(r), !0).forEach(function(n) {
      Ml(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Ab(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function wO(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function Cb(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, cS(n.key), n);
  }
}
function _O(e, t, r) {
  return t && Cb(e.prototype, t), r && Cb(e, r), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function jO(e, t, r) {
  return t = js(t), SO(e, lS() ? Reflect.construct(t, r || [], js(e).constructor) : t.apply(e, r));
}
function SO(e, t) {
  if (t && (Ca(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return kO(e);
}
function kO(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function lS() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (lS = function() {
    return !!e;
  })();
}
function js(e) {
  return js = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, js(e);
}
function EO(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && Fm(e, t);
}
function Fm(e, t) {
  return Fm = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, Fm(e, t);
}
function Ml(e, t, r) {
  return t = cS(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function cS(e) {
  var t = AO(e, "string");
  return Ca(t) == "symbol" ? t : t + "";
}
function AO(e, t) {
  if (Ca(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Ca(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
function CO(e, t) {
  if (e == null) return {};
  var r = TO(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function TO(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
function PO(e) {
  return e.value;
}
function OO(e, t) {
  if (/* @__PURE__ */ I.isValidElement(e))
    return /* @__PURE__ */ I.cloneElement(e, t);
  if (typeof e == "function")
    return /* @__PURE__ */ I.createElement(e, t);
  t.ref;
  var r = CO(t, xO);
  return /* @__PURE__ */ I.createElement(fp, r);
}
var Tb = 1, Un = /* @__PURE__ */ (function(e) {
  function t() {
    var r;
    wO(this, t);
    for (var n = arguments.length, a = new Array(n), i = 0; i < n; i++)
      a[i] = arguments[i];
    return r = jO(this, t, [].concat(a)), Ml(r, "lastBoundingBox", {
      width: -1,
      height: -1
    }), r;
  }
  return EO(t, e), _O(t, [{
    key: "componentDidMount",
    value: function() {
      this.updateBBox();
    }
  }, {
    key: "componentDidUpdate",
    value: function() {
      this.updateBBox();
    }
  }, {
    key: "getBBox",
    value: function() {
      if (this.wrapperNode && this.wrapperNode.getBoundingClientRect) {
        var n = this.wrapperNode.getBoundingClientRect();
        return n.height = this.wrapperNode.offsetHeight, n.width = this.wrapperNode.offsetWidth, n;
      }
      return null;
    }
  }, {
    key: "updateBBox",
    value: function() {
      var n = this.props.onBBoxUpdate, a = this.getBBox();
      a ? (Math.abs(a.width - this.lastBoundingBox.width) > Tb || Math.abs(a.height - this.lastBoundingBox.height) > Tb) && (this.lastBoundingBox.width = a.width, this.lastBoundingBox.height = a.height, n && n(a)) : (this.lastBoundingBox.width !== -1 || this.lastBoundingBox.height !== -1) && (this.lastBoundingBox.width = -1, this.lastBoundingBox.height = -1, n && n(null));
    }
  }, {
    key: "getBBoxSnapshot",
    value: function() {
      return this.lastBoundingBox.width >= 0 && this.lastBoundingBox.height >= 0 ? Ur({}, this.lastBoundingBox) : {
        width: 0,
        height: 0
      };
    }
  }, {
    key: "getDefaultPosition",
    value: function(n) {
      var a = this.props, i = a.layout, o = a.align, l = a.verticalAlign, c = a.margin, u = a.chartWidth, d = a.chartHeight, f, p;
      if (!n || (n.left === void 0 || n.left === null) && (n.right === void 0 || n.right === null))
        if (o === "center" && i === "vertical") {
          var m = this.getBBoxSnapshot();
          f = {
            left: ((u || 0) - m.width) / 2
          };
        } else
          f = o === "right" ? {
            right: c && c.right || 0
          } : {
            left: c && c.left || 0
          };
      if (!n || (n.top === void 0 || n.top === null) && (n.bottom === void 0 || n.bottom === null))
        if (l === "middle") {
          var y = this.getBBoxSnapshot();
          p = {
            top: ((d || 0) - y.height) / 2
          };
        } else
          p = l === "bottom" ? {
            bottom: c && c.bottom || 0
          } : {
            top: c && c.top || 0
          };
      return Ur(Ur({}, f), p);
    }
  }, {
    key: "render",
    value: function() {
      var n = this, a = this.props, i = a.content, o = a.width, l = a.height, c = a.wrapperStyle, u = a.payloadUniqBy, d = a.payload, f = Ur(Ur({
        position: "absolute",
        width: o || "auto",
        height: l || "auto"
      }, this.getDefaultPosition(c)), c);
      return /* @__PURE__ */ I.createElement("div", {
        className: "recharts-legend-wrapper",
        style: f,
        ref: function(m) {
          n.wrapperNode = m;
        }
      }, OO(i, Ur(Ur({}, this.props), {}, {
        payload: sS(d, u, PO)
      })));
    }
  }], [{
    key: "getWithHeight",
    value: function(n, a) {
      var i = Ur(Ur({}, this.defaultProps), n.props), o = i.layout;
      return o === "vertical" && me(n.props.height) ? {
        height: n.props.height
      } : o === "horizontal" ? {
        width: n.props.width || a
      } : null;
    }
  }]);
})(wn);
Ml(Un, "displayName", "Legend");
Ml(Un, "defaultProps", {
  iconSize: 14,
  layout: "horizontal",
  align: "center",
  verticalAlign: "bottom"
});
var Bd, Pb;
function NO() {
  if (Pb) return Bd;
  Pb = 1;
  var e = ko(), t = hp(), r = Xt(), n = e ? e.isConcatSpreadable : void 0;
  function a(i) {
    return r(i) || t(i) || !!(n && i && i[n]);
  }
  return Bd = a, Bd;
}
var Wd, Ob;
function uS() {
  if (Ob) return Wd;
  Ob = 1;
  var e = Jj(), t = NO();
  function r(n, a, i, o, l) {
    var c = -1, u = n.length;
    for (i || (i = t), l || (l = []); ++c < u; ) {
      var d = n[c];
      a > 0 && i(d) ? a > 1 ? r(d, a - 1, i, o, l) : e(l, d) : o || (l[l.length] = d);
    }
    return l;
  }
  return Wd = r, Wd;
}
var Vd, Nb;
function MO() {
  if (Nb) return Vd;
  Nb = 1;
  function e(t) {
    return function(r, n, a) {
      for (var i = -1, o = Object(r), l = a(r), c = l.length; c--; ) {
        var u = l[t ? c : ++i];
        if (n(o[u], u, o) === !1)
          break;
      }
      return r;
    };
  }
  return Vd = e, Vd;
}
var Hd, Mb;
function RO() {
  if (Mb) return Hd;
  Mb = 1;
  var e = MO(), t = e();
  return Hd = t, Hd;
}
var Gd, Rb;
function dS() {
  if (Rb) return Gd;
  Rb = 1;
  var e = RO(), t = Nl();
  function r(n, a) {
    return n && e(n, a, t);
  }
  return Gd = r, Gd;
}
var Kd, Ib;
function IO() {
  if (Ib) return Kd;
  Ib = 1;
  var e = Co();
  function t(r, n) {
    return function(a, i) {
      if (a == null)
        return a;
      if (!e(a))
        return r(a, i);
      for (var o = a.length, l = n ? o : -1, c = Object(a); (n ? l-- : ++l < o) && i(c[l], l, c) !== !1; )
        ;
      return a;
    };
  }
  return Kd = t, Kd;
}
var Yd, Db;
function vp() {
  if (Db) return Yd;
  Db = 1;
  var e = dS(), t = IO(), r = t(e);
  return Yd = r, Yd;
}
var Xd, $b;
function fS() {
  if ($b) return Xd;
  $b = 1;
  var e = vp(), t = Co();
  function r(n, a) {
    var i = -1, o = t(n) ? Array(n.length) : [];
    return e(n, function(l, c, u) {
      o[++i] = a(l, c, u);
    }), o;
  }
  return Xd = r, Xd;
}
var Qd, Lb;
function DO() {
  if (Lb) return Qd;
  Lb = 1;
  function e(t, r) {
    var n = t.length;
    for (t.sort(r); n--; )
      t[n] = t[n].value;
    return t;
  }
  return Qd = e, Qd;
}
var Jd, Fb;
function $O() {
  if (Fb) return Jd;
  Fb = 1;
  var e = ei();
  function t(r, n) {
    if (r !== n) {
      var a = r !== void 0, i = r === null, o = r === r, l = e(r), c = n !== void 0, u = n === null, d = n === n, f = e(n);
      if (!u && !f && !l && r > n || l && c && d && !u && !f || i && c && d || !a && d || !o)
        return 1;
      if (!i && !l && !f && r < n || f && a && o && !i && !l || u && a && o || !c && o || !d)
        return -1;
    }
    return 0;
  }
  return Jd = t, Jd;
}
var Zd, qb;
function LO() {
  if (qb) return Zd;
  qb = 1;
  var e = $O();
  function t(r, n, a) {
    for (var i = -1, o = r.criteria, l = n.criteria, c = o.length, u = a.length; ++i < c; ) {
      var d = e(o[i], l[i]);
      if (d) {
        if (i >= u)
          return d;
        var f = a[i];
        return d * (f == "desc" ? -1 : 1);
      }
    }
    return r.index - n.index;
  }
  return Zd = t, Zd;
}
var ef, Ub;
function FO() {
  if (Ub) return ef;
  Ub = 1;
  var e = ap(), t = ip(), r = jn(), n = fS(), a = DO(), i = eS(), o = LO(), l = ri(), c = Xt();
  function u(d, f, p) {
    f.length ? f = e(f, function(h) {
      return c(h) ? function(g) {
        return t(g, h.length === 1 ? h[0] : h);
      } : h;
    }) : f = [l];
    var m = -1;
    f = e(f, i(r));
    var y = n(d, function(h, g, v) {
      var b = e(f, function(w) {
        return w(h);
      });
      return { criteria: b, index: ++m, value: h };
    });
    return a(y, function(h, g) {
      return o(h, g, p);
    });
  }
  return ef = u, ef;
}
var tf, zb;
function qO() {
  if (zb) return tf;
  zb = 1;
  function e(t, r, n) {
    switch (n.length) {
      case 0:
        return t.call(r);
      case 1:
        return t.call(r, n[0]);
      case 2:
        return t.call(r, n[0], n[1]);
      case 3:
        return t.call(r, n[0], n[1], n[2]);
    }
    return t.apply(r, n);
  }
  return tf = e, tf;
}
var rf, Bb;
function UO() {
  if (Bb) return rf;
  Bb = 1;
  var e = qO(), t = Math.max;
  function r(n, a, i) {
    return a = t(a === void 0 ? n.length - 1 : a, 0), function() {
      for (var o = arguments, l = -1, c = t(o.length - a, 0), u = Array(c); ++l < c; )
        u[l] = o[a + l];
      l = -1;
      for (var d = Array(a + 1); ++l < a; )
        d[l] = o[l];
      return d[a] = i(u), e(n, this, d);
    };
  }
  return rf = r, rf;
}
var nf, Wb;
function zO() {
  if (Wb) return nf;
  Wb = 1;
  function e(t) {
    return function() {
      return t;
    };
  }
  return nf = e, nf;
}
var af, Vb;
function mS() {
  if (Vb) return af;
  Vb = 1;
  var e = Yn(), t = (function() {
    try {
      var r = e(Object, "defineProperty");
      return r({}, "", {}), r;
    } catch {
    }
  })();
  return af = t, af;
}
var of, Hb;
function BO() {
  if (Hb) return of;
  Hb = 1;
  var e = zO(), t = mS(), r = ri(), n = t ? function(a, i) {
    return t(a, "toString", {
      configurable: !0,
      enumerable: !1,
      value: e(i),
      writable: !0
    });
  } : r;
  return of = n, of;
}
var sf, Gb;
function WO() {
  if (Gb) return sf;
  Gb = 1;
  var e = 800, t = 16, r = Date.now;
  function n(a) {
    var i = 0, o = 0;
    return function() {
      var l = r(), c = t - (l - o);
      if (o = l, c > 0) {
        if (++i >= e)
          return arguments[0];
      } else
        i = 0;
      return a.apply(void 0, arguments);
    };
  }
  return sf = n, sf;
}
var lf, Kb;
function VO() {
  if (Kb) return lf;
  Kb = 1;
  var e = BO(), t = WO(), r = t(e);
  return lf = r, lf;
}
var cf, Yb;
function HO() {
  if (Yb) return cf;
  Yb = 1;
  var e = ri(), t = UO(), r = VO();
  function n(a, i) {
    return r(t(a, i, e), a + "");
  }
  return cf = n, cf;
}
var uf, Xb;
function Rl() {
  if (Xb) return uf;
  Xb = 1;
  var e = tp(), t = Co(), r = pp(), n = _n();
  function a(i, o, l) {
    if (!n(l))
      return !1;
    var c = typeof o;
    return (c == "number" ? t(l) && r(o, l.length) : c == "string" && o in l) ? e(l[o], i) : !1;
  }
  return uf = a, uf;
}
var df, Qb;
function GO() {
  if (Qb) return df;
  Qb = 1;
  var e = uS(), t = FO(), r = HO(), n = Rl(), a = r(function(i, o) {
    if (i == null)
      return [];
    var l = o.length;
    return l > 1 && n(i, o[0], o[1]) ? o = [] : l > 2 && n(o[0], o[1], o[2]) && (o = [o[0]]), t(i, e(o, 1), []);
  });
  return df = a, df;
}
var KO = GO();
const bp = /* @__PURE__ */ st(KO);
function Bi(e) {
  "@babel/helpers - typeof";
  return Bi = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Bi(e);
}
function qm() {
  return qm = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, qm.apply(this, arguments);
}
function YO(e, t) {
  return ZO(e) || JO(e, t) || QO(e, t) || XO();
}
function XO() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function QO(e, t) {
  if (e) {
    if (typeof e == "string") return Jb(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return Jb(e, t);
  }
}
function Jb(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function JO(e, t) {
  var r = e == null ? null : typeof Symbol < "u" && e[Symbol.iterator] || e["@@iterator"];
  if (r != null) {
    var n, a, i, o, l = [], c = !0, u = !1;
    try {
      if (i = (r = r.call(e)).next, t !== 0) for (; !(c = (n = i.call(r)).done) && (l.push(n.value), l.length !== t); c = !0) ;
    } catch (d) {
      u = !0, a = d;
    } finally {
      try {
        if (!c && r.return != null && (o = r.return(), Object(o) !== o)) return;
      } finally {
        if (u) throw a;
      }
    }
    return l;
  }
}
function ZO(e) {
  if (Array.isArray(e)) return e;
}
function Zb(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function ff(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Zb(Object(r), !0).forEach(function(n) {
      eN(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Zb(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function eN(e, t, r) {
  return t = tN(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function tN(e) {
  var t = rN(e, "string");
  return Bi(t) == "symbol" ? t : t + "";
}
function rN(e, t) {
  if (Bi(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Bi(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function nN(e) {
  return Array.isArray(e) && Pt(e[0]) && Pt(e[1]) ? e.join(" ~ ") : e;
}
var aN = function(t) {
  var r = t.separator, n = r === void 0 ? " : " : r, a = t.contentStyle, i = a === void 0 ? {} : a, o = t.itemStyle, l = o === void 0 ? {} : o, c = t.labelStyle, u = c === void 0 ? {} : c, d = t.payload, f = t.formatter, p = t.itemSorter, m = t.wrapperClassName, y = t.labelClassName, h = t.label, g = t.labelFormatter, v = t.accessibilityLayer, b = v === void 0 ? !1 : v, w = function() {
    if (d && d.length) {
      var A = {
        padding: 0,
        margin: 0
      }, T = (p ? bp(d, p) : d).map(function(M, N) {
        if (M.type === "none")
          return null;
        var q = ff({
          display: "block",
          paddingTop: 4,
          paddingBottom: 4,
          color: M.color || "#000"
        }, l), V = M.formatter || f || nN, B = M.value, ee = M.name, U = B, G = ee;
        if (V && U != null && G != null) {
          var Y = V(B, ee, M, N, d);
          if (Array.isArray(Y)) {
            var Q = YO(Y, 2);
            U = Q[0], G = Q[1];
          } else
            U = Y;
        }
        return (
          // eslint-disable-next-line react/no-array-index-key
          /* @__PURE__ */ I.createElement("li", {
            className: "recharts-tooltip-item",
            key: "tooltip-item-".concat(N),
            style: q
          }, Pt(G) ? /* @__PURE__ */ I.createElement("span", {
            className: "recharts-tooltip-item-name"
          }, G) : null, Pt(G) ? /* @__PURE__ */ I.createElement("span", {
            className: "recharts-tooltip-item-separator"
          }, n) : null, /* @__PURE__ */ I.createElement("span", {
            className: "recharts-tooltip-item-value"
          }, U), /* @__PURE__ */ I.createElement("span", {
            className: "recharts-tooltip-item-unit"
          }, M.unit || ""))
        );
      });
      return /* @__PURE__ */ I.createElement("ul", {
        className: "recharts-tooltip-item-list",
        style: A
      }, T);
    }
    return null;
  }, j = ff({
    margin: 0,
    padding: 10,
    backgroundColor: "#fff",
    border: "1px solid #ccc",
    whiteSpace: "nowrap"
  }, i), x = ff({
    margin: 0
  }, u), _ = !Le(h), S = _ ? h : "", k = Ve("recharts-default-tooltip", m), E = Ve("recharts-tooltip-label", y);
  _ && g && d !== void 0 && d !== null && (S = g(h, d));
  var L = b ? {
    role: "status",
    "aria-live": "assertive"
  } : {};
  return /* @__PURE__ */ I.createElement("div", qm({
    className: k,
    style: j
  }, L), /* @__PURE__ */ I.createElement("p", {
    className: E,
    style: x
  }, /* @__PURE__ */ I.isValidElement(S) ? S : "".concat(S)), w());
};
function Wi(e) {
  "@babel/helpers - typeof";
  return Wi = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Wi(e);
}
function Xo(e, t, r) {
  return t = iN(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function iN(e) {
  var t = oN(e, "string");
  return Wi(t) == "symbol" ? t : t + "";
}
function oN(e, t) {
  if (Wi(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Wi(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
var bi = "recharts-tooltip-wrapper", sN = {
  visibility: "hidden"
};
function lN(e) {
  var t = e.coordinate, r = e.translateX, n = e.translateY;
  return Ve(bi, Xo(Xo(Xo(Xo({}, "".concat(bi, "-right"), me(r) && t && me(t.x) && r >= t.x), "".concat(bi, "-left"), me(r) && t && me(t.x) && r < t.x), "".concat(bi, "-bottom"), me(n) && t && me(t.y) && n >= t.y), "".concat(bi, "-top"), me(n) && t && me(t.y) && n < t.y));
}
function e0(e) {
  var t = e.allowEscapeViewBox, r = e.coordinate, n = e.key, a = e.offsetTopLeft, i = e.position, o = e.reverseDirection, l = e.tooltipDimension, c = e.viewBox, u = e.viewBoxDimension;
  if (i && me(i[n]))
    return i[n];
  var d = r[n] - l - a, f = r[n] + a;
  if (t[n])
    return o[n] ? d : f;
  if (o[n]) {
    var p = d, m = c[n];
    return p < m ? Math.max(f, c[n]) : Math.max(d, c[n]);
  }
  var y = f + l, h = c[n] + u;
  return y > h ? Math.max(d, c[n]) : Math.max(f, c[n]);
}
function cN(e) {
  var t = e.translateX, r = e.translateY, n = e.useTranslate3d;
  return {
    transform: n ? "translate3d(".concat(t, "px, ").concat(r, "px, 0)") : "translate(".concat(t, "px, ").concat(r, "px)")
  };
}
function uN(e) {
  var t = e.allowEscapeViewBox, r = e.coordinate, n = e.offsetTopLeft, a = e.position, i = e.reverseDirection, o = e.tooltipBox, l = e.useTranslate3d, c = e.viewBox, u, d, f;
  return o.height > 0 && o.width > 0 && r ? (d = e0({
    allowEscapeViewBox: t,
    coordinate: r,
    key: "x",
    offsetTopLeft: n,
    position: a,
    reverseDirection: i,
    tooltipDimension: o.width,
    viewBox: c,
    viewBoxDimension: c.width
  }), f = e0({
    allowEscapeViewBox: t,
    coordinate: r,
    key: "y",
    offsetTopLeft: n,
    position: a,
    reverseDirection: i,
    tooltipDimension: o.height,
    viewBox: c,
    viewBoxDimension: c.height
  }), u = cN({
    translateX: d,
    translateY: f,
    useTranslate3d: l
  })) : u = sN, {
    cssProperties: u,
    cssClasses: lN({
      translateX: d,
      translateY: f,
      coordinate: r
    })
  };
}
function Ta(e) {
  "@babel/helpers - typeof";
  return Ta = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Ta(e);
}
function t0(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function r0(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? t0(Object(r), !0).forEach(function(n) {
      zm(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : t0(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function dN(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function fN(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, pS(n.key), n);
  }
}
function mN(e, t, r) {
  return t && fN(e.prototype, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function hN(e, t, r) {
  return t = Ss(t), pN(e, hS() ? Reflect.construct(t, r || [], Ss(e).constructor) : t.apply(e, r));
}
function pN(e, t) {
  if (t && (Ta(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return gN(e);
}
function gN(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function hS() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (hS = function() {
    return !!e;
  })();
}
function Ss(e) {
  return Ss = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, Ss(e);
}
function yN(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && Um(e, t);
}
function Um(e, t) {
  return Um = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, Um(e, t);
}
function zm(e, t, r) {
  return t = pS(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function pS(e) {
  var t = vN(e, "string");
  return Ta(t) == "symbol" ? t : t + "";
}
function vN(e, t) {
  if (Ta(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Ta(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
var n0 = 1, bN = /* @__PURE__ */ (function(e) {
  function t() {
    var r;
    dN(this, t);
    for (var n = arguments.length, a = new Array(n), i = 0; i < n; i++)
      a[i] = arguments[i];
    return r = hN(this, t, [].concat(a)), zm(r, "state", {
      dismissed: !1,
      dismissedAtCoordinate: {
        x: 0,
        y: 0
      },
      lastBoundingBox: {
        width: -1,
        height: -1
      }
    }), zm(r, "handleKeyDown", function(o) {
      if (o.key === "Escape") {
        var l, c, u, d;
        r.setState({
          dismissed: !0,
          dismissedAtCoordinate: {
            x: (l = (c = r.props.coordinate) === null || c === void 0 ? void 0 : c.x) !== null && l !== void 0 ? l : 0,
            y: (u = (d = r.props.coordinate) === null || d === void 0 ? void 0 : d.y) !== null && u !== void 0 ? u : 0
          }
        });
      }
    }), r;
  }
  return yN(t, e), mN(t, [{
    key: "updateBBox",
    value: function() {
      if (this.wrapperNode && this.wrapperNode.getBoundingClientRect) {
        var n = this.wrapperNode.getBoundingClientRect();
        (Math.abs(n.width - this.state.lastBoundingBox.width) > n0 || Math.abs(n.height - this.state.lastBoundingBox.height) > n0) && this.setState({
          lastBoundingBox: {
            width: n.width,
            height: n.height
          }
        });
      } else (this.state.lastBoundingBox.width !== -1 || this.state.lastBoundingBox.height !== -1) && this.setState({
        lastBoundingBox: {
          width: -1,
          height: -1
        }
      });
    }
  }, {
    key: "componentDidMount",
    value: function() {
      document.addEventListener("keydown", this.handleKeyDown), this.updateBBox();
    }
  }, {
    key: "componentWillUnmount",
    value: function() {
      document.removeEventListener("keydown", this.handleKeyDown);
    }
  }, {
    key: "componentDidUpdate",
    value: function() {
      var n, a;
      this.props.active && this.updateBBox(), this.state.dismissed && (((n = this.props.coordinate) === null || n === void 0 ? void 0 : n.x) !== this.state.dismissedAtCoordinate.x || ((a = this.props.coordinate) === null || a === void 0 ? void 0 : a.y) !== this.state.dismissedAtCoordinate.y) && (this.state.dismissed = !1);
    }
  }, {
    key: "render",
    value: function() {
      var n = this, a = this.props, i = a.active, o = a.allowEscapeViewBox, l = a.animationDuration, c = a.animationEasing, u = a.children, d = a.coordinate, f = a.hasPayload, p = a.isAnimationActive, m = a.offset, y = a.position, h = a.reverseDirection, g = a.useTranslate3d, v = a.viewBox, b = a.wrapperStyle, w = uN({
        allowEscapeViewBox: o,
        coordinate: d,
        offsetTopLeft: m,
        position: y,
        reverseDirection: h,
        tooltipBox: this.state.lastBoundingBox,
        useTranslate3d: g,
        viewBox: v
      }), j = w.cssClasses, x = w.cssProperties, _ = r0(r0({
        transition: p && i ? "transform ".concat(l, "ms ").concat(c) : void 0
      }, x), {}, {
        pointerEvents: "none",
        visibility: !this.state.dismissed && i && f ? "visible" : "hidden",
        position: "absolute",
        top: 0,
        left: 0
      }, b);
      return (
        // This element allow listening to the `Escape` key.
        // See https://github.com/recharts/recharts/pull/2925
        /* @__PURE__ */ I.createElement("div", {
          tabIndex: -1,
          className: j,
          style: _,
          ref: function(k) {
            n.wrapperNode = k;
          }
        }, u)
      );
    }
  }]);
})(wn), xN = function() {
  return !(typeof window < "u" && window.document && window.document.createElement && window.setTimeout);
}, ni = {
  isSsr: xN()
};
function Pa(e) {
  "@babel/helpers - typeof";
  return Pa = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Pa(e);
}
function a0(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function i0(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? a0(Object(r), !0).forEach(function(n) {
      xp(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : a0(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function wN(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function _N(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, yS(n.key), n);
  }
}
function jN(e, t, r) {
  return t && _N(e.prototype, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function SN(e, t, r) {
  return t = ks(t), kN(e, gS() ? Reflect.construct(t, r || [], ks(e).constructor) : t.apply(e, r));
}
function kN(e, t) {
  if (t && (Pa(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return EN(e);
}
function EN(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function gS() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (gS = function() {
    return !!e;
  })();
}
function ks(e) {
  return ks = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, ks(e);
}
function AN(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && Bm(e, t);
}
function Bm(e, t) {
  return Bm = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, Bm(e, t);
}
function xp(e, t, r) {
  return t = yS(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function yS(e) {
  var t = CN(e, "string");
  return Pa(t) == "symbol" ? t : t + "";
}
function CN(e, t) {
  if (Pa(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Pa(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
function TN(e) {
  return e.dataKey;
}
function PN(e, t) {
  return /* @__PURE__ */ I.isValidElement(e) ? /* @__PURE__ */ I.cloneElement(e, t) : typeof e == "function" ? /* @__PURE__ */ I.createElement(e, t) : /* @__PURE__ */ I.createElement(aN, t);
}
var Or = /* @__PURE__ */ (function(e) {
  function t() {
    return wN(this, t), SN(this, t, arguments);
  }
  return AN(t, e), jN(t, [{
    key: "render",
    value: function() {
      var n = this, a = this.props, i = a.active, o = a.allowEscapeViewBox, l = a.animationDuration, c = a.animationEasing, u = a.content, d = a.coordinate, f = a.filterNull, p = a.isAnimationActive, m = a.offset, y = a.payload, h = a.payloadUniqBy, g = a.position, v = a.reverseDirection, b = a.useTranslate3d, w = a.viewBox, j = a.wrapperStyle, x = y ?? [];
      f && x.length && (x = sS(y.filter(function(S) {
        return S.value != null && (S.hide !== !0 || n.props.includeHidden);
      }), h, TN));
      var _ = x.length > 0;
      return /* @__PURE__ */ I.createElement(bN, {
        allowEscapeViewBox: o,
        animationDuration: l,
        animationEasing: c,
        isAnimationActive: p,
        active: i,
        coordinate: d,
        hasPayload: _,
        offset: m,
        position: g,
        reverseDirection: v,
        useTranslate3d: b,
        viewBox: w,
        wrapperStyle: j
      }, PN(u, i0(i0({}, this.props), {}, {
        payload: x
      })));
    }
  }]);
})(wn);
xp(Or, "displayName", "Tooltip");
xp(Or, "defaultProps", {
  accessibilityLayer: !1,
  allowEscapeViewBox: {
    x: !1,
    y: !1
  },
  animationDuration: 400,
  animationEasing: "ease",
  contentStyle: {},
  coordinate: {
    x: 0,
    y: 0
  },
  cursor: !0,
  cursorStyle: {},
  filterNull: !0,
  isAnimationActive: !ni.isSsr,
  itemStyle: {},
  labelStyle: {},
  offset: 10,
  reverseDirection: {
    x: !1,
    y: !1
  },
  separator: " : ",
  trigger: "hover",
  useTranslate3d: !1,
  viewBox: {
    x: 0,
    y: 0,
    height: 0,
    width: 0
  },
  wrapperStyle: {}
});
var mf, o0;
function ON() {
  if (o0) return mf;
  o0 = 1;
  var e = Lr(), t = function() {
    return e.Date.now();
  };
  return mf = t, mf;
}
var hf, s0;
function NN() {
  if (s0) return hf;
  s0 = 1;
  var e = /\s/;
  function t(r) {
    for (var n = r.length; n-- && e.test(r.charAt(n)); )
      ;
    return n;
  }
  return hf = t, hf;
}
var pf, l0;
function MN() {
  if (l0) return pf;
  l0 = 1;
  var e = NN(), t = /^\s+/;
  function r(n) {
    return n && n.slice(0, e(n) + 1).replace(t, "");
  }
  return pf = r, pf;
}
var gf, c0;
function vS() {
  if (c0) return gf;
  c0 = 1;
  var e = MN(), t = _n(), r = ei(), n = NaN, a = /^[-+]0x[0-9a-f]+$/i, i = /^0b[01]+$/i, o = /^0o[0-7]+$/i, l = parseInt;
  function c(u) {
    if (typeof u == "number")
      return u;
    if (r(u))
      return n;
    if (t(u)) {
      var d = typeof u.valueOf == "function" ? u.valueOf() : u;
      u = t(d) ? d + "" : d;
    }
    if (typeof u != "string")
      return u === 0 ? u : +u;
    u = e(u);
    var f = i.test(u);
    return f || o.test(u) ? l(u.slice(2), f ? 2 : 8) : a.test(u) ? n : +u;
  }
  return gf = c, gf;
}
var yf, u0;
function RN() {
  if (u0) return yf;
  u0 = 1;
  var e = _n(), t = ON(), r = vS(), n = "Expected a function", a = Math.max, i = Math.min;
  function o(l, c, u) {
    var d, f, p, m, y, h, g = 0, v = !1, b = !1, w = !0;
    if (typeof l != "function")
      throw new TypeError(n);
    c = r(c) || 0, e(u) && (v = !!u.leading, b = "maxWait" in u, p = b ? a(r(u.maxWait) || 0, c) : p, w = "trailing" in u ? !!u.trailing : w);
    function j(T) {
      var M = d, N = f;
      return d = f = void 0, g = T, m = l.apply(N, M), m;
    }
    function x(T) {
      return g = T, y = setTimeout(k, c), v ? j(T) : m;
    }
    function _(T) {
      var M = T - h, N = T - g, q = c - M;
      return b ? i(q, p - N) : q;
    }
    function S(T) {
      var M = T - h, N = T - g;
      return h === void 0 || M >= c || M < 0 || b && N >= p;
    }
    function k() {
      var T = t();
      if (S(T))
        return E(T);
      y = setTimeout(k, _(T));
    }
    function E(T) {
      return y = void 0, w && d ? j(T) : (d = f = void 0, m);
    }
    function L() {
      y !== void 0 && clearTimeout(y), g = 0, d = h = f = y = void 0;
    }
    function P() {
      return y === void 0 ? m : E(t());
    }
    function A() {
      var T = t(), M = S(T);
      if (d = arguments, f = this, h = T, M) {
        if (y === void 0)
          return x(h);
        if (b)
          return clearTimeout(y), y = setTimeout(k, c), j(h);
      }
      return y === void 0 && (y = setTimeout(k, c)), m;
    }
    return A.cancel = L, A.flush = P, A;
  }
  return yf = o, yf;
}
var vf, d0;
function IN() {
  if (d0) return vf;
  d0 = 1;
  var e = RN(), t = _n(), r = "Expected a function";
  function n(a, i, o) {
    var l = !0, c = !0;
    if (typeof a != "function")
      throw new TypeError(r);
    return t(o) && (l = "leading" in o ? !!o.leading : l, c = "trailing" in o ? !!o.trailing : c), e(a, i, {
      leading: l,
      maxWait: i,
      trailing: c
    });
  }
  return vf = n, vf;
}
var DN = IN();
const bS = /* @__PURE__ */ st(DN);
function Vi(e) {
  "@babel/helpers - typeof";
  return Vi = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Vi(e);
}
function f0(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function Qo(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? f0(Object(r), !0).forEach(function(n) {
      $N(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : f0(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function $N(e, t, r) {
  return t = LN(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function LN(e) {
  var t = FN(e, "string");
  return Vi(t) == "symbol" ? t : t + "";
}
function FN(e, t) {
  if (Vi(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Vi(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function qN(e, t) {
  return WN(e) || BN(e, t) || zN(e, t) || UN();
}
function UN() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function zN(e, t) {
  if (e) {
    if (typeof e == "string") return m0(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return m0(e, t);
  }
}
function m0(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function BN(e, t) {
  var r = e == null ? null : typeof Symbol < "u" && e[Symbol.iterator] || e["@@iterator"];
  if (r != null) {
    var n, a, i, o, l = [], c = !0, u = !1;
    try {
      if (i = (r = r.call(e)).next, t !== 0) for (; !(c = (n = i.call(r)).done) && (l.push(n.value), l.length !== t); c = !0) ;
    } catch (d) {
      u = !0, a = d;
    } finally {
      try {
        if (!c && r.return != null && (o = r.return(), Object(o) !== o)) return;
      } finally {
        if (u) throw a;
      }
    }
    return l;
  }
}
function WN(e) {
  if (Array.isArray(e)) return e;
}
var VN = /* @__PURE__ */ U_(function(e, t) {
  var r = e.aspect, n = e.initialDimension, a = n === void 0 ? {
    width: -1,
    height: -1
  } : n, i = e.width, o = i === void 0 ? "100%" : i, l = e.height, c = l === void 0 ? "100%" : l, u = e.minWidth, d = u === void 0 ? 0 : u, f = e.minHeight, p = e.maxHeight, m = e.children, y = e.debounce, h = y === void 0 ? 0 : y, g = e.id, v = e.className, b = e.onResize, w = e.style, j = w === void 0 ? {} : w, x = Tr(null), _ = Tr();
  _.current = b, VE(t, function() {
    return Object.defineProperty(x.current, "current", {
      get: function() {
        return console.warn("The usage of ref.current.current is deprecated and will no longer be supported."), x.current;
      },
      configurable: !0
    });
  });
  var S = de({
    containerWidth: a.width,
    containerHeight: a.height
  }), k = qN(S, 2), E = k[0], L = k[1], P = ot(function(T, M) {
    L(function(N) {
      var q = Math.round(T), V = Math.round(M);
      return N.containerWidth === q && N.containerHeight === V ? N : {
        containerWidth: q,
        containerHeight: V
      };
    });
  }, []);
  et(function() {
    var T = function(ee) {
      var U, G = ee[0].contentRect, Y = G.width, Q = G.height;
      P(Y, Q), (U = _.current) === null || U === void 0 || U.call(_, Y, Q);
    };
    h > 0 && (T = bS(T, h, {
      trailing: !0,
      leading: !1
    }));
    var M = new ResizeObserver(T), N = x.current.getBoundingClientRect(), q = N.width, V = N.height;
    return P(q, V), M.observe(x.current), function() {
      M.disconnect();
    };
  }, [P, h]);
  var A = Ya(function() {
    var T = E.containerWidth, M = E.containerHeight;
    if (T < 0 || M < 0)
      return null;
    Xr(In(o) || In(c), `The width(%s) and height(%s) are both fixed numbers,
       maybe you don't need to use a ResponsiveContainer.`, o, c), Xr(!r || r > 0, "The aspect(%s) must be greater than zero.", r);
    var N = In(o) ? T : o, q = In(c) ? M : c;
    r && r > 0 && (N ? q = N / r : q && (N = q * r), p && q > p && (q = p)), Xr(N > 0 || q > 0, `The width(%s) and height(%s) of chart should be greater than 0,
       please check the style of container, or the props width(%s) and height(%s),
       or add a minWidth(%s) or minHeight(%s) or use aspect(%s) to control the
       height and width.`, N, q, o, c, d, f, r);
    var V = !Array.isArray(m) && Yr(m.type).endsWith("Chart");
    return I.Children.map(m, function(B) {
      return /* @__PURE__ */ I.isValidElement(B) ? /* @__PURE__ */ Tt(B, Qo({
        width: N,
        height: q
      }, V ? {
        style: Qo({
          height: "100%",
          width: "100%",
          maxHeight: q,
          maxWidth: N
        }, B.props.style)
      } : {})) : B;
    });
  }, [r, m, c, p, f, d, E, o]);
  return /* @__PURE__ */ I.createElement("div", {
    id: g ? "".concat(g) : void 0,
    className: Ve("recharts-responsive-container", v),
    style: Qo(Qo({}, j), {}, {
      width: o,
      height: c,
      minWidth: d,
      minHeight: f,
      maxHeight: p
    }),
    ref: x
  }, A);
}), xS = function(t) {
  return null;
};
xS.displayName = "Cell";
function Hi(e) {
  "@babel/helpers - typeof";
  return Hi = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Hi(e);
}
function h0(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function Wm(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? h0(Object(r), !0).forEach(function(n) {
      HN(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : h0(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function HN(e, t, r) {
  return t = GN(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function GN(e) {
  var t = KN(e, "string");
  return Hi(t) == "symbol" ? t : t + "";
}
function KN(e, t) {
  if (Hi(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Hi(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
var oa = {
  widthCache: {},
  cacheCount: 0
}, YN = 2e3, XN = {
  position: "absolute",
  top: "-20000px",
  left: 0,
  padding: 0,
  margin: 0,
  border: "none",
  whiteSpace: "pre"
}, p0 = "recharts_measurement_span";
function QN(e) {
  var t = Wm({}, e);
  return Object.keys(t).forEach(function(r) {
    t[r] || delete t[r];
  }), t;
}
var Ni = function(t) {
  var r = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
  if (t == null || ni.isSsr)
    return {
      width: 0,
      height: 0
    };
  var n = QN(r), a = JSON.stringify({
    text: t,
    copyStyle: n
  });
  if (oa.widthCache[a])
    return oa.widthCache[a];
  try {
    var i = document.getElementById(p0);
    i || (i = document.createElement("span"), i.setAttribute("id", p0), i.setAttribute("aria-hidden", "true"), document.body.appendChild(i));
    var o = Wm(Wm({}, XN), n);
    Object.assign(i.style, o), i.textContent = "".concat(t);
    var l = i.getBoundingClientRect(), c = {
      width: l.width,
      height: l.height
    };
    return oa.widthCache[a] = c, ++oa.cacheCount > YN && (oa.cacheCount = 0, oa.widthCache = {}), c;
  } catch {
    return {
      width: 0,
      height: 0
    };
  }
}, JN = function(t) {
  return {
    top: t.top + window.scrollY - document.documentElement.clientTop,
    left: t.left + window.scrollX - document.documentElement.clientLeft
  };
};
function Gi(e) {
  "@babel/helpers - typeof";
  return Gi = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Gi(e);
}
function Es(e, t) {
  return rM(e) || tM(e, t) || eM(e, t) || ZN();
}
function ZN() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function eM(e, t) {
  if (e) {
    if (typeof e == "string") return g0(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return g0(e, t);
  }
}
function g0(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function tM(e, t) {
  var r = e == null ? null : typeof Symbol < "u" && e[Symbol.iterator] || e["@@iterator"];
  if (r != null) {
    var n, a, i, o, l = [], c = !0, u = !1;
    try {
      if (i = (r = r.call(e)).next, t === 0) {
        if (Object(r) !== r) return;
        c = !1;
      } else for (; !(c = (n = i.call(r)).done) && (l.push(n.value), l.length !== t); c = !0) ;
    } catch (d) {
      u = !0, a = d;
    } finally {
      try {
        if (!c && r.return != null && (o = r.return(), Object(o) !== o)) return;
      } finally {
        if (u) throw a;
      }
    }
    return l;
  }
}
function rM(e) {
  if (Array.isArray(e)) return e;
}
function nM(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function y0(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, iM(n.key), n);
  }
}
function aM(e, t, r) {
  return t && y0(e.prototype, t), r && y0(e, r), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function iM(e) {
  var t = oM(e, "string");
  return Gi(t) == "symbol" ? t : t + "";
}
function oM(e, t) {
  if (Gi(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Gi(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
var v0 = /(-?\d+(?:\.\d+)?[a-zA-Z%]*)([*/])(-?\d+(?:\.\d+)?[a-zA-Z%]*)/, b0 = /(-?\d+(?:\.\d+)?[a-zA-Z%]*)([+-])(-?\d+(?:\.\d+)?[a-zA-Z%]*)/, sM = /^px|cm|vh|vw|em|rem|%|mm|in|pt|pc|ex|ch|vmin|vmax|Q$/, lM = /(-?\d+(?:\.\d+)?)([a-zA-Z%]+)?/, wS = {
  cm: 96 / 2.54,
  mm: 96 / 25.4,
  pt: 96 / 72,
  pc: 96 / 6,
  in: 96,
  Q: 96 / (2.54 * 40),
  px: 1
}, cM = Object.keys(wS), xa = "NaN";
function uM(e, t) {
  return e * wS[t];
}
var Jo = /* @__PURE__ */ (function() {
  function e(t, r) {
    nM(this, e), this.num = t, this.unit = r, this.num = t, this.unit = r, Number.isNaN(t) && (this.unit = ""), r !== "" && !sM.test(r) && (this.num = NaN, this.unit = ""), cM.includes(r) && (this.num = uM(t, r), this.unit = "px");
  }
  return aM(e, [{
    key: "add",
    value: function(r) {
      return this.unit !== r.unit ? new e(NaN, "") : new e(this.num + r.num, this.unit);
    }
  }, {
    key: "subtract",
    value: function(r) {
      return this.unit !== r.unit ? new e(NaN, "") : new e(this.num - r.num, this.unit);
    }
  }, {
    key: "multiply",
    value: function(r) {
      return this.unit !== "" && r.unit !== "" && this.unit !== r.unit ? new e(NaN, "") : new e(this.num * r.num, this.unit || r.unit);
    }
  }, {
    key: "divide",
    value: function(r) {
      return this.unit !== "" && r.unit !== "" && this.unit !== r.unit ? new e(NaN, "") : new e(this.num / r.num, this.unit || r.unit);
    }
  }, {
    key: "toString",
    value: function() {
      return "".concat(this.num).concat(this.unit);
    }
  }, {
    key: "isNaN",
    value: function() {
      return Number.isNaN(this.num);
    }
  }], [{
    key: "parse",
    value: function(r) {
      var n, a = (n = lM.exec(r)) !== null && n !== void 0 ? n : [], i = Es(a, 3), o = i[1], l = i[2];
      return new e(parseFloat(o), l ?? "");
    }
  }]);
})();
function _S(e) {
  if (e.includes(xa))
    return xa;
  for (var t = e; t.includes("*") || t.includes("/"); ) {
    var r, n = (r = v0.exec(t)) !== null && r !== void 0 ? r : [], a = Es(n, 4), i = a[1], o = a[2], l = a[3], c = Jo.parse(i ?? ""), u = Jo.parse(l ?? ""), d = o === "*" ? c.multiply(u) : c.divide(u);
    if (d.isNaN())
      return xa;
    t = t.replace(v0, d.toString());
  }
  for (; t.includes("+") || /.-\d+(?:\.\d+)?/.test(t); ) {
    var f, p = (f = b0.exec(t)) !== null && f !== void 0 ? f : [], m = Es(p, 4), y = m[1], h = m[2], g = m[3], v = Jo.parse(y ?? ""), b = Jo.parse(g ?? ""), w = h === "+" ? v.add(b) : v.subtract(b);
    if (w.isNaN())
      return xa;
    t = t.replace(b0, w.toString());
  }
  return t;
}
var x0 = /\(([^()]*)\)/;
function dM(e) {
  for (var t = e; t.includes("("); ) {
    var r = x0.exec(t), n = Es(r, 2), a = n[1];
    t = t.replace(x0, _S(a));
  }
  return t;
}
function fM(e) {
  var t = e.replace(/\s+/g, "");
  return t = dM(t), t = _S(t), t;
}
function mM(e) {
  try {
    return fM(e);
  } catch {
    return xa;
  }
}
function bf(e) {
  var t = mM(e.slice(5, -1));
  return t === xa ? "" : t;
}
var hM = ["x", "y", "lineHeight", "capHeight", "scaleToFit", "textAnchor", "verticalAnchor", "fill"], pM = ["dx", "dy", "angle", "className", "breakAll"];
function Vm() {
  return Vm = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Vm.apply(this, arguments);
}
function w0(e, t) {
  if (e == null) return {};
  var r = gM(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function gM(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
function _0(e, t) {
  return xM(e) || bM(e, t) || vM(e, t) || yM();
}
function yM() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function vM(e, t) {
  if (e) {
    if (typeof e == "string") return j0(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return j0(e, t);
  }
}
function j0(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function bM(e, t) {
  var r = e == null ? null : typeof Symbol < "u" && e[Symbol.iterator] || e["@@iterator"];
  if (r != null) {
    var n, a, i, o, l = [], c = !0, u = !1;
    try {
      if (i = (r = r.call(e)).next, t === 0) {
        if (Object(r) !== r) return;
        c = !1;
      } else for (; !(c = (n = i.call(r)).done) && (l.push(n.value), l.length !== t); c = !0) ;
    } catch (d) {
      u = !0, a = d;
    } finally {
      try {
        if (!c && r.return != null && (o = r.return(), Object(o) !== o)) return;
      } finally {
        if (u) throw a;
      }
    }
    return l;
  }
}
function xM(e) {
  if (Array.isArray(e)) return e;
}
var jS = /[ \f\n\r\t\v\u2028\u2029]+/, SS = function(t) {
  var r = t.children, n = t.breakAll, a = t.style;
  try {
    var i = [];
    Le(r) || (n ? i = r.toString().split("") : i = r.toString().split(jS));
    var o = i.map(function(c) {
      return {
        word: c,
        width: Ni(c, a).width
      };
    }), l = n ? 0 : Ni(" ", a).width;
    return {
      wordsWithComputedWidth: o,
      spaceWidth: l
    };
  } catch {
    return null;
  }
}, wM = function(t, r, n, a, i) {
  var o = t.maxLines, l = t.children, c = t.style, u = t.breakAll, d = me(o), f = l, p = function() {
    var N = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : [];
    return N.reduce(function(q, V) {
      var B = V.word, ee = V.width, U = q[q.length - 1];
      if (U && (a == null || i || U.width + ee + n < Number(a)))
        U.words.push(B), U.width += ee + n;
      else {
        var G = {
          words: [B],
          width: ee
        };
        q.push(G);
      }
      return q;
    }, []);
  }, m = p(r), y = function(N) {
    return N.reduce(function(q, V) {
      return q.width > V.width ? q : V;
    });
  };
  if (!d)
    return m;
  for (var h = "…", g = function(N) {
    var q = f.slice(0, N), V = SS({
      breakAll: u,
      style: c,
      children: q + h
    }).wordsWithComputedWidth, B = p(V), ee = B.length > o || y(B).width > Number(a);
    return [ee, B];
  }, v = 0, b = f.length - 1, w = 0, j; v <= b && w <= f.length - 1; ) {
    var x = Math.floor((v + b) / 2), _ = x - 1, S = g(_), k = _0(S, 2), E = k[0], L = k[1], P = g(x), A = _0(P, 1), T = A[0];
    if (!E && !T && (v = x + 1), E && T && (b = x - 1), !E && T) {
      j = L;
      break;
    }
    w++;
  }
  return j || m;
}, S0 = function(t) {
  var r = Le(t) ? [] : t.toString().split(jS);
  return [{
    words: r
  }];
}, _M = function(t) {
  var r = t.width, n = t.scaleToFit, a = t.children, i = t.style, o = t.breakAll, l = t.maxLines;
  if ((r || n) && !ni.isSsr) {
    var c, u, d = SS({
      breakAll: o,
      children: a,
      style: i
    });
    if (d) {
      var f = d.wordsWithComputedWidth, p = d.spaceWidth;
      c = f, u = p;
    } else
      return S0(a);
    return wM({
      breakAll: o,
      children: a,
      maxLines: l,
      style: i
    }, c, u, r, n);
  }
  return S0(a);
}, k0 = "#808080", As = function(t) {
  var r = t.x, n = r === void 0 ? 0 : r, a = t.y, i = a === void 0 ? 0 : a, o = t.lineHeight, l = o === void 0 ? "1em" : o, c = t.capHeight, u = c === void 0 ? "0.71em" : c, d = t.scaleToFit, f = d === void 0 ? !1 : d, p = t.textAnchor, m = p === void 0 ? "start" : p, y = t.verticalAnchor, h = y === void 0 ? "end" : y, g = t.fill, v = g === void 0 ? k0 : g, b = w0(t, hM), w = Ya(function() {
    return _M({
      breakAll: b.breakAll,
      children: b.children,
      maxLines: b.maxLines,
      scaleToFit: f,
      style: b.style,
      width: b.width
    });
  }, [b.breakAll, b.children, b.maxLines, f, b.style, b.width]), j = b.dx, x = b.dy, _ = b.angle, S = b.className, k = b.breakAll, E = w0(b, pM);
  if (!Pt(n) || !Pt(i))
    return null;
  var L = n + (me(j) ? j : 0), P = i + (me(x) ? x : 0), A;
  switch (h) {
    case "start":
      A = bf("calc(".concat(u, ")"));
      break;
    case "middle":
      A = bf("calc(".concat((w.length - 1) / 2, " * -").concat(l, " + (").concat(u, " / 2))"));
      break;
    default:
      A = bf("calc(".concat(w.length - 1, " * -").concat(l, ")"));
      break;
  }
  var T = [];
  if (f) {
    var M = w[0].width, N = b.width;
    T.push("scale(".concat((me(N) ? N / M : 1) / M, ")"));
  }
  return _ && T.push("rotate(".concat(_, ", ").concat(L, ", ").concat(P, ")")), T.length && (E.transform = T.join(" ")), /* @__PURE__ */ I.createElement("text", Vm({}, ze(E, !0), {
    x: L,
    y: P,
    className: Ve("recharts-text", S),
    textAnchor: m,
    fill: v.includes("url") ? k0 : v
  }), w.map(function(q, V) {
    var B = q.words.join(k ? "" : " ");
    return (
      // duplicate words will cause duplicate keys
      // eslint-disable-next-line react/no-array-index-key
      /* @__PURE__ */ I.createElement("tspan", {
        x: L,
        dy: V === 0 ? A : l,
        key: "".concat(B, "-").concat(V)
      }, B)
    );
  }));
};
function vn(e, t) {
  return e == null || t == null ? NaN : e < t ? -1 : e > t ? 1 : e >= t ? 0 : NaN;
}
function jM(e, t) {
  return e == null || t == null ? NaN : t < e ? -1 : t > e ? 1 : t >= e ? 0 : NaN;
}
function wp(e) {
  let t, r, n;
  e.length !== 2 ? (t = vn, r = (l, c) => vn(e(l), c), n = (l, c) => e(l) - c) : (t = e === vn || e === jM ? e : SM, r = e, n = e);
  function a(l, c, u = 0, d = l.length) {
    if (u < d) {
      if (t(c, c) !== 0) return d;
      do {
        const f = u + d >>> 1;
        r(l[f], c) < 0 ? u = f + 1 : d = f;
      } while (u < d);
    }
    return u;
  }
  function i(l, c, u = 0, d = l.length) {
    if (u < d) {
      if (t(c, c) !== 0) return d;
      do {
        const f = u + d >>> 1;
        r(l[f], c) <= 0 ? u = f + 1 : d = f;
      } while (u < d);
    }
    return u;
  }
  function o(l, c, u = 0, d = l.length) {
    const f = a(l, c, u, d - 1);
    return f > u && n(l[f - 1], c) > -n(l[f], c) ? f - 1 : f;
  }
  return { left: a, center: o, right: i };
}
function SM() {
  return 0;
}
function kS(e) {
  return e === null ? NaN : +e;
}
function* kM(e, t) {
  for (let r of e)
    r != null && (r = +r) >= r && (yield r);
}
const EM = wp(vn), To = EM.right;
wp(kS).center;
class E0 extends Map {
  constructor(t, r = TM) {
    if (super(), Object.defineProperties(this, { _intern: { value: /* @__PURE__ */ new Map() }, _key: { value: r } }), t != null) for (const [n, a] of t) this.set(n, a);
  }
  get(t) {
    return super.get(A0(this, t));
  }
  has(t) {
    return super.has(A0(this, t));
  }
  set(t, r) {
    return super.set(AM(this, t), r);
  }
  delete(t) {
    return super.delete(CM(this, t));
  }
}
function A0({ _intern: e, _key: t }, r) {
  const n = t(r);
  return e.has(n) ? e.get(n) : r;
}
function AM({ _intern: e, _key: t }, r) {
  const n = t(r);
  return e.has(n) ? e.get(n) : (e.set(n, r), r);
}
function CM({ _intern: e, _key: t }, r) {
  const n = t(r);
  return e.has(n) && (r = e.get(n), e.delete(n)), r;
}
function TM(e) {
  return e !== null && typeof e == "object" ? e.valueOf() : e;
}
function PM(e = vn) {
  if (e === vn) return ES;
  if (typeof e != "function") throw new TypeError("compare is not a function");
  return (t, r) => {
    const n = e(t, r);
    return n || n === 0 ? n : (e(r, r) === 0) - (e(t, t) === 0);
  };
}
function ES(e, t) {
  return (e == null || !(e >= e)) - (t == null || !(t >= t)) || (e < t ? -1 : e > t ? 1 : 0);
}
const OM = Math.sqrt(50), NM = Math.sqrt(10), MM = Math.sqrt(2);
function Cs(e, t, r) {
  const n = (t - e) / Math.max(0, r), a = Math.floor(Math.log10(n)), i = n / Math.pow(10, a), o = i >= OM ? 10 : i >= NM ? 5 : i >= MM ? 2 : 1;
  let l, c, u;
  return a < 0 ? (u = Math.pow(10, -a) / o, l = Math.round(e * u), c = Math.round(t * u), l / u < e && ++l, c / u > t && --c, u = -u) : (u = Math.pow(10, a) * o, l = Math.round(e / u), c = Math.round(t / u), l * u < e && ++l, c * u > t && --c), c < l && 0.5 <= r && r < 2 ? Cs(e, t, r * 2) : [l, c, u];
}
function Hm(e, t, r) {
  if (t = +t, e = +e, r = +r, !(r > 0)) return [];
  if (e === t) return [e];
  const n = t < e, [a, i, o] = n ? Cs(t, e, r) : Cs(e, t, r);
  if (!(i >= a)) return [];
  const l = i - a + 1, c = new Array(l);
  if (n)
    if (o < 0) for (let u = 0; u < l; ++u) c[u] = (i - u) / -o;
    else for (let u = 0; u < l; ++u) c[u] = (i - u) * o;
  else if (o < 0) for (let u = 0; u < l; ++u) c[u] = (a + u) / -o;
  else for (let u = 0; u < l; ++u) c[u] = (a + u) * o;
  return c;
}
function Gm(e, t, r) {
  return t = +t, e = +e, r = +r, Cs(e, t, r)[2];
}
function Km(e, t, r) {
  t = +t, e = +e, r = +r;
  const n = t < e, a = n ? Gm(t, e, r) : Gm(e, t, r);
  return (n ? -1 : 1) * (a < 0 ? 1 / -a : a);
}
function C0(e, t) {
  let r;
  for (const n of e)
    n != null && (r < n || r === void 0 && n >= n) && (r = n);
  return r;
}
function T0(e, t) {
  let r;
  for (const n of e)
    n != null && (r > n || r === void 0 && n >= n) && (r = n);
  return r;
}
function AS(e, t, r = 0, n = 1 / 0, a) {
  if (t = Math.floor(t), r = Math.floor(Math.max(0, r)), n = Math.floor(Math.min(e.length - 1, n)), !(r <= t && t <= n)) return e;
  for (a = a === void 0 ? ES : PM(a); n > r; ) {
    if (n - r > 600) {
      const c = n - r + 1, u = t - r + 1, d = Math.log(c), f = 0.5 * Math.exp(2 * d / 3), p = 0.5 * Math.sqrt(d * f * (c - f) / c) * (u - c / 2 < 0 ? -1 : 1), m = Math.max(r, Math.floor(t - u * f / c + p)), y = Math.min(n, Math.floor(t + (c - u) * f / c + p));
      AS(e, t, m, y, a);
    }
    const i = e[t];
    let o = r, l = n;
    for (xi(e, r, t), a(e[n], i) > 0 && xi(e, r, n); o < l; ) {
      for (xi(e, o, l), ++o, --l; a(e[o], i) < 0; ) ++o;
      for (; a(e[l], i) > 0; ) --l;
    }
    a(e[r], i) === 0 ? xi(e, r, l) : (++l, xi(e, l, n)), l <= t && (r = l + 1), t <= l && (n = l - 1);
  }
  return e;
}
function xi(e, t, r) {
  const n = e[t];
  e[t] = e[r], e[r] = n;
}
function RM(e, t, r) {
  if (e = Float64Array.from(kM(e)), !(!(n = e.length) || isNaN(t = +t))) {
    if (t <= 0 || n < 2) return T0(e);
    if (t >= 1) return C0(e);
    var n, a = (n - 1) * t, i = Math.floor(a), o = C0(AS(e, i).subarray(0, i + 1)), l = T0(e.subarray(i + 1));
    return o + (l - o) * (a - i);
  }
}
function IM(e, t, r = kS) {
  if (!(!(n = e.length) || isNaN(t = +t))) {
    if (t <= 0 || n < 2) return +r(e[0], 0, e);
    if (t >= 1) return +r(e[n - 1], n - 1, e);
    var n, a = (n - 1) * t, i = Math.floor(a), o = +r(e[i], i, e), l = +r(e[i + 1], i + 1, e);
    return o + (l - o) * (a - i);
  }
}
function DM(e, t, r) {
  e = +e, t = +t, r = (a = arguments.length) < 2 ? (t = e, e = 0, 1) : a < 3 ? 1 : +r;
  for (var n = -1, a = Math.max(0, Math.ceil((t - e) / r)) | 0, i = new Array(a); ++n < a; )
    i[n] = e + n * r;
  return i;
}
function br(e, t) {
  switch (arguments.length) {
    case 0:
      break;
    case 1:
      this.range(e);
      break;
    default:
      this.range(t).domain(e);
      break;
  }
  return this;
}
function on(e, t) {
  switch (arguments.length) {
    case 0:
      break;
    case 1: {
      typeof e == "function" ? this.interpolator(e) : this.range(e);
      break;
    }
    default: {
      this.domain(e), typeof t == "function" ? this.interpolator(t) : this.range(t);
      break;
    }
  }
  return this;
}
const Ym = Symbol("implicit");
function _p() {
  var e = new E0(), t = [], r = [], n = Ym;
  function a(i) {
    let o = e.get(i);
    if (o === void 0) {
      if (n !== Ym) return n;
      e.set(i, o = t.push(i) - 1);
    }
    return r[o % r.length];
  }
  return a.domain = function(i) {
    if (!arguments.length) return t.slice();
    t = [], e = new E0();
    for (const o of i)
      e.has(o) || e.set(o, t.push(o) - 1);
    return a;
  }, a.range = function(i) {
    return arguments.length ? (r = Array.from(i), a) : r.slice();
  }, a.unknown = function(i) {
    return arguments.length ? (n = i, a) : n;
  }, a.copy = function() {
    return _p(t, r).unknown(n);
  }, br.apply(a, arguments), a;
}
function Ki() {
  var e = _p().unknown(void 0), t = e.domain, r = e.range, n = 0, a = 1, i, o, l = !1, c = 0, u = 0, d = 0.5;
  delete e.unknown;
  function f() {
    var p = t().length, m = a < n, y = m ? a : n, h = m ? n : a;
    i = (h - y) / Math.max(1, p - c + u * 2), l && (i = Math.floor(i)), y += (h - y - i * (p - c)) * d, o = i * (1 - c), l && (y = Math.round(y), o = Math.round(o));
    var g = DM(p).map(function(v) {
      return y + i * v;
    });
    return r(m ? g.reverse() : g);
  }
  return e.domain = function(p) {
    return arguments.length ? (t(p), f()) : t();
  }, e.range = function(p) {
    return arguments.length ? ([n, a] = p, n = +n, a = +a, f()) : [n, a];
  }, e.rangeRound = function(p) {
    return [n, a] = p, n = +n, a = +a, l = !0, f();
  }, e.bandwidth = function() {
    return o;
  }, e.step = function() {
    return i;
  }, e.round = function(p) {
    return arguments.length ? (l = !!p, f()) : l;
  }, e.padding = function(p) {
    return arguments.length ? (c = Math.min(1, u = +p), f()) : c;
  }, e.paddingInner = function(p) {
    return arguments.length ? (c = Math.min(1, p), f()) : c;
  }, e.paddingOuter = function(p) {
    return arguments.length ? (u = +p, f()) : u;
  }, e.align = function(p) {
    return arguments.length ? (d = Math.max(0, Math.min(1, p)), f()) : d;
  }, e.copy = function() {
    return Ki(t(), [n, a]).round(l).paddingInner(c).paddingOuter(u).align(d);
  }, br.apply(f(), arguments);
}
function CS(e) {
  var t = e.copy;
  return e.padding = e.paddingOuter, delete e.paddingInner, delete e.paddingOuter, e.copy = function() {
    return CS(t());
  }, e;
}
function Mi() {
  return CS(Ki.apply(null, arguments).paddingInner(1));
}
function jp(e, t, r) {
  e.prototype = t.prototype = r, r.constructor = e;
}
function TS(e, t) {
  var r = Object.create(e.prototype);
  for (var n in t) r[n] = t[n];
  return r;
}
function Po() {
}
var Yi = 0.7, Ts = 1 / Yi, Sa = "\\s*([+-]?\\d+)\\s*", Xi = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)\\s*", Ir = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)%\\s*", $M = /^#([0-9a-f]{3,8})$/, LM = new RegExp(`^rgb\\(${Sa},${Sa},${Sa}\\)$`), FM = new RegExp(`^rgb\\(${Ir},${Ir},${Ir}\\)$`), qM = new RegExp(`^rgba\\(${Sa},${Sa},${Sa},${Xi}\\)$`), UM = new RegExp(`^rgba\\(${Ir},${Ir},${Ir},${Xi}\\)$`), zM = new RegExp(`^hsl\\(${Xi},${Ir},${Ir}\\)$`), BM = new RegExp(`^hsla\\(${Xi},${Ir},${Ir},${Xi}\\)$`), P0 = {
  aliceblue: 15792383,
  antiquewhite: 16444375,
  aqua: 65535,
  aquamarine: 8388564,
  azure: 15794175,
  beige: 16119260,
  bisque: 16770244,
  black: 0,
  blanchedalmond: 16772045,
  blue: 255,
  blueviolet: 9055202,
  brown: 10824234,
  burlywood: 14596231,
  cadetblue: 6266528,
  chartreuse: 8388352,
  chocolate: 13789470,
  coral: 16744272,
  cornflowerblue: 6591981,
  cornsilk: 16775388,
  crimson: 14423100,
  cyan: 65535,
  darkblue: 139,
  darkcyan: 35723,
  darkgoldenrod: 12092939,
  darkgray: 11119017,
  darkgreen: 25600,
  darkgrey: 11119017,
  darkkhaki: 12433259,
  darkmagenta: 9109643,
  darkolivegreen: 5597999,
  darkorange: 16747520,
  darkorchid: 10040012,
  darkred: 9109504,
  darksalmon: 15308410,
  darkseagreen: 9419919,
  darkslateblue: 4734347,
  darkslategray: 3100495,
  darkslategrey: 3100495,
  darkturquoise: 52945,
  darkviolet: 9699539,
  deeppink: 16716947,
  deepskyblue: 49151,
  dimgray: 6908265,
  dimgrey: 6908265,
  dodgerblue: 2003199,
  firebrick: 11674146,
  floralwhite: 16775920,
  forestgreen: 2263842,
  fuchsia: 16711935,
  gainsboro: 14474460,
  ghostwhite: 16316671,
  gold: 16766720,
  goldenrod: 14329120,
  gray: 8421504,
  green: 32768,
  greenyellow: 11403055,
  grey: 8421504,
  honeydew: 15794160,
  hotpink: 16738740,
  indianred: 13458524,
  indigo: 4915330,
  ivory: 16777200,
  khaki: 15787660,
  lavender: 15132410,
  lavenderblush: 16773365,
  lawngreen: 8190976,
  lemonchiffon: 16775885,
  lightblue: 11393254,
  lightcoral: 15761536,
  lightcyan: 14745599,
  lightgoldenrodyellow: 16448210,
  lightgray: 13882323,
  lightgreen: 9498256,
  lightgrey: 13882323,
  lightpink: 16758465,
  lightsalmon: 16752762,
  lightseagreen: 2142890,
  lightskyblue: 8900346,
  lightslategray: 7833753,
  lightslategrey: 7833753,
  lightsteelblue: 11584734,
  lightyellow: 16777184,
  lime: 65280,
  limegreen: 3329330,
  linen: 16445670,
  magenta: 16711935,
  maroon: 8388608,
  mediumaquamarine: 6737322,
  mediumblue: 205,
  mediumorchid: 12211667,
  mediumpurple: 9662683,
  mediumseagreen: 3978097,
  mediumslateblue: 8087790,
  mediumspringgreen: 64154,
  mediumturquoise: 4772300,
  mediumvioletred: 13047173,
  midnightblue: 1644912,
  mintcream: 16121850,
  mistyrose: 16770273,
  moccasin: 16770229,
  navajowhite: 16768685,
  navy: 128,
  oldlace: 16643558,
  olive: 8421376,
  olivedrab: 7048739,
  orange: 16753920,
  orangered: 16729344,
  orchid: 14315734,
  palegoldenrod: 15657130,
  palegreen: 10025880,
  paleturquoise: 11529966,
  palevioletred: 14381203,
  papayawhip: 16773077,
  peachpuff: 16767673,
  peru: 13468991,
  pink: 16761035,
  plum: 14524637,
  powderblue: 11591910,
  purple: 8388736,
  rebeccapurple: 6697881,
  red: 16711680,
  rosybrown: 12357519,
  royalblue: 4286945,
  saddlebrown: 9127187,
  salmon: 16416882,
  sandybrown: 16032864,
  seagreen: 3050327,
  seashell: 16774638,
  sienna: 10506797,
  silver: 12632256,
  skyblue: 8900331,
  slateblue: 6970061,
  slategray: 7372944,
  slategrey: 7372944,
  snow: 16775930,
  springgreen: 65407,
  steelblue: 4620980,
  tan: 13808780,
  teal: 32896,
  thistle: 14204888,
  tomato: 16737095,
  turquoise: 4251856,
  violet: 15631086,
  wheat: 16113331,
  white: 16777215,
  whitesmoke: 16119285,
  yellow: 16776960,
  yellowgreen: 10145074
};
jp(Po, Qi, {
  copy(e) {
    return Object.assign(new this.constructor(), this, e);
  },
  displayable() {
    return this.rgb().displayable();
  },
  hex: O0,
  // Deprecated! Use color.formatHex.
  formatHex: O0,
  formatHex8: WM,
  formatHsl: VM,
  formatRgb: N0,
  toString: N0
});
function O0() {
  return this.rgb().formatHex();
}
function WM() {
  return this.rgb().formatHex8();
}
function VM() {
  return PS(this).formatHsl();
}
function N0() {
  return this.rgb().formatRgb();
}
function Qi(e) {
  var t, r;
  return e = (e + "").trim().toLowerCase(), (t = $M.exec(e)) ? (r = t[1].length, t = parseInt(t[1], 16), r === 6 ? M0(t) : r === 3 ? new Kt(t >> 8 & 15 | t >> 4 & 240, t >> 4 & 15 | t & 240, (t & 15) << 4 | t & 15, 1) : r === 8 ? Zo(t >> 24 & 255, t >> 16 & 255, t >> 8 & 255, (t & 255) / 255) : r === 4 ? Zo(t >> 12 & 15 | t >> 8 & 240, t >> 8 & 15 | t >> 4 & 240, t >> 4 & 15 | t & 240, ((t & 15) << 4 | t & 15) / 255) : null) : (t = LM.exec(e)) ? new Kt(t[1], t[2], t[3], 1) : (t = FM.exec(e)) ? new Kt(t[1] * 255 / 100, t[2] * 255 / 100, t[3] * 255 / 100, 1) : (t = qM.exec(e)) ? Zo(t[1], t[2], t[3], t[4]) : (t = UM.exec(e)) ? Zo(t[1] * 255 / 100, t[2] * 255 / 100, t[3] * 255 / 100, t[4]) : (t = zM.exec(e)) ? D0(t[1], t[2] / 100, t[3] / 100, 1) : (t = BM.exec(e)) ? D0(t[1], t[2] / 100, t[3] / 100, t[4]) : P0.hasOwnProperty(e) ? M0(P0[e]) : e === "transparent" ? new Kt(NaN, NaN, NaN, 0) : null;
}
function M0(e) {
  return new Kt(e >> 16 & 255, e >> 8 & 255, e & 255, 1);
}
function Zo(e, t, r, n) {
  return n <= 0 && (e = t = r = NaN), new Kt(e, t, r, n);
}
function HM(e) {
  return e instanceof Po || (e = Qi(e)), e ? (e = e.rgb(), new Kt(e.r, e.g, e.b, e.opacity)) : new Kt();
}
function Xm(e, t, r, n) {
  return arguments.length === 1 ? HM(e) : new Kt(e, t, r, n ?? 1);
}
function Kt(e, t, r, n) {
  this.r = +e, this.g = +t, this.b = +r, this.opacity = +n;
}
jp(Kt, Xm, TS(Po, {
  brighter(e) {
    return e = e == null ? Ts : Math.pow(Ts, e), new Kt(this.r * e, this.g * e, this.b * e, this.opacity);
  },
  darker(e) {
    return e = e == null ? Yi : Math.pow(Yi, e), new Kt(this.r * e, this.g * e, this.b * e, this.opacity);
  },
  rgb() {
    return this;
  },
  clamp() {
    return new Kt(zn(this.r), zn(this.g), zn(this.b), Ps(this.opacity));
  },
  displayable() {
    return -0.5 <= this.r && this.r < 255.5 && -0.5 <= this.g && this.g < 255.5 && -0.5 <= this.b && this.b < 255.5 && 0 <= this.opacity && this.opacity <= 1;
  },
  hex: R0,
  // Deprecated! Use color.formatHex.
  formatHex: R0,
  formatHex8: GM,
  formatRgb: I0,
  toString: I0
}));
function R0() {
  return `#${Dn(this.r)}${Dn(this.g)}${Dn(this.b)}`;
}
function GM() {
  return `#${Dn(this.r)}${Dn(this.g)}${Dn(this.b)}${Dn((isNaN(this.opacity) ? 1 : this.opacity) * 255)}`;
}
function I0() {
  const e = Ps(this.opacity);
  return `${e === 1 ? "rgb(" : "rgba("}${zn(this.r)}, ${zn(this.g)}, ${zn(this.b)}${e === 1 ? ")" : `, ${e})`}`;
}
function Ps(e) {
  return isNaN(e) ? 1 : Math.max(0, Math.min(1, e));
}
function zn(e) {
  return Math.max(0, Math.min(255, Math.round(e) || 0));
}
function Dn(e) {
  return e = zn(e), (e < 16 ? "0" : "") + e.toString(16);
}
function D0(e, t, r, n) {
  return n <= 0 ? e = t = r = NaN : r <= 0 || r >= 1 ? e = t = NaN : t <= 0 && (e = NaN), new Sr(e, t, r, n);
}
function PS(e) {
  if (e instanceof Sr) return new Sr(e.h, e.s, e.l, e.opacity);
  if (e instanceof Po || (e = Qi(e)), !e) return new Sr();
  if (e instanceof Sr) return e;
  e = e.rgb();
  var t = e.r / 255, r = e.g / 255, n = e.b / 255, a = Math.min(t, r, n), i = Math.max(t, r, n), o = NaN, l = i - a, c = (i + a) / 2;
  return l ? (t === i ? o = (r - n) / l + (r < n) * 6 : r === i ? o = (n - t) / l + 2 : o = (t - r) / l + 4, l /= c < 0.5 ? i + a : 2 - i - a, o *= 60) : l = c > 0 && c < 1 ? 0 : o, new Sr(o, l, c, e.opacity);
}
function KM(e, t, r, n) {
  return arguments.length === 1 ? PS(e) : new Sr(e, t, r, n ?? 1);
}
function Sr(e, t, r, n) {
  this.h = +e, this.s = +t, this.l = +r, this.opacity = +n;
}
jp(Sr, KM, TS(Po, {
  brighter(e) {
    return e = e == null ? Ts : Math.pow(Ts, e), new Sr(this.h, this.s, this.l * e, this.opacity);
  },
  darker(e) {
    return e = e == null ? Yi : Math.pow(Yi, e), new Sr(this.h, this.s, this.l * e, this.opacity);
  },
  rgb() {
    var e = this.h % 360 + (this.h < 0) * 360, t = isNaN(e) || isNaN(this.s) ? 0 : this.s, r = this.l, n = r + (r < 0.5 ? r : 1 - r) * t, a = 2 * r - n;
    return new Kt(
      xf(e >= 240 ? e - 240 : e + 120, a, n),
      xf(e, a, n),
      xf(e < 120 ? e + 240 : e - 120, a, n),
      this.opacity
    );
  },
  clamp() {
    return new Sr($0(this.h), es(this.s), es(this.l), Ps(this.opacity));
  },
  displayable() {
    return (0 <= this.s && this.s <= 1 || isNaN(this.s)) && 0 <= this.l && this.l <= 1 && 0 <= this.opacity && this.opacity <= 1;
  },
  formatHsl() {
    const e = Ps(this.opacity);
    return `${e === 1 ? "hsl(" : "hsla("}${$0(this.h)}, ${es(this.s) * 100}%, ${es(this.l) * 100}%${e === 1 ? ")" : `, ${e})`}`;
  }
}));
function $0(e) {
  return e = (e || 0) % 360, e < 0 ? e + 360 : e;
}
function es(e) {
  return Math.max(0, Math.min(1, e || 0));
}
function xf(e, t, r) {
  return (e < 60 ? t + (r - t) * e / 60 : e < 180 ? r : e < 240 ? t + (r - t) * (240 - e) / 60 : t) * 255;
}
const Sp = (e) => () => e;
function YM(e, t) {
  return function(r) {
    return e + r * t;
  };
}
function XM(e, t, r) {
  return e = Math.pow(e, r), t = Math.pow(t, r) - e, r = 1 / r, function(n) {
    return Math.pow(e + n * t, r);
  };
}
function QM(e) {
  return (e = +e) == 1 ? OS : function(t, r) {
    return r - t ? XM(t, r, e) : Sp(isNaN(t) ? r : t);
  };
}
function OS(e, t) {
  var r = t - e;
  return r ? YM(e, r) : Sp(isNaN(e) ? t : e);
}
const L0 = (function e(t) {
  var r = QM(t);
  function n(a, i) {
    var o = r((a = Xm(a)).r, (i = Xm(i)).r), l = r(a.g, i.g), c = r(a.b, i.b), u = OS(a.opacity, i.opacity);
    return function(d) {
      return a.r = o(d), a.g = l(d), a.b = c(d), a.opacity = u(d), a + "";
    };
  }
  return n.gamma = e, n;
})(1);
function JM(e, t) {
  t || (t = []);
  var r = e ? Math.min(t.length, e.length) : 0, n = t.slice(), a;
  return function(i) {
    for (a = 0; a < r; ++a) n[a] = e[a] * (1 - i) + t[a] * i;
    return n;
  };
}
function ZM(e) {
  return ArrayBuffer.isView(e) && !(e instanceof DataView);
}
function eR(e, t) {
  var r = t ? t.length : 0, n = e ? Math.min(r, e.length) : 0, a = new Array(n), i = new Array(r), o;
  for (o = 0; o < n; ++o) a[o] = ai(e[o], t[o]);
  for (; o < r; ++o) i[o] = t[o];
  return function(l) {
    for (o = 0; o < n; ++o) i[o] = a[o](l);
    return i;
  };
}
function tR(e, t) {
  var r = /* @__PURE__ */ new Date();
  return e = +e, t = +t, function(n) {
    return r.setTime(e * (1 - n) + t * n), r;
  };
}
function Os(e, t) {
  return e = +e, t = +t, function(r) {
    return e * (1 - r) + t * r;
  };
}
function rR(e, t) {
  var r = {}, n = {}, a;
  (e === null || typeof e != "object") && (e = {}), (t === null || typeof t != "object") && (t = {});
  for (a in t)
    a in e ? r[a] = ai(e[a], t[a]) : n[a] = t[a];
  return function(i) {
    for (a in r) n[a] = r[a](i);
    return n;
  };
}
var Qm = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g, wf = new RegExp(Qm.source, "g");
function nR(e) {
  return function() {
    return e;
  };
}
function aR(e) {
  return function(t) {
    return e(t) + "";
  };
}
function iR(e, t) {
  var r = Qm.lastIndex = wf.lastIndex = 0, n, a, i, o = -1, l = [], c = [];
  for (e = e + "", t = t + ""; (n = Qm.exec(e)) && (a = wf.exec(t)); )
    (i = a.index) > r && (i = t.slice(r, i), l[o] ? l[o] += i : l[++o] = i), (n = n[0]) === (a = a[0]) ? l[o] ? l[o] += a : l[++o] = a : (l[++o] = null, c.push({ i: o, x: Os(n, a) })), r = wf.lastIndex;
  return r < t.length && (i = t.slice(r), l[o] ? l[o] += i : l[++o] = i), l.length < 2 ? c[0] ? aR(c[0].x) : nR(t) : (t = c.length, function(u) {
    for (var d = 0, f; d < t; ++d) l[(f = c[d]).i] = f.x(u);
    return l.join("");
  });
}
function ai(e, t) {
  var r = typeof t, n;
  return t == null || r === "boolean" ? Sp(t) : (r === "number" ? Os : r === "string" ? (n = Qi(t)) ? (t = n, L0) : iR : t instanceof Qi ? L0 : t instanceof Date ? tR : ZM(t) ? JM : Array.isArray(t) ? eR : typeof t.valueOf != "function" && typeof t.toString != "function" || isNaN(t) ? rR : Os)(e, t);
}
function kp(e, t) {
  return e = +e, t = +t, function(r) {
    return Math.round(e * (1 - r) + t * r);
  };
}
function oR(e, t) {
  t === void 0 && (t = e, e = ai);
  for (var r = 0, n = t.length - 1, a = t[0], i = new Array(n < 0 ? 0 : n); r < n; ) i[r] = e(a, a = t[++r]);
  return function(o) {
    var l = Math.max(0, Math.min(n - 1, Math.floor(o *= n)));
    return i[l](o - l);
  };
}
function sR(e) {
  return function() {
    return e;
  };
}
function Ns(e) {
  return +e;
}
var F0 = [0, 1];
function Wt(e) {
  return e;
}
function Jm(e, t) {
  return (t -= e = +e) ? function(r) {
    return (r - e) / t;
  } : sR(isNaN(t) ? NaN : 0.5);
}
function lR(e, t) {
  var r;
  return e > t && (r = e, e = t, t = r), function(n) {
    return Math.max(e, Math.min(t, n));
  };
}
function cR(e, t, r) {
  var n = e[0], a = e[1], i = t[0], o = t[1];
  return a < n ? (n = Jm(a, n), i = r(o, i)) : (n = Jm(n, a), i = r(i, o)), function(l) {
    return i(n(l));
  };
}
function uR(e, t, r) {
  var n = Math.min(e.length, t.length) - 1, a = new Array(n), i = new Array(n), o = -1;
  for (e[n] < e[0] && (e = e.slice().reverse(), t = t.slice().reverse()); ++o < n; )
    a[o] = Jm(e[o], e[o + 1]), i[o] = r(t[o], t[o + 1]);
  return function(l) {
    var c = To(e, l, 1, n) - 1;
    return i[c](a[c](l));
  };
}
function Oo(e, t) {
  return t.domain(e.domain()).range(e.range()).interpolate(e.interpolate()).clamp(e.clamp()).unknown(e.unknown());
}
function Il() {
  var e = F0, t = F0, r = ai, n, a, i, o = Wt, l, c, u;
  function d() {
    var p = Math.min(e.length, t.length);
    return o !== Wt && (o = lR(e[0], e[p - 1])), l = p > 2 ? uR : cR, c = u = null, f;
  }
  function f(p) {
    return p == null || isNaN(p = +p) ? i : (c || (c = l(e.map(n), t, r)))(n(o(p)));
  }
  return f.invert = function(p) {
    return o(a((u || (u = l(t, e.map(n), Os)))(p)));
  }, f.domain = function(p) {
    return arguments.length ? (e = Array.from(p, Ns), d()) : e.slice();
  }, f.range = function(p) {
    return arguments.length ? (t = Array.from(p), d()) : t.slice();
  }, f.rangeRound = function(p) {
    return t = Array.from(p), r = kp, d();
  }, f.clamp = function(p) {
    return arguments.length ? (o = p ? !0 : Wt, d()) : o !== Wt;
  }, f.interpolate = function(p) {
    return arguments.length ? (r = p, d()) : r;
  }, f.unknown = function(p) {
    return arguments.length ? (i = p, f) : i;
  }, function(p, m) {
    return n = p, a = m, d();
  };
}
function Ep() {
  return Il()(Wt, Wt);
}
function dR(e) {
  return Math.abs(e = Math.round(e)) >= 1e21 ? e.toLocaleString("en").replace(/,/g, "") : e.toString(10);
}
function Ms(e, t) {
  if ((r = (e = t ? e.toExponential(t - 1) : e.toExponential()).indexOf("e")) < 0) return null;
  var r, n = e.slice(0, r);
  return [
    n.length > 1 ? n[0] + n.slice(2) : n,
    +e.slice(r + 1)
  ];
}
function Oa(e) {
  return e = Ms(Math.abs(e)), e ? e[1] : NaN;
}
function fR(e, t) {
  return function(r, n) {
    for (var a = r.length, i = [], o = 0, l = e[0], c = 0; a > 0 && l > 0 && (c + l + 1 > n && (l = Math.max(1, n - c)), i.push(r.substring(a -= l, a + l)), !((c += l + 1) > n)); )
      l = e[o = (o + 1) % e.length];
    return i.reverse().join(t);
  };
}
function mR(e) {
  return function(t) {
    return t.replace(/[0-9]/g, function(r) {
      return e[+r];
    });
  };
}
var hR = /^(?:(.)?([<>=^]))?([+\-( ])?([$#])?(0)?(\d+)?(,)?(\.\d+)?(~)?([a-z%])?$/i;
function Ji(e) {
  if (!(t = hR.exec(e))) throw new Error("invalid format: " + e);
  var t;
  return new Ap({
    fill: t[1],
    align: t[2],
    sign: t[3],
    symbol: t[4],
    zero: t[5],
    width: t[6],
    comma: t[7],
    precision: t[8] && t[8].slice(1),
    trim: t[9],
    type: t[10]
  });
}
Ji.prototype = Ap.prototype;
function Ap(e) {
  this.fill = e.fill === void 0 ? " " : e.fill + "", this.align = e.align === void 0 ? ">" : e.align + "", this.sign = e.sign === void 0 ? "-" : e.sign + "", this.symbol = e.symbol === void 0 ? "" : e.symbol + "", this.zero = !!e.zero, this.width = e.width === void 0 ? void 0 : +e.width, this.comma = !!e.comma, this.precision = e.precision === void 0 ? void 0 : +e.precision, this.trim = !!e.trim, this.type = e.type === void 0 ? "" : e.type + "";
}
Ap.prototype.toString = function() {
  return this.fill + this.align + this.sign + this.symbol + (this.zero ? "0" : "") + (this.width === void 0 ? "" : Math.max(1, this.width | 0)) + (this.comma ? "," : "") + (this.precision === void 0 ? "" : "." + Math.max(0, this.precision | 0)) + (this.trim ? "~" : "") + this.type;
};
function pR(e) {
  e: for (var t = e.length, r = 1, n = -1, a; r < t; ++r)
    switch (e[r]) {
      case ".":
        n = a = r;
        break;
      case "0":
        n === 0 && (n = r), a = r;
        break;
      default:
        if (!+e[r]) break e;
        n > 0 && (n = 0);
        break;
    }
  return n > 0 ? e.slice(0, n) + e.slice(a + 1) : e;
}
var NS;
function gR(e, t) {
  var r = Ms(e, t);
  if (!r) return e + "";
  var n = r[0], a = r[1], i = a - (NS = Math.max(-8, Math.min(8, Math.floor(a / 3))) * 3) + 1, o = n.length;
  return i === o ? n : i > o ? n + new Array(i - o + 1).join("0") : i > 0 ? n.slice(0, i) + "." + n.slice(i) : "0." + new Array(1 - i).join("0") + Ms(e, Math.max(0, t + i - 1))[0];
}
function q0(e, t) {
  var r = Ms(e, t);
  if (!r) return e + "";
  var n = r[0], a = r[1];
  return a < 0 ? "0." + new Array(-a).join("0") + n : n.length > a + 1 ? n.slice(0, a + 1) + "." + n.slice(a + 1) : n + new Array(a - n.length + 2).join("0");
}
const U0 = {
  "%": (e, t) => (e * 100).toFixed(t),
  b: (e) => Math.round(e).toString(2),
  c: (e) => e + "",
  d: dR,
  e: (e, t) => e.toExponential(t),
  f: (e, t) => e.toFixed(t),
  g: (e, t) => e.toPrecision(t),
  o: (e) => Math.round(e).toString(8),
  p: (e, t) => q0(e * 100, t),
  r: q0,
  s: gR,
  X: (e) => Math.round(e).toString(16).toUpperCase(),
  x: (e) => Math.round(e).toString(16)
};
function z0(e) {
  return e;
}
var B0 = Array.prototype.map, W0 = ["y", "z", "a", "f", "p", "n", "µ", "m", "", "k", "M", "G", "T", "P", "E", "Z", "Y"];
function yR(e) {
  var t = e.grouping === void 0 || e.thousands === void 0 ? z0 : fR(B0.call(e.grouping, Number), e.thousands + ""), r = e.currency === void 0 ? "" : e.currency[0] + "", n = e.currency === void 0 ? "" : e.currency[1] + "", a = e.decimal === void 0 ? "." : e.decimal + "", i = e.numerals === void 0 ? z0 : mR(B0.call(e.numerals, String)), o = e.percent === void 0 ? "%" : e.percent + "", l = e.minus === void 0 ? "−" : e.minus + "", c = e.nan === void 0 ? "NaN" : e.nan + "";
  function u(f) {
    f = Ji(f);
    var p = f.fill, m = f.align, y = f.sign, h = f.symbol, g = f.zero, v = f.width, b = f.comma, w = f.precision, j = f.trim, x = f.type;
    x === "n" ? (b = !0, x = "g") : U0[x] || (w === void 0 && (w = 12), j = !0, x = "g"), (g || p === "0" && m === "=") && (g = !0, p = "0", m = "=");
    var _ = h === "$" ? r : h === "#" && /[boxX]/.test(x) ? "0" + x.toLowerCase() : "", S = h === "$" ? n : /[%p]/.test(x) ? o : "", k = U0[x], E = /[defgprs%]/.test(x);
    w = w === void 0 ? 6 : /[gprs]/.test(x) ? Math.max(1, Math.min(21, w)) : Math.max(0, Math.min(20, w));
    function L(P) {
      var A = _, T = S, M, N, q;
      if (x === "c")
        T = k(P) + T, P = "";
      else {
        P = +P;
        var V = P < 0 || 1 / P < 0;
        if (P = isNaN(P) ? c : k(Math.abs(P), w), j && (P = pR(P)), V && +P == 0 && y !== "+" && (V = !1), A = (V ? y === "(" ? y : l : y === "-" || y === "(" ? "" : y) + A, T = (x === "s" ? W0[8 + NS / 3] : "") + T + (V && y === "(" ? ")" : ""), E) {
          for (M = -1, N = P.length; ++M < N; )
            if (q = P.charCodeAt(M), 48 > q || q > 57) {
              T = (q === 46 ? a + P.slice(M + 1) : P.slice(M)) + T, P = P.slice(0, M);
              break;
            }
        }
      }
      b && !g && (P = t(P, 1 / 0));
      var B = A.length + P.length + T.length, ee = B < v ? new Array(v - B + 1).join(p) : "";
      switch (b && g && (P = t(ee + P, ee.length ? v - T.length : 1 / 0), ee = ""), m) {
        case "<":
          P = A + P + T + ee;
          break;
        case "=":
          P = A + ee + P + T;
          break;
        case "^":
          P = ee.slice(0, B = ee.length >> 1) + A + P + T + ee.slice(B);
          break;
        default:
          P = ee + A + P + T;
          break;
      }
      return i(P);
    }
    return L.toString = function() {
      return f + "";
    }, L;
  }
  function d(f, p) {
    var m = u((f = Ji(f), f.type = "f", f)), y = Math.max(-8, Math.min(8, Math.floor(Oa(p) / 3))) * 3, h = Math.pow(10, -y), g = W0[8 + y / 3];
    return function(v) {
      return m(h * v) + g;
    };
  }
  return {
    format: u,
    formatPrefix: d
  };
}
var ts, Cp, MS;
vR({
  thousands: ",",
  grouping: [3],
  currency: ["$", ""]
});
function vR(e) {
  return ts = yR(e), Cp = ts.format, MS = ts.formatPrefix, ts;
}
function bR(e) {
  return Math.max(0, -Oa(Math.abs(e)));
}
function xR(e, t) {
  return Math.max(0, Math.max(-8, Math.min(8, Math.floor(Oa(t) / 3))) * 3 - Oa(Math.abs(e)));
}
function wR(e, t) {
  return e = Math.abs(e), t = Math.abs(t) - e, Math.max(0, Oa(t) - Oa(e)) + 1;
}
function RS(e, t, r, n) {
  var a = Km(e, t, r), i;
  switch (n = Ji(n ?? ",f"), n.type) {
    case "s": {
      var o = Math.max(Math.abs(e), Math.abs(t));
      return n.precision == null && !isNaN(i = xR(a, o)) && (n.precision = i), MS(n, o);
    }
    case "":
    case "e":
    case "g":
    case "p":
    case "r": {
      n.precision == null && !isNaN(i = wR(a, Math.max(Math.abs(e), Math.abs(t)))) && (n.precision = i - (n.type === "e"));
      break;
    }
    case "f":
    case "%": {
      n.precision == null && !isNaN(i = bR(a)) && (n.precision = i - (n.type === "%") * 2);
      break;
    }
  }
  return Cp(n);
}
function Sn(e) {
  var t = e.domain;
  return e.ticks = function(r) {
    var n = t();
    return Hm(n[0], n[n.length - 1], r ?? 10);
  }, e.tickFormat = function(r, n) {
    var a = t();
    return RS(a[0], a[a.length - 1], r ?? 10, n);
  }, e.nice = function(r) {
    r == null && (r = 10);
    var n = t(), a = 0, i = n.length - 1, o = n[a], l = n[i], c, u, d = 10;
    for (l < o && (u = o, o = l, l = u, u = a, a = i, i = u); d-- > 0; ) {
      if (u = Gm(o, l, r), u === c)
        return n[a] = o, n[i] = l, t(n);
      if (u > 0)
        o = Math.floor(o / u) * u, l = Math.ceil(l / u) * u;
      else if (u < 0)
        o = Math.ceil(o * u) / u, l = Math.floor(l * u) / u;
      else
        break;
      c = u;
    }
    return e;
  }, e;
}
function Rs() {
  var e = Ep();
  return e.copy = function() {
    return Oo(e, Rs());
  }, br.apply(e, arguments), Sn(e);
}
function IS(e) {
  var t;
  function r(n) {
    return n == null || isNaN(n = +n) ? t : n;
  }
  return r.invert = r, r.domain = r.range = function(n) {
    return arguments.length ? (e = Array.from(n, Ns), r) : e.slice();
  }, r.unknown = function(n) {
    return arguments.length ? (t = n, r) : t;
  }, r.copy = function() {
    return IS(e).unknown(t);
  }, e = arguments.length ? Array.from(e, Ns) : [0, 1], Sn(r);
}
function DS(e, t) {
  e = e.slice();
  var r = 0, n = e.length - 1, a = e[r], i = e[n], o;
  return i < a && (o = r, r = n, n = o, o = a, a = i, i = o), e[r] = t.floor(a), e[n] = t.ceil(i), e;
}
function V0(e) {
  return Math.log(e);
}
function H0(e) {
  return Math.exp(e);
}
function _R(e) {
  return -Math.log(-e);
}
function jR(e) {
  return -Math.exp(-e);
}
function SR(e) {
  return isFinite(e) ? +("1e" + e) : e < 0 ? 0 : e;
}
function kR(e) {
  return e === 10 ? SR : e === Math.E ? Math.exp : (t) => Math.pow(e, t);
}
function ER(e) {
  return e === Math.E ? Math.log : e === 10 && Math.log10 || e === 2 && Math.log2 || (e = Math.log(e), (t) => Math.log(t) / e);
}
function G0(e) {
  return (t, r) => -e(-t, r);
}
function Tp(e) {
  const t = e(V0, H0), r = t.domain;
  let n = 10, a, i;
  function o() {
    return a = ER(n), i = kR(n), r()[0] < 0 ? (a = G0(a), i = G0(i), e(_R, jR)) : e(V0, H0), t;
  }
  return t.base = function(l) {
    return arguments.length ? (n = +l, o()) : n;
  }, t.domain = function(l) {
    return arguments.length ? (r(l), o()) : r();
  }, t.ticks = (l) => {
    const c = r();
    let u = c[0], d = c[c.length - 1];
    const f = d < u;
    f && ([u, d] = [d, u]);
    let p = a(u), m = a(d), y, h;
    const g = l == null ? 10 : +l;
    let v = [];
    if (!(n % 1) && m - p < g) {
      if (p = Math.floor(p), m = Math.ceil(m), u > 0) {
        for (; p <= m; ++p)
          for (y = 1; y < n; ++y)
            if (h = p < 0 ? y / i(-p) : y * i(p), !(h < u)) {
              if (h > d) break;
              v.push(h);
            }
      } else for (; p <= m; ++p)
        for (y = n - 1; y >= 1; --y)
          if (h = p > 0 ? y / i(-p) : y * i(p), !(h < u)) {
            if (h > d) break;
            v.push(h);
          }
      v.length * 2 < g && (v = Hm(u, d, g));
    } else
      v = Hm(p, m, Math.min(m - p, g)).map(i);
    return f ? v.reverse() : v;
  }, t.tickFormat = (l, c) => {
    if (l == null && (l = 10), c == null && (c = n === 10 ? "s" : ","), typeof c != "function" && (!(n % 1) && (c = Ji(c)).precision == null && (c.trim = !0), c = Cp(c)), l === 1 / 0) return c;
    const u = Math.max(1, n * l / t.ticks().length);
    return (d) => {
      let f = d / i(Math.round(a(d)));
      return f * n < n - 0.5 && (f *= n), f <= u ? c(d) : "";
    };
  }, t.nice = () => r(DS(r(), {
    floor: (l) => i(Math.floor(a(l))),
    ceil: (l) => i(Math.ceil(a(l)))
  })), t;
}
function $S() {
  const e = Tp(Il()).domain([1, 10]);
  return e.copy = () => Oo(e, $S()).base(e.base()), br.apply(e, arguments), e;
}
function K0(e) {
  return function(t) {
    return Math.sign(t) * Math.log1p(Math.abs(t / e));
  };
}
function Y0(e) {
  return function(t) {
    return Math.sign(t) * Math.expm1(Math.abs(t)) * e;
  };
}
function Pp(e) {
  var t = 1, r = e(K0(t), Y0(t));
  return r.constant = function(n) {
    return arguments.length ? e(K0(t = +n), Y0(t)) : t;
  }, Sn(r);
}
function LS() {
  var e = Pp(Il());
  return e.copy = function() {
    return Oo(e, LS()).constant(e.constant());
  }, br.apply(e, arguments);
}
function X0(e) {
  return function(t) {
    return t < 0 ? -Math.pow(-t, e) : Math.pow(t, e);
  };
}
function AR(e) {
  return e < 0 ? -Math.sqrt(-e) : Math.sqrt(e);
}
function CR(e) {
  return e < 0 ? -e * e : e * e;
}
function Op(e) {
  var t = e(Wt, Wt), r = 1;
  function n() {
    return r === 1 ? e(Wt, Wt) : r === 0.5 ? e(AR, CR) : e(X0(r), X0(1 / r));
  }
  return t.exponent = function(a) {
    return arguments.length ? (r = +a, n()) : r;
  }, Sn(t);
}
function Np() {
  var e = Op(Il());
  return e.copy = function() {
    return Oo(e, Np()).exponent(e.exponent());
  }, br.apply(e, arguments), e;
}
function TR() {
  return Np.apply(null, arguments).exponent(0.5);
}
function Q0(e) {
  return Math.sign(e) * e * e;
}
function PR(e) {
  return Math.sign(e) * Math.sqrt(Math.abs(e));
}
function FS() {
  var e = Ep(), t = [0, 1], r = !1, n;
  function a(i) {
    var o = PR(e(i));
    return isNaN(o) ? n : r ? Math.round(o) : o;
  }
  return a.invert = function(i) {
    return e.invert(Q0(i));
  }, a.domain = function(i) {
    return arguments.length ? (e.domain(i), a) : e.domain();
  }, a.range = function(i) {
    return arguments.length ? (e.range((t = Array.from(i, Ns)).map(Q0)), a) : t.slice();
  }, a.rangeRound = function(i) {
    return a.range(i).round(!0);
  }, a.round = function(i) {
    return arguments.length ? (r = !!i, a) : r;
  }, a.clamp = function(i) {
    return arguments.length ? (e.clamp(i), a) : e.clamp();
  }, a.unknown = function(i) {
    return arguments.length ? (n = i, a) : n;
  }, a.copy = function() {
    return FS(e.domain(), t).round(r).clamp(e.clamp()).unknown(n);
  }, br.apply(a, arguments), Sn(a);
}
function qS() {
  var e = [], t = [], r = [], n;
  function a() {
    var o = 0, l = Math.max(1, t.length);
    for (r = new Array(l - 1); ++o < l; ) r[o - 1] = IM(e, o / l);
    return i;
  }
  function i(o) {
    return o == null || isNaN(o = +o) ? n : t[To(r, o)];
  }
  return i.invertExtent = function(o) {
    var l = t.indexOf(o);
    return l < 0 ? [NaN, NaN] : [
      l > 0 ? r[l - 1] : e[0],
      l < r.length ? r[l] : e[e.length - 1]
    ];
  }, i.domain = function(o) {
    if (!arguments.length) return e.slice();
    e = [];
    for (let l of o) l != null && !isNaN(l = +l) && e.push(l);
    return e.sort(vn), a();
  }, i.range = function(o) {
    return arguments.length ? (t = Array.from(o), a()) : t.slice();
  }, i.unknown = function(o) {
    return arguments.length ? (n = o, i) : n;
  }, i.quantiles = function() {
    return r.slice();
  }, i.copy = function() {
    return qS().domain(e).range(t).unknown(n);
  }, br.apply(i, arguments);
}
function US() {
  var e = 0, t = 1, r = 1, n = [0.5], a = [0, 1], i;
  function o(c) {
    return c != null && c <= c ? a[To(n, c, 0, r)] : i;
  }
  function l() {
    var c = -1;
    for (n = new Array(r); ++c < r; ) n[c] = ((c + 1) * t - (c - r) * e) / (r + 1);
    return o;
  }
  return o.domain = function(c) {
    return arguments.length ? ([e, t] = c, e = +e, t = +t, l()) : [e, t];
  }, o.range = function(c) {
    return arguments.length ? (r = (a = Array.from(c)).length - 1, l()) : a.slice();
  }, o.invertExtent = function(c) {
    var u = a.indexOf(c);
    return u < 0 ? [NaN, NaN] : u < 1 ? [e, n[0]] : u >= r ? [n[r - 1], t] : [n[u - 1], n[u]];
  }, o.unknown = function(c) {
    return arguments.length && (i = c), o;
  }, o.thresholds = function() {
    return n.slice();
  }, o.copy = function() {
    return US().domain([e, t]).range(a).unknown(i);
  }, br.apply(Sn(o), arguments);
}
function zS() {
  var e = [0.5], t = [0, 1], r, n = 1;
  function a(i) {
    return i != null && i <= i ? t[To(e, i, 0, n)] : r;
  }
  return a.domain = function(i) {
    return arguments.length ? (e = Array.from(i), n = Math.min(e.length, t.length - 1), a) : e.slice();
  }, a.range = function(i) {
    return arguments.length ? (t = Array.from(i), n = Math.min(e.length, t.length - 1), a) : t.slice();
  }, a.invertExtent = function(i) {
    var o = t.indexOf(i);
    return [e[o - 1], e[o]];
  }, a.unknown = function(i) {
    return arguments.length ? (r = i, a) : r;
  }, a.copy = function() {
    return zS().domain(e).range(t).unknown(r);
  }, br.apply(a, arguments);
}
const _f = /* @__PURE__ */ new Date(), jf = /* @__PURE__ */ new Date();
function Ot(e, t, r, n) {
  function a(i) {
    return e(i = arguments.length === 0 ? /* @__PURE__ */ new Date() : /* @__PURE__ */ new Date(+i)), i;
  }
  return a.floor = (i) => (e(i = /* @__PURE__ */ new Date(+i)), i), a.ceil = (i) => (e(i = new Date(i - 1)), t(i, 1), e(i), i), a.round = (i) => {
    const o = a(i), l = a.ceil(i);
    return i - o < l - i ? o : l;
  }, a.offset = (i, o) => (t(i = /* @__PURE__ */ new Date(+i), o == null ? 1 : Math.floor(o)), i), a.range = (i, o, l) => {
    const c = [];
    if (i = a.ceil(i), l = l == null ? 1 : Math.floor(l), !(i < o) || !(l > 0)) return c;
    let u;
    do
      c.push(u = /* @__PURE__ */ new Date(+i)), t(i, l), e(i);
    while (u < i && i < o);
    return c;
  }, a.filter = (i) => Ot((o) => {
    if (o >= o) for (; e(o), !i(o); ) o.setTime(o - 1);
  }, (o, l) => {
    if (o >= o)
      if (l < 0) for (; ++l <= 0; )
        for (; t(o, -1), !i(o); )
          ;
      else for (; --l >= 0; )
        for (; t(o, 1), !i(o); )
          ;
  }), r && (a.count = (i, o) => (_f.setTime(+i), jf.setTime(+o), e(_f), e(jf), Math.floor(r(_f, jf))), a.every = (i) => (i = Math.floor(i), !isFinite(i) || !(i > 0) ? null : i > 1 ? a.filter(n ? (o) => n(o) % i === 0 : (o) => a.count(0, o) % i === 0) : a)), a;
}
const Is = Ot(() => {
}, (e, t) => {
  e.setTime(+e + t);
}, (e, t) => t - e);
Is.every = (e) => (e = Math.floor(e), !isFinite(e) || !(e > 0) ? null : e > 1 ? Ot((t) => {
  t.setTime(Math.floor(t / e) * e);
}, (t, r) => {
  t.setTime(+t + r * e);
}, (t, r) => (r - t) / e) : Is);
Is.range;
const Hr = 1e3, hr = Hr * 60, Gr = hr * 60, Jr = Gr * 24, Mp = Jr * 7, J0 = Jr * 30, Sf = Jr * 365, $n = Ot((e) => {
  e.setTime(e - e.getMilliseconds());
}, (e, t) => {
  e.setTime(+e + t * Hr);
}, (e, t) => (t - e) / Hr, (e) => e.getUTCSeconds());
$n.range;
const Rp = Ot((e) => {
  e.setTime(e - e.getMilliseconds() - e.getSeconds() * Hr);
}, (e, t) => {
  e.setTime(+e + t * hr);
}, (e, t) => (t - e) / hr, (e) => e.getMinutes());
Rp.range;
const Ip = Ot((e) => {
  e.setUTCSeconds(0, 0);
}, (e, t) => {
  e.setTime(+e + t * hr);
}, (e, t) => (t - e) / hr, (e) => e.getUTCMinutes());
Ip.range;
const Dp = Ot((e) => {
  e.setTime(e - e.getMilliseconds() - e.getSeconds() * Hr - e.getMinutes() * hr);
}, (e, t) => {
  e.setTime(+e + t * Gr);
}, (e, t) => (t - e) / Gr, (e) => e.getHours());
Dp.range;
const $p = Ot((e) => {
  e.setUTCMinutes(0, 0, 0);
}, (e, t) => {
  e.setTime(+e + t * Gr);
}, (e, t) => (t - e) / Gr, (e) => e.getUTCHours());
$p.range;
const No = Ot(
  (e) => e.setHours(0, 0, 0, 0),
  (e, t) => e.setDate(e.getDate() + t),
  (e, t) => (t - e - (t.getTimezoneOffset() - e.getTimezoneOffset()) * hr) / Jr,
  (e) => e.getDate() - 1
);
No.range;
const Dl = Ot((e) => {
  e.setUTCHours(0, 0, 0, 0);
}, (e, t) => {
  e.setUTCDate(e.getUTCDate() + t);
}, (e, t) => (t - e) / Jr, (e) => e.getUTCDate() - 1);
Dl.range;
const BS = Ot((e) => {
  e.setUTCHours(0, 0, 0, 0);
}, (e, t) => {
  e.setUTCDate(e.getUTCDate() + t);
}, (e, t) => (t - e) / Jr, (e) => Math.floor(e / Jr));
BS.range;
function Xn(e) {
  return Ot((t) => {
    t.setDate(t.getDate() - (t.getDay() + 7 - e) % 7), t.setHours(0, 0, 0, 0);
  }, (t, r) => {
    t.setDate(t.getDate() + r * 7);
  }, (t, r) => (r - t - (r.getTimezoneOffset() - t.getTimezoneOffset()) * hr) / Mp);
}
const $l = Xn(0), Ds = Xn(1), OR = Xn(2), NR = Xn(3), Na = Xn(4), MR = Xn(5), RR = Xn(6);
$l.range;
Ds.range;
OR.range;
NR.range;
Na.range;
MR.range;
RR.range;
function Qn(e) {
  return Ot((t) => {
    t.setUTCDate(t.getUTCDate() - (t.getUTCDay() + 7 - e) % 7), t.setUTCHours(0, 0, 0, 0);
  }, (t, r) => {
    t.setUTCDate(t.getUTCDate() + r * 7);
  }, (t, r) => (r - t) / Mp);
}
const Ll = Qn(0), $s = Qn(1), IR = Qn(2), DR = Qn(3), Ma = Qn(4), $R = Qn(5), LR = Qn(6);
Ll.range;
$s.range;
IR.range;
DR.range;
Ma.range;
$R.range;
LR.range;
const Lp = Ot((e) => {
  e.setDate(1), e.setHours(0, 0, 0, 0);
}, (e, t) => {
  e.setMonth(e.getMonth() + t);
}, (e, t) => t.getMonth() - e.getMonth() + (t.getFullYear() - e.getFullYear()) * 12, (e) => e.getMonth());
Lp.range;
const Fp = Ot((e) => {
  e.setUTCDate(1), e.setUTCHours(0, 0, 0, 0);
}, (e, t) => {
  e.setUTCMonth(e.getUTCMonth() + t);
}, (e, t) => t.getUTCMonth() - e.getUTCMonth() + (t.getUTCFullYear() - e.getUTCFullYear()) * 12, (e) => e.getUTCMonth());
Fp.range;
const Zr = Ot((e) => {
  e.setMonth(0, 1), e.setHours(0, 0, 0, 0);
}, (e, t) => {
  e.setFullYear(e.getFullYear() + t);
}, (e, t) => t.getFullYear() - e.getFullYear(), (e) => e.getFullYear());
Zr.every = (e) => !isFinite(e = Math.floor(e)) || !(e > 0) ? null : Ot((t) => {
  t.setFullYear(Math.floor(t.getFullYear() / e) * e), t.setMonth(0, 1), t.setHours(0, 0, 0, 0);
}, (t, r) => {
  t.setFullYear(t.getFullYear() + r * e);
});
Zr.range;
const en = Ot((e) => {
  e.setUTCMonth(0, 1), e.setUTCHours(0, 0, 0, 0);
}, (e, t) => {
  e.setUTCFullYear(e.getUTCFullYear() + t);
}, (e, t) => t.getUTCFullYear() - e.getUTCFullYear(), (e) => e.getUTCFullYear());
en.every = (e) => !isFinite(e = Math.floor(e)) || !(e > 0) ? null : Ot((t) => {
  t.setUTCFullYear(Math.floor(t.getUTCFullYear() / e) * e), t.setUTCMonth(0, 1), t.setUTCHours(0, 0, 0, 0);
}, (t, r) => {
  t.setUTCFullYear(t.getUTCFullYear() + r * e);
});
en.range;
function WS(e, t, r, n, a, i) {
  const o = [
    [$n, 1, Hr],
    [$n, 5, 5 * Hr],
    [$n, 15, 15 * Hr],
    [$n, 30, 30 * Hr],
    [i, 1, hr],
    [i, 5, 5 * hr],
    [i, 15, 15 * hr],
    [i, 30, 30 * hr],
    [a, 1, Gr],
    [a, 3, 3 * Gr],
    [a, 6, 6 * Gr],
    [a, 12, 12 * Gr],
    [n, 1, Jr],
    [n, 2, 2 * Jr],
    [r, 1, Mp],
    [t, 1, J0],
    [t, 3, 3 * J0],
    [e, 1, Sf]
  ];
  function l(u, d, f) {
    const p = d < u;
    p && ([u, d] = [d, u]);
    const m = f && typeof f.range == "function" ? f : c(u, d, f), y = m ? m.range(u, +d + 1) : [];
    return p ? y.reverse() : y;
  }
  function c(u, d, f) {
    const p = Math.abs(d - u) / f, m = wp(([, , g]) => g).right(o, p);
    if (m === o.length) return e.every(Km(u / Sf, d / Sf, f));
    if (m === 0) return Is.every(Math.max(Km(u, d, f), 1));
    const [y, h] = o[p / o[m - 1][2] < o[m][2] / p ? m - 1 : m];
    return y.every(h);
  }
  return [l, c];
}
const [FR, qR] = WS(en, Fp, Ll, BS, $p, Ip), [UR, zR] = WS(Zr, Lp, $l, No, Dp, Rp);
function kf(e) {
  if (0 <= e.y && e.y < 100) {
    var t = new Date(-1, e.m, e.d, e.H, e.M, e.S, e.L);
    return t.setFullYear(e.y), t;
  }
  return new Date(e.y, e.m, e.d, e.H, e.M, e.S, e.L);
}
function Ef(e) {
  if (0 <= e.y && e.y < 100) {
    var t = new Date(Date.UTC(-1, e.m, e.d, e.H, e.M, e.S, e.L));
    return t.setUTCFullYear(e.y), t;
  }
  return new Date(Date.UTC(e.y, e.m, e.d, e.H, e.M, e.S, e.L));
}
function wi(e, t, r) {
  return { y: e, m: t, d: r, H: 0, M: 0, S: 0, L: 0 };
}
function BR(e) {
  var t = e.dateTime, r = e.date, n = e.time, a = e.periods, i = e.days, o = e.shortDays, l = e.months, c = e.shortMonths, u = _i(a), d = ji(a), f = _i(i), p = ji(i), m = _i(o), y = ji(o), h = _i(l), g = ji(l), v = _i(c), b = ji(c), w = {
    a: V,
    A: B,
    b: ee,
    B: U,
    c: null,
    d: ax,
    e: ax,
    f: fI,
    g: _I,
    G: SI,
    H: cI,
    I: uI,
    j: dI,
    L: VS,
    m: mI,
    M: hI,
    p: G,
    q: Y,
    Q: sx,
    s: lx,
    S: pI,
    u: gI,
    U: yI,
    V: vI,
    w: bI,
    W: xI,
    x: null,
    X: null,
    y: wI,
    Y: jI,
    Z: kI,
    "%": ox
  }, j = {
    a: Q,
    A: ne,
    b: z,
    B: se,
    c: null,
    d: ix,
    e: ix,
    f: TI,
    g: FI,
    G: UI,
    H: EI,
    I: AI,
    j: CI,
    L: GS,
    m: PI,
    M: OI,
    p: oe,
    q: ie,
    Q: sx,
    s: lx,
    S: NI,
    u: MI,
    U: RI,
    V: II,
    w: DI,
    W: $I,
    x: null,
    X: null,
    y: LI,
    Y: qI,
    Z: zI,
    "%": ox
  }, x = {
    a: L,
    A: P,
    b: A,
    B: T,
    c: M,
    d: rx,
    e: rx,
    f: iI,
    g: tx,
    G: ex,
    H: nx,
    I: nx,
    j: tI,
    L: aI,
    m: eI,
    M: rI,
    p: E,
    q: ZR,
    Q: sI,
    s: lI,
    S: nI,
    u: KR,
    U: YR,
    V: XR,
    w: GR,
    W: QR,
    x: N,
    X: q,
    y: tx,
    Y: ex,
    Z: JR,
    "%": oI
  };
  w.x = _(r, w), w.X = _(n, w), w.c = _(t, w), j.x = _(r, j), j.X = _(n, j), j.c = _(t, j);
  function _(K, ae) {
    return function(fe) {
      var $ = [], Ee = -1, re = 0, Pe = K.length, Oe, Ne, ke;
      for (fe instanceof Date || (fe = /* @__PURE__ */ new Date(+fe)); ++Ee < Pe; )
        K.charCodeAt(Ee) === 37 && ($.push(K.slice(re, Ee)), (Ne = Z0[Oe = K.charAt(++Ee)]) != null ? Oe = K.charAt(++Ee) : Ne = Oe === "e" ? " " : "0", (ke = ae[Oe]) && (Oe = ke(fe, Ne)), $.push(Oe), re = Ee + 1);
      return $.push(K.slice(re, Ee)), $.join("");
    };
  }
  function S(K, ae) {
    return function(fe) {
      var $ = wi(1900, void 0, 1), Ee = k($, K, fe += "", 0), re, Pe;
      if (Ee != fe.length) return null;
      if ("Q" in $) return new Date($.Q);
      if ("s" in $) return new Date($.s * 1e3 + ("L" in $ ? $.L : 0));
      if (ae && !("Z" in $) && ($.Z = 0), "p" in $ && ($.H = $.H % 12 + $.p * 12), $.m === void 0 && ($.m = "q" in $ ? $.q : 0), "V" in $) {
        if ($.V < 1 || $.V > 53) return null;
        "w" in $ || ($.w = 1), "Z" in $ ? (re = Ef(wi($.y, 0, 1)), Pe = re.getUTCDay(), re = Pe > 4 || Pe === 0 ? $s.ceil(re) : $s(re), re = Dl.offset(re, ($.V - 1) * 7), $.y = re.getUTCFullYear(), $.m = re.getUTCMonth(), $.d = re.getUTCDate() + ($.w + 6) % 7) : (re = kf(wi($.y, 0, 1)), Pe = re.getDay(), re = Pe > 4 || Pe === 0 ? Ds.ceil(re) : Ds(re), re = No.offset(re, ($.V - 1) * 7), $.y = re.getFullYear(), $.m = re.getMonth(), $.d = re.getDate() + ($.w + 6) % 7);
      } else ("W" in $ || "U" in $) && ("w" in $ || ($.w = "u" in $ ? $.u % 7 : "W" in $ ? 1 : 0), Pe = "Z" in $ ? Ef(wi($.y, 0, 1)).getUTCDay() : kf(wi($.y, 0, 1)).getDay(), $.m = 0, $.d = "W" in $ ? ($.w + 6) % 7 + $.W * 7 - (Pe + 5) % 7 : $.w + $.U * 7 - (Pe + 6) % 7);
      return "Z" in $ ? ($.H += $.Z / 100 | 0, $.M += $.Z % 100, Ef($)) : kf($);
    };
  }
  function k(K, ae, fe, $) {
    for (var Ee = 0, re = ae.length, Pe = fe.length, Oe, Ne; Ee < re; ) {
      if ($ >= Pe) return -1;
      if (Oe = ae.charCodeAt(Ee++), Oe === 37) {
        if (Oe = ae.charAt(Ee++), Ne = x[Oe in Z0 ? ae.charAt(Ee++) : Oe], !Ne || ($ = Ne(K, fe, $)) < 0) return -1;
      } else if (Oe != fe.charCodeAt($++))
        return -1;
    }
    return $;
  }
  function E(K, ae, fe) {
    var $ = u.exec(ae.slice(fe));
    return $ ? (K.p = d.get($[0].toLowerCase()), fe + $[0].length) : -1;
  }
  function L(K, ae, fe) {
    var $ = m.exec(ae.slice(fe));
    return $ ? (K.w = y.get($[0].toLowerCase()), fe + $[0].length) : -1;
  }
  function P(K, ae, fe) {
    var $ = f.exec(ae.slice(fe));
    return $ ? (K.w = p.get($[0].toLowerCase()), fe + $[0].length) : -1;
  }
  function A(K, ae, fe) {
    var $ = v.exec(ae.slice(fe));
    return $ ? (K.m = b.get($[0].toLowerCase()), fe + $[0].length) : -1;
  }
  function T(K, ae, fe) {
    var $ = h.exec(ae.slice(fe));
    return $ ? (K.m = g.get($[0].toLowerCase()), fe + $[0].length) : -1;
  }
  function M(K, ae, fe) {
    return k(K, t, ae, fe);
  }
  function N(K, ae, fe) {
    return k(K, r, ae, fe);
  }
  function q(K, ae, fe) {
    return k(K, n, ae, fe);
  }
  function V(K) {
    return o[K.getDay()];
  }
  function B(K) {
    return i[K.getDay()];
  }
  function ee(K) {
    return c[K.getMonth()];
  }
  function U(K) {
    return l[K.getMonth()];
  }
  function G(K) {
    return a[+(K.getHours() >= 12)];
  }
  function Y(K) {
    return 1 + ~~(K.getMonth() / 3);
  }
  function Q(K) {
    return o[K.getUTCDay()];
  }
  function ne(K) {
    return i[K.getUTCDay()];
  }
  function z(K) {
    return c[K.getUTCMonth()];
  }
  function se(K) {
    return l[K.getUTCMonth()];
  }
  function oe(K) {
    return a[+(K.getUTCHours() >= 12)];
  }
  function ie(K) {
    return 1 + ~~(K.getUTCMonth() / 3);
  }
  return {
    format: function(K) {
      var ae = _(K += "", w);
      return ae.toString = function() {
        return K;
      }, ae;
    },
    parse: function(K) {
      var ae = S(K += "", !1);
      return ae.toString = function() {
        return K;
      }, ae;
    },
    utcFormat: function(K) {
      var ae = _(K += "", j);
      return ae.toString = function() {
        return K;
      }, ae;
    },
    utcParse: function(K) {
      var ae = S(K += "", !0);
      return ae.toString = function() {
        return K;
      }, ae;
    }
  };
}
var Z0 = { "-": "", _: " ", 0: "0" }, Mt = /^\s*\d+/, WR = /^%/, VR = /[\\^$*+?|[\]().{}]/g;
function He(e, t, r) {
  var n = e < 0 ? "-" : "", a = (n ? -e : e) + "", i = a.length;
  return n + (i < r ? new Array(r - i + 1).join(t) + a : a);
}
function HR(e) {
  return e.replace(VR, "\\$&");
}
function _i(e) {
  return new RegExp("^(?:" + e.map(HR).join("|") + ")", "i");
}
function ji(e) {
  return new Map(e.map((t, r) => [t.toLowerCase(), r]));
}
function GR(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 1));
  return n ? (e.w = +n[0], r + n[0].length) : -1;
}
function KR(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 1));
  return n ? (e.u = +n[0], r + n[0].length) : -1;
}
function YR(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 2));
  return n ? (e.U = +n[0], r + n[0].length) : -1;
}
function XR(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 2));
  return n ? (e.V = +n[0], r + n[0].length) : -1;
}
function QR(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 2));
  return n ? (e.W = +n[0], r + n[0].length) : -1;
}
function ex(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 4));
  return n ? (e.y = +n[0], r + n[0].length) : -1;
}
function tx(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 2));
  return n ? (e.y = +n[0] + (+n[0] > 68 ? 1900 : 2e3), r + n[0].length) : -1;
}
function JR(e, t, r) {
  var n = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(t.slice(r, r + 6));
  return n ? (e.Z = n[1] ? 0 : -(n[2] + (n[3] || "00")), r + n[0].length) : -1;
}
function ZR(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 1));
  return n ? (e.q = n[0] * 3 - 3, r + n[0].length) : -1;
}
function eI(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 2));
  return n ? (e.m = n[0] - 1, r + n[0].length) : -1;
}
function rx(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 2));
  return n ? (e.d = +n[0], r + n[0].length) : -1;
}
function tI(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 3));
  return n ? (e.m = 0, e.d = +n[0], r + n[0].length) : -1;
}
function nx(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 2));
  return n ? (e.H = +n[0], r + n[0].length) : -1;
}
function rI(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 2));
  return n ? (e.M = +n[0], r + n[0].length) : -1;
}
function nI(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 2));
  return n ? (e.S = +n[0], r + n[0].length) : -1;
}
function aI(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 3));
  return n ? (e.L = +n[0], r + n[0].length) : -1;
}
function iI(e, t, r) {
  var n = Mt.exec(t.slice(r, r + 6));
  return n ? (e.L = Math.floor(n[0] / 1e3), r + n[0].length) : -1;
}
function oI(e, t, r) {
  var n = WR.exec(t.slice(r, r + 1));
  return n ? r + n[0].length : -1;
}
function sI(e, t, r) {
  var n = Mt.exec(t.slice(r));
  return n ? (e.Q = +n[0], r + n[0].length) : -1;
}
function lI(e, t, r) {
  var n = Mt.exec(t.slice(r));
  return n ? (e.s = +n[0], r + n[0].length) : -1;
}
function ax(e, t) {
  return He(e.getDate(), t, 2);
}
function cI(e, t) {
  return He(e.getHours(), t, 2);
}
function uI(e, t) {
  return He(e.getHours() % 12 || 12, t, 2);
}
function dI(e, t) {
  return He(1 + No.count(Zr(e), e), t, 3);
}
function VS(e, t) {
  return He(e.getMilliseconds(), t, 3);
}
function fI(e, t) {
  return VS(e, t) + "000";
}
function mI(e, t) {
  return He(e.getMonth() + 1, t, 2);
}
function hI(e, t) {
  return He(e.getMinutes(), t, 2);
}
function pI(e, t) {
  return He(e.getSeconds(), t, 2);
}
function gI(e) {
  var t = e.getDay();
  return t === 0 ? 7 : t;
}
function yI(e, t) {
  return He($l.count(Zr(e) - 1, e), t, 2);
}
function HS(e) {
  var t = e.getDay();
  return t >= 4 || t === 0 ? Na(e) : Na.ceil(e);
}
function vI(e, t) {
  return e = HS(e), He(Na.count(Zr(e), e) + (Zr(e).getDay() === 4), t, 2);
}
function bI(e) {
  return e.getDay();
}
function xI(e, t) {
  return He(Ds.count(Zr(e) - 1, e), t, 2);
}
function wI(e, t) {
  return He(e.getFullYear() % 100, t, 2);
}
function _I(e, t) {
  return e = HS(e), He(e.getFullYear() % 100, t, 2);
}
function jI(e, t) {
  return He(e.getFullYear() % 1e4, t, 4);
}
function SI(e, t) {
  var r = e.getDay();
  return e = r >= 4 || r === 0 ? Na(e) : Na.ceil(e), He(e.getFullYear() % 1e4, t, 4);
}
function kI(e) {
  var t = e.getTimezoneOffset();
  return (t > 0 ? "-" : (t *= -1, "+")) + He(t / 60 | 0, "0", 2) + He(t % 60, "0", 2);
}
function ix(e, t) {
  return He(e.getUTCDate(), t, 2);
}
function EI(e, t) {
  return He(e.getUTCHours(), t, 2);
}
function AI(e, t) {
  return He(e.getUTCHours() % 12 || 12, t, 2);
}
function CI(e, t) {
  return He(1 + Dl.count(en(e), e), t, 3);
}
function GS(e, t) {
  return He(e.getUTCMilliseconds(), t, 3);
}
function TI(e, t) {
  return GS(e, t) + "000";
}
function PI(e, t) {
  return He(e.getUTCMonth() + 1, t, 2);
}
function OI(e, t) {
  return He(e.getUTCMinutes(), t, 2);
}
function NI(e, t) {
  return He(e.getUTCSeconds(), t, 2);
}
function MI(e) {
  var t = e.getUTCDay();
  return t === 0 ? 7 : t;
}
function RI(e, t) {
  return He(Ll.count(en(e) - 1, e), t, 2);
}
function KS(e) {
  var t = e.getUTCDay();
  return t >= 4 || t === 0 ? Ma(e) : Ma.ceil(e);
}
function II(e, t) {
  return e = KS(e), He(Ma.count(en(e), e) + (en(e).getUTCDay() === 4), t, 2);
}
function DI(e) {
  return e.getUTCDay();
}
function $I(e, t) {
  return He($s.count(en(e) - 1, e), t, 2);
}
function LI(e, t) {
  return He(e.getUTCFullYear() % 100, t, 2);
}
function FI(e, t) {
  return e = KS(e), He(e.getUTCFullYear() % 100, t, 2);
}
function qI(e, t) {
  return He(e.getUTCFullYear() % 1e4, t, 4);
}
function UI(e, t) {
  var r = e.getUTCDay();
  return e = r >= 4 || r === 0 ? Ma(e) : Ma.ceil(e), He(e.getUTCFullYear() % 1e4, t, 4);
}
function zI() {
  return "+0000";
}
function ox() {
  return "%";
}
function sx(e) {
  return +e;
}
function lx(e) {
  return Math.floor(+e / 1e3);
}
var sa, YS, XS;
BI({
  dateTime: "%x, %X",
  date: "%-m/%-d/%Y",
  time: "%-I:%M:%S %p",
  periods: ["AM", "PM"],
  days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
  shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
  shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
});
function BI(e) {
  return sa = BR(e), YS = sa.format, sa.parse, XS = sa.utcFormat, sa.utcParse, sa;
}
function WI(e) {
  return new Date(e);
}
function VI(e) {
  return e instanceof Date ? +e : +/* @__PURE__ */ new Date(+e);
}
function qp(e, t, r, n, a, i, o, l, c, u) {
  var d = Ep(), f = d.invert, p = d.domain, m = u(".%L"), y = u(":%S"), h = u("%I:%M"), g = u("%I %p"), v = u("%a %d"), b = u("%b %d"), w = u("%B"), j = u("%Y");
  function x(_) {
    return (c(_) < _ ? m : l(_) < _ ? y : o(_) < _ ? h : i(_) < _ ? g : n(_) < _ ? a(_) < _ ? v : b : r(_) < _ ? w : j)(_);
  }
  return d.invert = function(_) {
    return new Date(f(_));
  }, d.domain = function(_) {
    return arguments.length ? p(Array.from(_, VI)) : p().map(WI);
  }, d.ticks = function(_) {
    var S = p();
    return e(S[0], S[S.length - 1], _ ?? 10);
  }, d.tickFormat = function(_, S) {
    return S == null ? x : u(S);
  }, d.nice = function(_) {
    var S = p();
    return (!_ || typeof _.range != "function") && (_ = t(S[0], S[S.length - 1], _ ?? 10)), _ ? p(DS(S, _)) : d;
  }, d.copy = function() {
    return Oo(d, qp(e, t, r, n, a, i, o, l, c, u));
  }, d;
}
function HI() {
  return br.apply(qp(UR, zR, Zr, Lp, $l, No, Dp, Rp, $n, YS).domain([new Date(2e3, 0, 1), new Date(2e3, 0, 2)]), arguments);
}
function GI() {
  return br.apply(qp(FR, qR, en, Fp, Ll, Dl, $p, Ip, $n, XS).domain([Date.UTC(2e3, 0, 1), Date.UTC(2e3, 0, 2)]), arguments);
}
function Fl() {
  var e = 0, t = 1, r, n, a, i, o = Wt, l = !1, c;
  function u(f) {
    return f == null || isNaN(f = +f) ? c : o(a === 0 ? 0.5 : (f = (i(f) - r) * a, l ? Math.max(0, Math.min(1, f)) : f));
  }
  u.domain = function(f) {
    return arguments.length ? ([e, t] = f, r = i(e = +e), n = i(t = +t), a = r === n ? 0 : 1 / (n - r), u) : [e, t];
  }, u.clamp = function(f) {
    return arguments.length ? (l = !!f, u) : l;
  }, u.interpolator = function(f) {
    return arguments.length ? (o = f, u) : o;
  };
  function d(f) {
    return function(p) {
      var m, y;
      return arguments.length ? ([m, y] = p, o = f(m, y), u) : [o(0), o(1)];
    };
  }
  return u.range = d(ai), u.rangeRound = d(kp), u.unknown = function(f) {
    return arguments.length ? (c = f, u) : c;
  }, function(f) {
    return i = f, r = f(e), n = f(t), a = r === n ? 0 : 1 / (n - r), u;
  };
}
function kn(e, t) {
  return t.domain(e.domain()).interpolator(e.interpolator()).clamp(e.clamp()).unknown(e.unknown());
}
function QS() {
  var e = Sn(Fl()(Wt));
  return e.copy = function() {
    return kn(e, QS());
  }, on.apply(e, arguments);
}
function JS() {
  var e = Tp(Fl()).domain([1, 10]);
  return e.copy = function() {
    return kn(e, JS()).base(e.base());
  }, on.apply(e, arguments);
}
function ZS() {
  var e = Pp(Fl());
  return e.copy = function() {
    return kn(e, ZS()).constant(e.constant());
  }, on.apply(e, arguments);
}
function Up() {
  var e = Op(Fl());
  return e.copy = function() {
    return kn(e, Up()).exponent(e.exponent());
  }, on.apply(e, arguments);
}
function KI() {
  return Up.apply(null, arguments).exponent(0.5);
}
function ek() {
  var e = [], t = Wt;
  function r(n) {
    if (n != null && !isNaN(n = +n)) return t((To(e, n, 1) - 1) / (e.length - 1));
  }
  return r.domain = function(n) {
    if (!arguments.length) return e.slice();
    e = [];
    for (let a of n) a != null && !isNaN(a = +a) && e.push(a);
    return e.sort(vn), r;
  }, r.interpolator = function(n) {
    return arguments.length ? (t = n, r) : t;
  }, r.range = function() {
    return e.map((n, a) => t(a / (e.length - 1)));
  }, r.quantiles = function(n) {
    return Array.from({ length: n + 1 }, (a, i) => RM(e, i / n));
  }, r.copy = function() {
    return ek(t).domain(e);
  }, on.apply(r, arguments);
}
function ql() {
  var e = 0, t = 0.5, r = 1, n = 1, a, i, o, l, c, u = Wt, d, f = !1, p;
  function m(h) {
    return isNaN(h = +h) ? p : (h = 0.5 + ((h = +d(h)) - i) * (n * h < n * i ? l : c), u(f ? Math.max(0, Math.min(1, h)) : h));
  }
  m.domain = function(h) {
    return arguments.length ? ([e, t, r] = h, a = d(e = +e), i = d(t = +t), o = d(r = +r), l = a === i ? 0 : 0.5 / (i - a), c = i === o ? 0 : 0.5 / (o - i), n = i < a ? -1 : 1, m) : [e, t, r];
  }, m.clamp = function(h) {
    return arguments.length ? (f = !!h, m) : f;
  }, m.interpolator = function(h) {
    return arguments.length ? (u = h, m) : u;
  };
  function y(h) {
    return function(g) {
      var v, b, w;
      return arguments.length ? ([v, b, w] = g, u = oR(h, [v, b, w]), m) : [u(0), u(0.5), u(1)];
    };
  }
  return m.range = y(ai), m.rangeRound = y(kp), m.unknown = function(h) {
    return arguments.length ? (p = h, m) : p;
  }, function(h) {
    return d = h, a = h(e), i = h(t), o = h(r), l = a === i ? 0 : 0.5 / (i - a), c = i === o ? 0 : 0.5 / (o - i), n = i < a ? -1 : 1, m;
  };
}
function tk() {
  var e = Sn(ql()(Wt));
  return e.copy = function() {
    return kn(e, tk());
  }, on.apply(e, arguments);
}
function rk() {
  var e = Tp(ql()).domain([0.1, 1, 10]);
  return e.copy = function() {
    return kn(e, rk()).base(e.base());
  }, on.apply(e, arguments);
}
function nk() {
  var e = Pp(ql());
  return e.copy = function() {
    return kn(e, nk()).constant(e.constant());
  }, on.apply(e, arguments);
}
function zp() {
  var e = Op(ql());
  return e.copy = function() {
    return kn(e, zp()).exponent(e.exponent());
  }, on.apply(e, arguments);
}
function YI() {
  return zp.apply(null, arguments).exponent(0.5);
}
const cx = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  scaleBand: Ki,
  scaleDiverging: tk,
  scaleDivergingLog: rk,
  scaleDivergingPow: zp,
  scaleDivergingSqrt: YI,
  scaleDivergingSymlog: nk,
  scaleIdentity: IS,
  scaleImplicit: Ym,
  scaleLinear: Rs,
  scaleLog: $S,
  scaleOrdinal: _p,
  scalePoint: Mi,
  scalePow: Np,
  scaleQuantile: qS,
  scaleQuantize: US,
  scaleRadial: FS,
  scaleSequential: QS,
  scaleSequentialLog: JS,
  scaleSequentialPow: Up,
  scaleSequentialQuantile: ek,
  scaleSequentialSqrt: KI,
  scaleSequentialSymlog: ZS,
  scaleSqrt: TR,
  scaleSymlog: LS,
  scaleThreshold: zS,
  scaleTime: HI,
  scaleUtc: GI,
  tickFormat: RS
}, Symbol.toStringTag, { value: "Module" }));
var Af, ux;
function ak() {
  if (ux) return Af;
  ux = 1;
  var e = ei();
  function t(r, n, a) {
    for (var i = -1, o = r.length; ++i < o; ) {
      var l = r[i], c = n(l);
      if (c != null && (u === void 0 ? c === c && !e(c) : a(c, u)))
        var u = c, d = l;
    }
    return d;
  }
  return Af = t, Af;
}
var Cf, dx;
function XI() {
  if (dx) return Cf;
  dx = 1;
  function e(t, r) {
    return t > r;
  }
  return Cf = e, Cf;
}
var Tf, fx;
function QI() {
  if (fx) return Tf;
  fx = 1;
  var e = ak(), t = XI(), r = ri();
  function n(a) {
    return a && a.length ? e(a, r, t) : void 0;
  }
  return Tf = n, Tf;
}
var JI = QI();
const Ul = /* @__PURE__ */ st(JI);
var Pf, mx;
function ZI() {
  if (mx) return Pf;
  mx = 1;
  function e(t, r) {
    return t < r;
  }
  return Pf = e, Pf;
}
var Of, hx;
function eD() {
  if (hx) return Of;
  hx = 1;
  var e = ak(), t = ZI(), r = ri();
  function n(a) {
    return a && a.length ? e(a, r, t) : void 0;
  }
  return Of = n, Of;
}
var tD = eD();
const zl = /* @__PURE__ */ st(tD);
var Nf, px;
function rD() {
  if (px) return Nf;
  px = 1;
  var e = ap(), t = jn(), r = fS(), n = Xt();
  function a(i, o) {
    var l = n(i) ? e : r;
    return l(i, t(o, 3));
  }
  return Nf = a, Nf;
}
var Mf, gx;
function nD() {
  if (gx) return Mf;
  gx = 1;
  var e = uS(), t = rD();
  function r(n, a) {
    return e(t(n, a), 1);
  }
  return Mf = r, Mf;
}
var aD = nD();
const iD = /* @__PURE__ */ st(aD);
var Rf, yx;
function oD() {
  if (yx) return Rf;
  yx = 1;
  var e = yp();
  function t(r, n) {
    return e(r, n);
  }
  return Rf = t, Rf;
}
var sD = oD();
const Bl = /* @__PURE__ */ st(sD);
var ii = 1e9, lD = {
  // These values must be integers within the stated ranges (inclusive).
  // Most of these values can be changed during run-time using `Decimal.config`.
  // The maximum number of significant digits of the result of a calculation or base conversion.
  // E.g. `Decimal.config({ precision: 20 });`
  precision: 20,
  // 1 to MAX_DIGITS
  // The rounding mode used by default by `toInteger`, `toDecimalPlaces`, `toExponential`,
  // `toFixed`, `toPrecision` and `toSignificantDigits`.
  //
  // ROUND_UP         0 Away from zero.
  // ROUND_DOWN       1 Towards zero.
  // ROUND_CEIL       2 Towards +Infinity.
  // ROUND_FLOOR      3 Towards -Infinity.
  // ROUND_HALF_UP    4 Towards nearest neighbour. If equidistant, up.
  // ROUND_HALF_DOWN  5 Towards nearest neighbour. If equidistant, down.
  // ROUND_HALF_EVEN  6 Towards nearest neighbour. If equidistant, towards even neighbour.
  // ROUND_HALF_CEIL  7 Towards nearest neighbour. If equidistant, towards +Infinity.
  // ROUND_HALF_FLOOR 8 Towards nearest neighbour. If equidistant, towards -Infinity.
  //
  // E.g.
  // `Decimal.rounding = 4;`
  // `Decimal.rounding = Decimal.ROUND_HALF_UP;`
  rounding: 4,
  // 0 to 8
  // The exponent value at and beneath which `toString` returns exponential notation.
  // JavaScript numbers: -7
  toExpNeg: -7,
  // 0 to -MAX_E
  // The exponent value at and above which `toString` returns exponential notation.
  // JavaScript numbers: 21
  toExpPos: 21,
  // 0 to MAX_E
  // The natural logarithm of 10.
  // 115 digits
  LN10: "2.302585092994045684017991454684364207601101488628772976033327900967572609677352480235997205089598298341967784042286"
}, Wp, ut = !0, yr = "[DecimalError] ", Bn = yr + "Invalid argument: ", Bp = yr + "Exponent out of range: ", oi = Math.floor, Mn = Math.pow, cD = /^(\d+(\.\d*)?|\.\d+)(e[+-]?\d+)?$/i, nr, Nt = 1e7, lt = 7, ik = 9007199254740991, Ls = oi(ik / lt), ye = {};
ye.absoluteValue = ye.abs = function() {
  var e = new this.constructor(this);
  return e.s && (e.s = 1), e;
};
ye.comparedTo = ye.cmp = function(e) {
  var t, r, n, a, i = this;
  if (e = new i.constructor(e), i.s !== e.s) return i.s || -e.s;
  if (i.e !== e.e) return i.e > e.e ^ i.s < 0 ? 1 : -1;
  for (n = i.d.length, a = e.d.length, t = 0, r = n < a ? n : a; t < r; ++t)
    if (i.d[t] !== e.d[t]) return i.d[t] > e.d[t] ^ i.s < 0 ? 1 : -1;
  return n === a ? 0 : n > a ^ i.s < 0 ? 1 : -1;
};
ye.decimalPlaces = ye.dp = function() {
  var e = this, t = e.d.length - 1, r = (t - e.e) * lt;
  if (t = e.d[t], t) for (; t % 10 == 0; t /= 10) r--;
  return r < 0 ? 0 : r;
};
ye.dividedBy = ye.div = function(e) {
  return Qr(this, new this.constructor(e));
};
ye.dividedToIntegerBy = ye.idiv = function(e) {
  var t = this, r = t.constructor;
  return at(Qr(t, new r(e), 0, 1), r.precision);
};
ye.equals = ye.eq = function(e) {
  return !this.cmp(e);
};
ye.exponent = function() {
  return wt(this);
};
ye.greaterThan = ye.gt = function(e) {
  return this.cmp(e) > 0;
};
ye.greaterThanOrEqualTo = ye.gte = function(e) {
  return this.cmp(e) >= 0;
};
ye.isInteger = ye.isint = function() {
  return this.e > this.d.length - 2;
};
ye.isNegative = ye.isneg = function() {
  return this.s < 0;
};
ye.isPositive = ye.ispos = function() {
  return this.s > 0;
};
ye.isZero = function() {
  return this.s === 0;
};
ye.lessThan = ye.lt = function(e) {
  return this.cmp(e) < 0;
};
ye.lessThanOrEqualTo = ye.lte = function(e) {
  return this.cmp(e) < 1;
};
ye.logarithm = ye.log = function(e) {
  var t, r = this, n = r.constructor, a = n.precision, i = a + 5;
  if (e === void 0)
    e = new n(10);
  else if (e = new n(e), e.s < 1 || e.eq(nr)) throw Error(yr + "NaN");
  if (r.s < 1) throw Error(yr + (r.s ? "NaN" : "-Infinity"));
  return r.eq(nr) ? new n(0) : (ut = !1, t = Qr(Zi(r, i), Zi(e, i), i), ut = !0, at(t, a));
};
ye.minus = ye.sub = function(e) {
  var t = this;
  return e = new t.constructor(e), t.s == e.s ? lk(t, e) : ok(t, (e.s = -e.s, e));
};
ye.modulo = ye.mod = function(e) {
  var t, r = this, n = r.constructor, a = n.precision;
  if (e = new n(e), !e.s) throw Error(yr + "NaN");
  return r.s ? (ut = !1, t = Qr(r, e, 0, 1).times(e), ut = !0, r.minus(t)) : at(new n(r), a);
};
ye.naturalExponential = ye.exp = function() {
  return sk(this);
};
ye.naturalLogarithm = ye.ln = function() {
  return Zi(this);
};
ye.negated = ye.neg = function() {
  var e = new this.constructor(this);
  return e.s = -e.s || 0, e;
};
ye.plus = ye.add = function(e) {
  var t = this;
  return e = new t.constructor(e), t.s == e.s ? ok(t, e) : lk(t, (e.s = -e.s, e));
};
ye.precision = ye.sd = function(e) {
  var t, r, n, a = this;
  if (e !== void 0 && e !== !!e && e !== 1 && e !== 0) throw Error(Bn + e);
  if (t = wt(a) + 1, n = a.d.length - 1, r = n * lt + 1, n = a.d[n], n) {
    for (; n % 10 == 0; n /= 10) r--;
    for (n = a.d[0]; n >= 10; n /= 10) r++;
  }
  return e && t > r ? t : r;
};
ye.squareRoot = ye.sqrt = function() {
  var e, t, r, n, a, i, o, l = this, c = l.constructor;
  if (l.s < 1) {
    if (!l.s) return new c(0);
    throw Error(yr + "NaN");
  }
  for (e = wt(l), ut = !1, a = Math.sqrt(+l), a == 0 || a == 1 / 0 ? (t = Mr(l.d), (t.length + e) % 2 == 0 && (t += "0"), a = Math.sqrt(t), e = oi((e + 1) / 2) - (e < 0 || e % 2), a == 1 / 0 ? t = "5e" + e : (t = a.toExponential(), t = t.slice(0, t.indexOf("e") + 1) + e), n = new c(t)) : n = new c(a.toString()), r = c.precision, a = o = r + 3; ; )
    if (i = n, n = i.plus(Qr(l, i, o + 2)).times(0.5), Mr(i.d).slice(0, o) === (t = Mr(n.d)).slice(0, o)) {
      if (t = t.slice(o - 3, o + 1), a == o && t == "4999") {
        if (at(i, r + 1, 0), i.times(i).eq(l)) {
          n = i;
          break;
        }
      } else if (t != "9999")
        break;
      o += 4;
    }
  return ut = !0, at(n, r);
};
ye.times = ye.mul = function(e) {
  var t, r, n, a, i, o, l, c, u, d = this, f = d.constructor, p = d.d, m = (e = new f(e)).d;
  if (!d.s || !e.s) return new f(0);
  for (e.s *= d.s, r = d.e + e.e, c = p.length, u = m.length, c < u && (i = p, p = m, m = i, o = c, c = u, u = o), i = [], o = c + u, n = o; n--; ) i.push(0);
  for (n = u; --n >= 0; ) {
    for (t = 0, a = c + n; a > n; )
      l = i[a] + m[n] * p[a - n - 1] + t, i[a--] = l % Nt | 0, t = l / Nt | 0;
    i[a] = (i[a] + t) % Nt | 0;
  }
  for (; !i[--o]; ) i.pop();
  return t ? ++r : i.shift(), e.d = i, e.e = r, ut ? at(e, f.precision) : e;
};
ye.toDecimalPlaces = ye.todp = function(e, t) {
  var r = this, n = r.constructor;
  return r = new n(r), e === void 0 ? r : ($r(e, 0, ii), t === void 0 ? t = n.rounding : $r(t, 0, 8), at(r, e + wt(r) + 1, t));
};
ye.toExponential = function(e, t) {
  var r, n = this, a = n.constructor;
  return e === void 0 ? r = Hn(n, !0) : ($r(e, 0, ii), t === void 0 ? t = a.rounding : $r(t, 0, 8), n = at(new a(n), e + 1, t), r = Hn(n, !0, e + 1)), r;
};
ye.toFixed = function(e, t) {
  var r, n, a = this, i = a.constructor;
  return e === void 0 ? Hn(a) : ($r(e, 0, ii), t === void 0 ? t = i.rounding : $r(t, 0, 8), n = at(new i(a), e + wt(a) + 1, t), r = Hn(n.abs(), !1, e + wt(n) + 1), a.isneg() && !a.isZero() ? "-" + r : r);
};
ye.toInteger = ye.toint = function() {
  var e = this, t = e.constructor;
  return at(new t(e), wt(e) + 1, t.rounding);
};
ye.toNumber = function() {
  return +this;
};
ye.toPower = ye.pow = function(e) {
  var t, r, n, a, i, o, l = this, c = l.constructor, u = 12, d = +(e = new c(e));
  if (!e.s) return new c(nr);
  if (l = new c(l), !l.s) {
    if (e.s < 1) throw Error(yr + "Infinity");
    return l;
  }
  if (l.eq(nr)) return l;
  if (n = c.precision, e.eq(nr)) return at(l, n);
  if (t = e.e, r = e.d.length - 1, o = t >= r, i = l.s, o) {
    if ((r = d < 0 ? -d : d) <= ik) {
      for (a = new c(nr), t = Math.ceil(n / lt + 4), ut = !1; r % 2 && (a = a.times(l), bx(a.d, t)), r = oi(r / 2), r !== 0; )
        l = l.times(l), bx(l.d, t);
      return ut = !0, e.s < 0 ? new c(nr).div(a) : at(a, n);
    }
  } else if (i < 0) throw Error(yr + "NaN");
  return i = i < 0 && e.d[Math.max(t, r)] & 1 ? -1 : 1, l.s = 1, ut = !1, a = e.times(Zi(l, n + u)), ut = !0, a = sk(a), a.s = i, a;
};
ye.toPrecision = function(e, t) {
  var r, n, a = this, i = a.constructor;
  return e === void 0 ? (r = wt(a), n = Hn(a, r <= i.toExpNeg || r >= i.toExpPos)) : ($r(e, 1, ii), t === void 0 ? t = i.rounding : $r(t, 0, 8), a = at(new i(a), e, t), r = wt(a), n = Hn(a, e <= r || r <= i.toExpNeg, e)), n;
};
ye.toSignificantDigits = ye.tosd = function(e, t) {
  var r = this, n = r.constructor;
  return e === void 0 ? (e = n.precision, t = n.rounding) : ($r(e, 1, ii), t === void 0 ? t = n.rounding : $r(t, 0, 8)), at(new n(r), e, t);
};
ye.toString = ye.valueOf = ye.val = ye.toJSON = ye[Symbol.for("nodejs.util.inspect.custom")] = function() {
  var e = this, t = wt(e), r = e.constructor;
  return Hn(e, t <= r.toExpNeg || t >= r.toExpPos);
};
function ok(e, t) {
  var r, n, a, i, o, l, c, u, d = e.constructor, f = d.precision;
  if (!e.s || !t.s)
    return t.s || (t = new d(e)), ut ? at(t, f) : t;
  if (c = e.d, u = t.d, o = e.e, a = t.e, c = c.slice(), i = o - a, i) {
    for (i < 0 ? (n = c, i = -i, l = u.length) : (n = u, a = o, l = c.length), o = Math.ceil(f / lt), l = o > l ? o + 1 : l + 1, i > l && (i = l, n.length = 1), n.reverse(); i--; ) n.push(0);
    n.reverse();
  }
  for (l = c.length, i = u.length, l - i < 0 && (i = l, n = u, u = c, c = n), r = 0; i; )
    r = (c[--i] = c[i] + u[i] + r) / Nt | 0, c[i] %= Nt;
  for (r && (c.unshift(r), ++a), l = c.length; c[--l] == 0; ) c.pop();
  return t.d = c, t.e = a, ut ? at(t, f) : t;
}
function $r(e, t, r) {
  if (e !== ~~e || e < t || e > r)
    throw Error(Bn + e);
}
function Mr(e) {
  var t, r, n, a = e.length - 1, i = "", o = e[0];
  if (a > 0) {
    for (i += o, t = 1; t < a; t++)
      n = e[t] + "", r = lt - n.length, r && (i += mn(r)), i += n;
    o = e[t], n = o + "", r = lt - n.length, r && (i += mn(r));
  } else if (o === 0)
    return "0";
  for (; o % 10 === 0; ) o /= 10;
  return i + o;
}
var Qr = /* @__PURE__ */ (function() {
  function e(n, a) {
    var i, o = 0, l = n.length;
    for (n = n.slice(); l--; )
      i = n[l] * a + o, n[l] = i % Nt | 0, o = i / Nt | 0;
    return o && n.unshift(o), n;
  }
  function t(n, a, i, o) {
    var l, c;
    if (i != o)
      c = i > o ? 1 : -1;
    else
      for (l = c = 0; l < i; l++)
        if (n[l] != a[l]) {
          c = n[l] > a[l] ? 1 : -1;
          break;
        }
    return c;
  }
  function r(n, a, i) {
    for (var o = 0; i--; )
      n[i] -= o, o = n[i] < a[i] ? 1 : 0, n[i] = o * Nt + n[i] - a[i];
    for (; !n[0] && n.length > 1; ) n.shift();
  }
  return function(n, a, i, o) {
    var l, c, u, d, f, p, m, y, h, g, v, b, w, j, x, _, S, k, E = n.constructor, L = n.s == a.s ? 1 : -1, P = n.d, A = a.d;
    if (!n.s) return new E(n);
    if (!a.s) throw Error(yr + "Division by zero");
    for (c = n.e - a.e, S = A.length, x = P.length, m = new E(L), y = m.d = [], u = 0; A[u] == (P[u] || 0); ) ++u;
    if (A[u] > (P[u] || 0) && --c, i == null ? b = i = E.precision : o ? b = i + (wt(n) - wt(a)) + 1 : b = i, b < 0) return new E(0);
    if (b = b / lt + 2 | 0, u = 0, S == 1)
      for (d = 0, A = A[0], b++; (u < x || d) && b--; u++)
        w = d * Nt + (P[u] || 0), y[u] = w / A | 0, d = w % A | 0;
    else {
      for (d = Nt / (A[0] + 1) | 0, d > 1 && (A = e(A, d), P = e(P, d), S = A.length, x = P.length), j = S, h = P.slice(0, S), g = h.length; g < S; ) h[g++] = 0;
      k = A.slice(), k.unshift(0), _ = A[0], A[1] >= Nt / 2 && ++_;
      do
        d = 0, l = t(A, h, S, g), l < 0 ? (v = h[0], S != g && (v = v * Nt + (h[1] || 0)), d = v / _ | 0, d > 1 ? (d >= Nt && (d = Nt - 1), f = e(A, d), p = f.length, g = h.length, l = t(f, h, p, g), l == 1 && (d--, r(f, S < p ? k : A, p))) : (d == 0 && (l = d = 1), f = A.slice()), p = f.length, p < g && f.unshift(0), r(h, f, g), l == -1 && (g = h.length, l = t(A, h, S, g), l < 1 && (d++, r(h, S < g ? k : A, g))), g = h.length) : l === 0 && (d++, h = [0]), y[u++] = d, l && h[0] ? h[g++] = P[j] || 0 : (h = [P[j]], g = 1);
      while ((j++ < x || h[0] !== void 0) && b--);
    }
    return y[0] || y.shift(), m.e = c, at(m, o ? i + wt(m) + 1 : i);
  };
})();
function sk(e, t) {
  var r, n, a, i, o, l, c = 0, u = 0, d = e.constructor, f = d.precision;
  if (wt(e) > 16) throw Error(Bp + wt(e));
  if (!e.s) return new d(nr);
  for (ut = !1, l = f, o = new d(0.03125); e.abs().gte(0.1); )
    e = e.times(o), u += 5;
  for (n = Math.log(Mn(2, u)) / Math.LN10 * 2 + 5 | 0, l += n, r = a = i = new d(nr), d.precision = l; ; ) {
    if (a = at(a.times(e), l), r = r.times(++c), o = i.plus(Qr(a, r, l)), Mr(o.d).slice(0, l) === Mr(i.d).slice(0, l)) {
      for (; u--; ) i = at(i.times(i), l);
      return d.precision = f, t == null ? (ut = !0, at(i, f)) : i;
    }
    i = o;
  }
}
function wt(e) {
  for (var t = e.e * lt, r = e.d[0]; r >= 10; r /= 10) t++;
  return t;
}
function If(e, t, r) {
  if (t > e.LN10.sd())
    throw ut = !0, r && (e.precision = r), Error(yr + "LN10 precision limit exceeded");
  return at(new e(e.LN10), t);
}
function mn(e) {
  for (var t = ""; e--; ) t += "0";
  return t;
}
function Zi(e, t) {
  var r, n, a, i, o, l, c, u, d, f = 1, p = 10, m = e, y = m.d, h = m.constructor, g = h.precision;
  if (m.s < 1) throw Error(yr + (m.s ? "NaN" : "-Infinity"));
  if (m.eq(nr)) return new h(0);
  if (t == null ? (ut = !1, u = g) : u = t, m.eq(10))
    return t == null && (ut = !0), If(h, u);
  if (u += p, h.precision = u, r = Mr(y), n = r.charAt(0), i = wt(m), Math.abs(i) < 15e14) {
    for (; n < 7 && n != 1 || n == 1 && r.charAt(1) > 3; )
      m = m.times(e), r = Mr(m.d), n = r.charAt(0), f++;
    i = wt(m), n > 1 ? (m = new h("0." + r), i++) : m = new h(n + "." + r.slice(1));
  } else
    return c = If(h, u + 2, g).times(i + ""), m = Zi(new h(n + "." + r.slice(1)), u - p).plus(c), h.precision = g, t == null ? (ut = !0, at(m, g)) : m;
  for (l = o = m = Qr(m.minus(nr), m.plus(nr), u), d = at(m.times(m), u), a = 3; ; ) {
    if (o = at(o.times(d), u), c = l.plus(Qr(o, new h(a), u)), Mr(c.d).slice(0, u) === Mr(l.d).slice(0, u))
      return l = l.times(2), i !== 0 && (l = l.plus(If(h, u + 2, g).times(i + ""))), l = Qr(l, new h(f), u), h.precision = g, t == null ? (ut = !0, at(l, g)) : l;
    l = c, a += 2;
  }
}
function vx(e, t) {
  var r, n, a;
  for ((r = t.indexOf(".")) > -1 && (t = t.replace(".", "")), (n = t.search(/e/i)) > 0 ? (r < 0 && (r = n), r += +t.slice(n + 1), t = t.substring(0, n)) : r < 0 && (r = t.length), n = 0; t.charCodeAt(n) === 48; ) ++n;
  for (a = t.length; t.charCodeAt(a - 1) === 48; ) --a;
  if (t = t.slice(n, a), t) {
    if (a -= n, r = r - n - 1, e.e = oi(r / lt), e.d = [], n = (r + 1) % lt, r < 0 && (n += lt), n < a) {
      for (n && e.d.push(+t.slice(0, n)), a -= lt; n < a; ) e.d.push(+t.slice(n, n += lt));
      t = t.slice(n), n = lt - t.length;
    } else
      n -= a;
    for (; n--; ) t += "0";
    if (e.d.push(+t), ut && (e.e > Ls || e.e < -Ls)) throw Error(Bp + r);
  } else
    e.s = 0, e.e = 0, e.d = [0];
  return e;
}
function at(e, t, r) {
  var n, a, i, o, l, c, u, d, f = e.d;
  for (o = 1, i = f[0]; i >= 10; i /= 10) o++;
  if (n = t - o, n < 0)
    n += lt, a = t, u = f[d = 0];
  else {
    if (d = Math.ceil((n + 1) / lt), i = f.length, d >= i) return e;
    for (u = i = f[d], o = 1; i >= 10; i /= 10) o++;
    n %= lt, a = n - lt + o;
  }
  if (r !== void 0 && (i = Mn(10, o - a - 1), l = u / i % 10 | 0, c = t < 0 || f[d + 1] !== void 0 || u % i, c = r < 4 ? (l || c) && (r == 0 || r == (e.s < 0 ? 3 : 2)) : l > 5 || l == 5 && (r == 4 || c || r == 6 && // Check whether the digit to the left of the rounding digit is odd.
  (n > 0 ? a > 0 ? u / Mn(10, o - a) : 0 : f[d - 1]) % 10 & 1 || r == (e.s < 0 ? 8 : 7))), t < 1 || !f[0])
    return c ? (i = wt(e), f.length = 1, t = t - i - 1, f[0] = Mn(10, (lt - t % lt) % lt), e.e = oi(-t / lt) || 0) : (f.length = 1, f[0] = e.e = e.s = 0), e;
  if (n == 0 ? (f.length = d, i = 1, d--) : (f.length = d + 1, i = Mn(10, lt - n), f[d] = a > 0 ? (u / Mn(10, o - a) % Mn(10, a) | 0) * i : 0), c)
    for (; ; )
      if (d == 0) {
        (f[0] += i) == Nt && (f[0] = 1, ++e.e);
        break;
      } else {
        if (f[d] += i, f[d] != Nt) break;
        f[d--] = 0, i = 1;
      }
  for (n = f.length; f[--n] === 0; ) f.pop();
  if (ut && (e.e > Ls || e.e < -Ls))
    throw Error(Bp + wt(e));
  return e;
}
function lk(e, t) {
  var r, n, a, i, o, l, c, u, d, f, p = e.constructor, m = p.precision;
  if (!e.s || !t.s)
    return t.s ? t.s = -t.s : t = new p(e), ut ? at(t, m) : t;
  if (c = e.d, f = t.d, n = t.e, u = e.e, c = c.slice(), o = u - n, o) {
    for (d = o < 0, d ? (r = c, o = -o, l = f.length) : (r = f, n = u, l = c.length), a = Math.max(Math.ceil(m / lt), l) + 2, o > a && (o = a, r.length = 1), r.reverse(), a = o; a--; ) r.push(0);
    r.reverse();
  } else {
    for (a = c.length, l = f.length, d = a < l, d && (l = a), a = 0; a < l; a++)
      if (c[a] != f[a]) {
        d = c[a] < f[a];
        break;
      }
    o = 0;
  }
  for (d && (r = c, c = f, f = r, t.s = -t.s), l = c.length, a = f.length - l; a > 0; --a) c[l++] = 0;
  for (a = f.length; a > o; ) {
    if (c[--a] < f[a]) {
      for (i = a; i && c[--i] === 0; ) c[i] = Nt - 1;
      --c[i], c[a] += Nt;
    }
    c[a] -= f[a];
  }
  for (; c[--l] === 0; ) c.pop();
  for (; c[0] === 0; c.shift()) --n;
  return c[0] ? (t.d = c, t.e = n, ut ? at(t, m) : t) : new p(0);
}
function Hn(e, t, r) {
  var n, a = wt(e), i = Mr(e.d), o = i.length;
  return t ? (r && (n = r - o) > 0 ? i = i.charAt(0) + "." + i.slice(1) + mn(n) : o > 1 && (i = i.charAt(0) + "." + i.slice(1)), i = i + (a < 0 ? "e" : "e+") + a) : a < 0 ? (i = "0." + mn(-a - 1) + i, r && (n = r - o) > 0 && (i += mn(n))) : a >= o ? (i += mn(a + 1 - o), r && (n = r - a - 1) > 0 && (i = i + "." + mn(n))) : ((n = a + 1) < o && (i = i.slice(0, n) + "." + i.slice(n)), r && (n = r - o) > 0 && (a + 1 === o && (i += "."), i += mn(n))), e.s < 0 ? "-" + i : i;
}
function bx(e, t) {
  if (e.length > t)
    return e.length = t, !0;
}
function ck(e) {
  var t, r, n;
  function a(i) {
    var o = this;
    if (!(o instanceof a)) return new a(i);
    if (o.constructor = a, i instanceof a) {
      o.s = i.s, o.e = i.e, o.d = (i = i.d) ? i.slice() : i;
      return;
    }
    if (typeof i == "number") {
      if (i * 0 !== 0)
        throw Error(Bn + i);
      if (i > 0)
        o.s = 1;
      else if (i < 0)
        i = -i, o.s = -1;
      else {
        o.s = 0, o.e = 0, o.d = [0];
        return;
      }
      if (i === ~~i && i < 1e7) {
        o.e = 0, o.d = [i];
        return;
      }
      return vx(o, i.toString());
    } else if (typeof i != "string")
      throw Error(Bn + i);
    if (i.charCodeAt(0) === 45 ? (i = i.slice(1), o.s = -1) : o.s = 1, cD.test(i)) vx(o, i);
    else throw Error(Bn + i);
  }
  if (a.prototype = ye, a.ROUND_UP = 0, a.ROUND_DOWN = 1, a.ROUND_CEIL = 2, a.ROUND_FLOOR = 3, a.ROUND_HALF_UP = 4, a.ROUND_HALF_DOWN = 5, a.ROUND_HALF_EVEN = 6, a.ROUND_HALF_CEIL = 7, a.ROUND_HALF_FLOOR = 8, a.clone = ck, a.config = a.set = uD, e === void 0 && (e = {}), e)
    for (n = ["precision", "rounding", "toExpNeg", "toExpPos", "LN10"], t = 0; t < n.length; ) e.hasOwnProperty(r = n[t++]) || (e[r] = this[r]);
  return a.config(e), a;
}
function uD(e) {
  if (!e || typeof e != "object")
    throw Error(yr + "Object expected");
  var t, r, n, a = [
    "precision",
    1,
    ii,
    "rounding",
    0,
    8,
    "toExpNeg",
    -1 / 0,
    0,
    "toExpPos",
    0,
    1 / 0
  ];
  for (t = 0; t < a.length; t += 3)
    if ((n = e[r = a[t]]) !== void 0)
      if (oi(n) === n && n >= a[t + 1] && n <= a[t + 2]) this[r] = n;
      else throw Error(Bn + r + ": " + n);
  if ((n = e[r = "LN10"]) !== void 0)
    if (n == Math.LN10) this[r] = new this(n);
    else throw Error(Bn + r + ": " + n);
  return this;
}
var Wp = ck(lD);
nr = new Wp(1);
const tt = Wp;
function dD(e) {
  return pD(e) || hD(e) || mD(e) || fD();
}
function fD() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function mD(e, t) {
  if (e) {
    if (typeof e == "string") return Zm(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return Zm(e, t);
  }
}
function hD(e) {
  if (typeof Symbol < "u" && Symbol.iterator in Object(e)) return Array.from(e);
}
function pD(e) {
  if (Array.isArray(e)) return Zm(e);
}
function Zm(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++)
    n[r] = e[r];
  return n;
}
var gD = function(t) {
  return t;
}, uk = {}, dk = function(t) {
  return t === uk;
}, xx = function(t) {
  return function r() {
    return arguments.length === 0 || arguments.length === 1 && dk(arguments.length <= 0 ? void 0 : arguments[0]) ? r : t.apply(void 0, arguments);
  };
}, yD = function e(t, r) {
  return t === 1 ? r : xx(function() {
    for (var n = arguments.length, a = new Array(n), i = 0; i < n; i++)
      a[i] = arguments[i];
    var o = a.filter(function(l) {
      return l !== uk;
    }).length;
    return o >= t ? r.apply(void 0, a) : e(t - o, xx(function() {
      for (var l = arguments.length, c = new Array(l), u = 0; u < l; u++)
        c[u] = arguments[u];
      var d = a.map(function(f) {
        return dk(f) ? c.shift() : f;
      });
      return r.apply(void 0, dD(d).concat(c));
    }));
  });
}, Wl = function(t) {
  return yD(t.length, t);
}, eh = function(t, r) {
  for (var n = [], a = t; a < r; ++a)
    n[a - t] = a;
  return n;
}, vD = Wl(function(e, t) {
  return Array.isArray(t) ? t.map(e) : Object.keys(t).map(function(r) {
    return t[r];
  }).map(e);
}), bD = function() {
  for (var t = arguments.length, r = new Array(t), n = 0; n < t; n++)
    r[n] = arguments[n];
  if (!r.length)
    return gD;
  var a = r.reverse(), i = a[0], o = a.slice(1);
  return function() {
    return o.reduce(function(l, c) {
      return c(l);
    }, i.apply(void 0, arguments));
  };
}, th = function(t) {
  return Array.isArray(t) ? t.reverse() : t.split("").reverse.join("");
}, fk = function(t) {
  var r = null, n = null;
  return function() {
    for (var a = arguments.length, i = new Array(a), o = 0; o < a; o++)
      i[o] = arguments[o];
    return r && i.every(function(l, c) {
      return l === r[c];
    }) || (r = i, n = t.apply(void 0, i)), n;
  };
};
function xD(e) {
  var t;
  return e === 0 ? t = 1 : t = Math.floor(new tt(e).abs().log(10).toNumber()) + 1, t;
}
function wD(e, t, r) {
  for (var n = new tt(e), a = 0, i = []; n.lt(t) && a < 1e5; )
    i.push(n.toNumber()), n = n.add(r), a++;
  return i;
}
var _D = Wl(function(e, t, r) {
  var n = +e, a = +t;
  return n + r * (a - n);
}), jD = Wl(function(e, t, r) {
  var n = t - +e;
  return n = n || 1 / 0, (r - e) / n;
}), SD = Wl(function(e, t, r) {
  var n = t - +e;
  return n = n || 1 / 0, Math.max(0, Math.min(1, (r - e) / n));
});
const Vl = {
  rangeStep: wD,
  getDigitCount: xD,
  interpolateNumber: _D,
  uninterpolateNumber: jD,
  uninterpolateTruncation: SD
};
function rh(e) {
  return AD(e) || ED(e) || mk(e) || kD();
}
function kD() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function ED(e) {
  if (typeof Symbol < "u" && Symbol.iterator in Object(e)) return Array.from(e);
}
function AD(e) {
  if (Array.isArray(e)) return nh(e);
}
function eo(e, t) {
  return PD(e) || TD(e, t) || mk(e, t) || CD();
}
function CD() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function mk(e, t) {
  if (e) {
    if (typeof e == "string") return nh(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return nh(e, t);
  }
}
function nh(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++)
    n[r] = e[r];
  return n;
}
function TD(e, t) {
  if (!(typeof Symbol > "u" || !(Symbol.iterator in Object(e)))) {
    var r = [], n = !0, a = !1, i = void 0;
    try {
      for (var o = e[Symbol.iterator](), l; !(n = (l = o.next()).done) && (r.push(l.value), !(t && r.length === t)); n = !0)
        ;
    } catch (c) {
      a = !0, i = c;
    } finally {
      try {
        !n && o.return != null && o.return();
      } finally {
        if (a) throw i;
      }
    }
    return r;
  }
}
function PD(e) {
  if (Array.isArray(e)) return e;
}
function hk(e) {
  var t = eo(e, 2), r = t[0], n = t[1], a = r, i = n;
  return r > n && (a = n, i = r), [a, i];
}
function pk(e, t, r) {
  if (e.lte(0))
    return new tt(0);
  var n = Vl.getDigitCount(e.toNumber()), a = new tt(10).pow(n), i = e.div(a), o = n !== 1 ? 0.05 : 0.1, l = new tt(Math.ceil(i.div(o).toNumber())).add(r).mul(o), c = l.mul(a);
  return t ? c : new tt(Math.ceil(c));
}
function OD(e, t, r) {
  var n = 1, a = new tt(e);
  if (!a.isint() && r) {
    var i = Math.abs(e);
    i < 1 ? (n = new tt(10).pow(Vl.getDigitCount(e) - 1), a = new tt(Math.floor(a.div(n).toNumber())).mul(n)) : i > 1 && (a = new tt(Math.floor(e)));
  } else e === 0 ? a = new tt(Math.floor((t - 1) / 2)) : r || (a = new tt(Math.floor(e)));
  var o = Math.floor((t - 1) / 2), l = bD(vD(function(c) {
    return a.add(new tt(c - o).mul(n)).toNumber();
  }), eh);
  return l(0, t);
}
function gk(e, t, r, n) {
  var a = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : 0;
  if (!Number.isFinite((t - e) / (r - 1)))
    return {
      step: new tt(0),
      tickMin: new tt(0),
      tickMax: new tt(0)
    };
  var i = pk(new tt(t).sub(e).div(r - 1), n, a), o;
  e <= 0 && t >= 0 ? o = new tt(0) : (o = new tt(e).add(t).div(2), o = o.sub(new tt(o).mod(i)));
  var l = Math.ceil(o.sub(e).div(i).toNumber()), c = Math.ceil(new tt(t).sub(o).div(i).toNumber()), u = l + c + 1;
  return u > r ? gk(e, t, r, n, a + 1) : (u < r && (c = t > 0 ? c + (r - u) : c, l = t > 0 ? l : l + (r - u)), {
    step: i,
    tickMin: o.sub(new tt(l).mul(i)),
    tickMax: o.add(new tt(c).mul(i))
  });
}
function ND(e) {
  var t = eo(e, 2), r = t[0], n = t[1], a = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 6, i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0, o = Math.max(a, 2), l = hk([r, n]), c = eo(l, 2), u = c[0], d = c[1];
  if (u === -1 / 0 || d === 1 / 0) {
    var f = d === 1 / 0 ? [u].concat(rh(eh(0, a - 1).map(function() {
      return 1 / 0;
    }))) : [].concat(rh(eh(0, a - 1).map(function() {
      return -1 / 0;
    })), [d]);
    return r > n ? th(f) : f;
  }
  if (u === d)
    return OD(u, a, i);
  var p = gk(u, d, o, i), m = p.step, y = p.tickMin, h = p.tickMax, g = Vl.rangeStep(y, h.add(new tt(0.1).mul(m)), m);
  return r > n ? th(g) : g;
}
function MD(e, t) {
  var r = eo(e, 2), n = r[0], a = r[1], i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0, o = hk([n, a]), l = eo(o, 2), c = l[0], u = l[1];
  if (c === -1 / 0 || u === 1 / 0)
    return [n, a];
  if (c === u)
    return [c];
  var d = Math.max(t, 2), f = pk(new tt(u).sub(c).div(d - 1), i, 0), p = [].concat(rh(Vl.rangeStep(new tt(c), new tt(u).sub(new tt(0.99).mul(f)), f)), [u]);
  return n > a ? th(p) : p;
}
var RD = fk(ND), ID = fk(MD), DD = process.env.NODE_ENV === "production", Df = "Invariant failed";
function Yt(e, t) {
  if (DD)
    throw new Error(Df);
  var r = typeof t == "function" ? t() : t, n = r ? "".concat(Df, ": ").concat(r) : Df;
  throw new Error(n);
}
var $D = ["offset", "layout", "width", "dataKey", "data", "dataPointFormatter", "xAxis", "yAxis"];
function Ra(e) {
  "@babel/helpers - typeof";
  return Ra = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Ra(e);
}
function Fs() {
  return Fs = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Fs.apply(this, arguments);
}
function LD(e, t) {
  return zD(e) || UD(e, t) || qD(e, t) || FD();
}
function FD() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function qD(e, t) {
  if (e) {
    if (typeof e == "string") return wx(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return wx(e, t);
  }
}
function wx(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function UD(e, t) {
  var r = e == null ? null : typeof Symbol < "u" && e[Symbol.iterator] || e["@@iterator"];
  if (r != null) {
    var n, a, i, o, l = [], c = !0, u = !1;
    try {
      if (i = (r = r.call(e)).next, t !== 0) for (; !(c = (n = i.call(r)).done) && (l.push(n.value), l.length !== t); c = !0) ;
    } catch (d) {
      u = !0, a = d;
    } finally {
      try {
        if (!c && r.return != null && (o = r.return(), Object(o) !== o)) return;
      } finally {
        if (u) throw a;
      }
    }
    return l;
  }
}
function zD(e) {
  if (Array.isArray(e)) return e;
}
function BD(e, t) {
  if (e == null) return {};
  var r = WD(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function WD(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
function VD(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function HD(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, bk(n.key), n);
  }
}
function GD(e, t, r) {
  return t && HD(e.prototype, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function KD(e, t, r) {
  return t = qs(t), YD(e, yk() ? Reflect.construct(t, r || [], qs(e).constructor) : t.apply(e, r));
}
function YD(e, t) {
  if (t && (Ra(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return XD(e);
}
function XD(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function yk() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (yk = function() {
    return !!e;
  })();
}
function qs(e) {
  return qs = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, qs(e);
}
function QD(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && ah(e, t);
}
function ah(e, t) {
  return ah = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, ah(e, t);
}
function vk(e, t, r) {
  return t = bk(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function bk(e) {
  var t = JD(e, "string");
  return Ra(t) == "symbol" ? t : t + "";
}
function JD(e, t) {
  if (Ra(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Ra(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
var Mo = /* @__PURE__ */ (function(e) {
  function t() {
    return VD(this, t), KD(this, t, arguments);
  }
  return QD(t, e), GD(t, [{
    key: "render",
    value: function() {
      var n = this.props, a = n.offset, i = n.layout, o = n.width, l = n.dataKey, c = n.data, u = n.dataPointFormatter, d = n.xAxis, f = n.yAxis, p = BD(n, $D), m = ze(p, !1);
      this.props.direction === "x" && d.type !== "number" && (process.env.NODE_ENV !== "production" ? Yt(!1, 'ErrorBar requires Axis type property to be "number".') : Yt());
      var y = c.map(function(h) {
        var g = u(h, l), v = g.x, b = g.y, w = g.value, j = g.errorVal;
        if (!j)
          return null;
        var x = [], _, S;
        if (Array.isArray(j)) {
          var k = LD(j, 2);
          _ = k[0], S = k[1];
        } else
          _ = S = j;
        if (i === "vertical") {
          var E = d.scale, L = b + a, P = L + o, A = L - o, T = E(w - _), M = E(w + S);
          x.push({
            x1: M,
            y1: P,
            x2: M,
            y2: A
          }), x.push({
            x1: T,
            y1: L,
            x2: M,
            y2: L
          }), x.push({
            x1: T,
            y1: P,
            x2: T,
            y2: A
          });
        } else if (i === "horizontal") {
          var N = f.scale, q = v + a, V = q - o, B = q + o, ee = N(w - _), U = N(w + S);
          x.push({
            x1: V,
            y1: U,
            x2: B,
            y2: U
          }), x.push({
            x1: q,
            y1: ee,
            x2: q,
            y2: U
          }), x.push({
            x1: V,
            y1: ee,
            x2: B,
            y2: ee
          });
        }
        return /* @__PURE__ */ I.createElement(yt, Fs({
          className: "recharts-errorBar",
          key: "bar-".concat(x.map(function(G) {
            return "".concat(G.x1, "-").concat(G.x2, "-").concat(G.y1, "-").concat(G.y2);
          }))
        }, m), x.map(function(G) {
          return /* @__PURE__ */ I.createElement("line", Fs({}, G, {
            key: "line-".concat(G.x1, "-").concat(G.x2, "-").concat(G.y1, "-").concat(G.y2)
          }));
        }));
      });
      return /* @__PURE__ */ I.createElement(yt, {
        className: "recharts-errorBars"
      }, y);
    }
  }]);
})(I.Component);
vk(Mo, "defaultProps", {
  stroke: "black",
  strokeWidth: 1.5,
  width: 5,
  offset: 0,
  layout: "horizontal"
});
vk(Mo, "displayName", "ErrorBar");
function to(e) {
  "@babel/helpers - typeof";
  return to = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, to(e);
}
function _x(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function Pn(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? _x(Object(r), !0).forEach(function(n) {
      ZD(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : _x(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function ZD(e, t, r) {
  return t = e2(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function e2(e) {
  var t = t2(e, "string");
  return to(t) == "symbol" ? t : t + "";
}
function t2(e, t) {
  if (to(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (to(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
var xk = function(t) {
  var r = t.children, n = t.formattedGraphicalItems, a = t.legendWidth, i = t.legendContent, o = rr(r, Un);
  if (!o)
    return null;
  var l = Un.defaultProps, c = l !== void 0 ? Pn(Pn({}, l), o.props) : {}, u;
  return o.props && o.props.payload ? u = o.props && o.props.payload : i === "children" ? u = (n || []).reduce(function(d, f) {
    var p = f.item, m = f.props, y = m.sectors || m.data || [];
    return d.concat(y.map(function(h) {
      return {
        type: o.props.iconType || p.props.legendType,
        value: h.name,
        color: h.fill,
        payload: h
      };
    }));
  }, []) : u = (n || []).map(function(d) {
    var f = d.item, p = f.type.defaultProps, m = p !== void 0 ? Pn(Pn({}, p), f.props) : {}, y = m.dataKey, h = m.name, g = m.legendType, v = m.hide;
    return {
      inactive: v,
      dataKey: y,
      type: c.iconType || g || "square",
      color: Vp(f),
      value: h || y,
      // @ts-expect-error property strokeDasharray is required in Payload but optional in props
      payload: m
    };
  }), Pn(Pn(Pn({}, c), Un.getWithHeight(o, a)), {}, {
    payload: u,
    item: o
  });
};
function ro(e) {
  "@babel/helpers - typeof";
  return ro = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, ro(e);
}
function jx(e) {
  return i2(e) || a2(e) || n2(e) || r2();
}
function r2() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function n2(e, t) {
  if (e) {
    if (typeof e == "string") return ih(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return ih(e, t);
  }
}
function a2(e) {
  if (typeof Symbol < "u" && e[Symbol.iterator] != null || e["@@iterator"] != null) return Array.from(e);
}
function i2(e) {
  if (Array.isArray(e)) return ih(e);
}
function ih(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function Sx(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function ft(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Sx(Object(r), !0).forEach(function(n) {
      ka(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Sx(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function ka(e, t, r) {
  return t = o2(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function o2(e) {
  var t = s2(e, "string");
  return ro(t) == "symbol" ? t : t + "";
}
function s2(e, t) {
  if (ro(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (ro(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function ir(e, t, r) {
  return Le(e) || Le(t) ? r : Pt(t) ? pr(e, t, r) : $e(t) ? t(e) : r;
}
function Ri(e, t, r, n) {
  var a = iD(e, function(l) {
    return ir(l, t);
  });
  if (r === "number") {
    var i = a.filter(function(l) {
      return me(l) || parseFloat(l);
    });
    return i.length ? [zl(i), Ul(i)] : [1 / 0, -1 / 0];
  }
  var o = n ? a.filter(function(l) {
    return !Le(l);
  }) : a;
  return o.map(function(l) {
    return Pt(l) || l instanceof Date ? l : "";
  });
}
var l2 = function(t) {
  var r, n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [], a = arguments.length > 2 ? arguments[2] : void 0, i = arguments.length > 3 ? arguments[3] : void 0, o = -1, l = (r = n == null ? void 0 : n.length) !== null && r !== void 0 ? r : 0;
  if (l <= 1)
    return 0;
  if (i && i.axisType === "angleAxis" && Math.abs(Math.abs(i.range[1] - i.range[0]) - 360) <= 1e-6)
    for (var c = i.range, u = 0; u < l; u++) {
      var d = u > 0 ? a[u - 1].coordinate : a[l - 1].coordinate, f = a[u].coordinate, p = u >= l - 1 ? a[0].coordinate : a[u + 1].coordinate, m = void 0;
      if (Ar(f - d) !== Ar(p - f)) {
        var y = [];
        if (Ar(p - f) === Ar(c[1] - c[0])) {
          m = p;
          var h = f + c[1] - c[0];
          y[0] = Math.min(h, (h + d) / 2), y[1] = Math.max(h, (h + d) / 2);
        } else {
          m = d;
          var g = p + c[1] - c[0];
          y[0] = Math.min(f, (g + f) / 2), y[1] = Math.max(f, (g + f) / 2);
        }
        var v = [Math.min(f, (m + f) / 2), Math.max(f, (m + f) / 2)];
        if (t > v[0] && t <= v[1] || t >= y[0] && t <= y[1]) {
          o = a[u].index;
          break;
        }
      } else {
        var b = Math.min(d, p), w = Math.max(d, p);
        if (t > (b + f) / 2 && t <= (w + f) / 2) {
          o = a[u].index;
          break;
        }
      }
    }
  else
    for (var j = 0; j < l; j++)
      if (j === 0 && t <= (n[j].coordinate + n[j + 1].coordinate) / 2 || j > 0 && j < l - 1 && t > (n[j].coordinate + n[j - 1].coordinate) / 2 && t <= (n[j].coordinate + n[j + 1].coordinate) / 2 || j === l - 1 && t > (n[j].coordinate + n[j - 1].coordinate) / 2) {
        o = n[j].index;
        break;
      }
  return o;
}, Vp = function(t) {
  var r, n = t, a = n.type.displayName, i = (r = t.type) !== null && r !== void 0 && r.defaultProps ? ft(ft({}, t.type.defaultProps), t.props) : t.props, o = i.stroke, l = i.fill, c;
  switch (a) {
    case "Line":
      c = o;
      break;
    case "Area":
    case "Radar":
      c = o && o !== "none" ? o : l;
      break;
    default:
      c = l;
      break;
  }
  return c;
}, c2 = function(t) {
  var r = t.barSize, n = t.totalSize, a = t.stackGroups, i = a === void 0 ? {} : a;
  if (!i)
    return {};
  for (var o = {}, l = Object.keys(i), c = 0, u = l.length; c < u; c++)
    for (var d = i[l[c]].stackGroups, f = Object.keys(d), p = 0, m = f.length; p < m; p++) {
      var y = d[f[p]], h = y.items, g = y.cateAxisId, v = h.filter(function(S) {
        return Yr(S.type).indexOf("Bar") >= 0;
      });
      if (v && v.length) {
        var b = v[0].type.defaultProps, w = b !== void 0 ? ft(ft({}, b), v[0].props) : v[0].props, j = w.barSize, x = w[g];
        o[x] || (o[x] = []);
        var _ = Le(j) ? r : j;
        o[x].push({
          item: v[0],
          stackList: v.slice(1),
          barSize: Le(_) ? void 0 : Vn(_, n, 0)
        });
      }
    }
  return o;
}, u2 = function(t) {
  var r = t.barGap, n = t.barCategoryGap, a = t.bandSize, i = t.sizeList, o = i === void 0 ? [] : i, l = t.maxBarSize, c = o.length;
  if (c < 1) return null;
  var u = Vn(r, a, 0, !0), d, f = [];
  if (o[0].barSize === +o[0].barSize) {
    var p = !1, m = a / c, y = o.reduce(function(j, x) {
      return j + x.barSize || 0;
    }, 0);
    y += (c - 1) * u, y >= a && (y -= (c - 1) * u, u = 0), y >= a && m > 0 && (p = !0, m *= 0.9, y = c * m);
    var h = (a - y) / 2 >> 0, g = {
      offset: h - u,
      size: 0
    };
    d = o.reduce(function(j, x) {
      var _ = {
        item: x.item,
        position: {
          offset: g.offset + g.size + u,
          // @ts-expect-error the type check above does not check for type number explicitly
          size: p ? m : x.barSize
        }
      }, S = [].concat(jx(j), [_]);
      return g = S[S.length - 1].position, x.stackList && x.stackList.length && x.stackList.forEach(function(k) {
        S.push({
          item: k,
          position: g
        });
      }), S;
    }, f);
  } else {
    var v = Vn(n, a, 0, !0);
    a - 2 * v - (c - 1) * u <= 0 && (u = 0);
    var b = (a - 2 * v - (c - 1) * u) / c;
    b > 1 && (b >>= 0);
    var w = l === +l ? Math.min(b, l) : b;
    d = o.reduce(function(j, x, _) {
      var S = [].concat(jx(j), [{
        item: x.item,
        position: {
          offset: v + (b + u) * _ + (b - w) / 2,
          size: w
        }
      }]);
      return x.stackList && x.stackList.length && x.stackList.forEach(function(k) {
        S.push({
          item: k,
          position: S[S.length - 1].position
        });
      }), S;
    }, f);
  }
  return d;
}, d2 = function(t, r, n, a) {
  var i = n.children, o = n.width, l = n.margin, c = o - (l.left || 0) - (l.right || 0), u = xk({
    children: i,
    legendWidth: c
  });
  if (u) {
    var d = a || {}, f = d.width, p = d.height, m = u.align, y = u.verticalAlign, h = u.layout;
    if ((h === "vertical" || h === "horizontal" && y === "middle") && m !== "center" && me(t[m]))
      return ft(ft({}, t), {}, ka({}, m, t[m] + (f || 0)));
    if ((h === "horizontal" || h === "vertical" && m === "center") && y !== "middle" && me(t[y]))
      return ft(ft({}, t), {}, ka({}, y, t[y] + (p || 0)));
  }
  return t;
}, f2 = function(t, r, n) {
  return Le(r) ? !0 : t === "horizontal" ? r === "yAxis" : t === "vertical" || n === "x" ? r === "xAxis" : n === "y" ? r === "yAxis" : !0;
}, wk = function(t, r, n, a, i) {
  var o = r.props.children, l = gr(o, Mo).filter(function(u) {
    return f2(a, i, u.props.direction);
  });
  if (l && l.length) {
    var c = l.map(function(u) {
      return u.props.dataKey;
    });
    return t.reduce(function(u, d) {
      var f = ir(d, n);
      if (Le(f)) return u;
      var p = Array.isArray(f) ? [zl(f), Ul(f)] : [f, f], m = c.reduce(function(y, h) {
        var g = ir(d, h, 0), v = p[0] - Math.abs(Array.isArray(g) ? g[0] : g), b = p[1] + Math.abs(Array.isArray(g) ? g[1] : g);
        return [Math.min(v, y[0]), Math.max(b, y[1])];
      }, [1 / 0, -1 / 0]);
      return [Math.min(m[0], u[0]), Math.max(m[1], u[1])];
    }, [1 / 0, -1 / 0]);
  }
  return null;
}, m2 = function(t, r, n, a, i) {
  var o = r.map(function(l) {
    return wk(t, l, n, i, a);
  }).filter(function(l) {
    return !Le(l);
  });
  return o && o.length ? o.reduce(function(l, c) {
    return [Math.min(l[0], c[0]), Math.max(l[1], c[1])];
  }, [1 / 0, -1 / 0]) : null;
}, _k = function(t, r, n, a, i) {
  var o = r.map(function(c) {
    var u = c.props.dataKey;
    return n === "number" && u && wk(t, c, u, a) || Ri(t, u, n, i);
  });
  if (n === "number")
    return o.reduce(
      // @ts-expect-error if (type === number) means that the domain is numerical type
      // - but this link is missing in the type definition
      function(c, u) {
        return [Math.min(c[0], u[0]), Math.max(c[1], u[1])];
      },
      [1 / 0, -1 / 0]
    );
  var l = {};
  return o.reduce(function(c, u) {
    for (var d = 0, f = u.length; d < f; d++)
      l[u[d]] || (l[u[d]] = !0, c.push(u[d]));
    return c;
  }, []);
}, jk = function(t, r) {
  return t === "horizontal" && r === "xAxis" || t === "vertical" && r === "yAxis" || t === "centric" && r === "angleAxis" || t === "radial" && r === "radiusAxis";
}, Sk = function(t, r, n, a) {
  if (a)
    return t.map(function(c) {
      return c.coordinate;
    });
  var i, o, l = t.map(function(c) {
    return c.coordinate === r && (i = !0), c.coordinate === n && (o = !0), c.coordinate;
  });
  return i || l.push(r), o || l.push(n), l;
}, Kr = function(t, r, n) {
  if (!t) return null;
  var a = t.scale, i = t.duplicateDomain, o = t.type, l = t.range, c = t.realScaleType === "scaleBand" ? a.bandwidth() / 2 : 2, u = (r || n) && o === "category" && a.bandwidth ? a.bandwidth() / c : 0;
  if (u = t.axisType === "angleAxis" && (l == null ? void 0 : l.length) >= 2 ? Ar(l[0] - l[1]) * 2 * u : u, r && (t.ticks || t.niceTicks)) {
    var d = (t.ticks || t.niceTicks).map(function(f) {
      var p = i ? i.indexOf(f) : f;
      return {
        // If the scaleContent is not a number, the coordinate will be NaN.
        // That could be the case for example with a PointScale and a string as domain.
        coordinate: a(p) + u,
        value: f,
        offset: u
      };
    });
    return d.filter(function(f) {
      return !Eo(f.coordinate);
    });
  }
  return t.isCategorical && t.categoricalDomain ? t.categoricalDomain.map(function(f, p) {
    return {
      coordinate: a(f) + u,
      value: f,
      index: p,
      offset: u
    };
  }) : a.ticks && !n ? a.ticks(t.tickCount).map(function(f) {
    return {
      coordinate: a(f) + u,
      value: f,
      offset: u
    };
  }) : a.domain().map(function(f, p) {
    return {
      coordinate: a(f) + u,
      value: i ? i[f] : f,
      index: p,
      offset: u
    };
  });
}, $f = /* @__PURE__ */ new WeakMap(), rs = function(t, r) {
  if (typeof r != "function")
    return t;
  $f.has(t) || $f.set(t, /* @__PURE__ */ new WeakMap());
  var n = $f.get(t);
  if (n.has(r))
    return n.get(r);
  var a = function() {
    t.apply(void 0, arguments), r.apply(void 0, arguments);
  };
  return n.set(r, a), a;
}, h2 = function(t, r, n) {
  var a = t.scale, i = t.type, o = t.layout, l = t.axisType;
  if (a === "auto")
    return o === "radial" && l === "radiusAxis" ? {
      scale: Ki(),
      realScaleType: "band"
    } : o === "radial" && l === "angleAxis" ? {
      scale: Rs(),
      realScaleType: "linear"
    } : i === "category" && r && (r.indexOf("LineChart") >= 0 || r.indexOf("AreaChart") >= 0 || r.indexOf("ComposedChart") >= 0 && !n) ? {
      scale: Mi(),
      realScaleType: "point"
    } : i === "category" ? {
      scale: Ki(),
      realScaleType: "band"
    } : {
      scale: Rs(),
      realScaleType: "linear"
    };
  if (Wn(a)) {
    var c = "scale".concat(Cl(a));
    return {
      scale: (cx[c] || Mi)(),
      realScaleType: cx[c] ? c : "point"
    };
  }
  return $e(a) ? {
    scale: a
  } : {
    scale: Mi(),
    realScaleType: "point"
  };
}, kx = 1e-4, p2 = function(t) {
  var r = t.domain();
  if (!(!r || r.length <= 2)) {
    var n = r.length, a = t.range(), i = Math.min(a[0], a[1]) - kx, o = Math.max(a[0], a[1]) + kx, l = t(r[0]), c = t(r[n - 1]);
    (l < i || l > o || c < i || c > o) && t.domain([r[0], r[n - 1]]);
  }
}, g2 = function(t, r) {
  if (!t)
    return null;
  for (var n = 0, a = t.length; n < a; n++)
    if (t[n].item === r)
      return t[n].position;
  return null;
}, y2 = function(t, r) {
  if (!r || r.length !== 2 || !me(r[0]) || !me(r[1]))
    return t;
  var n = Math.min(r[0], r[1]), a = Math.max(r[0], r[1]), i = [t[0], t[1]];
  return (!me(t[0]) || t[0] < n) && (i[0] = n), (!me(t[1]) || t[1] > a) && (i[1] = a), i[0] > a && (i[0] = a), i[1] < n && (i[1] = n), i;
}, v2 = function(t) {
  var r = t.length;
  if (!(r <= 0))
    for (var n = 0, a = t[0].length; n < a; ++n)
      for (var i = 0, o = 0, l = 0; l < r; ++l) {
        var c = Eo(t[l][n][1]) ? t[l][n][0] : t[l][n][1];
        c >= 0 ? (t[l][n][0] = i, t[l][n][1] = i + c, i = t[l][n][1]) : (t[l][n][0] = o, t[l][n][1] = o + c, o = t[l][n][1]);
      }
}, b2 = function(t) {
  var r = t.length;
  if (!(r <= 0))
    for (var n = 0, a = t[0].length; n < a; ++n)
      for (var i = 0, o = 0; o < r; ++o) {
        var l = Eo(t[o][n][1]) ? t[o][n][0] : t[o][n][1];
        l >= 0 ? (t[o][n][0] = i, t[o][n][1] = i + l, i = t[o][n][1]) : (t[o][n][0] = 0, t[o][n][1] = 0);
      }
}, x2 = {
  sign: v2,
  // @ts-expect-error definitelytyped types are incorrect
  expand: rP,
  // @ts-expect-error definitelytyped types are incorrect
  none: Ea,
  // @ts-expect-error definitelytyped types are incorrect
  silhouette: nP,
  // @ts-expect-error definitelytyped types are incorrect
  wiggle: aP,
  positive: b2
}, w2 = function(t, r, n) {
  var a = r.map(function(l) {
    return l.props.dataKey;
  }), i = x2[n], o = tP().keys(a).value(function(l, c) {
    return +ir(l, c, 0);
  }).order(Im).offset(i);
  return o(t);
}, _2 = function(t, r, n, a, i, o) {
  if (!t)
    return null;
  var l = o ? r.reverse() : r, c = {}, u = l.reduce(function(f, p) {
    var m, y = (m = p.type) !== null && m !== void 0 && m.defaultProps ? ft(ft({}, p.type.defaultProps), p.props) : p.props, h = y.stackId, g = y.hide;
    if (g)
      return f;
    var v = y[n], b = f[v] || {
      hasStack: !1,
      stackGroups: {}
    };
    if (Pt(h)) {
      var w = b.stackGroups[h] || {
        numericAxisId: n,
        cateAxisId: a,
        items: []
      };
      w.items.push(p), b.hasStack = !0, b.stackGroups[h] = w;
    } else
      b.stackGroups[Ao("_stackId_")] = {
        numericAxisId: n,
        cateAxisId: a,
        items: [p]
      };
    return ft(ft({}, f), {}, ka({}, v, b));
  }, c), d = {};
  return Object.keys(u).reduce(function(f, p) {
    var m = u[p];
    if (m.hasStack) {
      var y = {};
      m.stackGroups = Object.keys(m.stackGroups).reduce(function(h, g) {
        var v = m.stackGroups[g];
        return ft(ft({}, h), {}, ka({}, g, {
          numericAxisId: n,
          cateAxisId: a,
          items: v.items,
          stackedData: w2(t, v.items, i)
        }));
      }, y);
    }
    return ft(ft({}, f), {}, ka({}, p, m));
  }, d);
}, j2 = function(t, r) {
  var n = r.realScaleType, a = r.type, i = r.tickCount, o = r.originalDomain, l = r.allowDecimals, c = n || r.scale;
  if (c !== "auto" && c !== "linear")
    return null;
  if (i && a === "number" && o && (o[0] === "auto" || o[1] === "auto")) {
    var u = t.domain();
    if (!u.length)
      return null;
    var d = RD(u, i, l);
    return t.domain([zl(d), Ul(d)]), {
      niceTicks: d
    };
  }
  if (i && a === "number") {
    var f = t.domain(), p = ID(f, i, l);
    return {
      niceTicks: p
    };
  }
  return null;
};
function Ex(e) {
  var t = e.axis, r = e.ticks, n = e.bandSize, a = e.entry, i = e.index, o = e.dataKey;
  if (t.type === "category") {
    if (!t.allowDuplicatedCategory && t.dataKey && !Le(a[t.dataKey])) {
      var l = hs(r, "value", a[t.dataKey]);
      if (l)
        return l.coordinate + n / 2;
    }
    return r[i] ? r[i].coordinate + n / 2 : null;
  }
  var c = ir(a, Le(o) ? t.dataKey : o);
  return Le(c) ? null : t.scale(c);
}
var Ax = function(t) {
  var r = t.axis, n = t.ticks, a = t.offset, i = t.bandSize, o = t.entry, l = t.index;
  if (r.type === "category")
    return n[l] ? n[l].coordinate + a : null;
  var c = ir(o, r.dataKey, r.domain[l]);
  return Le(c) ? null : r.scale(c) - i / 2 + a;
}, S2 = function(t) {
  var r = t.numericAxis, n = r.scale.domain();
  if (r.type === "number") {
    var a = Math.min(n[0], n[1]), i = Math.max(n[0], n[1]);
    return a <= 0 && i >= 0 ? 0 : i < 0 ? i : a;
  }
  return n[0];
}, k2 = function(t, r) {
  var n, a = (n = t.type) !== null && n !== void 0 && n.defaultProps ? ft(ft({}, t.type.defaultProps), t.props) : t.props, i = a.stackId;
  if (Pt(i)) {
    var o = r[i];
    if (o) {
      var l = o.items.indexOf(t);
      return l >= 0 ? o.stackedData[l] : null;
    }
  }
  return null;
}, E2 = function(t) {
  return t.reduce(function(r, n) {
    return [zl(n.concat([r[0]]).filter(me)), Ul(n.concat([r[1]]).filter(me))];
  }, [1 / 0, -1 / 0]);
}, kk = function(t, r, n) {
  return Object.keys(t).reduce(function(a, i) {
    var o = t[i], l = o.stackedData, c = l.reduce(function(u, d) {
      var f = E2(d.slice(r, n + 1));
      return [Math.min(u[0], f[0]), Math.max(u[1], f[1])];
    }, [1 / 0, -1 / 0]);
    return [Math.min(c[0], a[0]), Math.max(c[1], a[1])];
  }, [1 / 0, -1 / 0]).map(function(a) {
    return a === 1 / 0 || a === -1 / 0 ? 0 : a;
  });
}, Cx = /^dataMin[\s]*-[\s]*([0-9]+([.]{1}[0-9]+){0,1})$/, Tx = /^dataMax[\s]*\+[\s]*([0-9]+([.]{1}[0-9]+){0,1})$/, oh = function(t, r, n) {
  if ($e(t))
    return t(r, n);
  if (!Array.isArray(t))
    return r;
  var a = [];
  if (me(t[0]))
    a[0] = n ? t[0] : Math.min(t[0], r[0]);
  else if (Cx.test(t[0])) {
    var i = +Cx.exec(t[0])[1];
    a[0] = r[0] - i;
  } else $e(t[0]) ? a[0] = t[0](r[0]) : a[0] = r[0];
  if (me(t[1]))
    a[1] = n ? t[1] : Math.max(t[1], r[1]);
  else if (Tx.test(t[1])) {
    var o = +Tx.exec(t[1])[1];
    a[1] = r[1] + o;
  } else $e(t[1]) ? a[1] = t[1](r[1]) : a[1] = r[1];
  return a;
}, Us = function(t, r, n) {
  if (t && t.scale && t.scale.bandwidth) {
    var a = t.scale.bandwidth();
    if (!n || a > 0)
      return a;
  }
  if (t && r && r.length >= 2) {
    for (var i = bp(r, function(f) {
      return f.coordinate;
    }), o = 1 / 0, l = 1, c = i.length; l < c; l++) {
      var u = i[l], d = i[l - 1];
      o = Math.min((u.coordinate || 0) - (d.coordinate || 0), o);
    }
    return o === 1 / 0 ? 0 : o;
  }
  return n ? void 0 : 0;
}, Px = function(t, r, n) {
  return !t || !t.length || Bl(t, pr(n, "type.defaultProps.domain")) ? r : t;
}, Ek = function(t, r) {
  var n = t.type.defaultProps ? ft(ft({}, t.type.defaultProps), t.props) : t.props, a = n.dataKey, i = n.name, o = n.unit, l = n.formatter, c = n.tooltipType, u = n.chartType, d = n.hide;
  return ft(ft({}, ze(t, !1)), {}, {
    dataKey: a,
    unit: o,
    formatter: l,
    name: i || a,
    color: Vp(t),
    value: ir(r, a),
    type: c,
    payload: r,
    chartType: u,
    hide: d
  });
};
function no(e) {
  "@babel/helpers - typeof";
  return no = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, no(e);
}
function Ox(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function Nx(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Ox(Object(r), !0).forEach(function(n) {
      A2(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Ox(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function A2(e, t, r) {
  return t = C2(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function C2(e) {
  var t = T2(e, "string");
  return no(t) == "symbol" ? t : t + "";
}
function T2(e, t) {
  if (no(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (no(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
var zs = Math.PI / 180, P2 = function(t) {
  return t * 180 / Math.PI;
}, $t = function(t, r, n, a) {
  return {
    x: t + Math.cos(-zs * a) * n,
    y: r + Math.sin(-zs * a) * n
  };
}, O2 = function(t, r) {
  var n = t.x, a = t.y, i = r.x, o = r.y;
  return Math.sqrt(Math.pow(n - i, 2) + Math.pow(a - o, 2));
}, N2 = function(t, r) {
  var n = t.x, a = t.y, i = r.cx, o = r.cy, l = O2({
    x: n,
    y: a
  }, {
    x: i,
    y: o
  });
  if (l <= 0)
    return {
      radius: l
    };
  var c = (n - i) / l, u = Math.acos(c);
  return a > o && (u = 2 * Math.PI - u), {
    radius: l,
    angle: P2(u),
    angleInRadian: u
  };
}, M2 = function(t) {
  var r = t.startAngle, n = t.endAngle, a = Math.floor(r / 360), i = Math.floor(n / 360), o = Math.min(a, i);
  return {
    startAngle: r - o * 360,
    endAngle: n - o * 360
  };
}, R2 = function(t, r) {
  var n = r.startAngle, a = r.endAngle, i = Math.floor(n / 360), o = Math.floor(a / 360), l = Math.min(i, o);
  return t + l * 360;
}, Mx = function(t, r) {
  var n = t.x, a = t.y, i = N2({
    x: n,
    y: a
  }, r), o = i.radius, l = i.angle, c = r.innerRadius, u = r.outerRadius;
  if (o < c || o > u)
    return !1;
  if (o === 0)
    return !0;
  var d = M2(r), f = d.startAngle, p = d.endAngle, m = l, y;
  if (f <= p) {
    for (; m > p; )
      m -= 360;
    for (; m < f; )
      m += 360;
    y = m >= f && m <= p;
  } else {
    for (; m > f; )
      m -= 360;
    for (; m < p; )
      m += 360;
    y = m >= p && m <= f;
  }
  return y ? Nx(Nx({}, r), {}, {
    radius: o,
    angle: R2(m, r)
  }) : null;
};
function ao(e) {
  "@babel/helpers - typeof";
  return ao = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, ao(e);
}
var I2 = ["offset"];
function D2(e) {
  return q2(e) || F2(e) || L2(e) || $2();
}
function $2() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function L2(e, t) {
  if (e) {
    if (typeof e == "string") return sh(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return sh(e, t);
  }
}
function F2(e) {
  if (typeof Symbol < "u" && e[Symbol.iterator] != null || e["@@iterator"] != null) return Array.from(e);
}
function q2(e) {
  if (Array.isArray(e)) return sh(e);
}
function sh(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function U2(e, t) {
  if (e == null) return {};
  var r = z2(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function z2(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
function Rx(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function At(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Rx(Object(r), !0).forEach(function(n) {
      B2(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Rx(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function B2(e, t, r) {
  return t = W2(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function W2(e) {
  var t = V2(e, "string");
  return ao(t) == "symbol" ? t : t + "";
}
function V2(e, t) {
  if (ao(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (ao(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function io() {
  return io = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, io.apply(this, arguments);
}
var H2 = function(t) {
  var r = t.value, n = t.formatter, a = Le(t.children) ? r : t.children;
  return $e(n) ? n(a) : a;
}, G2 = function(t, r) {
  var n = Ar(r - t), a = Math.min(Math.abs(r - t), 360);
  return n * a;
}, K2 = function(t, r, n) {
  var a = t.position, i = t.viewBox, o = t.offset, l = t.className, c = i, u = c.cx, d = c.cy, f = c.innerRadius, p = c.outerRadius, m = c.startAngle, y = c.endAngle, h = c.clockWise, g = (f + p) / 2, v = G2(m, y), b = v >= 0 ? 1 : -1, w, j;
  a === "insideStart" ? (w = m + b * o, j = h) : a === "insideEnd" ? (w = y - b * o, j = !h) : a === "end" && (w = y + b * o, j = h), j = v <= 0 ? j : !j;
  var x = $t(u, d, g, w), _ = $t(u, d, g, w + (j ? 1 : -1) * 359), S = "M".concat(x.x, ",").concat(x.y, `
    A`).concat(g, ",").concat(g, ",0,1,").concat(j ? 0 : 1, `,
    `).concat(_.x, ",").concat(_.y), k = Le(t.id) ? Ao("recharts-radial-line-") : t.id;
  return /* @__PURE__ */ I.createElement("text", io({}, n, {
    dominantBaseline: "central",
    className: Ve("recharts-radial-bar-label", l)
  }), /* @__PURE__ */ I.createElement("defs", null, /* @__PURE__ */ I.createElement("path", {
    id: k,
    d: S
  })), /* @__PURE__ */ I.createElement("textPath", {
    xlinkHref: "#".concat(k)
  }, r));
}, Y2 = function(t) {
  var r = t.viewBox, n = t.offset, a = t.position, i = r, o = i.cx, l = i.cy, c = i.innerRadius, u = i.outerRadius, d = i.startAngle, f = i.endAngle, p = (d + f) / 2;
  if (a === "outside") {
    var m = $t(o, l, u + n, p), y = m.x, h = m.y;
    return {
      x: y,
      y: h,
      textAnchor: y >= o ? "start" : "end",
      verticalAnchor: "middle"
    };
  }
  if (a === "center")
    return {
      x: o,
      y: l,
      textAnchor: "middle",
      verticalAnchor: "middle"
    };
  if (a === "centerTop")
    return {
      x: o,
      y: l,
      textAnchor: "middle",
      verticalAnchor: "start"
    };
  if (a === "centerBottom")
    return {
      x: o,
      y: l,
      textAnchor: "middle",
      verticalAnchor: "end"
    };
  var g = (c + u) / 2, v = $t(o, l, g, p), b = v.x, w = v.y;
  return {
    x: b,
    y: w,
    textAnchor: "middle",
    verticalAnchor: "middle"
  };
}, X2 = function(t) {
  var r = t.viewBox, n = t.parentViewBox, a = t.offset, i = t.position, o = r, l = o.x, c = o.y, u = o.width, d = o.height, f = d >= 0 ? 1 : -1, p = f * a, m = f > 0 ? "end" : "start", y = f > 0 ? "start" : "end", h = u >= 0 ? 1 : -1, g = h * a, v = h > 0 ? "end" : "start", b = h > 0 ? "start" : "end";
  if (i === "top") {
    var w = {
      x: l + u / 2,
      y: c - f * a,
      textAnchor: "middle",
      verticalAnchor: m
    };
    return At(At({}, w), n ? {
      height: Math.max(c - n.y, 0),
      width: u
    } : {});
  }
  if (i === "bottom") {
    var j = {
      x: l + u / 2,
      y: c + d + p,
      textAnchor: "middle",
      verticalAnchor: y
    };
    return At(At({}, j), n ? {
      height: Math.max(n.y + n.height - (c + d), 0),
      width: u
    } : {});
  }
  if (i === "left") {
    var x = {
      x: l - g,
      y: c + d / 2,
      textAnchor: v,
      verticalAnchor: "middle"
    };
    return At(At({}, x), n ? {
      width: Math.max(x.x - n.x, 0),
      height: d
    } : {});
  }
  if (i === "right") {
    var _ = {
      x: l + u + g,
      y: c + d / 2,
      textAnchor: b,
      verticalAnchor: "middle"
    };
    return At(At({}, _), n ? {
      width: Math.max(n.x + n.width - _.x, 0),
      height: d
    } : {});
  }
  var S = n ? {
    width: u,
    height: d
  } : {};
  return i === "insideLeft" ? At({
    x: l + g,
    y: c + d / 2,
    textAnchor: b,
    verticalAnchor: "middle"
  }, S) : i === "insideRight" ? At({
    x: l + u - g,
    y: c + d / 2,
    textAnchor: v,
    verticalAnchor: "middle"
  }, S) : i === "insideTop" ? At({
    x: l + u / 2,
    y: c + p,
    textAnchor: "middle",
    verticalAnchor: y
  }, S) : i === "insideBottom" ? At({
    x: l + u / 2,
    y: c + d - p,
    textAnchor: "middle",
    verticalAnchor: m
  }, S) : i === "insideTopLeft" ? At({
    x: l + g,
    y: c + p,
    textAnchor: b,
    verticalAnchor: y
  }, S) : i === "insideTopRight" ? At({
    x: l + u - g,
    y: c + p,
    textAnchor: v,
    verticalAnchor: y
  }, S) : i === "insideBottomLeft" ? At({
    x: l + g,
    y: c + d - p,
    textAnchor: b,
    verticalAnchor: m
  }, S) : i === "insideBottomRight" ? At({
    x: l + u - g,
    y: c + d - p,
    textAnchor: v,
    verticalAnchor: m
  }, S) : ti(i) && (me(i.x) || In(i.x)) && (me(i.y) || In(i.y)) ? At({
    x: l + Vn(i.x, u),
    y: c + Vn(i.y, d),
    textAnchor: "end",
    verticalAnchor: "end"
  }, S) : At({
    x: l + u / 2,
    y: c + d / 2,
    textAnchor: "middle",
    verticalAnchor: "middle"
  }, S);
}, Q2 = function(t) {
  return "cx" in t && me(t.cx);
};
function qt(e) {
  var t = e.offset, r = t === void 0 ? 5 : t, n = U2(e, I2), a = At({
    offset: r
  }, n), i = a.viewBox, o = a.position, l = a.value, c = a.children, u = a.content, d = a.className, f = d === void 0 ? "" : d, p = a.textBreakAll;
  if (!i || Le(l) && Le(c) && !/* @__PURE__ */ Cr(u) && !$e(u))
    return null;
  if (/* @__PURE__ */ Cr(u))
    return /* @__PURE__ */ Tt(u, a);
  var m;
  if ($e(u)) {
    if (m = /* @__PURE__ */ z_(u, a), /* @__PURE__ */ Cr(m))
      return m;
  } else
    m = H2(a);
  var y = Q2(i), h = ze(a, !0);
  if (y && (o === "insideStart" || o === "insideEnd" || o === "end"))
    return K2(a, m, h);
  var g = y ? Y2(a) : X2(a);
  return /* @__PURE__ */ I.createElement(As, io({
    className: Ve("recharts-label", f)
  }, h, g, {
    breakAll: p
  }), m);
}
qt.displayName = "Label";
var Ak = function(t) {
  var r = t.cx, n = t.cy, a = t.angle, i = t.startAngle, o = t.endAngle, l = t.r, c = t.radius, u = t.innerRadius, d = t.outerRadius, f = t.x, p = t.y, m = t.top, y = t.left, h = t.width, g = t.height, v = t.clockWise, b = t.labelViewBox;
  if (b)
    return b;
  if (me(h) && me(g)) {
    if (me(f) && me(p))
      return {
        x: f,
        y: p,
        width: h,
        height: g
      };
    if (me(m) && me(y))
      return {
        x: m,
        y,
        width: h,
        height: g
      };
  }
  return me(f) && me(p) ? {
    x: f,
    y: p,
    width: 0,
    height: 0
  } : me(r) && me(n) ? {
    cx: r,
    cy: n,
    startAngle: i || a || 0,
    endAngle: o || a || 0,
    innerRadius: u || 0,
    outerRadius: d || c || l || 0,
    clockWise: v
  } : t.viewBox ? t.viewBox : {};
}, J2 = function(t, r) {
  return t ? t === !0 ? /* @__PURE__ */ I.createElement(qt, {
    key: "label-implicit",
    viewBox: r
  }) : Pt(t) ? /* @__PURE__ */ I.createElement(qt, {
    key: "label-implicit",
    viewBox: r,
    value: t
  }) : /* @__PURE__ */ Cr(t) ? t.type === qt ? /* @__PURE__ */ Tt(t, {
    key: "label-implicit",
    viewBox: r
  }) : /* @__PURE__ */ I.createElement(qt, {
    key: "label-implicit",
    content: t,
    viewBox: r
  }) : $e(t) ? /* @__PURE__ */ I.createElement(qt, {
    key: "label-implicit",
    content: t,
    viewBox: r
  }) : ti(t) ? /* @__PURE__ */ I.createElement(qt, io({
    viewBox: r
  }, t, {
    key: "label-implicit"
  })) : null : null;
}, Z2 = function(t, r) {
  var n = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
  if (!t || !t.children && n && !t.label)
    return null;
  var a = t.children, i = Ak(t), o = gr(a, qt).map(function(c, u) {
    return /* @__PURE__ */ Tt(c, {
      viewBox: r || i,
      // eslint-disable-next-line react/no-array-index-key
      key: "label-".concat(u)
    });
  });
  if (!n)
    return o;
  var l = J2(t.label, r || i);
  return [l].concat(D2(o));
};
qt.parseViewBox = Ak;
qt.renderCallByParent = Z2;
var Lf, Ix;
function e$() {
  if (Ix) return Lf;
  Ix = 1;
  function e(t) {
    var r = t == null ? 0 : t.length;
    return r ? t[r - 1] : void 0;
  }
  return Lf = e, Lf;
}
var t$ = e$();
const r$ = /* @__PURE__ */ st(t$);
function oo(e) {
  "@babel/helpers - typeof";
  return oo = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, oo(e);
}
var n$ = ["valueAccessor"], a$ = ["data", "dataKey", "clockWise", "id", "textBreakAll"];
function i$(e) {
  return c$(e) || l$(e) || s$(e) || o$();
}
function o$() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function s$(e, t) {
  if (e) {
    if (typeof e == "string") return lh(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return lh(e, t);
  }
}
function l$(e) {
  if (typeof Symbol < "u" && e[Symbol.iterator] != null || e["@@iterator"] != null) return Array.from(e);
}
function c$(e) {
  if (Array.isArray(e)) return lh(e);
}
function lh(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function Bs() {
  return Bs = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Bs.apply(this, arguments);
}
function Dx(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function $x(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Dx(Object(r), !0).forEach(function(n) {
      u$(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Dx(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function u$(e, t, r) {
  return t = d$(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function d$(e) {
  var t = f$(e, "string");
  return oo(t) == "symbol" ? t : t + "";
}
function f$(e, t) {
  if (oo(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (oo(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function Lx(e, t) {
  if (e == null) return {};
  var r = m$(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function m$(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
var h$ = function(t) {
  return Array.isArray(t.value) ? r$(t.value) : t.value;
};
function bn(e) {
  var t = e.valueAccessor, r = t === void 0 ? h$ : t, n = Lx(e, n$), a = n.data, i = n.dataKey, o = n.clockWise, l = n.id, c = n.textBreakAll, u = Lx(n, a$);
  return !a || !a.length ? null : /* @__PURE__ */ I.createElement(yt, {
    className: "recharts-label-list"
  }, a.map(function(d, f) {
    var p = Le(i) ? r(d, f) : ir(d && d.payload, i), m = Le(l) ? {} : {
      id: "".concat(l, "-").concat(f)
    };
    return /* @__PURE__ */ I.createElement(qt, Bs({}, ze(d, !0), u, m, {
      parentViewBox: d.parentViewBox,
      value: p,
      textBreakAll: c,
      viewBox: qt.parseViewBox(Le(o) ? d : $x($x({}, d), {}, {
        clockWise: o
      })),
      key: "label-".concat(f),
      index: f
    }));
  }));
}
bn.displayName = "LabelList";
function p$(e, t) {
  return e ? e === !0 ? /* @__PURE__ */ I.createElement(bn, {
    key: "labelList-implicit",
    data: t
  }) : /* @__PURE__ */ I.isValidElement(e) || $e(e) ? /* @__PURE__ */ I.createElement(bn, {
    key: "labelList-implicit",
    data: t,
    content: e
  }) : ti(e) ? /* @__PURE__ */ I.createElement(bn, Bs({
    data: t
  }, e, {
    key: "labelList-implicit"
  })) : null : null;
}
function g$(e, t) {
  var r = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
  if (!e || !e.children && r && !e.label)
    return null;
  var n = e.children, a = gr(n, bn).map(function(o, l) {
    return /* @__PURE__ */ Tt(o, {
      data: t,
      // eslint-disable-next-line react/no-array-index-key
      key: "labelList-".concat(l)
    });
  });
  if (!r)
    return a;
  var i = p$(e.label, t);
  return [i].concat(i$(a));
}
bn.renderCallByParent = g$;
function so(e) {
  "@babel/helpers - typeof";
  return so = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, so(e);
}
function ch() {
  return ch = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, ch.apply(this, arguments);
}
function Fx(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function qx(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Fx(Object(r), !0).forEach(function(n) {
      y$(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Fx(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function y$(e, t, r) {
  return t = v$(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function v$(e) {
  var t = b$(e, "string");
  return so(t) == "symbol" ? t : t + "";
}
function b$(e, t) {
  if (so(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (so(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
var x$ = function(t, r) {
  var n = Ar(r - t), a = Math.min(Math.abs(r - t), 359.999);
  return n * a;
}, ns = function(t) {
  var r = t.cx, n = t.cy, a = t.radius, i = t.angle, o = t.sign, l = t.isExternal, c = t.cornerRadius, u = t.cornerIsExternal, d = c * (l ? 1 : -1) + a, f = Math.asin(c / d) / zs, p = u ? i : i + o * f, m = $t(r, n, d, p), y = $t(r, n, a, p), h = u ? i - o * f : i, g = $t(r, n, d * Math.cos(f * zs), h);
  return {
    center: m,
    circleTangency: y,
    lineTangency: g,
    theta: f
  };
}, Ck = function(t) {
  var r = t.cx, n = t.cy, a = t.innerRadius, i = t.outerRadius, o = t.startAngle, l = t.endAngle, c = x$(o, l), u = o + c, d = $t(r, n, i, o), f = $t(r, n, i, u), p = "M ".concat(d.x, ",").concat(d.y, `
    A `).concat(i, ",").concat(i, `,0,
    `).concat(+(Math.abs(c) > 180), ",").concat(+(o > u), `,
    `).concat(f.x, ",").concat(f.y, `
  `);
  if (a > 0) {
    var m = $t(r, n, a, o), y = $t(r, n, a, u);
    p += "L ".concat(y.x, ",").concat(y.y, `
            A `).concat(a, ",").concat(a, `,0,
            `).concat(+(Math.abs(c) > 180), ",").concat(+(o <= u), `,
            `).concat(m.x, ",").concat(m.y, " Z");
  } else
    p += "L ".concat(r, ",").concat(n, " Z");
  return p;
}, w$ = function(t) {
  var r = t.cx, n = t.cy, a = t.innerRadius, i = t.outerRadius, o = t.cornerRadius, l = t.forceCornerRadius, c = t.cornerIsExternal, u = t.startAngle, d = t.endAngle, f = Ar(d - u), p = ns({
    cx: r,
    cy: n,
    radius: i,
    angle: u,
    sign: f,
    cornerRadius: o,
    cornerIsExternal: c
  }), m = p.circleTangency, y = p.lineTangency, h = p.theta, g = ns({
    cx: r,
    cy: n,
    radius: i,
    angle: d,
    sign: -f,
    cornerRadius: o,
    cornerIsExternal: c
  }), v = g.circleTangency, b = g.lineTangency, w = g.theta, j = c ? Math.abs(u - d) : Math.abs(u - d) - h - w;
  if (j < 0)
    return l ? "M ".concat(y.x, ",").concat(y.y, `
        a`).concat(o, ",").concat(o, ",0,0,1,").concat(o * 2, `,0
        a`).concat(o, ",").concat(o, ",0,0,1,").concat(-o * 2, `,0
      `) : Ck({
      cx: r,
      cy: n,
      innerRadius: a,
      outerRadius: i,
      startAngle: u,
      endAngle: d
    });
  var x = "M ".concat(y.x, ",").concat(y.y, `
    A`).concat(o, ",").concat(o, ",0,0,").concat(+(f < 0), ",").concat(m.x, ",").concat(m.y, `
    A`).concat(i, ",").concat(i, ",0,").concat(+(j > 180), ",").concat(+(f < 0), ",").concat(v.x, ",").concat(v.y, `
    A`).concat(o, ",").concat(o, ",0,0,").concat(+(f < 0), ",").concat(b.x, ",").concat(b.y, `
  `);
  if (a > 0) {
    var _ = ns({
      cx: r,
      cy: n,
      radius: a,
      angle: u,
      sign: f,
      isExternal: !0,
      cornerRadius: o,
      cornerIsExternal: c
    }), S = _.circleTangency, k = _.lineTangency, E = _.theta, L = ns({
      cx: r,
      cy: n,
      radius: a,
      angle: d,
      sign: -f,
      isExternal: !0,
      cornerRadius: o,
      cornerIsExternal: c
    }), P = L.circleTangency, A = L.lineTangency, T = L.theta, M = c ? Math.abs(u - d) : Math.abs(u - d) - E - T;
    if (M < 0 && o === 0)
      return "".concat(x, "L").concat(r, ",").concat(n, "Z");
    x += "L".concat(A.x, ",").concat(A.y, `
      A`).concat(o, ",").concat(o, ",0,0,").concat(+(f < 0), ",").concat(P.x, ",").concat(P.y, `
      A`).concat(a, ",").concat(a, ",0,").concat(+(M > 180), ",").concat(+(f > 0), ",").concat(S.x, ",").concat(S.y, `
      A`).concat(o, ",").concat(o, ",0,0,").concat(+(f < 0), ",").concat(k.x, ",").concat(k.y, "Z");
  } else
    x += "L".concat(r, ",").concat(n, "Z");
  return x;
}, _$ = {
  cx: 0,
  cy: 0,
  innerRadius: 0,
  outerRadius: 0,
  startAngle: 0,
  endAngle: 0,
  cornerRadius: 0,
  forceCornerRadius: !1,
  cornerIsExternal: !1
}, Tk = function(t) {
  var r = qx(qx({}, _$), t), n = r.cx, a = r.cy, i = r.innerRadius, o = r.outerRadius, l = r.cornerRadius, c = r.forceCornerRadius, u = r.cornerIsExternal, d = r.startAngle, f = r.endAngle, p = r.className;
  if (o < i || d === f)
    return null;
  var m = Ve("recharts-sector", p), y = o - i, h = Vn(l, y, 0, !0), g;
  return h > 0 && Math.abs(d - f) < 360 ? g = w$({
    cx: n,
    cy: a,
    innerRadius: i,
    outerRadius: o,
    cornerRadius: Math.min(h, y / 2),
    forceCornerRadius: c,
    cornerIsExternal: u,
    startAngle: d,
    endAngle: f
  }) : g = Ck({
    cx: n,
    cy: a,
    innerRadius: i,
    outerRadius: o,
    startAngle: d,
    endAngle: f
  }), /* @__PURE__ */ I.createElement("path", ch({}, ze(r, !0), {
    className: m,
    d: g,
    role: "img"
  }));
};
function lo(e) {
  "@babel/helpers - typeof";
  return lo = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, lo(e);
}
function uh() {
  return uh = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, uh.apply(this, arguments);
}
function Ux(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function zx(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Ux(Object(r), !0).forEach(function(n) {
      j$(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Ux(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function j$(e, t, r) {
  return t = S$(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function S$(e) {
  var t = k$(e, "string");
  return lo(t) == "symbol" ? t : t + "";
}
function k$(e, t) {
  if (lo(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (lo(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
var Bx = {
  curveBasisClosed: WT,
  curveBasisOpen: VT,
  curveBasis: BT,
  curveBumpX: TT,
  curveBumpY: PT,
  curveLinearClosed: HT,
  curveLinear: Pl,
  curveMonotoneX: GT,
  curveMonotoneY: KT,
  curveNatural: YT,
  curveStep: XT,
  curveStepAfter: JT,
  curveStepBefore: QT
}, as = function(t) {
  return t.x === +t.x && t.y === +t.y;
}, Si = function(t) {
  return t.x;
}, ki = function(t) {
  return t.y;
}, E$ = function(t, r) {
  if ($e(t))
    return t;
  var n = "curve".concat(Cl(t));
  return (n === "curveMonotone" || n === "curveBump") && r ? Bx["".concat(n).concat(r === "vertical" ? "Y" : "X")] : Bx[n] || Pl;
}, A$ = function(t) {
  var r = t.type, n = r === void 0 ? "linear" : r, a = t.points, i = a === void 0 ? [] : a, o = t.baseLine, l = t.layout, c = t.connectNulls, u = c === void 0 ? !1 : c, d = E$(n, l), f = u ? i.filter(function(h) {
    return as(h);
  }) : i, p;
  if (Array.isArray(o)) {
    var m = u ? o.filter(function(h) {
      return as(h);
    }) : o, y = f.map(function(h, g) {
      return zx(zx({}, h), {}, {
        base: m[g]
      });
    });
    return l === "vertical" ? p = Yo().y(ki).x1(Si).x0(function(h) {
      return h.base.x;
    }) : p = Yo().x(Si).y1(ki).y0(function(h) {
      return h.base.y;
    }), p.defined(as).curve(d), p(y);
  }
  return l === "vertical" && me(o) ? p = Yo().y(ki).x1(Si).x0(o) : me(o) ? p = Yo().x(Si).y1(ki).y0(o) : p = Mj().x(Si).y(ki), p.defined(as).curve(d), p(f);
}, dh = function(t) {
  var r = t.className, n = t.points, a = t.path, i = t.pathRef;
  if ((!n || !n.length) && !a)
    return null;
  var o = n && n.length ? A$(t) : a;
  return /* @__PURE__ */ xn.createElement("path", uh({}, ze(t, !1), ps(t), {
    className: Ve("recharts-curve", r),
    d: o,
    ref: i
  }));
}, is = { exports: {} }, os = { exports: {} }, Xe = {};
/** @license React v16.13.1
 * react-is.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var Wx;
function C$() {
  if (Wx) return Xe;
  Wx = 1;
  var e = typeof Symbol == "function" && Symbol.for, t = e ? Symbol.for("react.element") : 60103, r = e ? Symbol.for("react.portal") : 60106, n = e ? Symbol.for("react.fragment") : 60107, a = e ? Symbol.for("react.strict_mode") : 60108, i = e ? Symbol.for("react.profiler") : 60114, o = e ? Symbol.for("react.provider") : 60109, l = e ? Symbol.for("react.context") : 60110, c = e ? Symbol.for("react.async_mode") : 60111, u = e ? Symbol.for("react.concurrent_mode") : 60111, d = e ? Symbol.for("react.forward_ref") : 60112, f = e ? Symbol.for("react.suspense") : 60113, p = e ? Symbol.for("react.suspense_list") : 60120, m = e ? Symbol.for("react.memo") : 60115, y = e ? Symbol.for("react.lazy") : 60116, h = e ? Symbol.for("react.block") : 60121, g = e ? Symbol.for("react.fundamental") : 60117, v = e ? Symbol.for("react.responder") : 60118, b = e ? Symbol.for("react.scope") : 60119;
  function w(x) {
    if (typeof x == "object" && x !== null) {
      var _ = x.$$typeof;
      switch (_) {
        case t:
          switch (x = x.type, x) {
            case c:
            case u:
            case n:
            case i:
            case a:
            case f:
              return x;
            default:
              switch (x = x && x.$$typeof, x) {
                case l:
                case d:
                case y:
                case m:
                case o:
                  return x;
                default:
                  return _;
              }
          }
        case r:
          return _;
      }
    }
  }
  function j(x) {
    return w(x) === u;
  }
  return Xe.AsyncMode = c, Xe.ConcurrentMode = u, Xe.ContextConsumer = l, Xe.ContextProvider = o, Xe.Element = t, Xe.ForwardRef = d, Xe.Fragment = n, Xe.Lazy = y, Xe.Memo = m, Xe.Portal = r, Xe.Profiler = i, Xe.StrictMode = a, Xe.Suspense = f, Xe.isAsyncMode = function(x) {
    return j(x) || w(x) === c;
  }, Xe.isConcurrentMode = j, Xe.isContextConsumer = function(x) {
    return w(x) === l;
  }, Xe.isContextProvider = function(x) {
    return w(x) === o;
  }, Xe.isElement = function(x) {
    return typeof x == "object" && x !== null && x.$$typeof === t;
  }, Xe.isForwardRef = function(x) {
    return w(x) === d;
  }, Xe.isFragment = function(x) {
    return w(x) === n;
  }, Xe.isLazy = function(x) {
    return w(x) === y;
  }, Xe.isMemo = function(x) {
    return w(x) === m;
  }, Xe.isPortal = function(x) {
    return w(x) === r;
  }, Xe.isProfiler = function(x) {
    return w(x) === i;
  }, Xe.isStrictMode = function(x) {
    return w(x) === a;
  }, Xe.isSuspense = function(x) {
    return w(x) === f;
  }, Xe.isValidElementType = function(x) {
    return typeof x == "string" || typeof x == "function" || x === n || x === u || x === i || x === a || x === f || x === p || typeof x == "object" && x !== null && (x.$$typeof === y || x.$$typeof === m || x.$$typeof === o || x.$$typeof === l || x.$$typeof === d || x.$$typeof === g || x.$$typeof === v || x.$$typeof === b || x.$$typeof === h);
  }, Xe.typeOf = w, Xe;
}
var Qe = {};
/** @license React v16.13.1
 * react-is.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var Vx;
function T$() {
  return Vx || (Vx = 1, process.env.NODE_ENV !== "production" && (function() {
    var e = typeof Symbol == "function" && Symbol.for, t = e ? Symbol.for("react.element") : 60103, r = e ? Symbol.for("react.portal") : 60106, n = e ? Symbol.for("react.fragment") : 60107, a = e ? Symbol.for("react.strict_mode") : 60108, i = e ? Symbol.for("react.profiler") : 60114, o = e ? Symbol.for("react.provider") : 60109, l = e ? Symbol.for("react.context") : 60110, c = e ? Symbol.for("react.async_mode") : 60111, u = e ? Symbol.for("react.concurrent_mode") : 60111, d = e ? Symbol.for("react.forward_ref") : 60112, f = e ? Symbol.for("react.suspense") : 60113, p = e ? Symbol.for("react.suspense_list") : 60120, m = e ? Symbol.for("react.memo") : 60115, y = e ? Symbol.for("react.lazy") : 60116, h = e ? Symbol.for("react.block") : 60121, g = e ? Symbol.for("react.fundamental") : 60117, v = e ? Symbol.for("react.responder") : 60118, b = e ? Symbol.for("react.scope") : 60119;
    function w($) {
      return typeof $ == "string" || typeof $ == "function" || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
      $ === n || $ === u || $ === i || $ === a || $ === f || $ === p || typeof $ == "object" && $ !== null && ($.$$typeof === y || $.$$typeof === m || $.$$typeof === o || $.$$typeof === l || $.$$typeof === d || $.$$typeof === g || $.$$typeof === v || $.$$typeof === b || $.$$typeof === h);
    }
    function j($) {
      if (typeof $ == "object" && $ !== null) {
        var Ee = $.$$typeof;
        switch (Ee) {
          case t:
            var re = $.type;
            switch (re) {
              case c:
              case u:
              case n:
              case i:
              case a:
              case f:
                return re;
              default:
                var Pe = re && re.$$typeof;
                switch (Pe) {
                  case l:
                  case d:
                  case y:
                  case m:
                  case o:
                    return Pe;
                  default:
                    return Ee;
                }
            }
          case r:
            return Ee;
        }
      }
    }
    var x = c, _ = u, S = l, k = o, E = t, L = d, P = n, A = y, T = m, M = r, N = i, q = a, V = f, B = !1;
    function ee($) {
      return B || (B = !0, console.warn("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.")), U($) || j($) === c;
    }
    function U($) {
      return j($) === u;
    }
    function G($) {
      return j($) === l;
    }
    function Y($) {
      return j($) === o;
    }
    function Q($) {
      return typeof $ == "object" && $ !== null && $.$$typeof === t;
    }
    function ne($) {
      return j($) === d;
    }
    function z($) {
      return j($) === n;
    }
    function se($) {
      return j($) === y;
    }
    function oe($) {
      return j($) === m;
    }
    function ie($) {
      return j($) === r;
    }
    function K($) {
      return j($) === i;
    }
    function ae($) {
      return j($) === a;
    }
    function fe($) {
      return j($) === f;
    }
    Qe.AsyncMode = x, Qe.ConcurrentMode = _, Qe.ContextConsumer = S, Qe.ContextProvider = k, Qe.Element = E, Qe.ForwardRef = L, Qe.Fragment = P, Qe.Lazy = A, Qe.Memo = T, Qe.Portal = M, Qe.Profiler = N, Qe.StrictMode = q, Qe.Suspense = V, Qe.isAsyncMode = ee, Qe.isConcurrentMode = U, Qe.isContextConsumer = G, Qe.isContextProvider = Y, Qe.isElement = Q, Qe.isForwardRef = ne, Qe.isFragment = z, Qe.isLazy = se, Qe.isMemo = oe, Qe.isPortal = ie, Qe.isProfiler = K, Qe.isStrictMode = ae, Qe.isSuspense = fe, Qe.isValidElementType = w, Qe.typeOf = j;
  })()), Qe;
}
var Hx;
function Pk() {
  return Hx || (Hx = 1, process.env.NODE_ENV === "production" ? os.exports = C$() : os.exports = T$()), os.exports;
}
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/
var Ff, Gx;
function P$() {
  if (Gx) return Ff;
  Gx = 1;
  var e = Object.getOwnPropertySymbols, t = Object.prototype.hasOwnProperty, r = Object.prototype.propertyIsEnumerable;
  function n(i) {
    if (i == null)
      throw new TypeError("Object.assign cannot be called with null or undefined");
    return Object(i);
  }
  function a() {
    try {
      if (!Object.assign)
        return !1;
      var i = new String("abc");
      if (i[5] = "de", Object.getOwnPropertyNames(i)[0] === "5")
        return !1;
      for (var o = {}, l = 0; l < 10; l++)
        o["_" + String.fromCharCode(l)] = l;
      var c = Object.getOwnPropertyNames(o).map(function(d) {
        return o[d];
      });
      if (c.join("") !== "0123456789")
        return !1;
      var u = {};
      return "abcdefghijklmnopqrst".split("").forEach(function(d) {
        u[d] = d;
      }), Object.keys(Object.assign({}, u)).join("") === "abcdefghijklmnopqrst";
    } catch {
      return !1;
    }
  }
  return Ff = a() ? Object.assign : function(i, o) {
    for (var l, c = n(i), u, d = 1; d < arguments.length; d++) {
      l = Object(arguments[d]);
      for (var f in l)
        t.call(l, f) && (c[f] = l[f]);
      if (e) {
        u = e(l);
        for (var p = 0; p < u.length; p++)
          r.call(l, u[p]) && (c[u[p]] = l[u[p]]);
      }
    }
    return c;
  }, Ff;
}
var qf, Kx;
function Hp() {
  if (Kx) return qf;
  Kx = 1;
  var e = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
  return qf = e, qf;
}
var Uf, Yx;
function Ok() {
  return Yx || (Yx = 1, Uf = Function.call.bind(Object.prototype.hasOwnProperty)), Uf;
}
var zf, Xx;
function O$() {
  if (Xx) return zf;
  Xx = 1;
  var e = function() {
  };
  if (process.env.NODE_ENV !== "production") {
    var t = /* @__PURE__ */ Hp(), r = {}, n = /* @__PURE__ */ Ok();
    e = function(i) {
      var o = "Warning: " + i;
      typeof console < "u" && console.error(o);
      try {
        throw new Error(o);
      } catch {
      }
    };
  }
  function a(i, o, l, c, u) {
    if (process.env.NODE_ENV !== "production") {
      for (var d in i)
        if (n(i, d)) {
          var f;
          try {
            if (typeof i[d] != "function") {
              var p = Error(
                (c || "React class") + ": " + l + " type `" + d + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof i[d] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`."
              );
              throw p.name = "Invariant Violation", p;
            }
            f = i[d](o, d, c, l, null, t);
          } catch (y) {
            f = y;
          }
          if (f && !(f instanceof Error) && e(
            (c || "React class") + ": type specification of " + l + " `" + d + "` is invalid; the type checker function must return `null` or an `Error` but returned a " + typeof f + ". You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument)."
          ), f instanceof Error && !(f.message in r)) {
            r[f.message] = !0;
            var m = u ? u() : "";
            e(
              "Failed " + l + " type: " + f.message + (m ?? "")
            );
          }
        }
    }
  }
  return a.resetWarningCache = function() {
    process.env.NODE_ENV !== "production" && (r = {});
  }, zf = a, zf;
}
var Bf, Qx;
function N$() {
  if (Qx) return Bf;
  Qx = 1;
  var e = Pk(), t = P$(), r = /* @__PURE__ */ Hp(), n = /* @__PURE__ */ Ok(), a = /* @__PURE__ */ O$(), i = function() {
  };
  process.env.NODE_ENV !== "production" && (i = function(l) {
    var c = "Warning: " + l;
    typeof console < "u" && console.error(c);
    try {
      throw new Error(c);
    } catch {
    }
  });
  function o() {
    return null;
  }
  return Bf = function(l, c) {
    var u = typeof Symbol == "function" && Symbol.iterator, d = "@@iterator";
    function f(U) {
      var G = U && (u && U[u] || U[d]);
      if (typeof G == "function")
        return G;
    }
    var p = "<<anonymous>>", m = {
      array: v("array"),
      bigint: v("bigint"),
      bool: v("boolean"),
      func: v("function"),
      number: v("number"),
      object: v("object"),
      string: v("string"),
      symbol: v("symbol"),
      any: b(),
      arrayOf: w,
      element: j(),
      elementType: x(),
      instanceOf: _,
      node: L(),
      objectOf: k,
      oneOf: S,
      oneOfType: E,
      shape: A,
      exact: T
    };
    function y(U, G) {
      return U === G ? U !== 0 || 1 / U === 1 / G : U !== U && G !== G;
    }
    function h(U, G) {
      this.message = U, this.data = G && typeof G == "object" ? G : {}, this.stack = "";
    }
    h.prototype = Error.prototype;
    function g(U) {
      if (process.env.NODE_ENV !== "production")
        var G = {}, Y = 0;
      function Q(z, se, oe, ie, K, ae, fe) {
        if (ie = ie || p, ae = ae || oe, fe !== r) {
          if (c) {
            var $ = new Error(
              "Calling PropTypes validators directly is not supported by the `prop-types` package. Use `PropTypes.checkPropTypes()` to call them. Read more at http://fb.me/use-check-prop-types"
            );
            throw $.name = "Invariant Violation", $;
          } else if (process.env.NODE_ENV !== "production" && typeof console < "u") {
            var Ee = ie + ":" + oe;
            !G[Ee] && // Avoid spamming the console because they are often not actionable except for lib authors
            Y < 3 && (i(
              "You are manually calling a React.PropTypes validation function for the `" + ae + "` prop on `" + ie + "`. This is deprecated and will throw in the standalone `prop-types` package. You may be seeing this warning due to a third-party PropTypes library. See https://fb.me/react-warning-dont-call-proptypes for details."
            ), G[Ee] = !0, Y++);
          }
        }
        return se[oe] == null ? z ? se[oe] === null ? new h("The " + K + " `" + ae + "` is marked as required " + ("in `" + ie + "`, but its value is `null`.")) : new h("The " + K + " `" + ae + "` is marked as required in " + ("`" + ie + "`, but its value is `undefined`.")) : null : U(se, oe, ie, K, ae);
      }
      var ne = Q.bind(null, !1);
      return ne.isRequired = Q.bind(null, !0), ne;
    }
    function v(U) {
      function G(Y, Q, ne, z, se, oe) {
        var ie = Y[Q], K = q(ie);
        if (K !== U) {
          var ae = V(ie);
          return new h(
            "Invalid " + z + " `" + se + "` of type " + ("`" + ae + "` supplied to `" + ne + "`, expected ") + ("`" + U + "`."),
            { expectedType: U }
          );
        }
        return null;
      }
      return g(G);
    }
    function b() {
      return g(o);
    }
    function w(U) {
      function G(Y, Q, ne, z, se) {
        if (typeof U != "function")
          return new h("Property `" + se + "` of component `" + ne + "` has invalid PropType notation inside arrayOf.");
        var oe = Y[Q];
        if (!Array.isArray(oe)) {
          var ie = q(oe);
          return new h("Invalid " + z + " `" + se + "` of type " + ("`" + ie + "` supplied to `" + ne + "`, expected an array."));
        }
        for (var K = 0; K < oe.length; K++) {
          var ae = U(oe, K, ne, z, se + "[" + K + "]", r);
          if (ae instanceof Error)
            return ae;
        }
        return null;
      }
      return g(G);
    }
    function j() {
      function U(G, Y, Q, ne, z) {
        var se = G[Y];
        if (!l(se)) {
          var oe = q(se);
          return new h("Invalid " + ne + " `" + z + "` of type " + ("`" + oe + "` supplied to `" + Q + "`, expected a single ReactElement."));
        }
        return null;
      }
      return g(U);
    }
    function x() {
      function U(G, Y, Q, ne, z) {
        var se = G[Y];
        if (!e.isValidElementType(se)) {
          var oe = q(se);
          return new h("Invalid " + ne + " `" + z + "` of type " + ("`" + oe + "` supplied to `" + Q + "`, expected a single ReactElement type."));
        }
        return null;
      }
      return g(U);
    }
    function _(U) {
      function G(Y, Q, ne, z, se) {
        if (!(Y[Q] instanceof U)) {
          var oe = U.name || p, ie = ee(Y[Q]);
          return new h("Invalid " + z + " `" + se + "` of type " + ("`" + ie + "` supplied to `" + ne + "`, expected ") + ("instance of `" + oe + "`."));
        }
        return null;
      }
      return g(G);
    }
    function S(U) {
      if (!Array.isArray(U))
        return process.env.NODE_ENV !== "production" && (arguments.length > 1 ? i(
          "Invalid arguments supplied to oneOf, expected an array, got " + arguments.length + " arguments. A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z])."
        ) : i("Invalid argument supplied to oneOf, expected an array.")), o;
      function G(Y, Q, ne, z, se) {
        for (var oe = Y[Q], ie = 0; ie < U.length; ie++)
          if (y(oe, U[ie]))
            return null;
        var K = JSON.stringify(U, function(fe, $) {
          var Ee = V($);
          return Ee === "symbol" ? String($) : $;
        });
        return new h("Invalid " + z + " `" + se + "` of value `" + String(oe) + "` " + ("supplied to `" + ne + "`, expected one of " + K + "."));
      }
      return g(G);
    }
    function k(U) {
      function G(Y, Q, ne, z, se) {
        if (typeof U != "function")
          return new h("Property `" + se + "` of component `" + ne + "` has invalid PropType notation inside objectOf.");
        var oe = Y[Q], ie = q(oe);
        if (ie !== "object")
          return new h("Invalid " + z + " `" + se + "` of type " + ("`" + ie + "` supplied to `" + ne + "`, expected an object."));
        for (var K in oe)
          if (n(oe, K)) {
            var ae = U(oe, K, ne, z, se + "." + K, r);
            if (ae instanceof Error)
              return ae;
          }
        return null;
      }
      return g(G);
    }
    function E(U) {
      if (!Array.isArray(U))
        return process.env.NODE_ENV !== "production" && i("Invalid argument supplied to oneOfType, expected an instance of array."), o;
      for (var G = 0; G < U.length; G++) {
        var Y = U[G];
        if (typeof Y != "function")
          return i(
            "Invalid argument supplied to oneOfType. Expected an array of check functions, but received " + B(Y) + " at index " + G + "."
          ), o;
      }
      function Q(ne, z, se, oe, ie) {
        for (var K = [], ae = 0; ae < U.length; ae++) {
          var fe = U[ae], $ = fe(ne, z, se, oe, ie, r);
          if ($ == null)
            return null;
          $.data && n($.data, "expectedType") && K.push($.data.expectedType);
        }
        var Ee = K.length > 0 ? ", expected one of type [" + K.join(", ") + "]" : "";
        return new h("Invalid " + oe + " `" + ie + "` supplied to " + ("`" + se + "`" + Ee + "."));
      }
      return g(Q);
    }
    function L() {
      function U(G, Y, Q, ne, z) {
        return M(G[Y]) ? null : new h("Invalid " + ne + " `" + z + "` supplied to " + ("`" + Q + "`, expected a ReactNode."));
      }
      return g(U);
    }
    function P(U, G, Y, Q, ne) {
      return new h(
        (U || "React class") + ": " + G + " type `" + Y + "." + Q + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + ne + "`."
      );
    }
    function A(U) {
      function G(Y, Q, ne, z, se) {
        var oe = Y[Q], ie = q(oe);
        if (ie !== "object")
          return new h("Invalid " + z + " `" + se + "` of type `" + ie + "` " + ("supplied to `" + ne + "`, expected `object`."));
        for (var K in U) {
          var ae = U[K];
          if (typeof ae != "function")
            return P(ne, z, se, K, V(ae));
          var fe = ae(oe, K, ne, z, se + "." + K, r);
          if (fe)
            return fe;
        }
        return null;
      }
      return g(G);
    }
    function T(U) {
      function G(Y, Q, ne, z, se) {
        var oe = Y[Q], ie = q(oe);
        if (ie !== "object")
          return new h("Invalid " + z + " `" + se + "` of type `" + ie + "` " + ("supplied to `" + ne + "`, expected `object`."));
        var K = t({}, Y[Q], U);
        for (var ae in K) {
          var fe = U[ae];
          if (n(U, ae) && typeof fe != "function")
            return P(ne, z, se, ae, V(fe));
          if (!fe)
            return new h(
              "Invalid " + z + " `" + se + "` key `" + ae + "` supplied to `" + ne + "`.\nBad object: " + JSON.stringify(Y[Q], null, "  ") + `
Valid keys: ` + JSON.stringify(Object.keys(U), null, "  ")
            );
          var $ = fe(oe, ae, ne, z, se + "." + ae, r);
          if ($)
            return $;
        }
        return null;
      }
      return g(G);
    }
    function M(U) {
      switch (typeof U) {
        case "number":
        case "string":
        case "undefined":
          return !0;
        case "boolean":
          return !U;
        case "object":
          if (Array.isArray(U))
            return U.every(M);
          if (U === null || l(U))
            return !0;
          var G = f(U);
          if (G) {
            var Y = G.call(U), Q;
            if (G !== U.entries) {
              for (; !(Q = Y.next()).done; )
                if (!M(Q.value))
                  return !1;
            } else
              for (; !(Q = Y.next()).done; ) {
                var ne = Q.value;
                if (ne && !M(ne[1]))
                  return !1;
              }
          } else
            return !1;
          return !0;
        default:
          return !1;
      }
    }
    function N(U, G) {
      return U === "symbol" ? !0 : G ? G["@@toStringTag"] === "Symbol" || typeof Symbol == "function" && G instanceof Symbol : !1;
    }
    function q(U) {
      var G = typeof U;
      return Array.isArray(U) ? "array" : U instanceof RegExp ? "object" : N(G, U) ? "symbol" : G;
    }
    function V(U) {
      if (typeof U > "u" || U === null)
        return "" + U;
      var G = q(U);
      if (G === "object") {
        if (U instanceof Date)
          return "date";
        if (U instanceof RegExp)
          return "regexp";
      }
      return G;
    }
    function B(U) {
      var G = V(U);
      switch (G) {
        case "array":
        case "object":
          return "an " + G;
        case "boolean":
        case "date":
        case "regexp":
          return "a " + G;
        default:
          return G;
      }
    }
    function ee(U) {
      return !U.constructor || !U.constructor.name ? p : U.constructor.name;
    }
    return m.checkPropTypes = a, m.resetWarningCache = a.resetWarningCache, m.PropTypes = m, m;
  }, Bf;
}
var Wf, Jx;
function M$() {
  if (Jx) return Wf;
  Jx = 1;
  var e = /* @__PURE__ */ Hp();
  function t() {
  }
  function r() {
  }
  return r.resetWarningCache = t, Wf = function() {
    function n(o, l, c, u, d, f) {
      if (f !== e) {
        var p = new Error(
          "Calling PropTypes validators directly is not supported by the `prop-types` package. Use PropTypes.checkPropTypes() to call them. Read more at http://fb.me/use-check-prop-types"
        );
        throw p.name = "Invariant Violation", p;
      }
    }
    n.isRequired = n;
    function a() {
      return n;
    }
    var i = {
      array: n,
      bigint: n,
      bool: n,
      func: n,
      number: n,
      object: n,
      string: n,
      symbol: n,
      any: n,
      arrayOf: a,
      element: n,
      elementType: n,
      instanceOf: a,
      node: n,
      objectOf: a,
      oneOf: a,
      oneOfType: a,
      shape: a,
      exact: a,
      checkPropTypes: r,
      resetWarningCache: t
    };
    return i.PropTypes = i, i;
  }, Wf;
}
var Zx;
function R$() {
  if (Zx) return is.exports;
  if (Zx = 1, process.env.NODE_ENV !== "production") {
    var e = Pk(), t = !0;
    is.exports = /* @__PURE__ */ N$()(e.isElement, t);
  } else
    is.exports = /* @__PURE__ */ M$()();
  return is.exports;
}
var I$ = /* @__PURE__ */ R$();
const Je = /* @__PURE__ */ st(I$), { getOwnPropertyNames: D$, getOwnPropertySymbols: $$ } = Object, { hasOwnProperty: L$ } = Object.prototype;
function Vf(e, t) {
  return function(n, a, i) {
    return e(n, a, i) && t(n, a, i);
  };
}
function ss(e) {
  return function(r, n, a) {
    if (!r || !n || typeof r != "object" || typeof n != "object")
      return e(r, n, a);
    const { cache: i } = a, o = i.get(r), l = i.get(n);
    if (o && l)
      return o === n && l === r;
    i.set(r, n), i.set(n, r);
    const c = e(r, n, a);
    return i.delete(r), i.delete(n), c;
  };
}
function F$(e) {
  return e != null ? e[Symbol.toStringTag] : void 0;
}
function ew(e) {
  return D$(e).concat($$(e));
}
const q$ = (
  // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
  Object.hasOwn || ((e, t) => L$.call(e, t))
);
function Jn(e, t) {
  return e === t || !e && !t && e !== e && t !== t;
}
const U$ = "__v", z$ = "__o", B$ = "_owner", { getOwnPropertyDescriptor: tw, keys: rw } = Object;
function W$(e, t) {
  return e.byteLength === t.byteLength && Ws(new Uint8Array(e), new Uint8Array(t));
}
function V$(e, t, r) {
  let n = e.length;
  if (t.length !== n)
    return !1;
  for (; n-- > 0; )
    if (!r.equals(e[n], t[n], n, n, e, t, r))
      return !1;
  return !0;
}
function H$(e, t) {
  return e.byteLength === t.byteLength && Ws(new Uint8Array(e.buffer, e.byteOffset, e.byteLength), new Uint8Array(t.buffer, t.byteOffset, t.byteLength));
}
function G$(e, t) {
  return Jn(e.getTime(), t.getTime());
}
function K$(e, t) {
  return e.name === t.name && e.message === t.message && e.cause === t.cause && e.stack === t.stack;
}
function Y$(e, t) {
  return e === t;
}
function nw(e, t, r) {
  const n = e.size;
  if (n !== t.size)
    return !1;
  if (!n)
    return !0;
  const a = new Array(n), i = e.entries();
  let o, l, c = 0;
  for (; (o = i.next()) && !o.done; ) {
    const u = t.entries();
    let d = !1, f = 0;
    for (; (l = u.next()) && !l.done; ) {
      if (a[f]) {
        f++;
        continue;
      }
      const p = o.value, m = l.value;
      if (r.equals(p[0], m[0], c, f, e, t, r) && r.equals(p[1], m[1], p[0], m[0], e, t, r)) {
        d = a[f] = !0;
        break;
      }
      f++;
    }
    if (!d)
      return !1;
    c++;
  }
  return !0;
}
const X$ = Jn;
function Q$(e, t, r) {
  const n = rw(e);
  let a = n.length;
  if (rw(t).length !== a)
    return !1;
  for (; a-- > 0; )
    if (!Nk(e, t, r, n[a]))
      return !1;
  return !0;
}
function Ei(e, t, r) {
  const n = ew(e);
  let a = n.length;
  if (ew(t).length !== a)
    return !1;
  let i, o, l;
  for (; a-- > 0; )
    if (i = n[a], !Nk(e, t, r, i) || (o = tw(e, i), l = tw(t, i), (o || l) && (!o || !l || o.configurable !== l.configurable || o.enumerable !== l.enumerable || o.writable !== l.writable)))
      return !1;
  return !0;
}
function J$(e, t) {
  return Jn(e.valueOf(), t.valueOf());
}
function Z$(e, t) {
  return e.source === t.source && e.flags === t.flags;
}
function aw(e, t, r) {
  const n = e.size;
  if (n !== t.size)
    return !1;
  if (!n)
    return !0;
  const a = new Array(n), i = e.values();
  let o, l;
  for (; (o = i.next()) && !o.done; ) {
    const c = t.values();
    let u = !1, d = 0;
    for (; (l = c.next()) && !l.done; ) {
      if (!a[d] && r.equals(o.value, l.value, o.value, l.value, e, t, r)) {
        u = a[d] = !0;
        break;
      }
      d++;
    }
    if (!u)
      return !1;
  }
  return !0;
}
function Ws(e, t) {
  let r = e.byteLength;
  if (t.byteLength !== r || e.byteOffset !== t.byteOffset)
    return !1;
  for (; r-- > 0; )
    if (e[r] !== t[r])
      return !1;
  return !0;
}
function eL(e, t) {
  return e.hostname === t.hostname && e.pathname === t.pathname && e.protocol === t.protocol && e.port === t.port && e.hash === t.hash && e.username === t.username && e.password === t.password;
}
function Nk(e, t, r, n) {
  return (n === B$ || n === z$ || n === U$) && (e.$$typeof || t.$$typeof) ? !0 : q$(t, n) && r.equals(e[n], t[n], n, n, e, t, r);
}
const tL = "[object ArrayBuffer]", rL = "[object Arguments]", nL = "[object Boolean]", aL = "[object DataView]", iL = "[object Date]", oL = "[object Error]", sL = "[object Map]", lL = "[object Number]", cL = "[object Object]", uL = "[object RegExp]", dL = "[object Set]", fL = "[object String]", mL = {
  "[object Int8Array]": !0,
  "[object Uint8Array]": !0,
  "[object Uint8ClampedArray]": !0,
  "[object Int16Array]": !0,
  "[object Uint16Array]": !0,
  "[object Int32Array]": !0,
  "[object Uint32Array]": !0,
  "[object Float16Array]": !0,
  "[object Float32Array]": !0,
  "[object Float64Array]": !0,
  "[object BigInt64Array]": !0,
  "[object BigUint64Array]": !0
}, hL = "[object URL]", pL = Object.prototype.toString;
function gL({ areArrayBuffersEqual: e, areArraysEqual: t, areDataViewsEqual: r, areDatesEqual: n, areErrorsEqual: a, areFunctionsEqual: i, areMapsEqual: o, areNumbersEqual: l, areObjectsEqual: c, arePrimitiveWrappersEqual: u, areRegExpsEqual: d, areSetsEqual: f, areTypedArraysEqual: p, areUrlsEqual: m, unknownTagComparators: y }) {
  return function(g, v, b) {
    if (g === v)
      return !0;
    if (g == null || v == null)
      return !1;
    const w = typeof g;
    if (w !== typeof v)
      return !1;
    if (w !== "object")
      return w === "number" ? l(g, v, b) : w === "function" ? i(g, v, b) : !1;
    const j = g.constructor;
    if (j !== v.constructor)
      return !1;
    if (j === Object)
      return c(g, v, b);
    if (Array.isArray(g))
      return t(g, v, b);
    if (j === Date)
      return n(g, v, b);
    if (j === RegExp)
      return d(g, v, b);
    if (j === Map)
      return o(g, v, b);
    if (j === Set)
      return f(g, v, b);
    const x = pL.call(g);
    if (x === iL)
      return n(g, v, b);
    if (x === uL)
      return d(g, v, b);
    if (x === sL)
      return o(g, v, b);
    if (x === dL)
      return f(g, v, b);
    if (x === cL)
      return typeof g.then != "function" && typeof v.then != "function" && c(g, v, b);
    if (x === hL)
      return m(g, v, b);
    if (x === oL)
      return a(g, v, b);
    if (x === rL)
      return c(g, v, b);
    if (mL[x])
      return p(g, v, b);
    if (x === tL)
      return e(g, v, b);
    if (x === aL)
      return r(g, v, b);
    if (x === nL || x === lL || x === fL)
      return u(g, v, b);
    if (y) {
      let _ = y[x];
      if (!_) {
        const S = F$(g);
        S && (_ = y[S]);
      }
      if (_)
        return _(g, v, b);
    }
    return !1;
  };
}
function yL({ circular: e, createCustomConfig: t, strict: r }) {
  let n = {
    areArrayBuffersEqual: W$,
    areArraysEqual: r ? Ei : V$,
    areDataViewsEqual: H$,
    areDatesEqual: G$,
    areErrorsEqual: K$,
    areFunctionsEqual: Y$,
    areMapsEqual: r ? Vf(nw, Ei) : nw,
    areNumbersEqual: X$,
    areObjectsEqual: r ? Ei : Q$,
    arePrimitiveWrappersEqual: J$,
    areRegExpsEqual: Z$,
    areSetsEqual: r ? Vf(aw, Ei) : aw,
    areTypedArraysEqual: r ? Vf(Ws, Ei) : Ws,
    areUrlsEqual: eL,
    unknownTagComparators: void 0
  };
  if (t && (n = Object.assign({}, n, t(n))), e) {
    const a = ss(n.areArraysEqual), i = ss(n.areMapsEqual), o = ss(n.areObjectsEqual), l = ss(n.areSetsEqual);
    n = Object.assign({}, n, {
      areArraysEqual: a,
      areMapsEqual: i,
      areObjectsEqual: o,
      areSetsEqual: l
    });
  }
  return n;
}
function vL(e) {
  return function(t, r, n, a, i, o, l) {
    return e(t, r, l);
  };
}
function bL({ circular: e, comparator: t, createState: r, equals: n, strict: a }) {
  if (r)
    return function(l, c) {
      const { cache: u = e ? /* @__PURE__ */ new WeakMap() : void 0, meta: d } = r();
      return t(l, c, {
        cache: u,
        equals: n,
        meta: d,
        strict: a
      });
    };
  if (e)
    return function(l, c) {
      return t(l, c, {
        cache: /* @__PURE__ */ new WeakMap(),
        equals: n,
        meta: void 0,
        strict: a
      });
    };
  const i = {
    cache: void 0,
    equals: n,
    meta: void 0,
    strict: a
  };
  return function(l, c) {
    return t(l, c, i);
  };
}
const xL = En();
En({ strict: !0 });
En({ circular: !0 });
En({
  circular: !0,
  strict: !0
});
En({
  createInternalComparator: () => Jn
});
En({
  strict: !0,
  createInternalComparator: () => Jn
});
En({
  circular: !0,
  createInternalComparator: () => Jn
});
En({
  circular: !0,
  createInternalComparator: () => Jn,
  strict: !0
});
function En(e = {}) {
  const { circular: t = !1, createInternalComparator: r, createState: n, strict: a = !1 } = e, i = yL(e), o = gL(i), l = r ? r(o) : vL(o);
  return bL({ circular: t, comparator: o, createState: n, equals: l, strict: a });
}
function wL(e) {
  typeof requestAnimationFrame < "u" && requestAnimationFrame(e);
}
function iw(e) {
  var t = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0, r = -1, n = function a(i) {
    r < 0 && (r = i), i - r > t ? (e(i), r = -1) : wL(a);
  };
  requestAnimationFrame(n);
}
function fh(e) {
  "@babel/helpers - typeof";
  return fh = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, fh(e);
}
function _L(e) {
  return EL(e) || kL(e) || SL(e) || jL();
}
function jL() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function SL(e, t) {
  if (e) {
    if (typeof e == "string") return ow(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return ow(e, t);
  }
}
function ow(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function kL(e) {
  if (typeof Symbol < "u" && e[Symbol.iterator] != null || e["@@iterator"] != null) return Array.from(e);
}
function EL(e) {
  if (Array.isArray(e)) return e;
}
function AL() {
  var e = {}, t = function() {
    return null;
  }, r = !1, n = function a(i) {
    if (!r) {
      if (Array.isArray(i)) {
        if (!i.length)
          return;
        var o = i, l = _L(o), c = l[0], u = l.slice(1);
        if (typeof c == "number") {
          iw(a.bind(null, u), c);
          return;
        }
        a(c), iw(a.bind(null, u));
        return;
      }
      fh(i) === "object" && (e = i, t(e)), typeof i == "function" && i();
    }
  };
  return {
    stop: function() {
      r = !0;
    },
    start: function(i) {
      r = !1, n(i);
    },
    subscribe: function(i) {
      return t = i, function() {
        t = function() {
          return null;
        };
      };
    }
  };
}
function co(e) {
  "@babel/helpers - typeof";
  return co = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, co(e);
}
function sw(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function lw(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? sw(Object(r), !0).forEach(function(n) {
      Mk(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : sw(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function Mk(e, t, r) {
  return t = CL(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function CL(e) {
  var t = TL(e, "string");
  return co(t) === "symbol" ? t : String(t);
}
function TL(e, t) {
  if (co(e) !== "object" || e === null) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (co(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
var PL = function(t, r) {
  return [Object.keys(t), Object.keys(r)].reduce(function(n, a) {
    return n.filter(function(i) {
      return a.includes(i);
    });
  });
}, OL = function(t) {
  return t;
}, NL = function(t) {
  return t.replace(/([A-Z])/g, function(r) {
    return "-".concat(r.toLowerCase());
  });
}, Ii = function(t, r) {
  return Object.keys(r).reduce(function(n, a) {
    return lw(lw({}, n), {}, Mk({}, a, t(a, r[a])));
  }, {});
}, cw = function(t, r, n) {
  return t.map(function(a) {
    return "".concat(NL(a), " ").concat(r, "ms ").concat(n);
  }).join(",");
}, ML = process.env.NODE_ENV !== "production", Vs = function(t, r, n, a, i, o, l, c) {
  if (ML && typeof console < "u" && console.warn && (r === void 0 && console.warn("LogUtils requires an error message argument"), !t))
    if (r === void 0)
      console.warn("Minified exception occurred; use the non-minified dev environment for the full error message and additional helpful warnings.");
    else {
      var u = [n, a, i, o, l, c], d = 0;
      console.warn(r.replace(/%s/g, function() {
        return u[d++];
      }));
    }
};
function RL(e, t) {
  return $L(e) || DL(e, t) || Rk(e, t) || IL();
}
function IL() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function DL(e, t) {
  var r = e == null ? null : typeof Symbol < "u" && e[Symbol.iterator] || e["@@iterator"];
  if (r != null) {
    var n, a, i, o, l = [], c = !0, u = !1;
    try {
      if (i = (r = r.call(e)).next, t !== 0) for (; !(c = (n = i.call(r)).done) && (l.push(n.value), l.length !== t); c = !0) ;
    } catch (d) {
      u = !0, a = d;
    } finally {
      try {
        if (!c && r.return != null && (o = r.return(), Object(o) !== o)) return;
      } finally {
        if (u) throw a;
      }
    }
    return l;
  }
}
function $L(e) {
  if (Array.isArray(e)) return e;
}
function LL(e) {
  return UL(e) || qL(e) || Rk(e) || FL();
}
function FL() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function Rk(e, t) {
  if (e) {
    if (typeof e == "string") return mh(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return mh(e, t);
  }
}
function qL(e) {
  if (typeof Symbol < "u" && e[Symbol.iterator] != null || e["@@iterator"] != null) return Array.from(e);
}
function UL(e) {
  if (Array.isArray(e)) return mh(e);
}
function mh(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
var Hs = 1e-4, Ik = function(t, r) {
  return [0, 3 * t, 3 * r - 6 * t, 3 * t - 3 * r + 1];
}, Dk = function(t, r) {
  return t.map(function(n, a) {
    return n * Math.pow(r, a);
  }).reduce(function(n, a) {
    return n + a;
  });
}, uw = function(t, r) {
  return function(n) {
    var a = Ik(t, r);
    return Dk(a, n);
  };
}, zL = function(t, r) {
  return function(n) {
    var a = Ik(t, r), i = [].concat(LL(a.map(function(o, l) {
      return o * l;
    }).slice(1)), [0]);
    return Dk(i, n);
  };
}, dw = function() {
  for (var t = arguments.length, r = new Array(t), n = 0; n < t; n++)
    r[n] = arguments[n];
  var a = r[0], i = r[1], o = r[2], l = r[3];
  if (r.length === 1)
    switch (r[0]) {
      case "linear":
        a = 0, i = 0, o = 1, l = 1;
        break;
      case "ease":
        a = 0.25, i = 0.1, o = 0.25, l = 1;
        break;
      case "ease-in":
        a = 0.42, i = 0, o = 1, l = 1;
        break;
      case "ease-out":
        a = 0.42, i = 0, o = 0.58, l = 1;
        break;
      case "ease-in-out":
        a = 0, i = 0, o = 0.58, l = 1;
        break;
      default: {
        var c = r[0].split("(");
        if (c[0] === "cubic-bezier" && c[1].split(")")[0].split(",").length === 4) {
          var u = c[1].split(")")[0].split(",").map(function(g) {
            return parseFloat(g);
          }), d = RL(u, 4);
          a = d[0], i = d[1], o = d[2], l = d[3];
        } else
          Vs(!1, "[configBezier]: arguments should be one of oneOf 'linear', 'ease', 'ease-in', 'ease-out', 'ease-in-out','cubic-bezier(x1,y1,x2,y2)', instead received %s", r);
      }
    }
  Vs([a, o, i, l].every(function(g) {
    return typeof g == "number" && g >= 0 && g <= 1;
  }), "[configBezier]: arguments should be x1, y1, x2, y2 of [0, 1] instead received %s", r);
  var f = uw(a, o), p = uw(i, l), m = zL(a, o), y = function(v) {
    return v > 1 ? 1 : v < 0 ? 0 : v;
  }, h = function(v) {
    for (var b = v > 1 ? 1 : v, w = b, j = 0; j < 8; ++j) {
      var x = f(w) - b, _ = m(w);
      if (Math.abs(x - b) < Hs || _ < Hs)
        return p(w);
      w = y(w - x / _);
    }
    return p(w);
  };
  return h.isStepper = !1, h;
}, BL = function() {
  var t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, r = t.stiff, n = r === void 0 ? 100 : r, a = t.damping, i = a === void 0 ? 8 : a, o = t.dt, l = o === void 0 ? 17 : o, c = function(d, f, p) {
    var m = -(d - f) * n, y = p * i, h = p + (m - y) * l / 1e3, g = p * l / 1e3 + d;
    return Math.abs(g - f) < Hs && Math.abs(h) < Hs ? [f, 0] : [g, h];
  };
  return c.isStepper = !0, c.dt = l, c;
}, WL = function() {
  for (var t = arguments.length, r = new Array(t), n = 0; n < t; n++)
    r[n] = arguments[n];
  var a = r[0];
  if (typeof a == "string")
    switch (a) {
      case "ease":
      case "ease-in-out":
      case "ease-out":
      case "ease-in":
      case "linear":
        return dw(a);
      case "spring":
        return BL();
      default:
        if (a.split("(")[0] === "cubic-bezier")
          return dw(a);
        Vs(!1, "[configEasing]: first argument should be one of 'ease', 'ease-in', 'ease-out', 'ease-in-out','cubic-bezier(x1,y1,x2,y2)', 'linear' and 'spring', instead  received %s", r);
    }
  return typeof a == "function" ? a : (Vs(!1, "[configEasing]: first argument type should be function or string, instead received %s", r), null);
};
function uo(e) {
  "@babel/helpers - typeof";
  return uo = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, uo(e);
}
function fw(e) {
  return GL(e) || HL(e) || $k(e) || VL();
}
function VL() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function HL(e) {
  if (typeof Symbol < "u" && e[Symbol.iterator] != null || e["@@iterator"] != null) return Array.from(e);
}
function GL(e) {
  if (Array.isArray(e)) return ph(e);
}
function mw(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function It(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? mw(Object(r), !0).forEach(function(n) {
      hh(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : mw(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function hh(e, t, r) {
  return t = KL(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function KL(e) {
  var t = YL(e, "string");
  return uo(t) === "symbol" ? t : String(t);
}
function YL(e, t) {
  if (uo(e) !== "object" || e === null) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (uo(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function XL(e, t) {
  return ZL(e) || JL(e, t) || $k(e, t) || QL();
}
function QL() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function $k(e, t) {
  if (e) {
    if (typeof e == "string") return ph(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return ph(e, t);
  }
}
function ph(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function JL(e, t) {
  var r = e == null ? null : typeof Symbol < "u" && e[Symbol.iterator] || e["@@iterator"];
  if (r != null) {
    var n, a, i, o, l = [], c = !0, u = !1;
    try {
      if (i = (r = r.call(e)).next, t !== 0) for (; !(c = (n = i.call(r)).done) && (l.push(n.value), l.length !== t); c = !0) ;
    } catch (d) {
      u = !0, a = d;
    } finally {
      try {
        if (!c && r.return != null && (o = r.return(), Object(o) !== o)) return;
      } finally {
        if (u) throw a;
      }
    }
    return l;
  }
}
function ZL(e) {
  if (Array.isArray(e)) return e;
}
var Gs = function(t, r, n) {
  return t + (r - t) * n;
}, gh = function(t) {
  var r = t.from, n = t.to;
  return r !== n;
}, e3 = function e(t, r, n) {
  var a = Ii(function(i, o) {
    if (gh(o)) {
      var l = t(o.from, o.to, o.velocity), c = XL(l, 2), u = c[0], d = c[1];
      return It(It({}, o), {}, {
        from: u,
        velocity: d
      });
    }
    return o;
  }, r);
  return n < 1 ? Ii(function(i, o) {
    return gh(o) ? It(It({}, o), {}, {
      velocity: Gs(o.velocity, a[i].velocity, n),
      from: Gs(o.from, a[i].from, n)
    }) : o;
  }, r) : e(t, a, n - 1);
};
const t3 = (function(e, t, r, n, a) {
  var i = PL(e, t), o = i.reduce(function(g, v) {
    return It(It({}, g), {}, hh({}, v, [e[v], t[v]]));
  }, {}), l = i.reduce(function(g, v) {
    return It(It({}, g), {}, hh({}, v, {
      from: e[v],
      velocity: 0,
      to: t[v]
    }));
  }, {}), c = -1, u, d, f = function() {
    return null;
  }, p = function() {
    return Ii(function(v, b) {
      return b.from;
    }, l);
  }, m = function() {
    return !Object.values(l).filter(gh).length;
  }, y = function(v) {
    u || (u = v);
    var b = v - u, w = b / r.dt;
    l = e3(r, l, w), a(It(It(It({}, e), t), p())), u = v, m() || (c = requestAnimationFrame(f));
  }, h = function(v) {
    d || (d = v);
    var b = (v - d) / n, w = Ii(function(x, _) {
      return Gs.apply(void 0, fw(_).concat([r(b)]));
    }, o);
    if (a(It(It(It({}, e), t), w)), b < 1)
      c = requestAnimationFrame(f);
    else {
      var j = Ii(function(x, _) {
        return Gs.apply(void 0, fw(_).concat([r(1)]));
      }, o);
      a(It(It(It({}, e), t), j));
    }
  };
  return f = r.isStepper ? y : h, function() {
    return requestAnimationFrame(f), function() {
      cancelAnimationFrame(c);
    };
  };
});
function Ia(e) {
  "@babel/helpers - typeof";
  return Ia = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Ia(e);
}
var r3 = ["children", "begin", "duration", "attributeName", "easing", "isActive", "steps", "from", "to", "canBegin", "onAnimationEnd", "shouldReAnimate", "onAnimationReStart"];
function n3(e, t) {
  if (e == null) return {};
  var r = a3(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function a3(e, t) {
  if (e == null) return {};
  var r = {}, n = Object.keys(e), a, i;
  for (i = 0; i < n.length; i++)
    a = n[i], !(t.indexOf(a) >= 0) && (r[a] = e[a]);
  return r;
}
function Hf(e) {
  return l3(e) || s3(e) || o3(e) || i3();
}
function i3() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function o3(e, t) {
  if (e) {
    if (typeof e == "string") return yh(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return yh(e, t);
  }
}
function s3(e) {
  if (typeof Symbol < "u" && e[Symbol.iterator] != null || e["@@iterator"] != null) return Array.from(e);
}
function l3(e) {
  if (Array.isArray(e)) return yh(e);
}
function yh(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function hw(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function xr(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? hw(Object(r), !0).forEach(function(n) {
      Oi(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : hw(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function Oi(e, t, r) {
  return t = Lk(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function c3(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function u3(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, Lk(n.key), n);
  }
}
function d3(e, t, r) {
  return t && u3(e.prototype, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function Lk(e) {
  var t = f3(e, "string");
  return Ia(t) === "symbol" ? t : String(t);
}
function f3(e, t) {
  if (Ia(e) !== "object" || e === null) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Ia(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function m3(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && vh(e, t);
}
function vh(e, t) {
  return vh = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, vh(e, t);
}
function h3(e) {
  var t = p3();
  return function() {
    var n = Ks(e), a;
    if (t) {
      var i = Ks(this).constructor;
      a = Reflect.construct(n, arguments, i);
    } else
      a = n.apply(this, arguments);
    return bh(this, a);
  };
}
function bh(e, t) {
  if (t && (Ia(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return xh(e);
}
function xh(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function p3() {
  if (typeof Reflect > "u" || !Reflect.construct || Reflect.construct.sham) return !1;
  if (typeof Proxy == "function") return !0;
  try {
    return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    })), !0;
  } catch {
    return !1;
  }
}
function Ks(e) {
  return Ks = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, Ks(e);
}
var tn = /* @__PURE__ */ (function(e) {
  m3(r, e);
  var t = h3(r);
  function r(n, a) {
    var i;
    c3(this, r), i = t.call(this, n, a);
    var o = i.props, l = o.isActive, c = o.attributeName, u = o.from, d = o.to, f = o.steps, p = o.children, m = o.duration;
    if (i.handleStyleChange = i.handleStyleChange.bind(xh(i)), i.changeStyle = i.changeStyle.bind(xh(i)), !l || m <= 0)
      return i.state = {
        style: {}
      }, typeof p == "function" && (i.state = {
        style: d
      }), bh(i);
    if (f && f.length)
      i.state = {
        style: f[0].style
      };
    else if (u) {
      if (typeof p == "function")
        return i.state = {
          style: u
        }, bh(i);
      i.state = {
        style: c ? Oi({}, c, u) : u
      };
    } else
      i.state = {
        style: {}
      };
    return i;
  }
  return d3(r, [{
    key: "componentDidMount",
    value: function() {
      var a = this.props, i = a.isActive, o = a.canBegin;
      this.mounted = !0, !(!i || !o) && this.runAnimation(this.props);
    }
  }, {
    key: "componentDidUpdate",
    value: function(a) {
      var i = this.props, o = i.isActive, l = i.canBegin, c = i.attributeName, u = i.shouldReAnimate, d = i.to, f = i.from, p = this.state.style;
      if (l) {
        if (!o) {
          var m = {
            style: c ? Oi({}, c, d) : d
          };
          this.state && p && (c && p[c] !== d || !c && p !== d) && this.setState(m);
          return;
        }
        if (!(xL(a.to, d) && a.canBegin && a.isActive)) {
          var y = !a.canBegin || !a.isActive;
          this.manager && this.manager.stop(), this.stopJSAnimation && this.stopJSAnimation();
          var h = y || u ? f : a.to;
          if (this.state && p) {
            var g = {
              style: c ? Oi({}, c, h) : h
            };
            (c && p[c] !== h || !c && p !== h) && this.setState(g);
          }
          this.runAnimation(xr(xr({}, this.props), {}, {
            from: h,
            begin: 0
          }));
        }
      }
    }
  }, {
    key: "componentWillUnmount",
    value: function() {
      this.mounted = !1;
      var a = this.props.onAnimationEnd;
      this.unSubscribe && this.unSubscribe(), this.manager && (this.manager.stop(), this.manager = null), this.stopJSAnimation && this.stopJSAnimation(), a && a();
    }
  }, {
    key: "handleStyleChange",
    value: function(a) {
      this.changeStyle(a);
    }
  }, {
    key: "changeStyle",
    value: function(a) {
      this.mounted && this.setState({
        style: a
      });
    }
  }, {
    key: "runJSAnimation",
    value: function(a) {
      var i = this, o = a.from, l = a.to, c = a.duration, u = a.easing, d = a.begin, f = a.onAnimationEnd, p = a.onAnimationStart, m = t3(o, l, WL(u), c, this.changeStyle), y = function() {
        i.stopJSAnimation = m();
      };
      this.manager.start([p, d, y, c, f]);
    }
  }, {
    key: "runStepAnimation",
    value: function(a) {
      var i = this, o = a.steps, l = a.begin, c = a.onAnimationStart, u = o[0], d = u.style, f = u.duration, p = f === void 0 ? 0 : f, m = function(h, g, v) {
        if (v === 0)
          return h;
        var b = g.duration, w = g.easing, j = w === void 0 ? "ease" : w, x = g.style, _ = g.properties, S = g.onAnimationEnd, k = v > 0 ? o[v - 1] : g, E = _ || Object.keys(x);
        if (typeof j == "function" || j === "spring")
          return [].concat(Hf(h), [i.runJSAnimation.bind(i, {
            from: k.style,
            to: x,
            duration: b,
            easing: j
          }), b]);
        var L = cw(E, b, j), P = xr(xr(xr({}, k.style), x), {}, {
          transition: L
        });
        return [].concat(Hf(h), [P, b, S]).filter(OL);
      };
      return this.manager.start([c].concat(Hf(o.reduce(m, [d, Math.max(p, l)])), [a.onAnimationEnd]));
    }
  }, {
    key: "runAnimation",
    value: function(a) {
      this.manager || (this.manager = AL());
      var i = a.begin, o = a.duration, l = a.attributeName, c = a.to, u = a.easing, d = a.onAnimationStart, f = a.onAnimationEnd, p = a.steps, m = a.children, y = this.manager;
      if (this.unSubscribe = y.subscribe(this.handleStyleChange), typeof u == "function" || typeof m == "function" || u === "spring") {
        this.runJSAnimation(a);
        return;
      }
      if (p.length > 1) {
        this.runStepAnimation(a);
        return;
      }
      var h = l ? Oi({}, l, c) : c, g = cw(Object.keys(h), o, u);
      y.start([d, i, xr(xr({}, h), {}, {
        transition: g
      }), o, f]);
    }
  }, {
    key: "render",
    value: function() {
      var a = this.props, i = a.children;
      a.begin;
      var o = a.duration;
      a.attributeName, a.easing;
      var l = a.isActive;
      a.steps, a.from, a.to, a.canBegin, a.onAnimationEnd, a.shouldReAnimate, a.onAnimationReStart;
      var c = n3(a, r3), u = qn.count(i), d = this.state.style;
      if (typeof i == "function")
        return i(d);
      if (!l || u === 0 || o <= 0)
        return i;
      var f = function(m) {
        var y = m.props, h = y.style, g = h === void 0 ? {} : h, v = y.className, b = /* @__PURE__ */ Tt(m, xr(xr({}, c), {}, {
          style: xr(xr({}, g), d),
          className: v
        }));
        return b;
      };
      return u === 1 ? f(qn.only(i)) : /* @__PURE__ */ I.createElement("div", null, qn.map(i, function(p) {
        return f(p);
      }));
    }
  }]), r;
})(wn);
tn.displayName = "Animate";
tn.defaultProps = {
  begin: 0,
  duration: 1e3,
  from: "",
  to: "",
  attributeName: "",
  easing: "ease",
  isActive: !0,
  canBegin: !0,
  steps: [],
  onAnimationEnd: function() {
  },
  onAnimationStart: function() {
  }
};
tn.propTypes = {
  from: Je.oneOfType([Je.object, Je.string]),
  to: Je.oneOfType([Je.object, Je.string]),
  attributeName: Je.string,
  // animation duration
  duration: Je.number,
  begin: Je.number,
  easing: Je.oneOfType([Je.string, Je.func]),
  steps: Je.arrayOf(Je.shape({
    duration: Je.number.isRequired,
    style: Je.object.isRequired,
    easing: Je.oneOfType([Je.oneOf(["ease", "ease-in", "ease-out", "ease-in-out", "linear"]), Je.func]),
    // transition css properties(dash case), optional
    properties: Je.arrayOf("string"),
    onAnimationEnd: Je.func
  })),
  children: Je.oneOfType([Je.node, Je.func]),
  isActive: Je.bool,
  canBegin: Je.bool,
  onAnimationEnd: Je.func,
  // decide if it should reanimate with initial from style when props change
  shouldReAnimate: Je.bool,
  onAnimationStart: Je.func,
  onAnimationReStart: Je.func
};
function fo(e) {
  "@babel/helpers - typeof";
  return fo = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, fo(e);
}
function Ys() {
  return Ys = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Ys.apply(this, arguments);
}
function g3(e, t) {
  return x3(e) || b3(e, t) || v3(e, t) || y3();
}
function y3() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function v3(e, t) {
  if (e) {
    if (typeof e == "string") return pw(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return pw(e, t);
  }
}
function pw(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function b3(e, t) {
  var r = e == null ? null : typeof Symbol < "u" && e[Symbol.iterator] || e["@@iterator"];
  if (r != null) {
    var n, a, i, o, l = [], c = !0, u = !1;
    try {
      if (i = (r = r.call(e)).next, t !== 0) for (; !(c = (n = i.call(r)).done) && (l.push(n.value), l.length !== t); c = !0) ;
    } catch (d) {
      u = !0, a = d;
    } finally {
      try {
        if (!c && r.return != null && (o = r.return(), Object(o) !== o)) return;
      } finally {
        if (u) throw a;
      }
    }
    return l;
  }
}
function x3(e) {
  if (Array.isArray(e)) return e;
}
function gw(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function yw(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? gw(Object(r), !0).forEach(function(n) {
      w3(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : gw(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function w3(e, t, r) {
  return t = _3(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function _3(e) {
  var t = j3(e, "string");
  return fo(t) == "symbol" ? t : t + "";
}
function j3(e, t) {
  if (fo(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (fo(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
var vw = function(t, r, n, a, i) {
  var o = Math.min(Math.abs(n) / 2, Math.abs(a) / 2), l = a >= 0 ? 1 : -1, c = n >= 0 ? 1 : -1, u = a >= 0 && n >= 0 || a < 0 && n < 0 ? 1 : 0, d;
  if (o > 0 && i instanceof Array) {
    for (var f = [0, 0, 0, 0], p = 0, m = 4; p < m; p++)
      f[p] = i[p] > o ? o : i[p];
    d = "M".concat(t, ",").concat(r + l * f[0]), f[0] > 0 && (d += "A ".concat(f[0], ",").concat(f[0], ",0,0,").concat(u, ",").concat(t + c * f[0], ",").concat(r)), d += "L ".concat(t + n - c * f[1], ",").concat(r), f[1] > 0 && (d += "A ".concat(f[1], ",").concat(f[1], ",0,0,").concat(u, `,
        `).concat(t + n, ",").concat(r + l * f[1])), d += "L ".concat(t + n, ",").concat(r + a - l * f[2]), f[2] > 0 && (d += "A ".concat(f[2], ",").concat(f[2], ",0,0,").concat(u, `,
        `).concat(t + n - c * f[2], ",").concat(r + a)), d += "L ".concat(t + c * f[3], ",").concat(r + a), f[3] > 0 && (d += "A ".concat(f[3], ",").concat(f[3], ",0,0,").concat(u, `,
        `).concat(t, ",").concat(r + a - l * f[3])), d += "Z";
  } else if (o > 0 && i === +i && i > 0) {
    var y = Math.min(o, i);
    d = "M ".concat(t, ",").concat(r + l * y, `
            A `).concat(y, ",").concat(y, ",0,0,").concat(u, ",").concat(t + c * y, ",").concat(r, `
            L `).concat(t + n - c * y, ",").concat(r, `
            A `).concat(y, ",").concat(y, ",0,0,").concat(u, ",").concat(t + n, ",").concat(r + l * y, `
            L `).concat(t + n, ",").concat(r + a - l * y, `
            A `).concat(y, ",").concat(y, ",0,0,").concat(u, ",").concat(t + n - c * y, ",").concat(r + a, `
            L `).concat(t + c * y, ",").concat(r + a, `
            A `).concat(y, ",").concat(y, ",0,0,").concat(u, ",").concat(t, ",").concat(r + a - l * y, " Z");
  } else
    d = "M ".concat(t, ",").concat(r, " h ").concat(n, " v ").concat(a, " h ").concat(-n, " Z");
  return d;
}, S3 = function(t, r) {
  if (!t || !r)
    return !1;
  var n = t.x, a = t.y, i = r.x, o = r.y, l = r.width, c = r.height;
  if (Math.abs(l) > 0 && Math.abs(c) > 0) {
    var u = Math.min(i, i + l), d = Math.max(i, i + l), f = Math.min(o, o + c), p = Math.max(o, o + c);
    return n >= u && n <= d && a >= f && a <= p;
  }
  return !1;
}, k3 = {
  x: 0,
  y: 0,
  width: 0,
  height: 0,
  // The radius of border
  // The radius of four corners when radius is a number
  // The radius of left-top, right-top, right-bottom, left-bottom when radius is an array
  radius: 0,
  isAnimationActive: !1,
  isUpdateAnimationActive: !1,
  animationBegin: 0,
  animationDuration: 1500,
  animationEasing: "ease"
}, Gp = function(t) {
  var r = yw(yw({}, k3), t), n = Tr(), a = de(-1), i = g3(a, 2), o = i[0], l = i[1];
  et(function() {
    if (n.current && n.current.getTotalLength)
      try {
        var j = n.current.getTotalLength();
        j && l(j);
      } catch {
      }
  }, []);
  var c = r.x, u = r.y, d = r.width, f = r.height, p = r.radius, m = r.className, y = r.animationEasing, h = r.animationDuration, g = r.animationBegin, v = r.isAnimationActive, b = r.isUpdateAnimationActive;
  if (c !== +c || u !== +u || d !== +d || f !== +f || d === 0 || f === 0)
    return null;
  var w = Ve("recharts-rectangle", m);
  return b ? /* @__PURE__ */ I.createElement(tn, {
    canBegin: o > 0,
    from: {
      width: d,
      height: f,
      x: c,
      y: u
    },
    to: {
      width: d,
      height: f,
      x: c,
      y: u
    },
    duration: h,
    animationEasing: y,
    isActive: b
  }, function(j) {
    var x = j.width, _ = j.height, S = j.x, k = j.y;
    return /* @__PURE__ */ I.createElement(tn, {
      canBegin: o > 0,
      from: "0px ".concat(o === -1 ? 1 : o, "px"),
      to: "".concat(o, "px 0px"),
      attributeName: "strokeDasharray",
      begin: g,
      duration: h,
      isActive: v,
      easing: y
    }, /* @__PURE__ */ I.createElement("path", Ys({}, ze(r, !0), {
      className: w,
      d: vw(S, k, x, _, p),
      ref: n
    })));
  }) : /* @__PURE__ */ I.createElement("path", Ys({}, ze(r, !0), {
    className: w,
    d: vw(c, u, d, f, p)
  }));
};
function wh() {
  return wh = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, wh.apply(this, arguments);
}
var Kp = function(t) {
  var r = t.cx, n = t.cy, a = t.r, i = t.className, o = Ve("recharts-dot", i);
  return r === +r && n === +n && a === +a ? /* @__PURE__ */ xn.createElement("circle", wh({}, ze(t, !1), ps(t), {
    className: o,
    cx: r,
    cy: n,
    r: a
  })) : null;
};
function mo(e) {
  "@babel/helpers - typeof";
  return mo = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, mo(e);
}
var E3 = ["x", "y", "top", "left", "width", "height", "className"];
function _h() {
  return _h = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, _h.apply(this, arguments);
}
function bw(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function A3(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? bw(Object(r), !0).forEach(function(n) {
      C3(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : bw(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function C3(e, t, r) {
  return t = T3(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function T3(e) {
  var t = P3(e, "string");
  return mo(t) == "symbol" ? t : t + "";
}
function P3(e, t) {
  if (mo(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (mo(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function O3(e, t) {
  if (e == null) return {};
  var r = N3(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function N3(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
var M3 = function(t, r, n, a, i, o) {
  return "M".concat(t, ",").concat(i, "v").concat(a, "M").concat(o, ",").concat(r, "h").concat(n);
}, R3 = function(t) {
  var r = t.x, n = r === void 0 ? 0 : r, a = t.y, i = a === void 0 ? 0 : a, o = t.top, l = o === void 0 ? 0 : o, c = t.left, u = c === void 0 ? 0 : c, d = t.width, f = d === void 0 ? 0 : d, p = t.height, m = p === void 0 ? 0 : p, y = t.className, h = O3(t, E3), g = A3({
    x: n,
    y: i,
    top: l,
    left: u,
    width: f,
    height: m
  }, h);
  return !me(n) || !me(i) || !me(f) || !me(m) || !me(l) || !me(u) ? null : /* @__PURE__ */ I.createElement("path", _h({}, ze(g, !0), {
    className: Ve("recharts-cross", y),
    d: M3(n, i, f, m, l, u)
  }));
}, Gf, xw;
function I3() {
  if (xw) return Gf;
  xw = 1;
  var e = rS(), t = e(Object.getPrototypeOf, Object);
  return Gf = t, Gf;
}
var Kf, ww;
function D3() {
  if (ww) return Kf;
  ww = 1;
  var e = nn(), t = I3(), r = an(), n = "[object Object]", a = Function.prototype, i = Object.prototype, o = a.toString, l = i.hasOwnProperty, c = o.call(Object);
  function u(d) {
    if (!r(d) || e(d) != n)
      return !1;
    var f = t(d);
    if (f === null)
      return !0;
    var p = l.call(f, "constructor") && f.constructor;
    return typeof p == "function" && p instanceof p && o.call(p) == c;
  }
  return Kf = u, Kf;
}
var $3 = D3();
const L3 = /* @__PURE__ */ st($3);
var Yf, _w;
function F3() {
  if (_w) return Yf;
  _w = 1;
  var e = nn(), t = an(), r = "[object Boolean]";
  function n(a) {
    return a === !0 || a === !1 || t(a) && e(a) == r;
  }
  return Yf = n, Yf;
}
var q3 = F3();
const U3 = /* @__PURE__ */ st(q3);
function ho(e) {
  "@babel/helpers - typeof";
  return ho = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, ho(e);
}
function Xs() {
  return Xs = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Xs.apply(this, arguments);
}
function z3(e, t) {
  return H3(e) || V3(e, t) || W3(e, t) || B3();
}
function B3() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function W3(e, t) {
  if (e) {
    if (typeof e == "string") return jw(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return jw(e, t);
  }
}
function jw(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function V3(e, t) {
  var r = e == null ? null : typeof Symbol < "u" && e[Symbol.iterator] || e["@@iterator"];
  if (r != null) {
    var n, a, i, o, l = [], c = !0, u = !1;
    try {
      if (i = (r = r.call(e)).next, t !== 0) for (; !(c = (n = i.call(r)).done) && (l.push(n.value), l.length !== t); c = !0) ;
    } catch (d) {
      u = !0, a = d;
    } finally {
      try {
        if (!c && r.return != null && (o = r.return(), Object(o) !== o)) return;
      } finally {
        if (u) throw a;
      }
    }
    return l;
  }
}
function H3(e) {
  if (Array.isArray(e)) return e;
}
function Sw(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function kw(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Sw(Object(r), !0).forEach(function(n) {
      G3(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Sw(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function G3(e, t, r) {
  return t = K3(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function K3(e) {
  var t = Y3(e, "string");
  return ho(t) == "symbol" ? t : t + "";
}
function Y3(e, t) {
  if (ho(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (ho(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
var Ew = function(t, r, n, a, i) {
  var o = n - a, l;
  return l = "M ".concat(t, ",").concat(r), l += "L ".concat(t + n, ",").concat(r), l += "L ".concat(t + n - o / 2, ",").concat(r + i), l += "L ".concat(t + n - o / 2 - a, ",").concat(r + i), l += "L ".concat(t, ",").concat(r, " Z"), l;
}, X3 = {
  x: 0,
  y: 0,
  upperWidth: 0,
  lowerWidth: 0,
  height: 0,
  isUpdateAnimationActive: !1,
  animationBegin: 0,
  animationDuration: 1500,
  animationEasing: "ease"
}, Q3 = function(t) {
  var r = kw(kw({}, X3), t), n = Tr(), a = de(-1), i = z3(a, 2), o = i[0], l = i[1];
  et(function() {
    if (n.current && n.current.getTotalLength)
      try {
        var w = n.current.getTotalLength();
        w && l(w);
      } catch {
      }
  }, []);
  var c = r.x, u = r.y, d = r.upperWidth, f = r.lowerWidth, p = r.height, m = r.className, y = r.animationEasing, h = r.animationDuration, g = r.animationBegin, v = r.isUpdateAnimationActive;
  if (c !== +c || u !== +u || d !== +d || f !== +f || p !== +p || d === 0 && f === 0 || p === 0)
    return null;
  var b = Ve("recharts-trapezoid", m);
  return v ? /* @__PURE__ */ I.createElement(tn, {
    canBegin: o > 0,
    from: {
      upperWidth: 0,
      lowerWidth: 0,
      height: p,
      x: c,
      y: u
    },
    to: {
      upperWidth: d,
      lowerWidth: f,
      height: p,
      x: c,
      y: u
    },
    duration: h,
    animationEasing: y,
    isActive: v
  }, function(w) {
    var j = w.upperWidth, x = w.lowerWidth, _ = w.height, S = w.x, k = w.y;
    return /* @__PURE__ */ I.createElement(tn, {
      canBegin: o > 0,
      from: "0px ".concat(o === -1 ? 1 : o, "px"),
      to: "".concat(o, "px 0px"),
      attributeName: "strokeDasharray",
      begin: g,
      duration: h,
      easing: y
    }, /* @__PURE__ */ I.createElement("path", Xs({}, ze(r, !0), {
      className: b,
      d: Ew(S, k, j, x, _),
      ref: n
    })));
  }) : /* @__PURE__ */ I.createElement("g", null, /* @__PURE__ */ I.createElement("path", Xs({}, ze(r, !0), {
    className: b,
    d: Ew(c, u, d, f, p)
  })));
}, J3 = ["option", "shapeType", "propTransformer", "activeClassName", "isActive"];
function po(e) {
  "@babel/helpers - typeof";
  return po = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, po(e);
}
function Z3(e, t) {
  if (e == null) return {};
  var r = eF(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function eF(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
function Aw(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function Qs(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Aw(Object(r), !0).forEach(function(n) {
      tF(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Aw(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function tF(e, t, r) {
  return t = rF(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function rF(e) {
  var t = nF(e, "string");
  return po(t) == "symbol" ? t : t + "";
}
function nF(e, t) {
  if (po(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (po(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function aF(e, t) {
  return Qs(Qs({}, t), e);
}
function iF(e, t) {
  return e === "symbols";
}
function Cw(e) {
  var t = e.shapeType, r = e.elementProps;
  switch (t) {
    case "rectangle":
      return /* @__PURE__ */ I.createElement(Gp, r);
    case "trapezoid":
      return /* @__PURE__ */ I.createElement(Q3, r);
    case "sector":
      return /* @__PURE__ */ I.createElement(Tk, r);
    case "symbols":
      if (iF(t))
        return /* @__PURE__ */ I.createElement(dp, r);
      break;
    default:
      return null;
  }
}
function oF(e) {
  return /* @__PURE__ */ Cr(e) ? e.props : e;
}
function sF(e) {
  var t = e.option, r = e.shapeType, n = e.propTransformer, a = n === void 0 ? aF : n, i = e.activeClassName, o = i === void 0 ? "recharts-active-shape" : i, l = e.isActive, c = Z3(e, J3), u;
  if (/* @__PURE__ */ Cr(t))
    u = /* @__PURE__ */ Tt(t, Qs(Qs({}, c), oF(t)));
  else if ($e(t))
    u = t(c);
  else if (L3(t) && !U3(t)) {
    var d = a(t, c);
    u = /* @__PURE__ */ I.createElement(Cw, {
      shapeType: r,
      elementProps: d
    });
  } else {
    var f = c;
    u = /* @__PURE__ */ I.createElement(Cw, {
      shapeType: r,
      elementProps: f
    });
  }
  return l ? /* @__PURE__ */ I.createElement(yt, {
    className: o
  }, u) : u;
}
function Hl(e, t) {
  return t != null && "trapezoids" in e.props;
}
function Gl(e, t) {
  return t != null && "sectors" in e.props;
}
function go(e, t) {
  return t != null && "points" in e.props;
}
function lF(e, t) {
  var r, n, a = e.x === (t == null || (r = t.labelViewBox) === null || r === void 0 ? void 0 : r.x) || e.x === t.x, i = e.y === (t == null || (n = t.labelViewBox) === null || n === void 0 ? void 0 : n.y) || e.y === t.y;
  return a && i;
}
function cF(e, t) {
  var r = e.endAngle === t.endAngle, n = e.startAngle === t.startAngle;
  return r && n;
}
function uF(e, t) {
  var r = e.x === t.x, n = e.y === t.y, a = e.z === t.z;
  return r && n && a;
}
function dF(e, t) {
  var r;
  return Hl(e, t) ? r = lF : Gl(e, t) ? r = cF : go(e, t) && (r = uF), r;
}
function fF(e, t) {
  var r;
  return Hl(e, t) ? r = "trapezoids" : Gl(e, t) ? r = "sectors" : go(e, t) && (r = "points"), r;
}
function mF(e, t) {
  if (Hl(e, t)) {
    var r;
    return (r = t.tooltipPayload) === null || r === void 0 || (r = r[0]) === null || r === void 0 || (r = r.payload) === null || r === void 0 ? void 0 : r.payload;
  }
  if (Gl(e, t)) {
    var n;
    return (n = t.tooltipPayload) === null || n === void 0 || (n = n[0]) === null || n === void 0 || (n = n.payload) === null || n === void 0 ? void 0 : n.payload;
  }
  return go(e, t) ? t.payload : {};
}
function hF(e) {
  var t = e.activeTooltipItem, r = e.graphicalItem, n = e.itemData, a = fF(r, t), i = mF(r, t), o = n.filter(function(c, u) {
    var d = Bl(i, c), f = r.props[a].filter(function(y) {
      var h = dF(r, t);
      return h(y, t);
    }), p = r.props[a].indexOf(f[f.length - 1]), m = u === p;
    return d && m;
  }), l = n.indexOf(o[o.length - 1]);
  return l;
}
var Xf, Tw;
function pF() {
  if (Tw) return Xf;
  Tw = 1;
  var e = Math.ceil, t = Math.max;
  function r(n, a, i, o) {
    for (var l = -1, c = t(e((a - n) / (i || 1)), 0), u = Array(c); c--; )
      u[o ? c : ++l] = n, n += i;
    return u;
  }
  return Xf = r, Xf;
}
var Qf, Pw;
function Fk() {
  if (Pw) return Qf;
  Pw = 1;
  var e = vS(), t = 1 / 0, r = 17976931348623157e292;
  function n(a) {
    if (!a)
      return a === 0 ? a : 0;
    if (a = e(a), a === t || a === -t) {
      var i = a < 0 ? -1 : 1;
      return i * r;
    }
    return a === a ? a : 0;
  }
  return Qf = n, Qf;
}
var Jf, Ow;
function gF() {
  if (Ow) return Jf;
  Ow = 1;
  var e = pF(), t = Rl(), r = Fk();
  function n(a) {
    return function(i, o, l) {
      return l && typeof l != "number" && t(i, o, l) && (o = l = void 0), i = r(i), o === void 0 ? (o = i, i = 0) : o = r(o), l = l === void 0 ? i < o ? 1 : -1 : r(l), e(i, o, l, a);
    };
  }
  return Jf = n, Jf;
}
var Zf, Nw;
function yF() {
  if (Nw) return Zf;
  Nw = 1;
  var e = gF(), t = e();
  return Zf = t, Zf;
}
var vF = yF();
const Js = /* @__PURE__ */ st(vF);
function yo(e) {
  "@babel/helpers - typeof";
  return yo = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, yo(e);
}
function Mw(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function Rw(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Mw(Object(r), !0).forEach(function(n) {
      qk(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Mw(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function qk(e, t, r) {
  return t = bF(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function bF(e) {
  var t = xF(e, "string");
  return yo(t) == "symbol" ? t : t + "";
}
function xF(e, t) {
  if (yo(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (yo(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
var wF = ["Webkit", "Moz", "O", "ms"], _F = function(t, r) {
  var n = t.replace(/(\w)/, function(i) {
    return i.toUpperCase();
  }), a = wF.reduce(function(i, o) {
    return Rw(Rw({}, i), {}, qk({}, o + n, r));
  }, {});
  return a[t] = r, a;
};
function Da(e) {
  "@babel/helpers - typeof";
  return Da = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Da(e);
}
function Zs() {
  return Zs = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Zs.apply(this, arguments);
}
function Iw(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function em(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Iw(Object(r), !0).forEach(function(n) {
      tr(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Iw(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function jF(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function Dw(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, zk(n.key), n);
  }
}
function SF(e, t, r) {
  return t && Dw(e.prototype, t), r && Dw(e, r), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function kF(e, t, r) {
  return t = el(t), EF(e, Uk() ? Reflect.construct(t, r || [], el(e).constructor) : t.apply(e, r));
}
function EF(e, t) {
  if (t && (Da(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return AF(e);
}
function AF(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function Uk() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (Uk = function() {
    return !!e;
  })();
}
function el(e) {
  return el = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, el(e);
}
function CF(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && jh(e, t);
}
function jh(e, t) {
  return jh = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, jh(e, t);
}
function tr(e, t, r) {
  return t = zk(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function zk(e) {
  var t = TF(e, "string");
  return Da(t) == "symbol" ? t : t + "";
}
function TF(e, t) {
  if (Da(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Da(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
var PF = function(t) {
  var r = t.data, n = t.startIndex, a = t.endIndex, i = t.x, o = t.width, l = t.travellerWidth;
  if (!r || !r.length)
    return {};
  var c = r.length, u = Mi().domain(Js(0, c)).range([i, i + o - l]), d = u.domain().map(function(f) {
    return u(f);
  });
  return {
    isTextActive: !1,
    isSlideMoving: !1,
    isTravellerMoving: !1,
    isTravellerFocused: !1,
    startX: u(n),
    endX: u(a),
    scale: u,
    scaleValues: d
  };
}, $w = function(t) {
  return t.changedTouches && !!t.changedTouches.length;
}, $a = /* @__PURE__ */ (function(e) {
  function t(r) {
    var n;
    return jF(this, t), n = kF(this, t, [r]), tr(n, "handleDrag", function(a) {
      n.leaveTimer && (clearTimeout(n.leaveTimer), n.leaveTimer = null), n.state.isTravellerMoving ? n.handleTravellerMove(a) : n.state.isSlideMoving && n.handleSlideDrag(a);
    }), tr(n, "handleTouchMove", function(a) {
      a.changedTouches != null && a.changedTouches.length > 0 && n.handleDrag(a.changedTouches[0]);
    }), tr(n, "handleDragEnd", function() {
      n.setState({
        isTravellerMoving: !1,
        isSlideMoving: !1
      }, function() {
        var a = n.props, i = a.endIndex, o = a.onDragEnd, l = a.startIndex;
        o == null || o({
          endIndex: i,
          startIndex: l
        });
      }), n.detachDragEndListener();
    }), tr(n, "handleLeaveWrapper", function() {
      (n.state.isTravellerMoving || n.state.isSlideMoving) && (n.leaveTimer = window.setTimeout(n.handleDragEnd, n.props.leaveTimeOut));
    }), tr(n, "handleEnterSlideOrTraveller", function() {
      n.setState({
        isTextActive: !0
      });
    }), tr(n, "handleLeaveSlideOrTraveller", function() {
      n.setState({
        isTextActive: !1
      });
    }), tr(n, "handleSlideDragStart", function(a) {
      var i = $w(a) ? a.changedTouches[0] : a;
      n.setState({
        isTravellerMoving: !1,
        isSlideMoving: !0,
        slideMoveStartX: i.pageX
      }), n.attachDragEndListener();
    }), n.travellerDragStartHandlers = {
      startX: n.handleTravellerDragStart.bind(n, "startX"),
      endX: n.handleTravellerDragStart.bind(n, "endX")
    }, n.state = {}, n;
  }
  return CF(t, e), SF(t, [{
    key: "componentWillUnmount",
    value: function() {
      this.leaveTimer && (clearTimeout(this.leaveTimer), this.leaveTimer = null), this.detachDragEndListener();
    }
  }, {
    key: "getIndex",
    value: function(n) {
      var a = n.startX, i = n.endX, o = this.state.scaleValues, l = this.props, c = l.gap, u = l.data, d = u.length - 1, f = Math.min(a, i), p = Math.max(a, i), m = t.getIndexInRange(o, f), y = t.getIndexInRange(o, p);
      return {
        startIndex: m - m % c,
        endIndex: y === d ? d : y - y % c
      };
    }
  }, {
    key: "getTextOfTick",
    value: function(n) {
      var a = this.props, i = a.data, o = a.tickFormatter, l = a.dataKey, c = ir(i[n], l, n);
      return $e(o) ? o(c, n) : c;
    }
  }, {
    key: "attachDragEndListener",
    value: function() {
      window.addEventListener("mouseup", this.handleDragEnd, !0), window.addEventListener("touchend", this.handleDragEnd, !0), window.addEventListener("mousemove", this.handleDrag, !0);
    }
  }, {
    key: "detachDragEndListener",
    value: function() {
      window.removeEventListener("mouseup", this.handleDragEnd, !0), window.removeEventListener("touchend", this.handleDragEnd, !0), window.removeEventListener("mousemove", this.handleDrag, !0);
    }
  }, {
    key: "handleSlideDrag",
    value: function(n) {
      var a = this.state, i = a.slideMoveStartX, o = a.startX, l = a.endX, c = this.props, u = c.x, d = c.width, f = c.travellerWidth, p = c.startIndex, m = c.endIndex, y = c.onChange, h = n.pageX - i;
      h > 0 ? h = Math.min(h, u + d - f - l, u + d - f - o) : h < 0 && (h = Math.max(h, u - o, u - l));
      var g = this.getIndex({
        startX: o + h,
        endX: l + h
      });
      (g.startIndex !== p || g.endIndex !== m) && y && y(g), this.setState({
        startX: o + h,
        endX: l + h,
        slideMoveStartX: n.pageX
      });
    }
  }, {
    key: "handleTravellerDragStart",
    value: function(n, a) {
      var i = $w(a) ? a.changedTouches[0] : a;
      this.setState({
        isSlideMoving: !1,
        isTravellerMoving: !0,
        movingTravellerId: n,
        brushMoveStartX: i.pageX
      }), this.attachDragEndListener();
    }
  }, {
    key: "handleTravellerMove",
    value: function(n) {
      var a = this.state, i = a.brushMoveStartX, o = a.movingTravellerId, l = a.endX, c = a.startX, u = this.state[o], d = this.props, f = d.x, p = d.width, m = d.travellerWidth, y = d.onChange, h = d.gap, g = d.data, v = {
        startX: this.state.startX,
        endX: this.state.endX
      }, b = n.pageX - i;
      b > 0 ? b = Math.min(b, f + p - m - u) : b < 0 && (b = Math.max(b, f - u)), v[o] = u + b;
      var w = this.getIndex(v), j = w.startIndex, x = w.endIndex, _ = function() {
        var k = g.length - 1;
        return o === "startX" && (l > c ? j % h === 0 : x % h === 0) || l < c && x === k || o === "endX" && (l > c ? x % h === 0 : j % h === 0) || l > c && x === k;
      };
      this.setState(tr(tr({}, o, u + b), "brushMoveStartX", n.pageX), function() {
        y && _() && y(w);
      });
    }
  }, {
    key: "handleTravellerMoveKeyboard",
    value: function(n, a) {
      var i = this, o = this.state, l = o.scaleValues, c = o.startX, u = o.endX, d = this.state[a], f = l.indexOf(d);
      if (f !== -1) {
        var p = f + n;
        if (!(p === -1 || p >= l.length)) {
          var m = l[p];
          a === "startX" && m >= u || a === "endX" && m <= c || this.setState(tr({}, a, m), function() {
            i.props.onChange(i.getIndex({
              startX: i.state.startX,
              endX: i.state.endX
            }));
          });
        }
      }
    }
  }, {
    key: "renderBackground",
    value: function() {
      var n = this.props, a = n.x, i = n.y, o = n.width, l = n.height, c = n.fill, u = n.stroke;
      return /* @__PURE__ */ I.createElement("rect", {
        stroke: u,
        fill: c,
        x: a,
        y: i,
        width: o,
        height: l
      });
    }
  }, {
    key: "renderPanorama",
    value: function() {
      var n = this.props, a = n.x, i = n.y, o = n.width, l = n.height, c = n.data, u = n.children, d = n.padding, f = qn.only(u);
      return f ? /* @__PURE__ */ I.cloneElement(f, {
        x: a,
        y: i,
        width: o,
        height: l,
        margin: d,
        compact: !0,
        data: c
      }) : null;
    }
  }, {
    key: "renderTravellerLayer",
    value: function(n, a) {
      var i, o, l = this, c = this.props, u = c.y, d = c.travellerWidth, f = c.height, p = c.traveller, m = c.ariaLabel, y = c.data, h = c.startIndex, g = c.endIndex, v = Math.max(n, this.props.x), b = em(em({}, ze(this.props, !1)), {}, {
        x: v,
        y: u,
        width: d,
        height: f
      }), w = m || "Min value: ".concat((i = y[h]) === null || i === void 0 ? void 0 : i.name, ", Max value: ").concat((o = y[g]) === null || o === void 0 ? void 0 : o.name);
      return /* @__PURE__ */ I.createElement(yt, {
        tabIndex: 0,
        role: "slider",
        "aria-label": w,
        "aria-valuenow": n,
        className: "recharts-brush-traveller",
        onMouseEnter: this.handleEnterSlideOrTraveller,
        onMouseLeave: this.handleLeaveSlideOrTraveller,
        onMouseDown: this.travellerDragStartHandlers[a],
        onTouchStart: this.travellerDragStartHandlers[a],
        onKeyDown: function(x) {
          ["ArrowLeft", "ArrowRight"].includes(x.key) && (x.preventDefault(), x.stopPropagation(), l.handleTravellerMoveKeyboard(x.key === "ArrowRight" ? 1 : -1, a));
        },
        onFocus: function() {
          l.setState({
            isTravellerFocused: !0
          });
        },
        onBlur: function() {
          l.setState({
            isTravellerFocused: !1
          });
        },
        style: {
          cursor: "col-resize"
        }
      }, t.renderTraveller(p, b));
    }
  }, {
    key: "renderSlide",
    value: function(n, a) {
      var i = this.props, o = i.y, l = i.height, c = i.stroke, u = i.travellerWidth, d = Math.min(n, a) + u, f = Math.max(Math.abs(a - n) - u, 0);
      return /* @__PURE__ */ I.createElement("rect", {
        className: "recharts-brush-slide",
        onMouseEnter: this.handleEnterSlideOrTraveller,
        onMouseLeave: this.handleLeaveSlideOrTraveller,
        onMouseDown: this.handleSlideDragStart,
        onTouchStart: this.handleSlideDragStart,
        style: {
          cursor: "move"
        },
        stroke: "none",
        fill: c,
        fillOpacity: 0.2,
        x: d,
        y: o,
        width: f,
        height: l
      });
    }
  }, {
    key: "renderText",
    value: function() {
      var n = this.props, a = n.startIndex, i = n.endIndex, o = n.y, l = n.height, c = n.travellerWidth, u = n.stroke, d = this.state, f = d.startX, p = d.endX, m = 5, y = {
        pointerEvents: "none",
        fill: u
      };
      return /* @__PURE__ */ I.createElement(yt, {
        className: "recharts-brush-texts"
      }, /* @__PURE__ */ I.createElement(As, Zs({
        textAnchor: "end",
        verticalAnchor: "middle",
        x: Math.min(f, p) - m,
        y: o + l / 2
      }, y), this.getTextOfTick(a)), /* @__PURE__ */ I.createElement(As, Zs({
        textAnchor: "start",
        verticalAnchor: "middle",
        x: Math.max(f, p) + c + m,
        y: o + l / 2
      }, y), this.getTextOfTick(i)));
    }
  }, {
    key: "render",
    value: function() {
      var n = this.props, a = n.data, i = n.className, o = n.children, l = n.x, c = n.y, u = n.width, d = n.height, f = n.alwaysShowText, p = this.state, m = p.startX, y = p.endX, h = p.isTextActive, g = p.isSlideMoving, v = p.isTravellerMoving, b = p.isTravellerFocused;
      if (!a || !a.length || !me(l) || !me(c) || !me(u) || !me(d) || u <= 0 || d <= 0)
        return null;
      var w = Ve("recharts-brush", i), j = I.Children.count(o) === 1, x = _F("userSelect", "none");
      return /* @__PURE__ */ I.createElement(yt, {
        className: w,
        onMouseLeave: this.handleLeaveWrapper,
        onTouchMove: this.handleTouchMove,
        style: x
      }, this.renderBackground(), j && this.renderPanorama(), this.renderSlide(m, y), this.renderTravellerLayer(m, "startX"), this.renderTravellerLayer(y, "endX"), (h || g || v || b || f) && this.renderText());
    }
  }], [{
    key: "renderDefaultTraveller",
    value: function(n) {
      var a = n.x, i = n.y, o = n.width, l = n.height, c = n.stroke, u = Math.floor(i + l / 2) - 1;
      return /* @__PURE__ */ I.createElement(I.Fragment, null, /* @__PURE__ */ I.createElement("rect", {
        x: a,
        y: i,
        width: o,
        height: l,
        fill: c,
        stroke: "none"
      }), /* @__PURE__ */ I.createElement("line", {
        x1: a + 1,
        y1: u,
        x2: a + o - 1,
        y2: u,
        fill: "none",
        stroke: "#fff"
      }), /* @__PURE__ */ I.createElement("line", {
        x1: a + 1,
        y1: u + 2,
        x2: a + o - 1,
        y2: u + 2,
        fill: "none",
        stroke: "#fff"
      }));
    }
  }, {
    key: "renderTraveller",
    value: function(n, a) {
      var i;
      return /* @__PURE__ */ I.isValidElement(n) ? i = /* @__PURE__ */ I.cloneElement(n, a) : $e(n) ? i = n(a) : i = t.renderDefaultTraveller(a), i;
    }
  }, {
    key: "getDerivedStateFromProps",
    value: function(n, a) {
      var i = n.data, o = n.width, l = n.x, c = n.travellerWidth, u = n.updateId, d = n.startIndex, f = n.endIndex;
      if (i !== a.prevData || u !== a.prevUpdateId)
        return em({
          prevData: i,
          prevTravellerWidth: c,
          prevUpdateId: u,
          prevX: l,
          prevWidth: o
        }, i && i.length ? PF({
          data: i,
          width: o,
          x: l,
          travellerWidth: c,
          startIndex: d,
          endIndex: f
        }) : {
          scale: null,
          scaleValues: null
        });
      if (a.scale && (o !== a.prevWidth || l !== a.prevX || c !== a.prevTravellerWidth)) {
        a.scale.range([l, l + o - c]);
        var p = a.scale.domain().map(function(m) {
          return a.scale(m);
        });
        return {
          prevData: i,
          prevTravellerWidth: c,
          prevUpdateId: u,
          prevX: l,
          prevWidth: o,
          startX: a.scale(n.startIndex),
          endX: a.scale(n.endIndex),
          scaleValues: p
        };
      }
      return null;
    }
  }, {
    key: "getIndexInRange",
    value: function(n, a) {
      for (var i = n.length, o = 0, l = i - 1; l - o > 1; ) {
        var c = Math.floor((o + l) / 2);
        n[c] > a ? l = c : o = c;
      }
      return a >= n[l] ? l : o;
    }
  }]);
})(wn);
tr($a, "displayName", "Brush");
tr($a, "defaultProps", {
  height: 40,
  travellerWidth: 5,
  gap: 1,
  fill: "#fff",
  stroke: "#666",
  padding: {
    top: 1,
    right: 1,
    bottom: 1,
    left: 1
  },
  leaveTimeOut: 1e3,
  alwaysShowText: !1
});
var tm, Lw;
function OF() {
  if (Lw) return tm;
  Lw = 1;
  var e = vp();
  function t(r, n) {
    var a;
    return e(r, function(i, o, l) {
      return a = n(i, o, l), !a;
    }), !!a;
  }
  return tm = t, tm;
}
var rm, Fw;
function NF() {
  if (Fw) return rm;
  Fw = 1;
  var e = Yj(), t = jn(), r = OF(), n = Xt(), a = Rl();
  function i(o, l, c) {
    var u = n(o) ? e : r;
    return c && a(o, l, c) && (l = void 0), u(o, t(l, 3));
  }
  return rm = i, rm;
}
var MF = NF();
const RF = /* @__PURE__ */ st(MF);
var Dr = function(t, r) {
  var n = t.alwaysShow, a = t.ifOverflow;
  return n && (a = "extendDomain"), a === r;
}, nm, qw;
function IF() {
  if (qw) return nm;
  qw = 1;
  var e = mS();
  function t(r, n, a) {
    n == "__proto__" && e ? e(r, n, {
      configurable: !0,
      enumerable: !0,
      value: a,
      writable: !0
    }) : r[n] = a;
  }
  return nm = t, nm;
}
var am, Uw;
function DF() {
  if (Uw) return am;
  Uw = 1;
  var e = IF(), t = dS(), r = jn();
  function n(a, i) {
    var o = {};
    return i = r(i, 3), t(a, function(l, c, u) {
      e(o, c, i(l, c, u));
    }), o;
  }
  return am = n, am;
}
var $F = DF();
const LF = /* @__PURE__ */ st($F);
var im, zw;
function FF() {
  if (zw) return im;
  zw = 1;
  function e(t, r) {
    for (var n = -1, a = t == null ? 0 : t.length; ++n < a; )
      if (!r(t[n], n, t))
        return !1;
    return !0;
  }
  return im = e, im;
}
var om, Bw;
function qF() {
  if (Bw) return om;
  Bw = 1;
  var e = vp();
  function t(r, n) {
    var a = !0;
    return e(r, function(i, o, l) {
      return a = !!n(i, o, l), a;
    }), a;
  }
  return om = t, om;
}
var sm, Ww;
function UF() {
  if (Ww) return sm;
  Ww = 1;
  var e = FF(), t = qF(), r = jn(), n = Xt(), a = Rl();
  function i(o, l, c) {
    var u = n(o) ? e : t;
    return c && a(o, l, c) && (l = void 0), u(o, r(l, 3));
  }
  return sm = i, sm;
}
var zF = UF();
const Bk = /* @__PURE__ */ st(zF);
var BF = ["x", "y"];
function La(e) {
  "@babel/helpers - typeof";
  return La = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, La(e);
}
function Sh() {
  return Sh = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Sh.apply(this, arguments);
}
function Vw(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function Ai(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Vw(Object(r), !0).forEach(function(n) {
      WF(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Vw(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function WF(e, t, r) {
  return t = VF(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function VF(e) {
  var t = HF(e, "string");
  return La(t) == "symbol" ? t : t + "";
}
function HF(e, t) {
  if (La(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (La(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function GF(e, t) {
  if (e == null) return {};
  var r = KF(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function KF(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
function YF(e, t) {
  var r = e.x, n = e.y, a = GF(e, BF), i = "".concat(r), o = parseInt(i, 10), l = "".concat(n), c = parseInt(l, 10), u = "".concat(t.height || a.height), d = parseInt(u, 10), f = "".concat(t.width || a.width), p = parseInt(f, 10);
  return Ai(Ai(Ai(Ai(Ai({}, t), a), o ? {
    x: o
  } : {}), c ? {
    y: c
  } : {}), {}, {
    height: d,
    width: p,
    name: t.name,
    radius: t.radius
  });
}
function Hw(e) {
  return /* @__PURE__ */ I.createElement(sF, Sh({
    shapeType: "rectangle",
    propTransformer: YF,
    activeClassName: "recharts-active-bar"
  }, e));
}
var XF = function(t) {
  var r = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
  return function(n, a) {
    if (typeof t == "number") return t;
    var i = me(n) || YC(n);
    return i ? t(n, a) : (i || (process.env.NODE_ENV !== "production" ? Yt(!1, "minPointSize callback function received a value with type of ".concat(La(n), ". Currently only numbers or null/undefined are supported.")) : Yt()), r);
  };
}, QF = ["value", "background"], Wk;
function Fa(e) {
  "@babel/helpers - typeof";
  return Fa = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Fa(e);
}
function JF(e, t) {
  if (e == null) return {};
  var r = ZF(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function ZF(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
function tl() {
  return tl = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, tl.apply(this, arguments);
}
function Gw(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function xt(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Gw(Object(r), !0).forEach(function(n) {
      yn(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Gw(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function eq(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function Kw(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, Hk(n.key), n);
  }
}
function tq(e, t, r) {
  return t && Kw(e.prototype, t), r && Kw(e, r), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function rq(e, t, r) {
  return t = rl(t), nq(e, Vk() ? Reflect.construct(t, r || [], rl(e).constructor) : t.apply(e, r));
}
function nq(e, t) {
  if (t && (Fa(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return aq(e);
}
function aq(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function Vk() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (Vk = function() {
    return !!e;
  })();
}
function rl(e) {
  return rl = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, rl(e);
}
function iq(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && kh(e, t);
}
function kh(e, t) {
  return kh = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, kh(e, t);
}
function yn(e, t, r) {
  return t = Hk(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function Hk(e) {
  var t = oq(e, "string");
  return Fa(t) == "symbol" ? t : t + "";
}
function oq(e, t) {
  if (Fa(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Fa(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
var Zn = /* @__PURE__ */ (function(e) {
  function t() {
    var r;
    eq(this, t);
    for (var n = arguments.length, a = new Array(n), i = 0; i < n; i++)
      a[i] = arguments[i];
    return r = rq(this, t, [].concat(a)), yn(r, "state", {
      isAnimationFinished: !1
    }), yn(r, "id", Ao("recharts-bar-")), yn(r, "handleAnimationEnd", function() {
      var o = r.props.onAnimationEnd;
      r.setState({
        isAnimationFinished: !0
      }), o && o();
    }), yn(r, "handleAnimationStart", function() {
      var o = r.props.onAnimationStart;
      r.setState({
        isAnimationFinished: !1
      }), o && o();
    }), r;
  }
  return iq(t, e), tq(t, [{
    key: "renderRectanglesStatically",
    value: function(n) {
      var a = this, i = this.props, o = i.shape, l = i.dataKey, c = i.activeIndex, u = i.activeBar, d = ze(this.props, !1);
      return n && n.map(function(f, p) {
        var m = p === c, y = m ? u : o, h = xt(xt(xt({}, d), f), {}, {
          isActive: m,
          option: y,
          index: p,
          dataKey: l,
          onAnimationStart: a.handleAnimationStart,
          onAnimationEnd: a.handleAnimationEnd
        });
        return /* @__PURE__ */ I.createElement(yt, tl({
          className: "recharts-bar-rectangle"
        }, gs(a.props, f, p), {
          // https://github.com/recharts/recharts/issues/5415
          // eslint-disable-next-line react/no-array-index-key
          key: "rectangle-".concat(f == null ? void 0 : f.x, "-").concat(f == null ? void 0 : f.y, "-").concat(f == null ? void 0 : f.value, "-").concat(p)
        }), /* @__PURE__ */ I.createElement(Hw, h));
      });
    }
  }, {
    key: "renderRectanglesWithAnimation",
    value: function() {
      var n = this, a = this.props, i = a.data, o = a.layout, l = a.isAnimationActive, c = a.animationBegin, u = a.animationDuration, d = a.animationEasing, f = a.animationId, p = this.state.prevData;
      return /* @__PURE__ */ I.createElement(tn, {
        begin: c,
        duration: u,
        isActive: l,
        easing: d,
        from: {
          t: 0
        },
        to: {
          t: 1
        },
        key: "bar-".concat(f),
        onAnimationEnd: this.handleAnimationEnd,
        onAnimationStart: this.handleAnimationStart
      }, function(m) {
        var y = m.t, h = i.map(function(g, v) {
          var b = p && p[v];
          if (b) {
            var w = jr(b.x, g.x), j = jr(b.y, g.y), x = jr(b.width, g.width), _ = jr(b.height, g.height);
            return xt(xt({}, g), {}, {
              x: w(y),
              y: j(y),
              width: x(y),
              height: _(y)
            });
          }
          if (o === "horizontal") {
            var S = jr(0, g.height), k = S(y);
            return xt(xt({}, g), {}, {
              y: g.y + g.height - k,
              height: k
            });
          }
          var E = jr(0, g.width), L = E(y);
          return xt(xt({}, g), {}, {
            width: L
          });
        });
        return /* @__PURE__ */ I.createElement(yt, null, n.renderRectanglesStatically(h));
      });
    }
  }, {
    key: "renderRectangles",
    value: function() {
      var n = this.props, a = n.data, i = n.isAnimationActive, o = this.state.prevData;
      return i && a && a.length && (!o || !Bl(o, a)) ? this.renderRectanglesWithAnimation() : this.renderRectanglesStatically(a);
    }
  }, {
    key: "renderBackground",
    value: function() {
      var n = this, a = this.props, i = a.data, o = a.dataKey, l = a.activeIndex, c = ze(this.props.background, !1);
      return i.map(function(u, d) {
        u.value;
        var f = u.background, p = JF(u, QF);
        if (!f)
          return null;
        var m = xt(xt(xt(xt(xt({}, p), {}, {
          fill: "#eee"
        }, f), c), gs(n.props, u, d)), {}, {
          onAnimationStart: n.handleAnimationStart,
          onAnimationEnd: n.handleAnimationEnd,
          dataKey: o,
          index: d,
          className: "recharts-bar-background-rectangle"
        });
        return /* @__PURE__ */ I.createElement(Hw, tl({
          key: "background-bar-".concat(d),
          option: n.props.background,
          isActive: d === l
        }, m));
      });
    }
  }, {
    key: "renderErrorBar",
    value: function(n, a) {
      if (this.props.isAnimationActive && !this.state.isAnimationFinished)
        return null;
      var i = this.props, o = i.data, l = i.xAxis, c = i.yAxis, u = i.layout, d = i.children, f = gr(d, Mo);
      if (!f)
        return null;
      var p = u === "vertical" ? o[0].height / 2 : o[0].width / 2, m = function(g, v) {
        var b = Array.isArray(g.value) ? g.value[1] : g.value;
        return {
          x: g.x,
          y: g.y,
          value: b,
          errorVal: ir(g, v)
        };
      }, y = {
        clipPath: n ? "url(#clipPath-".concat(a, ")") : null
      };
      return /* @__PURE__ */ I.createElement(yt, y, f.map(function(h) {
        return /* @__PURE__ */ I.cloneElement(h, {
          key: "error-bar-".concat(a, "-").concat(h.props.dataKey),
          data: o,
          xAxis: l,
          yAxis: c,
          layout: u,
          offset: p,
          dataPointFormatter: m
        });
      }));
    }
  }, {
    key: "render",
    value: function() {
      var n = this.props, a = n.hide, i = n.data, o = n.className, l = n.xAxis, c = n.yAxis, u = n.left, d = n.top, f = n.width, p = n.height, m = n.isAnimationActive, y = n.background, h = n.id;
      if (a || !i || !i.length)
        return null;
      var g = this.state.isAnimationFinished, v = Ve("recharts-bar", o), b = l && l.allowDataOverflow, w = c && c.allowDataOverflow, j = b || w, x = Le(h) ? this.id : h;
      return /* @__PURE__ */ I.createElement(yt, {
        className: v
      }, b || w ? /* @__PURE__ */ I.createElement("defs", null, /* @__PURE__ */ I.createElement("clipPath", {
        id: "clipPath-".concat(x)
      }, /* @__PURE__ */ I.createElement("rect", {
        x: b ? u : u - f / 2,
        y: w ? d : d - p / 2,
        width: b ? f : f * 2,
        height: w ? p : p * 2
      }))) : null, /* @__PURE__ */ I.createElement(yt, {
        className: "recharts-bar-rectangles",
        clipPath: j ? "url(#clipPath-".concat(x, ")") : null
      }, y ? this.renderBackground() : null, this.renderRectangles()), this.renderErrorBar(j, x), (!m || g) && bn.renderCallByParent(this.props, i));
    }
  }], [{
    key: "getDerivedStateFromProps",
    value: function(n, a) {
      return n.animationId !== a.prevAnimationId ? {
        prevAnimationId: n.animationId,
        curData: n.data,
        prevData: a.curData
      } : n.data !== a.curData ? {
        curData: n.data
      } : null;
    }
  }]);
})(wn);
Wk = Zn;
yn(Zn, "displayName", "Bar");
yn(Zn, "defaultProps", {
  xAxisId: 0,
  yAxisId: 0,
  legendType: "rect",
  minPointSize: 0,
  hide: !1,
  data: [],
  layout: "vertical",
  activeBar: !1,
  isAnimationActive: !ni.isSsr,
  animationBegin: 0,
  animationDuration: 400,
  animationEasing: "ease"
});
yn(Zn, "getComposedData", function(e) {
  var t = e.props, r = e.item, n = e.barPosition, a = e.bandSize, i = e.xAxis, o = e.yAxis, l = e.xAxisTicks, c = e.yAxisTicks, u = e.stackedData, d = e.dataStartIndex, f = e.displayedData, p = e.offset, m = g2(n, r);
  if (!m)
    return null;
  var y = t.layout, h = r.type.defaultProps, g = h !== void 0 ? xt(xt({}, h), r.props) : r.props, v = g.dataKey, b = g.children, w = g.minPointSize, j = y === "horizontal" ? o : i, x = u ? j.scale.domain() : null, _ = S2({
    numericAxis: j
  }), S = gr(b, xS), k = f.map(function(E, L) {
    var P, A, T, M, N, q;
    u ? P = y2(u[d + L], x) : (P = ir(E, v), Array.isArray(P) || (P = [_, P]));
    var V = XF(w, Wk.defaultProps.minPointSize)(P[1], L);
    if (y === "horizontal") {
      var B, ee = [o.scale(P[0]), o.scale(P[1])], U = ee[0], G = ee[1];
      A = Ax({
        axis: i,
        ticks: l,
        bandSize: a,
        offset: m.offset,
        entry: E,
        index: L
      }), T = (B = G ?? U) !== null && B !== void 0 ? B : void 0, M = m.size;
      var Y = U - G;
      if (N = Number.isNaN(Y) ? 0 : Y, q = {
        x: A,
        y: o.y,
        width: M,
        height: o.height
      }, Math.abs(V) > 0 && Math.abs(N) < Math.abs(V)) {
        var Q = Ar(N || V) * (Math.abs(V) - Math.abs(N));
        T -= Q, N += Q;
      }
    } else {
      var ne = [i.scale(P[0]), i.scale(P[1])], z = ne[0], se = ne[1];
      if (A = z, T = Ax({
        axis: o,
        ticks: c,
        bandSize: a,
        offset: m.offset,
        entry: E,
        index: L
      }), M = se - z, N = m.size, q = {
        x: i.x,
        y: T,
        width: i.width,
        height: N
      }, Math.abs(V) > 0 && Math.abs(M) < Math.abs(V)) {
        var oe = Ar(M || V) * (Math.abs(V) - Math.abs(M));
        M += oe;
      }
    }
    return xt(xt(xt({}, E), {}, {
      x: A,
      y: T,
      width: M,
      height: N,
      value: u ? P : P[1],
      payload: E,
      background: q
    }, S && S[L] && S[L].props), {}, {
      tooltipPayload: [Ek(r, E)],
      tooltipPosition: {
        x: A + M / 2,
        y: T + N / 2
      }
    });
  });
  return xt({
    data: k,
    layout: y
  }, p);
});
function vo(e) {
  "@babel/helpers - typeof";
  return vo = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, vo(e);
}
function sq(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function Yw(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, Gk(n.key), n);
  }
}
function lq(e, t, r) {
  return t && Yw(e.prototype, t), r && Yw(e, r), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function Xw(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function wr(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Xw(Object(r), !0).forEach(function(n) {
      Kl(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : Xw(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function Kl(e, t, r) {
  return t = Gk(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function Gk(e) {
  var t = cq(e, "string");
  return vo(t) == "symbol" ? t : t + "";
}
function cq(e, t) {
  if (vo(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (vo(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
var Kk = function(t, r, n, a, i) {
  var o = t.width, l = t.height, c = t.layout, u = t.children, d = Object.keys(r), f = {
    left: n.left,
    leftMirror: n.left,
    right: o - n.right,
    rightMirror: o - n.right,
    top: n.top,
    topMirror: n.top,
    bottom: l - n.bottom,
    bottomMirror: l - n.bottom
  }, p = !!rr(u, Zn);
  return d.reduce(function(m, y) {
    var h = r[y], g = h.orientation, v = h.domain, b = h.padding, w = b === void 0 ? {} : b, j = h.mirror, x = h.reversed, _ = "".concat(g).concat(j ? "Mirror" : ""), S, k, E, L, P;
    if (h.type === "number" && (h.padding === "gap" || h.padding === "no-gap")) {
      var A = v[1] - v[0], T = 1 / 0, M = h.categoricalDomain.sort(JC);
      if (M.forEach(function(ne, z) {
        z > 0 && (T = Math.min((ne || 0) - (M[z - 1] || 0), T));
      }), Number.isFinite(T)) {
        var N = T / A, q = h.layout === "vertical" ? n.height : n.width;
        if (h.padding === "gap" && (S = N * q / 2), h.padding === "no-gap") {
          var V = Vn(t.barCategoryGap, N * q), B = N * q / 2;
          S = B - V - (B - V) / q * V;
        }
      }
    }
    a === "xAxis" ? k = [n.left + (w.left || 0) + (S || 0), n.left + n.width - (w.right || 0) - (S || 0)] : a === "yAxis" ? k = c === "horizontal" ? [n.top + n.height - (w.bottom || 0), n.top + (w.top || 0)] : [n.top + (w.top || 0) + (S || 0), n.top + n.height - (w.bottom || 0) - (S || 0)] : k = h.range, x && (k = [k[1], k[0]]);
    var ee = h2(h, i, p), U = ee.scale, G = ee.realScaleType;
    U.domain(v).range(k), p2(U);
    var Y = j2(U, wr(wr({}, h), {}, {
      realScaleType: G
    }));
    a === "xAxis" ? (P = g === "top" && !j || g === "bottom" && j, E = n.left, L = f[_] - P * h.height) : a === "yAxis" && (P = g === "left" && !j || g === "right" && j, E = f[_] - P * h.width, L = n.top);
    var Q = wr(wr(wr({}, h), Y), {}, {
      realScaleType: G,
      x: E,
      y: L,
      scale: U,
      width: a === "xAxis" ? n.width : h.width,
      height: a === "yAxis" ? n.height : h.height
    });
    return Q.bandSize = Us(Q, Y), !h.hide && a === "xAxis" ? f[_] += (P ? -1 : 1) * Q.height : h.hide || (f[_] += (P ? -1 : 1) * Q.width), wr(wr({}, m), {}, Kl({}, y, Q));
  }, {});
}, Yk = function(t, r) {
  var n = t.x, a = t.y, i = r.x, o = r.y;
  return {
    x: Math.min(n, i),
    y: Math.min(a, o),
    width: Math.abs(i - n),
    height: Math.abs(o - a)
  };
}, uq = function(t) {
  var r = t.x1, n = t.y1, a = t.x2, i = t.y2;
  return Yk({
    x: r,
    y: n
  }, {
    x: a,
    y: i
  });
}, Xk = /* @__PURE__ */ (function() {
  function e(t) {
    sq(this, e), this.scale = t;
  }
  return lq(e, [{
    key: "domain",
    get: function() {
      return this.scale.domain;
    }
  }, {
    key: "range",
    get: function() {
      return this.scale.range;
    }
  }, {
    key: "rangeMin",
    get: function() {
      return this.range()[0];
    }
  }, {
    key: "rangeMax",
    get: function() {
      return this.range()[1];
    }
  }, {
    key: "bandwidth",
    get: function() {
      return this.scale.bandwidth;
    }
  }, {
    key: "apply",
    value: function(r) {
      var n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, a = n.bandAware, i = n.position;
      if (r !== void 0) {
        if (i)
          switch (i) {
            case "start":
              return this.scale(r);
            case "middle": {
              var o = this.bandwidth ? this.bandwidth() / 2 : 0;
              return this.scale(r) + o;
            }
            case "end": {
              var l = this.bandwidth ? this.bandwidth() : 0;
              return this.scale(r) + l;
            }
            default:
              return this.scale(r);
          }
        if (a) {
          var c = this.bandwidth ? this.bandwidth() / 2 : 0;
          return this.scale(r) + c;
        }
        return this.scale(r);
      }
    }
  }, {
    key: "isInRange",
    value: function(r) {
      var n = this.range(), a = n[0], i = n[n.length - 1];
      return a <= i ? r >= a && r <= i : r >= i && r <= a;
    }
  }], [{
    key: "create",
    value: function(r) {
      return new e(r);
    }
  }]);
})();
Kl(Xk, "EPS", 1e-4);
var Yp = function(t) {
  var r = Object.keys(t).reduce(function(n, a) {
    return wr(wr({}, n), {}, Kl({}, a, Xk.create(t[a])));
  }, {});
  return wr(wr({}, r), {}, {
    apply: function(a) {
      var i = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, o = i.bandAware, l = i.position;
      return LF(a, function(c, u) {
        return r[u].apply(c, {
          bandAware: o,
          position: l
        });
      });
    },
    isInRange: function(a) {
      return Bk(a, function(i, o) {
        return r[o].isInRange(i);
      });
    }
  });
};
function dq(e) {
  return (e % 180 + 180) % 180;
}
var fq = function(t) {
  var r = t.width, n = t.height, a = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0, i = dq(a), o = i * Math.PI / 180, l = Math.atan(n / r), c = o > l && o < Math.PI - l ? n / Math.sin(o) : r / Math.cos(o);
  return Math.abs(c);
}, lm, Qw;
function mq() {
  if (Qw) return lm;
  Qw = 1;
  var e = jn(), t = Co(), r = Nl();
  function n(a) {
    return function(i, o, l) {
      var c = Object(i);
      if (!t(i)) {
        var u = e(o, 3);
        i = r(i), o = function(f) {
          return u(c[f], f, c);
        };
      }
      var d = a(i, o, l);
      return d > -1 ? c[u ? i[d] : d] : void 0;
    };
  }
  return lm = n, lm;
}
var cm, Jw;
function hq() {
  if (Jw) return cm;
  Jw = 1;
  var e = Fk();
  function t(r) {
    var n = e(r), a = n % 1;
    return n === n ? a ? n - a : n : 0;
  }
  return cm = t, cm;
}
var um, Zw;
function pq() {
  if (Zw) return um;
  Zw = 1;
  var e = oS(), t = jn(), r = hq(), n = Math.max;
  function a(i, o, l) {
    var c = i == null ? 0 : i.length;
    if (!c)
      return -1;
    var u = l == null ? 0 : r(l);
    return u < 0 && (u = n(c + u, 0)), e(i, t(o, 3), u);
  }
  return um = a, um;
}
var dm, e_;
function gq() {
  if (e_) return dm;
  e_ = 1;
  var e = mq(), t = pq(), r = e(t);
  return dm = r, dm;
}
var yq = gq();
const vq = /* @__PURE__ */ st(yq);
var bq = _j();
const xq = /* @__PURE__ */ st(bq);
var wq = xq(function(e) {
  return {
    x: e.left,
    y: e.top,
    width: e.width,
    height: e.height
  };
}, function(e) {
  return ["l", e.left, "t", e.top, "w", e.width, "h", e.height].join("");
});
function nl(e) {
  "@babel/helpers - typeof";
  return nl = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, nl(e);
}
var Xp = /* @__PURE__ */ Kn(void 0), Qp = /* @__PURE__ */ Kn(void 0), Qk = /* @__PURE__ */ Kn(void 0), Jk = /* @__PURE__ */ Kn({}), Zk = /* @__PURE__ */ Kn(void 0), eE = /* @__PURE__ */ Kn(0), tE = /* @__PURE__ */ Kn(0), t_ = function(t) {
  var r = t.state, n = r.xAxisMap, a = r.yAxisMap, i = r.offset, o = t.clipPathId, l = t.children, c = t.width, u = t.height, d = wq(i);
  return /* @__PURE__ */ I.createElement(Xp.Provider, {
    value: n
  }, /* @__PURE__ */ I.createElement(Qp.Provider, {
    value: a
  }, /* @__PURE__ */ I.createElement(Jk.Provider, {
    value: i
  }, /* @__PURE__ */ I.createElement(Qk.Provider, {
    value: d
  }, /* @__PURE__ */ I.createElement(Zk.Provider, {
    value: o
  }, /* @__PURE__ */ I.createElement(eE.Provider, {
    value: u
  }, /* @__PURE__ */ I.createElement(tE.Provider, {
    value: c
  }, l)))))));
}, _q = function() {
  return rn(Zk);
};
function rE(e) {
  var t = Object.keys(e);
  return t.length === 0 ? "There are no available ids." : "Available ids are: ".concat(t, ".");
}
var nE = function(t) {
  var r = rn(Xp);
  r == null && (process.env.NODE_ENV !== "production" ? Yt(!1, "Could not find Recharts context; are you sure this is rendered inside a Recharts wrapper component?") : Yt());
  var n = r[t];
  return n == null && (process.env.NODE_ENV !== "production" ? Yt(!1, 'Could not find xAxis by id "'.concat(t, '" [').concat(nl(t), "]. ").concat(rE(r))) : Yt()), n;
}, jq = function() {
  var t = rn(Xp);
  return pn(t);
}, Sq = function() {
  var t = rn(Qp), r = vq(t, function(n) {
    return Bk(n.domain, Number.isFinite);
  });
  return r || pn(t);
}, aE = function(t) {
  var r = rn(Qp);
  r == null && (process.env.NODE_ENV !== "production" ? Yt(!1, "Could not find Recharts context; are you sure this is rendered inside a Recharts wrapper component?") : Yt());
  var n = r[t];
  return n == null && (process.env.NODE_ENV !== "production" ? Yt(!1, 'Could not find yAxis by id "'.concat(t, '" [').concat(nl(t), "]. ").concat(rE(r))) : Yt()), n;
}, kq = function() {
  var t = rn(Qk);
  return t;
}, Eq = function() {
  return rn(Jk);
}, Jp = function() {
  return rn(tE);
}, Zp = function() {
  return rn(eE);
};
function qa(e) {
  "@babel/helpers - typeof";
  return qa = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, qa(e);
}
function Aq(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function Cq(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, oE(n.key), n);
  }
}
function Tq(e, t, r) {
  return t && Cq(e.prototype, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function Pq(e, t, r) {
  return t = al(t), Oq(e, iE() ? Reflect.construct(t, r || [], al(e).constructor) : t.apply(e, r));
}
function Oq(e, t) {
  if (t && (qa(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return Nq(e);
}
function Nq(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function iE() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (iE = function() {
    return !!e;
  })();
}
function al(e) {
  return al = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, al(e);
}
function Mq(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && Eh(e, t);
}
function Eh(e, t) {
  return Eh = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, Eh(e, t);
}
function r_(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function n_(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? r_(Object(r), !0).forEach(function(n) {
      eg(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : r_(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function eg(e, t, r) {
  return t = oE(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function oE(e) {
  var t = Rq(e, "string");
  return qa(t) == "symbol" ? t : t + "";
}
function Rq(e, t) {
  if (qa(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (qa(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
function Iq(e, t) {
  return Fq(e) || Lq(e, t) || $q(e, t) || Dq();
}
function Dq() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function $q(e, t) {
  if (e) {
    if (typeof e == "string") return a_(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return a_(e, t);
  }
}
function a_(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function Lq(e, t) {
  var r = e == null ? null : typeof Symbol < "u" && e[Symbol.iterator] || e["@@iterator"];
  if (r != null) {
    var n, a, i, o, l = [], c = !0, u = !1;
    try {
      if (i = (r = r.call(e)).next, t !== 0) for (; !(c = (n = i.call(r)).done) && (l.push(n.value), l.length !== t); c = !0) ;
    } catch (d) {
      u = !0, a = d;
    } finally {
      try {
        if (!c && r.return != null && (o = r.return(), Object(o) !== o)) return;
      } finally {
        if (u) throw a;
      }
    }
    return l;
  }
}
function Fq(e) {
  if (Array.isArray(e)) return e;
}
function Ah() {
  return Ah = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Ah.apply(this, arguments);
}
var qq = function(t, r) {
  var n;
  return /* @__PURE__ */ I.isValidElement(t) ? n = /* @__PURE__ */ I.cloneElement(t, r) : $e(t) ? n = t(r) : n = /* @__PURE__ */ I.createElement("line", Ah({}, r, {
    className: "recharts-reference-line-line"
  })), n;
}, Uq = function(t, r, n, a, i, o, l, c, u) {
  var d = i.x, f = i.y, p = i.width, m = i.height;
  if (n) {
    var y = u.y, h = t.y.apply(y, {
      position: o
    });
    if (Dr(u, "discard") && !t.y.isInRange(h))
      return null;
    var g = [{
      x: d + p,
      y: h
    }, {
      x: d,
      y: h
    }];
    return c === "left" ? g.reverse() : g;
  }
  if (r) {
    var v = u.x, b = t.x.apply(v, {
      position: o
    });
    if (Dr(u, "discard") && !t.x.isInRange(b))
      return null;
    var w = [{
      x: b,
      y: f + m
    }, {
      x: b,
      y: f
    }];
    return l === "top" ? w.reverse() : w;
  }
  if (a) {
    var j = u.segment, x = j.map(function(_) {
      return t.apply(_, {
        position: o
      });
    });
    return Dr(u, "discard") && RF(x, function(_) {
      return !t.isInRange(_);
    }) ? null : x;
  }
  return null;
};
function zq(e) {
  var t = e.x, r = e.y, n = e.segment, a = e.xAxisId, i = e.yAxisId, o = e.shape, l = e.className, c = e.alwaysShow, u = _q(), d = nE(a), f = aE(i), p = kq();
  if (!u || !p)
    return null;
  Xr(c === void 0, 'The alwaysShow prop is deprecated. Please use ifOverflow="extendDomain" instead.');
  var m = Yp({
    x: d.scale,
    y: f.scale
  }), y = Pt(t), h = Pt(r), g = n && n.length === 2, v = Uq(m, y, h, g, p, e.position, d.orientation, f.orientation, e);
  if (!v)
    return null;
  var b = Iq(v, 2), w = b[0], j = w.x, x = w.y, _ = b[1], S = _.x, k = _.y, E = Dr(e, "hidden") ? "url(#".concat(u, ")") : void 0, L = n_(n_({
    clipPath: E
  }, ze(e, !0)), {}, {
    x1: j,
    y1: x,
    x2: S,
    y2: k
  });
  return /* @__PURE__ */ I.createElement(yt, {
    className: Ve("recharts-reference-line", l)
  }, qq(o, L), qt.renderCallByParent(e, uq({
    x1: j,
    y1: x,
    x2: S,
    y2: k
  })));
}
var tg = /* @__PURE__ */ (function(e) {
  function t() {
    return Aq(this, t), Pq(this, t, arguments);
  }
  return Mq(t, e), Tq(t, [{
    key: "render",
    value: function() {
      return /* @__PURE__ */ I.createElement(zq, this.props);
    }
  }]);
})(I.Component);
eg(tg, "displayName", "ReferenceLine");
eg(tg, "defaultProps", {
  isFront: !1,
  ifOverflow: "discard",
  xAxisId: 0,
  yAxisId: 0,
  fill: "none",
  stroke: "#ccc",
  fillOpacity: 1,
  strokeWidth: 1,
  position: "middle"
});
function Ch() {
  return Ch = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Ch.apply(this, arguments);
}
function Ua(e) {
  "@babel/helpers - typeof";
  return Ua = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Ua(e);
}
function i_(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function o_(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? i_(Object(r), !0).forEach(function(n) {
      Yl(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : i_(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function Bq(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function Wq(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, lE(n.key), n);
  }
}
function Vq(e, t, r) {
  return t && Wq(e.prototype, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function Hq(e, t, r) {
  return t = il(t), Gq(e, sE() ? Reflect.construct(t, r || [], il(e).constructor) : t.apply(e, r));
}
function Gq(e, t) {
  if (t && (Ua(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return Kq(e);
}
function Kq(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function sE() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (sE = function() {
    return !!e;
  })();
}
function il(e) {
  return il = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, il(e);
}
function Yq(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && Th(e, t);
}
function Th(e, t) {
  return Th = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, Th(e, t);
}
function Yl(e, t, r) {
  return t = lE(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function lE(e) {
  var t = Xq(e, "string");
  return Ua(t) == "symbol" ? t : t + "";
}
function Xq(e, t) {
  if (Ua(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Ua(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
var Qq = function(t) {
  var r = t.x, n = t.y, a = t.xAxis, i = t.yAxis, o = Yp({
    x: a.scale,
    y: i.scale
  }), l = o.apply({
    x: r,
    y: n
  }, {
    bandAware: !0
  });
  return Dr(t, "discard") && !o.isInRange(l) ? null : l;
}, Xl = /* @__PURE__ */ (function(e) {
  function t() {
    return Bq(this, t), Hq(this, t, arguments);
  }
  return Yq(t, e), Vq(t, [{
    key: "render",
    value: function() {
      var n = this.props, a = n.x, i = n.y, o = n.r, l = n.alwaysShow, c = n.clipPathId, u = Pt(a), d = Pt(i);
      if (Xr(l === void 0, 'The alwaysShow prop is deprecated. Please use ifOverflow="extendDomain" instead.'), !u || !d)
        return null;
      var f = Qq(this.props);
      if (!f)
        return null;
      var p = f.x, m = f.y, y = this.props, h = y.shape, g = y.className, v = Dr(this.props, "hidden") ? "url(#".concat(c, ")") : void 0, b = o_(o_({
        clipPath: v
      }, ze(this.props, !0)), {}, {
        cx: p,
        cy: m
      });
      return /* @__PURE__ */ I.createElement(yt, {
        className: Ve("recharts-reference-dot", g)
      }, t.renderDot(h, b), qt.renderCallByParent(this.props, {
        x: p - o,
        y: m - o,
        width: 2 * o,
        height: 2 * o
      }));
    }
  }]);
})(I.Component);
Yl(Xl, "displayName", "ReferenceDot");
Yl(Xl, "defaultProps", {
  isFront: !1,
  ifOverflow: "discard",
  xAxisId: 0,
  yAxisId: 0,
  r: 10,
  fill: "#fff",
  stroke: "#ccc",
  fillOpacity: 1,
  strokeWidth: 1
});
Yl(Xl, "renderDot", function(e, t) {
  var r;
  return /* @__PURE__ */ I.isValidElement(e) ? r = /* @__PURE__ */ I.cloneElement(e, t) : $e(e) ? r = e(t) : r = /* @__PURE__ */ I.createElement(Kp, Ch({}, t, {
    cx: t.cx,
    cy: t.cy,
    className: "recharts-reference-dot-dot"
  })), r;
});
function Ph() {
  return Ph = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Ph.apply(this, arguments);
}
function za(e) {
  "@babel/helpers - typeof";
  return za = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, za(e);
}
function s_(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function l_(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? s_(Object(r), !0).forEach(function(n) {
      Ql(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : s_(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function Jq(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function Zq(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, uE(n.key), n);
  }
}
function e5(e, t, r) {
  return t && Zq(e.prototype, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function t5(e, t, r) {
  return t = ol(t), r5(e, cE() ? Reflect.construct(t, r || [], ol(e).constructor) : t.apply(e, r));
}
function r5(e, t) {
  if (t && (za(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return n5(e);
}
function n5(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function cE() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (cE = function() {
    return !!e;
  })();
}
function ol(e) {
  return ol = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, ol(e);
}
function a5(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && Oh(e, t);
}
function Oh(e, t) {
  return Oh = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, Oh(e, t);
}
function Ql(e, t, r) {
  return t = uE(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function uE(e) {
  var t = i5(e, "string");
  return za(t) == "symbol" ? t : t + "";
}
function i5(e, t) {
  if (za(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (za(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
var o5 = function(t, r, n, a, i) {
  var o = i.x1, l = i.x2, c = i.y1, u = i.y2, d = i.xAxis, f = i.yAxis;
  if (!d || !f) return null;
  var p = Yp({
    x: d.scale,
    y: f.scale
  }), m = {
    x: t ? p.x.apply(o, {
      position: "start"
    }) : p.x.rangeMin,
    y: n ? p.y.apply(c, {
      position: "start"
    }) : p.y.rangeMin
  }, y = {
    x: r ? p.x.apply(l, {
      position: "end"
    }) : p.x.rangeMax,
    y: a ? p.y.apply(u, {
      position: "end"
    }) : p.y.rangeMax
  };
  return Dr(i, "discard") && (!p.isInRange(m) || !p.isInRange(y)) ? null : Yk(m, y);
}, Jl = /* @__PURE__ */ (function(e) {
  function t() {
    return Jq(this, t), t5(this, t, arguments);
  }
  return a5(t, e), e5(t, [{
    key: "render",
    value: function() {
      var n = this.props, a = n.x1, i = n.x2, o = n.y1, l = n.y2, c = n.className, u = n.alwaysShow, d = n.clipPathId;
      Xr(u === void 0, 'The alwaysShow prop is deprecated. Please use ifOverflow="extendDomain" instead.');
      var f = Pt(a), p = Pt(i), m = Pt(o), y = Pt(l), h = this.props.shape;
      if (!f && !p && !m && !y && !h)
        return null;
      var g = o5(f, p, m, y, this.props);
      if (!g && !h)
        return null;
      var v = Dr(this.props, "hidden") ? "url(#".concat(d, ")") : void 0;
      return /* @__PURE__ */ I.createElement(yt, {
        className: Ve("recharts-reference-area", c)
      }, t.renderRect(h, l_(l_({
        clipPath: v
      }, ze(this.props, !0)), g)), qt.renderCallByParent(this.props, g));
    }
  }]);
})(I.Component);
Ql(Jl, "displayName", "ReferenceArea");
Ql(Jl, "defaultProps", {
  isFront: !1,
  ifOverflow: "discard",
  xAxisId: 0,
  yAxisId: 0,
  r: 10,
  fill: "#ccc",
  fillOpacity: 0.5,
  stroke: "none",
  strokeWidth: 1
});
Ql(Jl, "renderRect", function(e, t) {
  var r;
  return /* @__PURE__ */ I.isValidElement(e) ? r = /* @__PURE__ */ I.cloneElement(e, t) : $e(e) ? r = e(t) : r = /* @__PURE__ */ I.createElement(Gp, Ph({}, t, {
    className: "recharts-reference-area-rect"
  })), r;
});
function dE(e, t, r) {
  if (t < 1)
    return [];
  if (t === 1 && r === void 0)
    return e;
  for (var n = [], a = 0; a < e.length; a += t)
    n.push(e[a]);
  return n;
}
function s5(e, t, r) {
  var n = {
    width: e.width + t.width,
    height: e.height + t.height
  };
  return fq(n, r);
}
function l5(e, t, r) {
  var n = r === "width", a = e.x, i = e.y, o = e.width, l = e.height;
  return t === 1 ? {
    start: n ? a : i,
    end: n ? a + o : i + l
  } : {
    start: n ? a + o : i + l,
    end: n ? a : i
  };
}
function sl(e, t, r, n, a) {
  if (e * t < e * n || e * t > e * a)
    return !1;
  var i = r();
  return e * (t - e * i / 2 - n) >= 0 && e * (t + e * i / 2 - a) <= 0;
}
function c5(e, t) {
  return dE(e, t + 1);
}
function u5(e, t, r, n, a) {
  for (var i = (n || []).slice(), o = t.start, l = t.end, c = 0, u = 1, d = o, f = function() {
    var y = n == null ? void 0 : n[c];
    if (y === void 0)
      return {
        v: dE(n, u)
      };
    var h = c, g, v = function() {
      return g === void 0 && (g = r(y, h)), g;
    }, b = y.coordinate, w = c === 0 || sl(e, b, v, d, l);
    w || (c = 0, d = o, u += 1), w && (d = b + e * (v() / 2 + a), c += u);
  }, p; u <= i.length; )
    if (p = f(), p) return p.v;
  return [];
}
function bo(e) {
  "@babel/helpers - typeof";
  return bo = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, bo(e);
}
function c_(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function Ft(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? c_(Object(r), !0).forEach(function(n) {
      d5(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : c_(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function d5(e, t, r) {
  return t = f5(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function f5(e) {
  var t = m5(e, "string");
  return bo(t) == "symbol" ? t : t + "";
}
function m5(e, t) {
  if (bo(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (bo(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function h5(e, t, r, n, a) {
  for (var i = (n || []).slice(), o = i.length, l = t.start, c = t.end, u = function(p) {
    var m = i[p], y, h = function() {
      return y === void 0 && (y = r(m, p)), y;
    };
    if (p === o - 1) {
      var g = e * (m.coordinate + e * h() / 2 - c);
      i[p] = m = Ft(Ft({}, m), {}, {
        tickCoord: g > 0 ? m.coordinate - g * e : m.coordinate
      });
    } else
      i[p] = m = Ft(Ft({}, m), {}, {
        tickCoord: m.coordinate
      });
    var v = sl(e, m.tickCoord, h, l, c);
    v && (c = m.tickCoord - e * (h() / 2 + a), i[p] = Ft(Ft({}, m), {}, {
      isShow: !0
    }));
  }, d = o - 1; d >= 0; d--)
    u(d);
  return i;
}
function p5(e, t, r, n, a, i) {
  var o = (n || []).slice(), l = o.length, c = t.start, u = t.end;
  if (i) {
    var d = n[l - 1], f = r(d, l - 1), p = e * (d.coordinate + e * f / 2 - u);
    o[l - 1] = d = Ft(Ft({}, d), {}, {
      tickCoord: p > 0 ? d.coordinate - p * e : d.coordinate
    });
    var m = sl(e, d.tickCoord, function() {
      return f;
    }, c, u);
    m && (u = d.tickCoord - e * (f / 2 + a), o[l - 1] = Ft(Ft({}, d), {}, {
      isShow: !0
    }));
  }
  for (var y = i ? l - 1 : l, h = function(b) {
    var w = o[b], j, x = function() {
      return j === void 0 && (j = r(w, b)), j;
    };
    if (b === 0) {
      var _ = e * (w.coordinate - e * x() / 2 - c);
      o[b] = w = Ft(Ft({}, w), {}, {
        tickCoord: _ < 0 ? w.coordinate - _ * e : w.coordinate
      });
    } else
      o[b] = w = Ft(Ft({}, w), {}, {
        tickCoord: w.coordinate
      });
    var S = sl(e, w.tickCoord, x, c, u);
    S && (c = w.tickCoord + e * (x() / 2 + a), o[b] = Ft(Ft({}, w), {}, {
      isShow: !0
    }));
  }, g = 0; g < y; g++)
    h(g);
  return o;
}
function rg(e, t, r) {
  var n = e.tick, a = e.ticks, i = e.viewBox, o = e.minTickGap, l = e.orientation, c = e.interval, u = e.tickFormatter, d = e.unit, f = e.angle;
  if (!a || !a.length || !n)
    return [];
  if (me(c) || ni.isSsr)
    return c5(a, typeof c == "number" && me(c) ? c : 0);
  var p = [], m = l === "top" || l === "bottom" ? "width" : "height", y = d && m === "width" ? Ni(d, {
    fontSize: t,
    letterSpacing: r
  }) : {
    width: 0,
    height: 0
  }, h = function(w, j) {
    var x = $e(u) ? u(w.value, j) : w.value;
    return m === "width" ? s5(Ni(x, {
      fontSize: t,
      letterSpacing: r
    }), y, f) : Ni(x, {
      fontSize: t,
      letterSpacing: r
    })[m];
  }, g = a.length >= 2 ? Ar(a[1].coordinate - a[0].coordinate) : 1, v = l5(i, g, m);
  return c === "equidistantPreserveStart" ? u5(g, v, h, a, o) : (c === "preserveStart" || c === "preserveStartEnd" ? p = p5(g, v, h, a, o, c === "preserveStartEnd") : p = h5(g, v, h, a, o), p.filter(function(b) {
    return b.isShow;
  }));
}
var g5 = ["viewBox"], y5 = ["viewBox"], v5 = ["ticks"];
function Ba(e) {
  "@babel/helpers - typeof";
  return Ba = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Ba(e);
}
function wa() {
  return wa = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, wa.apply(this, arguments);
}
function u_(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function Et(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? u_(Object(r), !0).forEach(function(n) {
      ng(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : u_(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function fm(e, t) {
  if (e == null) return {};
  var r = b5(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function b5(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
function x5(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function d_(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, mE(n.key), n);
  }
}
function w5(e, t, r) {
  return t && d_(e.prototype, t), r && d_(e, r), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function _5(e, t, r) {
  return t = ll(t), j5(e, fE() ? Reflect.construct(t, r || [], ll(e).constructor) : t.apply(e, r));
}
function j5(e, t) {
  if (t && (Ba(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return S5(e);
}
function S5(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function fE() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (fE = function() {
    return !!e;
  })();
}
function ll(e) {
  return ll = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, ll(e);
}
function k5(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && Nh(e, t);
}
function Nh(e, t) {
  return Nh = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, Nh(e, t);
}
function ng(e, t, r) {
  return t = mE(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function mE(e) {
  var t = E5(e, "string");
  return Ba(t) == "symbol" ? t : t + "";
}
function E5(e, t) {
  if (Ba(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Ba(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
var si = /* @__PURE__ */ (function(e) {
  function t(r) {
    var n;
    return x5(this, t), n = _5(this, t, [r]), n.state = {
      fontSize: "",
      letterSpacing: ""
    }, n;
  }
  return k5(t, e), w5(t, [{
    key: "shouldComponentUpdate",
    value: function(n, a) {
      var i = n.viewBox, o = fm(n, g5), l = this.props, c = l.viewBox, u = fm(l, y5);
      return !ja(i, c) || !ja(o, u) || !ja(a, this.state);
    }
  }, {
    key: "componentDidMount",
    value: function() {
      var n = this.layerReference;
      if (n) {
        var a = n.getElementsByClassName("recharts-cartesian-axis-tick-value")[0];
        a && this.setState({
          fontSize: window.getComputedStyle(a).fontSize,
          letterSpacing: window.getComputedStyle(a).letterSpacing
        });
      }
    }
    /**
     * Calculate the coordinates of endpoints in ticks
     * @param  {Object} data The data of a simple tick
     * @return {Object} (x1, y1): The coordinate of endpoint close to tick text
     *  (x2, y2): The coordinate of endpoint close to axis
     */
  }, {
    key: "getTickLineCoord",
    value: function(n) {
      var a = this.props, i = a.x, o = a.y, l = a.width, c = a.height, u = a.orientation, d = a.tickSize, f = a.mirror, p = a.tickMargin, m, y, h, g, v, b, w = f ? -1 : 1, j = n.tickSize || d, x = me(n.tickCoord) ? n.tickCoord : n.coordinate;
      switch (u) {
        case "top":
          m = y = n.coordinate, g = o + +!f * c, h = g - w * j, b = h - w * p, v = x;
          break;
        case "left":
          h = g = n.coordinate, y = i + +!f * l, m = y - w * j, v = m - w * p, b = x;
          break;
        case "right":
          h = g = n.coordinate, y = i + +f * l, m = y + w * j, v = m + w * p, b = x;
          break;
        default:
          m = y = n.coordinate, g = o + +f * c, h = g + w * j, b = h + w * p, v = x;
          break;
      }
      return {
        line: {
          x1: m,
          y1: h,
          x2: y,
          y2: g
        },
        tick: {
          x: v,
          y: b
        }
      };
    }
  }, {
    key: "getTickTextAnchor",
    value: function() {
      var n = this.props, a = n.orientation, i = n.mirror, o;
      switch (a) {
        case "left":
          o = i ? "start" : "end";
          break;
        case "right":
          o = i ? "end" : "start";
          break;
        default:
          o = "middle";
          break;
      }
      return o;
    }
  }, {
    key: "getTickVerticalAnchor",
    value: function() {
      var n = this.props, a = n.orientation, i = n.mirror, o = "end";
      switch (a) {
        case "left":
        case "right":
          o = "middle";
          break;
        case "top":
          o = i ? "start" : "end";
          break;
        default:
          o = i ? "end" : "start";
          break;
      }
      return o;
    }
  }, {
    key: "renderAxisLine",
    value: function() {
      var n = this.props, a = n.x, i = n.y, o = n.width, l = n.height, c = n.orientation, u = n.mirror, d = n.axisLine, f = Et(Et(Et({}, ze(this.props, !1)), ze(d, !1)), {}, {
        fill: "none"
      });
      if (c === "top" || c === "bottom") {
        var p = +(c === "top" && !u || c === "bottom" && u);
        f = Et(Et({}, f), {}, {
          x1: a,
          y1: i + p * l,
          x2: a + o,
          y2: i + p * l
        });
      } else {
        var m = +(c === "left" && !u || c === "right" && u);
        f = Et(Et({}, f), {}, {
          x1: a + m * o,
          y1: i,
          x2: a + m * o,
          y2: i + l
        });
      }
      return /* @__PURE__ */ I.createElement("line", wa({}, f, {
        className: Ve("recharts-cartesian-axis-line", pr(d, "className"))
      }));
    }
  }, {
    key: "renderTicks",
    value: (
      /**
       * render the ticks
       * @param {Array} ticks The ticks to actually render (overrides what was passed in props)
       * @param {string} fontSize Fontsize to consider for tick spacing
       * @param {string} letterSpacing Letterspacing to consider for tick spacing
       * @return {ReactComponent} renderedTicks
       */
      function(n, a, i) {
        var o = this, l = this.props, c = l.tickLine, u = l.stroke, d = l.tick, f = l.tickFormatter, p = l.unit, m = rg(Et(Et({}, this.props), {}, {
          ticks: n
        }), a, i), y = this.getTickTextAnchor(), h = this.getTickVerticalAnchor(), g = ze(this.props, !1), v = ze(d, !1), b = Et(Et({}, g), {}, {
          fill: "none"
        }, ze(c, !1)), w = m.map(function(j, x) {
          var _ = o.getTickLineCoord(j), S = _.line, k = _.tick, E = Et(Et(Et(Et({
            textAnchor: y,
            verticalAnchor: h
          }, g), {}, {
            stroke: "none",
            fill: u
          }, v), k), {}, {
            index: x,
            payload: j,
            visibleTicksCount: m.length,
            tickFormatter: f
          });
          return /* @__PURE__ */ I.createElement(yt, wa({
            className: "recharts-cartesian-axis-tick",
            key: "tick-".concat(j.value, "-").concat(j.coordinate, "-").concat(j.tickCoord)
          }, gs(o.props, j, x)), c && /* @__PURE__ */ I.createElement("line", wa({}, b, S, {
            className: Ve("recharts-cartesian-axis-tick-line", pr(c, "className"))
          })), d && t.renderTickItem(d, E, "".concat($e(f) ? f(j.value, x) : j.value).concat(p || "")));
        });
        return /* @__PURE__ */ I.createElement("g", {
          className: "recharts-cartesian-axis-ticks"
        }, w);
      }
    )
  }, {
    key: "render",
    value: function() {
      var n = this, a = this.props, i = a.axisLine, o = a.width, l = a.height, c = a.ticksGenerator, u = a.className, d = a.hide;
      if (d)
        return null;
      var f = this.props, p = f.ticks, m = fm(f, v5), y = p;
      return $e(c) && (y = p && p.length > 0 ? c(this.props) : c(m)), o <= 0 || l <= 0 || !y || !y.length ? null : /* @__PURE__ */ I.createElement(yt, {
        className: Ve("recharts-cartesian-axis", u),
        ref: function(g) {
          n.layerReference = g;
        }
      }, i && this.renderAxisLine(), this.renderTicks(y, this.state.fontSize, this.state.letterSpacing), qt.renderCallByParent(this.props));
    }
  }], [{
    key: "renderTickItem",
    value: function(n, a, i) {
      var o, l = Ve(a.className, "recharts-cartesian-axis-tick-value");
      return /* @__PURE__ */ I.isValidElement(n) ? o = /* @__PURE__ */ I.cloneElement(n, Et(Et({}, a), {}, {
        className: l
      })) : $e(n) ? o = n(Et(Et({}, a), {}, {
        className: l
      })) : o = /* @__PURE__ */ I.createElement(As, wa({}, a, {
        className: "recharts-cartesian-axis-tick-value"
      }), i), o;
    }
  }]);
})(B_);
ng(si, "displayName", "CartesianAxis");
ng(si, "defaultProps", {
  x: 0,
  y: 0,
  width: 0,
  height: 0,
  viewBox: {
    x: 0,
    y: 0,
    width: 0,
    height: 0
  },
  // The orientation of axis
  orientation: "bottom",
  // The ticks
  ticks: [],
  stroke: "#666",
  tickLine: !0,
  axisLine: !0,
  tick: !0,
  mirror: !1,
  minTickGap: 5,
  // The width or height of tick
  tickSize: 6,
  tickMargin: 2,
  interval: "preserveEnd"
});
var A5 = ["x1", "y1", "x2", "y2", "key"], C5 = ["offset"];
function Gn(e) {
  "@babel/helpers - typeof";
  return Gn = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Gn(e);
}
function f_(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function Ut(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? f_(Object(r), !0).forEach(function(n) {
      T5(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : f_(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function T5(e, t, r) {
  return t = P5(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function P5(e) {
  var t = O5(e, "string");
  return Gn(t) == "symbol" ? t : t + "";
}
function O5(e, t) {
  if (Gn(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Gn(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function Ln() {
  return Ln = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Ln.apply(this, arguments);
}
function m_(e, t) {
  if (e == null) return {};
  var r = N5(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function N5(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
var M5 = function(t) {
  var r = t.fill;
  if (!r || r === "none")
    return null;
  var n = t.fillOpacity, a = t.x, i = t.y, o = t.width, l = t.height, c = t.ry;
  return /* @__PURE__ */ I.createElement("rect", {
    x: a,
    y: i,
    ry: c,
    width: o,
    height: l,
    stroke: "none",
    fill: r,
    fillOpacity: n,
    className: "recharts-cartesian-grid-bg"
  });
};
function hE(e, t) {
  var r;
  if (/* @__PURE__ */ I.isValidElement(e))
    r = /* @__PURE__ */ I.cloneElement(e, t);
  else if ($e(e))
    r = e(t);
  else {
    var n = t.x1, a = t.y1, i = t.x2, o = t.y2, l = t.key, c = m_(t, A5), u = ze(c, !1);
    u.offset;
    var d = m_(u, C5);
    r = /* @__PURE__ */ I.createElement("line", Ln({}, d, {
      x1: n,
      y1: a,
      x2: i,
      y2: o,
      fill: "none",
      key: l
    }));
  }
  return r;
}
function R5(e) {
  var t = e.x, r = e.width, n = e.horizontal, a = n === void 0 ? !0 : n, i = e.horizontalPoints;
  if (!a || !i || !i.length)
    return null;
  var o = i.map(function(l, c) {
    var u = Ut(Ut({}, e), {}, {
      x1: t,
      y1: l,
      x2: t + r,
      y2: l,
      key: "line-".concat(c),
      index: c
    });
    return hE(a, u);
  });
  return /* @__PURE__ */ I.createElement("g", {
    className: "recharts-cartesian-grid-horizontal"
  }, o);
}
function I5(e) {
  var t = e.y, r = e.height, n = e.vertical, a = n === void 0 ? !0 : n, i = e.verticalPoints;
  if (!a || !i || !i.length)
    return null;
  var o = i.map(function(l, c) {
    var u = Ut(Ut({}, e), {}, {
      x1: l,
      y1: t,
      x2: l,
      y2: t + r,
      key: "line-".concat(c),
      index: c
    });
    return hE(a, u);
  });
  return /* @__PURE__ */ I.createElement("g", {
    className: "recharts-cartesian-grid-vertical"
  }, o);
}
function D5(e) {
  var t = e.horizontalFill, r = e.fillOpacity, n = e.x, a = e.y, i = e.width, o = e.height, l = e.horizontalPoints, c = e.horizontal, u = c === void 0 ? !0 : c;
  if (!u || !t || !t.length)
    return null;
  var d = l.map(function(p) {
    return Math.round(p + a - a);
  }).sort(function(p, m) {
    return p - m;
  });
  a !== d[0] && d.unshift(0);
  var f = d.map(function(p, m) {
    var y = !d[m + 1], h = y ? a + o - p : d[m + 1] - p;
    if (h <= 0)
      return null;
    var g = m % t.length;
    return /* @__PURE__ */ I.createElement("rect", {
      key: "react-".concat(m),
      y: p,
      x: n,
      height: h,
      width: i,
      stroke: "none",
      fill: t[g],
      fillOpacity: r,
      className: "recharts-cartesian-grid-bg"
    });
  });
  return /* @__PURE__ */ I.createElement("g", {
    className: "recharts-cartesian-gridstripes-horizontal"
  }, f);
}
function $5(e) {
  var t = e.vertical, r = t === void 0 ? !0 : t, n = e.verticalFill, a = e.fillOpacity, i = e.x, o = e.y, l = e.width, c = e.height, u = e.verticalPoints;
  if (!r || !n || !n.length)
    return null;
  var d = u.map(function(p) {
    return Math.round(p + i - i);
  }).sort(function(p, m) {
    return p - m;
  });
  i !== d[0] && d.unshift(0);
  var f = d.map(function(p, m) {
    var y = !d[m + 1], h = y ? i + l - p : d[m + 1] - p;
    if (h <= 0)
      return null;
    var g = m % n.length;
    return /* @__PURE__ */ I.createElement("rect", {
      key: "react-".concat(m),
      x: p,
      y: o,
      width: h,
      height: c,
      stroke: "none",
      fill: n[g],
      fillOpacity: a,
      className: "recharts-cartesian-grid-bg"
    });
  });
  return /* @__PURE__ */ I.createElement("g", {
    className: "recharts-cartesian-gridstripes-vertical"
  }, f);
}
var L5 = function(t, r) {
  var n = t.xAxis, a = t.width, i = t.height, o = t.offset;
  return Sk(rg(Ut(Ut(Ut({}, si.defaultProps), n), {}, {
    ticks: Kr(n, !0),
    viewBox: {
      x: 0,
      y: 0,
      width: a,
      height: i
    }
  })), o.left, o.left + o.width, r);
}, F5 = function(t, r) {
  var n = t.yAxis, a = t.width, i = t.height, o = t.offset;
  return Sk(rg(Ut(Ut(Ut({}, si.defaultProps), n), {}, {
    ticks: Kr(n, !0),
    viewBox: {
      x: 0,
      y: 0,
      width: a,
      height: i
    }
  })), o.top, o.top + o.height, r);
}, la = {
  horizontal: !0,
  vertical: !0,
  stroke: "#ccc",
  fill: "none",
  // The fill of colors of grid lines
  verticalFill: [],
  horizontalFill: []
};
function pE(e) {
  var t, r, n, a, i, o, l = Jp(), c = Zp(), u = Eq(), d = Ut(Ut({}, e), {}, {
    stroke: (t = e.stroke) !== null && t !== void 0 ? t : la.stroke,
    fill: (r = e.fill) !== null && r !== void 0 ? r : la.fill,
    horizontal: (n = e.horizontal) !== null && n !== void 0 ? n : la.horizontal,
    horizontalFill: (a = e.horizontalFill) !== null && a !== void 0 ? a : la.horizontalFill,
    vertical: (i = e.vertical) !== null && i !== void 0 ? i : la.vertical,
    verticalFill: (o = e.verticalFill) !== null && o !== void 0 ? o : la.verticalFill,
    x: me(e.x) ? e.x : u.left,
    y: me(e.y) ? e.y : u.top,
    width: me(e.width) ? e.width : u.width,
    height: me(e.height) ? e.height : u.height
  }), f = d.x, p = d.y, m = d.width, y = d.height, h = d.syncWithTicks, g = d.horizontalValues, v = d.verticalValues, b = jq(), w = Sq();
  if (!me(m) || m <= 0 || !me(y) || y <= 0 || !me(f) || f !== +f || !me(p) || p !== +p)
    return null;
  var j = d.verticalCoordinatesGenerator || L5, x = d.horizontalCoordinatesGenerator || F5, _ = d.horizontalPoints, S = d.verticalPoints;
  if ((!_ || !_.length) && $e(x)) {
    var k = g && g.length, E = x({
      yAxis: w ? Ut(Ut({}, w), {}, {
        ticks: k ? g : w.ticks
      }) : void 0,
      width: l,
      height: c,
      offset: u
    }, k ? !0 : h);
    Xr(Array.isArray(E), "horizontalCoordinatesGenerator should return Array but instead it returned [".concat(Gn(E), "]")), Array.isArray(E) && (_ = E);
  }
  if ((!S || !S.length) && $e(j)) {
    var L = v && v.length, P = j({
      xAxis: b ? Ut(Ut({}, b), {}, {
        ticks: L ? v : b.ticks
      }) : void 0,
      width: l,
      height: c,
      offset: u
    }, L ? !0 : h);
    Xr(Array.isArray(P), "verticalCoordinatesGenerator should return Array but instead it returned [".concat(Gn(P), "]")), Array.isArray(P) && (S = P);
  }
  return /* @__PURE__ */ I.createElement("g", {
    className: "recharts-cartesian-grid"
  }, /* @__PURE__ */ I.createElement(M5, {
    fill: d.fill,
    fillOpacity: d.fillOpacity,
    x: d.x,
    y: d.y,
    width: d.width,
    height: d.height,
    ry: d.ry
  }), /* @__PURE__ */ I.createElement(R5, Ln({}, d, {
    offset: u,
    horizontalPoints: _,
    xAxis: b,
    yAxis: w
  })), /* @__PURE__ */ I.createElement(I5, Ln({}, d, {
    offset: u,
    verticalPoints: S,
    xAxis: b,
    yAxis: w
  })), /* @__PURE__ */ I.createElement(D5, Ln({}, d, {
    horizontalPoints: _
  })), /* @__PURE__ */ I.createElement($5, Ln({}, d, {
    verticalPoints: S
  })));
}
pE.displayName = "CartesianGrid";
var q5 = ["type", "layout", "connectNulls", "ref"], U5 = ["key"];
function Wa(e) {
  "@babel/helpers - typeof";
  return Wa = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Wa(e);
}
function h_(e, t) {
  if (e == null) return {};
  var r = z5(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function z5(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
function Di() {
  return Di = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Di.apply(this, arguments);
}
function p_(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function er(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? p_(Object(r), !0).forEach(function(n) {
      _r(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : p_(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function ca(e) {
  return H5(e) || V5(e) || W5(e) || B5();
}
function B5() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function W5(e, t) {
  if (e) {
    if (typeof e == "string") return Mh(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return Mh(e, t);
  }
}
function V5(e) {
  if (typeof Symbol < "u" && e[Symbol.iterator] != null || e["@@iterator"] != null) return Array.from(e);
}
function H5(e) {
  if (Array.isArray(e)) return Mh(e);
}
function Mh(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function G5(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function g_(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, yE(n.key), n);
  }
}
function K5(e, t, r) {
  return t && g_(e.prototype, t), r && g_(e, r), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function Y5(e, t, r) {
  return t = cl(t), X5(e, gE() ? Reflect.construct(t, r || [], cl(e).constructor) : t.apply(e, r));
}
function X5(e, t) {
  if (t && (Wa(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return Q5(e);
}
function Q5(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function gE() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (gE = function() {
    return !!e;
  })();
}
function cl(e) {
  return cl = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, cl(e);
}
function J5(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && Rh(e, t);
}
function Rh(e, t) {
  return Rh = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, Rh(e, t);
}
function _r(e, t, r) {
  return t = yE(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function yE(e) {
  var t = Z5(e, "string");
  return Wa(t) == "symbol" ? t : t + "";
}
function Z5(e, t) {
  if (Wa(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Wa(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
var Ro = /* @__PURE__ */ (function(e) {
  function t() {
    var r;
    G5(this, t);
    for (var n = arguments.length, a = new Array(n), i = 0; i < n; i++)
      a[i] = arguments[i];
    return r = Y5(this, t, [].concat(a)), _r(r, "state", {
      isAnimationFinished: !0,
      totalLength: 0
    }), _r(r, "generateSimpleStrokeDasharray", function(o, l) {
      return "".concat(l, "px ").concat(o - l, "px");
    }), _r(r, "getStrokeDasharray", function(o, l, c) {
      var u = c.reduce(function(v, b) {
        return v + b;
      });
      if (!u)
        return r.generateSimpleStrokeDasharray(l, o);
      for (var d = Math.floor(o / u), f = o % u, p = l - o, m = [], y = 0, h = 0; y < c.length; h += c[y], ++y)
        if (h + c[y] > f) {
          m = [].concat(ca(c.slice(0, y)), [f - h]);
          break;
        }
      var g = m.length % 2 === 0 ? [0, p] : [p];
      return [].concat(ca(t.repeat(c, d)), ca(m), g).map(function(v) {
        return "".concat(v, "px");
      }).join(", ");
    }), _r(r, "id", Ao("recharts-line-")), _r(r, "pathRef", function(o) {
      r.mainCurve = o;
    }), _r(r, "handleAnimationEnd", function() {
      r.setState({
        isAnimationFinished: !0
      }), r.props.onAnimationEnd && r.props.onAnimationEnd();
    }), _r(r, "handleAnimationStart", function() {
      r.setState({
        isAnimationFinished: !1
      }), r.props.onAnimationStart && r.props.onAnimationStart();
    }), r;
  }
  return J5(t, e), K5(t, [{
    key: "componentDidMount",
    value: function() {
      if (this.props.isAnimationActive) {
        var n = this.getTotalLength();
        this.setState({
          totalLength: n
        });
      }
    }
  }, {
    key: "componentDidUpdate",
    value: function() {
      if (this.props.isAnimationActive) {
        var n = this.getTotalLength();
        n !== this.state.totalLength && this.setState({
          totalLength: n
        });
      }
    }
  }, {
    key: "getTotalLength",
    value: function() {
      var n = this.mainCurve;
      try {
        return n && n.getTotalLength && n.getTotalLength() || 0;
      } catch {
        return 0;
      }
    }
  }, {
    key: "renderErrorBar",
    value: function(n, a) {
      if (this.props.isAnimationActive && !this.state.isAnimationFinished)
        return null;
      var i = this.props, o = i.points, l = i.xAxis, c = i.yAxis, u = i.layout, d = i.children, f = gr(d, Mo);
      if (!f)
        return null;
      var p = function(h, g) {
        return {
          x: h.x,
          y: h.y,
          value: h.value,
          errorVal: ir(h.payload, g)
        };
      }, m = {
        clipPath: n ? "url(#clipPath-".concat(a, ")") : null
      };
      return /* @__PURE__ */ I.createElement(yt, m, f.map(function(y) {
        return /* @__PURE__ */ I.cloneElement(y, {
          key: "bar-".concat(y.props.dataKey),
          data: o,
          xAxis: l,
          yAxis: c,
          layout: u,
          dataPointFormatter: p
        });
      }));
    }
  }, {
    key: "renderDots",
    value: function(n, a, i) {
      var o = this.props.isAnimationActive;
      if (o && !this.state.isAnimationFinished)
        return null;
      var l = this.props, c = l.dot, u = l.points, d = l.dataKey, f = ze(this.props, !1), p = ze(c, !0), m = u.map(function(h, g) {
        var v = er(er(er({
          key: "dot-".concat(g),
          r: 3
        }, f), p), {}, {
          index: g,
          cx: h.x,
          cy: h.y,
          value: h.value,
          dataKey: d,
          payload: h.payload,
          points: u
        });
        return t.renderDotItem(c, v);
      }), y = {
        clipPath: n ? "url(#clipPath-".concat(a ? "" : "dots-").concat(i, ")") : null
      };
      return /* @__PURE__ */ I.createElement(yt, Di({
        className: "recharts-line-dots",
        key: "dots"
      }, y), m);
    }
  }, {
    key: "renderCurveStatically",
    value: function(n, a, i, o) {
      var l = this.props, c = l.type, u = l.layout, d = l.connectNulls;
      l.ref;
      var f = h_(l, q5), p = er(er(er({}, ze(f, !0)), {}, {
        fill: "none",
        className: "recharts-line-curve",
        clipPath: a ? "url(#clipPath-".concat(i, ")") : null,
        points: n
      }, o), {}, {
        type: c,
        layout: u,
        connectNulls: d
      });
      return /* @__PURE__ */ I.createElement(dh, Di({}, p, {
        pathRef: this.pathRef
      }));
    }
  }, {
    key: "renderCurveWithAnimation",
    value: function(n, a) {
      var i = this, o = this.props, l = o.points, c = o.strokeDasharray, u = o.isAnimationActive, d = o.animationBegin, f = o.animationDuration, p = o.animationEasing, m = o.animationId, y = o.animateNewValues, h = o.width, g = o.height, v = this.state, b = v.prevPoints, w = v.totalLength;
      return /* @__PURE__ */ I.createElement(tn, {
        begin: d,
        duration: f,
        isActive: u,
        easing: p,
        from: {
          t: 0
        },
        to: {
          t: 1
        },
        key: "line-".concat(m),
        onAnimationEnd: this.handleAnimationEnd,
        onAnimationStart: this.handleAnimationStart
      }, function(j) {
        var x = j.t;
        if (b) {
          var _ = b.length / l.length, S = l.map(function(A, T) {
            var M = Math.floor(T * _);
            if (b[M]) {
              var N = b[M], q = jr(N.x, A.x), V = jr(N.y, A.y);
              return er(er({}, A), {}, {
                x: q(x),
                y: V(x)
              });
            }
            if (y) {
              var B = jr(h * 2, A.x), ee = jr(g / 2, A.y);
              return er(er({}, A), {}, {
                x: B(x),
                y: ee(x)
              });
            }
            return er(er({}, A), {}, {
              x: A.x,
              y: A.y
            });
          });
          return i.renderCurveStatically(S, n, a);
        }
        var k = jr(0, w), E = k(x), L;
        if (c) {
          var P = "".concat(c).split(/[,\s]+/gim).map(function(A) {
            return parseFloat(A);
          });
          L = i.getStrokeDasharray(E, w, P);
        } else
          L = i.generateSimpleStrokeDasharray(w, E);
        return i.renderCurveStatically(l, n, a, {
          strokeDasharray: L
        });
      });
    }
  }, {
    key: "renderCurve",
    value: function(n, a) {
      var i = this.props, o = i.points, l = i.isAnimationActive, c = this.state, u = c.prevPoints, d = c.totalLength;
      return l && o && o.length && (!u && d > 0 || !Bl(u, o)) ? this.renderCurveWithAnimation(n, a) : this.renderCurveStatically(o, n, a);
    }
  }, {
    key: "render",
    value: function() {
      var n, a = this.props, i = a.hide, o = a.dot, l = a.points, c = a.className, u = a.xAxis, d = a.yAxis, f = a.top, p = a.left, m = a.width, y = a.height, h = a.isAnimationActive, g = a.id;
      if (i || !l || !l.length)
        return null;
      var v = this.state.isAnimationFinished, b = l.length === 1, w = Ve("recharts-line", c), j = u && u.allowDataOverflow, x = d && d.allowDataOverflow, _ = j || x, S = Le(g) ? this.id : g, k = (n = ze(o, !1)) !== null && n !== void 0 ? n : {
        r: 3,
        strokeWidth: 2
      }, E = k.r, L = E === void 0 ? 3 : E, P = k.strokeWidth, A = P === void 0 ? 2 : P, T = sT(o) ? o : {}, M = T.clipDot, N = M === void 0 ? !0 : M, q = L * 2 + A;
      return /* @__PURE__ */ I.createElement(yt, {
        className: w
      }, j || x ? /* @__PURE__ */ I.createElement("defs", null, /* @__PURE__ */ I.createElement("clipPath", {
        id: "clipPath-".concat(S)
      }, /* @__PURE__ */ I.createElement("rect", {
        x: j ? p : p - m / 2,
        y: x ? f : f - y / 2,
        width: j ? m : m * 2,
        height: x ? y : y * 2
      })), !N && /* @__PURE__ */ I.createElement("clipPath", {
        id: "clipPath-dots-".concat(S)
      }, /* @__PURE__ */ I.createElement("rect", {
        x: p - q / 2,
        y: f - q / 2,
        width: m + q,
        height: y + q
      }))) : null, !b && this.renderCurve(_, S), this.renderErrorBar(_, S), (b || o) && this.renderDots(_, N, S), (!h || v) && bn.renderCallByParent(this.props, l));
    }
  }], [{
    key: "getDerivedStateFromProps",
    value: function(n, a) {
      return n.animationId !== a.prevAnimationId ? {
        prevAnimationId: n.animationId,
        curPoints: n.points,
        prevPoints: a.curPoints
      } : n.points !== a.curPoints ? {
        curPoints: n.points
      } : null;
    }
  }, {
    key: "repeat",
    value: function(n, a) {
      for (var i = n.length % 2 !== 0 ? [].concat(ca(n), [0]) : n, o = [], l = 0; l < a; ++l)
        o = [].concat(ca(o), ca(i));
      return o;
    }
  }, {
    key: "renderDotItem",
    value: function(n, a) {
      var i;
      if (/* @__PURE__ */ I.isValidElement(n))
        i = /* @__PURE__ */ I.cloneElement(n, a);
      else if ($e(n))
        i = n(a);
      else {
        var o = a.key, l = h_(a, U5), c = Ve("recharts-line-dot", typeof n != "boolean" ? n.className : "");
        i = /* @__PURE__ */ I.createElement(Kp, Di({
          key: o
        }, l, {
          className: c
        }));
      }
      return i;
    }
  }]);
})(wn);
_r(Ro, "displayName", "Line");
_r(Ro, "defaultProps", {
  xAxisId: 0,
  yAxisId: 0,
  connectNulls: !1,
  activeDot: !0,
  dot: !0,
  legendType: "line",
  stroke: "#3182bd",
  strokeWidth: 1,
  fill: "#fff",
  points: [],
  isAnimationActive: !ni.isSsr,
  animateNewValues: !0,
  animationBegin: 0,
  animationDuration: 1500,
  animationEasing: "ease",
  hide: !1,
  label: !1
});
_r(Ro, "getComposedData", function(e) {
  var t = e.props, r = e.xAxis, n = e.yAxis, a = e.xAxisTicks, i = e.yAxisTicks, o = e.dataKey, l = e.bandSize, c = e.displayedData, u = e.offset, d = t.layout, f = c.map(function(p, m) {
    var y = ir(p, o);
    return d === "horizontal" ? {
      x: Ex({
        axis: r,
        ticks: a,
        bandSize: l,
        entry: p,
        index: m
      }),
      y: Le(y) ? null : n.scale(y),
      value: y,
      payload: p
    } : {
      x: Le(y) ? null : r.scale(y),
      y: Ex({
        axis: n,
        ticks: i,
        bandSize: l,
        entry: p,
        index: m
      }),
      value: y,
      payload: p
    };
  });
  return er({
    points: f,
    layout: d
  }, u);
});
function Va(e) {
  "@babel/helpers - typeof";
  return Va = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Va(e);
}
function e4(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function t4(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, xE(n.key), n);
  }
}
function r4(e, t, r) {
  return t && t4(e.prototype, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function n4(e, t, r) {
  return t = ul(t), a4(e, vE() ? Reflect.construct(t, r || [], ul(e).constructor) : t.apply(e, r));
}
function a4(e, t) {
  if (t && (Va(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return i4(e);
}
function i4(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function vE() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (vE = function() {
    return !!e;
  })();
}
function ul(e) {
  return ul = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, ul(e);
}
function o4(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && Ih(e, t);
}
function Ih(e, t) {
  return Ih = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, Ih(e, t);
}
function bE(e, t, r) {
  return t = xE(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function xE(e) {
  var t = s4(e, "string");
  return Va(t) == "symbol" ? t : t + "";
}
function s4(e, t) {
  if (Va(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Va(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
function Dh() {
  return Dh = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Dh.apply(this, arguments);
}
function l4(e) {
  var t = e.xAxisId, r = Jp(), n = Zp(), a = nE(t);
  return a == null ? null : (
    // @ts-expect-error the axisOptions type is not exactly what CartesianAxis is expecting.
    /* @__PURE__ */ xn.createElement(si, Dh({}, a, {
      className: Ve("recharts-".concat(a.axisType, " ").concat(a.axisType), a.className),
      viewBox: {
        x: 0,
        y: 0,
        width: r,
        height: n
      },
      ticksGenerator: function(o) {
        return Kr(o, !0);
      }
    }))
  );
}
var Io = /* @__PURE__ */ (function(e) {
  function t() {
    return e4(this, t), n4(this, t, arguments);
  }
  return o4(t, e), r4(t, [{
    key: "render",
    value: function() {
      return /* @__PURE__ */ xn.createElement(l4, this.props);
    }
  }]);
})(xn.Component);
bE(Io, "displayName", "XAxis");
bE(Io, "defaultProps", {
  allowDecimals: !0,
  hide: !1,
  orientation: "bottom",
  width: 0,
  height: 30,
  mirror: !1,
  xAxisId: 0,
  tickCount: 5,
  type: "category",
  padding: {
    left: 0,
    right: 0
  },
  allowDataOverflow: !1,
  scale: "auto",
  reversed: !1,
  allowDuplicatedCategory: !0
});
function Ha(e) {
  "@babel/helpers - typeof";
  return Ha = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Ha(e);
}
function c4(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function u4(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, jE(n.key), n);
  }
}
function d4(e, t, r) {
  return t && u4(e.prototype, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function f4(e, t, r) {
  return t = dl(t), m4(e, wE() ? Reflect.construct(t, r || [], dl(e).constructor) : t.apply(e, r));
}
function m4(e, t) {
  if (t && (Ha(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return h4(e);
}
function h4(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function wE() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (wE = function() {
    return !!e;
  })();
}
function dl(e) {
  return dl = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, dl(e);
}
function p4(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && $h(e, t);
}
function $h(e, t) {
  return $h = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, $h(e, t);
}
function _E(e, t, r) {
  return t = jE(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function jE(e) {
  var t = g4(e, "string");
  return Ha(t) == "symbol" ? t : t + "";
}
function g4(e, t) {
  if (Ha(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Ha(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
function Lh() {
  return Lh = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, Lh.apply(this, arguments);
}
var y4 = function(t) {
  var r = t.yAxisId, n = Jp(), a = Zp(), i = aE(r);
  return i == null ? null : (
    // @ts-expect-error the axisOptions type is not exactly what CartesianAxis is expecting.
    /* @__PURE__ */ xn.createElement(si, Lh({}, i, {
      className: Ve("recharts-".concat(i.axisType, " ").concat(i.axisType), i.className),
      viewBox: {
        x: 0,
        y: 0,
        width: n,
        height: a
      },
      ticksGenerator: function(l) {
        return Kr(l, !0);
      }
    }))
  );
}, Do = /* @__PURE__ */ (function(e) {
  function t() {
    return c4(this, t), f4(this, t, arguments);
  }
  return p4(t, e), d4(t, [{
    key: "render",
    value: function() {
      return /* @__PURE__ */ xn.createElement(y4, this.props);
    }
  }]);
})(xn.Component);
_E(Do, "displayName", "YAxis");
_E(Do, "defaultProps", {
  allowDuplicatedCategory: !0,
  allowDecimals: !0,
  hide: !1,
  orientation: "left",
  width: 60,
  height: 0,
  mirror: !1,
  yAxisId: 0,
  tickCount: 5,
  type: "number",
  padding: {
    top: 0,
    bottom: 0
  },
  allowDataOverflow: !1,
  scale: "auto",
  reversed: !1
});
function y_(e) {
  return w4(e) || x4(e) || b4(e) || v4();
}
function v4() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function b4(e, t) {
  if (e) {
    if (typeof e == "string") return Fh(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return Fh(e, t);
  }
}
function x4(e) {
  if (typeof Symbol < "u" && e[Symbol.iterator] != null || e["@@iterator"] != null) return Array.from(e);
}
function w4(e) {
  if (Array.isArray(e)) return Fh(e);
}
function Fh(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
var qh = function(t, r, n, a, i) {
  var o = gr(t, tg), l = gr(t, Xl), c = [].concat(y_(o), y_(l)), u = gr(t, Jl), d = "".concat(a, "Id"), f = a[0], p = r;
  if (c.length && (p = c.reduce(function(h, g) {
    if (g.props[d] === n && Dr(g.props, "extendDomain") && me(g.props[f])) {
      var v = g.props[f];
      return [Math.min(h[0], v), Math.max(h[1], v)];
    }
    return h;
  }, p)), u.length) {
    var m = "".concat(f, "1"), y = "".concat(f, "2");
    p = u.reduce(function(h, g) {
      if (g.props[d] === n && Dr(g.props, "extendDomain") && me(g.props[m]) && me(g.props[y])) {
        var v = g.props[m], b = g.props[y];
        return [Math.min(h[0], v, b), Math.max(h[1], v, b)];
      }
      return h;
    }, p);
  }
  return i && i.length && (p = i.reduce(function(h, g) {
    return me(g) ? [Math.min(h[0], g), Math.max(h[1], g)] : h;
  }, p)), p;
}, mm = { exports: {} }, v_;
function _4() {
  return v_ || (v_ = 1, (function(e) {
    var t = Object.prototype.hasOwnProperty, r = "~";
    function n() {
    }
    Object.create && (n.prototype = /* @__PURE__ */ Object.create(null), new n().__proto__ || (r = !1));
    function a(c, u, d) {
      this.fn = c, this.context = u, this.once = d || !1;
    }
    function i(c, u, d, f, p) {
      if (typeof d != "function")
        throw new TypeError("The listener must be a function");
      var m = new a(d, f || c, p), y = r ? r + u : u;
      return c._events[y] ? c._events[y].fn ? c._events[y] = [c._events[y], m] : c._events[y].push(m) : (c._events[y] = m, c._eventsCount++), c;
    }
    function o(c, u) {
      --c._eventsCount === 0 ? c._events = new n() : delete c._events[u];
    }
    function l() {
      this._events = new n(), this._eventsCount = 0;
    }
    l.prototype.eventNames = function() {
      var u = [], d, f;
      if (this._eventsCount === 0) return u;
      for (f in d = this._events)
        t.call(d, f) && u.push(r ? f.slice(1) : f);
      return Object.getOwnPropertySymbols ? u.concat(Object.getOwnPropertySymbols(d)) : u;
    }, l.prototype.listeners = function(u) {
      var d = r ? r + u : u, f = this._events[d];
      if (!f) return [];
      if (f.fn) return [f.fn];
      for (var p = 0, m = f.length, y = new Array(m); p < m; p++)
        y[p] = f[p].fn;
      return y;
    }, l.prototype.listenerCount = function(u) {
      var d = r ? r + u : u, f = this._events[d];
      return f ? f.fn ? 1 : f.length : 0;
    }, l.prototype.emit = function(u, d, f, p, m, y) {
      var h = r ? r + u : u;
      if (!this._events[h]) return !1;
      var g = this._events[h], v = arguments.length, b, w;
      if (g.fn) {
        switch (g.once && this.removeListener(u, g.fn, void 0, !0), v) {
          case 1:
            return g.fn.call(g.context), !0;
          case 2:
            return g.fn.call(g.context, d), !0;
          case 3:
            return g.fn.call(g.context, d, f), !0;
          case 4:
            return g.fn.call(g.context, d, f, p), !0;
          case 5:
            return g.fn.call(g.context, d, f, p, m), !0;
          case 6:
            return g.fn.call(g.context, d, f, p, m, y), !0;
        }
        for (w = 1, b = new Array(v - 1); w < v; w++)
          b[w - 1] = arguments[w];
        g.fn.apply(g.context, b);
      } else {
        var j = g.length, x;
        for (w = 0; w < j; w++)
          switch (g[w].once && this.removeListener(u, g[w].fn, void 0, !0), v) {
            case 1:
              g[w].fn.call(g[w].context);
              break;
            case 2:
              g[w].fn.call(g[w].context, d);
              break;
            case 3:
              g[w].fn.call(g[w].context, d, f);
              break;
            case 4:
              g[w].fn.call(g[w].context, d, f, p);
              break;
            default:
              if (!b) for (x = 1, b = new Array(v - 1); x < v; x++)
                b[x - 1] = arguments[x];
              g[w].fn.apply(g[w].context, b);
          }
      }
      return !0;
    }, l.prototype.on = function(u, d, f) {
      return i(this, u, d, f, !1);
    }, l.prototype.once = function(u, d, f) {
      return i(this, u, d, f, !0);
    }, l.prototype.removeListener = function(u, d, f, p) {
      var m = r ? r + u : u;
      if (!this._events[m]) return this;
      if (!d)
        return o(this, m), this;
      var y = this._events[m];
      if (y.fn)
        y.fn === d && (!p || y.once) && (!f || y.context === f) && o(this, m);
      else {
        for (var h = 0, g = [], v = y.length; h < v; h++)
          (y[h].fn !== d || p && !y[h].once || f && y[h].context !== f) && g.push(y[h]);
        g.length ? this._events[m] = g.length === 1 ? g[0] : g : o(this, m);
      }
      return this;
    }, l.prototype.removeAllListeners = function(u) {
      var d;
      return u ? (d = r ? r + u : u, this._events[d] && o(this, d)) : (this._events = new n(), this._eventsCount = 0), this;
    }, l.prototype.off = l.prototype.removeListener, l.prototype.addListener = l.prototype.on, l.prefixed = r, l.EventEmitter = l, e.exports = l;
  })(mm)), mm.exports;
}
var j4 = _4();
const S4 = /* @__PURE__ */ st(j4);
var hm = new S4(), pm = "recharts.syncMouseEvents";
function xo(e) {
  "@babel/helpers - typeof";
  return xo = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, xo(e);
}
function k4(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function E4(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, SE(n.key), n);
  }
}
function A4(e, t, r) {
  return t && E4(e.prototype, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function gm(e, t, r) {
  return t = SE(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function SE(e) {
  var t = C4(e, "string");
  return xo(t) == "symbol" ? t : t + "";
}
function C4(e, t) {
  if (xo(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (xo(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(e);
}
var T4 = /* @__PURE__ */ (function() {
  function e() {
    k4(this, e), gm(this, "activeIndex", 0), gm(this, "coordinateList", []), gm(this, "layout", "horizontal");
  }
  return A4(e, [{
    key: "setDetails",
    value: function(r) {
      var n, a = r.coordinateList, i = a === void 0 ? null : a, o = r.container, l = o === void 0 ? null : o, c = r.layout, u = c === void 0 ? null : c, d = r.offset, f = d === void 0 ? null : d, p = r.mouseHandlerCallback, m = p === void 0 ? null : p;
      this.coordinateList = (n = i ?? this.coordinateList) !== null && n !== void 0 ? n : [], this.container = l ?? this.container, this.layout = u ?? this.layout, this.offset = f ?? this.offset, this.mouseHandlerCallback = m ?? this.mouseHandlerCallback, this.activeIndex = Math.min(Math.max(this.activeIndex, 0), this.coordinateList.length - 1);
    }
  }, {
    key: "focus",
    value: function() {
      this.spoofMouse();
    }
  }, {
    key: "keyboardEvent",
    value: function(r) {
      if (this.coordinateList.length !== 0)
        switch (r.key) {
          case "ArrowRight": {
            if (this.layout !== "horizontal")
              return;
            this.activeIndex = Math.min(this.activeIndex + 1, this.coordinateList.length - 1), this.spoofMouse();
            break;
          }
          case "ArrowLeft": {
            if (this.layout !== "horizontal")
              return;
            this.activeIndex = Math.max(this.activeIndex - 1, 0), this.spoofMouse();
            break;
          }
        }
    }
  }, {
    key: "setIndex",
    value: function(r) {
      this.activeIndex = r;
    }
  }, {
    key: "spoofMouse",
    value: function() {
      var r, n;
      if (this.layout === "horizontal" && this.coordinateList.length !== 0) {
        var a = this.container.getBoundingClientRect(), i = a.x, o = a.y, l = a.height, c = this.coordinateList[this.activeIndex].coordinate, u = ((r = window) === null || r === void 0 ? void 0 : r.scrollX) || 0, d = ((n = window) === null || n === void 0 ? void 0 : n.scrollY) || 0, f = i + c + u, p = o + this.offset.top + l / 2 + d;
        this.mouseHandlerCallback({
          pageX: f,
          pageY: p
        });
      }
    }
  }]);
})();
function P4(e, t, r) {
  if (r === "number" && t === !0 && Array.isArray(e)) {
    var n = e == null ? void 0 : e[0], a = e == null ? void 0 : e[1];
    if (n && a && me(n) && me(a))
      return !0;
  }
  return !1;
}
function O4(e, t, r, n) {
  var a = n / 2;
  return {
    stroke: "none",
    fill: "#ccc",
    x: e === "horizontal" ? t.x - a : r.left + 0.5,
    y: e === "horizontal" ? r.top + 0.5 : t.y - a,
    width: e === "horizontal" ? n : r.width - 1,
    height: e === "horizontal" ? r.height - 1 : n
  };
}
function kE(e) {
  var t = e.cx, r = e.cy, n = e.radius, a = e.startAngle, i = e.endAngle, o = $t(t, r, n, a), l = $t(t, r, n, i);
  return {
    points: [o, l],
    cx: t,
    cy: r,
    radius: n,
    startAngle: a,
    endAngle: i
  };
}
function N4(e, t, r) {
  var n, a, i, o;
  if (e === "horizontal")
    n = t.x, i = n, a = r.top, o = r.top + r.height;
  else if (e === "vertical")
    a = t.y, o = a, n = r.left, i = r.left + r.width;
  else if (t.cx != null && t.cy != null)
    if (e === "centric") {
      var l = t.cx, c = t.cy, u = t.innerRadius, d = t.outerRadius, f = t.angle, p = $t(l, c, u, f), m = $t(l, c, d, f);
      n = p.x, a = p.y, i = m.x, o = m.y;
    } else
      return kE(t);
  return [{
    x: n,
    y: a
  }, {
    x: i,
    y: o
  }];
}
function wo(e) {
  "@babel/helpers - typeof";
  return wo = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, wo(e);
}
function b_(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function ls(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? b_(Object(r), !0).forEach(function(n) {
      M4(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : b_(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function M4(e, t, r) {
  return t = R4(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function R4(e) {
  var t = I4(e, "string");
  return wo(t) == "symbol" ? t : t + "";
}
function I4(e, t) {
  if (wo(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (wo(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
function D4(e) {
  var t, r, n = e.element, a = e.tooltipEventType, i = e.isActive, o = e.activeCoordinate, l = e.activePayload, c = e.offset, u = e.activeTooltipIndex, d = e.tooltipAxisBandSize, f = e.layout, p = e.chartName, m = (t = n.props.cursor) !== null && t !== void 0 ? t : (r = n.type.defaultProps) === null || r === void 0 ? void 0 : r.cursor;
  if (!n || !m || !i || !o || p !== "ScatterChart" && a !== "axis")
    return null;
  var y, h = dh;
  if (p === "ScatterChart")
    y = o, h = R3;
  else if (p === "BarChart")
    y = O4(f, o, c, d), h = Gp;
  else if (f === "radial") {
    var g = kE(o), v = g.cx, b = g.cy, w = g.radius, j = g.startAngle, x = g.endAngle;
    y = {
      cx: v,
      cy: b,
      startAngle: j,
      endAngle: x,
      innerRadius: w,
      outerRadius: w
    }, h = Tk;
  } else
    y = {
      points: N4(f, o, c)
    }, h = dh;
  var _ = ls(ls(ls(ls({
    stroke: "#ccc",
    pointerEvents: "none"
  }, c), y), ze(m, !1)), {}, {
    payload: l,
    payloadIndex: u,
    className: Ve("recharts-tooltip-cursor", m.className)
  });
  return /* @__PURE__ */ Cr(m) ? /* @__PURE__ */ Tt(m, _) : /* @__PURE__ */ z_(h, _);
}
var $4 = ["item"], L4 = ["children", "className", "width", "height", "style", "compact", "title", "desc"];
function Ga(e) {
  "@babel/helpers - typeof";
  return Ga = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Ga(e);
}
function _a() {
  return _a = Object.assign ? Object.assign.bind() : function(e) {
    for (var t = 1; t < arguments.length; t++) {
      var r = arguments[t];
      for (var n in r)
        Object.prototype.hasOwnProperty.call(r, n) && (e[n] = r[n]);
    }
    return e;
  }, _a.apply(this, arguments);
}
function x_(e, t) {
  return U4(e) || q4(e, t) || AE(e, t) || F4();
}
function F4() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function q4(e, t) {
  var r = e == null ? null : typeof Symbol < "u" && e[Symbol.iterator] || e["@@iterator"];
  if (r != null) {
    var n, a, i, o, l = [], c = !0, u = !1;
    try {
      if (i = (r = r.call(e)).next, t !== 0) for (; !(c = (n = i.call(r)).done) && (l.push(n.value), l.length !== t); c = !0) ;
    } catch (d) {
      u = !0, a = d;
    } finally {
      try {
        if (!c && r.return != null && (o = r.return(), Object(o) !== o)) return;
      } finally {
        if (u) throw a;
      }
    }
    return l;
  }
}
function U4(e) {
  if (Array.isArray(e)) return e;
}
function w_(e, t) {
  if (e == null) return {};
  var r = z4(e, t), n, a;
  if (Object.getOwnPropertySymbols) {
    var i = Object.getOwnPropertySymbols(e);
    for (a = 0; a < i.length; a++)
      n = i[a], !(t.indexOf(n) >= 0) && Object.prototype.propertyIsEnumerable.call(e, n) && (r[n] = e[n]);
  }
  return r;
}
function z4(e, t) {
  if (e == null) return {};
  var r = {};
  for (var n in e)
    if (Object.prototype.hasOwnProperty.call(e, n)) {
      if (t.indexOf(n) >= 0) continue;
      r[n] = e[n];
    }
  return r;
}
function B4(e, t) {
  if (!(e instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function W4(e, t) {
  for (var r = 0; r < t.length; r++) {
    var n = t[r];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(e, CE(n.key), n);
  }
}
function V4(e, t, r) {
  return t && W4(e.prototype, t), Object.defineProperty(e, "prototype", { writable: !1 }), e;
}
function H4(e, t, r) {
  return t = fl(t), G4(e, EE() ? Reflect.construct(t, r || [], fl(e).constructor) : t.apply(e, r));
}
function G4(e, t) {
  if (t && (Ga(t) === "object" || typeof t == "function"))
    return t;
  if (t !== void 0)
    throw new TypeError("Derived constructors may only return object or undefined");
  return K4(e);
}
function K4(e) {
  if (e === void 0)
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function EE() {
  try {
    var e = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch {
  }
  return (EE = function() {
    return !!e;
  })();
}
function fl(e) {
  return fl = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(r) {
    return r.__proto__ || Object.getPrototypeOf(r);
  }, fl(e);
}
function Y4(e, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Super expression must either be null or a function");
  e.prototype = Object.create(t && t.prototype, { constructor: { value: e, writable: !0, configurable: !0 } }), Object.defineProperty(e, "prototype", { writable: !1 }), t && Uh(e, t);
}
function Uh(e, t) {
  return Uh = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(n, a) {
    return n.__proto__ = a, n;
  }, Uh(e, t);
}
function Ka(e) {
  return J4(e) || Q4(e) || AE(e) || X4();
}
function X4() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function AE(e, t) {
  if (e) {
    if (typeof e == "string") return zh(e, t);
    var r = Object.prototype.toString.call(e).slice(8, -1);
    if (r === "Object" && e.constructor && (r = e.constructor.name), r === "Map" || r === "Set") return Array.from(e);
    if (r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)) return zh(e, t);
  }
}
function Q4(e) {
  if (typeof Symbol < "u" && e[Symbol.iterator] != null || e["@@iterator"] != null) return Array.from(e);
}
function J4(e) {
  if (Array.isArray(e)) return zh(e);
}
function zh(e, t) {
  (t == null || t > e.length) && (t = e.length);
  for (var r = 0, n = new Array(t); r < t; r++) n[r] = e[r];
  return n;
}
function __(e, t) {
  var r = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e);
    t && (n = n.filter(function(a) {
      return Object.getOwnPropertyDescriptor(e, a).enumerable;
    })), r.push.apply(r, n);
  }
  return r;
}
function J(e) {
  for (var t = 1; t < arguments.length; t++) {
    var r = arguments[t] != null ? arguments[t] : {};
    t % 2 ? __(Object(r), !0).forEach(function(n) {
      Te(e, n, r[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(r)) : __(Object(r)).forEach(function(n) {
      Object.defineProperty(e, n, Object.getOwnPropertyDescriptor(r, n));
    });
  }
  return e;
}
function Te(e, t, r) {
  return t = CE(t), t in e ? Object.defineProperty(e, t, { value: r, enumerable: !0, configurable: !0, writable: !0 }) : e[t] = r, e;
}
function CE(e) {
  var t = Z4(e, "string");
  return Ga(t) == "symbol" ? t : t + "";
}
function Z4(e, t) {
  if (Ga(e) != "object" || !e) return e;
  var r = e[Symbol.toPrimitive];
  if (r !== void 0) {
    var n = r.call(e, t);
    if (Ga(n) != "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(e);
}
var eU = {
  xAxis: ["bottom", "top"],
  yAxis: ["left", "right"]
}, tU = {
  width: "100%",
  height: "100%"
}, TE = {
  x: 0,
  y: 0
};
function cs(e) {
  return e;
}
var rU = function(t, r) {
  return r === "horizontal" ? t.x : r === "vertical" ? t.y : r === "centric" ? t.angle : t.radius;
}, nU = function(t, r, n, a) {
  var i = r.find(function(d) {
    return d && d.index === n;
  });
  if (i) {
    if (t === "horizontal")
      return {
        x: i.coordinate,
        y: a.y
      };
    if (t === "vertical")
      return {
        x: a.x,
        y: i.coordinate
      };
    if (t === "centric") {
      var o = i.coordinate, l = a.radius;
      return J(J(J({}, a), $t(a.cx, a.cy, l, o)), {}, {
        angle: o,
        radius: l
      });
    }
    var c = i.coordinate, u = a.angle;
    return J(J(J({}, a), $t(a.cx, a.cy, c, u)), {}, {
      angle: u,
      radius: c
    });
  }
  return TE;
}, Zl = function(t, r) {
  var n = r.graphicalItems, a = r.dataStartIndex, i = r.dataEndIndex, o = (n ?? []).reduce(function(l, c) {
    var u = c.props.data;
    return u && u.length ? [].concat(Ka(l), Ka(u)) : l;
  }, []);
  return o.length > 0 ? o : t && t.length && me(a) && me(i) ? t.slice(a, i + 1) : [];
};
function PE(e) {
  return e === "number" ? [0, "auto"] : void 0;
}
var Bh = function(t, r, n, a) {
  var i = t.graphicalItems, o = t.tooltipAxis, l = Zl(r, t);
  return n < 0 || !i || !i.length || n >= l.length ? null : i.reduce(function(c, u) {
    var d, f = (d = u.props.data) !== null && d !== void 0 ? d : r;
    f && t.dataStartIndex + t.dataEndIndex !== 0 && // https://github.com/recharts/recharts/issues/4717
    // The data is sliced only when the active index is within the start/end index range.
    t.dataEndIndex - t.dataStartIndex >= n && (f = f.slice(t.dataStartIndex, t.dataEndIndex + 1));
    var p;
    if (o.dataKey && !o.allowDuplicatedCategory) {
      var m = f === void 0 ? l : f;
      p = hs(m, o.dataKey, a);
    } else
      p = f && f[n] || l[n];
    return p ? [].concat(Ka(c), [Ek(u, p)]) : c;
  }, []);
}, j_ = function(t, r, n, a) {
  var i = a || {
    x: t.chartX,
    y: t.chartY
  }, o = rU(i, n), l = t.orderedTooltipTicks, c = t.tooltipAxis, u = t.tooltipTicks, d = l2(o, l, u, c);
  if (d >= 0 && u) {
    var f = u[d] && u[d].value, p = Bh(t, r, d, f), m = nU(n, l, d, i);
    return {
      activeTooltipIndex: d,
      activeLabel: f,
      activePayload: p,
      activeCoordinate: m
    };
  }
  return null;
}, aU = function(t, r) {
  var n = r.axes, a = r.graphicalItems, i = r.axisType, o = r.axisIdKey, l = r.stackGroups, c = r.dataStartIndex, u = r.dataEndIndex, d = t.layout, f = t.children, p = t.stackOffset, m = jk(d, i);
  return n.reduce(function(y, h) {
    var g, v = h.type.defaultProps !== void 0 ? J(J({}, h.type.defaultProps), h.props) : h.props, b = v.type, w = v.dataKey, j = v.allowDataOverflow, x = v.allowDuplicatedCategory, _ = v.scale, S = v.ticks, k = v.includeHidden, E = v[o];
    if (y[E])
      return y;
    var L = Zl(t.data, {
      graphicalItems: a.filter(function(Y) {
        var Q, ne = o in Y.props ? Y.props[o] : (Q = Y.type.defaultProps) === null || Q === void 0 ? void 0 : Q[o];
        return ne === E;
      }),
      dataStartIndex: c,
      dataEndIndex: u
    }), P = L.length, A, T, M;
    P4(v.domain, j, b) && (A = oh(v.domain, null, j), m && (b === "number" || _ !== "auto") && (M = Ri(L, w, "category")));
    var N = PE(b);
    if (!A || A.length === 0) {
      var q, V = (q = v.domain) !== null && q !== void 0 ? q : N;
      if (w) {
        if (A = Ri(L, w, b), b === "category" && m) {
          var B = QC(A);
          x && B ? (T = A, A = Js(0, P)) : x || (A = Px(V, A, h).reduce(function(Y, Q) {
            return Y.indexOf(Q) >= 0 ? Y : [].concat(Ka(Y), [Q]);
          }, []));
        } else if (b === "category")
          x ? A = A.filter(function(Y) {
            return Y !== "" && !Le(Y);
          }) : A = Px(V, A, h).reduce(function(Y, Q) {
            return Y.indexOf(Q) >= 0 || Q === "" || Le(Q) ? Y : [].concat(Ka(Y), [Q]);
          }, []);
        else if (b === "number") {
          var ee = m2(L, a.filter(function(Y) {
            var Q, ne, z = o in Y.props ? Y.props[o] : (Q = Y.type.defaultProps) === null || Q === void 0 ? void 0 : Q[o], se = "hide" in Y.props ? Y.props.hide : (ne = Y.type.defaultProps) === null || ne === void 0 ? void 0 : ne.hide;
            return z === E && (k || !se);
          }), w, i, d);
          ee && (A = ee);
        }
        m && (b === "number" || _ !== "auto") && (M = Ri(L, w, "category"));
      } else m ? A = Js(0, P) : l && l[E] && l[E].hasStack && b === "number" ? A = p === "expand" ? [0, 1] : kk(l[E].stackGroups, c, u) : A = _k(L, a.filter(function(Y) {
        var Q = o in Y.props ? Y.props[o] : Y.type.defaultProps[o], ne = "hide" in Y.props ? Y.props.hide : Y.type.defaultProps.hide;
        return Q === E && (k || !ne);
      }), b, d, !0);
      if (b === "number")
        A = qh(f, A, E, i, S), V && (A = oh(V, A, j));
      else if (b === "category" && V) {
        var U = V, G = A.every(function(Y) {
          return U.indexOf(Y) >= 0;
        });
        G && (A = U);
      }
    }
    return J(J({}, y), {}, Te({}, E, J(J({}, v), {}, {
      axisType: i,
      domain: A,
      categoricalDomain: M,
      duplicateDomain: T,
      originalDomain: (g = v.domain) !== null && g !== void 0 ? g : N,
      isCategorical: m,
      layout: d
    })));
  }, {});
}, iU = function(t, r) {
  var n = r.graphicalItems, a = r.Axis, i = r.axisType, o = r.axisIdKey, l = r.stackGroups, c = r.dataStartIndex, u = r.dataEndIndex, d = t.layout, f = t.children, p = Zl(t.data, {
    graphicalItems: n,
    dataStartIndex: c,
    dataEndIndex: u
  }), m = p.length, y = jk(d, i), h = -1;
  return n.reduce(function(g, v) {
    var b = v.type.defaultProps !== void 0 ? J(J({}, v.type.defaultProps), v.props) : v.props, w = b[o], j = PE("number");
    if (!g[w]) {
      h++;
      var x;
      return y ? x = Js(0, m) : l && l[w] && l[w].hasStack ? (x = kk(l[w].stackGroups, c, u), x = qh(f, x, w, i)) : (x = oh(j, _k(p, n.filter(function(_) {
        var S, k, E = o in _.props ? _.props[o] : (S = _.type.defaultProps) === null || S === void 0 ? void 0 : S[o], L = "hide" in _.props ? _.props.hide : (k = _.type.defaultProps) === null || k === void 0 ? void 0 : k.hide;
        return E === w && !L;
      }), "number", d), a.defaultProps.allowDataOverflow), x = qh(f, x, w, i)), J(J({}, g), {}, Te({}, w, J(J({
        axisType: i
      }, a.defaultProps), {}, {
        hide: !0,
        orientation: pr(eU, "".concat(i, ".").concat(h % 2), null),
        domain: x,
        originalDomain: j,
        isCategorical: y,
        layout: d
        // specify scale when no Axis
        // scale: isCategorical ? 'band' : 'linear',
      })));
    }
    return g;
  }, {});
}, oU = function(t, r) {
  var n = r.axisType, a = n === void 0 ? "xAxis" : n, i = r.AxisComp, o = r.graphicalItems, l = r.stackGroups, c = r.dataStartIndex, u = r.dataEndIndex, d = t.children, f = "".concat(a, "Id"), p = gr(d, i), m = {};
  return p && p.length ? m = aU(t, {
    axes: p,
    graphicalItems: o,
    axisType: a,
    axisIdKey: f,
    stackGroups: l,
    dataStartIndex: c,
    dataEndIndex: u
  }) : o && o.length && (m = iU(t, {
    Axis: i,
    graphicalItems: o,
    axisType: a,
    axisIdKey: f,
    stackGroups: l,
    dataStartIndex: c,
    dataEndIndex: u
  })), m;
}, sU = function(t) {
  var r = pn(t), n = Kr(r, !1, !0);
  return {
    tooltipTicks: n,
    orderedTooltipTicks: bp(n, function(a) {
      return a.coordinate;
    }),
    tooltipAxis: r,
    tooltipAxisBandSize: Us(r, n)
  };
}, S_ = function(t) {
  var r = t.children, n = t.defaultShowTooltip, a = rr(r, $a), i = 0, o = 0;
  return t.data && t.data.length !== 0 && (o = t.data.length - 1), a && a.props && (a.props.startIndex >= 0 && (i = a.props.startIndex), a.props.endIndex >= 0 && (o = a.props.endIndex)), {
    chartX: 0,
    chartY: 0,
    dataStartIndex: i,
    dataEndIndex: o,
    activeTooltipIndex: -1,
    isTooltipActive: !!n
  };
}, lU = function(t) {
  return !t || !t.length ? !1 : t.some(function(r) {
    var n = Yr(r && r.type);
    return n && n.indexOf("Bar") >= 0;
  });
}, k_ = function(t) {
  return t === "horizontal" ? {
    numericAxisName: "yAxis",
    cateAxisName: "xAxis"
  } : t === "vertical" ? {
    numericAxisName: "xAxis",
    cateAxisName: "yAxis"
  } : t === "centric" ? {
    numericAxisName: "radiusAxis",
    cateAxisName: "angleAxis"
  } : {
    numericAxisName: "angleAxis",
    cateAxisName: "radiusAxis"
  };
}, cU = function(t, r) {
  var n = t.props, a = t.graphicalItems, i = t.xAxisMap, o = i === void 0 ? {} : i, l = t.yAxisMap, c = l === void 0 ? {} : l, u = n.width, d = n.height, f = n.children, p = n.margin || {}, m = rr(f, $a), y = rr(f, Un), h = Object.keys(c).reduce(function(x, _) {
    var S = c[_], k = S.orientation;
    return !S.mirror && !S.hide ? J(J({}, x), {}, Te({}, k, x[k] + S.width)) : x;
  }, {
    left: p.left || 0,
    right: p.right || 0
  }), g = Object.keys(o).reduce(function(x, _) {
    var S = o[_], k = S.orientation;
    return !S.mirror && !S.hide ? J(J({}, x), {}, Te({}, k, pr(x, "".concat(k)) + S.height)) : x;
  }, {
    top: p.top || 0,
    bottom: p.bottom || 0
  }), v = J(J({}, g), h), b = v.bottom;
  m && (v.bottom += m.props.height || $a.defaultProps.height), y && r && (v = d2(v, a, n, r));
  var w = u - v.left - v.right, j = d - v.top - v.bottom;
  return J(J({
    brushBottom: b
  }, v), {}, {
    // never return negative values for height and width
    width: Math.max(w, 0),
    height: Math.max(j, 0)
  });
}, uU = function(t, r) {
  if (r === "xAxis")
    return t[r].width;
  if (r === "yAxis")
    return t[r].height;
}, OE = function(t) {
  var r = t.chartName, n = t.GraphicalChild, a = t.defaultTooltipEventType, i = a === void 0 ? "axis" : a, o = t.validateTooltipEventTypes, l = o === void 0 ? ["axis"] : o, c = t.axisComponents, u = t.legendContent, d = t.formatAxisMap, f = t.defaultProps, p = function(v, b) {
    var w = b.graphicalItems, j = b.stackGroups, x = b.offset, _ = b.updateId, S = b.dataStartIndex, k = b.dataEndIndex, E = v.barSize, L = v.layout, P = v.barGap, A = v.barCategoryGap, T = v.maxBarSize, M = k_(L), N = M.numericAxisName, q = M.cateAxisName, V = lU(w), B = [];
    return w.forEach(function(ee, U) {
      var G = Zl(v.data, {
        graphicalItems: [ee],
        dataStartIndex: S,
        dataEndIndex: k
      }), Y = ee.type.defaultProps !== void 0 ? J(J({}, ee.type.defaultProps), ee.props) : ee.props, Q = Y.dataKey, ne = Y.maxBarSize, z = Y["".concat(N, "Id")], se = Y["".concat(q, "Id")], oe = {}, ie = c.reduce(function(C, R) {
        var W, ue, le = b["".concat(R.axisType, "Map")], ce = Y["".concat(R.axisType, "Id")];
        le && le[ce] || R.axisType === "zAxis" || (process.env.NODE_ENV !== "production" ? Yt(!1, "Specifying a(n) ".concat(R.axisType, "Id requires a corresponding ").concat(
          R.axisType,
          "Id on the targeted graphical component "
        ).concat((W = ee == null || (ue = ee.type) === null || ue === void 0 ? void 0 : ue.displayName) !== null && W !== void 0 ? W : "")) : Yt());
        var _e = le[ce];
        return J(J({}, C), {}, Te(Te({}, R.axisType, _e), "".concat(R.axisType, "Ticks"), Kr(_e)));
      }, oe), K = ie[q], ae = ie["".concat(q, "Ticks")], fe = j && j[z] && j[z].hasStack && k2(ee, j[z].stackGroups), $ = Yr(ee.type).indexOf("Bar") >= 0, Ee = Us(K, ae), re = [], Pe = V && c2({
        barSize: E,
        stackGroups: j,
        totalSize: uU(ie, q)
      });
      if ($) {
        var Oe, Ne, ke = Le(ne) ? T : ne, Me = (Oe = (Ne = Us(K, ae, !0)) !== null && Ne !== void 0 ? Ne : ke) !== null && Oe !== void 0 ? Oe : 0;
        re = u2({
          barGap: P,
          barCategoryGap: A,
          bandSize: Me !== Ee ? Me : Ee,
          sizeList: Pe[se],
          maxBarSize: ke
        }), Me !== Ee && (re = re.map(function(C) {
          return J(J({}, C), {}, {
            position: J(J({}, C.position), {}, {
              offset: C.position.offset - Me / 2
            })
          });
        }));
      }
      var Ae = ee && ee.type && ee.type.getComposedData;
      Ae && B.push({
        props: J(J({}, Ae(J(J({}, ie), {}, {
          displayedData: G,
          props: v,
          dataKey: Q,
          item: ee,
          bandSize: Ee,
          barPosition: re,
          offset: x,
          stackedData: fe,
          layout: L,
          dataStartIndex: S,
          dataEndIndex: k
        }))), {}, Te(Te(Te({
          key: ee.key || "item-".concat(U)
        }, N, ie[N]), q, ie[q]), "animationId", _)),
        childIndex: uT(ee, v.children),
        item: ee
      });
    }), B;
  }, m = function(v, b) {
    var w = v.props, j = v.dataStartIndex, x = v.dataEndIndex, _ = v.updateId;
    if (!Wy({
      props: w
    }))
      return null;
    var S = w.children, k = w.layout, E = w.stackOffset, L = w.data, P = w.reverseStackOrder, A = k_(k), T = A.numericAxisName, M = A.cateAxisName, N = gr(S, n), q = _2(L, N, "".concat(T, "Id"), "".concat(M, "Id"), E, P), V = c.reduce(function(Y, Q) {
      var ne = "".concat(Q.axisType, "Map");
      return J(J({}, Y), {}, Te({}, ne, oU(w, J(J({}, Q), {}, {
        graphicalItems: N,
        stackGroups: Q.axisType === T && q,
        dataStartIndex: j,
        dataEndIndex: x
      }))));
    }, {}), B = cU(J(J({}, V), {}, {
      props: w,
      graphicalItems: N
    }), b == null ? void 0 : b.legendBBox);
    Object.keys(V).forEach(function(Y) {
      V[Y] = d(w, V[Y], B, Y.replace("Map", ""), r);
    });
    var ee = V["".concat(M, "Map")], U = sU(ee), G = p(w, J(J({}, V), {}, {
      dataStartIndex: j,
      dataEndIndex: x,
      updateId: _,
      graphicalItems: N,
      stackGroups: q,
      offset: B
    }));
    return J(J({
      formattedGraphicalItems: G,
      graphicalItems: N,
      offset: B,
      stackGroups: q
    }, U), V);
  }, y = /* @__PURE__ */ (function(g) {
    function v(b) {
      var w, j, x;
      return B4(this, v), x = H4(this, v, [b]), Te(x, "eventEmitterSymbol", Symbol("rechartsEventEmitter")), Te(x, "accessibilityManager", new T4()), Te(x, "handleLegendBBoxUpdate", function(_) {
        if (_) {
          var S = x.state, k = S.dataStartIndex, E = S.dataEndIndex, L = S.updateId;
          x.setState(J({
            legendBBox: _
          }, m({
            props: x.props,
            dataStartIndex: k,
            dataEndIndex: E,
            updateId: L
          }, J(J({}, x.state), {}, {
            legendBBox: _
          }))));
        }
      }), Te(x, "handleReceiveSyncEvent", function(_, S, k) {
        if (x.props.syncId === _) {
          if (k === x.eventEmitterSymbol && typeof x.props.syncMethod != "function")
            return;
          x.applySyncEvent(S);
        }
      }), Te(x, "handleBrushChange", function(_) {
        var S = _.startIndex, k = _.endIndex;
        if (S !== x.state.dataStartIndex || k !== x.state.dataEndIndex) {
          var E = x.state.updateId;
          x.setState(function() {
            return J({
              dataStartIndex: S,
              dataEndIndex: k
            }, m({
              props: x.props,
              dataStartIndex: S,
              dataEndIndex: k,
              updateId: E
            }, x.state));
          }), x.triggerSyncEvent({
            dataStartIndex: S,
            dataEndIndex: k
          });
        }
      }), Te(x, "handleMouseEnter", function(_) {
        var S = x.getMouseInfo(_);
        if (S) {
          var k = J(J({}, S), {}, {
            isTooltipActive: !0
          });
          x.setState(k), x.triggerSyncEvent(k);
          var E = x.props.onMouseEnter;
          $e(E) && E(k, _);
        }
      }), Te(x, "triggeredAfterMouseMove", function(_) {
        var S = x.getMouseInfo(_), k = S ? J(J({}, S), {}, {
          isTooltipActive: !0
        }) : {
          isTooltipActive: !1
        };
        x.setState(k), x.triggerSyncEvent(k);
        var E = x.props.onMouseMove;
        $e(E) && E(k, _);
      }), Te(x, "handleItemMouseEnter", function(_) {
        x.setState(function() {
          return {
            isTooltipActive: !0,
            activeItem: _,
            activePayload: _.tooltipPayload,
            activeCoordinate: _.tooltipPosition || {
              x: _.cx,
              y: _.cy
            }
          };
        });
      }), Te(x, "handleItemMouseLeave", function() {
        x.setState(function() {
          return {
            isTooltipActive: !1
          };
        });
      }), Te(x, "handleMouseMove", function(_) {
        _.persist(), x.throttleTriggeredAfterMouseMove(_);
      }), Te(x, "handleMouseLeave", function(_) {
        x.throttleTriggeredAfterMouseMove.cancel();
        var S = {
          isTooltipActive: !1
        };
        x.setState(S), x.triggerSyncEvent(S);
        var k = x.props.onMouseLeave;
        $e(k) && k(S, _);
      }), Te(x, "handleOuterEvent", function(_) {
        var S = cT(_), k = pr(x.props, "".concat(S));
        if (S && $e(k)) {
          var E, L;
          /.*touch.*/i.test(S) ? L = x.getMouseInfo(_.changedTouches[0]) : L = x.getMouseInfo(_), k((E = L) !== null && E !== void 0 ? E : {}, _);
        }
      }), Te(x, "handleClick", function(_) {
        var S = x.getMouseInfo(_);
        if (S) {
          var k = J(J({}, S), {}, {
            isTooltipActive: !0
          });
          x.setState(k), x.triggerSyncEvent(k);
          var E = x.props.onClick;
          $e(E) && E(k, _);
        }
      }), Te(x, "handleMouseDown", function(_) {
        var S = x.props.onMouseDown;
        if ($e(S)) {
          var k = x.getMouseInfo(_);
          S(k, _);
        }
      }), Te(x, "handleMouseUp", function(_) {
        var S = x.props.onMouseUp;
        if ($e(S)) {
          var k = x.getMouseInfo(_);
          S(k, _);
        }
      }), Te(x, "handleTouchMove", function(_) {
        _.changedTouches != null && _.changedTouches.length > 0 && x.throttleTriggeredAfterMouseMove(_.changedTouches[0]);
      }), Te(x, "handleTouchStart", function(_) {
        _.changedTouches != null && _.changedTouches.length > 0 && x.handleMouseDown(_.changedTouches[0]);
      }), Te(x, "handleTouchEnd", function(_) {
        _.changedTouches != null && _.changedTouches.length > 0 && x.handleMouseUp(_.changedTouches[0]);
      }), Te(x, "handleDoubleClick", function(_) {
        var S = x.props.onDoubleClick;
        if ($e(S)) {
          var k = x.getMouseInfo(_);
          S(k, _);
        }
      }), Te(x, "handleContextMenu", function(_) {
        var S = x.props.onContextMenu;
        if ($e(S)) {
          var k = x.getMouseInfo(_);
          S(k, _);
        }
      }), Te(x, "triggerSyncEvent", function(_) {
        x.props.syncId !== void 0 && hm.emit(pm, x.props.syncId, _, x.eventEmitterSymbol);
      }), Te(x, "applySyncEvent", function(_) {
        var S = x.props, k = S.layout, E = S.syncMethod, L = x.state.updateId, P = _.dataStartIndex, A = _.dataEndIndex;
        if (_.dataStartIndex !== void 0 || _.dataEndIndex !== void 0)
          x.setState(J({
            dataStartIndex: P,
            dataEndIndex: A
          }, m({
            props: x.props,
            dataStartIndex: P,
            dataEndIndex: A,
            updateId: L
          }, x.state)));
        else if (_.activeTooltipIndex !== void 0) {
          var T = _.chartX, M = _.chartY, N = _.activeTooltipIndex, q = x.state, V = q.offset, B = q.tooltipTicks;
          if (!V)
            return;
          if (typeof E == "function")
            N = E(B, _);
          else if (E === "value") {
            N = -1;
            for (var ee = 0; ee < B.length; ee++)
              if (B[ee].value === _.activeLabel) {
                N = ee;
                break;
              }
          }
          var U = J(J({}, V), {}, {
            x: V.left,
            y: V.top
          }), G = Math.min(T, U.x + U.width), Y = Math.min(M, U.y + U.height), Q = B[N] && B[N].value, ne = Bh(x.state, x.props.data, N), z = B[N] ? {
            x: k === "horizontal" ? B[N].coordinate : G,
            y: k === "horizontal" ? Y : B[N].coordinate
          } : TE;
          x.setState(J(J({}, _), {}, {
            activeLabel: Q,
            activeCoordinate: z,
            activePayload: ne,
            activeTooltipIndex: N
          }));
        } else
          x.setState(_);
      }), Te(x, "renderCursor", function(_) {
        var S, k = x.state, E = k.isTooltipActive, L = k.activeCoordinate, P = k.activePayload, A = k.offset, T = k.activeTooltipIndex, M = k.tooltipAxisBandSize, N = x.getTooltipEventType(), q = (S = _.props.active) !== null && S !== void 0 ? S : E, V = x.props.layout, B = _.key || "_recharts-cursor";
        return /* @__PURE__ */ I.createElement(D4, {
          key: B,
          activeCoordinate: L,
          activePayload: P,
          activeTooltipIndex: T,
          chartName: r,
          element: _,
          isActive: q,
          layout: V,
          offset: A,
          tooltipAxisBandSize: M,
          tooltipEventType: N
        });
      }), Te(x, "renderPolarAxis", function(_, S, k) {
        var E = pr(_, "type.axisType"), L = pr(x.state, "".concat(E, "Map")), P = _.type.defaultProps, A = P !== void 0 ? J(J({}, P), _.props) : _.props, T = L && L[A["".concat(E, "Id")]];
        return /* @__PURE__ */ Tt(_, J(J({}, T), {}, {
          className: Ve(E, T.className),
          key: _.key || "".concat(S, "-").concat(k),
          ticks: Kr(T, !0)
        }));
      }), Te(x, "renderPolarGrid", function(_) {
        var S = _.props, k = S.radialLines, E = S.polarAngles, L = S.polarRadius, P = x.state, A = P.radiusAxisMap, T = P.angleAxisMap, M = pn(A), N = pn(T), q = N.cx, V = N.cy, B = N.innerRadius, ee = N.outerRadius;
        return /* @__PURE__ */ Tt(_, {
          polarAngles: Array.isArray(E) ? E : Kr(N, !0).map(function(U) {
            return U.coordinate;
          }),
          polarRadius: Array.isArray(L) ? L : Kr(M, !0).map(function(U) {
            return U.coordinate;
          }),
          cx: q,
          cy: V,
          innerRadius: B,
          outerRadius: ee,
          key: _.key || "polar-grid",
          radialLines: k
        });
      }), Te(x, "renderLegend", function() {
        var _ = x.state.formattedGraphicalItems, S = x.props, k = S.children, E = S.width, L = S.height, P = x.props.margin || {}, A = E - (P.left || 0) - (P.right || 0), T = xk({
          children: k,
          formattedGraphicalItems: _,
          legendWidth: A,
          legendContent: u
        });
        if (!T)
          return null;
        var M = T.item, N = w_(T, $4);
        return /* @__PURE__ */ Tt(M, J(J({}, N), {}, {
          chartWidth: E,
          chartHeight: L,
          margin: P,
          onBBoxUpdate: x.handleLegendBBoxUpdate
        }));
      }), Te(x, "renderTooltip", function() {
        var _, S = x.props, k = S.children, E = S.accessibilityLayer, L = rr(k, Or);
        if (!L)
          return null;
        var P = x.state, A = P.isTooltipActive, T = P.activeCoordinate, M = P.activePayload, N = P.activeLabel, q = P.offset, V = (_ = L.props.active) !== null && _ !== void 0 ? _ : A;
        return /* @__PURE__ */ Tt(L, {
          viewBox: J(J({}, q), {}, {
            x: q.left,
            y: q.top
          }),
          active: V,
          label: N,
          payload: V ? M : [],
          coordinate: T,
          accessibilityLayer: E
        });
      }), Te(x, "renderBrush", function(_) {
        var S = x.props, k = S.margin, E = S.data, L = x.state, P = L.offset, A = L.dataStartIndex, T = L.dataEndIndex, M = L.updateId;
        return /* @__PURE__ */ Tt(_, {
          key: _.key || "_recharts-brush",
          onChange: rs(x.handleBrushChange, _.props.onChange),
          data: E,
          x: me(_.props.x) ? _.props.x : P.left,
          y: me(_.props.y) ? _.props.y : P.top + P.height + P.brushBottom - (k.bottom || 0),
          width: me(_.props.width) ? _.props.width : P.width,
          startIndex: A,
          endIndex: T,
          updateId: "brush-".concat(M)
        });
      }), Te(x, "renderReferenceElement", function(_, S, k) {
        if (!_)
          return null;
        var E = x, L = E.clipPathId, P = x.state, A = P.xAxisMap, T = P.yAxisMap, M = P.offset, N = _.type.defaultProps || {}, q = _.props, V = q.xAxisId, B = V === void 0 ? N.xAxisId : V, ee = q.yAxisId, U = ee === void 0 ? N.yAxisId : ee;
        return /* @__PURE__ */ Tt(_, {
          key: _.key || "".concat(S, "-").concat(k),
          xAxis: A[B],
          yAxis: T[U],
          viewBox: {
            x: M.left,
            y: M.top,
            width: M.width,
            height: M.height
          },
          clipPathId: L
        });
      }), Te(x, "renderActivePoints", function(_) {
        var S = _.item, k = _.activePoint, E = _.basePoint, L = _.childIndex, P = _.isRange, A = [], T = S.props.key, M = S.item.type.defaultProps !== void 0 ? J(J({}, S.item.type.defaultProps), S.item.props) : S.item.props, N = M.activeDot, q = M.dataKey, V = J(J({
          index: L,
          dataKey: q,
          cx: k.x,
          cy: k.y,
          r: 4,
          fill: Vp(S.item),
          strokeWidth: 2,
          stroke: "#fff",
          payload: k.payload,
          value: k.value
        }, ze(N, !1)), ps(N));
        return A.push(v.renderActiveDot(N, V, "".concat(T, "-activePoint-").concat(L))), E ? A.push(v.renderActiveDot(N, J(J({}, V), {}, {
          cx: E.x,
          cy: E.y
        }), "".concat(T, "-basePoint-").concat(L))) : P && A.push(null), A;
      }), Te(x, "renderGraphicChild", function(_, S, k) {
        var E = x.filterFormatItem(_, S, k);
        if (!E)
          return null;
        var L = x.getTooltipEventType(), P = x.state, A = P.isTooltipActive, T = P.tooltipAxis, M = P.activeTooltipIndex, N = P.activeLabel, q = x.props.children, V = rr(q, Or), B = E.props, ee = B.points, U = B.isRange, G = B.baseLine, Y = E.item.type.defaultProps !== void 0 ? J(J({}, E.item.type.defaultProps), E.item.props) : E.item.props, Q = Y.activeDot, ne = Y.hide, z = Y.activeBar, se = Y.activeShape, oe = !!(!ne && A && V && (Q || z || se)), ie = {};
        L !== "axis" && V && V.props.trigger === "click" ? ie = {
          onClick: rs(x.handleItemMouseEnter, _.props.onClick)
        } : L !== "axis" && (ie = {
          onMouseLeave: rs(x.handleItemMouseLeave, _.props.onMouseLeave),
          onMouseEnter: rs(x.handleItemMouseEnter, _.props.onMouseEnter)
        });
        var K = /* @__PURE__ */ Tt(_, J(J({}, E.props), ie));
        function ae(R) {
          return typeof T.dataKey == "function" ? T.dataKey(R.payload) : null;
        }
        if (oe)
          if (M >= 0) {
            var fe, $;
            if (T.dataKey && !T.allowDuplicatedCategory) {
              var Ee = typeof T.dataKey == "function" ? ae : "payload.".concat(T.dataKey.toString());
              fe = hs(ee, Ee, N), $ = U && G && hs(G, Ee, N);
            } else
              fe = ee == null ? void 0 : ee[M], $ = U && G && G[M];
            if (se || z) {
              var re = _.props.activeIndex !== void 0 ? _.props.activeIndex : M;
              return [/* @__PURE__ */ Tt(_, J(J(J({}, E.props), ie), {}, {
                activeIndex: re
              })), null, null];
            }
            if (!Le(fe))
              return [K].concat(Ka(x.renderActivePoints({
                item: E,
                activePoint: fe,
                basePoint: $,
                childIndex: M,
                isRange: U
              })));
          } else {
            var Pe, Oe = (Pe = x.getItemByXY(x.state.activeCoordinate)) !== null && Pe !== void 0 ? Pe : {
              graphicalItem: K
            }, Ne = Oe.graphicalItem, ke = Ne.item, Me = ke === void 0 ? _ : ke, Ae = Ne.childIndex, C = J(J(J({}, E.props), ie), {}, {
              activeIndex: Ae
            });
            return [/* @__PURE__ */ Tt(Me, C), null, null];
          }
        return U ? [K, null, null] : [K, null];
      }), Te(x, "renderCustomized", function(_, S, k) {
        return /* @__PURE__ */ Tt(_, J(J({
          key: "recharts-customized-".concat(k)
        }, x.props), x.state));
      }), Te(x, "renderMap", {
        CartesianGrid: {
          handler: cs,
          once: !0
        },
        ReferenceArea: {
          handler: x.renderReferenceElement
        },
        ReferenceLine: {
          handler: cs
        },
        ReferenceDot: {
          handler: x.renderReferenceElement
        },
        XAxis: {
          handler: cs
        },
        YAxis: {
          handler: cs
        },
        Brush: {
          handler: x.renderBrush,
          once: !0
        },
        Bar: {
          handler: x.renderGraphicChild
        },
        Line: {
          handler: x.renderGraphicChild
        },
        Area: {
          handler: x.renderGraphicChild
        },
        Radar: {
          handler: x.renderGraphicChild
        },
        RadialBar: {
          handler: x.renderGraphicChild
        },
        Scatter: {
          handler: x.renderGraphicChild
        },
        Pie: {
          handler: x.renderGraphicChild
        },
        Funnel: {
          handler: x.renderGraphicChild
        },
        Tooltip: {
          handler: x.renderCursor,
          once: !0
        },
        PolarGrid: {
          handler: x.renderPolarGrid,
          once: !0
        },
        PolarAngleAxis: {
          handler: x.renderPolarAxis
        },
        PolarRadiusAxis: {
          handler: x.renderPolarAxis
        },
        Customized: {
          handler: x.renderCustomized
        }
      }), x.clipPathId = "".concat((w = b.id) !== null && w !== void 0 ? w : Ao("recharts"), "-clip"), x.throttleTriggeredAfterMouseMove = bS(x.triggeredAfterMouseMove, (j = b.throttleDelay) !== null && j !== void 0 ? j : 1e3 / 60), x.state = {}, x;
    }
    return Y4(v, g), V4(v, [{
      key: "componentDidMount",
      value: function() {
        var w, j;
        this.addListener(), this.accessibilityManager.setDetails({
          container: this.container,
          offset: {
            left: (w = this.props.margin.left) !== null && w !== void 0 ? w : 0,
            top: (j = this.props.margin.top) !== null && j !== void 0 ? j : 0
          },
          coordinateList: this.state.tooltipTicks,
          mouseHandlerCallback: this.triggeredAfterMouseMove,
          layout: this.props.layout
        }), this.displayDefaultTooltip();
      }
    }, {
      key: "displayDefaultTooltip",
      value: function() {
        var w = this.props, j = w.children, x = w.data, _ = w.height, S = w.layout, k = rr(j, Or);
        if (k) {
          var E = k.props.defaultIndex;
          if (!(typeof E != "number" || E < 0 || E > this.state.tooltipTicks.length - 1)) {
            var L = this.state.tooltipTicks[E] && this.state.tooltipTicks[E].value, P = Bh(this.state, x, E, L), A = this.state.tooltipTicks[E].coordinate, T = (this.state.offset.top + _) / 2, M = S === "horizontal", N = M ? {
              x: A,
              y: T
            } : {
              y: A,
              x: T
            }, q = this.state.formattedGraphicalItems.find(function(B) {
              var ee = B.item;
              return ee.type.name === "Scatter";
            });
            q && (N = J(J({}, N), q.props.points[E].tooltipPosition), P = q.props.points[E].tooltipPayload);
            var V = {
              activeTooltipIndex: E,
              isTooltipActive: !0,
              activeLabel: L,
              activePayload: P,
              activeCoordinate: N
            };
            this.setState(V), this.renderCursor(k), this.accessibilityManager.setIndex(E);
          }
        }
      }
    }, {
      key: "getSnapshotBeforeUpdate",
      value: function(w, j) {
        if (!this.props.accessibilityLayer)
          return null;
        if (this.state.tooltipTicks !== j.tooltipTicks && this.accessibilityManager.setDetails({
          coordinateList: this.state.tooltipTicks
        }), this.props.layout !== w.layout && this.accessibilityManager.setDetails({
          layout: this.props.layout
        }), this.props.margin !== w.margin) {
          var x, _;
          this.accessibilityManager.setDetails({
            offset: {
              left: (x = this.props.margin.left) !== null && x !== void 0 ? x : 0,
              top: (_ = this.props.margin.top) !== null && _ !== void 0 ? _ : 0
            }
          });
        }
        return null;
      }
    }, {
      key: "componentDidUpdate",
      value: function(w) {
        Cm([rr(w.children, Or)], [rr(this.props.children, Or)]) || this.displayDefaultTooltip();
      }
    }, {
      key: "componentWillUnmount",
      value: function() {
        this.removeListener(), this.throttleTriggeredAfterMouseMove.cancel();
      }
    }, {
      key: "getTooltipEventType",
      value: function() {
        var w = rr(this.props.children, Or);
        if (w && typeof w.props.shared == "boolean") {
          var j = w.props.shared ? "axis" : "item";
          return l.indexOf(j) >= 0 ? j : i;
        }
        return i;
      }
      /**
       * Get the information of mouse in chart, return null when the mouse is not in the chart
       * @param  {MousePointer} event    The event object
       * @return {Object}          Mouse data
       */
    }, {
      key: "getMouseInfo",
      value: function(w) {
        if (!this.container)
          return null;
        var j = this.container, x = j.getBoundingClientRect(), _ = JN(x), S = {
          chartX: Math.round(w.pageX - _.left),
          chartY: Math.round(w.pageY - _.top)
        }, k = x.width / j.offsetWidth || 1, E = this.inRange(S.chartX, S.chartY, k);
        if (!E)
          return null;
        var L = this.state, P = L.xAxisMap, A = L.yAxisMap, T = this.getTooltipEventType(), M = j_(this.state, this.props.data, this.props.layout, E);
        if (T !== "axis" && P && A) {
          var N = pn(P).scale, q = pn(A).scale, V = N && N.invert ? N.invert(S.chartX) : null, B = q && q.invert ? q.invert(S.chartY) : null;
          return J(J({}, S), {}, {
            xValue: V,
            yValue: B
          }, M);
        }
        return M ? J(J({}, S), M) : null;
      }
    }, {
      key: "inRange",
      value: function(w, j) {
        var x = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 1, _ = this.props.layout, S = w / x, k = j / x;
        if (_ === "horizontal" || _ === "vertical") {
          var E = this.state.offset, L = S >= E.left && S <= E.left + E.width && k >= E.top && k <= E.top + E.height;
          return L ? {
            x: S,
            y: k
          } : null;
        }
        var P = this.state, A = P.angleAxisMap, T = P.radiusAxisMap;
        if (A && T) {
          var M = pn(A);
          return Mx({
            x: S,
            y: k
          }, M);
        }
        return null;
      }
    }, {
      key: "parseEventsOfWrapper",
      value: function() {
        var w = this.props.children, j = this.getTooltipEventType(), x = rr(w, Or), _ = {};
        x && j === "axis" && (x.props.trigger === "click" ? _ = {
          onClick: this.handleClick
        } : _ = {
          onMouseEnter: this.handleMouseEnter,
          onDoubleClick: this.handleDoubleClick,
          onMouseMove: this.handleMouseMove,
          onMouseLeave: this.handleMouseLeave,
          onTouchMove: this.handleTouchMove,
          onTouchStart: this.handleTouchStart,
          onTouchEnd: this.handleTouchEnd,
          onContextMenu: this.handleContextMenu
        });
        var S = ps(this.props, this.handleOuterEvent);
        return J(J({}, S), _);
      }
    }, {
      key: "addListener",
      value: function() {
        hm.on(pm, this.handleReceiveSyncEvent);
      }
    }, {
      key: "removeListener",
      value: function() {
        hm.removeListener(pm, this.handleReceiveSyncEvent);
      }
    }, {
      key: "filterFormatItem",
      value: function(w, j, x) {
        for (var _ = this.state.formattedGraphicalItems, S = 0, k = _.length; S < k; S++) {
          var E = _[S];
          if (E.item === w || E.props.key === w.key || j === Yr(E.item.type) && x === E.childIndex)
            return E;
        }
        return null;
      }
    }, {
      key: "renderClipPath",
      value: function() {
        var w = this.clipPathId, j = this.state.offset, x = j.left, _ = j.top, S = j.height, k = j.width;
        return /* @__PURE__ */ I.createElement("defs", null, /* @__PURE__ */ I.createElement("clipPath", {
          id: w
        }, /* @__PURE__ */ I.createElement("rect", {
          x,
          y: _,
          height: S,
          width: k
        })));
      }
    }, {
      key: "getXScales",
      value: function() {
        var w = this.state.xAxisMap;
        return w ? Object.entries(w).reduce(function(j, x) {
          var _ = x_(x, 2), S = _[0], k = _[1];
          return J(J({}, j), {}, Te({}, S, k.scale));
        }, {}) : null;
      }
    }, {
      key: "getYScales",
      value: function() {
        var w = this.state.yAxisMap;
        return w ? Object.entries(w).reduce(function(j, x) {
          var _ = x_(x, 2), S = _[0], k = _[1];
          return J(J({}, j), {}, Te({}, S, k.scale));
        }, {}) : null;
      }
    }, {
      key: "getXScaleByAxisId",
      value: function(w) {
        var j;
        return (j = this.state.xAxisMap) === null || j === void 0 || (j = j[w]) === null || j === void 0 ? void 0 : j.scale;
      }
    }, {
      key: "getYScaleByAxisId",
      value: function(w) {
        var j;
        return (j = this.state.yAxisMap) === null || j === void 0 || (j = j[w]) === null || j === void 0 ? void 0 : j.scale;
      }
    }, {
      key: "getItemByXY",
      value: function(w) {
        var j = this.state, x = j.formattedGraphicalItems, _ = j.activeItem;
        if (x && x.length)
          for (var S = 0, k = x.length; S < k; S++) {
            var E = x[S], L = E.props, P = E.item, A = P.type.defaultProps !== void 0 ? J(J({}, P.type.defaultProps), P.props) : P.props, T = Yr(P.type);
            if (T === "Bar") {
              var M = (L.data || []).find(function(B) {
                return S3(w, B);
              });
              if (M)
                return {
                  graphicalItem: E,
                  payload: M
                };
            } else if (T === "RadialBar") {
              var N = (L.data || []).find(function(B) {
                return Mx(w, B);
              });
              if (N)
                return {
                  graphicalItem: E,
                  payload: N
                };
            } else if (Hl(E, _) || Gl(E, _) || go(E, _)) {
              var q = hF({
                graphicalItem: E,
                activeTooltipItem: _,
                itemData: A.data
              }), V = A.activeIndex === void 0 ? q : A.activeIndex;
              return {
                graphicalItem: J(J({}, E), {}, {
                  childIndex: V
                }),
                payload: go(E, _) ? A.data[q] : E.props.data[q]
              };
            }
          }
        return null;
      }
    }, {
      key: "render",
      value: function() {
        var w = this;
        if (!Wy(this))
          return null;
        var j = this.props, x = j.children, _ = j.className, S = j.width, k = j.height, E = j.style, L = j.compact, P = j.title, A = j.desc, T = w_(j, L4), M = ze(T, !1);
        if (L)
          return /* @__PURE__ */ I.createElement(t_, {
            state: this.state,
            width: this.props.width,
            height: this.props.height,
            clipPathId: this.clipPathId
          }, /* @__PURE__ */ I.createElement(Pm, _a({}, M, {
            width: S,
            height: k,
            title: P,
            desc: A
          }), this.renderClipPath(), Hy(x, this.renderMap)));
        if (this.props.accessibilityLayer) {
          var N, q;
          M.tabIndex = (N = this.props.tabIndex) !== null && N !== void 0 ? N : 0, M.role = (q = this.props.role) !== null && q !== void 0 ? q : "application", M.onKeyDown = function(B) {
            w.accessibilityManager.keyboardEvent(B);
          }, M.onFocus = function() {
            w.accessibilityManager.focus();
          };
        }
        var V = this.parseEventsOfWrapper();
        return /* @__PURE__ */ I.createElement(t_, {
          state: this.state,
          width: this.props.width,
          height: this.props.height,
          clipPathId: this.clipPathId
        }, /* @__PURE__ */ I.createElement("div", _a({
          className: Ve("recharts-wrapper", _),
          style: J({
            position: "relative",
            cursor: "default",
            width: S,
            height: k
          }, E)
        }, V, {
          ref: function(ee) {
            w.container = ee;
          }
        }), /* @__PURE__ */ I.createElement(Pm, _a({}, M, {
          width: S,
          height: k,
          title: P,
          desc: A,
          style: tU
        }), this.renderClipPath(), Hy(x, this.renderMap)), this.renderLegend(), this.renderTooltip()));
      }
    }]);
  })(B_);
  Te(y, "displayName", r), Te(y, "defaultProps", J({
    layout: "horizontal",
    stackOffset: "none",
    barCategoryGap: "10%",
    barGap: 4,
    margin: {
      top: 5,
      right: 5,
      bottom: 5,
      left: 5
    },
    reverseStackOrder: !1,
    syncMethod: "index"
  }, f)), Te(y, "getDerivedStateFromProps", function(g, v) {
    var b = g.dataKey, w = g.data, j = g.children, x = g.width, _ = g.height, S = g.layout, k = g.stackOffset, E = g.margin, L = v.dataStartIndex, P = v.dataEndIndex;
    if (v.updateId === void 0) {
      var A = S_(g);
      return J(J(J({}, A), {}, {
        updateId: 0
      }, m(J(J({
        props: g
      }, A), {}, {
        updateId: 0
      }), v)), {}, {
        prevDataKey: b,
        prevData: w,
        prevWidth: x,
        prevHeight: _,
        prevLayout: S,
        prevStackOffset: k,
        prevMargin: E,
        prevChildren: j
      });
    }
    if (b !== v.prevDataKey || w !== v.prevData || x !== v.prevWidth || _ !== v.prevHeight || S !== v.prevLayout || k !== v.prevStackOffset || !ja(E, v.prevMargin)) {
      var T = S_(g), M = {
        // (chartX, chartY) are (0,0) in default state, but we want to keep the last mouse position to avoid
        // any flickering
        chartX: v.chartX,
        chartY: v.chartY,
        // The tooltip should stay active when it was active in the previous render. If this is not
        // the case, the tooltip disappears and immediately re-appears, causing a flickering effect
        isTooltipActive: v.isTooltipActive
      }, N = J(J({}, j_(v, w, S)), {}, {
        updateId: v.updateId + 1
      }), q = J(J(J({}, T), M), N);
      return J(J(J({}, q), m(J({
        props: g
      }, q), v)), {}, {
        prevDataKey: b,
        prevData: w,
        prevWidth: x,
        prevHeight: _,
        prevLayout: S,
        prevStackOffset: k,
        prevMargin: E,
        prevChildren: j
      });
    }
    if (!Cm(j, v.prevChildren)) {
      var V, B, ee, U, G = rr(j, $a), Y = G && (V = (B = G.props) === null || B === void 0 ? void 0 : B.startIndex) !== null && V !== void 0 ? V : L, Q = G && (ee = (U = G.props) === null || U === void 0 ? void 0 : U.endIndex) !== null && ee !== void 0 ? ee : P, ne = Y !== L || Q !== P, z = !Le(w), se = z && !ne ? v.updateId : v.updateId + 1;
      return J(J({
        updateId: se
      }, m(J(J({
        props: g
      }, v), {}, {
        updateId: se,
        dataStartIndex: Y,
        dataEndIndex: Q
      }), v)), {}, {
        prevChildren: j,
        dataStartIndex: Y,
        dataEndIndex: Q
      });
    }
    return null;
  }), Te(y, "renderActiveDot", function(g, v, b) {
    var w;
    return /* @__PURE__ */ Cr(g) ? w = /* @__PURE__ */ Tt(g, v) : $e(g) ? w = g(v) : w = /* @__PURE__ */ I.createElement(Kp, v), /* @__PURE__ */ I.createElement(yt, {
      className: "recharts-active-dot",
      key: b
    }, w);
  });
  var h = /* @__PURE__ */ U_(function(v, b) {
    return /* @__PURE__ */ I.createElement(y, _a({}, v, {
      ref: b
    }));
  });
  return h.displayName = y.displayName, h;
}, dU = OE({
  chartName: "LineChart",
  GraphicalChild: Ro,
  axisComponents: [{
    axisType: "xAxis",
    AxisComp: Io
  }, {
    axisType: "yAxis",
    AxisComp: Do
  }],
  formatAxisMap: Kk
}), fU = OE({
  chartName: "BarChart",
  GraphicalChild: Zn,
  defaultTooltipEventType: "axis",
  validateTooltipEventTypes: ["axis", "item"],
  axisComponents: [{
    axisType: "xAxis",
    AxisComp: Io
  }, {
    axisType: "yAxis",
    AxisComp: Do
  }],
  formatAxisMap: Kk
});
function E_({
  data: e,
  type: t = "line",
  title: r,
  color: n = "#3b82f6",
  className: a,
  height: i = 300
}) {
  const o = t === "line" ? dU : fU;
  return /* @__PURE__ */ s.jsxs("div", { className: Ie("performance-chart bg-white border border-gray-200 rounded-lg p-6", a), children: [
    r && /* @__PURE__ */ s.jsx("h3", { className: "text-lg font-semibold text-gray-900 mb-4", children: r }),
    /* @__PURE__ */ s.jsx(VN, { width: "100%", height: i, children: /* @__PURE__ */ s.jsxs(o, { data: e, children: [
      /* @__PURE__ */ s.jsx(pE, { strokeDasharray: "3 3", stroke: "#e5e7eb" }),
      /* @__PURE__ */ s.jsx(
        Io,
        {
          dataKey: "timestamp",
          tick: { fill: "#6b7280", fontSize: 12 },
          stroke: "#9ca3af"
        }
      ),
      /* @__PURE__ */ s.jsx(
        Do,
        {
          tick: { fill: "#6b7280", fontSize: 12 },
          stroke: "#9ca3af"
        }
      ),
      /* @__PURE__ */ s.jsx(
        Or,
        {
          contentStyle: {
            backgroundColor: "white",
            border: "1px solid #e5e7eb",
            borderRadius: "0.5rem"
          }
        }
      ),
      /* @__PURE__ */ s.jsx(Un, {}),
      t === "line" ? /* @__PURE__ */ s.jsx(
        Ro,
        {
          type: "monotone",
          dataKey: "value",
          stroke: n,
          strokeWidth: 2,
          name: r || "Value"
        }
      ) : /* @__PURE__ */ s.jsx(
        Zn,
        {
          type: "monotone",
          dataKey: "value",
          fill: n,
          name: r || "Value"
        }
      )
    ] }) })
  ] });
}
function mU({ artifacts: e, onRowClick: t, className: r }) {
  const [n, a] = de({
    key: "created",
    dir: "desc"
  }), [i, o] = de(""), l = (f) => {
    a((p) => ({
      key: f,
      dir: p.key === f && p.dir === "asc" ? "desc" : "asc"
    }));
  }, u = [...e.filter(
    (f) => f.name.toLowerCase().includes(i.toLowerCase()) || f.type.toLowerCase().includes(i.toLowerCase())
  )].sort((f, p) => {
    const m = f[n.key], y = p[n.key];
    if (typeof m == "string" && typeof y == "string") {
      const h = m.localeCompare(y);
      return n.dir === "asc" ? h : -h;
    }
    if (typeof m == "number" && typeof y == "number") {
      const h = m - y;
      return n.dir === "asc" ? h : -h;
    }
    return 0;
  }), d = (f) => n.key !== f ? null : n.dir === "asc" ? "^" : "v";
  return /* @__PURE__ */ s.jsxs("div", { className: Ie("artifact-table", r), children: [
    /* @__PURE__ */ s.jsx("div", { className: "mb-4", children: /* @__PURE__ */ s.jsx(
      D,
      {
        type: "text",
        value: i,
        onChange: (f) => o(f.target.value),
        placeholder: "Filter artifacts..."
      }
    ) }),
    /* @__PURE__ */ s.jsxs("div", { className: "overflow-x-auto border border-gray-200 rounded-lg", children: [
      /* @__PURE__ */ s.jsxs("table", { className: "min-w-full divide-y divide-gray-200", children: [
        /* @__PURE__ */ s.jsx("thead", { className: "bg-gray-50", children: /* @__PURE__ */ s.jsxs("tr", { children: [
          /* @__PURE__ */ s.jsxs(
            "th",
            {
              onClick: () => l("name"),
              className: "px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider cursor-pointer hover:bg-gray-100",
              children: [
                "Name ",
                d("name")
              ]
            }
          ),
          /* @__PURE__ */ s.jsxs(
            "th",
            {
              onClick: () => l("type"),
              className: "px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider cursor-pointer hover:bg-gray-100",
              children: [
                "Type ",
                d("type")
              ]
            }
          ),
          /* @__PURE__ */ s.jsxs(
            "th",
            {
              onClick: () => l("status"),
              className: "px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider cursor-pointer hover:bg-gray-100",
              children: [
                "Status ",
                d("status")
              ]
            }
          ),
          /* @__PURE__ */ s.jsxs(
            "th",
            {
              onClick: () => l("fitness"),
              className: "px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider cursor-pointer hover:bg-gray-100",
              children: [
                "Fitness ",
                d("fitness")
              ]
            }
          ),
          /* @__PURE__ */ s.jsxs(
            "th",
            {
              onClick: () => l("generation"),
              className: "px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider cursor-pointer hover:bg-gray-100",
              children: [
                "Generation ",
                d("generation")
              ]
            }
          ),
          /* @__PURE__ */ s.jsxs(
            "th",
            {
              onClick: () => l("created"),
              className: "px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider cursor-pointer hover:bg-gray-100",
              children: [
                "Created ",
                d("created")
              ]
            }
          )
        ] }) }),
        /* @__PURE__ */ s.jsx("tbody", { className: "bg-white divide-y divide-gray-200", children: u.map((f) => {
          var p;
          return /* @__PURE__ */ s.jsxs(
            "tr",
            {
              onClick: () => t == null ? void 0 : t(f),
              className: Ie(
                "hover:bg-gray-50 transition-colors",
                t && "cursor-pointer"
              ),
              children: [
                /* @__PURE__ */ s.jsx("td", { className: "px-6 py-4 whitespace-nowrap", children: /* @__PURE__ */ s.jsx("div", { className: "text-sm font-medium text-gray-900", children: f.name }) }),
                /* @__PURE__ */ s.jsx("td", { className: "px-6 py-4 whitespace-nowrap", children: /* @__PURE__ */ s.jsx("div", { className: "text-sm text-gray-500", children: f.type }) }),
                /* @__PURE__ */ s.jsx("td", { className: "px-6 py-4 whitespace-nowrap", children: /* @__PURE__ */ s.jsx("span", { className: Ie(
                  "inline-flex px-2 py-1 text-xs font-semibold rounded-full",
                  f.status === "success" && "bg-green-100 text-green-800",
                  f.status === "pending" && "bg-yellow-100 text-yellow-800",
                  f.status === "failed" && "bg-red-100 text-red-800"
                ), children: f.status }) }),
                /* @__PURE__ */ s.jsx("td", { className: "px-6 py-4 whitespace-nowrap", children: /* @__PURE__ */ s.jsx("div", { className: "text-sm text-gray-900", children: ((p = f.fitness) == null ? void 0 : p.toFixed(4)) || "N/A" }) }),
                /* @__PURE__ */ s.jsx("td", { className: "px-6 py-4 whitespace-nowrap", children: /* @__PURE__ */ s.jsx("div", { className: "text-sm text-gray-500", children: f.generation || "N/A" }) }),
                /* @__PURE__ */ s.jsx("td", { className: "px-6 py-4 whitespace-nowrap", children: /* @__PURE__ */ s.jsx("div", { className: "text-sm text-gray-500", children: new Date(f.created).toLocaleString() }) })
              ]
            },
            f.id
          );
        }) })
      ] }),
      u.length === 0 && /* @__PURE__ */ s.jsx("div", { className: "text-center py-12 text-gray-500", children: /* @__PURE__ */ s.jsx("p", { children: "No artifacts found" }) })
    ] })
  ] });
}
function _z() {
  const [e, t] = de("30d"), r = Ya(() => {
    const y = /* @__PURE__ */ new Date();
    let h = new Date(y);
    return e === "7d" ? h.setDate(y.getDate() - 7) : e === "30d" ? h.setDate(y.getDate() - 30) : e === "90d" ? h.setDate(y.getDate() - 90) : h = /* @__PURE__ */ new Date("2000-01-01T00:00:00Z"), {
      start: h.toISOString(),
      end: y.toISOString(),
      granularity: e === "90d" ? "week" : "day"
    };
  }, [e]), { metrics: n, performance: a, isLoading: i, error: o } = i1(r), l = n == null ? void 0 : n.metrics, c = (n == null ? void 0 : n.time_series) || [], u = [
    {
      title: "Total Evolutions",
      value: (l == null ? void 0 : l.total_evolutions) || 0,
      change: 0,
      unit: "",
      trend: "up"
    },
    {
      title: "Success Rate",
      value: l != null && l.success_rate ? Math.round(l.success_rate * 100) : 0,
      change: 0,
      unit: "%",
      trend: "up"
    },
    {
      title: "Adversarial Tests",
      value: (l == null ? void 0 : l.total_adversarial_tests) || 0,
      change: 0,
      unit: "",
      trend: "up"
    },
    {
      title: "Avg. Fitness Gain",
      value: (l == null ? void 0 : l.average_fitness_improvement) || 0,
      change: 0,
      unit: "",
      trend: "up"
    }
  ], d = c.length > 0 ? c.map((y) => ({
    timestamp: y.timestamp,
    value: y.average_fitness
  })) : [], f = c.length > 0 ? c.map((y) => ({
    timestamp: y.timestamp,
    value: y.evolutions
  })) : [], p = (a == null ? void 0 : a.model_performance) || [], m = [];
  return /* @__PURE__ */ s.jsxs("div", { className: "analytics-dashboard min-h-screen bg-slate-50 p-6", children: [
    /* @__PURE__ */ s.jsx("header", { className: "mb-6", children: /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ s.jsxs("div", { children: [
        /* @__PURE__ */ s.jsx("h1", { className: "text-3xl font-bold text-slate-900", children: "Analytics Dashboard" }),
        /* @__PURE__ */ s.jsx("p", { className: "text-slate-600 mt-1", children: "Track your evolution performance and metrics" })
      ] }),
      /* @__PURE__ */ s.jsx("div", { className: "flex gap-2", children: ["7d", "30d", "90d", "all"].map((y) => /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => t(y),
          variant: e === y ? "primary" : "secondary",
          children: [
            y === "7d" && "Last 7 Days",
            y === "30d" && "Last 30 Days",
            y === "90d" && "Last 90 Days",
            y === "all" && "All Time"
          ]
        },
        y
      )) })
    ] }) }),
    o ? /* @__PURE__ */ s.jsxs("div", { className: "bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg", children: [
      "Failed to load analytics data: ",
      o instanceof Error ? o.message : String(o)
    ] }) : i ? /* @__PURE__ */ s.jsxs("div", { className: "text-center py-12", children: [
      /* @__PURE__ */ s.jsx("div", { className: "inline-block animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600" }),
      /* @__PURE__ */ s.jsx("p", { className: "mt-4 text-gray-600", children: "Loading analytics..." })
    ] }) : /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
      /* @__PURE__ */ s.jsx(sC, { stats: u, columns: 4 }),
      /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 lg:grid-cols-2 gap-6", children: [
        /* @__PURE__ */ s.jsx(
          E_,
          {
            data: d,
            type: "line",
            title: "Fitness Over Time",
            color: "#3b82f6"
          }
        ),
        /* @__PURE__ */ s.jsx(
          E_,
          {
            data: f,
            type: "bar",
            title: "Evolutions Completed",
            color: "#10b981"
          }
        )
      ] }),
      /* @__PURE__ */ s.jsx(he, { title: "Recent Artifacts", description: "Artifacts created during this period.", children: /* @__PURE__ */ s.jsx(
        mU,
        {
          artifacts: m,
          onRowClick: (y) => console.log("Selected:", y)
        }
      ) }),
      /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 lg:grid-cols-3 gap-6", children: [
        /* @__PURE__ */ s.jsx(he, { title: "Top Performing Models", description: "Quality and latency leaders.", children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-2", children: [
          p.length === 0 && /* @__PURE__ */ s.jsx("p", { className: "text-sm text-slate-400", children: "No model performance data." }),
          p.slice(0, 5).map((y) => /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between text-sm", children: [
            /* @__PURE__ */ s.jsx("span", { className: "text-slate-800", children: y.model }),
            /* @__PURE__ */ s.jsxs("span", { className: "text-slate-600", children: [
              Math.round(y.success_rate * 100),
              "%"
            ] })
          ] }, `${y.provider}-${y.model}`))
        ] }) }),
        /* @__PURE__ */ s.jsx(he, { title: "Resource Usage", description: "Live utilization from monitoring.", children: /* @__PURE__ */ s.jsx("p", { className: "text-sm text-slate-500", children: "Use the System Health panel for live resource telemetry." }) }),
        /* @__PURE__ */ s.jsx(he, { title: "Recent Activity", description: "Audit events and workflow updates.", children: /* @__PURE__ */ s.jsx("p", { className: "text-sm text-slate-500", children: "Activity feed will appear here once enabled." }) })
      ] })
    ] })
  ] });
}
function jz() {
  var L, P;
  const { config: e } = bl(), { models: t } = xl(), { startWorkflow: r } = oj(), [n, a] = de(""), [i, o] = de(""), [l, c] = de(""), [u, d] = de(null), [f, p] = de([]), m = Tr(null), { workflow: y, pause: h, resume: g, stop: v, isLoading: b } = l1(u || void 0), w = (y == null ? void 0 : y.status) || "idle", j = ((L = y == null ? void 0 : y.progress) == null ? void 0 : L.percentage) || 0, x = w === "running";
  et(() => {
    w !== m.current && (m.current = w, p((A) => [
      ...A,
      {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        level: "info",
        message: `Workflow status: ${w}`
      }
    ]));
  }, [w]);
  const _ = async () => {
    if (!l.trim()) {
      alert("Please enter initial content");
      return;
    }
    p([
      {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        level: "info",
        message: "Starting workflow..."
      }
    ]);
    const A = await r({
      content: l,
      mode: e.mode,
      parameters: {
        max_iterations: e.max_iterations,
        population_size: e.population_size,
        temperature: e.temperature,
        top_p: e.top_p
      },
      models: t.map((T) => ({
        provider: T.provider,
        model: T.model,
        api_key: T.api_key || ""
      }))
    });
    A != null && A.evolution_id && d(A.evolution_id);
  }, S = () => {
    u && h();
  }, k = () => {
    d(null), p([]);
  }, E = () => {
    u && v();
  };
  return /* @__PURE__ */ s.jsxs("div", { className: "workflow-builder flex h-screen bg-slate-50", children: [
    /* @__PURE__ */ s.jsx(wl, {}),
    /* @__PURE__ */ s.jsxs("main", { className: "flex-1 flex flex-col overflow-hidden", children: [
      /* @__PURE__ */ s.jsxs("header", { className: "bg-white border-b border-slate-200 px-6 py-4", children: [
        /* @__PURE__ */ s.jsx("h1", { className: "text-2xl font-bold text-gray-900", children: "Workflow Builder" }),
        /* @__PURE__ */ s.jsx("p", { className: "text-sm text-gray-600", children: "Create and manage evolutionary workflows" })
      ] }),
      /* @__PURE__ */ s.jsx("div", { className: "flex-1 overflow-auto p-6", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 lg:grid-cols-3 gap-6", children: [
        /* @__PURE__ */ s.jsxs("div", { className: "lg:col-span-2 space-y-6", children: [
          /* @__PURE__ */ s.jsx(he, { title: "Workflow Details", children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
            /* @__PURE__ */ s.jsx(H, { label: "Workflow Name", children: /* @__PURE__ */ s.jsx(
              D,
              {
                type: "text",
                value: n,
                onChange: (A) => a(A.target.value),
                placeholder: "Enter workflow name..."
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Description", children: /* @__PURE__ */ s.jsx(
              Lt,
              {
                value: i,
                onChange: (A) => o(A.target.value),
                placeholder: "Describe the workflow purpose...",
                rows: 3
              }
            ) }),
            /* @__PURE__ */ s.jsx(H, { label: "Initial Content", children: /* @__PURE__ */ s.jsx(
              Lt,
              {
                value: l,
                onChange: (A) => c(A.target.value),
                placeholder: "Enter initial content to evolve...",
                rows: 10,
                className: "font-mono text-sm"
              }
            ) })
          ] }) }),
          /* @__PURE__ */ s.jsxs(he, { title: "Execution Controls", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between mb-4", children: [
              /* @__PURE__ */ s.jsxs("div", { className: "flex items-center gap-3", children: [
                /* @__PURE__ */ s.jsx(So, { status: w }),
                x && /* @__PURE__ */ s.jsx("span", { className: "text-sm text-gray-600", children: "Running..." })
              ] }),
              /* @__PURE__ */ s.jsxs("div", { className: "flex gap-2", children: [
                !x && w === "idle" && /* @__PURE__ */ s.jsx(
                  X,
                  {
                    onClick: _,
                    variant: "primary",
                    children: "Start Workflow"
                  }
                ),
                x && /* @__PURE__ */ s.jsx(
                  X,
                  {
                    onClick: S,
                    variant: "secondary",
                    children: "Pause"
                  }
                ),
                !x && w === "paused" && /* @__PURE__ */ s.jsx(
                  X,
                  {
                    onClick: () => g(),
                    variant: "primary",
                    children: "Resume"
                  }
                ),
                u && w !== "stopped" && w !== "completed" && /* @__PURE__ */ s.jsx(
                  X,
                  {
                    onClick: E,
                    variant: "secondary",
                    children: "Stop"
                  }
                ),
                /* @__PURE__ */ s.jsx(
                  X,
                  {
                    onClick: k,
                    variant: "secondary",
                    children: "Reset"
                  }
                )
              ] })
            ] }),
            j > 0 && /* @__PURE__ */ s.jsx(Jh, { progress: j, label: "Workflow Progress" })
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { className: "space-y-6", children: [
          /* @__PURE__ */ s.jsx(he, { title: "Status", children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-3", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex justify-between text-sm", children: [
              /* @__PURE__ */ s.jsx("span", { className: "text-gray-600", children: "Mode" }),
              /* @__PURE__ */ s.jsx("span", { className: "font-medium", children: e.mode })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { className: "flex justify-between text-sm", children: [
              /* @__PURE__ */ s.jsx("span", { className: "text-gray-600", children: "Iterations" }),
              /* @__PURE__ */ s.jsxs("span", { className: "font-medium", children: [
                ((P = y == null ? void 0 : y.progress) == null ? void 0 : P.current_iteration) || 0,
                "/",
                e.max_iterations
              ] })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { className: "flex justify-between text-sm", children: [
              /* @__PURE__ */ s.jsx("span", { className: "text-gray-600", children: "Population" }),
              /* @__PURE__ */ s.jsx("span", { className: "font-medium", children: e.population_size })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { className: "flex justify-between text-sm", children: [
              /* @__PURE__ */ s.jsx("span", { className: "text-gray-600", children: "Temperature" }),
              /* @__PURE__ */ s.jsx("span", { className: "font-medium", children: e.temperature })
            ] })
          ] }) }),
          /* @__PURE__ */ s.jsxs(he, { title: "Live Logs", children: [
            /* @__PURE__ */ s.jsx(_l, { logs: f, maxHeight: "300px" }),
            b && /* @__PURE__ */ s.jsx("p", { className: "mt-3 text-xs text-slate-400", children: "Fetching workflow status..." })
          ] })
        ] })
      ] }) })
    ] })
  ] });
}
function Sz() {
  var j;
  const { config: e } = bl(), { models: t } = xl(), [r, n] = de(""), [a, i] = de(null), [o, l] = de([]), c = Tr(null), {
    evolution: u,
    startEvolution: d,
    pauseEvolution: f,
    resumeEvolution: p,
    stopEvolution: m,
    isLoading: y
  } = n1(a || void 0), h = (u == null ? void 0 : u.status) || "idle", g = ((j = u == null ? void 0 : u.progress) == null ? void 0 : j.percentage) || 0, v = h === "running";
  et(() => {
    h !== c.current && (c.current = h, l((x) => [
      ...x,
      {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        level: "info",
        message: `Evolution status: ${h}`
      }
    ]));
  }, [h]);
  const b = async () => {
    if (!r.trim()) {
      alert("Please enter initial content");
      return;
    }
    l([
      {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        level: "info",
        message: "Starting evolution..."
      }
    ]);
    const x = await d({
      content: r,
      mode: e.mode,
      parameters: {
        max_iterations: e.max_iterations,
        population_size: e.population_size,
        temperature: e.temperature,
        top_p: e.top_p
      },
      models: t.map((_) => ({
        provider: _.provider,
        model: _.model,
        api_key: _.api_key || ""
      }))
    });
    x != null && x.evolution_id && i(x.evolution_id);
  }, w = () => {
    i(null), l([]);
  };
  return /* @__PURE__ */ s.jsxs("div", { className: "flex h-screen bg-slate-50", children: [
    /* @__PURE__ */ s.jsx(wl, {}),
    /* @__PURE__ */ s.jsxs("main", { className: "flex-1 flex flex-col overflow-hidden", children: [
      /* @__PURE__ */ s.jsxs("header", { className: "bg-white border-b border-slate-200 px-6 py-4", children: [
        /* @__PURE__ */ s.jsx("h1", { className: "text-2xl font-bold text-slate-900", children: "Evolution Engine" }),
        /* @__PURE__ */ s.jsx("p", { className: "text-sm text-slate-600", children: "Run evolutionary optimization workflows." })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex-1 overflow-auto p-6 space-y-6", children: [
        /* @__PURE__ */ s.jsx(he, { title: "Evolution Setup", children: /* @__PURE__ */ s.jsx("div", { className: "space-y-4", children: /* @__PURE__ */ s.jsx(H, { label: "Initial Content", children: /* @__PURE__ */ s.jsx(
          Lt,
          {
            rows: 8,
            value: r,
            onChange: (x) => n(x.target.value),
            placeholder: "Enter initial content to evolve..."
          }
        ) }) }) }),
        /* @__PURE__ */ s.jsxs(he, { title: "Execution Controls", children: [
          /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap items-center justify-between gap-4", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex items-center gap-3", children: [
              /* @__PURE__ */ s.jsx(So, { status: h }),
              v && /* @__PURE__ */ s.jsx("span", { className: "text-sm text-slate-500", children: "Running..." })
            ] }),
            /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap gap-2", children: [
              !v && h === "idle" && /* @__PURE__ */ s.jsx(X, { onClick: b, children: "Start Evolution" }),
              v && /* @__PURE__ */ s.jsx(X, { onClick: () => f(a), variant: "secondary", children: "Pause" }),
              !v && h === "paused" && /* @__PURE__ */ s.jsx(X, { onClick: () => p(a), children: "Resume" }),
              a && h !== "stopped" && h !== "completed" && /* @__PURE__ */ s.jsx(X, { onClick: () => m(a), variant: "secondary", children: "Stop" }),
              /* @__PURE__ */ s.jsx(X, { onClick: w, variant: "secondary", children: "Reset" })
            ] })
          ] }),
          g > 0 && /* @__PURE__ */ s.jsx(Jh, { progress: g, label: "Evolution Progress" })
        ] }),
        a && /* @__PURE__ */ s.jsx(bj, { executionId: a }),
        /* @__PURE__ */ s.jsxs(he, { title: "Live Logs", children: [
          /* @__PURE__ */ s.jsx(_l, { logs: o, maxHeight: "260px" }),
          y && /* @__PURE__ */ s.jsx("p", { className: "mt-3 text-xs text-slate-400", children: "Fetching evolution status..." })
        ] })
      ] })
    ] })
  ] });
}
const hU = [
  "prompt_injection",
  "jailbreak",
  "data_exfiltration",
  "system_prompt_leak",
  "tool_misuse"
];
function kz() {
  const { providers: e } = ij(), [t, r] = de(""), [n, a] = de(["prompt_injection"]), [i, o] = de(5), [l, c] = de(null), [u, d] = de([]), f = Tr(null), [p, m] = de([
    { provider: "openai", model: "gpt-4" }
  ]), [y, h] = de([
    { provider: "anthropic", model: "claude-3-sonnet" }
  ]), g = Ya(() => !e || e.length === 0 ? [
    { provider: "openai", name: "OpenAI", models: ["gpt-4"] },
    { provider: "anthropic", name: "Anthropic", models: ["claude-3-sonnet"] }
  ] : e, [e]), {
    test: v,
    startTest: b,
    stopTest: w,
    isLoading: j
  } = a1(l || void 0), x = (v == null ? void 0 : v.status) || "idle";
  et(() => {
    x !== f.current && (f.current = x, d((A) => [
      ...A,
      {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        level: "info",
        message: `Adversarial status: ${x}`
      }
    ]));
  }, [x]);
  const _ = (A) => {
    a(
      (T) => T.includes(A) ? T.filter((M) => M !== A) : [...T, A]
    );
  }, S = (A, T, M) => {
    (A === "red" ? m : h)((A === "red" ? p : y).map((V, B) => B === T ? { ...V, ...M } : V));
  }, k = (A) => {
    const T = A === "red" ? m : h, M = g[0];
    T((N) => {
      var q;
      return [
        ...N,
        { provider: (M == null ? void 0 : M.provider) || "openai", model: ((q = M == null ? void 0 : M.models) == null ? void 0 : q[0]) || "gpt-4" }
      ];
    });
  }, E = (A, T) => {
    (A === "red" ? m : h)((N) => N.filter((q, V) => V !== T));
  }, L = async () => {
    if (!t.trim()) {
      alert("Please enter content to test");
      return;
    }
    d([
      {
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        level: "info",
        message: "Starting adversarial test..."
      }
    ]);
    const A = await b({
      content: t,
      attack_modes: n,
      parameters: {
        num_rounds: i,
        red_team_models: p,
        blue_team_models: y
      }
    });
    A != null && A.test_id && c(A.test_id);
  }, P = () => {
    c(null), d([]);
  };
  return /* @__PURE__ */ s.jsxs("div", { className: "flex h-screen bg-slate-50", children: [
    /* @__PURE__ */ s.jsx(wl, {}),
    /* @__PURE__ */ s.jsxs("main", { className: "flex-1 flex flex-col overflow-hidden", children: [
      /* @__PURE__ */ s.jsxs("header", { className: "bg-white border-b border-slate-200 px-6 py-4", children: [
        /* @__PURE__ */ s.jsx("h1", { className: "text-2xl font-bold text-slate-900", children: "Adversarial Testing" }),
        /* @__PURE__ */ s.jsx("p", { className: "text-sm text-slate-600", children: "Run red/blue team validation workflows." })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex-1 overflow-auto p-6 space-y-6", children: [
        /* @__PURE__ */ s.jsx(he, { title: "Test Setup", children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ s.jsx(H, { label: "Content Under Test", children: /* @__PURE__ */ s.jsx(
            Lt,
            {
              rows: 6,
              value: t,
              onChange: (A) => r(A.target.value),
              placeholder: "Paste content or prompts for adversarial testing..."
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Attack Modes", children: /* @__PURE__ */ s.jsx("div", { className: "flex flex-wrap gap-2", children: hU.map((A) => /* @__PURE__ */ s.jsx(
            Ze,
            {
              checked: n.includes(A),
              onChange: () => _(A),
              label: A.replace(/_/g, " ")
            },
            A
          )) }) }),
          /* @__PURE__ */ s.jsx(H, { label: "Rounds", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              min: 1,
              value: i,
              onChange: (A) => o(parseInt(A.target.value, 10) || 1)
            }
          ) })
        ] }) }),
        /* @__PURE__ */ s.jsx(he, { title: "Team Models", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 lg:grid-cols-2 gap-6", children: [
          /* @__PURE__ */ s.jsxs("div", { className: "space-y-3", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
              /* @__PURE__ */ s.jsx("h3", { className: "text-sm font-semibold text-slate-700", children: "Red Team Models" }),
              /* @__PURE__ */ s.jsx(X, { onClick: () => k("red"), variant: "secondary", children: "Add" })
            ] }),
            p.map((A, T) => {
              var M;
              return /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-6 gap-2", children: [
                /* @__PURE__ */ s.jsx(
                  je,
                  {
                    className: "col-span-2",
                    value: A.provider,
                    onChange: (N) => S("red", T, { provider: N.target.value }),
                    children: g.map((N) => /* @__PURE__ */ s.jsx("option", { value: N.provider, children: N.name || N.provider }, N.provider))
                  }
                ),
                /* @__PURE__ */ s.jsx(
                  je,
                  {
                    className: "col-span-3",
                    value: A.model,
                    onChange: (N) => S("red", T, { model: N.target.value }),
                    children: (((M = g.find((N) => N.provider === A.provider)) == null ? void 0 : M.models) || []).map((N) => /* @__PURE__ */ s.jsx("option", { value: N, children: N }, N))
                  }
                ),
                /* @__PURE__ */ s.jsx(X, { onClick: () => E("red", T), variant: "secondary", children: "Remove" })
              ] }, `red-${T}`);
            })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "space-y-3", children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
              /* @__PURE__ */ s.jsx("h3", { className: "text-sm font-semibold text-slate-700", children: "Blue Team Models" }),
              /* @__PURE__ */ s.jsx(X, { onClick: () => k("blue"), variant: "secondary", children: "Add" })
            ] }),
            y.map((A, T) => {
              var M;
              return /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-6 gap-2", children: [
                /* @__PURE__ */ s.jsx(
                  je,
                  {
                    className: "col-span-2",
                    value: A.provider,
                    onChange: (N) => S("blue", T, { provider: N.target.value }),
                    children: g.map((N) => /* @__PURE__ */ s.jsx("option", { value: N.provider, children: N.name || N.provider }, N.provider))
                  }
                ),
                /* @__PURE__ */ s.jsx(
                  je,
                  {
                    className: "col-span-3",
                    value: A.model,
                    onChange: (N) => S("blue", T, { model: N.target.value }),
                    children: (((M = g.find((N) => N.provider === A.provider)) == null ? void 0 : M.models) || []).map((N) => /* @__PURE__ */ s.jsx("option", { value: N, children: N }, N))
                  }
                ),
                /* @__PURE__ */ s.jsx(X, { onClick: () => E("blue", T), variant: "secondary", children: "Remove" })
              ] }, `blue-${T}`);
            })
          ] })
        ] }) }),
        /* @__PURE__ */ s.jsx(he, { title: "Execution Controls", children: /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap items-center justify-between gap-4", children: [
          /* @__PURE__ */ s.jsx(So, { status: x }),
          /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap gap-2", children: [
            !l && /* @__PURE__ */ s.jsx(X, { onClick: L, children: "Start Test" }),
            l && x !== "stopped" && /* @__PURE__ */ s.jsx(X, { onClick: () => w(l), variant: "secondary", children: "Stop Test" }),
            /* @__PURE__ */ s.jsx(X, { onClick: P, variant: "secondary", children: "Reset" })
          ] })
        ] }) }),
        /* @__PURE__ */ s.jsxs(he, { title: "Live Logs", children: [
          /* @__PURE__ */ s.jsx(_l, { logs: u, maxHeight: "260px" }),
          j && /* @__PURE__ */ s.jsx("p", { className: "mt-3 text-xs text-slate-400", children: "Fetching adversarial status..." })
        ] })
      ] })
    ] })
  ] });
}
function pU({
  value: e,
  onChange: t,
  language: r = "lean",
  readOnly: n = !1,
  className: a
}) {
  const [i, o] = de(e);
  et(() => {
    o(e);
  }, [e]);
  const l = (c) => {
    o(c), t(c);
  };
  return /* @__PURE__ */ s.jsxs("div", { className: Ie("proof-editor flex flex-col h-full", a), children: [
    !n && /* @__PURE__ */ s.jsxs("div", { className: "flex items-center gap-2 p-2 bg-gray-50 border-b border-gray-200", children: [
      /* @__PURE__ */ s.jsx("span", { className: "text-sm text-gray-600", children: "Lean 4 Editor" }),
      /* @__PURE__ */ s.jsx("div", { className: "flex-1" }),
      /* @__PURE__ */ s.jsx(
        X,
        {
          onClick: () => l(""),
          variant: "ghost",
          className: "px-2 py-1",
          children: "Clear"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          onClick: () => {
            navigator.clipboard.readText().then((c) => l(i + c));
          },
          variant: "ghost",
          className: "px-2 py-1",
          children: "Paste"
        }
      )
    ] }),
    /* @__PURE__ */ s.jsx("div", { className: "flex-1 relative", children: /* @__PURE__ */ s.jsx(
      Lt,
      {
        value: i,
        onChange: (c) => l(c.target.value),
        readOnly: n,
        className: Ie(
          "w-full h-full p-4 font-mono text-sm resize-none",
          "bg-gray-50 border-0 focus:ring-0",
          n && "bg-gray-100"
        ),
        placeholder: "-- Enter Lean 4 code here...",
        spellCheck: !1
      }
    ) }),
    /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between px-4 py-2 bg-gray-50 border-t border-gray-200 text-xs text-gray-600", children: [
      /* @__PURE__ */ s.jsxs("div", { children: [
        "Lines: ",
        i.split(`
`).length,
        " | Characters: ",
        i.length
      ] }),
      /* @__PURE__ */ s.jsx("div", { children: r })
    ] })
  ] });
}
function gU({
  models: e,
  selectedModel: t,
  onModelChange: r,
  className: n
}) {
  const [a, i] = de(!1), o = e.find((c) => c.id === t), l = e.reduce((c, u) => (c[u.provider] || (c[u.provider] = []), c[u.provider].push(u), c), {});
  return /* @__PURE__ */ s.jsxs("div", { className: Ie("model-selector", n), children: [
    /* @__PURE__ */ s.jsx("div", { className: "mb-3", children: /* @__PURE__ */ s.jsx(H, { label: "Select Model", children: /* @__PURE__ */ s.jsx(
      je,
      {
        value: t,
        onChange: (c) => r(c.target.value),
        children: Object.entries(l).map(([c, u]) => /* @__PURE__ */ s.jsx("optgroup", { label: c.toUpperCase(), children: u.map((d) => /* @__PURE__ */ s.jsx("option", { value: d.id, children: d.name }, d.id)) }, c))
      }
    ) }) }),
    /* @__PURE__ */ s.jsxs(
      X,
      {
        onClick: () => i(!a),
        variant: "ghost",
        className: "px-0",
        children: [
          a ? "Hide" : "Show",
          " model details"
        ]
      }
    ),
    a && o && /* @__PURE__ */ s.jsxs("div", { className: "bg-gray-50 border border-gray-200 rounded-lg p-4 space-y-2", children: [
      /* @__PURE__ */ s.jsxs("div", { children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-sm font-medium text-gray-900", children: o.name }),
        /* @__PURE__ */ s.jsx("div", { className: "text-xs text-gray-600", children: o.provider })
      ] }),
      o.description && /* @__PURE__ */ s.jsx("div", { className: "text-sm text-gray-700", children: o.description }),
      o.capabilities.length > 0 && /* @__PURE__ */ s.jsxs("div", { children: [
        /* @__PURE__ */ s.jsx("div", { className: "text-xs font-medium text-gray-700 mb-1", children: "Capabilities" }),
        /* @__PURE__ */ s.jsx("div", { className: "flex flex-wrap gap-1", children: o.capabilities.map((c) => /* @__PURE__ */ s.jsx(Re, { tone: "info", children: c }, c)) })
      ] })
    ] }),
    !a && /* @__PURE__ */ s.jsxs("div", { className: "mt-3 space-y-1", children: [
      /* @__PURE__ */ s.jsx("div", { className: "text-xs text-gray-600 mb-1", children: "Available Models:" }),
      e.map((c) => /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => r(c.id),
          variant: t === c.id ? "secondary" : "ghost",
          className: Ie("w-full text-left justify-start"),
          children: [
            /* @__PURE__ */ s.jsx("div", { className: "font-medium", children: c.name }),
            /* @__PURE__ */ s.jsx("div", { className: "text-xs text-gray-500", children: c.provider })
          ]
        },
        c.id
      ))
    ] })
  ] });
}
function yU({ result: e, className: t }) {
  const [r, n] = de("errors"), a = (o) => {
    switch (o) {
      case "success":
        return "success";
      case "failed":
        return "danger";
      case "running":
        return "info";
      case "pending":
      default:
        return "neutral";
    }
  }, i = [...e.errors, ...e.warnings];
  return /* @__PURE__ */ s.jsx(he, { className: Ie("verification-display", t), title: "Verification Status", children: /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
    /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap items-center justify-between gap-3", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center gap-3", children: [
        /* @__PURE__ */ s.jsx(Re, { tone: a(e.status), children: e.status.toUpperCase() }),
        e.duration && /* @__PURE__ */ s.jsxs("span", { className: "text-sm text-gray-600", children: [
          "Completed in ",
          e.duration,
          "ms"
        ] })
      ] }),
      e.status === "failed" && /* @__PURE__ */ s.jsxs("div", { className: "text-sm text-gray-600", children: [
        e.errors.length,
        " errors, ",
        e.warnings.length,
        " warnings"
      ] })
    ] }),
    /* @__PURE__ */ s.jsx("div", { className: "flex flex-wrap gap-2", children: ["errors", "output", "details"].map((o) => {
      const l = o === "errors" ? i.length : o === "output" ? e.output ? 1 : 0 : 1;
      return /* @__PURE__ */ s.jsxs(
        X,
        {
          onClick: () => n(o),
          disabled: l === 0,
          variant: r === o ? "primary" : "secondary",
          children: [
            o,
            " ",
            l > 0 && `(${l})`
          ]
        },
        o
      );
    }) }),
    /* @__PURE__ */ s.jsxs("div", { children: [
      r === "errors" && /* @__PURE__ */ s.jsx("div", { className: "space-y-2", children: i.length === 0 ? /* @__PURE__ */ s.jsx("div", { className: "text-center py-6 text-gray-500", children: /* @__PURE__ */ s.jsx("p", { className: "text-emerald-600 font-medium", children: "No errors or warnings" }) }) : i.map((o, l) => /* @__PURE__ */ s.jsxs(
        "div",
        {
          className: Ie(
            "flex items-start gap-3 rounded-lg border p-3",
            o.severity === "error" && "border-red-200 bg-red-50",
            o.severity === "warning" && "border-yellow-200 bg-yellow-50",
            o.severity === "info" && "border-blue-200 bg-blue-50"
          ),
          children: [
            /* @__PURE__ */ s.jsx(
              Re,
              {
                tone: o.severity === "error" ? "danger" : o.severity === "warning" ? "warning" : "info",
                children: o.severity.toUpperCase()
              }
            ),
            /* @__PURE__ */ s.jsxs("div", { className: "flex-1", children: [
              /* @__PURE__ */ s.jsx("div", { className: "text-sm font-medium text-gray-900", children: o.message }),
              /* @__PURE__ */ s.jsxs("div", { className: "text-xs text-gray-600 mt-1", children: [
                "Line ",
                o.line,
                ", Column ",
                o.column
              ] })
            ] })
          ]
        },
        l
      )) }),
      r === "output" && /* @__PURE__ */ s.jsx("div", { children: e.output ? /* @__PURE__ */ s.jsx("pre", { className: "bg-gray-50 p-4 rounded-lg text-sm font-mono overflow-x-auto whitespace-pre-wrap", children: e.output }) : /* @__PURE__ */ s.jsx("div", { className: "text-center py-6 text-gray-500", children: "No output available" }) }),
      r === "details" && /* @__PURE__ */ s.jsx("div", { className: "space-y-3", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-2 gap-4 text-sm", children: [
        /* @__PURE__ */ s.jsxs("div", { children: [
          /* @__PURE__ */ s.jsx("div", { className: "font-medium text-gray-700", children: "Status" }),
          /* @__PURE__ */ s.jsx("div", { className: "text-gray-900", children: e.status })
        ] }),
        e.duration && /* @__PURE__ */ s.jsxs("div", { children: [
          /* @__PURE__ */ s.jsx("div", { className: "font-medium text-gray-700", children: "Duration" }),
          /* @__PURE__ */ s.jsxs("div", { className: "text-gray-900", children: [
            e.duration,
            "ms"
          ] })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { children: [
          /* @__PURE__ */ s.jsx("div", { className: "font-medium text-gray-700", children: "Errors" }),
          /* @__PURE__ */ s.jsx("div", { className: "text-gray-900", children: e.errors.length })
        ] }),
        /* @__PURE__ */ s.jsxs("div", { children: [
          /* @__PURE__ */ s.jsx("div", { className: "font-medium text-gray-700", children: "Warnings" }),
          /* @__PURE__ */ s.jsx("div", { className: "text-gray-900", children: e.warnings.length })
        ] })
      ] }) })
    ] })
  ] }) });
}
function vU({ steps: e, className: t }) {
  const r = (i) => {
    switch (i) {
      case "completed":
        return "✓";
      case "failed":
        return "✕";
      case "in_progress":
        return "⟳";
      case "pending":
        return "○";
    }
  }, n = (i) => {
    switch (i) {
      case "completed":
        return "bg-green-100 border-green-500 text-green-700";
      case "failed":
        return "bg-red-100 border-red-500 text-red-700";
      case "in_progress":
        return "bg-blue-100 border-blue-500 text-blue-700 animate-pulse";
      case "pending":
        return "bg-gray-100 border-gray-300 text-gray-500";
    }
  }, a = (i) => {
    if (i === e.length - 1) return "border-gray-300";
    const o = e[i + 1];
    return o.status === "completed" ? "border-green-500" : o.status === "failed" ? "border-red-500" : "border-gray-300";
  };
  return /* @__PURE__ */ s.jsx("div", { className: Ie("progress-tracker", t), children: /* @__PURE__ */ s.jsx("div", { className: "relative", children: e.map((i, o) => /* @__PURE__ */ s.jsxs("div", { className: "relative flex items-start gap-4 pb-8 last:pb-0", children: [
    o < e.length - 1 && /* @__PURE__ */ s.jsx(
      "div",
      {
        className: Ie(
          "absolute left-[15px] top-8 w-0.5 h-full",
          a(o)
        )
      }
    ),
    /* @__PURE__ */ s.jsx("div", { className: Ie(
      "relative z-10 w-8 h-8 rounded-full border-2 flex items-center justify-center text-sm font-medium flex-shrink-0",
      n(i.status)
    ), children: r(i.status) }),
    /* @__PURE__ */ s.jsxs("div", { className: "flex-1 pt-1", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between mb-1", children: [
        /* @__PURE__ */ s.jsx("h4", { className: Ie(
          "text-sm font-medium",
          i.status === "completed" && "text-green-900",
          i.status === "failed" && "text-red-900",
          i.status === "in_progress" && "text-blue-900",
          i.status === "pending" && "text-gray-600"
        ), children: i.label }),
        i.duration && /* @__PURE__ */ s.jsxs("span", { className: "text-xs text-gray-500", children: [
          i.duration,
          "ms"
        ] })
      ] }),
      i.timestamp && /* @__PURE__ */ s.jsx("div", { className: "text-xs text-gray-500 mb-1", children: new Date(i.timestamp).toLocaleString() }),
      i.status === "in_progress" && /* @__PURE__ */ s.jsx("div", { className: "mt-2", children: /* @__PURE__ */ s.jsx("div", { className: "w-full bg-gray-200 rounded-full h-1.5", children: /* @__PURE__ */ s.jsx("div", { className: "bg-blue-600 h-1.5 rounded-full animate-pulse w-1/2" }) }) })
    ] })
  ] }, i.id)) }) });
}
const A_ = [
  { id: "1", label: "Parse Lean code", status: "pending" },
  { id: "2", label: "Type check", status: "pending" },
  { id: "3", label: "Generate proof", status: "pending" },
  { id: "4", label: "Verify proof", status: "pending" }
], bU = [
  {
    id: "lean-4-prover",
    name: "Lean 4 Prover",
    provider: "OpenAI",
    description: "GPT-4 based Lean theorem prover",
    capabilities: ["Type Checking", "Proof Generation", "Tactic Suggestion"]
  },
  {
    id: "claude-lean",
    name: "Claude Lean",
    provider: "Anthropic",
    description: "Claude-based Lean assistant",
    capabilities: ["Code Completion", "Proof Strategy", "Error Analysis"]
  }
];
function Ez() {
  const [e, t] = de(""), [r, n] = de("lean-4-prover"), [a, i] = de(null), [o, l] = de(A_), { models: c, verifyProof: u, isVerifying: d } = s1(), f = Ya(() => !c || c.length === 0 ? bU : c.flatMap(
    (m) => m.models.map((y) => ({
      id: `${m.provider}-${y}`,
      name: y,
      provider: m.provider,
      description: `${m.provider} ${y}`,
      capabilities: ["Proof Generation", "Verification"]
    }))
  ), [c]), p = async () => {
    l(
      A_.map((m) => ({ ...m, status: "in_progress" }))
    ), i({
      status: "running",
      errors: [],
      warnings: [],
      output: ""
    });
    try {
      const m = await u(e);
      i({
        status: m != null && m.success ? "success" : "failed",
        errors: (m == null ? void 0 : m.errors) || [],
        warnings: (m == null ? void 0 : m.warnings) || [],
        output: (m == null ? void 0 : m.output) || "",
        duration: (m == null ? void 0 : m.elapsed_time) || 0
      }), l(
        (y) => y.map((h) => ({ ...h, status: "completed" }))
      );
    } catch (m) {
      i({
        status: "failed",
        errors: [(m == null ? void 0 : m.message) || "Verification failed."],
        warnings: [],
        output: "",
        duration: 0
      }), l(
        (y) => y.map((h, g) => ({
          ...h,
          status: g === y.length - 1 ? "failed" : "completed"
        }))
      );
    }
  };
  return /* @__PURE__ */ s.jsx("div", { className: "leanaide-page flex h-screen bg-slate-50", children: /* @__PURE__ */ s.jsxs("main", { className: "flex-1 flex flex-col overflow-hidden", children: [
    /* @__PURE__ */ s.jsx("header", { className: "bg-white border-b border-slate-200 px-6 py-4", children: /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ s.jsxs("div", { children: [
        /* @__PURE__ */ s.jsx("h1", { className: "text-2xl font-bold text-slate-900", children: "LeanAide - Proof Assistant" }),
        /* @__PURE__ */ s.jsx("p", { className: "text-sm text-slate-600", children: "Generate and verify Lean 4 proofs" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex gap-2", children: [
        /* @__PURE__ */ s.jsx(X, { onClick: () => t(""), variant: "secondary", children: "Clear" }),
        /* @__PURE__ */ s.jsx(
          X,
          {
            onClick: p,
            disabled: d || !e.trim(),
            variant: "primary",
            children: d ? "Verifying..." : "Verify Proof"
          }
        )
      ] })
    ] }) }),
    /* @__PURE__ */ s.jsx("div", { className: "flex-1 overflow-auto p-6", children: /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-1 lg:grid-cols-2 gap-6 h-full", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex flex-col space-y-4", children: [
        /* @__PURE__ */ s.jsx(he, { title: "Model Selection", children: /* @__PURE__ */ s.jsx(
          gU,
          {
            models: f,
            selectedModel: r,
            onModelChange: n
          }
        ) }),
        /* @__PURE__ */ s.jsx(he, { title: "Proof Editor", className: "flex-1 overflow-hidden", children: /* @__PURE__ */ s.jsx(
          pU,
          {
            value: e,
            onChange: t,
            language: "lean",
            className: "h-full"
          }
        ) })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex flex-col space-y-4", children: [
        /* @__PURE__ */ s.jsx(he, { title: "Verification Progress", children: /* @__PURE__ */ s.jsx(vU, { steps: o }) }),
        a && /* @__PURE__ */ s.jsx(yU, { result: a }),
        /* @__PURE__ */ s.jsx(he, { title: "Example Proofs", className: "flex-1 overflow-auto", children: /* @__PURE__ */ s.jsx("div", { className: "space-y-2", children: [
          {
            name: "Basic Proposition",
            code: `theorem test (p : Prop) : p -> p := by
  intro h
  exact h`
          },
          {
            name: "Addition Commutative",
            code: `example : forall a b : Nat, a + b = b + a := by
  intro a b
  exact Nat.add_comm a b`
          }
        ].map((m, y) => /* @__PURE__ */ s.jsxs(
          X,
          {
            onClick: () => t(m.code),
            variant: "ghost",
            className: "w-full text-left justify-start rounded-lg bg-slate-50 px-3 py-2 hover:bg-slate-100",
            children: [
              /* @__PURE__ */ s.jsx("div", { className: "font-medium text-slate-900", children: m.name }),
              /* @__PURE__ */ s.jsx("div", { className: "mt-1 truncate font-mono text-xs text-slate-600", children: m.code })
            ]
          },
          y
        )) }) })
      ] })
    ] }) })
  ] }) });
}
const xU = pl()(
  Yh(
    (e, t) => ({
      artifacts: [],
      selectedArtifact: null,
      graphData: null,
      searchQuery: "",
      searchFilters: {
        query: "",
        tags: []
      },
      searchResults: [],
      isLoading: !1,
      error: null,
      viewMode: "list",
      setArtifacts: (r) => e({ artifacts: r }),
      addArtifact: (r) => e((n) => ({
        artifacts: [r, ...n.artifacts]
      })),
      updateArtifact: (r, n) => e((a) => {
        var i;
        return {
          artifacts: a.artifacts.map(
            (o) => o.artifact_id === r ? { ...o, ...n } : o
          ),
          selectedArtifact: ((i = a.selectedArtifact) == null ? void 0 : i.artifact_id) === r ? { ...a.selectedArtifact, ...n } : a.selectedArtifact
        };
      }),
      removeArtifact: (r) => e((n) => {
        var a;
        return {
          artifacts: n.artifacts.filter((i) => i.artifact_id !== r),
          selectedArtifact: ((a = n.selectedArtifact) == null ? void 0 : a.artifact_id) === r ? null : n.selectedArtifact
        };
      }),
      setSelectedArtifact: (r) => e({ selectedArtifact: r }),
      setGraphData: (r) => e({ graphData: r }),
      setSearchQuery: (r) => e((n) => ({
        searchQuery: r,
        searchFilters: { ...n.searchFilters, query: r }
      })),
      setSearchFilters: (r) => e((n) => ({
        searchFilters: { ...n.searchFilters, ...r }
      })),
      clearSearchFilters: () => e({
        searchFilters: {
          query: "",
          tags: []
        },
        searchQuery: ""
      }),
      setLoading: (r) => e({ isLoading: r }),
      setError: (r) => e({ error: r }),
      setViewMode: (r) => e({ viewMode: r }),
      reset: () => e({
        selectedArtifact: null,
        searchResults: [],
        error: null
      })
    }),
    { name: "KnowledgeStore" }
  )
);
function wU() {
  const [e, t] = de({
    data: null,
    loading: !1,
    error: null,
    progress: 0
  }), r = Tr(null), {
    artifacts: n,
    graphData: a,
    setArtifacts: i,
    setGraphData: o,
    addArtifact: l,
    updateArtifact: c,
    setLoading: u,
    setError: d
  } = xU(), f = ot(async (_) => {
    t((S) => ({ ...S, loading: !0, error: null })), u(!0), d(null);
    try {
      r.current = new AbortController();
      const S = await Se.post(
        "/knowledge/query",
        _
      );
      return t((k) => ({ ...k, data: S.results, loading: !1 })), u(!1), S.results;
    } catch (S) {
      const k = S instanceof Error ? S : new Error(String(S));
      return t((E) => ({ ...E, error: k, loading: !1 })), d(k.message), u(!1), [];
    }
  }, [u, d]), p = ot(async (_) => {
    t((S) => ({ ...S, loading: !0, error: null, progress: 0 })), u(!0), d(null);
    try {
      r.current = new AbortController();
      const S = setInterval(() => {
        t((E) => ({
          ...E,
          progress: Math.min(E.progress + 10, 90)
        }));
      }, 200), k = await Se.post(
        "/knowledge/ingest",
        _
      );
      return clearInterval(S), t((E) => ({
        ...E,
        data: k,
        loading: !1,
        progress: 100
      })), l(k), u(!1), k;
    } catch (S) {
      const k = S instanceof Error ? S : new Error(String(S));
      return t((E) => ({ ...E, error: k, loading: !1, progress: 0 })), d(k.message), u(!1), null;
    }
  }, [l, u, d]), m = ot(async (_) => {
    t((S) => ({ ...S, loading: !0, error: null })), u(!0), d(null);
    try {
      const S = await Se.get(
        "/knowledge/graph",
        _
      );
      return o(S), t((k) => ({ ...k, data: S, loading: !1 })), u(!1), S;
    } catch (S) {
      const k = S instanceof Error ? S : new Error(String(S));
      return t((E) => ({ ...E, error: k, loading: !1 })), d(k.message), u(!1), null;
    }
  }, [o, u, d]), y = ot(async () => {
    t((_) => ({ ..._, loading: !0, error: null })), u(!0), d(null);
    try {
      const _ = await Se.get(
        "/knowledge/artifacts"
      );
      return i(_.artifacts), t((S) => ({ ...S, data: _.artifacts, loading: !1 })), u(!1), _.artifacts;
    } catch (_) {
      const S = _ instanceof Error ? _ : new Error(String(_));
      return t((k) => ({ ...k, error: S, loading: !1 })), d(S.message), u(!1), [];
    }
  }, [i, u, d]), h = ot(async (_) => {
    t((S) => ({ ...S, loading: !0, error: null })), u(!0);
    try {
      const S = await Se.get(
        `/knowledge/artifacts/${_}`
      );
      return t((k) => ({ ...k, data: S, loading: !1 })), u(!1), S;
    } catch (S) {
      const k = S instanceof Error ? S : new Error(String(S));
      return t((E) => ({ ...E, error: k, loading: !1 })), d(k.message), u(!1), null;
    }
  }, [u, d]), g = ot(async (_, S) => {
    t((k) => ({ ...k, loading: !0 })), u(!0);
    try {
      const k = await Se.put(
        `/knowledge/artifacts/${_}`,
        S
      );
      c(_, k), t((E) => ({ ...E, loading: !1 })), u(!1);
    } catch (k) {
      const E = k instanceof Error ? k : new Error(String(k));
      t((L) => ({ ...L, error: E, loading: !1 })), d(E.message), u(!1);
    }
  }, [c, u, d]), v = ot(async (_) => {
    t((S) => ({ ...S, loading: !0 })), u(!0);
    try {
      await Se.delete(`/knowledge/artifacts/${_}`);
      const S = n.filter((k) => k.artifact_id !== _);
      i(S), t((k) => ({ ...k, loading: !1 })), u(!1);
    } catch (S) {
      const k = S instanceof Error ? S : new Error(String(S));
      t((E) => ({ ...E, error: k, loading: !1 })), d(k.message), u(!1);
    }
  }, [n, i, u, d]), b = ot(async (_) => {
    try {
      return (await Se.get(
        `/knowledge/artifacts/${_}/relationships`
      )).relationships;
    } catch (S) {
      const k = S instanceof Error ? S : new Error(String(S));
      return d(k.message), [];
    }
  }, [d]), w = ot(async (_, S = 10) => {
    t((k) => ({ ...k, loading: !0 })), u(!0);
    try {
      const k = await Se.post(
        "/knowledge/semantic-search",
        { query: _, limit: S }
      );
      return t((E) => ({ ...E, data: k.results, loading: !1 })), u(!1), k.results;
    } catch (k) {
      const E = k instanceof Error ? k : new Error(String(k));
      return t((L) => ({ ...L, error: E, loading: !1 })), d(E.message), u(!1), [];
    }
  }, [u, d]), j = ot(() => {
    var _;
    (_ = r.current) == null || _.abort(), t((S) => ({ ...S, loading: !1, progress: 0 })), u(!1);
  }, [u]), x = ot(() => {
    t({
      data: null,
      loading: !1,
      error: null,
      progress: 0
    }), d(null);
  }, [d]);
  return et(() => () => {
    var _;
    (_ = r.current) == null || _.abort();
  }, []), {
    ...e,
    artifacts: n,
    graphData: a,
    query: f,
    ingest: p,
    getGraph: m,
    getArtifacts: y,
    getArtifact: h,
    updateArtifact: g,
    deleteArtifact: v,
    getRelationships: b,
    semanticSearch: w,
    cancel: j,
    reset: x
  };
}
function _U({
  artifacts: e,
  onArtifactSelect: t,
  onArtifactDelete: r,
  className: n
}) {
  const [a, i] = de(""), [o, l] = de("all"), [c, u] = de("all"), d = Array.from(new Set(e.map((m) => m.type))), f = Array.from(new Set(e.flatMap((m) => m.tags))), p = e.filter((m) => {
    var v;
    const y = m.name.toLowerCase().includes(a.toLowerCase()) || ((v = m.description) == null ? void 0 : v.toLowerCase().includes(a.toLowerCase())), h = o === "all" || m.type === o, g = c === "all" || m.tags.includes(c);
    return y && h && g;
  });
  return /* @__PURE__ */ s.jsxs("div", { className: Ie("artifact-list", n), children: [
    /* @__PURE__ */ s.jsxs("div", { className: "mb-6 space-y-4", children: [
      /* @__PURE__ */ s.jsx(
        D,
        {
          type: "text",
          value: a,
          onChange: (m) => i(m.target.value),
          placeholder: "Search artifacts..."
        }
      ),
      /* @__PURE__ */ s.jsxs("div", { className: "grid gap-4 md:grid-cols-2", children: [
        /* @__PURE__ */ s.jsx(H, { label: "Type", children: /* @__PURE__ */ s.jsxs(
          je,
          {
            value: o,
            onChange: (m) => l(m.target.value),
            children: [
              /* @__PURE__ */ s.jsx("option", { value: "all", children: "All Types" }),
              d.map((m) => /* @__PURE__ */ s.jsx("option", { value: m, children: m }, m))
            ]
          }
        ) }),
        /* @__PURE__ */ s.jsx(H, { label: "Tag", children: /* @__PURE__ */ s.jsxs(
          je,
          {
            value: c,
            onChange: (m) => u(m.target.value),
            children: [
              /* @__PURE__ */ s.jsx("option", { value: "all", children: "All Tags" }),
              f.map((m) => /* @__PURE__ */ s.jsx("option", { value: m, children: m }, m))
            ]
          }
        ) })
      ] })
    ] }),
    /* @__PURE__ */ s.jsx("div", { className: "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4", children: p.map((m) => /* @__PURE__ */ s.jsxs(
      "div",
      {
        className: "bg-white border border-gray-200 rounded-lg p-4 hover:shadow-lg transition-all cursor-pointer",
        onClick: () => t == null ? void 0 : t(m),
        children: [
          /* @__PURE__ */ s.jsxs("div", { className: "flex items-start justify-between mb-2", children: [
            /* @__PURE__ */ s.jsx("h3", { className: "text-lg font-semibold text-gray-900", children: m.name }),
            /* @__PURE__ */ s.jsx(
              X,
              {
                onClick: (y) => {
                  y.stopPropagation(), r == null || r(m.id);
                },
                variant: "ghost",
                className: "px-2 py-1 text-red-600",
                children: "Delete"
              }
            )
          ] }),
          m.description && /* @__PURE__ */ s.jsx("p", { className: "text-sm text-gray-600 mb-3 line-clamp-2", children: m.description }),
          /* @__PURE__ */ s.jsxs("div", { className: "flex items-center gap-2 mb-2", children: [
            /* @__PURE__ */ s.jsx(Re, { tone: "info", children: m.type }),
            /* @__PURE__ */ s.jsxs("span", { className: "text-xs text-gray-500", children: [
              "v",
              m.version
            ] })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap gap-1 mb-3", children: [
            m.tags.slice(0, 3).map((y) => /* @__PURE__ */ s.jsx(Re, { tone: "neutral", children: y }, y)),
            m.tags.length > 3 && /* @__PURE__ */ s.jsxs("span", { className: "text-xs text-gray-500", children: [
              "+",
              m.tags.length - 3
            ] })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { className: "text-xs text-gray-500", children: [
            "Updated ",
            new Date(m.updated).toLocaleDateString()
          ] })
        ]
      },
      m.id
    )) }),
    p.length === 0 && /* @__PURE__ */ s.jsx("div", { className: "text-center py-12 text-gray-500", children: /* @__PURE__ */ s.jsx("p", { children: "No artifacts found" }) })
  ] });
}
function jU({ onSearch: e, onResultClick: t, className: r }) {
  const [n, a] = de(""), [i, o] = de([]), [l, c] = de(!1), [u, d] = de(!1), f = async () => {
    if (n.trim()) {
      c(!0);
      try {
        const m = await e(n);
        o(m);
      } catch (m) {
        console.error("Search failed:", m);
      } finally {
        c(!1);
      }
    }
  }, p = (m) => {
    m.key === "Enter" && f();
  };
  return /* @__PURE__ */ s.jsxs("div", { className: Ie("knowledge-search", r), children: [
    /* @__PURE__ */ s.jsxs("div", { className: "flex gap-2 mb-4", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex-1 relative", children: [
        /* @__PURE__ */ s.jsx(
          D,
          {
            type: "text",
            value: n,
            onChange: (m) => a(m.target.value),
            onKeyPress: p,
            placeholder: "Search knowledge base...",
            className: "pl-10"
          }
        ),
        /* @__PURE__ */ s.jsx(
          "svg",
          {
            className: "absolute left-3 top-2.5 h-5 w-5 text-gray-400",
            fill: "none",
            viewBox: "0 0 24 24",
            stroke: "currentColor",
            children: /* @__PURE__ */ s.jsx(
              "path",
              {
                strokeLinecap: "round",
                strokeLinejoin: "round",
                strokeWidth: 2,
                d: "M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"
              }
            )
          }
        )
      ] }),
      /* @__PURE__ */ s.jsx(
        X,
        {
          onClick: f,
          disabled: l || !n.trim(),
          children: l ? "Searching..." : "Search"
        }
      ),
      /* @__PURE__ */ s.jsx(
        X,
        {
          onClick: () => d(!u),
          variant: "secondary",
          children: "Advanced"
        }
      )
    ] }),
    u && /* @__PURE__ */ s.jsxs("div", { className: "mb-4 p-4 bg-gray-50 rounded-lg border border-gray-200", children: [
      /* @__PURE__ */ s.jsx("h3", { className: "text-sm font-medium text-gray-700 mb-2", children: "Filters" }),
      /* @__PURE__ */ s.jsxs("div", { className: "grid grid-cols-2 gap-4", children: [
        /* @__PURE__ */ s.jsx(H, { label: "Content Types", children: /* @__PURE__ */ s.jsxs(je, { children: [
          /* @__PURE__ */ s.jsx("option", { children: "All Types" }),
          /* @__PURE__ */ s.jsx("option", { children: "Evolutions" }),
          /* @__PURE__ */ s.jsx("option", { children: "Artifacts" }),
          /* @__PURE__ */ s.jsx("option", { children: "Workflows" })
        ] }) }),
        /* @__PURE__ */ s.jsx(H, { label: "Date Range", children: /* @__PURE__ */ s.jsxs(je, { children: [
          /* @__PURE__ */ s.jsx("option", { children: "All Time" }),
          /* @__PURE__ */ s.jsx("option", { children: "Last 7 Days" }),
          /* @__PURE__ */ s.jsx("option", { children: "Last 30 Days" }),
          /* @__PURE__ */ s.jsx("option", { children: "Last Year" })
        ] }) })
      ] })
    ] }),
    i.length > 0 && /* @__PURE__ */ s.jsxs("div", { className: "space-y-3", children: [
      /* @__PURE__ */ s.jsxs("p", { className: "text-sm text-gray-600", children: [
        "Found ",
        i.length,
        " results"
      ] }),
      i.map((m) => /* @__PURE__ */ s.jsxs(
        "div",
        {
          onClick: () => t == null ? void 0 : t(m),
          className: "bg-white border border-gray-200 rounded-lg p-4 hover:shadow-md transition-shadow cursor-pointer",
          children: [
            /* @__PURE__ */ s.jsxs("div", { className: "flex items-start justify-between mb-2", children: [
              /* @__PURE__ */ s.jsx("h4", { className: "text-lg font-semibold text-gray-900", children: m.title }),
              /* @__PURE__ */ s.jsx(Re, { tone: "info", children: m.type })
            ] }),
            /* @__PURE__ */ s.jsx("p", { className: "text-sm text-gray-600 mb-2", children: m.snippet }),
            /* @__PURE__ */ s.jsx("div", { className: "flex items-center gap-2 text-xs text-gray-500", children: /* @__PURE__ */ s.jsxs("span", { children: [
              "Relevance: ",
              (m.score * 100).toFixed(0),
              "%"
            ] }) })
          ]
        },
        m.id
      ))
    ] }),
    n && i.length === 0 && !l && /* @__PURE__ */ s.jsx("div", { className: "text-center py-12 text-gray-500", children: /* @__PURE__ */ s.jsxs("p", { children: [
      'No results found for "',
      n,
      '"'
    ] }) })
  ] });
}
function SU({
  artifact: e,
  onSave: t,
  onCancel: r,
  types: n,
  className: a
}) {
  const [i, o] = de(
    e || {
      name: "",
      type: n[0] || "",
      description: "",
      content: "",
      tags: []
    }
  ), [l, c] = de(""), [u, d] = de(!1), [f, p] = de(null);
  et(() => {
    e && o(e);
  }, [e]);
  const m = (v) => {
    const { name: b, value: w } = v.target;
    o((j) => ({ ...j, [b]: w }));
  }, y = () => {
    l.trim() && !i.tags.includes(l.trim()) && (o((v) => ({
      ...v,
      tags: [...v.tags, l.trim()]
    })), c(""));
  }, h = (v) => {
    o((b) => ({
      ...b,
      tags: b.tags.filter((w) => w !== v)
    }));
  }, g = async (v) => {
    v.preventDefault(), d(!0), p(null);
    try {
      await t(i);
    } catch (b) {
      p(b instanceof Error ? b.message : "Failed to save artifact");
    } finally {
      d(!1);
    }
  };
  return /* @__PURE__ */ s.jsxs("form", { onSubmit: g, className: Ie("artifact-editor space-y-4", a), children: [
    f && /* @__PURE__ */ s.jsx("div", { className: "bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg", children: f }),
    /* @__PURE__ */ s.jsx(H, { label: "Name *", children: /* @__PURE__ */ s.jsx(
      D,
      {
        type: "text",
        name: "name",
        value: i.name,
        onChange: m,
        required: !0
      }
    ) }),
    /* @__PURE__ */ s.jsx(H, { label: "Type *", children: /* @__PURE__ */ s.jsx(
      je,
      {
        name: "type",
        value: i.type,
        onChange: m,
        required: !0,
        children: n.map((v) => /* @__PURE__ */ s.jsx("option", { value: v, children: v }, v))
      }
    ) }),
    /* @__PURE__ */ s.jsx(H, { label: "Description", children: /* @__PURE__ */ s.jsx(
      Lt,
      {
        name: "description",
        value: i.description,
        onChange: m,
        rows: 3
      }
    ) }),
    /* @__PURE__ */ s.jsx(H, { label: "Content *", children: /* @__PURE__ */ s.jsx(
      Lt,
      {
        name: "content",
        value: i.content,
        onChange: m,
        rows: 10,
        required: !0,
        className: "font-mono text-sm"
      }
    ) }),
    /* @__PURE__ */ s.jsxs(H, { label: "Tags", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex gap-2 mb-2", children: [
        /* @__PURE__ */ s.jsx(
          D,
          {
            type: "text",
            value: l,
            onChange: (v) => c(v.target.value),
            onKeyPress: (v) => v.key === "Enter" && (v.preventDefault(), y()),
            placeholder: "Add a tag...",
            className: "flex-1"
          }
        ),
        /* @__PURE__ */ s.jsx(X, { type: "button", onClick: y, children: "Add" })
      ] }),
      /* @__PURE__ */ s.jsx("div", { className: "flex flex-wrap gap-2", children: i.tags.map((v) => /* @__PURE__ */ s.jsxs("div", { className: "inline-flex items-center gap-2", children: [
        /* @__PURE__ */ s.jsx(Re, { tone: "neutral", children: v }),
        /* @__PURE__ */ s.jsx(
          X,
          {
            type: "button",
            onClick: () => h(v),
            variant: "ghost",
            className: "px-2 py-1",
            children: "Remove"
          }
        )
      ] }, v)) })
    ] }),
    /* @__PURE__ */ s.jsxs("div", { className: "flex gap-2 pt-4", children: [
      /* @__PURE__ */ s.jsx(X, { type: "submit", disabled: u, className: "flex-1", children: u ? "Saving..." : "Save" }),
      r && /* @__PURE__ */ s.jsx(X, { type: "button", onClick: r, variant: "secondary", children: "Cancel" })
    ] })
  ] });
}
function kU({
  artifact: e,
  onEdit: t,
  onDelete: r,
  onVersionRestore: n,
  className: a
}) {
  const [i, o] = de("content"), [l, c] = de(null), u = e.versions.find(
    (d) => d.version === (l || e.current_version)
  );
  return /* @__PURE__ */ s.jsxs("div", { className: Ie("artifact-detail", a), children: [
    /* @__PURE__ */ s.jsxs(
      he,
      {
        title: e.name,
        description: e.description,
        actions: /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap gap-2", children: [
          t && /* @__PURE__ */ s.jsx(X, { onClick: t, children: "Edit" }),
          r && /* @__PURE__ */ s.jsx(X, { onClick: r, variant: "secondary", children: "Delete" })
        ] }),
        children: [
          /* @__PURE__ */ s.jsxs("div", { className: "flex flex-wrap gap-4 text-sm text-gray-600", children: [
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("span", { className: "font-medium", children: "Type:" }),
              " ",
              e.type
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("span", { className: "font-medium", children: "Version:" }),
              " ",
              e.current_version
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("span", { className: "font-medium", children: "Created:" }),
              " ",
              new Date(e.created).toLocaleString()
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("span", { className: "font-medium", children: "Updated:" }),
              " ",
              new Date(e.updated).toLocaleString()
            ] }),
            /* @__PURE__ */ s.jsxs("div", { children: [
              /* @__PURE__ */ s.jsx("span", { className: "font-medium", children: "By:" }),
              " ",
              e.created_by
            ] })
          ] }),
          /* @__PURE__ */ s.jsx("div", { className: "flex flex-wrap gap-2 mt-3", children: e.tags.map((d) => /* @__PURE__ */ s.jsx(Re, { tone: "neutral", children: d }, d)) })
        ]
      }
    ),
    /* @__PURE__ */ s.jsx("div", { className: "mt-4 flex flex-wrap gap-2", children: ["content", "versions", "metadata"].map((d) => /* @__PURE__ */ s.jsx(
      X,
      {
        onClick: () => o(d),
        variant: i === d ? "primary" : "secondary",
        children: d
      },
      d
    )) }),
    /* @__PURE__ */ s.jsxs("div", { className: "mt-4 rounded-lg border border-gray-200 bg-white p-6", children: [
      i === "content" && /* @__PURE__ */ s.jsxs("div", { children: [
        l && /* @__PURE__ */ s.jsxs("div", { className: "mb-4 bg-yellow-50 border border-yellow-200 text-yellow-800 px-4 py-2 rounded-lg flex items-center justify-between", children: [
          /* @__PURE__ */ s.jsxs("span", { children: [
            "Viewing version ",
            l
          ] }),
          /* @__PURE__ */ s.jsx(X, { onClick: () => c(null), variant: "ghost", children: "Back to latest" })
        ] }),
        /* @__PURE__ */ s.jsx("pre", { className: "bg-gray-50 p-4 rounded-lg overflow-x-auto text-sm font-mono whitespace-pre-wrap", children: (u == null ? void 0 : u.content) || e.content })
      ] }),
      i === "versions" && /* @__PURE__ */ s.jsxs("div", { className: "space-y-3", children: [
        /* @__PURE__ */ s.jsx("h3", { className: "text-lg font-semibold text-gray-900", children: "Version History" }),
        e.versions.map((d) => /* @__PURE__ */ s.jsxs(
          "div",
          {
            className: "border border-gray-200 rounded-lg p-4 hover:bg-gray-50",
            children: [
              /* @__PURE__ */ s.jsxs("div", { className: "flex items-start justify-between mb-2", children: [
                /* @__PURE__ */ s.jsxs("div", { children: [
                  /* @__PURE__ */ s.jsxs("div", { className: "font-medium text-gray-900", children: [
                    "Version ",
                    d.version
                  ] }),
                  /* @__PURE__ */ s.jsxs("div", { className: "text-sm text-gray-600", children: [
                    new Date(d.created).toLocaleString(),
                    " by ",
                    d.created_by
                  ] }),
                  d.comment && /* @__PURE__ */ s.jsx("div", { className: "text-sm text-gray-500 mt-1", children: d.comment })
                ] }),
                d.version !== e.current_version && /* @__PURE__ */ s.jsx(
                  X,
                  {
                    onClick: () => c(d.version),
                    variant: "ghost",
                    children: "View"
                  }
                )
              ] }),
              n && d.version !== e.current_version && /* @__PURE__ */ s.jsx(X, { onClick: () => n(d.version), variant: "secondary", children: "Restore" })
            ]
          },
          d.version
        ))
      ] }),
      i === "metadata" && /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
        /* @__PURE__ */ s.jsx("h3", { className: "text-lg font-semibold text-gray-900", children: "Metadata" }),
        /* @__PURE__ */ s.jsxs("dl", { className: "grid grid-cols-2 gap-4", children: [
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-600", children: "ID" }),
            /* @__PURE__ */ s.jsx("dd", { className: "text-sm text-gray-900 font-mono", children: e.id })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-600", children: "Type" }),
            /* @__PURE__ */ s.jsx("dd", { className: "text-sm text-gray-900", children: e.type })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-600", children: "Current Version" }),
            /* @__PURE__ */ s.jsx("dd", { className: "text-sm text-gray-900", children: e.current_version })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-600", children: "Total Versions" }),
            /* @__PURE__ */ s.jsx("dd", { className: "text-sm text-gray-900", children: e.versions.length })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-600", children: "Created" }),
            /* @__PURE__ */ s.jsx("dd", { className: "text-sm text-gray-900", children: new Date(e.created).toLocaleString() })
          ] }),
          /* @__PURE__ */ s.jsxs("div", { children: [
            /* @__PURE__ */ s.jsx("dt", { className: "text-sm font-medium text-gray-600", children: "Updated" }),
            /* @__PURE__ */ s.jsx("dd", { className: "text-sm text-gray-900", children: new Date(e.updated).toLocaleString() })
          ] })
        ] })
      ] })
    ] })
  ] });
}
function Az() {
  const [e, t] = de("list"), [r, n] = de(null), { artifacts: a, loading: i, deleteArtifact: o, semanticSearch: l, ingest: c } = wU(), u = async (m) => (await l(m)).map((h) => {
    var g;
    return {
      id: h.artifact_id || h.id,
      title: h.title || h.name,
      snippet: ((g = h.content) == null ? void 0 : g.slice(0, 100)) || "",
      type: h.type || "unknown",
      score: h.score || 1
    };
  }), d = async (m) => {
    await c(m), t("list");
  }, f = async (m) => {
    confirm("Are you sure you want to delete this artifact?") && (await o(m), (r == null ? void 0 : r.id) === m && (n(null), t("list")));
  }, p = ["Evolution", "Workflow", "Proof", "Model", "Dataset"];
  return /* @__PURE__ */ s.jsxs("div", { className: "knowledge-base-page min-h-screen bg-slate-50", children: [
    /* @__PURE__ */ s.jsx("header", { className: "bg-white border-b border-slate-200 px-6 py-4", children: /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ s.jsxs("div", { children: [
        /* @__PURE__ */ s.jsx("h1", { className: "text-2xl font-bold text-slate-900", children: "Knowledge Base" }),
        /* @__PURE__ */ s.jsx("p", { className: "text-sm text-slate-600", children: "Manage your artifacts and knowledge" })
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "flex gap-2", children: [
        /* @__PURE__ */ s.jsx(
          X,
          {
            onClick: () => t("search"),
            variant: e === "search" ? "primary" : "secondary",
            children: "Search"
          }
        ),
        /* @__PURE__ */ s.jsx(
          X,
          {
            onClick: () => t("list"),
            variant: e === "list" ? "primary" : "secondary",
            children: "Browse"
          }
        ),
        /* @__PURE__ */ s.jsx(
          X,
          {
            onClick: () => {
              n(null), t("create");
            },
            variant: "primary",
            children: "+ New Artifact"
          }
        )
      ] })
    ] }) }),
    /* @__PURE__ */ s.jsxs("div", { className: "p-6", children: [
      e === "list" && /* @__PURE__ */ s.jsx(
        _U,
        {
          artifacts: a,
          onArtifactSelect: (m) => {
            n(m), t("detail");
          },
          onArtifactDelete: f
        }
      ),
      e === "search" && /* @__PURE__ */ s.jsx(
        jU,
        {
          onSearch: u,
          onResultClick: (m) => {
            const y = a.find((h) => h.artifact_id === m.id);
            y && (n(y), t("detail"));
          }
        }
      ),
      e === "create" && /* @__PURE__ */ s.jsxs("div", { className: "max-w-3xl mx-auto space-y-4", children: [
        /* @__PURE__ */ s.jsx(X, { onClick: () => t("list"), variant: "ghost", children: "<- Back to list" }),
        /* @__PURE__ */ s.jsx(he, { title: "Create New Artifact", children: /* @__PURE__ */ s.jsx(
          SU,
          {
            onSave: d,
            onCancel: () => t("list"),
            types: p
          }
        ) })
      ] }),
      e === "detail" && r && /* @__PURE__ */ s.jsxs("div", { className: "max-w-4xl mx-auto space-y-4", children: [
        /* @__PURE__ */ s.jsx(X, { onClick: () => t("list"), variant: "ghost", children: "<- Back to list" }),
        /* @__PURE__ */ s.jsx(
          kU,
          {
            artifact: r,
            onEdit: () => t("create"),
            onDelete: () => f(r.id)
          }
        )
      ] }),
      i && /* @__PURE__ */ s.jsxs("div", { className: "text-center py-12", children: [
        /* @__PURE__ */ s.jsx("div", { className: "inline-block animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600" }),
        /* @__PURE__ */ s.jsx("p", { className: "mt-4 text-gray-600", children: "Loading artifacts..." })
      ] })
    ] })
  ] });
}
function Cz({ className: e }) {
  const { config: t, updateConfig: r, reset: n } = bl(), { models: a, add: i, remove: o } = xl(), [l, c] = de(!1), u = ["openai", "anthropic", "cohere", "huggingface", "local"];
  return /* @__PURE__ */ s.jsx("aside", { className: Ie("w-80 bg-white border-r border-gray-200 overflow-y-auto", e), children: /* @__PURE__ */ s.jsxs("div", { className: "p-6", children: [
    /* @__PURE__ */ s.jsx("h2", { className: "text-lg font-semibold text-gray-900 mb-4", children: "Configuration" }),
    /* @__PURE__ */ s.jsx("div", { className: "mb-6", children: /* @__PURE__ */ s.jsx(H, { label: "Provider", children: /* @__PURE__ */ s.jsxs(
      je,
      {
        value: t.mode,
        onChange: (d) => r({ mode: d.target.value }),
        children: [
          /* @__PURE__ */ s.jsx("option", { value: "standard", children: "Standard Mode" }),
          /* @__PURE__ */ s.jsx("option", { value: "quality_diversity", children: "Quality + Diversity" }),
          /* @__PURE__ */ s.jsx("option", { value: "island_model", children: "Island Model" })
        ]
      }
    ) }) }),
    /* @__PURE__ */ s.jsxs("div", { className: "mb-6", children: [
      /* @__PURE__ */ s.jsx("h3", { className: "text-sm font-medium text-gray-700 mb-2", children: "Models" }),
      a.map((d, f) => /* @__PURE__ */ s.jsxs("div", { className: "flex gap-2 mb-2", children: [
        /* @__PURE__ */ s.jsx(
          je,
          {
            value: d.provider,
            onChange: (p) => {
              const m = [...a];
              m[f] = { ...d, provider: p.target.value }, i(m[f]);
            },
            className: "flex-1 text-sm",
            children: u.map((p) => /* @__PURE__ */ s.jsx("option", { value: p, children: p.charAt(0).toUpperCase() + p.slice(1) }, p))
          }
        ),
        /* @__PURE__ */ s.jsx(
          X,
          {
            onClick: () => o(f),
            variant: "ghost",
            className: "px-3 py-2 text-red-600",
            children: "Remove"
          }
        )
      ] }, f)),
      /* @__PURE__ */ s.jsx(
        X,
        {
          onClick: () => i({ provider: "openai", model: "gpt-4" }),
          variant: "secondary",
          className: "w-full text-sm",
          children: "Add model"
        }
      )
    ] }),
    /* @__PURE__ */ s.jsxs("div", { className: "mb-6", children: [
      /* @__PURE__ */ s.jsxs("div", { className: "flex items-center justify-between mb-2", children: [
        /* @__PURE__ */ s.jsx("h3", { className: "text-sm font-medium text-gray-700", children: "Parameters" }),
        /* @__PURE__ */ s.jsx(
          X,
          {
            onClick: () => c(!l),
            variant: "ghost",
            className: "px-2 py-1 text-xs",
            children: l ? "Less" : "Advanced"
          }
        )
      ] }),
      /* @__PURE__ */ s.jsxs("div", { className: "space-y-4", children: [
        /* @__PURE__ */ s.jsx(H, { label: "Max Iterations", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            value: t.max_iterations,
            onChange: (d) => r({ max_iterations: parseInt(d.target.value) })
          }
        ) }),
        /* @__PURE__ */ s.jsx(H, { label: "Population Size", children: /* @__PURE__ */ s.jsx(
          D,
          {
            type: "number",
            value: t.population_size,
            onChange: (d) => r({ population_size: parseInt(d.target.value) })
          }
        ) }),
        l && /* @__PURE__ */ s.jsxs(s.Fragment, { children: [
          /* @__PURE__ */ s.jsx(H, { label: "Temperature", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              step: "0.1",
              min: "0",
              max: "2",
              value: t.temperature,
              onChange: (d) => r({ temperature: parseFloat(d.target.value) })
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Top P", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              step: "0.1",
              min: "0",
              max: "1",
              value: t.top_p,
              onChange: (d) => r({ top_p: parseFloat(d.target.value) })
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Mutation Rate", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              step: "0.1",
              min: "0",
              max: "1",
              value: t.mutation_rate,
              onChange: (d) => r({ mutation_rate: parseFloat(d.target.value) })
            }
          ) }),
          /* @__PURE__ */ s.jsx(H, { label: "Crossover Rate", children: /* @__PURE__ */ s.jsx(
            D,
            {
              type: "number",
              step: "0.1",
              min: "0",
              max: "1",
              value: t.crossover_rate,
              onChange: (d) => r({ crossover_rate: parseFloat(d.target.value) })
            }
          ) })
        ] })
      ] })
    ] }),
    /* @__PURE__ */ s.jsx("div", { className: "space-y-2", children: /* @__PURE__ */ s.jsx(X, { onClick: n, variant: "secondary", className: "w-full text-sm", children: "Reset to Defaults" }) })
  ] }) });
}
var $o = (e) => e.type === "checkbox", Fn = (e) => e instanceof Date, Gt = (e) => e == null;
const NE = (e) => typeof e == "object";
var _t = (e) => !Gt(e) && !Array.isArray(e) && NE(e) && !Fn(e), EU = (e) => _t(e) && e.target ? $o(e.target) ? e.target.checked : e.target.value : e, AU = (e) => e.substring(0, e.search(/\.\d+(\.|$)/)) || e, CU = (e, t) => e.has(AU(t)), TU = (e) => {
  const t = e.constructor && e.constructor.prototype;
  return _t(t) && t.hasOwnProperty("isPrototypeOf");
}, ag = typeof window < "u" && typeof window.HTMLElement < "u" && typeof document < "u";
function Ct(e) {
  if (e instanceof Date)
    return new Date(e);
  const t = typeof FileList < "u" && e instanceof FileList;
  if (ag && (e instanceof Blob || t))
    return e;
  const r = Array.isArray(e);
  if (!r && !(_t(e) && TU(e)))
    return e;
  const n = r ? [] : Object.create(Object.getPrototypeOf(e));
  for (const a in e)
    Object.prototype.hasOwnProperty.call(e, a) && (n[a] = Ct(e[a]));
  return n;
}
var ec = (e) => /^\w*$/.test(e), dt = (e) => e === void 0, ig = (e) => Array.isArray(e) ? e.filter(Boolean) : [], og = (e) => ig(e.replace(/["|']|\]/g, "").split(/\.|\[/)), we = (e, t, r) => {
  if (!t || !_t(e))
    return r;
  const n = (ec(t) ? [t] : og(t)).reduce((a, i) => Gt(a) ? a : a[i], e);
  return dt(n) || n === e ? dt(e[t]) ? r : e[t] : n;
}, Nr = (e) => typeof e == "boolean", kr = (e) => typeof e == "function", nt = (e, t, r) => {
  let n = -1;
  const a = ec(t) ? [t] : og(t), i = a.length, o = i - 1;
  for (; ++n < i; ) {
    const l = a[n];
    let c = r;
    if (n !== o) {
      const u = e[l];
      c = _t(u) || Array.isArray(u) ? u : isNaN(+a[n + 1]) ? {} : [];
    }
    if (l === "__proto__" || l === "constructor" || l === "prototype")
      return;
    e[l] = c, e = e[l];
  }
};
const C_ = {
  BLUR: "blur",
  FOCUS_OUT: "focusout"
}, Er = {
  onBlur: "onBlur",
  onChange: "onChange",
  onSubmit: "onSubmit",
  onTouched: "onTouched",
  all: "all"
}, zr = {
  max: "max",
  min: "min",
  maxLength: "maxLength",
  minLength: "minLength",
  pattern: "pattern",
  required: "required",
  validate: "validate"
}, PU = I.createContext(null);
PU.displayName = "HookFormContext";
var OU = (e, t, r, n = !0) => {
  const a = {
    defaultValues: t._defaultValues
  };
  for (const i in e)
    Object.defineProperty(a, i, {
      get: () => {
        const o = i;
        return t._proxyFormState[o] !== Er.all && (t._proxyFormState[o] = !n || Er.all), e[o];
      }
    });
  return a;
};
const NU = typeof window < "u" ? I.useLayoutEffect : I.useEffect;
var ar = (e) => typeof e == "string", MU = (e, t, r, n, a) => ar(e) ? (n && t.watch.add(e), we(r, e, a)) : Array.isArray(e) ? e.map((i) => (n && t.watch.add(i), we(r, i))) : (n && (t.watchAll = !0), r), Wh = (e) => Gt(e) || !NE(e);
function gn(e, t, r = /* @__PURE__ */ new WeakSet()) {
  if (Wh(e) || Wh(t))
    return Object.is(e, t);
  if (Fn(e) && Fn(t))
    return Object.is(e.getTime(), t.getTime());
  const n = Object.keys(e), a = Object.keys(t);
  if (n.length !== a.length)
    return !1;
  if (r.has(e) || r.has(t))
    return !0;
  r.add(e), r.add(t);
  for (const i of n) {
    const o = e[i];
    if (!a.includes(i))
      return !1;
    if (i !== "ref") {
      const l = t[i];
      if (Fn(o) && Fn(l) || _t(o) && _t(l) || Array.isArray(o) && Array.isArray(l) ? !gn(o, l, r) : !Object.is(o, l))
        return !1;
    }
  }
  return !0;
}
var ME = (e, t, r, n, a) => t ? {
  ...r[e],
  types: {
    ...r[e] && r[e].types ? r[e].types : {},
    [n]: a || !0
  }
} : {}, $i = (e) => Array.isArray(e) ? e : [e], T_ = () => {
  let e = [];
  return {
    get observers() {
      return e;
    },
    next: (a) => {
      for (const i of e)
        i.next && i.next(a);
    },
    subscribe: (a) => (e.push(a), {
      unsubscribe: () => {
        e = e.filter((i) => i !== a);
      }
    }),
    unsubscribe: () => {
      e = [];
    }
  };
};
function RE(e, t) {
  const r = {};
  for (const n in e)
    if (e.hasOwnProperty(n)) {
      const a = e[n], i = t[n];
      if (a && _t(a) && i) {
        const o = RE(a, i);
        _t(o) && (r[n] = o);
      } else e[n] && (r[n] = i);
    }
  return r;
}
var Bt = (e) => _t(e) && !Object.keys(e).length, sg = (e) => e.type === "file", ml = (e) => {
  if (!ag)
    return !1;
  const t = e ? e.ownerDocument : 0;
  return e instanceof (t && t.defaultView ? t.defaultView.HTMLElement : HTMLElement);
}, IE = (e) => e.type === "select-multiple", lg = (e) => e.type === "radio", RU = (e) => lg(e) || $o(e), ym = (e) => ml(e) && e.isConnected;
function IU(e, t) {
  const r = t.slice(0, -1).length;
  let n = 0;
  for (; n < r; )
    e = dt(e) ? n++ : e[t[n++]];
  return e;
}
function DU(e) {
  for (const t in e)
    if (e.hasOwnProperty(t) && !dt(e[t]))
      return !1;
  return !0;
}
function bt(e, t) {
  const r = Array.isArray(t) ? t : ec(t) ? [t] : og(t), n = r.length === 1 ? e : IU(e, r), a = r.length - 1, i = r[a];
  return n && delete n[i], a !== 0 && (_t(n) && Bt(n) || Array.isArray(n) && DU(n)) && bt(e, r.slice(0, -1)), e;
}
var $U = (e) => {
  for (const t in e)
    if (kr(e[t]))
      return !0;
  return !1;
};
function DE(e) {
  return Array.isArray(e) || _t(e) && !$U(e);
}
function Vh(e, t = {}) {
  for (const r in e) {
    const n = e[r];
    DE(n) ? (t[r] = Array.isArray(n) ? [] : {}, Vh(n, t[r])) : dt(n) || (t[r] = !0);
  }
  return t;
}
function ba(e, t, r) {
  r || (r = Vh(t));
  for (const n in e) {
    const a = e[n];
    if (DE(a))
      dt(t) || Wh(r[n]) ? r[n] = Vh(a, Array.isArray(a) ? [] : {}) : ba(a, Gt(t) ? {} : t[n], r[n]);
    else {
      const i = t[n];
      r[n] = !gn(a, i);
    }
  }
  return r;
}
const P_ = {
  value: !1,
  isValid: !1
}, O_ = { value: !0, isValid: !0 };
var $E = (e) => {
  if (Array.isArray(e)) {
    if (e.length > 1) {
      const t = e.filter((r) => r && r.checked && !r.disabled).map((r) => r.value);
      return { value: t, isValid: !!t.length };
    }
    return e[0].checked && !e[0].disabled ? (
      // @ts-expect-error expected to work in the browser
      e[0].attributes && !dt(e[0].attributes.value) ? dt(e[0].value) || e[0].value === "" ? O_ : { value: e[0].value, isValid: !0 } : O_
    ) : P_;
  }
  return P_;
}, LE = (e, { valueAsNumber: t, valueAsDate: r, setValueAs: n }) => dt(e) ? e : t ? e === "" ? NaN : e && +e : r && ar(e) ? new Date(e) : n ? n(e) : e;
const N_ = {
  isValid: !1,
  value: null
};
var FE = (e) => Array.isArray(e) ? e.reduce((t, r) => r && r.checked && !r.disabled ? {
  isValid: !0,
  value: r.value
} : t, N_) : N_;
function M_(e) {
  const t = e.ref;
  return sg(t) ? t.files : lg(t) ? FE(e.refs).value : IE(t) ? [...t.selectedOptions].map(({ value: r }) => r) : $o(t) ? $E(e.refs).value : LE(dt(t.value) ? e.ref.value : t.value, e);
}
var LU = (e, t, r, n) => {
  const a = {};
  for (const i of e) {
    const o = we(t, i);
    o && nt(a, i, o._f);
  }
  return {
    criteriaMode: r,
    names: [...e],
    fields: a,
    shouldUseNativeValidation: n
  };
}, hl = (e) => e instanceof RegExp, Ci = (e) => dt(e) ? e : hl(e) ? e.source : _t(e) ? hl(e.value) ? e.value.source : e.value : e, R_ = (e) => ({
  isOnSubmit: !e || e === Er.onSubmit,
  isOnBlur: e === Er.onBlur,
  isOnChange: e === Er.onChange,
  isOnAll: e === Er.all,
  isOnTouch: e === Er.onTouched
});
const I_ = "AsyncFunction";
var FU = (e) => !!e && !!e.validate && !!(kr(e.validate) && e.validate.constructor.name === I_ || _t(e.validate) && Object.values(e.validate).find((t) => t.constructor.name === I_)), qU = (e) => e.mount && (e.required || e.min || e.max || e.maxLength || e.minLength || e.pattern || e.validate), D_ = (e, t, r) => !r && (t.watchAll || t.watch.has(e) || [...t.watch].some((n) => e.startsWith(n) && /^\.\w+/.test(e.slice(n.length))));
const Li = (e, t, r, n) => {
  for (const a of r || Object.keys(e)) {
    const i = we(e, a);
    if (i) {
      const { _f: o, ...l } = i;
      if (o) {
        if (o.refs && o.refs[0] && t(o.refs[0], a) && !n)
          return !0;
        if (o.ref && t(o.ref, o.name) && !n)
          return !0;
        if (Li(l, t))
          break;
      } else if (_t(l) && Li(l, t))
        break;
    }
  }
};
function $_(e, t, r) {
  const n = we(e, r);
  if (n || ec(r))
    return {
      error: n,
      name: r
    };
  const a = r.split(".");
  for (; a.length; ) {
    const i = a.join("."), o = we(t, i), l = we(e, i);
    if (o && !Array.isArray(o) && r !== i)
      return { name: r };
    if (l && l.type)
      return {
        name: i,
        error: l
      };
    if (l && l.root && l.root.type)
      return {
        name: `${i}.root`,
        error: l.root
      };
    a.pop();
  }
  return {
    name: r
  };
}
var UU = (e, t, r, n) => {
  r(e);
  const { name: a, ...i } = e;
  return Bt(i) || Object.keys(i).length >= Object.keys(t).length || Object.keys(i).find((o) => t[o] === (!n || Er.all));
}, zU = (e, t, r) => !e || !t || e === t || $i(e).some((n) => n && (r ? n === t : n.startsWith(t) || t.startsWith(n))), BU = (e, t, r, n, a) => a.isOnAll ? !1 : !r && a.isOnTouch ? !(t || e) : (r ? n.isOnBlur : a.isOnBlur) ? !e : (r ? n.isOnChange : a.isOnChange) ? e : !0, WU = (e, t) => !ig(we(e, t)).length && bt(e, t), VU = (e, t, r) => {
  const n = $i(we(e, r));
  return nt(n, "root", t[r]), nt(e, r, n), e;
};
function L_(e, t, r = "validate") {
  if (ar(e) || Array.isArray(e) && e.every(ar) || Nr(e) && !e)
    return {
      type: r,
      message: ar(e) ? e : "",
      ref: t
    };
}
var ua = (e) => _t(e) && !hl(e) ? e : {
  value: e,
  message: ""
}, F_ = async (e, t, r, n, a, i) => {
  const { ref: o, refs: l, required: c, maxLength: u, minLength: d, min: f, max: p, pattern: m, validate: y, name: h, valueAsNumber: g, mount: v } = e._f, b = we(r, h);
  if (!v || t.has(h))
    return {};
  const w = l ? l[0] : o, j = (A) => {
    a && w.reportValidity && (w.setCustomValidity(Nr(A) ? "" : A || ""), w.reportValidity());
  }, x = {}, _ = lg(o), S = $o(o), k = _ || S, E = (g || sg(o)) && dt(o.value) && dt(b) || ml(o) && o.value === "" || b === "" || Array.isArray(b) && !b.length, L = ME.bind(null, h, n, x), P = (A, T, M, N = zr.maxLength, q = zr.minLength) => {
    const V = A ? T : M;
    x[h] = {
      type: A ? N : q,
      message: V,
      ref: o,
      ...L(A ? N : q, V)
    };
  };
  if (i ? !Array.isArray(b) || !b.length : c && (!k && (E || Gt(b)) || Nr(b) && !b || S && !$E(l).isValid || _ && !FE(l).isValid)) {
    const { value: A, message: T } = ar(c) ? { value: !!c, message: c } : ua(c);
    if (A && (x[h] = {
      type: zr.required,
      message: T,
      ref: w,
      ...L(zr.required, T)
    }, !n))
      return j(T), x;
  }
  if (!E && (!Gt(f) || !Gt(p))) {
    let A, T;
    const M = ua(p), N = ua(f);
    if (!Gt(b) && !isNaN(b)) {
      const q = o.valueAsNumber || b && +b;
      Gt(M.value) || (A = q > M.value), Gt(N.value) || (T = q < N.value);
    } else {
      const q = o.valueAsDate || new Date(b), V = (U) => /* @__PURE__ */ new Date((/* @__PURE__ */ new Date()).toDateString() + " " + U), B = o.type == "time", ee = o.type == "week";
      ar(M.value) && b && (A = B ? V(b) > V(M.value) : ee ? b > M.value : q > new Date(M.value)), ar(N.value) && b && (T = B ? V(b) < V(N.value) : ee ? b < N.value : q < new Date(N.value));
    }
    if ((A || T) && (P(!!A, M.message, N.message, zr.max, zr.min), !n))
      return j(x[h].message), x;
  }
  if ((u || d) && !E && (ar(b) || i && Array.isArray(b))) {
    const A = ua(u), T = ua(d), M = !Gt(A.value) && b.length > +A.value, N = !Gt(T.value) && b.length < +T.value;
    if ((M || N) && (P(M, A.message, T.message), !n))
      return j(x[h].message), x;
  }
  if (m && !E && ar(b)) {
    const { value: A, message: T } = ua(m);
    if (hl(A) && !b.match(A) && (x[h] = {
      type: zr.pattern,
      message: T,
      ref: o,
      ...L(zr.pattern, T)
    }, !n))
      return j(T), x;
  }
  if (y) {
    if (kr(y)) {
      const A = await y(b, r), T = L_(A, w);
      if (T && (x[h] = {
        ...T,
        ...L(zr.validate, T.message)
      }, !n))
        return j(T.message), x;
    } else if (_t(y)) {
      let A = {};
      for (const T in y) {
        if (!Bt(A) && !n)
          break;
        const M = L_(await y[T](b, r), w, T);
        M && (A = {
          ...M,
          ...L(T, M.message)
        }, j(M.message), n && (x[h] = A));
      }
      if (!Bt(A) && (x[h] = {
        ref: w,
        ...A
      }, !n))
        return x;
    }
  }
  return j(!0), x;
};
const HU = {
  mode: Er.onSubmit,
  reValidateMode: Er.onChange,
  shouldFocusError: !0
};
function GU(e = {}) {
  let t = {
    ...HU,
    ...e
  }, r = {
    submitCount: 0,
    isDirty: !1,
    isReady: !1,
    isLoading: kr(t.defaultValues),
    isValidating: !1,
    isSubmitted: !1,
    isSubmitting: !1,
    isSubmitSuccessful: !1,
    isValid: !1,
    touchedFields: {},
    dirtyFields: {},
    validatingFields: {},
    errors: t.errors || {},
    disabled: t.disabled || !1
  }, n = {}, a = _t(t.defaultValues) || _t(t.values) ? Ct(t.defaultValues || t.values) || {} : {}, i = t.shouldUnregister ? {} : Ct(a), o = {
    action: !1,
    mount: !1,
    watch: !1,
    keepIsValid: !1
  }, l = {
    mount: /* @__PURE__ */ new Set(),
    disabled: /* @__PURE__ */ new Set(),
    unMount: /* @__PURE__ */ new Set(),
    array: /* @__PURE__ */ new Set(),
    watch: /* @__PURE__ */ new Set()
  }, c, u = 0;
  const d = {
    isDirty: !1,
    dirtyFields: !1,
    validatingFields: !1,
    touchedFields: !1,
    isValidating: !1,
    isValid: !1,
    errors: !1
  }, f = {
    ...d
  };
  let p = {
    ...f
  };
  const m = {
    array: T_(),
    state: T_()
  }, y = t.criteriaMode === Er.all, h = (C) => (R) => {
    clearTimeout(u), u = setTimeout(C, R);
  }, g = async (C) => {
    if (!o.keepIsValid && !t.disabled && (f.isValid || p.isValid || C)) {
      let R;
      t.resolver ? (R = Bt((await k()).errors), v()) : R = await L(n, !0), R !== r.isValid && m.state.next({
        isValid: R
      });
    }
  }, v = (C, R) => {
    !t.disabled && (f.isValidating || f.validatingFields || p.isValidating || p.validatingFields) && ((C || Array.from(l.mount)).forEach((W) => {
      W && (R ? nt(r.validatingFields, W, R) : bt(r.validatingFields, W));
    }), m.state.next({
      validatingFields: r.validatingFields,
      isValidating: !Bt(r.validatingFields)
    }));
  }, b = (C, R = [], W, ue, le = !0, ce = !0) => {
    if (ue && W && !t.disabled) {
      if (o.action = !0, ce && Array.isArray(we(n, C))) {
        const _e = W(we(n, C), ue.argA, ue.argB);
        le && nt(n, C, _e);
      }
      if (ce && Array.isArray(we(r.errors, C))) {
        const _e = W(we(r.errors, C), ue.argA, ue.argB);
        le && nt(r.errors, C, _e), WU(r.errors, C);
      }
      if ((f.touchedFields || p.touchedFields) && ce && Array.isArray(we(r.touchedFields, C))) {
        const _e = W(we(r.touchedFields, C), ue.argA, ue.argB);
        le && nt(r.touchedFields, C, _e);
      }
      (f.dirtyFields || p.dirtyFields) && (r.dirtyFields = ba(a, i)), m.state.next({
        name: C,
        isDirty: A(C, R),
        dirtyFields: r.dirtyFields,
        errors: r.errors,
        isValid: r.isValid
      });
    } else
      nt(i, C, R);
  }, w = (C, R) => {
    nt(r.errors, C, R), m.state.next({
      errors: r.errors
    });
  }, j = (C) => {
    r.errors = C, m.state.next({
      errors: r.errors,
      isValid: !1
    });
  }, x = (C, R, W, ue) => {
    const le = we(n, C);
    if (le) {
      const ce = we(i, C, dt(W) ? we(a, C) : W);
      dt(ce) || ue && ue.defaultChecked || R ? nt(i, C, R ? ce : M_(le._f)) : N(C, ce), o.mount && !o.action && g();
    }
  }, _ = (C, R, W, ue, le) => {
    let ce = !1, _e = !1;
    const Be = {
      name: C
    };
    if (!t.disabled) {
      if (!W || ue) {
        (f.isDirty || p.isDirty) && (_e = r.isDirty, r.isDirty = Be.isDirty = A(), ce = _e !== Be.isDirty);
        const Ue = gn(we(a, C), R);
        _e = !!we(r.dirtyFields, C), Ue ? bt(r.dirtyFields, C) : nt(r.dirtyFields, C, !0), Be.dirtyFields = r.dirtyFields, ce = ce || (f.dirtyFields || p.dirtyFields) && _e !== !Ue;
      }
      if (W) {
        const Ue = we(r.touchedFields, C);
        Ue || (nt(r.touchedFields, C, W), Be.touchedFields = r.touchedFields, ce = ce || (f.touchedFields || p.touchedFields) && Ue !== W);
      }
      ce && le && m.state.next(Be);
    }
    return ce ? Be : {};
  }, S = (C, R, W, ue) => {
    const le = we(r.errors, C), ce = (f.isValid || p.isValid) && Nr(R) && r.isValid !== R;
    if (t.delayError && W ? (c = h(() => w(C, W)), c(t.delayError)) : (clearTimeout(u), c = null, W ? nt(r.errors, C, W) : bt(r.errors, C)), (W ? !gn(le, W) : le) || !Bt(ue) || ce) {
      const _e = {
        ...ue,
        ...ce && Nr(R) ? { isValid: R } : {},
        errors: r.errors,
        name: C
      };
      r = {
        ...r,
        ..._e
      }, m.state.next(_e);
    }
  }, k = async (C) => (v(C, !0), await t.resolver(i, t.context, LU(C || l.mount, n, t.criteriaMode, t.shouldUseNativeValidation))), E = async (C) => {
    const { errors: R } = await k(C);
    if (v(C), C)
      for (const W of C) {
        const ue = we(R, W);
        ue ? nt(r.errors, W, ue) : bt(r.errors, W);
      }
    else
      r.errors = R;
    return R;
  }, L = async (C, R, W = {
    valid: !0
  }) => {
    for (const ue in C) {
      const le = C[ue];
      if (le) {
        const { _f: ce, ..._e } = le;
        if (ce) {
          const Be = l.array.has(ce.name), Ue = le._f && FU(le._f);
          Ue && f.validatingFields && v([ce.name], !0);
          const pt = await F_(le, l.disabled, i, y, t.shouldUseNativeValidation && !R, Be);
          if (Ue && f.validatingFields && v([ce.name]), pt[ce.name] && (W.valid = !1, R || e.shouldUseNativeValidation))
            break;
          !R && (we(pt, ce.name) ? Be ? VU(r.errors, pt, ce.name) : nt(r.errors, ce.name, pt[ce.name]) : bt(r.errors, ce.name));
        }
        !Bt(_e) && await L(_e, R, W);
      }
    }
    return W.valid;
  }, P = () => {
    for (const C of l.unMount) {
      const R = we(n, C);
      R && (R._f.refs ? R._f.refs.every((W) => !ym(W)) : !ym(R._f.ref)) && ie(C);
    }
    l.unMount = /* @__PURE__ */ new Set();
  }, A = (C, R) => !t.disabled && (C && R && nt(i, C, R), !gn(G(), a)), T = (C, R, W) => MU(C, l, {
    ...o.mount ? i : dt(R) ? a : ar(C) ? { [C]: R } : R
  }, W, R), M = (C) => ig(we(o.mount ? i : a, C, t.shouldUnregister ? we(a, C, []) : [])), N = (C, R, W = {}) => {
    const ue = we(n, C);
    let le = R;
    if (ue) {
      const ce = ue._f;
      ce && (!ce.disabled && nt(i, C, LE(R, ce)), le = ml(ce.ref) && Gt(R) ? "" : R, IE(ce.ref) ? [...ce.ref.options].forEach((_e) => _e.selected = le.includes(_e.value)) : ce.refs ? $o(ce.ref) ? ce.refs.forEach((_e) => {
        (!_e.defaultChecked || !_e.disabled) && (Array.isArray(le) ? _e.checked = !!le.find((Be) => Be === _e.value) : _e.checked = le === _e.value || !!le);
      }) : ce.refs.forEach((_e) => _e.checked = _e.value === le) : sg(ce.ref) ? ce.ref.value = "" : (ce.ref.value = le, ce.ref.type || m.state.next({
        name: C,
        values: Ct(i)
      })));
    }
    (W.shouldDirty || W.shouldTouch) && _(C, le, W.shouldTouch, W.shouldDirty, !0), W.shouldValidate && U(C);
  }, q = (C, R, W) => {
    for (const ue in R) {
      if (!R.hasOwnProperty(ue))
        return;
      const le = R[ue], ce = C + "." + ue, _e = we(n, ce);
      (l.array.has(C) || _t(le) || _e && !_e._f) && !Fn(le) ? q(ce, le, W) : N(ce, le, W);
    }
  }, V = (C, R, W = {}) => {
    const ue = we(n, C), le = l.array.has(C), ce = Ct(R);
    nt(i, C, ce), le ? (m.array.next({
      name: C,
      values: Ct(i)
    }), (f.isDirty || f.dirtyFields || p.isDirty || p.dirtyFields) && W.shouldDirty && m.state.next({
      name: C,
      dirtyFields: ba(a, i),
      isDirty: A(C, ce)
    })) : ue && !ue._f && !Gt(ce) ? q(C, ce, W) : N(C, ce, W), D_(C, l) ? m.state.next({
      ...r,
      name: C,
      values: Ct(i)
    }) : m.state.next({
      name: o.mount ? C : void 0,
      values: Ct(i)
    });
  }, B = async (C) => {
    o.mount = !0;
    const R = C.target;
    let W = R.name, ue = !0;
    const le = we(n, W), ce = (Ue) => {
      ue = Number.isNaN(Ue) || Fn(Ue) && isNaN(Ue.getTime()) || gn(Ue, we(i, W, Ue));
    }, _e = R_(t.mode), Be = R_(t.reValidateMode);
    if (le) {
      let Ue, pt;
      const or = R.type ? M_(le._f) : EU(C), Qt = C.type === C_.BLUR || C.type === C_.FOCUS_OUT, ea = !qU(le._f) && !t.resolver && !we(r.errors, W) && !le._f.deps || BU(Qt, we(r.touchedFields, W), r.isSubmitted, Be, _e), sn = D_(W, l, Qt);
      nt(i, W, or), Qt ? (!R || !R.readOnly) && (le._f.onBlur && le._f.onBlur(C), c && c(0)) : le._f.onChange && le._f.onChange(C);
      const ln = _(W, or, Qt), ta = !Bt(ln) || sn;
      if (!Qt && m.state.next({
        name: W,
        type: C.type,
        values: Ct(i)
      }), ea)
        return (f.isValid || p.isValid) && (t.mode === "onBlur" ? Qt && g() : Qt || g()), ta && m.state.next({ name: W, ...sn ? {} : ln });
      if (!Qt && sn && m.state.next({ ...r }), t.resolver) {
        const { errors: An } = await k([W]);
        if (v([W]), ce(or), ue) {
          const cn = $_(r.errors, n, W), un = $_(An, n, cn.name || W);
          Ue = un.error, W = un.name, pt = Bt(An);
        }
      } else
        v([W], !0), Ue = (await F_(le, l.disabled, i, y, t.shouldUseNativeValidation))[W], v([W]), ce(or), ue && (Ue ? pt = !1 : (f.isValid || p.isValid) && (pt = await L(n, !0)));
      ue && (le._f.deps && (!Array.isArray(le._f.deps) || le._f.deps.length > 0) && U(le._f.deps), S(W, pt, Ue, ln));
    }
  }, ee = (C, R) => {
    if (we(r.errors, R) && C.focus)
      return C.focus(), 1;
  }, U = async (C, R = {}) => {
    let W, ue;
    const le = $i(C);
    if (t.resolver) {
      const ce = await E(dt(C) ? C : le);
      W = Bt(ce), ue = C ? !le.some((_e) => we(ce, _e)) : W;
    } else C ? (ue = (await Promise.all(le.map(async (ce) => {
      const _e = we(n, ce);
      return await L(_e && _e._f ? { [ce]: _e } : _e);
    }))).every(Boolean), !(!ue && !r.isValid) && g()) : ue = W = await L(n);
    return m.state.next({
      ...!ar(C) || (f.isValid || p.isValid) && W !== r.isValid ? {} : { name: C },
      ...t.resolver || !C ? { isValid: W } : {},
      errors: r.errors
    }), R.shouldFocus && !ue && Li(n, ee, C ? le : l.mount), ue;
  }, G = (C, R) => {
    let W = {
      ...o.mount ? i : a
    };
    return R && (W = RE(R.dirtyFields ? r.dirtyFields : r.touchedFields, W)), dt(C) ? W : ar(C) ? we(W, C) : C.map((ue) => we(W, ue));
  }, Y = (C, R) => ({
    invalid: !!we((R || r).errors, C),
    isDirty: !!we((R || r).dirtyFields, C),
    error: we((R || r).errors, C),
    isValidating: !!we(r.validatingFields, C),
    isTouched: !!we((R || r).touchedFields, C)
  }), Q = (C) => {
    C && $i(C).forEach((R) => bt(r.errors, R)), m.state.next({
      errors: C ? r.errors : {}
    });
  }, ne = (C, R, W) => {
    const ue = (we(n, C, { _f: {} })._f || {}).ref, le = we(r.errors, C) || {}, { ref: ce, message: _e, type: Be, ...Ue } = le;
    nt(r.errors, C, {
      ...Ue,
      ...R,
      ref: ue
    }), m.state.next({
      name: C,
      errors: r.errors,
      isValid: !1
    }), W && W.shouldFocus && ue && ue.focus && ue.focus();
  }, z = (C, R) => kr(C) ? m.state.subscribe({
    next: (W) => "values" in W && C(T(void 0, R), W)
  }) : T(C, R, !0), se = (C) => m.state.subscribe({
    next: (R) => {
      zU(C.name, R.name, C.exact) && UU(R, C.formState || f, ke, C.reRenderRoot) && C.callback({
        values: { ...i },
        ...r,
        ...R,
        defaultValues: a
      });
    }
  }).unsubscribe, oe = (C) => (o.mount = !0, p = {
    ...p,
    ...C.formState
  }, se({
    ...C,
    formState: {
      ...d,
      ...C.formState
    }
  })), ie = (C, R = {}) => {
    for (const W of C ? $i(C) : l.mount)
      l.mount.delete(W), l.array.delete(W), R.keepValue || (bt(n, W), bt(i, W)), !R.keepError && bt(r.errors, W), !R.keepDirty && bt(r.dirtyFields, W), !R.keepTouched && bt(r.touchedFields, W), !R.keepIsValidating && bt(r.validatingFields, W), !t.shouldUnregister && !R.keepDefaultValue && bt(a, W);
    m.state.next({
      values: Ct(i)
    }), m.state.next({
      ...r,
      ...R.keepDirty ? { isDirty: A() } : {}
    }), !R.keepIsValid && g();
  }, K = ({ disabled: C, name: R }) => {
    (Nr(C) && o.mount || C || l.disabled.has(R)) && (C ? l.disabled.add(R) : l.disabled.delete(R));
  }, ae = (C, R = {}) => {
    let W = we(n, C);
    const ue = Nr(R.disabled) || Nr(t.disabled);
    return nt(n, C, {
      ...W || {},
      _f: {
        ...W && W._f ? W._f : { ref: { name: C } },
        name: C,
        mount: !0,
        ...R
      }
    }), l.mount.add(C), W ? K({
      disabled: Nr(R.disabled) ? R.disabled : t.disabled,
      name: C
    }) : x(C, !0, R.value), {
      ...ue ? { disabled: R.disabled || t.disabled } : {},
      ...t.progressive ? {
        required: !!R.required,
        min: Ci(R.min),
        max: Ci(R.max),
        minLength: Ci(R.minLength),
        maxLength: Ci(R.maxLength),
        pattern: Ci(R.pattern)
      } : {},
      name: C,
      onChange: B,
      onBlur: B,
      ref: (le) => {
        if (le) {
          ae(C, R), W = we(n, C);
          const ce = dt(le.value) && le.querySelectorAll && le.querySelectorAll("input,select,textarea")[0] || le, _e = RU(ce), Be = W._f.refs || [];
          if (_e ? Be.find((Ue) => Ue === ce) : ce === W._f.ref)
            return;
          nt(n, C, {
            _f: {
              ...W._f,
              ..._e ? {
                refs: [
                  ...Be.filter(ym),
                  ce,
                  ...Array.isArray(we(a, C)) ? [{}] : []
                ],
                ref: { type: ce.type, name: C }
              } : { ref: ce }
            }
          }), x(C, !1, void 0, ce);
        } else
          W = we(n, C, {}), W._f && (W._f.mount = !1), (t.shouldUnregister || R.shouldUnregister) && !(CU(l.array, C) && o.action) && l.unMount.add(C);
      }
    };
  }, fe = () => t.shouldFocusError && Li(n, ee, l.mount), $ = (C) => {
    Nr(C) && (m.state.next({ disabled: C }), Li(n, (R, W) => {
      const ue = we(n, W);
      ue && (R.disabled = ue._f.disabled || C, Array.isArray(ue._f.refs) && ue._f.refs.forEach((le) => {
        le.disabled = ue._f.disabled || C;
      }));
    }, 0, !1));
  }, Ee = (C, R) => async (W) => {
    let ue;
    W && (W.preventDefault && W.preventDefault(), W.persist && W.persist());
    let le = Ct(i);
    if (m.state.next({
      isSubmitting: !0
    }), t.resolver) {
      const { errors: ce, values: _e } = await k();
      v(), r.errors = ce, le = Ct(_e);
    } else
      await L(n);
    if (l.disabled.size)
      for (const ce of l.disabled)
        bt(le, ce);
    if (bt(r.errors, "root"), Bt(r.errors)) {
      m.state.next({
        errors: {}
      });
      try {
        await C(le, W);
      } catch (ce) {
        ue = ce;
      }
    } else
      R && await R({ ...r.errors }, W), fe(), setTimeout(fe);
    if (m.state.next({
      isSubmitted: !0,
      isSubmitting: !1,
      isSubmitSuccessful: Bt(r.errors) && !ue,
      submitCount: r.submitCount + 1,
      errors: r.errors
    }), ue)
      throw ue;
  }, re = (C, R = {}) => {
    we(n, C) && (dt(R.defaultValue) ? V(C, Ct(we(a, C))) : (V(C, R.defaultValue), nt(a, C, Ct(R.defaultValue))), R.keepTouched || bt(r.touchedFields, C), R.keepDirty || (bt(r.dirtyFields, C), r.isDirty = R.defaultValue ? A(C, Ct(we(a, C))) : A()), R.keepError || (bt(r.errors, C), f.isValid && g()), m.state.next({ ...r }));
  }, Pe = (C, R = {}) => {
    const W = C ? Ct(C) : a, ue = Ct(W), le = Bt(C), ce = le ? a : ue;
    if (R.keepDefaultValues || (a = W), !R.keepValues) {
      if (R.keepDirtyValues) {
        const _e = /* @__PURE__ */ new Set([
          ...l.mount,
          ...Object.keys(ba(a, i))
        ]);
        for (const Be of Array.from(_e)) {
          const Ue = we(r.dirtyFields, Be), pt = we(i, Be), or = we(ce, Be);
          Ue && !dt(pt) ? nt(ce, Be, pt) : !Ue && !dt(or) && V(Be, or);
        }
      } else {
        if (ag && dt(C))
          for (const _e of l.mount) {
            const Be = we(n, _e);
            if (Be && Be._f) {
              const Ue = Array.isArray(Be._f.refs) ? Be._f.refs[0] : Be._f.ref;
              if (ml(Ue)) {
                const pt = Ue.closest("form");
                if (pt) {
                  pt.reset();
                  break;
                }
              }
            }
          }
        if (R.keepFieldsRef)
          for (const _e of l.mount)
            V(_e, we(ce, _e));
        else
          n = {};
      }
      i = t.shouldUnregister ? R.keepDefaultValues ? Ct(a) : {} : Ct(ce), m.array.next({
        values: { ...ce }
      }), m.state.next({
        values: { ...ce }
      });
    }
    l = {
      mount: R.keepDirtyValues ? l.mount : /* @__PURE__ */ new Set(),
      unMount: /* @__PURE__ */ new Set(),
      array: /* @__PURE__ */ new Set(),
      disabled: /* @__PURE__ */ new Set(),
      watch: /* @__PURE__ */ new Set(),
      watchAll: !1,
      focus: ""
    }, o.mount = !f.isValid || !!R.keepIsValid || !!R.keepDirtyValues || !t.shouldUnregister && !Bt(ce), o.watch = !!t.shouldUnregister, o.keepIsValid = !!R.keepIsValid, o.action = !1, R.keepErrors || (r.errors = {}), m.state.next({
      submitCount: R.keepSubmitCount ? r.submitCount : 0,
      isDirty: le ? !1 : R.keepDirty ? r.isDirty : !!(R.keepDefaultValues && !gn(C, a)),
      isSubmitted: R.keepIsSubmitted ? r.isSubmitted : !1,
      dirtyFields: le ? {} : R.keepDirtyValues ? R.keepDefaultValues && i ? ba(a, i) : r.dirtyFields : R.keepDefaultValues && C ? ba(a, C) : R.keepDirty ? r.dirtyFields : {},
      touchedFields: R.keepTouched ? r.touchedFields : {},
      errors: R.keepErrors ? r.errors : {},
      isSubmitSuccessful: R.keepIsSubmitSuccessful ? r.isSubmitSuccessful : !1,
      isSubmitting: !1,
      defaultValues: a
    });
  }, Oe = (C, R) => Pe(kr(C) ? C(i) : C, { ...t.resetOptions, ...R }), Ne = (C, R = {}) => {
    const W = we(n, C), ue = W && W._f;
    if (ue) {
      const le = ue.refs ? ue.refs[0] : ue.ref;
      le.focus && setTimeout(() => {
        le.focus(), R.shouldSelect && kr(le.select) && le.select();
      });
    }
  }, ke = (C) => {
    r = {
      ...r,
      ...C
    };
  }, Ae = {
    control: {
      register: ae,
      unregister: ie,
      getFieldState: Y,
      handleSubmit: Ee,
      setError: ne,
      _subscribe: se,
      _runSchema: k,
      _updateIsValidating: v,
      _focusError: fe,
      _getWatch: T,
      _getDirty: A,
      _setValid: g,
      _setFieldArray: b,
      _setDisabledField: K,
      _setErrors: j,
      _getFieldArray: M,
      _reset: Pe,
      _resetDefaultValues: () => kr(t.defaultValues) && t.defaultValues().then((C) => {
        Oe(C, t.resetOptions), m.state.next({
          isLoading: !1
        });
      }),
      _removeUnmounted: P,
      _disableForm: $,
      _subjects: m,
      _proxyFormState: f,
      get _fields() {
        return n;
      },
      get _formValues() {
        return i;
      },
      get _state() {
        return o;
      },
      set _state(C) {
        o = C;
      },
      get _defaultValues() {
        return a;
      },
      get _names() {
        return l;
      },
      set _names(C) {
        l = C;
      },
      get _formState() {
        return r;
      },
      get _options() {
        return t;
      },
      set _options(C) {
        t = {
          ...t,
          ...C
        };
      }
    },
    subscribe: oe,
    trigger: U,
    register: ae,
    handleSubmit: Ee,
    watch: z,
    setValue: V,
    getValues: G,
    reset: Oe,
    resetField: re,
    clearErrors: Q,
    unregister: ie,
    setError: ne,
    setFocus: Ne,
    getFieldState: Y
  };
  return {
    ...Ae,
    formControl: Ae
  };
}
function KU(e = {}) {
  const t = I.useRef(void 0), r = I.useRef(void 0), [n, a] = I.useState({
    isDirty: !1,
    isValidating: !1,
    isLoading: kr(e.defaultValues),
    isSubmitted: !1,
    isSubmitting: !1,
    isSubmitSuccessful: !1,
    isValid: !1,
    submitCount: 0,
    dirtyFields: {},
    touchedFields: {},
    validatingFields: {},
    errors: e.errors || {},
    disabled: e.disabled || !1,
    isReady: !1,
    defaultValues: kr(e.defaultValues) ? void 0 : e.defaultValues
  });
  if (!t.current)
    if (e.formControl)
      t.current = {
        ...e.formControl,
        formState: n
      }, e.defaultValues && !kr(e.defaultValues) && e.formControl.reset(e.defaultValues, e.resetOptions);
    else {
      const { formControl: o, ...l } = GU(e);
      t.current = {
        ...l,
        formState: n
      };
    }
  const i = t.current.control;
  return i._options = e, NU(() => {
    const o = i._subscribe({
      formState: i._proxyFormState,
      callback: () => a({ ...i._formState }),
      reRenderRoot: !0
    });
    return a((l) => ({
      ...l,
      isReady: !0
    })), i._formState.isReady = !0, o;
  }, [i]), I.useEffect(() => i._disableForm(e.disabled), [i, e.disabled]), I.useEffect(() => {
    e.mode && (i._options.mode = e.mode), e.reValidateMode && (i._options.reValidateMode = e.reValidateMode);
  }, [i, e.mode, e.reValidateMode]), I.useEffect(() => {
    e.errors && (i._setErrors(e.errors), i._focusError());
  }, [i, e.errors]), I.useEffect(() => {
    e.shouldUnregister && i._subjects.state.next({
      values: i._getWatch()
    });
  }, [i, e.shouldUnregister]), I.useEffect(() => {
    if (i._proxyFormState.isDirty) {
      const o = i._getDirty();
      o !== n.isDirty && i._subjects.state.next({
        isDirty: o
      });
    }
  }, [i, n.isDirty]), I.useEffect(() => {
    var o;
    e.values && !gn(e.values, r.current) ? (i._reset(e.values, {
      keepFieldsRef: !0,
      ...i._options.resetOptions
    }), !((o = i._options.resetOptions) === null || o === void 0) && o.keepIsValid || i._setValid(), r.current = e.values, a((l) => ({ ...l }))) : i._resetDefaultValues();
  }, [i, e.values]), I.useEffect(() => {
    i._state.mount || (i._setValid(), i._state.mount = !0), i._state.watch && (i._state.watch = !1, i._subjects.state.next({ ...i._formState })), i._removeUnmounted();
  }), t.current.formState = OU(n, i), t.current;
}
const q_ = (e, t, r) => {
  if (e && "reportValidity" in e) {
    const n = we(r, t);
    e.setCustomValidity(n && n.message || ""), e.reportValidity();
  }
}, qE = (e, t) => {
  for (const r in t.fields) {
    const n = t.fields[r];
    n && n.ref && "reportValidity" in n.ref ? q_(n.ref, r, e) : n.refs && n.refs.forEach((a) => q_(a, r, e));
  }
}, YU = (e, t) => {
  t.shouldUseNativeValidation && qE(e, t);
  const r = {};
  for (const n in e) {
    const a = we(t.fields, n), i = Object.assign(e[n] || {}, { ref: a && a.ref });
    if (XU(t.names || Object.keys(e), n)) {
      const o = Object.assign({}, we(r, n));
      nt(o, "root", i), nt(r, n, o);
    } else nt(r, n, i);
  }
  return r;
}, XU = (e, t) => e.some((r) => r.startsWith(t + "."));
var QU = function(e, t) {
  for (var r = {}; e.length; ) {
    var n = e[0], a = n.code, i = n.message, o = n.path.join(".");
    if (!r[o]) if ("unionErrors" in n) {
      var l = n.unionErrors[0].errors[0];
      r[o] = { message: l.message, type: l.code };
    } else r[o] = { message: i, type: a };
    if ("unionErrors" in n && n.unionErrors.forEach(function(d) {
      return d.errors.forEach(function(f) {
        return e.push(f);
      });
    }), t) {
      var c = r[o].types, u = c && c[n.code];
      r[o] = ME(o, t, r, a, u ? [].concat(u, n.message) : n.message);
    }
    e.shift();
  }
  return r;
}, JU = function(e, t, r) {
  return r === void 0 && (r = {}), function(n, a, i) {
    try {
      return Promise.resolve((function(o, l) {
        try {
          var c = Promise.resolve(e[r.mode === "sync" ? "parse" : "parseAsync"](n, t)).then(function(u) {
            return i.shouldUseNativeValidation && qE({}, i), { errors: {}, values: r.raw ? n : u };
          });
        } catch (u) {
          return l(u);
        }
        return c && c.then ? c.then(void 0, l) : c;
      })(0, function(o) {
        if ((function(l) {
          return Array.isArray(l == null ? void 0 : l.errors);
        })(o)) return { values: {}, errors: YU(QU(o.errors, !i.shouldUseNativeValidation && i.criteriaMode === "all"), i) };
        throw o;
      }));
    } catch (o) {
      return Promise.reject(o);
    }
  };
};
function Tz({
  schema: e,
  onSubmit: t,
  children: r,
  defaultValues: n,
  className: a
}) {
  const i = KU({
    resolver: JU(e),
    defaultValues: n
  });
  return /* @__PURE__ */ s.jsx("form", { onSubmit: i.handleSubmit(t), className: a, children: r(i) });
}
function Pz(e = {}) {
  const [t, r] = de(null), [n, a] = de(!0), [i, o] = de(null), [l, c] = de(null);
  et(() => {
    try {
      const m = Sm(e);
      c(m);
      const y = m.getEnhancedState();
      r(y), a(!1);
    } catch (m) {
      o(m instanceof Error ? m : new Error(String(m))), a(!1);
    }
  }, [e]), et(() => {
    if (!l) return;
    const m = l.subscribeToEnhancedState((y) => {
      r(y);
    });
    return () => m();
  }, [l]);
  const u = (m) => {
    if (!l)
      return o(new Error("Plugin not initialized")), !1;
    try {
      return l.updateEnhancedConfig(m);
    } catch (y) {
      return o(y instanceof Error ? y : new Error(String(y))), !1;
    }
  }, d = () => {
    if (!l)
      return o(new Error("Plugin not initialized")), !1;
    try {
      return l.resetEnhancedConfig();
    } catch (m) {
      return o(m instanceof Error ? m : new Error(String(m))), !1;
    }
  }, f = () => !l || !t ? {
    performance: !1,
    security: !1,
    monitoring: !1,
    integration: !1,
    error_handling: !1
  } : {
    performance: l.validatePerformanceConfig(t.performanceConfig),
    security: l.validateSecurityConfig(t.securityConfig),
    monitoring: l.validateMonitoringConfig(t.monitoringConfig),
    integration: l.validateIntegrationConfig(t.integrationConfig),
    error_handling: l.validateErrorHandlingConfig(t.errorHandlingConfig)
  }, p = async (m, y = {}) => {
    if (!l)
      return o(new Error("Plugin not initialized")), { success: !1, error: new Error("Plugin not initialized") };
    try {
      return await l.executeEvolutionWithEnhancedFeatures(m, y);
    } catch (h) {
      return o(h instanceof Error ? h : new Error(String(h))), { success: !1, error: h instanceof Error ? h : new Error(String(h)) };
    }
  };
  return n || !t || !l ? {
    config: {
      performanceConfig: { enabled: !1 },
      securityConfig: { enabled: !1 },
      monitoringConfig: { enabled: !1 },
      integrationConfig: { enabled: !1 },
      errorHandlingConfig: { enabled: !1 },
      executionStatistics: { totalExecutions: 0, successfulExecutions: 0, failedExecutions: 0, totalExecutionTime: 0 },
      errorStatistics: { totalErrors: 0, errorsByType: {}, lastError: null },
      validationHistory: [],
      performanceProfiles: {},
      securityProfiles: {}
    },
    plugin: Sm(),
    isLoading: !0,
    error: i || new Error("Loading configuration..."),
    updateConfig: () => !1,
    resetConfig: () => !1,
    validateAll: () => ({ performance: !1, security: !1, monitoring: !1, integration: !1, error_handling: !1 }),
    executeWithEnhancedFeatures: async () => ({ success: !1, error: new Error("Loading...") })
  } : {
    config: t,
    plugin: l,
    isLoading: !1,
    error: i,
    updateConfig: u,
    resetConfig: d,
    validateAll: f,
    executeWithEnhancedFeatures: p
  };
}
class Oz {
  constructor(t = {}) {
    Z(this, "results", []);
    Z(this, "maxResults", 1e3);
    // Limit memory usage
    Z(this, "enableMemoryTracking", !0);
    this.maxResults = t.maxResults || 1e3, this.enableMemoryTracking = t.enableMemoryTracking !== !1;
  }
  /**
   * Measure execution time and memory usage of a function
   */
  async measure(t, r) {
    const n = performance.now(), a = this.enableMemoryTracking ? this.getMemoryUsage() : { used: 0, total: 0, free: 0, percentage: 0 };
    try {
      const i = await r(), o = performance.now(), l = this.enableMemoryTracking ? this.getMemoryUsage() : { used: 0, total: 0, free: 0, percentage: 0 }, c = o - n, u = this.enableMemoryTracking ? l.used - a.used : 0;
      return this.results.push({
        id: ht(),
        name: t,
        executionTime: c,
        memoryUsage: u,
        timestamp: Date.now()
      }), this.results.length > this.maxResults && (this.results = this.results.slice(-this.maxResults)), {
        result: i,
        metrics: { executionTime: c, memoryUsage: u }
      };
    } catch (i) {
      throw F.error(`Benchmark failed for ${t}: ${i instanceof Error ? i.message : String(i)}`), i;
    }
  }
  /**
   * Compare multiple approaches with optional parallel execution
   */
  async compare(t, r, n = {}) {
    const { parallel: a = !1, maxConcurrent: i = 3 } = n, o = [];
    if (a && r.length > 1) {
      const u = [];
      for (let d = 0; d < r.length; d += i)
        u.push(r.slice(d, d + i));
      for (const d of u) {
        const f = d.map(
          (m) => this.measure(m.name, m.fn).then(({ result: y, metrics: h }) => ({ ...h, name: m.name, result: y })).catch((y) => ({
            name: m.name,
            executionTime: -1,
            memoryUsage: -1,
            result: null,
            error: y instanceof Error ? y.message : String(y)
          }))
        ), p = await Promise.all(f);
        o.push(...p);
      }
    } else
      for (const u of r)
        try {
          const { result: d, metrics: f } = await this.measure(u.name, u.fn);
          o.push({ ...f, name: u.name, result: d });
        } catch (d) {
          o.push({
            name: u.name,
            executionTime: -1,
            memoryUsage: -1,
            result: null,
            error: d instanceof Error ? d.message : String(d)
          });
        }
    const l = o.filter((u) => u.executionTime >= 0);
    return { winner: l.length > 0 ? l.reduce(
      (u, d) => d.executionTime < u.executionTime || d.executionTime === u.executionTime && d.memoryUsage < u.memoryUsage ? d : u
    ).name : "None", results: o };
  }
  /**
   * Get all benchmark results
   */
  getResults() {
    return [...this.results];
  }
  /**
   * Clear benchmark results
   */
  clearResults() {
    this.results = [];
  }
  /**
   * Calculate performance improvement percentage
   */
  calculateImprovement(t, r) {
    return t <= 0 || r < 0 ? 0 : (t - r) / t * 100;
  }
  /**
   * Get performance statistics and insights
   */
  getPerformanceStatistics() {
    if (this.results.length === 0)
      return {
        totalBenchmarks: 0,
        averageExecutionTime: 0,
        averageMemoryUsage: 0
      };
    const t = this.results.filter((l) => l.executionTime >= 0), r = t.reduce((l, c) => l + c.executionTime, 0), n = t.reduce((l, c) => l + c.memoryUsage, 0), a = t.reduce(
      (l, c) => c.executionTime < l.executionTime ? c : l
    ), i = t.reduce(
      (l, c) => c.executionTime > l.executionTime ? c : l
    ), o = t.reduce(
      (l, c) => c.memoryUsage > l.memoryUsage ? c : l
    );
    return {
      totalBenchmarks: this.results.length,
      averageExecutionTime: r / t.length,
      averageMemoryUsage: n / t.length,
      fastestBenchmark: a ? { name: a.name, executionTime: a.executionTime } : void 0,
      slowestBenchmark: i ? { name: i.name, executionTime: i.executionTime } : void 0,
      memoryIntensiveBenchmark: o ? { name: o.name, memoryUsage: o.memoryUsage } : void 0
    };
  }
  /**
   * Get memory usage with fallback for different environments
   */
  getMemoryUsage() {
    if (typeof performance < "u" && performance.memory) {
      const t = performance.memory, r = t.usedJSHeapSize / 1024 / 1024, n = t.jsHeapSizeLimit / 1024 / 1024, a = n - r, i = r / n * 100;
      return { used: r, total: n, free: a, percentage: i };
    } else if (typeof process < "u" && process.memoryUsage) {
      const t = process.memoryUsage(), r = t.heapUsed / 1024 / 1024, n = t.heapTotal / 1024 / 1024, a = n - r, i = r / n * 100;
      return { used: r, total: n, free: a, percentage: i };
    } else
      return { used: 0, total: 1024, free: 1024, percentage: 0 };
  }
  /**
   * Clear all benchmark results to free memory
   */
  clearAllResults() {
    this.results = [];
  }
  /**
   * Get results within a specific time window
   */
  getRecentResults(t = 36e5) {
    const r = Date.now();
    return this.results.filter((n) => r - n.timestamp <= t);
  }
  /**
   * Export results to JSON for analysis
   */
  exportResultsToJson(t = !0) {
    return t ? JSON.stringify(this.results, null, 2) : JSON.stringify(this.results);
  }
  /**
   * Import results from JSON
   */
  importResultsFromJson(t) {
    try {
      const r = JSON.parse(t);
      if (Array.isArray(r)) {
        const n = r.filter(
          (a) => a.id && a.name && typeof a.executionTime == "number"
        );
        this.results = [...this.results, ...n].slice(-this.maxResults);
      }
    } catch (r) {
      F.error(`Failed to import benchmark results: ${r instanceof Error ? r.message : String(r)}`);
    }
  }
}
class Nz {
  /**
   * Generate secure random token
   */
  static generateSecureToken(t = 32) {
    const r = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
    let n = "";
    if (typeof crypto < "u" && crypto.getRandomValues) {
      const a = new Uint32Array(t);
      crypto.getRandomValues(a);
      for (let i = 0; i < t; i++)
        n += r[a[i] % r.length];
    } else
      for (let a = 0; a < t; a++)
        n += r[Math.floor(Math.random() * r.length)];
    return n;
  }
  /**
   * Hash data using SHA-256 (browser implementation)
   */
  static async hashData(t) {
    if (typeof crypto < "u" && crypto.subtle) {
      const n = new TextEncoder().encode(t), a = await crypto.subtle.digest("SHA-256", n);
      return Array.from(new Uint8Array(a)).map((o) => o.toString(16).padStart(2, "0")).join("");
    }
    return this.simpleHash(t);
  }
  /**
   * Simple hash function (fallback)
   */
  static simpleHash(t) {
    let r = 0;
    for (let n = 0; n < t.length; n++) {
      const a = t.charCodeAt(n);
      r = (r << 5) - r + a, r = r & r;
    }
    return r.toString(16);
  }
  /**
   * Validate password strength
   */
  static validatePasswordStrength(t) {
    let r = 0;
    const n = [];
    t.length >= 12 ? r += 2 : t.length >= 8 ? (r += 1, n.push("Password should be at least 12 characters")) : n.push("Password is too short (minimum 8 characters)"), /[A-Z]/.test(t) ? r += 1 : n.push("Add uppercase letters"), /[a-z]/.test(t) ? r += 1 : n.push("Add lowercase letters"), /[0-9]/.test(t) ? r += 1 : n.push("Add numbers"), /[^A-Za-z0-9]/.test(t) ? r += 2 : n.push("Add special characters"), ["password", "123456", "qwerty", "admin", "welcome"].some((o) => t.toLowerCase().includes(o)) && (r -= 2, n.push("Avoid common password patterns")), /(.)\1{2,}/.test(t) && (r -= 1, n.push("Avoid repeated characters"));
    const i = r >= 6;
    return { score: r, feedback: n, isStrong: i };
  }
  /**
   * Generate JWT-like token (simplified)
   */
  static generateJwtLikeToken(t, r, n = 3600) {
    const a = JSON.stringify({ alg: "HS256", typ: "JWT" }), i = JSON.stringify({
      ...t,
      iat: Math.floor(Date.now() / 1e3),
      exp: Math.floor(Date.now() / 1e3) + n
    }), o = (c) => btoa(c).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, ""), l = this.simpleHash(a + "." + i + r);
    return [a, i, l].map(o).join(".");
  }
  /**
   * Validate JWT-like token (simplified)
   */
  static validateJwtLikeToken(t, r) {
    try {
      const n = t.split(".");
      if (n.length !== 3)
        return { valid: !1, error: "Invalid token format" };
      const [a, i, o] = n, l = this.simpleHash(a + "." + i + r);
      if (o !== l)
        return { valid: !1, error: "Invalid signature" };
      try {
        const c = JSON.parse(atob(i.replace(/-/g, "+").replace(/_/g, "/")));
        return c.exp && c.exp < Math.floor(Date.now() / 1e3) ? { valid: !1, error: "Token expired" } : { valid: !0, payload: c };
      } catch {
        return { valid: !1, error: "Invalid payload" };
      }
    } catch (n) {
      return { valid: !1, error: n instanceof Error ? n.message : "Unknown error" };
    }
  }
}
class Mz {
  constructor() {
    Z(this, "alerts", []);
  }
  /**
   * Check metric against threshold and create alert if needed
   */
  checkMetricAndAlert(t, r, n, a = "medium") {
    if (r > n) {
      const o = {
        id: ht(),
        metric: t,
        value: r,
        threshold: n,
        severity: a,
        timestamp: Date.now(),
        resolved: !1
      };
      switch (this.alerts.push(o), a) {
        case "critical":
          F.error(`🚨 CRITICAL ALERT: ${t} = ${r} (threshold: ${n})`);
          break;
        case "high":
          F.error(`⚠️ HIGH ALERT: ${t} = ${r} (threshold: ${n})`);
          break;
        case "medium":
          F.warn(`⚠️ MEDIUM ALERT: ${t} = ${r} (threshold: ${n})`);
          break;
        case "low":
          F.info(`ℹ️ LOW ALERT: ${t} = ${r} (threshold: ${n})`);
          break;
      }
      return !0;
    }
    return !1;
  }
  /**
   * Resolve an alert
   */
  resolveAlert(t) {
    const r = this.alerts.findIndex((n) => n.id === t);
    return r !== -1 ? (this.alerts[r].resolved = !0, F.success(`✅ Alert resolved: ${this.alerts[r].metric}`), !0) : !1;
  }
  /**
   * Get active alerts
   */
  getActiveAlerts() {
    return this.alerts.filter((t) => !t.resolved);
  }
  /**
   * Get alert history
   */
  getAlertHistory() {
    return [...this.alerts];
  }
  /**
   * Clear resolved alerts
   */
  clearResolvedAlerts() {
    this.alerts = this.alerts.filter((t) => !t.resolved);
  }
  /**
   * Calculate alert frequency
   */
  getAlertFrequency(t, r = 36e5) {
    const n = Date.now();
    return this.alerts.filter(
      (a) => a.metric === t && a.timestamp >= n - r
    ).length;
  }
  /**
   * Check if system is in alert storm
   */
  isAlertStorm(t = 3e5, r = 10) {
    const n = Date.now();
    return this.alerts.filter(
      (i) => i.timestamp >= n - t
    ).length >= r;
  }
}
class Rz {
  // Limit cache size to prevent memory issues
  constructor(t = {}) {
    Z(this, "apiCache", /* @__PURE__ */ new Map());
    Z(this, "cacheStats", { hits: 0, misses: 0, lastCleared: Date.now() });
    Z(this, "maxCacheSize", 100);
    this.maxCacheSize = t.maxCacheSize || 100;
  }
  /**
   * Smart API caller with caching and retry logic
   */
  async smartApiCall(t, r = {}) {
    const {
      method: n = "GET",
      headers: a = { "Content-Type": "application/json" },
      body: i,
      cacheTtl: o = 3e5,
      // 5 minutes default
      maxRetries: l = 3,
      retryDelay: c = 1e3,
      timeout: u = 1e4,
      useExponentialBackoff: d = !0
    } = r, f = `${n}:${t}:${i ? JSON.stringify(i) : ""}`;
    if (n === "GET" && o > 0) {
      const g = this.apiCache.get(f);
      if (g && Date.now() - g.timestamp < g.ttl)
        return this.cacheStats.hits++, {
          success: !0,
          data: g.data,
          fromCache: !0,
          retryCount: 0
        };
      this.cacheStats.misses++;
    }
    let p = 0, m = null, y = c;
    const h = Date.now();
    for (; p <= l; ) {
      if (Date.now() - h > u) {
        m = `Request timed out after ${u}ms`;
        break;
      }
      try {
        const g = await this.simulateApiCall(t, { method: n, headers: a, body: i });
        return n === "GET" && o > 0 && (this.apiCache.size >= this.maxCacheSize && this.cleanupCache(), this.apiCache.set(f, {
          data: g.data,
          timestamp: Date.now(),
          ttl: o
        })), {
          success: !0,
          data: g.data,
          fromCache: !1,
          retryCount: p
        };
      } catch (g) {
        m = g instanceof Error ? g.message : String(g), p++, p <= l && (d && (y = c * Math.pow(2, p - 1), y = Math.min(y, 1e4)), F.warn(`API call failed (attempt ${p}/${l}), retrying in ${y}ms...`), await new Promise((v) => setTimeout(v, y)));
      }
    }
    return {
      success: !1,
      error: m || "Unknown error",
      retryCount: l
    };
  }
  /**
   * Simulate API call (replace with actual fetch/axios in production)
   */
  async simulateApiCall(t, r) {
    if (await new Promise((n) => setTimeout(n, 500)), t.includes("error"))
      throw new Error("Simulated API error");
    return t.includes("slow") && await new Promise((n) => setTimeout(n, 2e3)), {
      data: {
        url: t,
        method: r.method,
        timestamp: Date.now(),
        success: !0
      }
    };
  }
  /**
   * Clear API cache
   */
  clearApiCache() {
    this.apiCache.clear();
  }
  /**
   * Get cache statistics
   */
  getCacheStats() {
    return {
      size: this.apiCache.size,
      hitRate: 0
      // Would need to track hits/misses in real implementation
    };
  }
  /**
   * Batch API calls with rate limiting
   */
  async batchApiCalls(t, r = 5, n = 1e3) {
    const a = [];
    for (let i = 0; i < t.length; i += r) {
      const l = t.slice(i, i + r).map(
        (u) => this.smartApiCall(u.url, u.options).then((d) => ({
          url: u.url,
          success: d.success,
          data: d.data,
          error: d.error
        })).catch((d) => ({
          url: u.url,
          success: !1,
          error: d instanceof Error ? d.message : String(d)
        }))
      ), c = await Promise.all(l);
      a.push(...c), i + r < t.length && await new Promise((u) => setTimeout(u, n));
    }
    return a;
  }
}
class Iz {
  constructor() {
    Z(this, "errorHistory", []);
  }
  /**
   * Analyze error and add to history
   */
  analyzeError(t, r = "unknown", n = "medium", a = {}) {
    const i = ht(), o = Date.now();
    let l = "UnknownError", c = "Unknown error", u;
    t instanceof Error ? (l = t.name, c = t.message, u = t.stack) : typeof t == "string" ? c = t : t && typeof t == "object" && (l = "ObjectError", c = JSON.stringify(t));
    const d = {
      id: i,
      errorType: l,
      errorMessage: c,
      context: r,
      severity: n,
      timestamp: o,
      stackTrace: u,
      metadata: a
    };
    switch (this.errorHistory.push(d), n) {
      case "critical":
        console.error("🚨 CRITICAL ERROR:", d), F.error(`🚨 CRITICAL: ${c}`);
        break;
      case "high":
        console.error("❌ HIGH ERROR:", d), F.error(`❌ HIGH: ${c}`);
        break;
      case "medium":
        console.warn("⚠️ MEDIUM ERROR:", d), F.warn(`⚠️ MEDIUM: ${c}`);
        break;
      case "low":
        console.log("ℹ️ LOW ERROR:", d), F.info(`ℹ️ LOW: ${c}`);
        break;
    }
    return i;
  }
  /**
   * Get error patterns and trends
   */
  getErrorPatterns(t = 864e5) {
    const r = Date.now(), n = this.errorHistory.filter(
      (l) => l.timestamp >= r - t
    ), a = {}, i = {}, o = {};
    return n.forEach((l) => {
      a[l.errorType] = (a[l.errorType] || 0) + 1, i[l.context] = (i[l.context] || 0) + 1, o[l.severity] = (o[l.severity] || 0) + 1;
    }), {
      byType: a,
      byContext: i,
      bySeverity: o,
      total: n.length
    };
  }
  /**
   * Get most common errors
   */
  getMostCommonErrors(t = 5) {
    const r = this.getErrorPatterns();
    return Object.entries(r.byType).sort((a, i) => i[1] - a[1]).slice(0, t).map(([a, i]) => ({
      errorType: a,
      count: i,
      percentage: i / r.total * 100
    }));
  }
  /**
   * Detect error trends (increasing frequency)
   */
  detectErrorTrends(t, r = [36e5, 864e5, 6048e5]) {
    const n = Date.now(), a = r.map((o) => {
      const l = this.errorHistory.filter(
        (u) => u.errorType === t && u.timestamp >= n - o
      ).length, c = l / (o / 36e5);
      return { windowMs: o, count: l, rate: c };
    });
    return { isTrending: a.length >= 2 && a[0].rate > a[1].rate * 1.5, trendData: a };
  }
  /**
   * Get error history
   */
  getErrorHistory(t = 100) {
    return [...this.errorHistory].slice(-t);
  }
  /**
   * Clear error history
   */
  clearErrorHistory() {
    this.errorHistory = [];
  }
  /**
   * Get error severity distribution
   */
  getSeverityDistribution() {
    const t = {
      low: 0,
      medium: 0,
      high: 0,
      critical: 0
    };
    return this.errorHistory.forEach((r) => {
      t[r.severity]++;
    }), t;
  }
}
class Dz {
  /**
   * Deep merge configuration objects
   */
  static deepMerge(t, r) {
    if (typeof t != "object" || t === null) return r;
    if (typeof r != "object" || r === null) return t;
    const n = { ...t };
    for (const a in r)
      r.hasOwnProperty(a) && (typeof r[a] == "object" && r[a] !== null && typeof t[a] == "object" && t[a] !== null ? n[a] = this.deepMerge(t[a], r[a]) : n[a] = r[a]);
    return n;
  }
  /**
   * Validate configuration against schema
   */
  static validateConfig(t, r) {
    const n = [];
    for (const a in r)
      if (r.hasOwnProperty(a)) {
        const i = r[a];
        if (i.required && (t[a] === void 0 || t[a] === null) && n.push(`Missing required field: ${a}`), t[a] !== void 0 && i.type) {
          const o = typeof t[a];
          o !== i.type && n.push(`Invalid type for ${a}: expected ${i.type}, got ${o}`);
        }
        t[a] !== void 0 && i.min !== void 0 && t[a] < i.min && n.push(`${a} must be at least ${i.min}, got ${t[a]}`), t[a] !== void 0 && i.max !== void 0 && t[a] > i.max && n.push(`${a} must be at most ${i.max}, got ${t[a]}`);
      }
    return {
      valid: n.length === 0,
      errors: n
    };
  }
  /**
   * Generate configuration diff
   */
  static generateConfigDiff(t, r) {
    const n = [], a = (i, o, l) => {
      for (const c in l)
        o.hasOwnProperty(c) || n.push({
          path: i ? `${i}.${c}` : c,
          oldValue: void 0,
          newValue: l[c],
          changeType: "added"
        });
      for (const c in o)
        l.hasOwnProperty(c) || n.push({
          path: i ? `${i}.${c}` : c,
          oldValue: o[c],
          newValue: void 0,
          changeType: "removed"
        });
      for (const c in o)
        if (l.hasOwnProperty(c)) {
          const u = o[c], d = l[c];
          typeof u == "object" && u !== null && typeof d == "object" && d !== null ? a(i ? `${i}.${c}` : c, u, d) : u !== d && n.push({
            path: i ? `${i}.${c}` : c,
            oldValue: u,
            newValue: d,
            changeType: "changed"
          });
        }
    };
    return a("", t, r), n;
  }
  /**
   * Sanitize configuration (remove sensitive data)
   */
  static sanitizeConfig(t, r = ["password", "secret", "token", "apiKey"]) {
    if (typeof t != "object" || t === null) return t;
    const n = { ...t };
    for (const a in n)
      r.some((i) => a.toLowerCase().includes(i.toLowerCase())) ? n[a] = "***REDACTED***" : typeof n[a] == "object" && (n[a] = this.sanitizeConfig(n[a], r));
    return n;
  }
  /**
   * Export configuration to JSON
   */
  static exportConfigToJson(t, r = !0) {
    return r ? JSON.stringify(t, null, 2) : JSON.stringify(t);
  }
  /**
   * Import configuration from JSON
   */
  static importConfigFromJson(t) {
    try {
      return JSON.parse(t);
    } catch (r) {
      return F.error(`Failed to parse JSON: ${r instanceof Error ? r.message : String(r)}`), null;
    }
  }
}
class ZU {
  constructor() {
    Z(this, "errorPatterns", /* @__PURE__ */ new Map());
    this.initializeDefaultPatterns();
  }
  /**
   * Initialize default error patterns
   */
  initializeDefaultPatterns() {
    this.addPattern("network_error", {
      pattern: (t) => {
        var r, n, a;
        return ((r = t.message) == null ? void 0 : r.includes("network")) || ((n = t.message) == null ? void 0 : n.includes("connection")) || ((a = t.message) == null ? void 0 : a.includes("timeout")) || t.code === "ECONNABORTED" || t.code === "ENOTFOUND";
      },
      category: "network_error",
      severity: "high",
      description: "Network connectivity issues",
      recoverySuggestions: [
        "Check internet connection",
        "Verify API endpoint availability",
        "Implement retry logic with exponential backoff",
        "Check firewall and network settings"
      ]
    }), this.addPattern("authentication_error", {
      pattern: (t) => {
        var r, n, a, i;
        return ((r = t.message) == null ? void 0 : r.includes("authentication")) || ((n = t.message) == null ? void 0 : n.includes("unauthorized")) || ((a = t.message) == null ? void 0 : a.includes("401")) || ((i = t.message) == null ? void 0 : i.includes("token")) || t.code === 401;
      },
      category: "authentication_error",
      severity: "high",
      description: "Authentication or authorization failures",
      recoverySuggestions: [
        "Verify credentials",
        "Check token expiration",
        "Renew authentication token",
        "Verify user permissions"
      ]
    }), this.addPattern("validation_error", {
      pattern: (t) => {
        var r, n, a, i;
        return ((r = t.message) == null ? void 0 : r.includes("validation")) || ((n = t.message) == null ? void 0 : n.includes("invalid")) || ((a = t.message) == null ? void 0 : a.includes("required")) || ((i = t.message) == null ? void 0 : i.includes("format")) || t.code === 422;
      },
      category: "validation_error",
      severity: "medium",
      description: "Input validation failures",
      recoverySuggestions: [
        "Check input data format",
        "Validate required fields",
        "Review data constraints",
        "Provide user-friendly error messages"
      ]
    }), this.addPattern("rate_limit_error", {
      pattern: (t) => {
        var r, n, a;
        return ((r = t.message) == null ? void 0 : r.includes("rate limit")) || ((n = t.message) == null ? void 0 : n.includes("too many requests")) || ((a = t.message) == null ? void 0 : a.includes("429")) || t.code === 429;
      },
      category: "rate_limit_error",
      severity: "medium",
      description: "API rate limiting exceeded",
      recoverySuggestions: [
        "Implement exponential backoff",
        "Check rate limit policies",
        "Optimize API calls",
        "Consider upgrading API plan"
      ]
    }), this.addPattern("database_error", {
      pattern: (t) => {
        var r, n, a, i, o, l;
        return ((r = t.message) == null ? void 0 : r.includes("database")) || ((n = t.message) == null ? void 0 : n.includes("query")) || ((a = t.message) == null ? void 0 : a.includes("connection")) || ((i = t.message) == null ? void 0 : i.includes("SQL")) || ((o = t.message) == null ? void 0 : o.includes("Mongo")) || ((l = t.message) == null ? void 0 : l.includes("collection"));
      },
      category: "database_error",
      severity: "critical",
      description: "Database operation failures",
      recoverySuggestions: [
        "Check database connection",
        "Verify query syntax",
        "Review database logs",
        "Implement transaction rollback"
      ]
    }), this.addPattern("memory_error", {
      pattern: (t) => {
        var r, n, a, i;
        return ((r = t.message) == null ? void 0 : r.includes("memory")) || ((n = t.message) == null ? void 0 : n.includes("heap")) || ((a = t.message) == null ? void 0 : a.includes("out of memory")) || ((i = t.message) == null ? void 0 : i.includes("allocation"));
      },
      category: "memory_error",
      severity: "critical",
      description: "Memory allocation failures",
      recoverySuggestions: [
        "Increase memory limits",
        "Optimize memory usage",
        "Implement garbage collection",
        "Review memory-intensive operations"
      ]
    }), this.addPattern("timeout_error", {
      pattern: (t) => {
        var r, n;
        return ((r = t.message) == null ? void 0 : r.includes("timeout")) || ((n = t.message) == null ? void 0 : n.includes("timed out")) || t.code === "ETIMEDOUT";
      },
      category: "timeout_error",
      severity: "high",
      description: "Operation timeout",
      recoverySuggestions: [
        "Increase timeout values",
        "Optimize slow operations",
        "Implement retry logic",
        "Check network latency"
      ]
    }), this.addPattern("configuration_error", {
      pattern: (t) => {
        var r, n, a, i;
        return ((r = t.message) == null ? void 0 : r.includes("configuration")) || ((n = t.message) == null ? void 0 : n.includes("config")) || ((a = t.message) == null ? void 0 : a.includes("setting")) || ((i = t.message) == null ? void 0 : i.includes("environment"));
      },
      category: "configuration_error",
      severity: "high",
      description: "Configuration issues",
      recoverySuggestions: [
        "Verify configuration files",
        "Check environment variables",
        "Review default settings",
        "Validate configuration schema"
      ]
    });
  }
  /**
   * Add custom error pattern
   */
  addPattern(t, r) {
    this.errorPatterns.set(t, r);
  }
  /**
   * Remove error pattern
   */
  removePattern(t) {
    return this.errorPatterns.delete(t);
  }
  /**
   * Classify error using pattern matching
   */
  classifyError(t) {
    let r = {
      category: "general_error",
      severity: "medium",
      description: "General error",
      recoverySuggestions: ["Check error details", "Review logs", "Contact support"],
      confidence: 0.5
    };
    return this.errorPatterns.forEach((n) => {
      let a = !1;
      if (n.pattern instanceof RegExp ? a = n.pattern.test(t.message || t.toString()) : typeof n.pattern == "function" && (a = n.pattern(t)), a) {
        const i = this.calculateConfidence(n, t);
        i > r.confidence && (r = {
          category: n.category,
          severity: n.severity,
          description: n.description,
          recoverySuggestions: n.recoverySuggestions,
          confidence: i
        });
      }
    }), r;
  }
  /**
   * Calculate confidence score for pattern match
   */
  calculateConfidence(t, r) {
    if (t.pattern instanceof RegExp) {
      const a = (r.message || r.toString()).match(t.pattern);
      return a ? a.length / 10 : 0.1;
    }
    return 0.8;
  }
  /**
   * Get all error patterns
   */
  getAllPatterns() {
    return new Map(this.errorPatterns);
  }
}
class ez {
  constructor() {
    Z(this, "recoveryStrategies", /* @__PURE__ */ new Map());
    this.initializeDefaultStrategies();
  }
  /**
   * Initialize default recovery strategies
   */
  initializeDefaultStrategies() {
    this.addStrategy("retry", {
      strategy: async (t, r = {}) => {
        const n = r.maxRetries || 3, a = r.retryDelay || 1e3, i = r.currentRetry || 0;
        return i < n ? (F.info(`Attempting retry ${i + 1}/${n}...`), await new Promise((o) => setTimeout(o, a)), !0) : !1;
      },
      applicableCategories: ["network_error", "timeout_error", "rate_limit_error"],
      description: "Retry the failed operation with exponential backoff"
    }), this.addStrategy("fallback", {
      strategy: async (t, r = {}) => {
        if (r.fallbackFunction && typeof r.fallbackFunction == "function")
          try {
            return F.info("Attempting fallback operation..."), await r.fallbackFunction(), !0;
          } catch {
            return F.error("Fallback operation failed"), !1;
          }
        return !1;
      },
      applicableCategories: ["network_error", "database_error", "api_error"],
      description: "Execute fallback operation when primary fails"
    }), this.addStrategy("cache", {
      strategy: async (t, r = {}) => r.cache && r.cacheKey && r.cache.get(r.cacheKey) ? (F.info("Using cached data as fallback"), !0) : !1,
      applicableCategories: ["network_error", "api_error", "database_error"],
      description: "Use cached data when fresh data is unavailable"
    }), this.addStrategy("circuit_breaker", {
      strategy: async (t, r = {}) => {
        const n = r.failureThreshold || 5, a = r.resetTimeout || 3e4;
        r.circuitState || (r.circuitState = { failures: 0, lastFailure: 0, isOpen: !1 });
        const i = Date.now();
        return r.circuitState.isOpen ? i - r.circuitState.lastFailure > a ? (r.circuitState.failures = 0, r.circuitState.isOpen = !1, F.info("Circuit breaker reset"), !0) : (F.warn("Circuit breaker is open - operation skipped"), !1) : (r.circuitState.failures++, r.circuitState.lastFailure = i, r.circuitState.failures >= n && (r.circuitState.isOpen = !0, F.error("Circuit breaker opened due to repeated failures")), !0);
      },
      applicableCategories: ["network_error", "database_error", "api_error"],
      description: "Circuit breaker pattern to prevent cascading failures"
    }), this.addStrategy("graceful_degradation", {
      strategy: async (t, r = {}) => {
        if (r.degradedFunction && typeof r.degradedFunction == "function")
          try {
            return F.info("Switching to degraded functionality"), await r.degradedFunction(), !0;
          } catch {
            return F.error("Degraded functionality also failed"), !1;
          }
        return !1;
      },
      applicableCategories: ["network_error", "database_error", "api_error", "performance_error"],
      description: "Switch to degraded functionality when full functionality fails"
    });
  }
  /**
   * Add custom recovery strategy
   */
  addStrategy(t, r) {
    this.recoveryStrategies.set(t, r);
  }
  /**
   * Remove recovery strategy
   */
  removeStrategy(t) {
    return this.recoveryStrategies.delete(t);
  }
  /**
   * Attempt error recovery using appropriate strategies
   */
  async attemptRecovery(t, r, n = {}) {
    const a = [];
    let i = t;
    const o = Array.from(this.recoveryStrategies.entries()).filter(
      ([l, c]) => c.applicableCategories.includes(r.category) || c.applicableCategories.includes("*")
    );
    o.sort((l, c) => {
      const u = l[1].applicableCategories.includes(r.category) ? 1 : 0;
      return (c[1].applicableCategories.includes(r.category) ? 1 : 0) - u;
    });
    for (const [l, c] of o)
      try {
        a.push(l), F.info(`Attempting recovery strategy: ${c.description}`);
        const u = { ...n, error: t, classification: r };
        if (await c.strategy(t, u))
          return { success: !0, strategiesAttempted: a };
      } catch (u) {
        F.error(`Recovery strategy failed: ${c.description}`), i = u, a.push(`${l}_failed`);
      }
    return {
      success: !1,
      strategiesAttempted: a,
      finalError: i
    };
  }
  /**
   * Get recovery strategy suggestions for error category
   */
  getRecoverySuggestions(t) {
    const r = [];
    return this.recoveryStrategies.forEach((n) => {
      (n.applicableCategories.includes(t) || n.applicableCategories.includes("*")) && r.push(n.description);
    }), r;
  }
  /**
   * Get all recovery strategies
   */
  getAllStrategies() {
    return new Map(this.recoveryStrategies);
  }
}
class tz {
  constructor() {
    Z(this, "errorQueue", []);
    Z(this, "reportingDestinations", /* @__PURE__ */ new Map());
    this.initializeDefaultDestinations();
  }
  /**
   * Initialize default reporting destinations
   */
  initializeDefaultDestinations() {
    this.addDestination("console", {
      reporter: async (t) => (console.error("Error Report:", t), !0),
      enabled: !0,
      maxRetries: 0
    }), this.addDestination("api", {
      reporter: async (t) => (await new Promise((r) => setTimeout(r, 500)), console.log("Reported to API:", t), !0),
      enabled: !1,
      // Disabled by default
      maxRetries: 3
    }), this.addDestination("email", {
      reporter: async (t) => (await new Promise((r) => setTimeout(r, 1e3)), console.log("Sent error email:", t), !0),
      enabled: !1,
      // Disabled by default
      maxRetries: 2
    }), this.addDestination("database", {
      reporter: async (t) => (await new Promise((r) => setTimeout(r, 300)), console.log("Stored error in database:", t), !0),
      enabled: !0,
      maxRetries: 3
    });
  }
  /**
   * Add reporting destination
   */
  addDestination(t, r) {
    this.reportingDestinations.set(t, r);
  }
  /**
   * Remove reporting destination
   */
  removeDestination(t) {
    return this.reportingDestinations.delete(t);
  }
  /**
   * Enable/disable reporting destination
   */
  setDestinationEnabled(t, r) {
    const n = this.reportingDestinations.get(t);
    return n ? (n.enabled = r, !0) : !1;
  }
  /**
   * Queue error for reporting
   */
  queueError(t) {
    const r = ht();
    return this.errorQueue.push({
      errorId: r,
      errorData: t,
      timestamp: Date.now(),
      retries: 0
    }), F.info(`Error queued for reporting (ID: ${r})`), r;
  }
  /**
   * Process error queue
   */
  async processQueue() {
    let t = 0, r = 0;
    for (; this.errorQueue.length > 0; ) {
      const n = this.errorQueue[0], { errorId: a, errorData: i, retries: o } = n;
      try {
        if ((await this.reportToDestinations(i)).some((c) => c.success))
          this.errorQueue.shift(), t++, F.success(`Error reported successfully (ID: ${a})`);
        else if (o >= this.getMaxRetriesForError(i))
          this.errorQueue.shift(), r++, F.error(`Failed to report error after max retries (ID: ${a})`);
        else {
          n.retries++, F.warn(`Retrying error report (attempt ${n.retries})`);
          break;
        }
      } catch (l) {
        F.error(`Error processing queue: ${l instanceof Error ? l.message : String(l)}`);
        break;
      }
    }
    return {
      successCount: t,
      failureCount: r,
      totalProcessed: t + r
    };
  }
  /**
   * Report error to all enabled destinations
   */
  async reportToDestinations(t) {
    const r = [];
    for (const [n, a] of this.reportingDestinations)
      if (a.enabled)
        try {
          const i = await a.reporter(t);
          r.push({ destination: n, success: i }), i ? F.success(`Reported to ${n} successfully`) : F.warn(`Failed to report to ${n}`);
        } catch (i) {
          r.push({
            destination: n,
            success: !1,
            error: i instanceof Error ? i.message : String(i)
          }), F.error(`Error reporting to ${n}: ${i instanceof Error ? i.message : String(i)}`);
        }
    return r;
  }
  /**
   * Get max retries for error based on destinations
   */
  getMaxRetriesForError(t) {
    let r = 0;
    return this.reportingDestinations.forEach((n) => {
      n.enabled && n.maxRetries > r && (r = n.maxRetries);
    }), r;
  }
  /**
   * Get queue status
   */
  getQueueStatus() {
    return {
      queueSize: this.errorQueue.length,
      oldestErrorTimestamp: this.errorQueue.length > 0 ? this.errorQueue[0].timestamp : void 0,
      destinations: Array.from(this.reportingDestinations.entries()).map(([t, r]) => ({
        id: t,
        enabled: r.enabled,
        maxRetries: r.maxRetries
      }))
    };
  }
  /**
   * Clear error queue
   */
  clearQueue() {
    this.errorQueue = [], F.info("Error queue cleared");
  }
  /**
   * Get all reporting destinations
   */
  getAllDestinations() {
    return new Map(this.reportingDestinations);
  }
}
class $z {
  constructor() {
    Z(this, "classifier");
    Z(this, "recovery");
    Z(this, "reporter");
    Z(this, "errorHistory", []);
    this.classifier = new ZU(), this.recovery = new ez(), this.reporter = new tz();
  }
  /**
   * Handle error with comprehensive processing
   */
  async handleError(t, r = {}, n = {}) {
    const { autoRecover: a = !0, autoReport: i = !0, metadata: o = {} } = n, l = ht(), c = Date.now(), u = this.classifier.classifyError(t);
    let d = null;
    if (a)
      try {
        d = await this.recovery.attemptRecovery(t, u, {
          ...r,
          errorId: l,
          classification: u,
          metadata: o
        });
      } catch (p) {
        console.error("Recovery failed:", p), d = {
          success: !1,
          strategiesAttempted: ["recovery_failed"],
          finalError: p
        };
      }
    let f = null;
    if (i)
      try {
        const p = this.prepareErrorData(t, u, d, r, o);
        this.reporter.queueError(p), u.severity === "critical" ? f = await this.reporter.processQueue() : f = { queued: !0 };
      } catch (p) {
        console.error("Reporting failed:", p), f = { success: !1, error: p };
      }
    return this.errorHistory.push({
      errorId: l,
      originalError: t,
      classification: u,
      recoveryAttempt: d,
      reportingStatus: f,
      timestamp: c
    }), this.notifyError(u, l), {
      errorId: l,
      classification: u,
      recoveryResult: d,
      reportingResult: f
    };
  }
  /**
   * Prepare error data for reporting
   */
  prepareErrorData(t, r, n, a, i) {
    return {
      errorId: ht(),
      timestamp: Date.now(),
      errorType: r.category,
      errorMessage: t.message || String(t),
      stackTrace: t.stack || null,
      severity: r.severity,
      context: JSON.stringify(a),
      classification: JSON.stringify(r),
      recoveryAttempt: n ? JSON.stringify({
        success: n.success,
        strategies: n.strategiesAttempted
      }) : null,
      metadata: JSON.stringify(i),
      userAgent: typeof navigator < "u" ? navigator.userAgent : "unknown",
      platform: typeof process < "u" ? process.platform : "unknown"
    };
  }
  /**
   * Notify error based on severity
   */
  notifyError(t, r) {
    const n = `Error: ${t.description} (${t.category})`;
    switch (t.severity) {
      case "critical":
        F.error(`🚨 CRITICAL: ${n} (ID: ${r})`), console.error("🚨 CRITICAL ERROR:", { classification: t, errorId: r });
        break;
      case "high":
        F.error(`❌ HIGH: ${n} (ID: ${r})`), console.error("❌ HIGH ERROR:", { classification: t, errorId: r });
        break;
      case "medium":
        F.warn(`⚠️ MEDIUM: ${n} (ID: ${r})`), console.warn("⚠️ MEDIUM ERROR:", { classification: t, errorId: r });
        break;
      case "low":
        F.info(`ℹ️ LOW: ${n} (ID: ${r})`), console.log("ℹ️ LOW ERROR:", { classification: t, errorId: r });
        break;
    }
  }
  /**
   * Process error queue
   */
  async processErrorQueue() {
    return this.reporter.processQueue();
  }
  /**
   * Get error history
   */
  getErrorHistory(t = 100) {
    return [...this.errorHistory].slice(-t);
  }
  /**
   * Clear error history
   */
  clearErrorHistory() {
    this.errorHistory = [];
  }
  /**
   * Get error statistics
   */
  getErrorStatistics() {
    const t = { low: 0, medium: 0, high: 0, critical: 0 }, r = {};
    let n = 0, a = 0;
    return this.errorHistory.forEach((i) => {
      var o, l;
      t[i.classification.severity]++, r[i.classification.category] = (r[i.classification.category] || 0) + 1, (o = i.recoveryAttempt) != null && o.success && n++, (l = i.reportingStatus) != null && l.successCount && a++;
    }), {
      totalErrors: this.errorHistory.length,
      bySeverity: t,
      byCategory: r,
      recoverySuccessRate: this.errorHistory.length > 0 ? n / this.errorHistory.length * 100 : 0,
      reportingSuccessRate: this.errorHistory.length > 0 ? a / this.errorHistory.length * 100 : 0
    };
  }
  /**
   * Get queue status
   */
  getQueueStatus() {
    return this.reporter.getQueueStatus();
  }
  /**
   * Add custom error pattern
   */
  addErrorPattern(t, r) {
    this.classifier.addPattern(t, r);
  }
  /**
   * Add custom recovery strategy
   */
  addRecoveryStrategy(t, r) {
    this.recovery.addStrategy(t, r);
  }
  /**
   * Add reporting destination
   */
  addReportingDestination(t, r) {
    this.reporter.addDestination(t, r);
  }
  /**
   * Get all components
   */
  getComponents() {
    return {
      classifier: this.classifier,
      recovery: this.recovery,
      reporter: this.reporter
    };
  }
}
function Lz(e, t, r) {
  const { NodeRegistry: n } = require("../nodes/registry");
  return n.create(e, t, r);
}
function Fz(e) {
  const { NodeRegistry: t } = require("../nodes/registry");
  return t.getMetadata(e);
}
function qz() {
  const { NodeRegistry: e } = require("../nodes/registry");
  return e.listAll();
}
function Uz() {
  const { NodeRegistry: e } = require("../nodes/registry");
  return e.getCategories();
}
function zz() {
  return {
    generations: 10,
    populationSize: 50,
    mutationRate: 0.1,
    crossoverRate: 0.8,
    selectionMethod: "tournament",
    elitismCount: 2,
    tournamentSize: 3,
    mutationStrategy: "gaussian",
    crossoverStrategy: "uniform"
  };
}
function Bz() {
  return {
    enabled: !0,
    attackStrategy: "fgsm",
    numExamples: 10,
    strength: 0.3,
    stepSize: 0.1,
    numSteps: 10,
    norm: "Linf",
    defenseStrategies: []
  };
}
function Wz() {
  return {
    strategy: "hierarchical",
    maxDepth: 3,
    pruningThreshold: 0.5,
    granularity: "medium",
    parallelDecomposition: !1,
    maxSubtasks: 10,
    dependencyAnalysis: !0,
    constraintPropagation: !0
  };
}
function Vz(e) {
  const t = [];
  return (!e.generations || e.generations < 1) && t.push("Generations must be at least 1"), (!e.populationSize || e.populationSize < 2) && t.push("Population size must be at least 2"), (e.mutationRate < 0 || e.mutationRate > 1) && t.push("Mutation rate must be between 0 and 1"), (e.crossoverRate < 0 || e.crossoverRate > 1) && t.push("Crossover rate must be between 0 and 1"), {
    valid: t.length === 0,
    errors: t
  };
}
function Hz(e) {
  const t = [];
  return e.attackStrategy || t.push("Attack strategy is required"), (!e.numExamples || e.numExamples < 1) && t.push("Number of examples must be at least 1"), (e.strength < 0 || e.strength > 1) && t.push("Strength must be between 0 and 1"), {
    valid: t.length === 0,
    errors: t
  };
}
function Gz(e) {
  const t = [];
  return e.strategy || t.push("Decomposition strategy is required"), (!e.maxDepth || e.maxDepth < 1) && t.push("Max depth must be at least 1"), (e.pruningThreshold < 0 || e.pruningThreshold > 1) && t.push("Pruning threshold must be between 0 and 1"), {
    valid: t.length === 0,
    errors: t
  };
}
function Kz(e) {
  return {
    debug: (t, r) => {
      process.env.NODE_ENV === "development" && console.debug(`[${e}] DEBUG:`, t, r || "");
    },
    info: (t, r) => {
      console.info(`[${e}] INFO:`, t, r || "");
    },
    warn: (t, r) => {
      console.warn(`[${e}] WARN:`, t, r || "");
    },
    error: (t, r) => {
      console.error(`[${e}] ERROR:`, t, r || "");
    }
  };
}
/**
 * OpenEvolve BubbleLab Plugin - Main Export
 *
 * Comprehensive plugin for integrating OpenEvolve workflow capabilities
 * with BubbleLab's visual programming interface.
 *
 * @package @openevolve/bubblelab-plugin
 * @version 1.0.0
 * @license MIT
 * @author OpenEvolve Team
 */
const Yz = "@openevolve/bubblelab-plugin", Xz = "1.0.0", Qz = {
  generations: 100,
  populationSize: 50,
  mutationRate: 0.1,
  crossoverRate: 0.8,
  selectionMethod: "tournament",
  elitismCount: 2,
  tournamentSize: 3
}, Jz = {
  enabled: !0,
  attackStrategy: "pgd",
  numExamples: 10,
  strength: 0.3,
  stepSize: 0.01,
  numSteps: 40
}, Zz = {
  strategy: "hierarchical",
  maxDepth: 5,
  pruningThreshold: 0.1,
  granularity: "medium",
  parallelDecomposition: !0
}, e6 = {
  knowledgeEngine: {
    enabled: !0,
    endpoint: "http://localhost:8080",
    timeout: 3e4
  },
  leanaide: {
    enabled: !0,
    endpoint: "http://localhost:8081",
    timeout: 6e4
  },
  crewai: {
    enabled: !0,
    endpoint: "http://localhost:8082",
    timeout: 3e4
  }
};
function t6(e) {
  return Kh(e);
}
function r6() {
  const e = window.__openevolve_plugin__;
  if (!e)
    throw new Error("OpenEvolve plugin not initialized. Call createPlugin() first.");
  return e;
}
function n6() {
  window.__openevolve_plugin__ && (window.__openevolve_plugin__.destroy(), delete window.__openevolve_plugin__);
}
export {
  ZU as AdvancedErrorClassifier,
  ez as AdvancedErrorRecovery,
  tz as AdvancedErrorReporter,
  qA as AdversarialConfigPanel,
  ma as AdversarialNode,
  kz as AdversarialPage,
  _z as AnalyticsDashboard,
  kU as ArtifactDetail,
  SU as ArtifactEditor,
  _U as ArtifactList,
  mU as ArtifactTable,
  Re as BubbleBadge,
  X as BubbleButton,
  he as BubbleCard,
  H as BubbleField,
  D as BubbleInput,
  je as BubbleSelect,
  Lt as BubbleTextArea,
  Ze as BubbleToggle,
  $z as ComprehensiveErrorHandler,
  CA as ConfigPanel,
  Dz as ConfigUtils,
  Jz as DEFAULT_ADVERSARIAL_CONFIG,
  Zz as DEFAULT_DECOMPOSITION_CONFIG,
  cg as DEFAULT_ENHANCED_OPENEVOLVE_CONFIG,
  Qz as DEFAULT_EVOLUTION_CONFIG,
  HE as DEFAULT_EXTENDED_OPENEVOLVE_CONFIG,
  e6 as DEFAULT_INTEGRATION_CONFIG,
  Rn as DEFAULT_OPENEVOLVE_CONFIG,
  GA as DecompositionConfigPanel,
  da as DecompositionNode,
  hi as ENHANCED_OPENEVOLVE_PLUGIN_CONSTANTS,
  qe as EXTENDED_OPENEVOLVE_PLUGIN_CONSTANTS,
  xz as EnhancedConfigPanel,
  vz as EnhancedOpenEvolveConfigPanel,
  Iz as ErrorAnalysisUtils,
  MA as EvolutionConfigPanel,
  fa as EvolutionNode,
  Sz as EvolutionPage,
  bj as ExecutionMonitor,
  Tz as FormWrapper,
  ga as CrewAINode,
  e1 as IntegrationConfigPanel,
  Rz as IntegrationUtils,
  jm as InventionNode,
  Az as KnowledgeBasePage,
  ha as KnowledgeQueryNode,
  jU as KnowledgeSearch,
  pa as LeanAideNode,
  Ez as LeanAidePage,
  _l as LiveLogViewer,
  va as MAKERNode,
  ya as MDAPNode,
  oC as MetricCard,
  gU as ModelSelector,
  Mz as MonitoringUtils,
  fn as NodeExecutionError,
  mr as NodeRegistry,
  SA as NodeStatus,
  ge as OPENEVOLVE_PLUGIN_CONSTANTS,
  lz as OpenEvolveBaseNode,
  bz as OpenEvolveConfigPanel,
  wz as OpenEvolveDashboard,
  vr as OpenEvolveNode,
  Yz as PLUGIN_NAME,
  Xz as PLUGIN_VERSION,
  Oz as PerformanceBenchmark,
  E_ as PerformanceChart,
  Jh as ProgressBar,
  vU as ProgressTracker,
  pU as ProofEditor,
  wl as ProviderSettingsPanel,
  _m as ROMANode,
  cz as Registry,
  Nz as SecurityUtils,
  bm as SolutionNode,
  sC as StatGrid,
  So as StatusBadge,
  aC as SystemHealthPanel,
  yU as VerificationDisplay,
  xm as VerificationNode,
  iC as WelcomeBanner,
  jz as WorkflowBuilder,
  u1 as WorkflowCard,
  Cz as WorkflowConfigPanel,
  d1 as WorkflowList,
  nC as WorkflowTabs,
  Bz as createDefaultAdversarialConfig,
  Wz as createDefaultDecompositionConfig,
  zz as createDefaultEvolutionConfig,
  X_ as createEnhancedOpenEvolvePlugin,
  Kz as createLogger,
  Lz as createNode,
  sz as createNodeFromConfig,
  sz as createNodeFromRegistryConfig,
  Kh as createOpenEvolvePlugin,
  t6 as createPlugin,
  uz as getAllNodeTypes,
  hz as getCategories,
  Sm as getEnhancedOpenEvolvePlugin,
  dz as getNode,
  Uz as getNodeCategories,
  Fz as getNodeMetadata,
  mz as getNodesByCategory,
  r6 as getPlugin,
  gz as getRegistryStats,
  qz as listAvailableNodes,
  jA as registerNodes,
  yz as resetEnhancedOpenEvolvePlugin,
  n6 as resetPlugin,
  fz as searchNodes,
  Pz as useEnhancedOpenEvolveConfig,
  Hz as validateAdversarialConfig,
  Gz as validateDecompositionConfig,
  Vz as validateEvolutionConfig,
  pz as validateNodeConfig
};
