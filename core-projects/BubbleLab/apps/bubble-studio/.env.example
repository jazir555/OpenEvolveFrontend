VITE_API_URL=http://localhost:3001
VITE_CLERK_PUBLISHABLE_KEY=

# Set to 'true' for dev mode (no authentication required)
# Set to 'false' or remove for production (requires Clerk key above)
VITE_DISABLE_AUTH=true

# PostHog Analytics Configuration
# ==================================
# PostHog is used to collect anonymous usage analytics to help improve Bubble Studio
#
# To set up PostHog:
# 1. Sign up at https://posthog.com (free tier available)
# 2. Create a new project
# 3. Get your Project API Key from: https://app.posthog.com/project/settings
# 4. Paste the API key below
#
# Data collection can be toggled on/off with VITE_ANALYTICS_ENABLED
# Set to 'false' to completely disable analytics data collection
VITE_POSTHOG_API_KEY=phc_xGmuGXWGaN68WjhY6T2gYaEefZ25gtUod2SdZodSB5k
VITE_POSTHOG_HOST=https://us.i.posthog.com
VITE_ANALYTICS_ENABLED=true

# Google Picker API Configuration
# ==================================
# Google Picker API is used to show a visual file picker for Google Drive files
#
# To set up Google Picker API:
# 1. Go to https://console.cloud.google.com
# 2. Create a new project or select existing
# 3. Enable "Google Picker API" in APIs & Services
# 4. Go to "Credentials" → "Create Credentials" → "API Key"
# 5. Copy the API key and paste below
#
# Note: This is a PUBLIC API key (safe to expose in frontend)
# It's used only for the Picker UI, not for accessing user data
VITE_GOOGLE_API_KEY=

# Google OAuth Client ID (for client-side OAuth with Picker)
# This should be the SAME OAuth client used by the backend
# Copy the value from GOOGLE_OAUTH_CLIENT_ID in the backend .env file
VITE_GOOGLE_OAUTH_CLIENT_ID=

# Debug Mode
# ==================================
# Enables extra developer features like copy buttons in console
# Defaults to true when running in dev mode (import.meta.env.DEV)
# Set to 'true' to enable in production builds
# VITE_DEBUG_MODE=true
