# API Server Dependencies for LoongFlow
#
# These are additional dependencies required for the FastAPI wrapper

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Data Validation
pydantic>=2.0.0

# YAML Configuration
pyyaml>=6.0

# HTTP Client (for health checks)
# Note: Using urllib.request from stdlib for simplicity in container,
# but requests can be used if needed
# requests>=2.31.0
