# =============================================================================
# Adaptive MDAP/MAKER Adapter Environment Configuration
# =============================================================================
# Federation Constitution - Law 5: Configuration Explicitness
# "No Magic Defaults. Every configurable value must be injected via Environment Variables."
#
# Copy this file to .env and configure your values.
# Service will crash immediately if required values are missing.
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED Configuration
# -----------------------------------------------------------------------------

# Request timeout in milliseconds
# Service CANNOT START without this value
ADAPTIVE_MDAP_TIMEOUT_MS=5000

# API Keys (REQUIRED for LLM-based operations)
# Service CANNOT START without at least one API key
DEEPSEEK_API_KEY=sk-your-deepseek-key-here
OPENAI_API_KEY=sk-your-openai-key-here

# -----------------------------------------------------------------------------
# OPTIONAL Configuration (with defaults)
# -----------------------------------------------------------------------------

# Maximum number of retry attempts for failed operations
# Default: 3
ADAPTIVE_MDAP_MAX_RETRIES=3

# Initial retry delay in milliseconds (exponential backoff)
# Default: 100
ADAPTIVE_MDAP_RETRY_DELAY_MS=100

# Number of failures before circuit breaker opens
# Default: 5
ADAPTIVE_MDAP_CIRCUIT_BREAKER_THRESHOLD=5

# Circuit breaker open timeout in milliseconds
# Default: 60000 (60 seconds)
ADAPTIVE_MDAP_CIRCUIT_BREAKER_TIMEOUT_MS=60000

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Default: INFO
ADAPTIVE_MDAP_LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# V2.0 Advanced Features Configuration
# -----------------------------------------------------------------------------

# Cache Configuration
# Default: 1000 items, 300 seconds TTL
ADAPTIVE_MDAP_CACHE_SIZE=1000
ADAPTIVE_MDAP_CACHE_TTL=300

# Async Processing Configuration
# Default: 10 concurrent operations
ADAPTIVE_MDAP_MAX_CONCURRENCY=10
ADAPTIVE_MDAP_ASYNC_TIMEOUT=5000

# Connection Pool Configuration
# Default: 20 max connections, 600 second idle timeout
ADAPTIVE_MDAP_POOL_MAX_SIZE=20
ADAPTIVE_MDAP_POOL_IDLE_TIMEOUT=600

# Feature Flags
# Enable/disable v2.0 features
# Default: true
ENABLE_ASYNC_ADAPTER=true
ENABLE_ADDITIONAL_SYSTEMS=true

# -----------------------------------------------------------------------------
# Optional: External Service Configuration
# -----------------------------------------------------------------------------

# MAKER API configuration (if using external MAKER service)
# MAKER_API_BASE_URL=http://localhost:8001
# MAKER_API_KEY=your-api-key-here

# Additional Systems (CrewAI, MCP, RAGBits, LeanAide, Z3)
# CREWAI_API_URL=http://localhost:8002
# MCP_TOOLS_URL=http://localhost:8003
# KNOWLEDGE_ENGINE_URL=http://localhost:8004
# LEANAIDE_URL=http://localhost:8005
# Z3_PROVER_URL=http://localhost:8006

# Monitoring (optional)
# PROMETHEUS_PORT=9090
# HEALTH_CHECK_PORT=8080
# DASHBOARD_PORT=8081
